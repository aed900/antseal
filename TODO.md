# antseal MVP — Adaptive TODO

**Source of truth for scope**: `MVP-SPEC.md` (Revision 2, 2026-07-27 — frozen; `SPEC-REVIEW.md` already folded in). When this list and the spec disagree, the spec wins; flag the conflict, don't silently diverge.
**Source of truth for status**: this file. Full per-task detail (Do / Accept / Notes / spec line refs) lives in `tasks/{P,F,C,G,S,A,R,U,Q}.md` — one file per domain, task IDs are stable and permanent.

Generated 2026-07-27 by a 9-agent decomposition of the spec + 2-agent adversarial coverage/consistency verification, then grown by the waves that execute it: **323 tasks** today (219 at generation; discovered work takes the next free ID in its domain and is never renumbered), **88 decisions D1–D88** of which **66 are resolved**, 6 milestone gates. Counts below are current, not original.

| Domain | Prefix | Scope | Tasks |
|---|---|---|---|
| Setup & toolchain | P | naming, repo, workspace, pins, CI skeleton, devnets | 20 |
| Formats | F | deterministic CBOR, manifest/bundle codecs, parser hardening | 49 |
| Crypto primitives | C | HKDF, AEAD, salted commitments, hybrid signatures | 29 |
| Content model | G | canonicalization, units, raw mirror, GGM fine tree | 29 |
| Storage | S | StorageBackend, payments, journal/resume, restore | 20 |
| Anchors | A | OTS, RFC 3161, receipt classification, verdict states | 29 |
| Reveal & verify | R | bundle build, verification pipeline, verifier web page | 51 |
| CLI & vault | U | command surface, vault, config, UX | 33 |
| Quality & release | Q | test infra, CI, threat model, docs, release | 62 |

---

## How to use this list (the adaptive protocol)

1. **Statuses** on the checkbox lines here (never in `tasks/*.md`):
   - `- [ ]` todo · `- [x]` done (append `✅ YYYY-MM-DD`) · append `⏳ WIP` while in progress · append `⛔ <blocker>` when blocked.
   - Never delete a task: strike it through (`~~…~~`) with a one-line reason and date. Renumbering is forbidden — IDs are referenced everywhere.
2. **Discovering new work**: take the next free number in the owning domain (e.g. `F18`), write the full entry (Milestone/Size/Deps/Spec/Do/Accept) in `tasks/<domain>.md`, add its checkbox line under the owning milestone here.
3. **Decisions**: when a Decision-register entry (below) is resolved, check it, record the outcome + date inline, and update the blocked tasks' detail entries. Decisions are as first-class as tasks — an unresolved decision past its due milestone is a blocker.
4. **Milestone gates**: a milestone is done only when its gate checklist passes — the gate, not the task count, is the exit criterion. Run the gate, record evidence, then move the Current-focus block forward.
5. **Re-verification triggers** (adaptive part — these mutate the list):
   - any `ant-core`/`self_encryption` bump → re-run S1 survey + S9 pinning + devnet E2E; adapter changes confined per S20.
   - any RUSTSEC advisory on a pinned crypto crate → P13/Q10 lane red → decision entry + possible new tasks.
   - upstream weekly check (P19/Q36) findings → new tasks under the affected domain.
   - any spec revision → re-derive the affected section's tasks before coding against it.
6. **Milestone fidelity**: spec-assigned milestones are normative. Where a milestone was inferred (mostly U; noted per task), it may move with a recorded reason; spec-assigned ones may not.
7. **Deps discipline**: Deps lines in `tasks/*.md` list true predecessors only; consumer/interface mentions belong in Notes or carry an explicit "consumer, not a dep" annotation. Where prose and this file disagree on ordering, **the `after:` lists on the checkbox lines here are the authoritative build order** (verified acyclic).

---

## Current focus

> **Phase: M0 COMPLETE — the `format-v1-freeze` tag exists.** Wave 7 executed 2026-07-28 (planning-first: **2 planning agents → 7 parallel worktree lanes → rolling integration → the gate**): **F4, F18, F20, F22, F23, F24, F25, F30, F33 ✅ · G24 ✅ · R33, R36, R37, R41, R42 ✅ · C27 ✅ · Q38, Q40, Q41, Q43, Q49–Q53, Q57, Q58 ✅ · Q14 ✅** — 28 tasks, 4 decisions frozen, 21 tasks discovered.
> **The gate, all green at the tag**: fmt · clippy `-D warnings` · **1221 tests** (1113 at wave-6 close) · wasm32 build + execution · native↔WASM bit-match **byte-identical over 12 vectors** · vector freeze + self-test · **wire-registry freeze digest (new, Q50)** · tamper matrix **COMPLETE (0 pending)** · cross-check **0 discrepancies, report dated to the freeze commit** · traceability **all 16 M0 rows `covered`** · ci-shell · ci-lanes. **[2026-07-31 correction (Q66): the traceability *lane* was in fact red at the tag** — not its substance (all 16 rows read `covered` throughout) but its self-test, whose fixtures still expected the un-ticked rows Q14's own execution ticked; `local-gate.sh` did not run that lane, so the gate verdict above was issued without it. Fixed at Q66; every other claimed lane re-verified green at HEAD by the 2026-07-31 five-agent audit.]
> **Frozen at Q14**: the CBOR profile + encoder pin, the v1 wire registry, domain tags, the 8 HKDF labels + info encoding, the signature context string, `sig_policy` ids (**D17**), Unicode 17.0.0, the GGM addressing + D83 leaf payload, the **191-code** error universe (**D30**), report **v1** (**D29**), and the 12-file vector set.
> **The gate audit found the gate itself unrunnable** (**Q49**): ticking Q14's normative rows broke Q14's own drift lint, because rows N1/N2 sit inside the byte-compared `FREEZE-BOUNDARY` block. Proven on a scratch tree, not argued. It also found **D30's freeze had no mechanism at all** — 53 of 191 codes were renameable with a fully green suite (**Q52**), and **the wire registry had no freeze digest** (**Q50**).
> **D8** resolved the last open M0 decision and **removed wire surface**: `tsa_anchor.source` leaves v1 (zero producers, zero consumers), and `block_number` was **under-determined** because multi-tx payments span blocks.
> **Three integration defects existed only on the merged tree**, all from D8's deletion landing in a different lane from its three users: source code, an assertion's grep target, and committed fixture bytes. A fourth was avoided — lanes C and E both moved `bundle.json`, and the merged file's digest matched **neither** lane's `FROZEN.sha256` entry; regenerating from the emitter reproduced git's merge byte-for-byte.
> **Next**: **M1 — Storage**. Its gate is S17/S18/S19 (devnet E2E, kill/resume incl. real SIGKILL, clean-tree restore). Post-freeze M0 residue is tracked but **not** blocking: F27–F29, F31/F32, F34–F39, F42–F44, F49–F52, G18/G25–G29, C25/C26/C29, R35/R38–R40/R43/R51/R52, R55–R58, Q42/Q54–Q56/Q62–Q64/Q68 (F53 and Q69 are M1; R55 is parked on Q68; R58 decides by M3). **[2026-08-01: the review's six tasks — F40, F41, C28, Q67, R53, R54 — are all DONE; F40 no longer needs its apart-listing.]**
> **[2026-07-31] Adversarial code review — the audit's missing piece, now run** (`docs/reviews/2026-07-31-adversarial-code-review.md`; 7 fresh-eyes lanes + 8 refute-first verifiers, 15 agents, **green suites ruled inadmissible as evidence**, no cargo run). **33 findings, zero critical, 8 confirmed.** The crypto core came back strong: `ggm-cover` machine-checked against an independent Python reimplementation (cover completeness *and* secrecy exhaustive for n ≤ 25–33, `fine_root` reproduced end-to-end); `aead-kdf` clean above `note` (nonce single-use is structural, all 8 HKDF vectors recomputed); `hybrid-sig` found no downgrade/masking/confusion path and read the pinned dalek and ml-dsa sources directly; `cbor-strict` clean above `note`. **The one real defect is F40**: D23 clause 3 ("at most one raw mirror per file") was decided, was repeated in prose as though enforced, and is implemented by **nothing** — so a two-mirror file verifies clean with only the first mirror bound by rows 9–10, and the report names the *unchecked* second one. Three lanes found it independently; re-verified by hand. Every fixture and the proptest strategy generate 0 or 1 mirrors, which is why 1221 tests never saw it. **The pattern is the more important result**: excluding the mirror cluster, the dominant defect class here is *recorded claims the code does not implement* (F41, C28, Q67, and eight `note`-level siblings) — each false claim disarms the future review that would catch its bug. For a project whose review culture leans this hard on its own records, that is the class to watch. Not re-run here and still unexamined: the remote-only macOS/Windows legs, and long fuzz campaigns. **[2026-08-01: REMEDIATED — all six tasks landed in a 3-wave staged fan-out (F40+C28+Q67 → F41+R53 → R54), every lane-reported-unverified finding re-verified before action (U1/U3/U4/U11/U12 all confirmed; U4 understated — six constructors, not four). Universe 191 → 194; zero frozen bytes moved (proven by 21-report byte-identical regeneration + 12-vector bit-match); at-cap verify 29 s → 2.6 s debug; stage-5-last re-verified correct and untouched. Gate at the wave head `1e9db93`: 1243 tests + full local gate (incl. traceability + cross-check) green. Eleven follow-ups recorded: F51–F53, C29, G29, Q68/Q69, R55–R58.]**
> **Maintainer actions** (all external, none taken): (1) **P2** `cargo login` + `scripts/reserve-crates.sh --execute`; (2) **P3** register antseal.org + antseal.dev; (3) **push — DONE 2026-08-01**: the 14-commit backlog (audit-era + the remediation wave) pushed with the user's express in-the-moment consent, fast-forward `d3345e1..62ea9d4`; **CI run 30697242826 GREEN on all 19 contexts at the head** — including `traceability`, which was red at `d3345e1` and cleared by Q66 exactly as predicted, so the freeze-push CI gap is closed with a recorded verdict at last. (The failure-styled annotations in that run's log are the lanes' planted-fault self-tests — e.g. `planted::zzz_this_one_is_the_planted_fault` — guards proving they fire, not failures.) The 2026-07-31 "remote cannot be re-read — no credentials" parenthetical was stale: `gh` is authed as aed900 (keyring; gist/read:org/repo/workflow scopes), and the scheduled fuzz-nightly lane has been green on the remote throughout. Future pushes still need express in-the-moment consent per the standing rule; (4) **branch protection is BLOCKED BY PLAN, not pending** — verified 2026-07-28: both the classic API and rulesets return 403 *"Upgrade to GitHub Pro or make this repository public"* on a private repo on GitHub Free. Three options recorded in `docs/ci-verification.md`; note that *making the repo public* is one of them and **triggers Q65** (publish-scope + pre-public scrub) first. The payload, if it ever applies, is **19** contexts from 17 jobs — the runbook's own copy lists 13; (5) optional courtesy: file docs/naming/upstream-blessing-request.md.

## Critical path

P1 → P4/P5/P6/P8 → **M0**: [P10+F1 CBOR pin] → F2→F3→F4/F5→F6→F7 · C11 probe → C12/C13→C14 · G1→G2 · C1/C2→C6 · G8→G9→G11→G12→G13 → R1→R5 → vectors (F12/F13, C16, G15, R9) → F14/Q11 independent cross-check → **Q14 format-v1 freeze** → **M1**: S2→S3/S4/S6 · U5→U6→U9 · S12/U13 pipeline → S17/S18/S19 E2E → **M2**: A5→A8→A9 · A11→A13→A14 · A18 → A21/A22 + A25 smoke · U22 gate → **M3**: R13→R16 · R17→R18 · R22→R23→R25→R26 · R27 → **M4**: Q30→Q31→Q32→Q33→**Q34 release**.

The M0 freeze (Q14) is the single most consequential gate: everything frozen there (formats, tags, labels, Unicode version, context string, sig-policy IDs) is permanent once the first real seal exists.

---

## Pre-M0 — naming, repo, toolchain (8 tasks)

Gate: name decided + propagation checklist owned; crates.io names reserved; verifier domain registered; repo pushed; workspace scaffold + toolchain pin + pin policy committed; CI skeleton green (fmt/clippy/test/wasm32).

- [x] **P1** (M) Finalize the product name — upstream blessing for `ant-` prefix or non-ant rename; propagation checklist (context string, format strings, binary/dir, crates, domain) ✅ 2026-07-27 — **`antseal` maintainer-confirmed FINAL** (third path: no upstream blessing — deviation + residual risk recorded in D1 doc; blessing request now optional courtesy; fallback retired); propagation checklist committed with owners (docs/naming/propagation-checklist.md)
- [ ] **P2** (S) Reserve crates.io names (placeholder publishes; re-verify availability) — after P1 ⛔ `cargo login` (maintainer) then `scripts/reserve-crates.sh --execute` — D1-final cleared 2026-07-27; availability re-verified same day ~18:58Z: all 5 names FREE + dry-run green (kit: docs/naming/P2-crates-reservation-runbook.md). **Execute promptly — availability decays**
- [ ] **P3** (S) Check and register the verifier domain — after P1 ⛔ registrar/payment (maintainer) — D1-final cleared 2026-07-27; antseal.org + antseal.dev re-verified FREE same day ~18:58Z (RDAP 404); plan: docs/naming/P3-domain-registration.md. **Register promptly — availability decays**
- [x] **P4** (S) Git init, initial commit (specs), remote hosting + platform decision ✅ 2026-07-27
- [x] **P5** (M) Cargo workspace scaffold exactly per Architecture tree (4 crates + verifier-web + testdata; lints; lockfile) — after P4 ✅ 2026-07-27
- [x] **P6** (S) Pin Rust toolchain, wasm32 target, edition/MSRV — after P4 ✅ 2026-07-27
- [x] **P7** (S) Dependency pin-governance policy + lockfile discipline (`--locked` CI; bump = deliberate event) — after P5 ✅ 2026-07-27
- [ ] **P8** (M) CI skeleton: fmt/clippy/test + wasm32 build lane + core dep-graph assertion — after P4,P5,P6 ⏳ WIP — workflow committed; scope granted + pushed 2026-07-27 (maintainer device flow); **first remote run GREEN — all 14 contexts** (run 30309407509; recorded in docs/ci-verification.md) — remaining: wasm-guard red-lane probe (maintainer deferred; procedure in the runbook)

## M0 — Core formats & crypto (188 tasks) — ends at the **format-v1 freeze**

Gate = **Q14 checklist**: Definitions frozen (CBOR profile+pin, domain tags, id encodings, HKDF info encoding, Unicode version, signature context string); all M0 golden vectors committed + frozen (incl. empty-anchor + unbalanced n=6); independent cross-check (Q11) clean; M0 tamper registry (Q8) fully implemented; HKDF pairwise-distinctness test green; WASM bit-match green; ML-DSA probe decision recorded; Security-assumptions sign-off; traceability M0 rows filled; annotated `format-v1-freeze` tag.

### Pins & probes (P)
- [x] **P9** (S) Re-verify + land `ant-core = "=0.5.0"` pin at M0 start (spec-mandated) ✅ 2026-07-27 — KEEP 0.5.0 (crates.io newest, not yanked, upstream HEAD ≡ tag); self_encryption locks 0.36.0; pin = workspace declaration only, lock unchanged (docs/upstream/P9-ant-core-reverification.md)
- [x] **P10** (M) Select + exact-pin the deterministic-CBOR encoder crate (joint with F1) ✅ 2026-07-27 — `minicbor = "=2.3.0"` (D7); cross-check nominee `cbor2 ==6.1.3` (D12)
- [x] **P11** (S) Decide + pin `ed25519-dalek` (2.x vs 3.0.0) — with C11 ✅ 2026-07-27 — **`=3.0.0`** (D13): strict-verify semantics probe-identical to 2.2.0; single RustCrypto generation (signature 3 / sha2 0.11 / getrandom 0.4) shared with ml-dsa; declaration-only until C12
- [x] **P12** (S) Pin `ml-dsa =0.1.1` + `fips204 =0.4.6` with RUSTSEC advisory assessment ✅ 2026-07-27 — both pins landed (declaration-only until C13); **3** advisories assessed (spec knew 2), all patched in 0.1.1; only 1 carries a RUSTSEC ID → GHSA/osv.dev sweep mandated (D19 amendment)
- [ ] **P13** (S) RUSTSEC advisory tracking lane (cargo-deny/audit, weekly schedule) ⏳ WIP — local complete 2026-07-27 (D19): deny.toml (advisories/bans/sources active; licenses stubbed → Q29) + `audit-deny` per-PR lane + weekly advisory-cron.yml, cargo-deny =0.19.8 pinned; `audit-deny` GREEN on the first remote run 2026-07-27; weekly cron armed (fires Mon 06:17 UTC on the default branch) — remaining: first scheduled-run evidence + D19 red-lane demo (maintainer deferred)
- [x] **P14** (M) WASM toolchain: getrandom js/wasm_js recipe, wasm-pack/wasm-bindgen pins, wasm32 test execution ✅ 2026-07-28 — wasm32 tests **execute** (286 of 311 unit tests) via a zero-import libtest binary under plain `WebAssembly.instantiate` in node, wired as cargo's `runner`; chosen over wasm-bindgen-test because it runs the real `#[test]` fns, needs no wasm-bindgen pin, and **leaves D18 free** (no crate-type change, no JS surface, nothing committed). An in-memory **execution witness** closes the "all passed" vs "zero ran" ambiguity (no stdio on this target). `core-dep-graph` intact by construction: the new `test-vectors` feature activates **zero** optional deps (cargo-tree diff byte-identical). No new third-party dependency. +1 required context (`wasm32-core-tests`, 14 → 15)

### Formats & parser hardening (F)
- [x] **F1** (M) Evaluate + pin the CBOR crate against RFC 8949 §4.2.1 + strict-decode feasibility (joint P10) ✅ 2026-07-27 — every line-73 rejection class implementable on minicbor's public probes (13 live eval tests: crates/antseal-core/tests/cbor_pin_eval.rs); derive off, F5–F9 manual impls
- [x] **F2** (M) Canonical CBOR encode layer (shortest forms, definite lengths, sorted integer keys) — after F1 ✅ 2026-07-27 — closure-scoped `CanonicalEncoder` (no API path to floats/indefinite/unsorted); boundary-pin + sorted-key-equivalence tests
- [x] **F3** (L) Strict canonical decode layer — hard-reject every non-canonical class, distinct errors, outer+inner layers — after F1,F2 ✅ 2026-07-27 — 13-variant/15-code `DecodeError` (dup vs disorder distinct), truncation sweep, per-class × outer/inner tests, native↔wasm error-class parity (u64 pre-check); VerifyError `Codec` arm landed (R1-rider F leg); frozen 8-step error precedence documented
- [x] **F4** (M) Frozen v1 wire-format registry: every map key, type, presence rule, byte length, reserved slots — with C/G/A/S inputs ✅ 2026-07-28 — **the v1 wire registry is FROZEN** (implements D8). `tsa_anchor.source` removed from v1; `block_number` given a total definition; D82 added (it was absent entirely); D74/D75/D77/D83's rulings applied; §13 rewritten from an open feed into a closed record of all ten items. 33 sections moved, 30 of them substantive — exactly D8's prediction. 156 JSON markers → `frozen-v1`, status column deleted from 14 tables (84 cells), `open_decisions` emptied. **Both armed tripwires fired as designed**, verified by running the pre-flip test against the flipped mirror. Of D8's C1–C10 assertion set, C5/C6/C10 and **D** were newly written — D mattering most, because until now **nothing in the tree parsed the `.md` at all**, which is why the JSON mirror had drifted from it
- [x] **F5** (M) Manifest body schema types + parse-time presence/shape validation (sig_policy, unit_commit-iff-not-covered, canon_commit/fine_root rules) — after F3,F4 ✅ 2026-07-28 — `antseal_core::manifest` (body/registry/sigmap/ids/fixtures); every reject class a distinct `manifest-` code with a pairwise-distinctness meta-test + a banned-prefix test (schema codes never borrow crypto-/cbor-/content-/verify-); reject+accept matrices driven by an **independent hand-rolled CBOR writer** so tests can build bytes the schema types could never construct
- [x] **F6** (M) Manifest envelope codec `{body: bstr, signatures}` — body-as-received, verifiers never re-encode — after F2,F3,F5 ✅ 2026-07-28 — zero-copy `body_bytes()`, outer/inner F3 error classes distinguishable, no public re-encode-for-verify path, enumerable signatures container (`SigAlgMap`) so C14/R5 can detect present-but-unlisted
- [x] **F7** (S) `work_id` + `anchor_digest` as two distinct functions/newtypes; `manifest_hash` identifier banned — after F6 ✅ 2026-07-28 — distinct newtypes, lowercase-hex Display, no-domain-tag rationale machine-asserted (`no_canonical_v1_item_can_start_with_a_domain_tag_byte`); repo-wide identifier-ban grep test landed. **Gap carried to F12**: the committed work_id/anchor_digest golden-vector artifact lands with the manifest vector layout (F12), not here
- [x] **F8** (L) `.sealproof` bundle schema types + parse-time shape validation (every line-112–114 content item; v1.1 slots reserved) — after F3,F4 ✅ 2026-07-28 — `antseal_core::bundle`, layer 1 of the three-layer decode; **32 `bundle-` codes** exercised from real bytes by a 32-row reject matrix built with an independent hand-rolled CBOR writer. D28's derived predicate is expressed by **absence**: `FullReveal` carries material, not a claim, and `is_full_reveal`/`reveal_mode`/`reveal_shape` joined F7's repo-wide identifier ban so no future edit can reintroduce a declared shape. Also implemented the code⟷registry cross-check that `manifest/registry.rs`'s docs had been claiming without a test behind it
- [x] **F9** (M) Bundle codec with three-layer nested strict decoding, deterministic encode — after F6,F8 ✅ 2026-07-28 — `SealProof`/`ProofLayer` codec + deterministic encode over F8's schema; nesting depth confirmed at 5 for the bundle chain. `bundle-unsupported-format-version` raised inline and documented as F10's to re-home behind dispatch; `decode_section` deliberately carries no `with_capacity`, naming F11 as its replacement (D10 still unresolved, no cap constants minted)
- [x] **F10** (M) Version fields, per-version decode dispatch, reserved-slot mechanics, per-version vector layout — after F4,F5,F8,F9 ✅ 2026-07-28 — discriminant read **first** and dispatched through a `version -> decoder` table; the v1 path is fenced behind `format::V1`, an admission witness with no public constructor whose *type* is what stops a v2 row from ever entering the v1 table. F9's inline `bundle-unsupported-format-version` re-homed behind dispatch; **zero codes minted or renamed**. Precedence is the load-bearing test: a v2 artifact that *also* carries a reserved key, an unknown key, a missing field or a non-canonical int still reports the version and nothing else — with a v1 control per mutation — while a *missing* discriminant stays `*-missing-key`. Reserved slots swept exhaustively off `MapId::ALL`/`BundleMapId::ALL`. New `testdata/vectors/v1/INDEX.json` roster, mutually enforcing with Q6's freeze. **Discovered F18** — none of its four codes has a tamper row
- [x] **F11** (M) Parser resource caps + allocation bounding (size/count/depth; clamp to remaining input) — after F3,F5,F8,F9 ✅ 2026-07-28 — D10's 19 frozen caps + the universal clamp + a work-global unit budget in `codec::caps`, enforced at all 8 sites ahead of every AEAD/hash/signature; **18 codes minted**; registry §11 + JSON mirror with a both-directions code==registry test; 25 tests (at-cap/cap+1 × 19, a counting-allocator test proven able to fail). Two derived caps rather than guessed: cover entries and path nodes follow `2·⌈log₂ n⌉` with `size` a uint, so `d ≤ 64` bounds the honest max at 128. **Reported 3 prose errors in D10** (amended there), incl. the ruling that a >256 MiB v2 bundle reports `bundle-too-large` under a v1 cap — cheapest rejection wins. Surfaced F22/F23
- [x] **F12** (M) Manifest golden vectors (per-version, native/WASM bit-match) — after F6,F7,F10 ✅ 2026-07-28 — 6 works incl. both `sig_policy` shapes as a one-field diff of an identical work; each commits canonical envelope bytes, `work_id`, `anchor_digest`, a two-layer diagnostic sidecar and a schema-layer view, re-checked against the **committed** bytes. **Closes F7's deferred `work_id`/`anchor_digest` artifact**, and states F7's separation property against real manifests: a mutated signature container moves `anchor_digest` and leaves `work_id` unchanged. Clears the `manifest-encode-decode` must-exist obligation. Surfaced F19 (F14's sidecar is in-file, and its rendering table is normative)
- [x] **F13** (M) Bundle golden vectors incl. the empty-anchor (UNANCHORED) vector — after F9,F10,F12 ✅ 2026-07-28 — 8 bundles with three-layer sidecars, incl. **the empty-anchor (UNANCHORED) milestone artifact**; the executor also asserts the three decode layers, `encode_bundle` byte-identity, the zero-copy `anchor_digest` preimage seam, and that **`verify_bundle` accepts every committed bundle**. Found and closed a real gap in R6 rather than forking: `AnchorSet` could not populate the OTS upgrade group, TSA intermediates or any receipt, so `AnchorSet::EveryKind { receipt }` landed **inside** R6 — one definition of a valid bundle. **Clears `bundle-empty-anchor`, the last must-exist obligation: v1's pending set is empty and Q14's vector gate condition is met.** Surfaced F20/F21
- [x] **F14** (M) Independent-CBOR cross-check artifact (second implementation; freeze gate input) — after F4,F12,F13 ✅ 2026-07-28 — Independent cross-check to D31: **225 checks, 14 cases, ZERO disagreements** with the committed bytes. `cbor2 ==6.1.3` **decodes only**; the RFC 8949 §4.2.1 encoder (real bytewise key rule) and the canonicality judgement are ours, retiring D12's ordering caveat instead of documenting it. T0 anchor: 33 RFC Appendix A examples. Also confirmed 13 F15 fixtures non-canonical *for the reason they claim* and 5 schema fixtures as valid CBOR. Generic by construction — scope is "the case carries a `diagnostic`", the envelope is found structurally via the key-0 rule, nothing hard-coded, so R32's and D83's re-emissions needed no edit here. Found **D12's independence rationale factually wrong** (cbor2 6.x is Rust/PyO3, no pure-Python fallback); what holds is *zero shared code* — minicbor has no deps at all. D7 amended. Surfaced F31, F32
- [x] **F15** (M) Format-level tamper fixtures: 4 non-canonical-body rows + oversized/deep + full line-73 class coverage — after F3–F12 ✅ 2026-07-28 — 20 fixtures + `FIXTURES.json` in `testdata/tamper/format/`, 7 rows; **no code minted**. **Drives the M0 tamper matrix to COMPLETE (0 pending).** Central finding: because F6/F9 surface a wrapped codec rejection's inner code *unchanged* at every layer (contract §2), a duplicate map key in the body, envelope, bundle map and embedded manifest is **four fixtures and one code** — minting per-layer codes would fork one taxonomy into four and freeze "which layer" into every name forever, so fixtures pin the `(code, layer)` pair and a row is registered only where the code is unclaimed. Oversized solved without a 256 MiB file via a **synthesized** fixture recipe (real bundle padded one byte past the cap; the O(1) length check reads nothing past the prefix), with a test forbidding any committed format fixture over 64 KiB. Over-deep is a direct-call row per D10 §6. Surfaced F24/F25/F26
- [x] **F16** (M) Codec property tests: round-trip, determinism, canonicality, mutation classes — after F5–F11 ✅ 2026-07-28 — 12 properties over schema-valid manifests **and** bundles, green at `PROPTEST_CASES=2000`; round-trip / determinism / encoder-passes-decoder / re-encode byte-identity + 12 mutation classes × 3 layers with a **deterministic** coverage assertion (not corpus-dependent). Generation runs against `strategies::GenCaps`, a test-only reduced-cap profile that **cannot leak into production structurally** (`codec::caps` does not know the type exists; no setter, cfg or global) with a standalone test pinning every field ≤ its production cap. Proven able to fail: mis-stating one class's code turns all 4 mutation tests red with shrinking
- [x] **F17** (M) cargo-fuzz targets: manifest decode, bundle decode, round-trip — after F6,F9,F11–F15 ✅ 2026-07-28 — three targets (`manifest_decode`/`bundle_decode`/`codec_round_trip`) on one engine, exercised by the ordinary suite every build; 171 drift-checked seeds; 400 000 iterations clean. **The fuzz input IS the document** (R10 reads entropy because it must get *past* the decoder; F17's job is the opposite), so libFuzzer mutates the wire format and an iteration costs one decode with no fixture build. **Load-bearing finding: it disproved `parser_caps_alloc.rs`'s stated consequence** — the clamp counts *elements*, not bytes, so a 152 B manifest drives a 4 704 B allocation and a 16 MiB one ~512 MiB before one map entry is read (32× amplification, worst in the WASM page). No verdict moves, which is why it went unnoticed → **F30**
- [x] **F18** (S) Tamper rows + a property for F10's version/reserved-slot surfaces (4 codes, no rows; Q8 cannot flag them — they are project-added, and line 168 predates the reserved-slot mechanism) — after F10,F15 ✅ 2026-07-28 — **two rows, not four.** Two of the four named codes have had rows since F15, and adding them would claim an outcome another row owns: registry §7.15 makes a *named* reserved slot take the ordinary reserved error, so a reserved key in the body, a unit entry and the receipt are **one observable**. Each row is a one-byte `format_version` bump on a golden vector's own bytes, proven to differ in exactly one position. Plus the universal peek/decode agreement property F10 proved only case-wise
- [x] **F19** (S) Hand F14 the diagnostic-sidecar contract: it is **in-file** at `expect.cases[].diagnostic` (a sibling file cannot exist — Q4 executes every `.json` under `v<n>/` as a vector) and its rendering is a normative table — after F12,F13 ✅ 2026-07-28 — contract in `docs/testing/cbor-cross-check.md` (a *new* doc, because R32 and D83 were re-emitting into the vector README the same wave): the sidecar is in-file at `expect.cases[].diagnostic`, the rendering table is normative, and map entries are **ordered pairs** — a checker that sorts silently weakens to a set comparison
- [x] **F20** (S) Tamper rows for the anchor-artifact schema surface F13 made constructible (upgrade group, intermediates, receipt, `chain_inputs`) — after F13,F15; coordinate with A21 ✅ 2026-07-28 — five anchor-schema rows, zero codes minted. **Its Accept was unachievable as written** ('typed construction from R6, never byte-patching'): F8 had since made every one of these states *unrepresentable* — `OtsUpgrade` holds all three D79 fields in one struct, `ReceiptRecord` refuses an empty `tx_hashes`, `AnchorStatus` is closed. The premise that those are the only two options was the error; the third is `tests/bundle_wire`, whose own docs name these shapes as its reason for existing. Provenance restored as an assertion that decodes both and compares section by section. Two of the seven enumerated mutations are one observable each, and a third — an empty `intermediates` entry — is **accepted**, correct layering but an unasserted silence until now
- [x] **F21** (S) Decide how the bit-match harness carries the vector tree before v2 doubles it (`build.rs` embeds all of `testdata/vectors/`; ~98 kB → ~520 kB this wave, ×per-version forever) — after F12,F13 ✅ 2026-07-28 — **D87** implemented: `include_bytes!` kept with `MAX_EMBEDDED_BYTES_PER_VERSION = 2 MiB` per `v<n>/`, enforced by a `build.rs` **panic** plus a testable second leg. Overturned F21's framing by measurement — carriage is 0.73 % of the lane and 2.32 % of the artifact; *executing* the vectors is 90.1 %. Re-measurement matched D87 §1 within noise; ceiling proven able to fail at **build** time. v1 uses 28.1 % of budget. Surfaced F28, F29
- [x] **F22** (S) Tamper rows for the 18 D10 cap codes + D77's mirror-only row (re-homed from F15: the only route is a byte mutation of a nested unit's `kind`, which needs F25) — after F11,F15,F25 ✅ 2026-07-28 — 5 rows + 13 named non-rows account for all nineteen D10/D77 codes. The representative unit is the **error variant**, not the discriminant, because `parser_caps.rs` already drives all nineteen at-cap/cap+1 through the real decode surface. D77's mirror-only row landed through F25: one byte, `unit_kind` 0→1, five levels down. **Its scope statement miscounted** — 'the other sixteen' three times over a seventeen-item enumeration
- [x] **F23** (S) Reverse coverage: every `bundle-`/`manifest-` code has a row or a named owner (the F-side twin of R7's check; F11 landing 18 such codes at once is the proof the direction is unguarded) — after F15 ✅ 2026-07-28 — the F-side twin of R7's check, and it earned itself immediately: **88 of the 95 `bundle-`/`manifest-` codes were unaccounted on the first run**. Structured as data (one `CoverageDomain` per family) so F24's family is one more const. Needs a third accounting route R7's did not — `claimed_in_integration_target`, validated from the other side so a claim cannot outlive its row. Surfaced **F39**: 65 schema-shape codes with no owner at all
- [x] **F24** (S) Reverse coverage for the `cbor-` family: 6 of F3's 15 codes have neither a row nor a named owner, falling between R7's and F23's checks *because* both delegate to them — after F15 ✅ 2026-07-28 — 5 rows + 1 named non-row + the `cbor-`-family sweep. **`cbor-int-out-of-range` is not 'representative-only' as the entry assumed — it is UNREACHABLE**: no v1 schema slot decodes through `i64()`, so it is a named non-row with the argument re-derived every run
- [x] **F25** (S) A span-locating splice primitive, and the fixtures blocked without one — after F15 ✅ 2026-07-28 — a span-locating cursor built entirely on the F3 decoder's public surface (`peek_kind` + typed reads); no head byte parsed, no shortest-form rule restated, so it cannot disagree with `check_canonical` about where an item ends — asserted over **every item of every committed golden vector** (16 documents, 500+ items). **Its own premise was false**: 'recursion is already depth-bounded' — the bound lives in a *private* method, so nothing on the surface the cursor is built from carries it; the cursor now carries its own
- [x] **F26** (S) Decide whether the decode **layer** reaches the user-facing verdict, and reconcile the two asymmetric `layer()` accessors (a bundle schema rejection reports `Some(Bundle)`, a manifest one reports `None`) — after F15 ✅ 2026-07-28 — **D86** implemented: the layer is **total on both accessors** (no `Option`), manifest side derived from a new `ManifestError::map() -> MapId`. Overturned F26's own lean (harmonise *downward*). The decode layer **never enters the report** — `layer` occurs zero times across all 21 pinned report strings and D27 §4 makes the route structurally unreachable, so F26's "expensive after Q14" premise never existed. All 28 rows of D86's table were correct, re-derived from `registry-v1.json`. Fixture table moved exactly three `expected.layer` values, zero digest/code/row. Also pinned D86 §4.5's rule (null **iff** the surface is not layered), which nothing asserted. Surfaced F33, F34
- [ ] **F27** (S) Assert the fixture table's discriminator is what the contract claims — the "pairwise informative" claim is documented in three places and asserted nowhere, and even after D86 the real discriminator is the triple `(surface, code, layer)` — after F26,F15
- [ ] **F28** (S) Bound the bit-match lane's **execution** cost — 90.1 % of the lane, doubles at v2, CI pays it twice, and nothing measures it while the 2 % now has a ceiling — after F21
- [ ] **F29** (S) The bit-match covers `debug` codegen only — no `[profile.*]`, no `--release` anywhere in CI, yet the M3 page ships a release wasm-bindgen build, so the codegen path a third party runs has never been compared — after F21
- [x] **F30** (S) Make the F11 clamp **element-aware**, and correct the claim it is documented under — the clamp counts elements not bytes, so a 16 MiB manifest can drive a ~512 MiB reservation before one map entry is read (32x, worst in the WASM page); no verdict moves, which is why it stood — after F11,F17 ✅ 2026-07-28 — D10 §4's rule was right; **its two operands were in different units**. `claimed_length` counts elements, `remaining_input` counts bytes, and `with_capacity` multiplies by `size_of::<T>()`. Now generic in the element type. Measured red-before-green: the `files` site amplifies **157×** (3 145 728 B from a 20 005 B input), worse than the recorded 32× — fuzzing simply reached the sigmap case first. Also found `parser_caps_alloc.rs` **structurally vacuous**: every fixture ended at its container head, so `remaining()` was **zero** at the allocation, and a mis-united clamp is indistinguishable from a correct one when one operand is zero
- [ ] **F31** (S) Make "commits format CBOR" a registry fact so no kind escapes the cross-check — a kind with format CBOR and no sidecar is silently out of scope, and absence is indistinguishable from "commits no CBOR" — after F14,F12,F13
- [ ] **F32** (S) Decide how `crosscheck_cbor.py` is carried across format versions **before v2 exists** — the day `v2/` is created the ~700-line checker is copied, and two divergent copies of the artifact whose whole job is to be a second implementation is the drift hazard its own guard exists to prevent — after F14
- [x] **F33** (S) The registry's field **names** are load-bearing and wholly unasserted — the §7.6.1 checked-absence guard is a name-based ban list that a rename silently evades — after F4,F26 ✅ 2026-07-28 — the §7.6.1 checked-absence guard was a name-based ban list a rename silently evaded; field **names** are now asserted, not just key numbers (D8's assertion C6), landed inside the registry freeze test
- [ ] **F34** (S) After D86 the fixture discriminator is still too coarse for the three body sub-maps — a reserved key in a unit entry and one in the body remain one observable, the same defect one level down — after F27,F15,F26
- [ ] **F35** (S) Make a changed freeze digest disclose **how much** changed — R32 moved 21 cases and the manifest moved one hex line; post-freeze that single line is the reviewer's entire signal — after Q6,R32
- [ ] **F36** (S) Per-layer `cbor-truncated` fixtures — F25's third consumer; also carries the finding that the cross-check's schema branch inspects the outer layer only, never embedded `bstr` layers — after F25,F24
- [ ] **F37** (S) Merge R7's, F23's and F24's three reverse-coverage sweeps into one mechanism — after F23,F24,R7
- [ ] **F38** (S) Provenance check for the every-anchor-kind fixture base — the two committed bases have one, this one does not, and it is now load-bearing for two fixtures — after F20,F22
- [ ] **F39** (M) Decide the row policy for the **65** unrowed `manifest-`/`bundle-` schema-shape codes — tagged by family, because the call is per family; F23's escape hatch names this as their owner — after F23
- [x] **F40** (S) **Enforce D23 clause 3 — "at most one raw mirror per file", decided and implemented by nothing.** `FileEntry::new` never counts mirrors, tiling exempts them by `kind`, so a two-mirror file verifies clean with only one bound by rows 9–10: a second contradictory "original", signed and anchored, and it is the one the report names. Exactly what MVP-SPEC.md line 121 exists to prevent; invisible to the suites because every fixture generates 0 or 1 mirrors. New `manifest-` code is legal (D30 §3 append-only) — after F5 · **2026-07-31 adversarial review, findings 1–3 (high)** ✅ 2026-08-01 — count rule in `FileEntry::new`, positioned after D77's normal-units check and before the coverage loop with the choice argued at the site (both decode and direct construction funnel through it); `manifest-multiple-raw-mirrors` minted, snapshot re-blessed via the union-only bless (universe 194 on the merged tree with C28's parallel append); tamper row + committed fixture `body-second-raw-mirror` + rejecting-direction ≥2-mirror proptest generator in new slice `tamper_rows_mirror`; red-direction proof and planted-fault both actually performed (decode + direct construction accepted two mirrors pre-rule; removing the rule turns `reject_a_file_with_two_raw_mirrors` red); D23's over-claiming Consequences parenthetical corrected at source. Registry §7.3 cannot carry the rule until the next sanctioned revision — recorded as a follow-up task
- [x] **F41** (M) Sweep the documented structural guarantees the code does not implement: `ManifestBodyV1` documented "not `Clone`, on purpose" in four places while deriving `Clone`; "constructible == decodable" false (D10 caps live only in decode); `sig_policy`'s no-cap justification unsound; `ids.rs`'s domain-tag universal false. Each doc actively disarms the review that would catch its bug class — after F5,F11 · **2026-07-31 adversarial review, finding 4 + U4/U5/U11/U12** ✅ 2026-08-01 — U4/U11/U12 re-verified before acting (all confirmed; U4 understated — **six** constructors, not four). (1) make-true: `Clone` gated to `test-util` (production builds have no `Clone`; `assert_not_impl`-style const guard, planted-fault E0283 shown; C7-style `compile_fail` doc-test structurally impossible under the self-dev-dep — the move half pinned via `E0507` doc-test instead, deviation recorded); (2) make-true: all six constructors enforce the decode-side D10 caps with identical variants/order, decode precedence untouched, aggregate-encoded-size residual named at the claim sites (follow-up task recorded); (3) make-honest: behaviour sound, timing argument false — corrected in code + D10 dated amendment + contract §7; (4) made true: map-head argument (any pre-image is a CBOR map, head ≥ 0xa0 > MAX_DOMAIN_TAG), `0x00` counterexample executed in the renamed test, golden vectors now assert the disjointness `crypto/domain.rs` promised. Universe stays 194; sweep of manifest/, bundle/, codec/ found + fixed one more instance of the class (ids.rs pointer to a test that never existed)
- [ ] **F42** (S) The registry restates machine-checkable facts in prose (§7.15's band list, §9's headroom table, §12's row column) and nothing checks the copies — now permanently — after F4
- [ ] **F43** (S) The checked-absence guard sweeps only bundle maps and only `fields[]`, so a banned name in a manifest map or as a named reserved slot passes — after F4,F33
- [ ] **F44** (S) A **frozen** registry cites an **unfrozen** spec by line number 100+ times — this is also Q58's undone half — after F4
- [ ] **F49** (S) Tighten `ROUND_TRIP_PEAK_FACTOR` (measured 1.96x) and `TOTAL_FACTOR` (unmeasured) rather than leaving them guessed — after F30
- [ ] **F50** (S) The encoder's own `Vec` doubling is unbounded relative to input — distinct from F30's clamp, surfaced when the peak factor was tightened — after F30
- [ ] **F51** (XS) Fold F40's mirror-count rule into frozen registry §7.3 key 6 + §12 at the **next sanctioned registry revision** — until then a third-party verifier implementing from the registry alone accepts two-mirror manifests (normative record today: contract §7 + D23 amendment + code); do not open the freeze for this alone — after F40 · discovered by F40 2026-08-01
- [ ] **F52** (XS) Stale `owed` accounting in `tamper_coverage.rs` — four F22-rowed codes still listed as owed, invisible because `lib_rows()` omits the caps/cbor slices, which also weakens the staleness guard itself — after F22,F23 · discovered by F40 2026-08-01
- [ ] **F53** (S) **(M1)** Encode-side aggregate size gate — F41 gave all six constructors the decode-side count caps, but `MAX_MANIFEST_BYTES`/`MAX_BUNDLE_BYTES` are encoded-artifact properties no constructor sees; enforce at seal time (manifest) and R34's bundle build, reusing decode's codes — after F41 · discovered by F41 2026-08-01

### Crypto primitives (C)
- [x] **C1** (S) Hash domain-tag registry 0x00–0x06, single module, `tagged_sha256` helper ✅ 2026-07-27 — crypto/domain.rs; grep test enforces single tag site (bite verified)
- [x] **C2** (M) HKDF-SHA256 with length-prefixed injective info encoding + full 8-label registry + sentinel id ✅ 2026-07-27 — typed per-label API only; sha2 =0.11.0 / hkdf =0.13.0 / hmac =0.13.0 pinned; RFC 5869 reference cross-checked; W seam → C5 (`MasterSecretRef`)
- [x] **C3** (S) HKDF golden test: pairwise-distinct infos (spec-mandated) + derivation vectors — after C2 ✅ 2026-07-27 — pairwise test + 10k-case proptest injectivity; vectors from an independent Python impl matched byte-for-byte (testdata/vectors/hkdf/); vector test opted into the cross-OS lane; **rider**: wasm bit-match execution joins Q5's harness
- [x] **C4** (S) Crypto error taxonomy (thiserror, distinct variants, secret-redaction discipline) ✅ 2026-07-27 — exact 15-variant set; distinctness + no-byte-content Display tests; `#![deny(clippy::unwrap_used)]` over the crypto tree
- [x] **C5** (S) `MasterSecret` (W) + `SealId` types: CSPRNG generation, ZeroizeOnDrop, redacted Debug — after C4 ✅ 2026-07-27 — injected `TryCryptoRng` (rand_core =0.10.1 trait-only; C4's `RngFailure` live); `SecretBuf` for U; wired into C2's `MasterSecretRef` seam
- [x] **C6** (M) Four salted commitments (unit/path/raw/canon) + `Salt16`/`Seed32`/`NodeHash32` length-checked newtypes — after C1,C2,C4,C5 ✅ 2026-07-27 — opaque `FileSalt` (content commitments uncallable with a freely-disclosed salt); `subtle` constant-time compare; distinct length errors
- [x] **C7** (M) Type-system enforcement: `unit_commit` iff not-covered (`UnitBinding`); `file_salt` reachable only via full-reveal constructor — after C5,C6 ✅ 2026-07-27 — `FullFileRevealContext::attest` multiset check; 4 compile_fail doc-test proofs; line-94/95 rationales in module docs
- [x] **C8** (S) Unit padding codec: `padded_length` formula, apply, length-first strip, two distinct rejections — after C4 ✅ 2026-07-27 — u128-internal (adversarial lengths total, no overflow); bucketing-leak docs
- [x] **C9** (M) Unit AEAD: k_u, XChaCha20-Poly1305, AAD = seal_id‖LE64(unit_id), internal fresh nonce, no caller-nonce API — after C2,C4,C5,C8 ✅ 2026-07-27 — chacha20poly1305 =0.11.0 (no getrandom in core); `test-util` mis-encryptors for C17/R7; key-direct `decrypt_unit_with_key` seam added at R2 (single impl, W path delegates)
- [x] **C10** (S) Manifest encryption k_m (sentinel label, empty AAD) + storage-record values — after C2,C5,C9 ✅ 2026-07-28 — crypto/manifest_aead.rs; empty-AAD frozen and asserted (a non-empty-AAD ciphertext fails decrypt), `{nonce, k_m}` storage record for F's schema, ManifestKey zeroizes, wasm32 green
- [x] **C11** (M) M0 signature-crate probe: ml-dsa wasm32 viability, ctx support, canonical-rejection evidence, dalek pin decision — gates C12–C16 ✅ 2026-07-27 — **ml-dsa =0.1.1 wasm32 VIABLE, executed bit-match** (zero-import probe ran under plain `WebAssembly.instantiate`, transcript SHA-256-identical to native); ctx first-class; complete canonical rejection at `Signature::decode` → **no C13 pre-validation layer**; fips204 byte-identical (keygen+deterministic sig) fallback; dalek parse infallible → C12 pre-checks (D16); probes/sig-probe + docs/research/C11-signature-probe.md; D13/D14 resolved, D15 inputs captured
- [x] **C12** (M) Ed25519 strict signing/verification with frozen context prefix `ctx‖0x00‖body`; NonCanonical vs Invalid distinct — after C2,C4,C11 ✅ 2026-07-28 — dalek =3.0.0 with the **D16 explicit pre-validation layer** (S < L; R/A decompress→recompress→compare; small-order), so mauled-but-strict-invalid surfaces `NonCanonicalSignature` distinctly from `SignatureInvalid`
- [x] **C13** (M) ML-DSA-65 signing/canonical-strict verification with FIPS-204 ctx; pre-validation layer if crate is lax — after C2,C4,C11 ✅ 2026-07-28 — **deterministic signing per D15** (rnd = 0³²); **no pre-validation layer** (C11 proved `Signature::decode` enforces the complete FIPS-204 canonical rule — decode-fails ⇒ NonCanonical, verify-false ⇒ Invalid); never uses the crate's `Signer`/`Verifier` traits (they sign with the EMPTY ctx — C11 §2.2); independence: the module reproduces the **C11 probe transcript golden**, an artifact already cross-checked against fips204 and executed on wasm32
- [x] **C14** (M) `sig_policy` validation + hybrid orchestration: present-set == policy-set, four hard-fail classes, Ed25519-only fallback path — after C12,C13 ✅ 2026-07-28 — `SigPolicy` valid **by construction**; `from_ids` is what makes `SigPolicyUnknownAlg` reachable (a `SigAlg` cannot represent an unregistered id); **D17 ratified in code** (0=ed25519 because the fallback policy is `[0]`, 1=ml-dsa-65, 2–15 rejected) with a test pinning C's mapping against F's wire registry over the full id range; `PolicyLabel` = R's verdict datum; anti-downgrade test uses a body that embeds its own policy
- [x] **C15** (M) Committed reject-vector suites for both algorithms (S≥L, small-order, hint/z bounds, wrong-ctx…) — after C12–C14 ✅ 2026-07-28 — 21 Ed25519 + 15 ML-DSA-65 committed cases as a new `sig-reject` **kind inside Q4's envelope** (a reject vector is a golden vector whose `expect` is an error code), so they inherit discovery, NON-SECRET enforcement, the cross-OS lane and the WASM-safe path — and now run on **Q5's bit-match**. Wrong-ctx covers the frozen `ctx‖0x00‖body` prefix, FIPS-204 ctx, and the empty ctx the crate's trait impls would silently use. The base doubles as a keygen + deterministic-signing golden (reproduces C12's pinned bytes)
- [x] **C16** (L) Crypto golden vectors + independent-implementation cross-check (HKDF, commitments, AEAD, signatures) — after C2,C6,C8–C10,C14 ✅ 2026-07-28 — four vector kinds (commitments, unit-aead, manifest-aead, signatures) with a **genuinely independent vehicle per surface**, named and justified: a **from-scratch Python XChaCha20-Poly1305** (cipher, not a binding — also matched libsodium), the **RFC 8032 §6 reference formulation** for Ed25519 (known-answer-checked against §7.1, libsodium and OpenSSL), and `fips204` for ML-DSA-65 — **stated plainly as one tier weaker** (same language/toolchain, no external oracle) and fed to D31. `reference.py` is validated against published known-answers before any generator runs it. **Zero discrepancies, first run**; two agreements had no opportunity to tune (the Python RFC 8032 code re-derived C12's weeks-old pinned pubkey; the from-scratch AEAD matched RustCrypto on all 12 ciphertexts)
- [x] **C17** (M) M0 crypto tamper rows (wrong salt/key, lengths, 4 sig_policy rows, non-canonical sigs, over-padded) + distinctness meta-test — after C6–C9,C14,C15 ✅ 2026-07-28 — 17 rows into Q7's harness (registry **17 → 34**), every one binding an existing `CryptoError` code, no new codes minted. Deliberately does **not** duplicate the three families Q7's seed rows already claimed — a second row would be a distinctness collision, and the contract forbids re-scoping a code to fit — and adds a meta-test asserting those codes stay **absent** from C's slice. What it adds instead for over-padding is what the seed rows cannot give: both C9 mis-encryptors driven end to end through encrypt → authenticate → strip, proving the padding rejections fire strictly after the AEAD
- [x] **C18** (M) Crypto property tests (injectivity, commitment exactness, padding, AEAD, hybrid round-trip) — after C2,C6,C8–C10,C14 ✅ 2026-07-28 — 9 properties per Q3's conventions. **Found a real defect in already-done work**: `PROPTEST_CASES` *overrides* a hardcoded `cases` (the macro re-applies `contextualize_config`), so C3's spec-mandated ≥10 000-case floor had been running 1024 in CI — fixed at C3. Also fixed integration-test proptests persisting **no** regression seeds (`SourceParallel` cannot find a regressions dir from `tests/`), via the additive `strategies::integration_test_config`
- [x] **C19** (S) Adversarial confirmation-attack doc-tests (unsalted-unit + unsalted-file-hash variants) — after C6 ✅ 2026-07-28 — `crypto::confirmation_attack`: two executable attacks on **default-feature public API** (no test-util escape hatches), so they run in the plain CI `test` lane as permanent regression guards and render as a standing warning in the API docs. Each shows the attack **succeeding** unsalted and **failing** against the real salted construction; the unsalted-file-hash one shows a one-unit-reveal recipient cannot mint `FullFileRevealContext` from a partial reveal. C20 must cite this module as the executable form of its hiding claim
- [x] **C20** (S) Author the frozen Security-assumptions block content (→ Q12 skeleton; sign-off recorded) — after C6,C8,C9,C14 ✅ 2026-07-28 — `docs/security-assumptions.md`: five assumption classes, each with assumption / what-breaks-it / real code citations, signed off (aed900) in the change header. The MUST-NOT-trust-AEAD rule appears **verbatim** in both AEAD module docs (spelling `favor`, matching MVP-SPEC.md line 103) and is machine-pinned
- [x] **C21** (S) Zeroization coverage audit + WASM browser-memory caveat — after C5,C9,C10,C12,C13 ✅ 2026-07-28 — roll-up compile-time assertions (native + wasm32) + `docs/zeroization-audit.md` with a 7-check grep sweep and residual risks R1–R5; WASM caveat on the crypto module root and in the threat model. **R1 is a live contradiction of MVP-SPEC.md line 143**: every `derive_*` builds an `Hkdf<Sha256>` whose inner `Hmac<Sha256>` is keyed with the PRK, and `extract` also materialises the raw PRK — both W-equivalent, neither zeroizable on the pins (`hkdf =0.13.0` has no zeroize feature; `hmac`'s does not reach `Hmac<D>` via `buffer_fixed!`). → **C22**. Deliberately did **not** mark `PartialRevealDisclosure` `ZeroizeOnDrop` — `Vec` growth moves elements bitwise and strands un-wiped salts, so the claim would be false

- [x] **C22** (S) Decide the disposition of the un-zeroized HKDF/HMAC key state (C21's residual risk R1, a live contradiction of spec line 143): (a) accept permanently, (b) upstream a `ZeroizeOnDrop` addition to `hmac`, (c) in-house RFC 5869 extract/expand over a zeroizing HMAC (~40 lines). Option (c) moves a **frozen derivation** into our code — a pin-governance event — after C21 ✅ 2026-07-28 — **D88** implemented: `sha2`'s `zeroize` feature enabled — **option (d), which C22 did not list**. Its premise "no feature fixes it" was **false**: `digest`'s `buffer_fixed!` `ZeroizeOnDrop` arm is a marker with no `Drop`, so the wipe is drop glue in `sha2`, a crate neither C21 nor C22 examined. Measured, each probe run **red first** against a control: `Hkdf<Sha256>` 114/144 non-zero → 0; the extract context 108/144 **with `W` verbatim** → 0. Zero derived bytes moved on native and wasm32; cost +2.2–3.5 %, ≈0 % at 4 KiB, under D88's 10 % gate. Four D88 prose errors amended at source, incl. a stop-condition that would have aborted a correct implementation
- [x] **C23** (S) Sweep the caller-owned secret preimage buffers C21's `crypto/`-only tables missed ✅ 2026-07-28 — 6 sites wiped across `content/ggm.rs`, `fine_tree/{ggm_walk,verify}.rs`, `crypto/material.rs`. The worst was on no record's radar: `ggm_walk::next_salt` copied the whole 32-byte leaf seed into a local just to truncate it to 16 — a full GGM ancestor seed, un-wiped, on **every leaf of every fine tree**. It is *deleted*, not wiped. `tagged_sha256`'s streaming shape documented as a **requirement**, since a "simplify to one Vec and one hash" refactor would recreate the class with nothing to wipe
- [x] **C24** (S) Guard the `sha2` zeroize feature ✅ 2026-07-28 — three layers (compile-time / declaration / behaviour) in **three separate test binaries**, so layer 1's `E0432` cannot bury layer 2's message and layer 3's residue counts. D88 §7's "no compile-time detector" is too strong — `sha2::digest::zeroize` does not resolve without the feature — but it is **half** a detector, blind to `sha2`'s own feature and so to the `Drop` that is the entire mechanism
- [ ] **C25** (S) Decide whether the material newtypes' `from_bytes` should take a reference — every call site passing `*seed.as_bytes()` materialises an un-wipeable argument temporary holding a full secret — after C23,C7
- [ ] **C26** (S) Decide whether caller-owned local wipes can be asserted **at all** — C22's claims are measured, C23's are review-only, because a function-scoped local cannot be probed without asserting on UB; so a C23 regression is invisible today — after C23,C22
- [x] **C27** (S) Make `signatures.json`'s ML-DSA half re-derivable by the cross-check — two fields are read back from the committed file rather than derived, so a tamper there cannot turn `--check` red (covered at T0 by ACVP; this is completeness) — after Q11,C16 ✅ 2026-07-28 — the claim was confirmed by **planting a fault, not by reading**: `check_all` copied the two fields into `borrowed` and the byte-diff compared the file against itself. Both self-test faults leave that leg green; the red comes only from the new derivation (ρ per FIPS 204 Alg 6, `HintBitUnpack` per Alg 21, lengths, residue). Framed honestly as checker completeness — ML-DSA-65 is already T0-covered by ACVP
- [x] **C28** (XS) `verify_body`'s doc promises pubkeys/signatures are "re-checked here so this function is safe to call on any input" — duplicate-freedom is **not** re-checked and `lookup` is first-wins, so a duplicated listed algorithm's garbage entry is silently ignored on the pub API. Unreachable through the shipped pipeline (F's schema rejects duplicates), but it is the cross-implementation divergence class spec line 73 names — after C14 · **2026-07-31 adversarial review, finding 5 (medium→low)** ✅ 2026-08-01 — verify_body re-checks duplicate-freedom on both collections before the unlisted/per-policy checks (red-then-green on the review's verbatim input); minted `crypto-pubkeys-duplicate`/`crypto-signatures-duplicate` (universe 191 → 193, appended per D30 §3, nothing renamed or re-scoped), contract §7 entry recorded; full lib 748 green + tamper/sig-reject/fmt/clippy/wasm32 clean
- [ ] **C29** (XS) `verify_body` silently ignores **unlisted pubkeys** — present-set==policy-set is enforced for `signatures` only, so an implementation that also rejects unlisted pubkeys diverges on the same input (the spec line-73 class, one collection over from C28); enforce symmetry or record the acceptance in the doc — after C28 · discovered by C28 2026-08-01

### Content model (G)
- [x] **G1** (M) Pin Unicode/NFC data version; ship normalization table in core; version-dispatch registry (retained forever) ✅ 2026-07-27 — `unicode-normalization =0.1.25` = Unicode 17.0.0 (verified from crate bytes, machine-asserted); descriptor string `unicode-17.0.0`; add-only registry + `UnknownUnicodeVersion` (D25)
- [x] **G2** (M) Canonicalization v1: detection, BOM/EOL/NFC pipeline, `canonicalize_v`, idempotent, total under --force-text — after G1 ✅ 2026-07-27 — per frozen D20/D21 (total forced-text lossy U+FFFD; strip-all-leading-BOM; one-pass EOL); 21 tests incl. byte-exact maximal-subparts KATs + idempotence proptests; **R4 note: raw-mirror recompute must call `TextMode::Forced`** (doc'd on the enum); corners for G3/G4/G5: BOM-only file → empty canonical, truncated-BOM → leading U+FFFD
- [x] **G3** (M) UTF-8 corpus with golden canonical outputs (CRLF, NFD/NFC, BOM, emoji/ZWJ, mixed scripts) + cross-platform stability — after G2 ✅ 2026-07-28 — 37 fixture pairs + 6 `corpus_*` tests; goldens from an **independent Python-stdlib implementation** of the D20/D21 pipeline (different NFC lineage), so a green run is cross-implementation agreement — the two decoders agreed on every fixture at first run; NFC oracle safety rests on the Unicode Normalization Stability Policy (all NFC-sensitive scalars assigned ≤ 6.0, `NFC_SAFE_FLOOR`). Also asserts D20's lossy≡strict equivalence on every valid fixture, distinct `InvalidUtf8` refusal on every invalid one, and totality over **every truncation** of every input. cross-OS byte-stability evidence satisfied by the wave-6 remote run (30376668778 — 18 contexts green incl. the 3-OS corpus/vector lanes at a corpus-carrying commit); stale ⛔ cleared 2026-07-31
- [x] **G4** (S) Canonicalization descriptor struct + construction logic + cross-field validation — after G2 ✅ 2026-07-28 — two layers: `describe_file` (seal side, total) and `try_new` (decode side, the predicate F/R invoke) with **six** distinct rejections incl. the biconditional's second half (absent tree WITH domain), plus `validate_with_size` for the empty-file rule
- [x] **G5** (M) Unit model: work-global LE64 unit_id assignment, size semantics, `is_fine_tree_covered` predicate — after G4 ✅ 2026-07-28 — `FileUnitPlan` in the frozen D23 order (normals, then mirror last); **D24 made structural**: `FileUnitPlan::split` demands a `SplitEligibleText` witness that only exists for fine-tree-covered text, so no code path can split a `--no-fine-tree` or binary file — the contradiction is unwritable, not runtime-rejected
- [x] **G6** (M) `--split blank-lines` with frozen boundary semantics; sorted/non-overlapping/exact-tiling output — after G2,G5 ✅ 2026-07-28 — D22's boundary semantics as a single left-to-right cut rule: cut after the last line of every maximal blank run with non-blank content on **both** sides. Operates on `&CanonicalBytes`, not `&[u8]`, so "on the canonical rendition" is a type-level fact; D24 stays structural (the `SplitEligibleText` witness is demanded at the entry point). 17 tests incl. CRLF ≡ lone-CR ≡ LF, and units-are-leaf-ranges asserted against a real `SaltTree`
- [x] **G7** (M) Raw-mirror model: detection, entry construction, tiling/concat exemptions, bare-`--units` rejection rule — after G2,G5 ✅ 2026-07-28 — `needs_mirror` = byte inequality; construction takes `raw_size` from the bytes themselves so range and `true_length` cannot be mis-stated. `tiling_exempt`/`full_reveal_concat_exempt` are `const fn(UnitKind)`; `RevealSelection` drives `mirror_selectable` with the distinct bare-`--units` rejection (`content-raw-mirror-not-unit-selectable`). Module docs pin R4's `TextMode::Forced` requirement (D20) — a `Detected` recompute would return a *decode* error on exactly the files the binding exists to bind
- [x] **G8** (M) GGM salt-tree derivation: 0x06 children, MSB-first indexing, 16-B leaf salts, node-address type — after G5 ✅ 2026-07-28 — pure/allocation-bounded over an injected `s_root`; **D9 resolved** — `NodeAddress` = `(level, index)`, whose index bits MSB-first ARE the root-to-node path (the spec's own leaf rule generalized); unused slots `i ≥ n` are unreachable through any public path (`salt` → `None`, `seed_at` → `UnusedGgmSlot`); n=6 leaf-2 path (0,1,0) asserted explicitly
- [x] **G9** (L) Streaming fine-tree construction O(log n) + `rebuild_fine_root`; 5–7 compressions/byte instrumented — after G2,G4,G8 ✅ 2026-07-28 — amortized sequential GGM walker (d+1 seed stack, ~2 compressions/leaf = the path-union edge count) + RFC-6962 binary-counter frontier; `rebuild_fine_root` is **one implementation** — the verifier path *is* the sealer path. Compressions/byte is **instrumented and asserted equal to the closed form** `n + 2(n−1) + G(n)`, not eyeballed; streaming == naive reference as a proptest over n ∈ 0..300, with chunk-boundary independence. Budget constants deliberately left to G18/D26
- [x] **G10** (S) Fine-tree cost estimator (for seal's printout) — after G9 ✅ 2026-07-28 — `estimate_fine_tree_cost(n) -> CostEstimate` with `Display` for U's seal-time print; `matches_measured()` ties it to G9's instrumentation exactly (landed as the cheap add-on it was scoped to be)
- [x] **G11** (M) Leaf-exact minimal GGM sub-cover; no-ancestor-of-unrevealed-leaf by construction; s_root iff full range — after G8 ✅ 2026-07-28 — by construction, not runtime rejection: `LeafExactCover` has no public constructor, `minimal_cover` is its only producer, and `cover_seeds` — the crate's **only** seed-disclosure path — accepts nothing else. `s_root`-iff-full-range falls out of the emission rule. Both spec-named KATs pinned (n=6 {2} → (3,2); n=6 [4,6) → (1,1), asserting the naive two-node cover is *not* emitted). Depth is derived from `n`, so the inconsistent `(n,d)` pair the task signature allowed is unrepresentable
- [x] **G12** (M) Range-proof generation; `prove_unit` = leaf-aligned range opening through the same machinery — after G9,G11 ✅ 2026-07-28 — `prove_unit` **is** `prove_range` (asserted byte-identical on the wire form), one code path not two; which units may be opened is the `CoveredUnit::of` witness over G5's predicate, so proving a raw-mirror or `--no-fine-tree` unit against `fine_root` is unwritable
- [x] **G13** (M) Range-proof verification against `fine_root`: leaf-exact cover enforcement, 7 distinct errors, defensive — after G8,G9,G11 ✅ 2026-07-28 — 7 distinct errors in a **frozen precedence** (`RangeOutOfBounds → ByteLenMismatch → BadSeedLength → BadNodeHashLength → OverBroadCover → WrongCoverShape → RootMismatch`), over-broadness classified before structural faults so it can never be masked; bounded on hostile `n` (verified at `u64::MAX`); no-panic proptest over wholly arbitrary fields. R2's pre-declared seam honoured — `FineTreeError` relocated into G with R2's two codes reproduced **verbatim** under a guard test, and R's exemplar list *sources* the fine-tree rows from G's so the two cannot drift
- [x] **G14** (M) Pure seal-side ContentModel assembly (files+flags → descriptors/units/mirrors/fine_roots) — after G2,G4–G7,G9 ✅ 2026-07-28 — one pure entry point `assemble_content_model`; D24 stays structural via G5's witness (the conflict is unwritable, not rejected); `fine_root` presence **is** the descriptor's answer, no unwrap. Assembly is TOTAL via a new `canon::canonicalize_forced` (stages 2–5 factored out) so **D20's totality is structural rather than an `expect`** — and that is also the verifier's own recompute mode, making `canonicalize(raw) == canonical` true by construction. Golden 4-file fixture + 9-invariant oracle on the WASM-safe tier; 14 tests. Surfaced G21/G22
- [x] **G15** (M) Fine-tree golden vectors: unbalanced n=6 pinning MSB-first + n=1/n=0/n-past-power-of-two — after G8,G9,G11,G12 ✅ 2026-07-28 — landed as a **Q4 vector kind (`fine-tree`)** inside the existing envelope, not a separate tree, clearing Q6's `fine-tree-unbalanced-n6` pending row; 4 cases (n=6/1/0/5) with every intermediate pinned — 12 GGM seeds, 6 salts, 11 RFC 6962 nodes, 8 full openings. MSB-first pinned **twice**: the committed salts, plus an LSB-first walk required to diverge (skipping palindromic paths). Three regenerators agree — the crate, an independent Python stdlib reference that reproduced the committed bytes exactly on first run, and the wasm32 bit-match
- [x] **G16** (S) Leaf-exact-cover disclosure test (n=6 reveal {2}; spec-mandated) — after G8,G11 ✅ 2026-07-28 — the closure argument on top of G11's KATs, computed by real `child_seed` descent from the shipped seeds: n=6 {2} → exactly `{salt_2}`, `s_root` absent; the companion [4,6) closure is exactly {4,5,6,7}, so the unused-slot allowance is **exercised, not sidestepped**. The regression claim is **proved, not asserted**: a concrete coarsening regression is caught naming the leaked salt, plus exhaustion over all 2^12 candidate covers × 4 ranges — 14 405 examined, accepted **iff** leaf-exact (3 220 accepted, all 11 185 merely-valid dyadic covers rejected)
- [x] **G17** (M) E2E: leaf-aligned AND arbitrary byte-range openings both verify against fine_root; covered units carry no unit_commit — after G12,G13,G14 ✅ 2026-07-28 — per-unit and non-unit-aligned ranges (incl. one spanning both unit boundaries) verified against the `fine_root` read **out of the model**, catching s_root/domain/leaf_count/range disagreements no layer test can see; unit-vs-range wire-identical at model level; `unit_commit` absent for all 4 covered units; 3 negative controls; green native + wasm32. Also cleared the 2 pre-existing wasm32 warnings in `fine_tree/`
- [ ] **G18** (M) Perf/memory budget tests (structural O(log n) bound; compressions/byte envelope) — after G9,G10
- [x] **G19** (S) Fine-tree tamper rows: covered-unit byte flip → RootMismatch; over-broad cover → OverBroadCover — after G13,G17 ✅ 2026-07-28 — both rows against G17's verified-honest opening: a flipped covered-unit byte → `fine-root-binding-failed` (any bit of any byte), and the honest cover replaced by its parent spanning 8 unrevealed real leaves → `fine-root-over-broad-cover` (a genuine seed at a valid address, so neither a length nor a shape fault can pre-empt it). Error identity + distinctness across all 7 G13 codes. `over_broad_cover` is the workspace's **single** such helper, exported for the R lane; a >100-node sweep asserts its output is always rejected as `OverBroadCover` and that it declines honestly for full reveals (D75)
- [x] **G20** (M) Consolidated content-model property suite (seeded, committed regressions) — after G2,G6,G9,G11–G13 ✅ 2026-07-28 — 14 tests at the public API covering all six mandated properties, n ≤ 2^12, against an independent raw-SHA-256 reference tree; both wave-4 findings carried forward and the seed-persistence one now asserted by a test. **Found D83** (a leaf-level cover seed's upper 16 bytes are inert, so `.sealproof` bytes are not unique for a given proof): registered OPEN, nothing implemented → **G23** before Q14; counterexample committed both as a seed line and as a named deterministic case

- [x] **G21** (S) ContentModel golden vector under `testdata/vectors/` — G14's fixture is Rust constants, so the assembly's derived values sit outside Q5's bit-match lane, which compares committed vector *files*; G15 covers the tree primitives, nothing covers the composition — after G14,G15 ✅ 2026-07-28 — `content-model` vector kind; the assembly's derived values now sit inside Q5's bit-match lane (12 vectors, byte-identical, zero per-vector wiring). The G14 tie is asserted **both** ways, so the committed file and the Rust constants cannot drift in either direction. Pins **no** proof bytes — asserted mechanically with a positive control — so D83's cover change could not move it
- [x] **G22** (S) Narrow the verifier-side canonicalization error type to the reachable class — add `canonicalize_v_forced` and route R4's recompute through it, making `content-canonicalize-invalid-utf8` structurally unreachable from every verifier path — after G14,R4 ✅ 2026-07-28 — `canonicalize_v_forced` added; R4's recompute returns the one-variant `UnicodeVersionError`, making `content-canonicalize-invalid-utf8` **structurally** unreachable from the verifier. **The task's accept criterion was wrong**: it is not reachable from the sealer either — assembly detects with `is_text` then forces, and no production call site selects `TextMode::Detected`. Real owner = the public `canon` API's strict-detection contract (G3's corpus). A code with an owner, a test and no row — a shape the reverse-coverage sweep must **accept**. Citable as `verify::CANONICALIZATION_SEAM_CODES`
- [x] **G23** (S) **Resolve D83** — the inert upper 16 bytes of a leaf-level cover seed. Options: (A) accept + document, (B) fixed canonical tail, (C) 16-byte payload at `level == d`. **Format event either way; must land before Q14** — after G11,G13; joint with F ✅ 2026-07-28 — **D83 option B** landed end to end: a `level == d` payload is `salt_i ‖ 0x00·16`; one code `fine-root-leaf-seed-tail-not-zero` surfaced from **both** the cover-entry and the `n == 1` `s_root` sites. **FORMAT EVENT** — two digests moved (fine-tree + report vectors), `bundle.json` and all 22 tamper-format digests provably unmoved. D83 §6 said the report vectors *could not* move: wrong, because R9's corpus carries a **one-byte file**, the very `n == 1` case the decision itself added — the scope extension invalidated the decision's own impact table. §4.5's prover scope also missed `full_reveal.s_root` entirely (it never passes through `cover_seeds`), so `canonical_leaf_level_payload` is now public and a real sealer must route §7.14 key 2 through it. Six prose corrections amended into D83
- [x] **G24** (S) Bundle-level coverage for a `level == d` cover node — **no committed bundle fixture has one** (every case has even unit boundaries), so after G23 the wire rule is pinned only in the fine-tree vector — after G23,F13,R36,R37 ✅ 2026-07-28 — two bundle-vector cases and three corpus seeds, so the D83 wire rule is pinned at bundle level and not only in the fine-tree vector. **Its Accept asked for a tamper row that D83 §4.1/§7 forbid** — a code names a rejection class, never a site, and the row has existed since G23 — so what landed is a third *fixture* separated by `(code, layer)`, leaving `MATRIX.json` untouched. One frozen digest moved, for D83 reasons, verified as a pure extension
- [ ] **G25** (S) `CostEstimate::seconds_at(rate)` — a pure, WASM-safe wall-clock projection; measured ~5 min for the spec's own n = 10^8 case — after G18
- [ ] **G26** (S) Closed-form accessors on `FineTreeStats`, upgrading G9's two `<= depth + 1` bounds to equalities — land **before** G18 so it asserts against them rather than restating formulas — after G9
- [ ] **G27** (S) The leaf-level incidence rule's only executable form lives in an R test file, and D83 Fact 4 has none at all despite carrying the §3 sizing — after G24,R36
- [ ] **G28** (S) Register the bundle-layer D83 fixture in the tamper harness — after G24
- [ ] **G29** (S) `mirror_selectable`'s doc claims "R re-checks it on any bundle it is handed" — no caller exists in `src/verify/`; decide a verifier-side rule (post-freeze behaviour change + D30 §3 append, rejects bundles D28 deems acceptable) vs correcting the doc to D28's deliberate partial+mirror acceptance with R53's report gate as the containment (lean: the latter) — after G7,R53 · discovered by R53 2026-08-01

### Storage survey (S)
- [x] **S1** (M) Re-verify ant-core 0.5.0 + survey exact payment/storage API shapes (written memo; feeds F4 address length, S2/S6/S7) ✅ 2026-07-27 — memo with source citations (docs/research/S1-ant-core-api-survey.md); address = 32 B BLAKE3 (D11); D32/D33/D37 inputs captured; external-signer flow recommended primary; GO on the pin

### Verification pipeline core (R)
- [x] **R1** (M) `VerificationReport` model + `VerifyError` taxonomy (deterministic serialization; every line-121 invariant a distinct variant) ✅ 2026-07-27 — 19 variants / 30 distinct stable codes; byte-deterministic JSON + pinned snapshot fixture; D27 resolved, D29 recommended; **rider**: C/G/F/A wrapper arms land with R2/F3/A-stage (documented extension point, compile-enforced)
- [x] **R2** (M) Per-unit evidence stages: decrypt → padding verify/strip → true_length binding → content-binding dispatch (fine_root vs unit_commit) — after R1 ✅ 2026-07-27 — `verify_revealed_unit` in exact stage order, all six named errors distinct; `Crypto`/`FineRootBindingFailed{source}` wrapper arms landed (stable codes 45 → **71**; C wrapper deliberately no-`#[from]`); **rider**: covered-arm E2E + real over-broad-cover rows land with G13/G17/G19 (compile-enforced `FineRangeCheck` seam + placeholder `FineTreeError` contract documented); usize/u64 wire-length convention flagged for R5/F5
- [x] **R3** (M) Structural checks: exact lengths, referential integrity, tiling invariant (+mirror exemption), path_commit — after R1 ✅ 2026-07-28 — `verify/structural.rs`, four groups in a frozen order (D27 determinism), over R1's pre-declared variants (no new codes needed). Two calls worth remembering: the tiling check runs a **sortedness pre-pass** so a descending unit table reports `Unsorted` rather than a first-range `Gap`; and `RawMirrorInTilingSet` fires on a precisely detectable condition — the non-mirror ranges fail but including the mirror's would tile exactly — catching raw bytes standing in for canonical coverage. Mirror exemption is by `kind` alone, **never** by D23's placement, so a hand-built manifest that moves the mirror still tiles correctly
- [x] **R4** (L) Reveal-shape classification + file-level checks: partial-reveal isolation, full-reveal concat + fine-tree rebuild, raw-mirror canonicalize(raw)==canonical — after R2,R3 ✅ 2026-07-28 — ten checks in a frozen order, file-major, first failure wins (D27), with **all presence rules before all length rules** — asserted, so a hostile length cannot displace the shape verdict. Resolved **D74** (reject) with the add-material argument: strip some units but leave material → leak arm, strip material but leave units → missing arm, *add* material → nothing fired, and the bundle is unsigned, so any relay could append it undetected (Q67 2026-07-31: the enclosing D28 shape-totality claim corrected — inconsistent edits only; consistent narrowing is invisible by design and harmless). D20's `TextMode::Forced` rule is now enforced **structurally** — the mode is fixed inside a private recompute and appears in no exposed signature, so no call site can select `Detected`
- [x] **R5** (M) `verify_bundle` orchestration (decode → structural → units → files → sigs → anchors-stub) — after R1–R4 ✅ 2026-07-28 — frozen stage order `decode → structural → units → files → sigs → anchors`, pinned by tests that violate **two** stages at once and assert the earlier code wins, each with a companion proving the mutation is observable alone; R5's coherence rules are the tail of *structural*, not a seventh stage. **D80** implemented as `revealed-unit-file-not-touched`, visiting the **manifest** unit table so "first error" cannot depend on bundle layout. **D75's owed agreement check proved unnecessary**: both routes bind the same *signed* `fine_root`, so an inconsistent cover yields different leaf salts and a different folded root, which R2 already rejects — an explicit comparison was rejected in both possible positions (unreachable after R4; before R4 it would displace the spec-mandated `partial-reveal-salt-leak-s-root` verdict), and the implied row is registered as a **non-row**. R's universe 79 → **84** codes / 26 variants. Anchors are a true stub: no artifact byte parsed and **no bundle-recorded metadata copied** into the verdict
- [x] **R6** (M) Test-only bundle fixture constructor (every M0 shape, seeded/deterministic) — after R1 ✅ 2026-07-28 — seeded constructor for 19 named M0 shapes (later extended to 22+ by F13/R9), all verifying end to end on the first run; **`test-vectors`-gated, not `test-util`** (the `wasm32-core-tests` dev-dependency edge enables only the former — the literal instruction and its stated purpose conflicted and the purpose won), zero optional deps, `core-dep-graph` intact. Partial-reveal isolation asserted **structurally over the decoded bundle**, incl. recomputing every cover node's real-leaf span from its `(level, index)` address and requiring it inside a revealed range. A `the_seed_is_live` test rules out the reproducibility claim also holding for a constructor that ignores its seed. R13 parity seam documented in five points
- [x] **R7** (L) M0 structural tamper-fixture suite — every line-121 row with its distinct error + positive leaf-exact-cover fixture — after R5,R6 ✅ 2026-07-28 — 24 rows, **no code minted** (every row binds a code R1–R5 had already shipped — the outcome the contract is meant to produce), registry 34 → 58 rows, 3 positive fixtures. Adds the **reverse coverage check** (error enum → rows) that Q8 structurally cannot do, and it earned itself immediately: R's *pipeline-level* `padded-length-mismatch` and `non-zero-padding` had no row and no owner. Second pipeline-unreachable R code recorded (`partial-reveal-salt-leak-s-root` — `FullReveal` makes `file_salt` mandatory so D28 row 1 always fires first), pinned by a test so it converts if F8 ever gains an `s_root`-only shape. Surfaced R29/R30
- [x] **R8** (M) Pipeline-integration tamper rows (flipped ciphertext, wrong key, swapped unit, altered manifest field both routes) — after R5–R7 ✅ 2026-07-28 — 2 rows in a new `tamper_rows_pipeline` slice; **3 mutations recorded as non-rows per D81**, one of them newly found: wrong `unit_salt` lands on the code the manifest-alteration row claims, which is D81's argument one layer up (a salted commitment opening is one bit too, because salt and stored commit are both inputs to the single comparison) → **R33** to ratify rather than rubber-stamp, since unlike the AEAD both inputs there are structurally attributable. Added the AAD-isolating C-level test D81 asked for. `EXPECTED_M0_PENDING` 5 → 2
- [x] **R9** (M) Verification golden vectors: bundle → expected report, native/WASM bit-match, every M0 shape — after R5,R6 ✅ 2026-07-28 — 21 canonical bundles → byte-exact serialized `VerificationReport`s, native and wasm32 identical; all ten enumerated M0 shapes plus eleven more, nothing unconstructible. The three `unbalanced-n6/unit-{0,1,2}` cases each open a different depth-1 subtree, between them walking every non-palindromic path of the `d = 3` grid. A vector **names** its bundle by R6 shape rather than embedding ~7 kB of ciphertext per case — both lanes build in-process and `bundle_sha256` proves they built the same bytes, keeping one definition of a valid bundle. Surfaced R31 and **R32** (a Q14 trap: `REPORT_VERSION` 0 → 1 rewrites all 21 pinned strings and must land before `status frozen`)
- [x] **R10** (S) `verify_bundle` no-panic fuzz + property coverage — after R5,R6 ✅ 2026-07-28 — shared mutation engine + 4 proptests + the 3 non-row stand-in properties, all in the **normal** suite; cargo-fuzz target built and detached from the workspace (`cargo metadata` still lists exactly 5 members, workspace lockfile unchanged), awaiting Q9's nightly pin and CI lanes. **Measured the M0 authentication boundary** by full byte sweep: exactly 88 bytes whose mutation still verifies (32 address + 24 nonce + 32 key in the storage record) plus 67 anchor-artifact bytes — both deliberate, neither previously asserted as an *equality*, now pinned in both directions with the anchor test instructed to invert when R12 lands. Surfaced R34
- [x] **R29** (S) Retire or reconcile R5's hand-built smoke fixture, now a second unrelated definition of "a valid bundle" beside R6's — more urgent after R9 (one definition is a frozen CI-enforced artifact, the other unpinned hand-built code, and the frozen half lends a shared bug false authority) — after R6,R9 ✅ 2026-07-28 — **option (b)**: R5's fixture kept as an independent construction and *bound* to R6's by two agreement tests. The entry framed option (a) as cheap and it is not — R5 needs two mutations R6 has no knob for, so deleting it meant editing R6 **in exactly the region D83 was changing**. The binding is one whole-report comparison with only `work_id` neutralised, not a field checklist, so a new report field is covered from the moment it exists. Entry also wrong that R5's work is `shapes::multi_file`
- [x] **R30** (S) Assert the report's native↔wasm32 byte-match over R6-**constructed** bundles (sequence after R9) — after R9 ✅ 2026-07-28 — 21 per-shape report-byte digests pinned in source over `shapes::catalogue()`, checked by **both** targets. The point: nothing carries a value between the native and wasm32 runs, so the pin **is** the cross-target comparison. Bidirectional name-set equality makes an unpinned new shape a failure, not a gap. Two entry corrections — it costs one extra build+verify pass, not "nothing"; and R9's "three omitted shapes" rider is stale (the sets are equal at 21), so the real value is a second lane independent of `wasm-bitmatch`
- [x] **R31** (XS) Hoist the golden-vector JSON diff locator out of the `fine-tree` kind into `vectors.rs` — after R9 ✅ 2026-07-28 — `first_difference` now lives in `test_util::vectors`, no re-export (one home, one path). The entry said "the two call sites"; there were **eight**
- [x] **R32** (S) **Bump `REPORT_VERSION` 0 → 1 and re-emit R9's vectors — before Q14 flips `status frozen`.** It is the first field of every report, so the bump rewrites all 21 pinned byte strings at once, and after the freeze `--update` refuses to modify an existing digest. Q14's checklist does not name this coupled edit — after R9, before Q14 ✅ 2026-07-28 — all 21 pinned strings now begin `{"report_version":1`; one freeze-manifest digest moved, ten untouched. **Two things the entry got wrong.** Its Accept asks for "21 changed digests" — unconstructible: the manifest is per-*file* and R9 put all 21 cases in one document (→ **F35**). And the coupled-edit list was short by a **fourth** site — D29's `EXPECTED_CANONICAL_JSON` in `verify/mod.rs`, enforced by nothing, which fails on wasm32 as a bare `the test binary trapped: unreachable` with no test name (→ **R41**). **Coupling verdict, on evidence**: `TRANSCRIPT_VERSION`/`EXPECTED_TRANSCRIPT_VERSION` are an *independent* axis and stay `0` — they version the transcript **envelope**, which carries no report field, aggregates all seven kinds and is never frozen. Q14's row corrected accordingly (→ **R42**)
- [x] **R33** (S) Decide whether `unit_commit` mismatch deserves a cause discriminator, or close it the way D81 closed the AEAD — worth a real look: unlike the AEAD, both inputs are **structurally** attributable (salt is bundle-side, commit is manifest-side and signed), so a verifier running signatures first could tell them apart, which is exactly the trade the frozen stage order rejects — after R8, before Q14 ✅ 2026-07-28 — the non-row ratified against D85. **Neither bookkeeping item was already correct**: the `record` field pointed at D81 and the registry rather than at D85, the error-code contract §7 had no D85/R33 entry at all, and D85 §4's module-doc citation was absent. All three applied; the gate still prints COMPLETE (0 pending)
- [x] **R34** (S) Extend R10's structure-aware mutators to the sections R6 cannot currently vary (needs a builder seam) — after R10 ✅ 2026-07-28 — `BundleV1::into_parts` seam (additive, the documented inverse of `new`) + 9 section-level recombinations. R6's constructor **untouched**, which is why this lane had zero merge surface with D83. Three R6 findings that each silently emptied a graft: `with_anchors` does not make a different work; per-file material is keyed on `file_id` under one shared `W` (→ **R39**); every fixture shares one constant storage record (→ **R38**). One falsification **passed** on the first attempt — no seed-corpus entry populates the receipt slot, so the seam could have silently dropped receipts
- [ ] **R35** (S) Record and assert that `.sealproof` bundles are **not union-closed** — merging two honest partial bundles whose revealed sets together cover a file hard-fails, because only `W` can produce the full-reveal material; correct, unstated anywhere, and it blocks any M3 "combine bundles" UX — after R4,R5
- [x] **R36** (S) Restate R10's authentication-boundary equality now D83 has landed — it reads as universal but is corpus-dependent (every current fixture has even unit boundaries) — after G23,R10 ✅ 2026-07-28 — **the test did not go red; it generalised.** D83 option B made the leaf-level tails a checked value rather than an inert region, so the equality is now true for a stated reason — proved by flipping all 32 bytes of a real leaf-level payload inside a real bundle and asserting the rejection **class** per half (`0..16` → `fine-root-binding-failed`, `16..32` → `fine-root-leaf-seed-tail-not-zero`). The `88 + 67` bound still holds exactly. **Its incidence rule was wrong**: a leaf-level node needs an odd **unit boundary**, not an odd `n` — a whole-file cover is the single root node for every `n`
- [x] **R37** (S) Add the leaf-level-cover reveal shapes R6's catalogue cannot produce (odd-length + one-byte fine trees) — after G23,R6 ✅ 2026-07-28 — the odd-boundary and one-byte fine-tree shapes, stated as a pinned census read off decoded cover addresses against `d` rather than inferred from a file length. Corrected the same incidence-rule error as R36, at source
- [ ] **R38** (XS) Give R6's works a **per-work** storage record — today every fixture shares one, so a cross-work graft of the one section whose M0 inertness is a named product property is a provable no-op — after R6,R34
- [ ] **R39** (S) Key R6's per-work material on the **work**, not the ordinal — two different works share one `W` and collide on ordinals, so **no fixture-built test can currently distinguish "another sealer's material" from "this work's"** — sequence before anything asserts cross-work rejection — after R6,R34
- [ ] **R40** (XS) Add the non-covered→covered migration now D83 has settled the cover encoding — after R34,G23
- [x] **R41** (S) Make a wasm32 `--lib` test failure say **which test failed** — today the whole diagnosis is `the test binary trapped: unreachable`, which is how R32's fourth coupled site hid — after P14,R32 ✅ 2026-07-28 — a wasm32 `--lib` failure now names its test, **with no new import**. Linear memory survives the trap and the program already wrote the diagnosis into it: libtest's unflushed progress line (stdout is a discarding sink *behind* std's line buffer, so the flushing newline never arrives) and the panic hook's record. The exported `__heap_base` is what makes the scan sound — test names and the panic format string are static literals, so an unfiltered scan reports decoys. Zero or several candidates are reported as such, never guessed
- [x] **R42** (XS) Carry `REPORT_VERSION`'s coupled-edit row into Q14's checklist, including the **negative** third clause — after R32 ✅ 2026-07-28 — **already landed at `88526bb`** (Q37's checklist correction) and never ticked. Verified rather than redone: Q14's row carries the coupled-edit list including the deliberate **negative** third clause (`TRANSCRIPT_VERSION` stays `0`)
- [ ] **R43** (S) `testdata/fuzz-seeds/` names are positional corpus selectors — G24's three-entry insertion silently re-pointed 133 named seeds — after R34,G24
- [ ] **R51** (S) D85 §5's three salt-route tests (`path_commit`, `canon_commit`, `raw_commit`) — the record's own falsification, asserted by nothing; if any lands on a different code, **D85's generalisation** needs amending, not the test — after R33
- [ ] **R52** (S) Give `test_util::bundle_fixtures` the `Tweak` knobs R51 needs — after R6,R33
- [x] **R53** (S) The two raw-mirror consumers disagree — `check_full_reveal_content` binds the **first** mirror (`.find`), `reveal_set` reports the **last** — and `raw_mirror` is emitted for *partial* reveals where rows 9–10 never run, so the report can name a mirror nothing checked (`fully_revealed` consults `is_full()` two lines below; the mirror arm does not) — after R4,R5,F40 · **2026-07-31 adversarial review, findings 1–3 + 8** ✅ 2026-08-01 — `resolve_raw_mirror` is now the sole mirror resolution, called by both consumers (structural `include_str!` test goes red on any second path); `raw_mirror` gated on the same `is_full()` as `fully_revealed` (red-then-green: a verifying partial-reveal bundle carried `Some(RawMirrorReveal{..})` before, `null` after; pinned via a D29 byte-snapshot test + an R30 digest row asserted native+wasm32 — the frozen R9 report document may not grow, its executor demands full M0 shape coverage per document, reason recorded); two-mirror pipeline outcome pinned to `manifest-multiple-raw-mirrors` through `verify_bundle`; **0 frozen bytes moved** — all 21 pinned reports regenerate byte-identically through the changed pipeline (native + wasm bit-match green)
- [x] **R54** (M) Quadratic pre-authentication scans: D10's count caps are simultaneously reachable and **nothing bounds their product**, so a legal ~8 MiB bundle with *no reveals* forces ~2×10⁹ iterations before any authenticator runs — a hang in the browser verifier. Index, do **not** reorder: stage 5 running last is correct for a self-signed manifest — after R3,R4,F11 · **2026-07-31 adversarial review, finding 6 + U1/U3** ✅ 2026-08-01 — U1/U3 re-verified TRUE first; index-once landed across stages 2–4, `reveal_set`, and the tier-[X] merge scans (sorted-input merge join preserves first-error); at-cap no-reveal verify 29.0 s → 2.6 s debug / 3.11 s → 0.13 s release, scaling 3.7× → 2.0× per doubled input, guarded by the machine-speed-free ratio test in `tests/verify_scan_shape.rs` (proven red against the pre-fix code; first fully *valid* at-cap work in the tree, reusable); `check_manifest_refs`' tautological arm documented unreachable-by-construction with `unknown-file-ref` still emitted at two pipeline-reachable sites (universe 194 intact); D10 amended — §5 row 2 made true, the cap product recorded as deliberately unbounded; stage order untouched
- [ ] **R55** (XS) Promote the `split-multi-unit/partial-with-mirror` pin (the shape that exposed finding 8) from in-tree tests into the frozen vector set — parked until Q68's union rule or the next format-version event; carriage, not construction — after R53,Q68 · discovered by R53 2026-08-01
- [ ] **R56** (S) Extend the scan-shape guard to the valid **fully-revealed** at-cap work — U1's own shape (~18 MB honest AEAD); post-R54 the reveals-driven terms are indexed but unguarded; venue decision needed (release assert vs opt-in lane) — after R54 · discovered by R54 2026-08-01
- [ ] **R57** (XS) U17: `proof_unit_refs`' tautological arm — re-verify (lane-reported, unverified), then the R54 treatment: document unreachable-by-construction or restructure with `unknown-unit-ref` still emitted (universe gate) — after R54 · from the review's U17, promoted by R54
- [ ] **R58** (S) **(decide by M3)** `Mode::Collect` accumulates an unbounded findings list on a hostile many-failure bundle — cap the rendering-only list or record why the existing caps bound it; D27's first-element invariant must survive — after R54 · discovered by R54 2026-08-01
- [ ] **R59** (S) **(decide at M3)** R11's home and its bundle-shaped half: the manifest adapter landed in `antseal-net` (only crate that sees both `StorageBackend` and the manifest types; D34 sends *drivers* to the `antseal_cli` lib, and R11 is a shape adapter, not a driver). When R21 lands in the cli lib, either build the bundle-shaped adapter through the public `StorageRecord::new` extension point and keep the split, or relocate the module — a pure move, no cross-crate contract — after R11,R21 · discovered by R11 (lane θ) 2026-08-02

### Anchor freeze boundary (A)
- [x] **A27** (S) Record the anchor-artifact limit contract (implements D84) ✅ 2026-07-28 — new `docs/format/anchor-artifact-limits.md` carrying rules **F1–F4**, spliced **programmatically** from D84 so byte-identity is guaranteed rather than hoped for. `tasks/A.md`'s A5 and A11 `Do` text was **wrong on disk** — both promised caps that are already frozen D10 constants, so an implementer reading it alone would mint duplicates. Found D84 citing **Q19** for the format-stability policy in three places; it is **Q27** (corrected at source)
- [ ] **A28** (S) Receive-side caps must be <= the frozen bundle-field caps — after A5,A11 (M2)
- [ ] **A29** (S) Carry D84's F2/F3 into R12's Accept criteria — a requirement living only in a consequences list is one the implementer never sees — after R12 (M2)

### Shared infrastructure & freeze (Q)
- [ ] **Q1** (M) Extend CI skeleton to full M0 matrix + multi-OS corpus/vector lane (+ mount points for later lanes; branch protection) ⏳ WIP — workflow extended 2026-07-27: 14 contexts incl. 3-OS `corpus_`/`vector_` suites; **first remote run GREEN on all 14** (run 30309407509 — cross-OS byte-stability proven on real macOS/Windows runners) — remaining: branch protection ⛔ BLOCKED BY PLAN, not a deferral (verified 2026-07-28: classic API and rulesets both 403 "Upgrade to GitHub Pro or make this repository public" on a private repo on GitHub Free; three options in docs/ci-verification.md, going public triggers Q65 first; payload = 19 contexts from 17 jobs)
- [x] **Q2** (S) `testdata/` layout + fixture conventions + no-real-secrets CI guard — after Q1 ✅ 2026-07-27 — full layout + per-dir ownership READMEs; secret convention (fixed test seed `0x00..0x1F` = `test_util::TEST_MASTER_SECRET_W`; vault magic `ANTSEAL VAULT EXPORT` reserved for U12); self-testing `secret-guard` CI job (**14th required context** — branch-protection payload superseded in ci-verification.md); hkdf vectors migrated to `vectors/v1/` (pre-Q6, field-verified byte-identical)
- [x] **Q3** (S) proptest conventions + shared strategy helpers — after Q2 ✅ 2026-07-27 — docs/testing/proptest-conventions.md; `test-util` feature = optional-proptest re-export + shared strategies (single frozen 1.11.0 workspace-wide; components self-dev-dep pattern); PROPTEST_CASES=1024 in the CI test lane
- [x] **Q4** (M) Golden-vector framework (schema, discovery, native runner) — after Q1,Q2 ✅ 2026-07-27 — strict JSON envelope (deny_unknown_fields, NON-SECRET marker enforced) + glob discovery + fail-loud runner (misfiled/stray/unknown/empty all fail); kind-dispatch extensible without harness change; executor is WASM-safe (zero I/O) for Q5 reuse; `golden-vectors` mount point claimed
- [x] **Q5** (M) WASM/native bit-match harness + required CI lane — after Q1,Q4 ✅ 2026-07-28 — same vectors, same executor, transcript compared by **recomputed digest** (SHA-256 over every recomputed byte, length-prefixed and domain-separated), native and wasm32 byte-identical over all **7** committed vector files. The red-lane proof is **permanent, not a one-off**: `--self-test` is the lane's first CI step, injecting a divergence (reversed order *and* uppercased hex, so it cannot decay into a no-op) and requiring red. Reuses Q4's zero-I/O executor rather than forking it; C3's HKDF rider wired in
- [x] **Q6** (S) Vector freeze + indefinite per-version retention (FROZEN.sha256; must-exist list) — after Q4 ✅ 2026-07-28 — the hash lines **are** the must-exist list, so there is nothing to drift. Three failure classes: modified (digest mismatch), **deleted (MISSING — the class the Q4 runner structurally cannot see)**, and committed-but-unlisted (UNFROZEN, printing the line to append). Two independent layers (coreutils `sha256sum -c` compatible + a Rust checker), self-tests first each run. The parser **refuses `status frozen` while the pending set is non-empty**, which makes Q14's gate executable rather than prose. Pending must-exist: `manifest-encode-decode` (F12), `bundle-empty-anchor` (F13), `fine-tree-unbalanced-n6` (G15)
- [x] **Q7** (M) Tamper-matrix harness: distinct-outcome assertion, no-panic rule, error-code contract — after Q1,Q2 ✅ 2026-07-28 — `test_util::tamper` (WASM-safe except the panic guard): rows carry an error code **or** a named verdict state (M2 anchor rows need the latter — an untrusted root does not error), in separate namespaces. Both tests-of-the-test present: colliding codes fail distinctness naming the other row; a panicking mutation fails with a PANICKED report. Seeded with **17 real rows across F/C/R**, all outcomes pairwise distinct. D30 formalized at docs/testing/error-code-contract.md (per-domain prefixes, append-only, three enforcement layers). Row population stays with F15/C17/G19/R7-R8; 1:1 completeness is Q8's
- [x] **Q8** (S) Tamper-matrix completeness registry vs the spec's M0 row list (CI-enforced 1:1) — after Q7 ✅ 2026-07-28 — 37 M0 cases over 17 families, each anchored to a **literal substring of MVP-SPEC.md line 168**: 24 implemented, 13 pending with named owners (F15×2, G19×2, R7×6, R8×3), 9 project-added with recorded justifications, 2 recorded non-rows, 7 M2 cases pre-registered. It earned its keep before a single new row exists — applying distinctness to **pending** rows surfaced three collisions ahead of implementation, the load-bearing one being D81. Q14 closes on `EXPECTED_M0_PENDING` empty
- [x] **Q9** (M) cargo-fuzz infra: smoke lane per PR + nightly + corpus management — after Q1,Q2 ✅ 2026-07-28 — last Q1 mount point claimed; context set stays 16. **Q39 landed here rather than split**: `fuzz/rust-toolchain.toml` = `nightly-2026-01-26`, governing `fuzz/` only because rustup resolves toolchain files upward and `fuzz/` is not an ancestor of `crates/` — `rust-toolchain.toml` and the MSRV untouched, `core-dep-graph` byte-identical. **Q9's Accept is unachievable on a stable pin at all**; the scoped toolchain file is the whole mechanism. Self-test arms a *permanent* tripwire and fails unless a crash is caught **and** an artifact written, so a green lane means something
- [x] **Q11** (L) Independent-implementation cross-check (CBOR + crypto + GGM + Ed25519; ACVP for ML-DSA) — freeze blocker — after Q4,Q6 ✅ 2026-07-28 — **discrepancy report EMPTY: 0 across all 13 independence rows, none skipped.** **ACVP probe verdict: BRANCH A** — external-interface groups with `context` (0–255 B) exist at `usnistgov/ACVP-Server` `2972def`, so ML-DSA-65 is **T0**, not a T1 hand-derivation; 115 cases, zero disagreements first run, asserted twice per case (the crate's `sign_deterministic` *and* our hand-derived pre-image, checked against NIST rather than ourselves). Deliberately **not** the 2024 pin `fips204` vendors — those validate the wrong crate. **D31's Unicode-version assertion is unsatisfiable** (no released CPython embeds 17.0.0; the lane would be permanently red) — built the real anchor instead, 1 537 lines of Unicode's own `NormalizationTest.txt`. Stated plainly for the gate: salted commitments, unit padding and the GGM tree are **T1 with no T0 anchor and none can exist** — those constructions are ours. `fips204` → dev-dependency (T2, not independence). Surfaced Q41, Q42, C27
- [x] **Q12** (S) Threat-model skeleton with frozen Security-assumptions block + sign-off — after C20 ✅ 2026-07-28 — `docs/threat-model.md` with C20's block spliced **programmatically** (21 465 bytes byte-identical), sign-off recorded, all ten M4 sections stubbed with scope notes. Drift is machine-enforced by 6 tests: byte-identical between markers with no normalization, a >4 kB floor on **both** copies (so emptying one cannot satisfy byte-equality), the AEAD rule in both modules and the block, all ten M4 headings, and the WASM caveat in all three places — negative-tested, a one-char edit reports the first differing byte
- [x] **Q13** (M) Verification-coverage traceability matrix (spec lines 165–175 → test IDs; CI-checked) — after Q4,Q7,Q8 ✅ 2026-07-28 — `docs/testing/verification-matrix.md`: 34 rows, 49 references at landing (55 at HEAD — V3.4's evidence cell was populated when the row flipped `gap → covered` later the same wave; corrected 2026-07-31), built by grepping the tree rather than from task text; CI lane `traceability`, self-testing first. **Found the one empty M0 row: V3.4 (line 169, "bundle/manifest CBOR fuzzing in CI")** — three pieces missing (F17's targets, a nightly pin, Q9's lane), all three landed this wave. Two rows state a *reading* rather than a tick (V1.1 covers 3 of 5 crates, the rest being pre-milestone stubs; V2.2 delegates line 168 to `MATRIX.json` rather than copying it)
- [x] **Q37** (S) Freeze-boundary rows into Q14's checklist (implements D84) ✅ 2026-07-28 — Q14 gained a normative-rows block: D84 §7, D88 §7 and D31 §10 spliced **byte-identically**, plus four authored rows. Writing the report-version row sharpened the problem it was meant to record — only **one** of the two couplings is machine-checked, and R32's implementation then overturned the row's own premise (the transcript constants are an independent axis and stay `0`). The corrected row keeps the superseded reading visible rather than quietly rewriting it
- [x] **Q39** (S) Pin the fuzz-lane nightly toolchain — the blocker sitting under F17/Q9/Q17, documented in two files and owned by neither ✅ 2026-07-28 — landed inside the Q9 lane: `fuzz/rust-toolchain.toml` = `nightly-2026-01-26`, dated not floating, governing `fuzz/` **only**. `rust-toolchain.toml` and the MSRV untouched; `core-dep-graph` byte-identical by cargo-tree diff
- [x] **Q38** (S) Report byte-format cross-check — D29 + R9's 21 pinned strings would otherwise freeze with **no** independent check at all; must run after R32 — after Q11,R32 ✅ 2026-07-28 — the report byte format was about to freeze with 21 byte-pinned strings and **no independent implementation checking any of it**. A stdlib-only reader now asserts D29's syntactic contract, nine properties per string, each traced to a numbered rule. **Its task text carried a wrong regex** — D29 rule 7's *enum* clause applied to *keys* — which rejects 20 of the 34 committed keys; at a freeze gate a checker failing 21 vectors invites fixing the bytes. Split and corrected at source with a permanent planted-fault guard. Recorded limit: it cannot detect a *uniform* key reordering
- [x] **Q40** ~~Machine-check the three coupled report-format version constants~~ **CLOSED 2026-07-28 as SUPERSEDED.** R32 disproved its premise: the equality it proposed would pin a coupling that does not exist and would go red the first time either version legitimately moved alone. The surviving hazard — a version constant whose doc comment promised something no check enforced — is closed by **Q53**, which asserts the *value* at both sites. That mattered more than expected: the `.mjs` runner compares its literal against the module's exported version **at runtime**, so bumping both kept the lane green and broke Q14's deliberate negative clause silently. Asserting their agreement was never enough. Q14's row was corrected by **R42**
- [x] **Q43** (S) Exercise every CI shell step locally, or move the logic into committed scripts — a `tamper-matrix` guard command was malformed since Q8 and sat latent because the lane had **never run remotely**; the guard worked, its own command had never been executed — after Q1,Q8 ✅ 2026-07-28 — 10 inline blocks moved to committed scripts; `check-ci-shell.py` enumerates **54 `run:` blocks across three workflows**. **The Accept scoped to `ci.yml` alone**, which would have closed the instance again — `advisory-cron.yml` carried a second copy of the `cargo deny` invocation. Best evidence it works: the lane reproduced the Q8 defect while building the guard (`-- vector_ -- --list`, count 0) and the local run caught it before any push
- [x] **Q41** (S) Enforce external-fixture provenance digests in CI — Q11 wrote the provenance records and the re-derivation scripts, but nothing *enforces* them offline — after Q11 ✅ 2026-07-28 — the four external T0 anchor fixtures had their digests in exactly one place each, their own `PROVENANCE.md` prose, and nowhere else; `FROZEN.sha256` structurally cannot cover them. The checker parses the digests out of `PROVENANCE.md` itself rather than duplicating them, so **both** directions go red. Discovery-by-marker had the very failure it guards against — deleting a `PROVENANCE.md` removed its directory from discovery — closed with the must-exist device
- [ ] **Q42** (S) Tighten the Unicode conformance anchor when CPython ships Unicode 17.0.0 — a tripwire, not scheduled work — after Q11 (M1)
- [x] **Q49** ✅ 2026-07-28 — **Q14's two Accept criteria were mutually exclusive**, proven on a scratch tree: rows N1/N2 live inside the `FREEZE-BOUNDARY` markers, byte-compared against D84 §7, so ticking them turned `--freeze-boundary` red. The gate was unrunnable as written. The marker — and only the marker — is now blanked on both sides; rule *text* still compares exactly. Four self-test cases bound the tolerance: a tick passes, a changed word or a **deleted** marker still goes red
- [x] **Q50** ✅ 2026-07-28 — both registry files under an append-only freeze digest. Until now the 19 D10 caps were pinned only by 'code and JSON agree', which a coordinated two-file edit satisfies forever — and D84 §7 puts those caps explicitly **inside** the freeze. Parser genuinely **shared** with Q6's, not copied. One deliberate re-bless, before any tag existed, recorded in its own commit rather than done silently
- [x] **Q51** ✅ 2026-07-28 — `--matrix` never read the status column, so a row could sit at `gap` through a milestone review with the lint green. That is exactly what happened to V3.4. Every row at or before the milestone under review must now read `covered`, cumulatively
- [x] **Q52** ✅ 2026-07-28 — **the mechanism D30's freeze never had.** 53 of 191 codes were renameable with a fully green suite, validated live rather than by grep alone. A methodological finding worth as much: the first planted rename used an uppercase suffix and went red in the **kebab-case shape guard**, not in rename detection — a detectability measurement that plants unrealistic names measures the wrong check
- [x] **Q53** ✅ 2026-07-28 — the pinned CBOR encoder is item 1 of Q14's own `Do` and was **asserted by no test**. The exact-pin class is now derived from `docs/dependency-policy.md` §1 rather than a hand list (17 members, 15 landed), plus the reverse direction: nothing may be exact-pinned without a policy row
- [x] **Q57** ✅ 2026-07-28 — D78/D79 promoted to real records and indexed; the enumerating check landed as `check-traceability.py --decisions`. Writing it corrected the entry's premise twice — three decisions are legitimately homed *inside* other records, 47 are legitimately open, and a naive sweep reports the Unicode surrogate `D800` in a comment as missing
- [x] **Q58** ✅ 2026-07-28 — all six line-number citations into the registry converted to section references **before** F4 moved ~30 sections and rotted them at once, plus a lint that fails on a reintroduced one. Residual stated, not dropped: the `MVP-SPEC.md line <n>` direction is **F44**'s
- [ ] **Q44** (S) `testdata/anchors/` needs a `PROVENANCE.md` when M2 captures real artifacts — the Q41 lane is discovery-by-marker and would silently skip it — after Q41,A25 (M2)
- [ ] **Q54** (S) Every registered vector kind must have a named cross-check vehicle or a recorded 'none possible' with its reason — extends F31 from 'commits CBOR' to 'has any vehicle' — after Q11,F31
- [ ] **Q55** (S) Reverse coverage for `crypto-` and `content-`/`fine-root-` — the two families no task owns; sequence with F37 as one mechanism — after F23,F24
- [ ] **Q56** (S) Generate `docs/ci-verification.md`'s context set from `ci.yml` — it disagreed with the workflow *and with itself* (17 vs 'still 16' vs the real 18) — after Q1
- [ ] **Q62** (S) Second-order CI-shell guard items surfaced by Q43 — after Q43
- [ ] **Q63** (S) Further CI-lane parity items surfaced by Q43 — after Q43
- [ ] **Q64** (S) Extend the freeze-boundary lint to D84's **F1–F4** block, or splice it programmatically the way §7 is — F1–F4 freeze at Q14 but are the *unchecked* copy, which is how a corrected source sat un-mirrored for a whole wave — after Q37,A27
- [x] **Q66** (S) Fix the traceability lane's vacuous self-test — its gate-state fixtures hardcoded `- [ ]`, so Q14's own tick made all three mutations match nothing and the lane exited 1 from the freeze commit onward, invisible because `local-gate.sh` never ran it — after Q49,Q43,Q13 ✅ 2026-07-31 — fixtures now derive the mutation from the row's current state (strip / flip / re-case; the vacuity guard that caught this is untouched), proven non-vacuous in BOTH gate states on a scratch tree; `local-gate.sh` gained the lane; `ci-lanes.sh traceability` green at HEAD. Found by the 2026-07-31 five-agent verification audit; the lane's substantive checks (matrix / decisions / freeze-boundary) were green throughout — only the self-test harness was red
- [x] **Q14** (S) Execute the M0 format-freeze gate; annotated `format-v1-freeze` tag — after Q5,Q6,Q8,Q11,Q12,Q13 + P9/P10 + C3/C11 + F4 + G1 ✅ 2026-07-28 — **M0 format-v1 freeze executed**; annotated `format-v1-freeze` tag created locally. Gate: fmt · clippy · **1221 tests** · wasm32 · native↔WASM bit-match over 12 vectors · vector freeze + self-test · **registry freeze digest (new)** · tamper matrix **COMPLETE (0 pending)** · cross-check **0 discrepancies** with a report dated to the freeze commit · traceability all 16 M0 rows `covered` · ci-shell + ci-lanes. D17/D29/D30 formally frozen with mechanisms, not declarations. **Nothing pushed**
- [x] **Q67** (S) D28 rationale 3's totality claim is unsound — "a third party cannot alter a bundle's reveal shape undetected" is false for a **consistent** strip (remove a file's reveals + its `touched_files` entry + its `full_reveals` entry together and neither the D80 leak arm nor the D82 missing arm can fire). Not a vulnerability — the bundle is unsigned by design and narrowing is honest — but D74 rationale 3, D74's wave-6 audit table and R4's entry all inherited the false clause, and **D74 was decided partly on its strength**. Records fix, not a code fix — after R4 · **2026-07-31 adversarial review, finding 7 (medium)** ✅ 2026-08-01 — corrected at source in seven places (D28 r3 + dated amendment with the counterexample and the inconsistent-edit→named-code table; D74 r3/provenance/wave-6 audit; decisions README D74 row; D82 r3 scope note; file_stages.rs row-5 docs; MATRIX.json D74 `why`; the R4 line here): detection is total over *inconsistent* edits only — consistent narrowing is invisible by design, harmless, and byte-identical to an honest narrower bundle
- [ ] **Q68** (S) The report-kind vector set is **structurally closed** post-freeze — the executor demands all 11 `REQUIRED_SHAPES` per document while the frozen 21-case document may not change a byte, so no new report vector can be committed at all; decide the union-coverage rule (zero frozen bytes move) or record the closure until v2 — after Q6,R9,R30 · discovered by R53 2026-08-01
- [ ] **Q69** (S) **(M1)** Doc-named-test liveness — `ids.rs`'s naming-ban pointer named a test and a file that never existed; a discipline test asserting every rustdoc-named `fn`/`tests/*.rs` pointer resolves (the rustdoc instance of F42's registry-prose problem) — after F41 · discovered by F41 2026-08-01

## M1 — Storage (46 tasks)

Gate: scripted devnet E2E green (S17); kill/resume matrix green incl. real SIGKILL, no-double-pay Anvil-verified (S18); clean-tree restore from vault backup only (S19); UNANCHORED library-verify via A1; `init`/`seal --no-anchor`/`list`/`restore`/`vault export|import` working end-to-end; secret-hygiene harness (U21) green; `--no-anchor`×mainnet rejection proven at CLI and pipeline levels.

### Environments & pins (P)
- [x] **P15** (S) `self_encryption` containment: CI prohibition on any direct dependency + the `blake3` exact pin (re-scoped by D35; formerly "pin to ant-core's locked version + verify WASM-safety") — after P9,P14 ✅ 2026-08-01 — `blake3 = "=1.8.5"` landed (default-features off; wasm32 probe green, official spec vectors matched on both asm and pure paths; policy §1 row); dep-graph lane proves no antseal crate declares `self_encryption` (declared-manifest scan + resolved-parent check, red direction proven both ways); P7 lockstep clause retired, D6 GPL flag discharged per D35
- [x] **P16** (M) Local devnet environment: in-process nodes + Anvil, configurable count via a launcher crate behind a non-default feature (scripts/devnet/local-{up,down,reset}) + machine-readable env surface — after P5,P9 ✅ 2026-08-01 — **14 nodes proven stable in 6.2 s** (5-node smoke 1.6 s; first release build 12 m 45 s, ~19 min cold); THE M1 lockfile event executed, **128 → 736 packages** (docs/upstream/P16-lockfile-event.md: ant-node `=0.15.0` exact pin; tested-set precise-pins ant-protocol/saorsa-core/saorsa-transport — coupled drift proven; 4 unmaintained-class advisories D19-ignored, zero vulnerability-class); default `--workspace` graph unchanged (+1 shell crate only, tree-diff proven); contracts + 2.5 M ANT premine verified on-chain (31337); down/reset leave zero residue (proven by listing)
- [ ] **P17** (M) Arbitrum-Sepolia devnet environment (chain 421614) + funding runbook — after P5,P9,P16
- [ ] **P20** (S) Dependency-graph containment assertions for the devnet era: default `--workspace` build/test lanes never compile `ant-node`; `self_encryption` never a direct antseal dependency (D35's prohibition — P15 lands the check, this extends it); alloy pinned in lockstep with evmlib — after P15,P16,S2 · discovered by D35/D52/P16 planning 2026-08-01

### Storage & pipeline (S)
- [x] **S2** (M) Batch-first `StorageBackend` trait + Blob/CostQuote/PaymentReceipt/Address types; churn-boundary enforcement — after S1 ✅ 2026-08-01 — trait + full type set in antseal-net (D32 cap constructor-enforced with a typed error; D37 map-shaped receipt skeleton; D33 payment-RPC scope note in the churn-boundary rustdoc; InsufficientAnt/InsufficientGas distinct by type); ant-core dep deliberately deferred to S6, tests driven by a hand-rolled std-only block_on
- [x] **S3** (M) `MockBackend` with fault injection + call-order logging — after S2 ✅ 2026-08-01 — behind non-default `test-util` (house self-dev-dep pattern; cli-consumable per D34's sub-finding); deterministic median-×3 quoting with per-round fresh quote hashes, sequential sub-batch receipts (forceable cap), one-shot faults at every backend-reachable S16/S18 kill point, full call log + consumed ordering assertion; address fn injectable (S21 wires S4's rule as the default)
- [x] **S4** (L) Deterministic Autonomi address recomputation (BLAKE3-256, D32/D35 re-scope) in antseal-core (WASM-safe; seal-time + linkage-layer primitive) — after S1,P15 ✅ 2026-08-01 — `storage::compute_storage_address` = BLAKE3-256(blob bytes) with cap+1 a typed `ExceedsChunkCap` rejection; blake3 =1.8.5 pin activated (core normal dep, wasm32 green); `storage-address` golden-vector kind added post-freeze via the Q6 append path (7-rung ladder incl. 272/cap-edge/cap+1-rejection, generative i%251 inputs under D87's budget, pure-Python independent generator agreeing byte-for-byte) so the Q5 bit-match covers it with zero harness change; net's MAX_CHUNK_SIZE now aliases core's (cross-crate test); dep-graph lane extended with the no-RNG ban
- [x] **S5** (M) Network mapping arbitrum-one/arbitrum-sepolia/devnet → chain/contracts/RPC/peers + wallet keygen/import-validation/address ops (for U11) — after S1,P16 ✅ 2026-08-01 — split per the containment rule: pure data half (`network`: NetworkId/NetworkConfig/DevnetEnv, chain ids 42161/421614 pinned, evmlib-cited contracts, 10-key run-scoped devnet env parse incl. `.devnet/env` file form, unknown name hard error, NOT-Ethereum-Sepolia warning) in the DEFAULT graph — proven byte-identical `cargo tree` before/after; heavy half (`evm`: to_evm_network incl. panic-free Custom mapping, D44 WalletKey generate/import/address with acceptance gate ≡ the pinned `Wallet::new_from_private_key`, EIP-55 rendering) behind non-default `ant-backend` over the ant-core edge (recorded why: ant_protocol's single-pin-point rule); feature-path test agrees our constants ≡ running evmlib built-ins byte-for-byte; key material SecretBuf-only, secret-guard gains the devnet-export signature (5/5 planted)
- [x] **S6** (L) `AntCoreBackend` over prepare→pay→finalize in ONE adapter file; never data_upload/chunk_put — after S1,S2,S4,S5 ✅ 2026-08-01 — one impl file (`ant_backend.rs`, non-default `ant-backend`): external-signer flow primary exactly as D37/D33 rule it — per-sub-batch `payForQuotes` calldata signed+submitted over our own provider, receipt awaited in-band (block number from the awaited object), capture hook fires with the cumulative receipt before the next sub-batch, exact-allowance approve (never U256::MAX); client built `with_evm_network` (never `with_wallet` — upstream's wallet-gated paths structurally unreachable); `finalize_batch` = receipt-driven `chunk_exists` skip + journaled-proof `chunk_put_with_proof` to CLOSE_GROUP_MAJORITY (same path fresh + resume; targets re-derived by an unpaid quote round when unstaged, D37 D5); `get_data` = `chunk_get` + BLAKE3 integrity recheck; D36 staleness check (staged quote round line-matched, stale quote = typed Payment refusal, proven no-tx on devnet); upstream errors mapped w/ payment-class detail REDACTED (upstream embeds quote-hash hex) + ProofsExpired classifier (24 h window, unit-tested); new exact pins ant-protocol =2.3.0 / alloy =1.8.3 / bytes =1.12.1 (policy §1 rows, lockstep-with-ant-core rules; Cargo.lock +6 lines, ZERO new packages); dep-graph lane gained the containment check (forbidden call sites + ant_core-use allowlist, red-direction proven both rules); LIVE devnet (14 nodes): 3-blob quote→pay→finalize→get_data byte-identical, re-finalize nonce-unchanged, AlreadyStored zero-cost line, NotFound distinct — 15.8 s, txs approve 46 394 gas / pay(3) 152 566 / pay(1) 73 212
- [x] **S7** (M) PaymentReceipt completeness (tx hashes, block number, quote preimages, proof_bytes) + instant-capture semantics — after S1,S2,S6 ✅ 2026-08-01 — serde derives across every receipt/quote/address type (default graph — the pipeline journals without the backend feature; workspace serde =1.0.229 derive-only); at-rest shape = `JournalReceipt` envelope: version tag FIRST (field order documented as format), private fields force `seal()`/`open()` so a parser cannot skip the version check, unknown version = typed refusal; quote→tx map serialized as ordered pair list (BTreeMap order; codecs without byte-map-keys survive; duplicate key = typed parse error); codec itself stays U9's — determinism proven through the exact-pinned serde_json (byte-identical across encodes AND across identically-driven mocks); mock-level multi-tx round-trip (forced cap 2, 5 blobs → 3 txs) proves no field loss; LIVE devnet: completeness checklist green after a real pay (all four element classes populated), capture-consistency green — journaled proof_bytes parse via upstream's own `deserialize_single_node_proof`, parsed tx hashes/preimages(9 fields)/sidecars == flat fields byte-for-byte; ≥2-sub-batch test = 3 REAL sequential EVM txs (adapter cap seam 2, sanctioned mirror of the mock's): map covers all 5 quotes, per-landing cumulative hook captures [2,4,5] blob records with tx-prefix ordering, last hook payload == returned receipt (instant capture), nonce delta exactly approve+3; enrichment-failure: block numbers stripped → `backfill_block_numbers` restores captured values exactly, idempotent second run fills 0, wallet nonce unchanged (no payment), backfilled receipt finalizes; hygiene rows carried by the existing prefix-only Debug + redaction tests
- [x] **S8** (M) ANT+ETH balance queries + preflight with distinct InsufficientAnt/InsufficientGas; true-complete-quote guarantee — after S2,S5,S6 ✅ 2026-08-01 — `AntCoreBackend::balances()` (ERC-20 balanceOf + eth_getBalance over the re-exported Wallet surface) and `preflight(&CostQuote)` returning a passed-`PreflightReport` or the DISTINCT typed shortfall (ANT checked first, documented deterministic order); `pay()` re-runs the same preflight internally so a skipped consent-side check still cannot move uncoverable money (devnet-proven: pay on an ANT-less wallet refuses with InsufficientAnt and the nonce stays put — zero transactions); reports are serde structured data for U14/`--json` (EvmAddress20 gained string-form serde: 0x-lowercase-hex, parse-validated on deserialize); true-complete-quote at this layer = quote covers exactly the handed-in blob set (asserted) — the pipeline composes the full set later (S12); LIVE devnet: funded-wallet balances/preflight numbers consistent with the quote; preflight-passed 2-blob seal completed with **on-chain ANT delta == receipt.storage_cost_atto ≤ quoted** and **ETH delta == receipt.gas.gas_cost_wei ≤ gas estimate** (Anvil balance deltas); shortfalls proven with fresh funded-by-construction wallets (ETH-only → InsufficientAnt with available=0; ANT-only → InsufficientGas with available=0 — same below-requirement state the entry's "drain" wording targets without mutating the shared dev wallet); messages actionable, hex-free, key-free; full 4-test devnet suite 49.8 s / 40 blocks on one 14-node run
- [x] **S9** (M) Pin MAX_CHUNK_SIZE + self-encryption thresholds against the real API (size-ladder devnet test) — after S4,S6,P16 ✅ 2026-08-02 — `tests/storage_constants.rs` (9: derived half default, linked half `ant-backend` — cap == `ant_protocol::chunk::MAX_CHUNK_SIZE`, sub-batch cap == upstream's 256) + `devnet_s9_size_ladder_addresses_and_byte_identity` GREEN on a live 14-node devnet (272 / 65 552 / 4 194 064 / 4 194 304 B; quote 7.3 s, pay 236 ms 1 tx, finalize 22.4 s, fetch 5.8 s; addresses byte-match S4 at quote AND finalize, divergence names the size class; cap+1 refused twice offline, nonce unchanged). **Investigation verdict: `QUOTE_MAX_AGE_SECS` does not exist in pinned ant-node 0.15.0 and the single-node path has NO proof-age gate** — the only pinned window is merkle's 7-day `MERKLE_PAYMENT_EXPIRATION`, off antseal's path; the ~24 h is client-side self-imposed policy. D37 corrected (dated note), D36 pointered, four in-crate citations amended at source
- [x] **S10** (M) Seal journal: staged ciphertext BYTES + nonces + addresses before payment; state machine; durable ordering — after S2,S4,S7,U9 ✅ 2026-08-02 — `SealJournal` interface + records + state machine in `antseal_cli::pipeline` (D34), `VaultJournal` over U9's store; fine `SealState` (staged/anchored/paid/finalizing/complete/abandoned) lives in journal entry 0 with U9's `WorkState` as a derived coarse mirror written in the same call — no U9 schema event; entry-key namespace chosen here (0 state, 1 plan, 2 manifest blob, 3+unit_id units) with the canonical blob order derived from it, never stored twice; illegal transitions refused **before** any write, terminal states are sinks, re-writes idempotent (6x6 table asserted as data); D43 reclassification proven to move no byte (staged entry file compared before/after); **the expected blob set is recorded, not derived** — a listing cannot see a deletion, so the plan record carries `unit_count` and `verify_all_staged` checks against it (the test written for the accept row found this); receipt codec = exact-pinned serde_json over net's `JournalReceipt` envelope (S7 put the derives in the default graph for this consumer); versioned canonical CBOR, future versions refused unparsed, malformed bytes error not panic; hygiene asserted on disk (planted marker + fixture W greped out of every record file). 17 acceptance tests over a real Argon2id vault + 10 unit tests
- [x] **S11** (L) Resume: byte-identical re-upload, finalize with recorded receipt, abandon guard (never re-encrypt under a journaled nonce) — after S6,S10 ✅ 2026-08-02 — resume from every kill point over the S10 machine, sharing one post-staging tail with S12's fresh seal; **resume never touches `W`** (`RecordedIdentity` carries no secret), so "never re-encrypts, never re-signs" is structural rather than promised; staged-bytes gate (S4 address recompute over the *recorded* expected set) runs before anything touches the network — failure abandons with zero backend/gate/consent calls; D36 unconditional re-quote + re-consent with pay-argument identity captured and compared on both sides; **decline writes no transition** (asserted: still `Anchored`, then resumes successfully paying once); D37 proofs-expired implemented not merely surfaced — re-quote + re-consent with the stranded-payment flag, then one re-payment, declining leaves it incomplete. Deviation recorded: the entry's "re-run the anchor, *then* continue" would anchor before consent — implemented in the normative order (quote → consent → anchor → pay) since anchoring makes submissions that outlive the invocation. Also lands `SealError` (per-class variants mapping many-to-one onto U2's frozen table, **no new exit class** — asserted), the 8 named fault barriers, and the `ConsentHook` contract. 15 acceptance + 6 unit tests
- [x] **S12** (L) Seal pipeline orchestration in exact normative order; ciphertext-only egress; fault-injection barriers — after S2,S4,S7,S8,S10 + G14/C/F ✅ 2026-08-02 — the staging half composed over G14/C6/C8/C9/C10/C14/F5/F6/S4 (first production caller of all of them); barrier trace + mock call log pin both sequences exactly; **staged bytes journaled before the quote**, **consent before the anchor gate** (this corrected S11's just-landed order — both now run one tail); ciphertext-only egress asserted from outside (no fragment of any input, the title, the app version or a recorded path appears in any uploaded blob); plan validation = argument checks → pure content-model assembly → D32's per-blob cap, satisfying both this task's "before any journal write/consent/anchor/backend call" and D32's "before consent/anchor/quote" (a unit's size is unknowable before splitting), proven by an isolated-vault byte-identical fingerprint + empty barrier trace; `--split` suggested only for a normal unit of a text file; D49 as a two-arm `JournalSink` type so "a dry-run writes nothing" is a property of one enum — byte-identical vault, exactly one backend call class, trace ending at `PostQuote`, and plan errors firing identically under the flag. **Two decisions recorded inline in tasks/S.md**: `ant-backend` forwarding feature for S17's in-package harness (cargo cannot feature a dev-dep conditionally; declares no new edge, S23's allowlist untouched); and no new core signing seam was needed — C2/C12/C13/C14's production derivation is already un-gated. 14 acceptance tests
- [x] **S13** (S) `--no-anchor` zero-anchor path + pipeline-level arbitrum-one rejection — after S12 + A1/F13 ✅ 2026-08-02 — **the skip is the pipeline's, not the injected gate's**: in no-anchor mode the gate is never called, so no injected gate — proven by driving both development networks with a *refusing* gate double, which would fail the seal if it were called; the mainnet guard sits in plan validation beneath U13's CLI check, so driving the library directly cannot mint a paid-for mainnet seal with the gate bypassed (byte-identical isolated vault, empty barrier trace, zero calls anywhere) and it fires under `--dry-run` too — an argument rule, not a payment rule; UNANCHORED shown to be a shape not a degradation (empty anchor set → `is_unanchored()`, no headline time; manifest decodes complete). Devnet half of the accept row is S17's
- [x] **S14** (M) `restore` engine: fetch, decrypt, verify commitments, write originals (raw-mirror preferred) — after S5,S6 ✅ 2026-08-02 — `antseal_cli::pipeline::restore` (D34): `RestoreEngine` over a backend + U9's store, returning **verified bytes or a typed per-file failure and nothing else** — "the affected file is not written" is a property of the return type, not a discipline; network-first with D43 §5's verified-cache fallback (both sources pass the same S4 address recompute *before* the AEAD, so the fallback costs no trust — and a substituted network blob silently heals through the cache when one exists); raw-mirror bytes preferred wherever a mirror exists (BOM+CRLF+NFD file restores byte-identical), and a mirror-less text file's tiling is checked against **both** `canon_commit` and `raw_commit`, turning `needs_mirror`'s silent claim into a proven one; manifest resolved vault-copy-first then fetch-by-journaled-address + `k_m`, bound to the work by `seal_id` **and** recomputed `work_id`; zero commitment code emitted (C's `verify_*_commit`, `decrypt_unit`, `strip_padding`, S4's `compute_storage_address`); no file I/O at all (U20 owns D48). **Deviation, and it blocks S19**: a `vault import`ed *complete* work carries **no journal entries at all** (U12 applies D43 §3's cache exclusion to the whole journal area, keys 0–2 included), so neither the plaintext manifest nor the encrypted manifest's `{address, nonce}` survives the round trip and the manifest becomes unlocatable on a content-addressed network — S14 Accept row 4 is unsatisfiable today, recorded as **S29** with the one-line fix. New error class `restore-verification-failed` (35) minted rather than U2's predicted 40s slot (that band stays U30's; D48 §6 severity is an explicit rank, never numeric). 12 acceptance + 4 unit tests
- [x] **S15** (S) `--live` persistence primitive: re-fetch + byte-compare, structured per-blob report — after S6 (R11 consumes) ✅ 2026-08-02 — `antseal_net::live::check_persistence` in the DEFAULT feature set (rides `StorageBackend` alone): four serde outcomes (identical / different+`first_diff_offset` / not-found / fetch-error), never fails as a whole, one `get_data` per DISTINCT address with input-order rows; 10 mock tests + a devnet row proving the same primitive over `AntCoreBackend`
- [x] **S16** (M) Mock-level pipeline invariant matrix (every barrier: ordering, idempotency, no-double-pay, abandon) — after S3,S10–S12 ✅ 2026-08-02 — 13 rows, every assertion reading the mock's call log / payment counter / journal, never timing. **Found a real double-payment defect on its first run**: resume branched on the journal's *state tag*, but D37's capture hook journals the receipt **inside** `pay`, so a crash in the post-pay/pre-receipt-journal window leaves a durable receipt on a pre-pay tag — and the resume re-quoted, re-consented and paid again. Fixed: the journaled receipt is the authority on whether money moved, the tag is repaired from it. That barrier is now asserted in **both directions** (with the hook: exactly one payment; without it: two — which is what makes installing it a requirement, recorded as **S27**). Rows: 8 barriers, D37's mid-sub-batch kill (each sub-batch paid once, map complete, no hash paid twice), the four D36 rows incl. pay-arg identity and a stale-quote-never-paid row, D49's dry-run row, partial-store remainder, abandon-on-missing-bytes, and a 24-case proptest over kill/resume interleavings (all terminate complete-or-abandoned, none double-pays; no counterexample, so no shrink fixture). "Never re-encrypted" is not sampled — it is structural (resume holds no `W`)
- [ ] **S29** (S) **A `vault import`ed complete work cannot be restored — the manifest is unlocatable.** U12's export applies D43 §3's cache exclusion to the *whole* journal area (`export.rs`: `state == Complete ⇒ journal = []`, and the import validator hard-errors if a complete work carries any journal bytes), so a complete work loses entry 0 (state), entry 1 (the plaintext manifest) and entry 2 (the encrypted manifest's `{address, nonce}`) — and an encrypted manifest cannot be found on a content-addressed network without its address, which is not derivable from `W` or `work_id`. S14's engine has both manifest paths and neither has a source after import; S19's clean-tree drill therefore cannot pass, and M3 `reveal`-after-import has the same hole (R16 needs the manifest too). D43 §3's *reasoning* is entirely about content scale ("record scale (KBs)" vs "1–2× sealed content"), which the plan record is not — so the exclusion looks over-broad by accident rather than by decision. Recommended fix (one line each side): scope the exclusion to the staged-blob keys (`≥ MANIFEST_BLOB_ENTRY`), exporting entries 0–1 for complete works; the alternative is a `{manifest_address, manifest_nonce}` pair in U9's always-exported meta record. Either is a D43/D47 amendment + an export-format review, so it was **not** taken unilaterally by lane ε — after S14,U12 + D43/D47 amendment · discovered by S14 (lane ε) 2026-08-02
- [ ] **S27** (S) Nothing forces the seal pipeline's backend constructor to install D37's per-sub-batch capture hook (`AntCoreBackend::with_capture_hook`), and without it a crash in the post-pay/pre-receipt-journal window costs a **second payment** — S16 asserts both directions of exactly that. The hook is what makes the window empty; wiring it must be a checked obligation of U13/S17's construction path, not a convention — after S6,S12,S16 · discovered by S16 (lane ι) 2026-08-02
- [ ] **S17** (L) M1 E2E on devnet via library APIs: multi-file --split seal, restore, UNANCHORED verify, --live — after S6,S12–S15 + P16 + R5
- [ ] **S18** (L) M1 kill/resume matrix on devnet: pay/finalize kill, mid-upload kill, changed-source abandon, real SIGKILL — after S11,S16,S17
- [ ] **S19** (M) Clean-tree restore from vault backup only (E2E) — after S14,S17 + U12
- [x] **S21** (S) MockBackend default address fn → S4's `compute_storage_address` (mock↔S4 address-equality test; delete the stand-in digest) — after S3,S4 · discovered by S3 (wave-1 lane β) 2026-08-01 ✅ 2026-08-01 — `MockBackend::new()` defaults to the real BLAKE3-256 rule via net's existing core edge; `standin_address` deleted, seam docs replaced, `with_address_fn` kept for deliberately-divergent tests; equality test covers a 0..4096 ladder incl. 272 B plus the full quote_batch path; all prior S3 tests green unmodified; in passing, `codec::decode::all_code_exemplars` gained the cfg gate every sibling domain already had (bare-lib dead-code error that broke any per-crate clippy of a core-dependent crate)
- [ ] **S22** (S) Gate feature policy for heavy non-default features (devnet, ant-backend): local-gate's `--all-features` sweeps the ~700-package union into the required gate that containment keeps out of default lanes — after P16,S5 · discovered by S5 (lane δ) 2026-08-01; likely re-homed to Q (gate scripts)
- [ ] **S23** (S) Manifest-level exclusivity check for the upstream payment-stack edges: no crate outside {antseal-net, devnet-launcher} may DECLARE ant-core/ant-protocol/alloy/bytes (S2's accept is phrased at the dependency-graph level; S6's lane covers source tokens only — a fully-qualified-path consumer would slip it) — after S6 · discovered by S6 (lane ζ) 2026-08-01
- [ ] **S24** (S) Store-path resilience of the adapter's sequential majority PUT loop: keep-or-harden verdict from S17/S18 churn evidence (upstream's close-group PUT is concurrent with retries; ours is deliberately simpler over the public surface) — after S6,S17/S18 · discovered by S6 (lane ζ) 2026-08-01
- [ ] **S25** (XS) S20's bump procedure gains an explicit **proof-age re-verification step**: S9's constants suite cannot assert a constant that does not exist, so the absence of node-side quote-age enforcement (`QUOTE_MAX_AGE_SECS` / `validate_quote_timestamps`, 0 occurrences in ant-node 0.15.0) must be re-grepped by a human at every bump — a silently returning gate would make long post-pay resumes start failing with no test diff — after S9,S20 · discovered by S9 (lane θ) 2026-08-02
- [ ] **S26** (XS) Record in S16/S18 that the **expired-proofs path is mock-only by upstream necessity**, not by test convenience: no pinned node produces a genuine age rejection (S9's finding), so a devnet can never exercise it — state it where the matrices live, so nobody later "fixes" the gap by trying to age quotes on a devnet — after S9,S16 · discovered by S9 (lane θ) 2026-08-02

### Anchor stub (A)
- [x] **A1** (S) Zero-anchor seal contract + minimal `absent`/UNANCHORED aggregate path (pre-M2 state machine) — after F13 ✅ 2026-08-01 — `AnchorGate` trait + `NoAnchorGate` + the normative zero-anchor contract (mainnet-rejection MUST rule stated; U13/S13 enforcement cross-referenced) in antseal-anchor; `antseal_core::verify::aggregate` zero-headline-eligible flag (report bytes untouched — derived data only); F13's committed empty-anchor + every-anchor-kind vectors library-verify UNANCHORED; no code path mints a headline time from an empty/ineligible set (tested); core wasm32-clean

### Verify-side live check (R)
- [x] **R11** (S) Library live-check wrapper over S15 (manifest-shaped for M1; bundle-shaped at M3) — after S15 ✅ 2026-08-02 — `antseal_net::live::manifest`: unit table + ciphertext map → labelled S15 records → `LiveVerdict` (`Divergent` > `SomeMissing` > `Inconclusive` > `AllPersisted`, worst established fact wins); exact coverage both ways, empty refused, encrypted-manifest blob covered, addresses taken from the manifest and never recomputed (that is R20's stage); placement argued in tasks/R.md (net, not the cli lib — shape adapter, not a D34 driver; R59 is the M3 re-home candidate); no report-format change, no new verify code

### CLI & vault (U)
- [x] **U1** (M) `antseal-cli` scaffold with the FULL canonical clap surface frozen day one (later commands stubbed) — after P5, P1 ✅ 2026-08-01 — full canonical tree + D39/D41 amendments as `antseal_cli` lib (D34) with thin main; clap 4.6.5 caret-per-policy (no color/wrap_help — deterministic help); 12-node help snapshot committed + parse-rule tests (18); stubs return typed "not implemented until M<x>"; tracing stderr-only via RUST_LOG
- [x] **U2** (M) Exit-code scheme + thiserror CLI error taxonomy (distinct token/gas/anchor/vault/resume codes) — after U1 ✅ 2026-08-01 — 24 classes, distinct codes 1–34 (40–49 reserved for U30), every 2026-08-01 decided class carried; import-refused-existing-vault routes to consent-not-obtained per D51's prompt-class table; token≠gas in code AND message; Display snapshot (34 exemplars) committed, no secret-typed fields; --json emits one provisional error object with identical exit code
- [x] **U3** (M) `--json` framework: one-document contract, schema registry per command, non-TTY rules — after U1,U2 ✅ 2026-08-01 — envelope v1 {v, command, network, ok, result|error} with U2's error object finalized UNCHANGED inside it; prompt-class registry on the same exhaustive match as command_name (compile-enforced completeness; consent/auth/config rows per D51 + the one deliberately channel-less prompt = import's overwrite, asserted unique); abort-not-hang harness drives all 10 commands × plain/--json × closed stdin against the real binary under a real 120 s watchdog (typed class per cell, stdout purity both modes); committed per-command envelope fixtures + shape test (ANTSEAL_BLESS); single machine-mode detection point over ε's stdin-isatty primitive; documented exemptions: --help/--version intrinsic, unparseable argv gets a null-command envelope iff the --json token is in argv (the U2-noted refinement closed)
- [x] **U4** (M) Config file: network mapping + TSA/endpoint override slots; precedence flag>config>default — after U1,U5 ✅ 2026-08-01 — config.toml beside the AEAD (D42), consuming S5's PURE NetworkId (no contract/RPC-default duplication — deliberate deviation from the entry's looser wording, per lane direction; net's pins stay the single source); `--network` became Option so flag>config>default is mechanically testable (explicit-flag-equals-default row included; help snapshot re-blessed); envelope `network` now config-resolved end-to-end; hand-parsed strict TOML subset (no new deps) with per-line typed errors — NEW class `malformed-config` exit 17, missing=defaults, malformed=hard error every command, unknown keys warn on stderr; validated reserved slots ([networks.<id>] rpc_url → S6, [anchors] tsa_urls → U26, [verify] endpoints → U30) + initial_config_text for U11; D47 cross-test: exported config survives import and still resolves; docs/config.md
- [x] **U5** (M) Vault store layout, versioned header, atomic writes + lockfile — after U1 ✅ 2026-08-01 — layout with the D42 three-beside partition structural (closed BesideFile enum; store/ = ciphertext-only), versioned header (frozen magic+[version,body] envelope, opaque capped KDF slot for U6, D50 wrap registry, golden vector + 12-case tamper matrix, newer-version → code 16), atomic temp+fsync+rename with deterministic kill matrix, flock-backed unlink-free lockfile (kernel-arbitrated staleness); AEAD/splice tests land with U6/U9
- [x] **U6** (M) Vault encryption: Argon2id m≥256 MiB t=3 p=1 / scrypt N≥2²⁰, 16-B salt, KDF header bound as AAD (downgrade = auth failure) — after U5 ✅ 2026-08-01 — D40 executed: frozen params + 16-B salt in a golden-vectored KDF block, pre-auth caps (1-TiB header dies pre-allocation, exit 14), typed low-RAM error at create AND unlock (exit 13, failpoint-tested), RFC 9106/7914 KATs incl. N=2²⁰, key-check record makes wrong-passphrase/tamper uniform (exit 12), record AEAD binds header+identity (D42 splice matrix at cipher layer), blake2 residue probe red-first-proven (65-byte chaining-state leak without the forced feature); measured 0.68 s argon2id / 2.4 s scrypt release; lowered-m Accept superseded by caps-at-floor (deviation note inline in tasks/U.md)
- [x] **U7** (M) Passphrase UX: no-echo prompts, strength floor, non-interactive supply, zeroizing buffers — after U6 ✅ 2026-08-01 — D41 byte semantics frozen in code (1 KiB raw cap, one-LF/CRLF strip, empty/NUL/LF/CR rejects — file, pipe, and cap-edge variants all exercised over real fds), rpassword =7.5.4 pinned for the no-echo prompt (argued: termios vs unsafe; /dev/tty only after stdin-isatty gates), create-time confirmation with typed mismatch + fd path skips it (D41 §5), strength floor DECIDED: 12 bytes length-only (zxcvbn rejected, rationale inline), prompt-parity proven against a real vault (prompt-created, fd-unlocked), machine-mode primitive for U3 (json ∨ non-tty ∨ fd 0), sentinel test seals the error surface; scripted init+seal drive deferred to U11/U13 by construction (commands don't exist yet)
- [ ] **U8** (M) Optional keyfile / OS-keystore wrap for W (recorded in AAD-bound header) — after U6,U7,U11
- [x] **U9** (L) Per-work record store: W, journal area, receipts (atomic fsync write), anchors, paths, costs; read APIs — after U5,U6 ✅ 2026-08-01 — one dir per work keyed by seal_id hex: versioned meta (W + network + state + D45 identity block + D36 consent record + optional work_id/cost), opaque journal entries (put/get/list/delete, canonical-decimal keys), receipt slot (single atomic fsync'd write — proven readable byte-identical across a fresh reopen), anchor slots; the U5-deferred D42 splice matrix executed at file level (5 swap axes all fail auth, neighbors still open); D43 reclassification = mark_complete state-tag write (journal bytes proven untouched); enumeration = readdir + small meta decrypts, proven to survive garbage journal/receipt bodies; 5-state fixture set round-trips across reopen; receipt/journal CONTENT stays opaque bytes by the task's own boundary (S7/S10 own shapes — deviation note inline in tasks/U.md)
- [x] **U10** (S) Arbitrum wallet key under its own vault sub-key; narrow accessor — after U6,U9 ✅ 2026-08-01 — HKDF-SHA256 sub-key under a frozen v1 label (tripwire-tested), record rides the D42 cipher under `RecordIdentity::Wallet` (key isolation proven by a main-key forgery test; splice both directions; blast-radius decoupling both directions); `WalletKeyHandle` = the M1 sign-capable handle (no Clone/Display, redacted Debug, zeroized, one accessor) with the evmlib-validation seam documented at the accessor — no net/backend edge enters cli
- [ ] **U11** (M) `init`: vault create, wallet generate/import, address + funding instructions, network config — after U4,U6,U7,U9,U10
- [x] **U12** (M) `vault export`/`import` (single encrypted backup; incomplete works survive) — after U5,U6,U9 ✅ 2026-08-01 — D47 end to end: magic + versioned det-CBOR header (U6 KDF block verbatim, fresh salt, AAD = magic‖header) + ONE AEAD over the plaintext logical state (uint-map/sorted-pair schema; import re-encrypts into a NEW vault — layout independence); D43 split enforced both directions (cache proven absent by size; loss-is-not-an-error post-import); mandatory self-verify = the full import validator over the synced temp BEFORE rename (a re-export can't destroy a good backup); import = refuse-existing-FIRST (absolute, all modes, code 10) → 1 GiB cap → pre-auth D40 caps pre-passphrase (bomb rejected with a panicking-closure proof) → validate-in-memory → temp-dir build → atomic rename → fresh-unlock post-verify; kill/corruption failpoints at export + import; real CLI handlers (machine-mode passphrase seam incl. --json-with-TTY) + spawned-binary E2E over --passphrase-fd 0; secret-guard exact-path exclusion same commit (the guard caught the draft test's literal — working as built)
- [ ] **U13** (L) `seal` orchestration in exact normative order (M1 scope: `--no-anchor` only; anchored path errors "arrives in M2") — after U2,U3,U9,U11,U14 + S12
- [ ] **U14** (M) Permanence-consent gate: file list, byte totals, true complete quote, both balances, exact permanence wording, --yes — after U2,U3,U13(interface) + S8
- [ ] **U15** (S) Seal warnings: title visibility, --no-fine-tree permanence, fine-tree cost estimate — after U13 + G10
- [ ] **U16** (S) `seal --dry-run`: all cheap steps + quote, zero side effects, zero vault mutation — after U13–U15
- [ ] **U17** (M) Resume/abandonment UX: resume plan, re-consent pre-pay, loud safety aborts — after U9,U13,U14 + S11
- [ ] **U18** (S) Vault loss/theft double nag + first-seal export nag — after U12,U13
- [x] **U19** (M) `list`: states (complete/incomplete/abandoned/UNANCHORED), per-work cost — after U3,U9 ✅ 2026-08-02 — `antseal_cli::listing` + the first non-vault CLI handler: work-id, title, date, network, state badge (UNANCHORED and degraded folded in), per-work cost, and — for unfinished seals — the **two clocks** saying opposite things (pre-pay: nothing paid, the quote is stale by design, resume re-quotes (D36); post-receipt: RESUME PROMPTLY, the proofs are PUT-usable ~24 h and past that finishing costs a second payment (D37)); the resume hint reproduces D45's invocation identity verbatim (paths + shaping flags + `--network` when non-default, session flags deliberately absent, conservative shell quoting). **Reads both state tags**: S10's fine tag when the journal still has it, U9's coarse mirror when it does not — which is the only reason `list` survives a `vault import` at all (a complete work's export carries no journal entries; same root cause as S29, and `list` was written to tolerate it rather than to fix it). Date = the D36 consent timestamp, the only clock the always-exported meta record carries (deviation recorded — `claimed_time` lives in the journaled manifest and does not survive an import). Amounts ride as exact decimal strings in `--json` (atto-ANT ≫ 2^53); U25's `pending_anchors` slot reserved as null; no vault lock taken (read-only — `list` must not hang behind a running seal). Registered `--json` fixture is rendered by the real renderer, not hand-written. 8 acceptance tests over a five-state fixture vault + snapshot, 4 unit tests
- [x] **U20** (M) `restore` CLI wiring (output dir policy, per-file verification report) — after U2,U3,U9 + S14 ✅ 2026-08-02 — `antseal_cli::restore_out`: D48 §1–§6 end to end. Work-scoped default dir; defensive lexical re-rooting (clean → strip absolute/prefix → **reject** surviving `..` or empty → join), **every target computed before any write** so an intra-work collision aborts with the directory still untouched; per-file byte-aware policy comparing against *exactly the bytes restore would write* (the raw-mirror rendition, never the canonical one) — absent → temp+rename, identical → confirmed with no mtime churn, differing → refused and left EXACTLY as it was, verification-failed → never written (there are no bytes to write: S14 returns none); no prompts; convergent re-run exits 0; severity-ordered exit class as an explicit rank. Human + `--json` per-file report (`restored`/`already-restored`/`refused-overwrite`/`verification-failed`/`fetch-failed`/`write-error`/`malformed-record`), registered fixture rendered by the real renderer. **Deviation**: the command's *network seam* is not built — S6's backend is behind the non-default `ant-backend` feature, is async with no runtime declared in this crate, and must carry D37's capture hook (S27); joining those is U13/S17's construction path, recorded as **U36**, and `commands::restore` refuses there with a specific message *before* asking for a passphrase. The whole policy half runs over any `StorageBackend` (`run_restore` is generic) and is driven end to end against `MockBackend`, which is how D34 says the M1 E2E drives things. 11 acceptance tests (incl. seal→wipe→restore→re-run and D48's three-state matrix) + 4 unit tests
- [ ] **U36** (M) **The CLI has no storage-backend construction seam**, so no command can actually reach Autonomi. Three things have to be joined and none of them is a single command's business: S6's `AntCoreBackend::connect(&NetworkConfig, &WalletKey)` sits behind the non-default `ant-backend` feature (S22's gate); it is `async` over ant-core's tokio client while `antseal-cli` declares no runtime (the dep-graph lane keeps the *normal* graph free of async/network crates, so an optional runtime under the feature is a dependency-policy event); and the constructed backend **must** carry D37's per-sub-batch capture hook or a post-pay crash costs a second payment (S27). `seal` (U13) and `restore` (U20) need the identical object, so it is one function: `crate::backend` declares it and documents the three obligations, U13/S17 fill it. Until then every network-touching command refuses there with a specific message rather than implying an outage. — after U13,S6,S17,S22,S27 · discovered by U20 (lane ε) 2026-08-02
- [ ] **U21** (M) Secret-hygiene harness: sentinel secrets never appear in stdout/stderr/logs/JSON at any verbosity — after U2,U7,U9,U10,U13

### CI strategy (Q)
- [ ] **Q15** (M) Devnet E2E execution strategy (CI job vs scripted local gate; log capture) — after Q1 + S17

## M2 — Anchors (35 tasks)

Gate: anchor golden vectors + wasm32 bit-parity green (A22 — spec's explicit M2 exit criteria); all 7 anchor tamper rows implemented + registered (A21/Q18); real-endpoint smoke incl. two-day OTS pending→upgraded cycle complete (A25); minimum-anchor gate live in `seal` with abort-before-payment proven (U22/A20); `status --upgrade` + opportunistic hook + `list` nags working (U23–U25); DER/`.ots` fuzz targets in CI (A23/Q17).

### Pins (P)
- [ ] **P18** (S) Pin `opentimestamps = "=0.2.0"` scoped codec-only; wasm32 viability check — after P7,P14

### Anchors (A)
- [ ] **A2** (M) Anchor artifact + verdict data model (7 states, eligibility flags, OnlineEvidence inputs) — after A1
- [ ] **A3** (S) `antseal-anchor` crate scaffold + HTTP substrate (timeouts, retries, typed errors)
- [ ] **A4** (S) DER `TimeStampReq` constructor (messageImprint = anchor_digest, nonce, certReq) — after A2
- [ ] **A5** (M) Strict-DER parsing: TimeStampResp/CMS/TSTInfo/X.509 with hard caps; BER rejected distinctly — after A2 + D-A8 pins
- [ ] **A6** (M) Pinned TSA root store: versioned, compiled into core, injection API for tests — after A5 (built with test roots; real roots land via A7)
- [ ] **A7** (S) Collect real TSA root certs with verified provenance (FreeTSA P-384 chain, DigiCert, alternates per D-A5) — with A6
- [ ] **A8** (L) CMS SignedData verification: signed attrs, messageImprint, nonce, ESSCertID(v2), critical EKU; RSA + ECDSA P-384 — after A5
- [ ] **A9** (L) X.509 single-path validation to pinned roots with genTime LTV (valid-at-stamping-cert-since-expired distinct) — after A6,A8
- [ ] **A10** (M) TSA HTTP capture client with immediate in-core verification (only verifying tokens count toward the gate) — after A3,A4,A8,A9
- [ ] **A11** (M) `.ots` codec wrapper: strict limits, op execution, digest-commitment check — after A2,P18
- [ ] **A12** (S) Embedded Bitcoin header offline check → `attested` (never headline-eligible offline) — after A11
- [ ] **A13** (M) OTS calendar submission client (≥2 calendars, merged pending `.ots`) — after A3,A11
- [ ] **A14** (L) OTS upgrade polling + attestation merge + upgrade-time header embed (must-agree fetched) — after A11–A13,A16
- [ ] **A15** (M) Opportunistic upgrade engine (bounded budget, never fails host command) + `status` data backend — after A14
- [ ] **A16** (M) Must-agree two-endpoint primitive + esplora pair (blockstream.info + mempool.space) — after A3
- [ ] **A17** (M) Arbitrum two-RPC tx confirmation — advisory only, never an anchor — after A3,A16 + S7
- [ ] **A18** (M) Per-anchor verdict state machine `evaluate_anchors` (pure; verify_at parameter; per-anchor states + eligibility for R — aggregation is R17's) — after A2,A8,A9,A11,A12,A16,A17
- [ ] **A19** (S) Receipt classification: SupportingEvidence class, never headline, unaffected by online confirmation — after A2,A17,A18
- [ ] **A20** (M) Anchor submission orchestrator + minimum-anchor gate (≥1 verified TSA or abort pre-payment; --force-degraded; --no-anchor rules) — after A1,A10,A13
- [ ] **A21** (L) M2 anchor tamper matrix: all 7 rows with distinct outcomes — after A8,A9,A11,A12,A18,A24
- [ ] **A22** (M) Anchor golden vectors + wasm32 verification parity — the spec's M2 exit criteria — after A18,A21,A25
- [ ] **A23** (M) cargo-fuzz targets for TimeStampResp + `.ots` parsers — after A5,A11
- [ ] **A24** (M) Mock TSA / mock calendar / stub esplora+RPC servers for CI (no real endpoints in CI) — after A3,A6
- [ ] **A25** (M) Real-endpoint smoke runs + record canonical fixtures (two-day OTS cycle; FreeTSA P-384 + DigiCert proven) — after A10,A13,A14,A16,A17
- [ ] **A26** (S) TSA root-store versioning + update process (append-only; release checklist hook) — after A6,A7 → continuous

### Verify integration (R)
- [ ] **R12** (S) Wire the anchor stage into `verify_bundle` (replaces M0 stub; receipt → supporting-evidence slot) — after R5 + A18

### CLI (U)
- [ ] **U22** (M) Wire anchor stage into seal: ≥1-TSA-or-abort before pay, --force-degraded, degraded reporting — after U13 + A20
- [ ] **U23** (M) `status <work-id> [--upgrade]` — after U3,U9 + A15,A18
- [ ] **U24** (M) Opportunistic OTS upgrade hook on EVERY CLI invocation (never delays/fails host command; no prompt) — after U1,U5,U9,U23 + A15
- [ ] **U25** (S) `list` pending-anchor nags (only-strong-anchor-pending rule) — after U19,U23
- [ ] **U26** (S) TSA-list override from config into the anchor stage — after U4,U22

### CI (Q)
- [ ] **Q16** (M) Anchor CI lanes (mock-only; no-real-network policy) + real-smoke runbook — after Q1,Q13 + A24
- [ ] **Q17** (S) Fuzz lanes extended with DER + `.ots` targets — after Q9 + A23
- [ ] **Q18** (S) M2 anchor rows registered in the tamper-matrix tracker; combined distinctness re-run — after Q7,Q8 + A21

## M3 — Reveal + verifier (21 tasks)

Gate: R27 suite green — reveal subset verified offline via CLI AND page (playwright) with string-identical verdicts, zero-network page run, endpoint-disagreement case on both; verdict wording snapshot-frozen (R18); redaction view complete (R19); page reproducible build with published hash (R25) and deployed at the canonical URL (R26); `show`/`reveal`/`verify` commands complete (U27–U30).

### Reveal & page (R)
- [ ] **R13** (L) Bundle builder (pure, self-verifying; generation-side isolation by construction; exact per-shape content sets) — after R5,R6 + C7,G11,G12,F9
- [ ] **R14** (M) Builder property tests (isolation, mirror rules, path discipline; independent ancestor-checker) — after R13
- [ ] **R15** (S) Disclosure-preview computation (rows + totals + mirror-rides-along; consumed by reveal and `show`) — after C9 + U9
- [ ] **R16** (L) Reveal-flow library API: selection resolution, mirror-id rejection, ciphertext gathering, builder invocation (CLI wiring is U28/U29's) — after R13–R15
- [ ] **R17** (M) Verdict aggregation: [H] map, ONE headline (earliest), >48 h divergence, UNANCHORED, receipt class, claimed-time exclusion — after R12
- [ ] **R18** (M) Frozen authoritative verdict-wording set + snapshot tests (shared verbatim by CLI and page) — after R17
- [ ] **R19** (M) Redaction-view model + CLI renderer (position+size always; sized blackouts; committed placeholders) — after R5,R18
- [ ] **R20** (M) Storage-linkage stage: offline address recomputation (units + re-encrypted manifest); rendered distinctly — after R5,R18 + S4,C10
- [ ] **R21** (L) Verify orchestration library API: offline, --online must-agree overlay + promotion, --live composition (CLI wiring + exit codes are U30's) — after R11,R17–R20 + A16–A18
- [ ] **R22** (S) wasm-bindgen binding surface (verify → serialized report; online-evidence entry; build info) — after R5,R17–R20 + P14
- [ ] **R23** (L) Verifier page: plain HTML/JS, drag-and-drop, offline-first FULL verification, redaction DOM, all versions — after R22,R18,R19
- [ ] **R24** (M) Page online mode: browser-fetch must-agree overlay, user-overridable endpoints, no --live affordance — after R23 + A16,A18
- [ ] **R25** (M) Reproducible page build + published SHA-256 + footer self-hash + CLI cross-check advice — after R23 + P6,P14
- [ ] **R26** (S) Deploy page to chosen host at the ONE canonical URL (single shared constant) — after R25 + P3
- [ ] **R27** (L) M3 E2E suite: CLI+page parity, offline zero-network run, endpoint-disagreement, partial+full reveal shapes — after R16,R21,R23,R24 + Q19

### CLI (U)
- [ ] **U27** (M) `show <work-id>` unit preview (kinds marked, snippet provenance labeled) — after U3,U9 + R15
- [ ] **U28** (L) `reveal` command wiring over R16's library API (arg parsing, output, canonical URL print) — after U2,U3,U9,U27 + R15,R16
- [ ] **U29** (S) Reveal disclosure consent + receipt-exposure warning — after U28 + R15
- [ ] **U30** (M) `verify` CLI command (vault-less, never prompts; verdict exit-code mapping; --json fixture) — after U1–U4 + R21

### CI & copy (Q)
- [ ] **Q19** (M) Playwright page-verification CI lane (offline-enforced; artifacts on failure) — after Q1 + R23,R25
- [ ] **Q20** (M) Positioning-copy style guide + repo-wide lint (with U31) — after R18 + U31

## M4 — Hardening & release (17 tasks)

Gate = **Q34 evidence bundle**: Sepolia-mode E2E green; exactly ONE mainnet smoke seal verified end-to-end from a clean machine using only the released signature-checked binary + hosted page; disk-loss restore drill passed; release published with mainnet default; traceability matrix 100 % green.

- [ ] **Q21** (M) Finalize the threat-model document (all 8 mandated topics + assumptions + residuals) — after Q12,Q20
- [ ] **Q22** (M) README + install + product-limits/disclosure docs — after Q20,Q30,Q31
- [ ] **Q23** (S) "Funding your wallet" doc (ANT + ETH on Arbitrum One; dev networks) — after Q20
- [ ] **Q24** (S) Vault-loss AND vault-theft doc pages — after Q20,Q21
- [ ] **Q25** (S) Wallet-hygiene guidance (fresh address per work/client; receipt exposure) — after Q20,Q21,Q23
- [ ] **Q26** (S) TSA defaults-and-alternates doc with recorded caveats — after Q20 + A26
- [ ] **Q27** (S) Format-stability policy doc (versions verifiable forever; Unicode-table retention; bump criteria) — after Q6,Q14
- [ ] **Q28** (S) Full-corpus copy audit: positioning + single canonical URL everywhere — after Q20–Q27 + U31/R25
- [ ] **Q29** (S) License decision + files (≥ permissive for core + verifier-web) — recommend deciding pre-M0 (D6)
- [ ] **Q30** (M) Signing keys + binary-signing pipeline (sha256sums + minisign/cosign; custody doc) — after Q1
- [ ] **Q31** (L) Release workflow: signed artifacts, double reproducible wasm build, versioning scheme, release checklist — after Q1,Q30 + R25,R26
- [ ] **Q32** (L) Script the M4 gate: Sepolia E2E + clean-machine procedure + disk-loss drill — after Q22,Q30,Q31 + P17,S19,U12,R26,A25
- [ ] **Q33** (S) Fund the gate wallets (fresh addresses; external action) — after Q25,Q32
- [ ] **Q34** (M) Execute the M4 gate: mainnet smoke seal, clean-machine verify, drill, SHIP — after Q13,Q21,Q28,Q31–Q33
- [ ] **Q35** (S) ≥10-external-users success-metric plan + tracking — after Q22,Q34
- [ ] **Q65** (M) Publish-scope decision + the pre-public scrub it gates — **triggered by any move to make the repo public** (one of the three options for branch protection, which is plan-blocked). `MVP-SPEC.md` **cannot** be removed (126 refs + 13 citations inside the frozen registry; the traceability lane resolves against its line numbers); only `MVP-SPEC.orig.md` and `SPEC-REVIEW.md` are unreferenced. Repo is private and nothing is exposed today — deferred by the maintainer 2026-07-28 — after Q22,Q28,Q27
- [ ] **U32** (M) CLI release freeze: exit codes/JSON schemas/help snapshots frozen; final hygiene + positioning audits; drill-feedback fixes — after U1–U30
- [ ] **U33** (S) Windows `--passphrase-fd` for non-stdin fds: implement (handle plumbing) or formally record the stdin-only limitation — after U7 ⛔ D72 (release-target set; discovered 2026-08-01 by U7 — `/dev/fd` has no Windows equivalent; fd 0 works everywhere)
- [ ] **U34** (S) Vault-global bookkeeping record slot: new cipher record class + store slot + D47 payload key — the machinery U18's export-performed flag needs and U9/U12 deliberately did not ship — after U9,U12; land with or before U18 (discovered 2026-08-01 by U12's payload design: the store has no vault-global non-wallet record, and the v1 export schema has no bookkeeping key; pre-release the payload may extend within format v1 while both sides move together — post-release it is a version bump, recorded in vault/export.rs)

## Continuous (6 tasks)

- [ ] **P19** (S) Weekly upstream-bump check (scheduled job + runbook; never auto-bump) — joint with Q36
- [ ] **Q36** (S) Weekly upstream-bump chore operation (report/issue; bump procedure doc) — joint with P19
- [ ] **Q10** (S) cargo-audit/deny RUSTSEC watch, permanent (extends P13; licenses completed by Q29)
- [ ] **S20** (S) ant-core churn watch: bump-impact procedure confined to the adapter file; E2E as bump gates
- [ ] **R28** (S) Per-released-format-version verification kept green (CLI + wasm + page) forever
- [ ] **U31** (S) Positioning/copy conformance: string catalog + banned-token lint (with Q20)

---

## Decision register

Unresolved decisions are blockers-in-waiting: each must land by its due milestone. On resolution: check it, record outcome + date, update the blocked tasks in `tasks/*.md`. (Merged from all nine domains; joint owners shown.)

### Due pre-M0
- [x] **D1** Product name: `antseal` with upstream written blessing vs non-ant rename (P — blocks P2, P3, C12 context string, F4 format strings, U1 binary/dir; gates the M0 freeze) — **Resolved 2026-07-27: `antseal`, maintainer-confirmed FINAL** (third path — no upstream blessing; deviation + residual risk recorded; blessing request downgraded to optional courtesy; `sealstone` fallback retired; availability re-verified at confirmation: 5 crates FREE + dry-run green, .org/.dev FREE). C12 `"antseal-manifest-v1"`, F4 format strings, U1 binary/vault-dir now UNBLOCKED on the name (frozen at Q14). History: provisional 2026-07-27 → deadline re-anchored (request unfiled at M0 start) → maintainer confirmation same day (docs/decisions/D1-product-name.md)
- [x] **D2** Repo hosting platform + CI provider (assumption: GitHub + Actions) (P — blocks P4, P8) — **Resolved 2026-07-27**: GitHub + GitHub Actions; private repo `aed900/antseal` (docs/decisions/D2-hosting-ci.md; hosting re-created under the maintainer account and all commit identities normalized to it before first push — see the D2 amendment)
- [x] **D3** Repo root layout: `code0/` as workspace root vs `antseal/` subdir per the spec tree (P — blocks P4, P5) — **Resolved 2026-07-27**: repo root = workspace root = the spec tree's `antseal/`; no nested subdir (docs/decisions/D3-repo-layout.md)
- [x] **D4** Rust edition + MSRV policy (P — blocks P6) — **Resolved 2026-07-27**: edition 2024; toolchain pinned =1.92.0; MSRV = pinned toolchain, moves only via P7 bump procedure (docs/decisions/D4-edition-msrv.md)
- [x] **D5** CLI crate publish name: spec's `antseal-cli` vs bare `antseal` for `cargo install` (P — reserve both in P2; recorded spec-disagreement) — **Resolved 2026-07-27** as recorded deferral: spec tree stays normative (crate `antseal-cli`, binary `antseal`); both names reserved at P2; publish-name call at Q31 (docs/decisions/D5-cli-crate-name.md)
- [x] **D6** License choice — spec milestone is M4 (Q29) but P2's crates.io placeholders need a license: decide early, execute at M4 (Q/P) — **Resolved 2026-07-27**: per-crate — antseal-core/anchor/verifier-web `MIT OR Apache-2.0`; antseal-net/cli own code dual-licensed but distribution effectively GPL-3.0 while ant-core's mandatory `self_encryption` dep stays GPL-3.0; ⚠ P15 direct-dep plan would pull GPL into the permissive core/WASM page — options recorded, decide at P15/S4; files land Q29 (docs/decisions/D6-license.md) — **2026-07-27 (S1 finding)**: GPL exposure in ant-core's graph is TWO crates — `self_encryption` 0.36.0 AND `evmlib` 0.9.0 (ant-core itself MIT/Apache-2.0); P15/S4 assessment must cover both

### Due M0 (format-freeze relevant — permanent once frozen)
- [x] **D7** Deterministic-CBOR crate + exact pin; in-house-codec contingency trigger (P10/F1) — **Resolved 2026-07-27**: `minicbor = "=2.3.0"`, features `alloc` only, derive off; strict rejections live in F3 on native probes; contingency trigger recorded (docs/decisions/D7-cbor-crate.md)
- [x] **D8** Complete v1 wire registry: key assignments, reserved ranges, signatures container, anchor-status enum values, byte-range representation, integer time encoding, explicit file_id in touched-file entries (F4, with C/G/A/S) — **RESOLVED 2026-07-28** (docs/decisions/D8-wire-registry-final.md), implemented at **F4**, frozen at Q14. All ten §13 feed items closed. **One genuine overturn**: `tsa_anchor` key 4 (`source`) is **removed from v1** — zero producers, zero consumers (the pipeline already refuses to copy bundle-recorded anchor metadata into the report), the only rendered `tstr` neither signed nor committed, and the only assigned key with no spec line; key 4 becomes plain reserved and its absence is asserted. Found `block_number` **under-determined** — S1's verified multi-tx path spans blocks, so two honest sealers emit different bytes — and gave it a total definition (minimum block over key 0). Found **D82 absent from the registry entirely**. Ruled the recorded `anchor_status` subset to be **no subset at all**: any narrowing would be a parse rule keyed on the field D79 forbids depending on. 30 substantive registry sections moved (predicted 30); 11 record errors listed in §17
- [x] **D9** GGM node-address representation ((level,index) vs bit-path) — co-frozen G8/F4 — **Resolved 2026-07-28: `(level, index)` pair of uints** — G8's implementation and the F4 draft converged independently; the index bits MSB-first ARE the root-to-node path, so it is the spec's own line-96 leaf rule generalized rather than a second convention, and canonical-CBOR uint encoding makes it canonical for free (a bit-path needs a pad-bit rule and its own rejection class). Wire shapes stay F4 Candidate A (`cover_entry`/`path_node` = `[level, index, bstr(32)]`); **co-frozen with the registry at Q14** (docs/decisions/D9-ggm-node-address.md)
- [x] **D10** Parser cap constants (bundle/manifest size, unit/file/anchor counts, list lengths, depth) (F11) — **RESOLVED 2026-07-28** (docs/decisions/D10-parser-caps.md): 19 frozen constants, one universal clamp rule `min(claimed_length, remaining_input)`, **18 new codes**, all enforced in verify stage 1 ahead of every AEAD/hash/signature; `u64` throughout so wasm32 and native cannot diverge. Cover/path caps are **derived, not guessed** (`2·⌈log₂ n⌉`; `size` is a uint so `d ≤ 64` bounds the honest max at 128 — the cap is 2×). Binding sizing case is the spec's n=10⁸ split text file with a mirror: 219 MiB, 14 % under the cap, the non-obvious term being padding. **Found that over-deep CBOR is unreachable through `SealProof::decode`** (fixed field types ⇒ `cbor-unexpected-type` at level 1), so F15's over-deep row is a direct-call row. Amended 2026-07-28 with three prose corrections from F11's implementation, incl. the ruling that the size cap precedes version dispatch (a >256 MiB v2 bundle reports `bundle-too-large`: cheapest rejection wins)
- [x] **D11** Autonomi address byte length pinned from ant-core source via S1 (F4/F5/F8) — **Resolved 2026-07-27**: **32 bytes** — `XorName = [u8; 32]`, BLAKE3-256 of chunk content (ant-protocol 2.3.0 src/chunk.rs:43; docs/research/S1-ant-core-api-survey.md)
- [x] **D12** Independent CBOR cross-check implementation (Python `cbor2` vs second Rust crate, dev-only) (F14/Q11) — **Resolved 2026-07-27**: Python `cbor2 ==6.1.3` dev-tool-only (independent lineage; boundary-value byte-identity proven; RFC 7049 ordering caveat recorded — moot for uint-only keys) (docs/decisions/D7-cbor-crate.md §D12)
- [x] **D13** `ed25519-dalek` 2.x vs 3.0.0 (P11/C11) — **Resolved 2026-07-27: `=3.0.0`** — strict-verify semantics probe-identical to 2.2.0 (byte-identical keys/sigs); single RustCrypto generation (signature 3 / sha2 0.11 / getrandom 0.4, shared with ml-dsa) vs 2.x demonstrably doubling the stack; ZIP-215 pubkey gap closed by C12 pre-validation (docs/decisions/D13-ed25519-dalek-pin.md)
- [x] **D14** Primary ML-DSA crate (ml-dsa vs fips204) + whether the Ed25519-only `sig_policy` fallback ships (C11) — **Resolved 2026-07-27: primary `ml-dsa =0.1.1`** (wasm32 probe PASSED with executed native↔wasm bit-match; complete FIPS-204 canonical rejection at `Signature::decode` — no C13 pre-validation needed); fallback `fips204 =0.4.6` pinned-unconsumed (byte-identical keygen/deterministic-sign proven — swap is a dep change, not a format event); **Ed25519-only fallback NOT shipped** (trigger did not fire) (docs/decisions/D14-mldsa-crate.md)
- [x] **D15** ML-DSA signing mode: hedged vs deterministic (affects golden-vector shape) (C16) — **Resolved 2026-07-27: deterministic (rnd = 0³²)**, implemented at C13. Portable C16 vectors, a byte-invisible fips204 fallback swap, one signing discipline with Ed25519, and no RNG in the signing path. Residual (recorded, threat model): hedged mode's fault/side-channel hardening is forgone — acceptable for a short-lived CLI process on the sealer's own machine with per-work keys. **Not format-encoded**, so a future version may switch without a format bump (docs/decisions/D15-mldsa-signing-mode.md)
- [x] **D16** Non-canonical-Ed25519-S rejection mechanism: crate parse-time vs explicit pre-validation (C12) — **Resolved 2026-07-27: explicit pre-validation layer in C12** — probe: dalek `Signature` parse is infallible on both 2.x/3.0.0 (S ≥ L checked only inside verify, opaque error), so parse-time rejection cannot produce the distinct `NonCanonicalSignature`; C12 pre-checks S < L and A-canonicality (ZIP-215 gap: decompress→recompress→compare) (C11 report §9)
- [x] **D17** Registered `sig_policy` algorithm-ID numeric values + reserved slots (C14/F4) — **FROZEN 2026-07-28 at Q14**. `0 = ed25519` (because the fallback policy is `[0]`), `1 = ml-dsa-65`, `2..=15` reserved and rejected. Ratified in code at C14/F5 in wave 3; the freeze needed no new test — `crypto_and_manifest_registries_agree` and `sig_alg_wire_mapping_matches_d17` already pin both directions over the full id range, and both mapping fns are wildcard-free. What F4 changed is the 9 `pending-D17` status markers, now `frozen-v1`. The 16-value universe freezes too, because D10's non-cap on `sig_policy`/`pubkeys`/`signatures` depends on it
- [ ] **D18** wasm-bindgen surface location: feature-gated in core vs thin wrapper crate (P14/R22; final by M3)
- [x] **D19** cargo-deny vs cargo-audit (or both) (P13) — **Resolved 2026-07-27: cargo-deny only** (`=0.19.8` CI pin; advisories/bans/sources active, licenses stubbed → D6/Q29 with BlueOak-1.0.0/GPL-3.0 notes; every ignore carries justification + review date); **amended same day (C11 finding): GHSA/osv.dev sweep mandatory** — RUSTSEC covers only 1 of 3 ml-dsa advisories (docs/decisions/D19-advisory-lane.md)
- [x] **D20** `--force-text` semantics on invalid UTF-8 (lossy U+FFFD total transform vs descriptor flag) (G2) — **Resolved 2026-07-27: deterministic lossy U+FFFD (Unicode §3.9 maximal subparts); text-mode canonicalization total; NO descriptor flag** — lossy ≡ strict on valid UTF-8, so `kind=Text` alone determines verifier recompute semantics; `--force-text` is a seal-time classification gate only; truncated-BOM → leading-U+FFFD corner KAT-pinned (docs/decisions/D20-force-text.md)
- [x] **D21** Canonicalization micro-semantics: lone-CR handling, single leading BOM, pipeline order (G2) — **Resolved 2026-07-27: pipeline decode → BOM strip → EOL → NFC frozen; strip ALL leading U+FEFF** (contiguous leading run — deliberate deviation from the strip-exactly-one proposal, forced by the normative idempotence property on multi-BOM inputs); one-pass CRLF→LF then lone CR→LF (CR CR LF → LF LF); canonical form never begins with U+FEFF, contains no CR, NFC (docs/decisions/D21-canonicalization-micro-semantics.md)
- [x] **D22** Blank-line definition for `--split` (whitespace-only lines; separator attachment) (G6) — **Resolved 2026-07-27: blank = content is solely ASCII space/tab (empty included), CommonMark-aligned**; separator run attaches to the **preceding** unit, leading run to the first, trailing run to the last. Rationale: a visually blank `"   \n"` treated as content would silently merge two paragraphs into one unit, so a user revealing "one paragraph" would disclose two. Exotic whitespace (U+00A0, U+3000, VT, FF) is deliberately non-blank — no Unicode table to freeze. **Seal-time CLI semantics, not verification law** (R checks only tiling), so changeable without a format bump; each seal's resulting ranges are permanent (docs/decisions/D22-split-blank-line-definition.md)
- [x] **D23** Raw-mirror placement in manifest order (proposal: appended after the file's normal units) — frozen forever (G5/G7) — **Resolved 2026-07-27: the mirror is its file's LAST unit**, at most one per file. Keeps id order ≡ range order for the tiling set, matches construction order (the mirror decision needs the finished canonical rendition), and ratifies the spec proposal + F4 draft. **R must not rely on it**: verifier exemption is by `kind`, so a hand-built manifest violating placement still verifies correctly (docs/decisions/D23-raw-mirror-placement.md)
- [x] **D24** `--no-fine-tree` × `--split` on one file: silent single-unit vs hard error (G5/G6/U13) — **Resolved 2026-07-27: hard CLI error** at plan validation (before the consent gate, before any payment), naming the file and both flags; binary files under `--split` are NOT an error (splitting never applies to them). Silent single-unit would permanently discard requested reveal granularity — irreversible and discovered only at first reveal. **Model-side it is unrepresentable, not checked**: G5's `SplitEligibleText` witness makes the contradiction unwritable (docs/decisions/D24-no-fine-tree-split-conflict.md)
- [x] **D25** Normalization crate + exact Unicode data version + multi-version retention architecture (G1/P7) — **Resolved 2026-07-27**: `unicode-normalization = "=0.1.25"` = Unicode **17.0.0** (verified from crate bytes; machine-asserted); descriptor `unicode-17.0.0`; append-only registry, future versions vendored side-by-side, shipped tables retained forever (docs/decisions/D25-unicode-normalization.md)
- [x] **D26** Perf/memory budget constants (C₁, corpus size, ceilings) (G18) — **RESOLVED 2026-07-28** (docs/decisions/D26-perf-memory-budgets.md): the framing is overturned — **`C₁` does not exist**. Both instrumented peaks are exact closed forms (`peak_seed_stack_len == d + 1`, `peak_frontier_len == bit_width(n)`), verified exhaustively for `n = 1..=4096` with zero mismatches, so a tolerance constant would license exactly the regression the test exists to catch; the entry's `<= C₁·⌈log₂ n⌉` form is also false at `n = 1`. Corpus sizes from measurement: CI runs the **dev** profile, where G18's suggested 256 MiB is **6.3 hours** (13.5 min in release). **Nothing here is format-permanent and G18 is not a Q14 dependency.** Also found G18's task text unimplementable twice over
- [x] **D27** `verify_bundle` error mode: fail-fast (tamper-authoritative) vs collect-all (rendering) (R1/R5/R7) — **Resolved 2026-07-27**: hybrid — fail-fast typed first-error is the sole normative mode (tamper matrix/Q7 bind to it); `VerifyFailures` is rendering-only, primary-first, first element ≡ the fail-fast error (docs/decisions/D27-verify-error-mode.md)
- [x] **D28** Full-reveal strictness: bundle revealing all non-mirror units MUST carry file_salt (+s_root if fine tree) or hard-fail (R4/R7) — **Resolved 2026-07-28: STRICT, hard-fail** — v1 has no "reveal every unit but withhold the whole-file openings" shape. Decisive argument: `canon_commit`/`raw_commit` have **no other verification path** (`fine_root`/`unit_commit` are per-unit at R2, `path_commit`/ranges at R3), so optionality would not weaken the check — it would **delete** it, leaving two signed, anchored fields a sealer may fill with anything. Also: strict→permissive is a legal future relaxation, permissive→strict is not (line 123), so strict is the reversible direction. Predicate is over `kind = Normal` units only, with an anti-vacuity `N(F) ≠ ∅` clause (a hand-built size-0 file tiles with zero units and would otherwise be vacuously "fully revealed"); `s_root` required iff `FineTree::Present`. Reuses R1's existing `FullRevealMaterialMissing` variant — no new codes. Residual (recorded): one `file_salt` covers both `raw_commit` and `canon_commit` (spec line 95), so a canonical-only full reveal is an offline oracle over the raw forms that canonicalize to the shown bytes — bounded, and far smaller than the guarantee permissiveness would surrender (docs/decisions/D28-full-reveal-strictness.md)
- [x] **D29** Deterministic verification-report byte format (the bit-match contract) (Q4/Q5/R1) — **FROZEN 2026-07-28 at Q14** as report **v1** (`REPORT_VERSION = 1`, bumped at R32). Byte-deterministic compact `serde_json`: declaration-order fields, Vec/BTreeMap only, no floats, lowercase hex, kebab-case wire names; `serde =1.0.229` / `serde_json =1.0.151` exact-pinned **and now asserted** (Q53 — the pin class was declared in policy and checked by nothing). Three enforcement layers, the load-bearing one being `FROZEN.sha256`'s post-freeze refusal to modify the 21-string report vector. **Q38 closed the gap that mattered**: until this wave the format was to freeze with *no* independent check at all. Report **v2 is already scheduled** for M2's anchor states — line 123 promises v1 stays verifiable, not that v1 is last (docs/decisions/D29-report-byte-format.md)
- [x] **D30** Stable machine-readable error-code contract across F/C/G/A/R (Q7/Q8) — **FROZEN 2026-07-28 at Q14**, and the freeze is what finally gave it a *mechanism*. Formalized at `docs/testing/error-code-contract.md` in wave 3, but until **Q52** the append-only rule was enforced by review discipline alone: all three existing layers check **distinctness** and **shape**, neither of which a rename violates, and **53 of 191 codes (28 %) could be renamed with a fully green suite** — measured, then validated live (renaming one produced exactly one failure across 706 tests). `testdata/error-codes/v1/CODES.txt` now snapshots the universe, compared every run, additions-only. Census also settled that **a prefix is not a partition of the enumerators**: `fine-root-` has 9 codes, not 8, because `fine-root-rebuild-mismatch` is minted by R — a second standing exception to §2, recorded in §7 because the code is frozen and cannot be renamed
- [x] **D31** Independent cross-check vehicles for the non-CBOR surfaces + permanence (one-shot artifact vs CI lane; is ml-dsa↔fips204+ACVP "independent" for ML-DSA? — the CBOR vehicle is D12's) (Q11) — **RESOLVED 2026-07-28** (docs/decisions/D31-cross-check-vehicles.md): one vehicle per surface, graded **T0** (external oracle) / **T1** (independent re-implementation) / **T2** (two crates in one ecosystem). The grading dissolves the register's hard question — `ml-dsa`↔`fips204` is T2, so it was never independence but D14's fallback check wearing the wrong label. **ML-DSA-65 = NIST ACVP (T0)**, which makes it the *best*-covered surface rather than the worst; Q11's Notes had it backwards. `cbor2` **decodes only**; our own §4.2.1 encoder is the encoding authority, retiring D12's ordering caveat. Permanence: **both** — a dated one-shot report gating Q14 plus a permanent unconditional CI lane. Found the report byte format would have frozen with no cross-check at all (→ Q38). **Executed at Q11: 0 discrepancies across 13 rows**
- [x] **D74** Extraneous `s_root` on a `FineTree::Absent` full reveal: reject vs silently ignore (R4/F8) — surfaced 2026-07-28 by D28's planning pass as the one **forced** second choice (R4's typed classifier has nowhere to put it). **Recommended: reject**, following C14's present-set-==-required-set precedent; proposed `FullRevealSRootWithoutFineTree { file_id }` → `full-reveal-s-root-without-fine-tree` (single code — a `FileSalt` arm would be unreachable). Resolve with R4 — **RESOLVED 2026-07-28** (docs/decisions/D74-extraneous-full-reveal-s-root.md): **reject**, ratified — and the window is recorded as *already closed*, since the code has landed with a row behind it, so reversing to "ignore" would delete a code (contract §3) in the permissive direction (line 123). Three corrections to the record, incl. that row 3 is reachable **only** by stripping the whole §7.14 entry — "simplifying" it to a missing-key mutation would silently re-bind it from R's code to F8's `bundle-missing-key`
- [x] **D75** Full-reveal cover shape: does a full reveal ship per-unit covers **and** `s_root` (F8/F9) — surfaced 2026-07-28 (D28). D28 holds either way; this decides whether R4's `s_root` fine-root rebuild is load-bearing or defence-in-depth. Resolve with F8 — **RESOLVED 2026-07-28** (docs/decisions/D75-full-reveal-cover-shape.md): **BOTH** — a full reveal ships per-unit covers *and* `s_root`. Outcome confirmed, **consequences overturned twice**: the "additionally proves no extra leaves exist" claim is vacuous (both routes take `n` from the signed `size`), and the `s_root` rebuild is not merely defence-in-depth — `check_fine_root_rebuild` is the **sole** consumer of the disclosed `s_root` anywhere, so deleting it would recreate exactly the unchecked bundle-side material D74 mints a permanent code to reject. That is what makes D74 and D75 one rule rather than two opinions
- [x] **D76** Split `file_salt` into separate raw/canonical salts (C2/C6) — surfaced 2026-07-28 (D28's residual-risk analysis: one salt covers both `raw_commit` and `canon_commit`, so a canonical-only full reveal is an offline oracle over the raw forms that canonicalize to the shown bytes). **RESOLVED 2026-07-28: NO for v1** (docs/decisions/D76-file-salt-split.md) — outcome confirmed, **its recorded reason overturned**: the "new HKDF label" cost is near-nil pre-Q14, because C2's info encoding is injective *by construction* precisely so labels can be added safely. The arguments that hold: (1) both salts' presence rules would read the manifest, dropping registry §7.14 key 1 from tier [P] to [R] under D78 and breaking the fact D28 rule 3 and D74's single-code argument rest on; (2) the permanent reveal-shape code surface roughly doubles, 5 → 8–9; (3) **the split does not eliminate the leak** — the mirror's `true_length` publishes raw size in plaintext and its existence asserts raw ≠ canonical, so the oracle only resolves same-length ambiguity. Exposure is exactly one of four full-reveal cases and leaks EOL/BOM/normalization metadata, never content
- [x] **D77** Zero-non-mirror-unit file: structural rejection vs tolerated (R3/F5) — surfaced 2026-07-28 (D28). `verify::structural::tile` accepts a hand-built size-0 file with zero units (`cursor == size` at 0); **RESOLVED 2026-07-28: REJECT at F5** (docs/decisions/D77-zero-unit-file.md), code `manifest-empty-normal-units` via a fifth `ContainerField::NormalUnits` variant — **implemented same day**. The substance was corrected in the process: the shape this entry and D28 describe (size-0 file, zero units) is **already closed** by F5's `manifest-empty-units` and cannot reach R3 from a decoded manifest. The hole that actually survives F5/R3/R4 is the **mirror-only file** (`N(F) = ∅` with a non-empty unit table), which no record named — it permanently un-opens `canon_commit`/`raw_commit`, reinstating per-file exactly what D28 rationale 1 exists to prevent. So it was a hole, not hardening
- [x] **D78** May bundle schema validation open the embedded manifest? (F8/F9, D8 §13 item 8) — surfaced 2026-07-28 by the D8 bundle pass; **Ratified same day: NO.** At schema time the bundle treats `manifest` as an opaque `bstr`; the manifest layer decodes it and owns the `manifest-`/`cbor-` codes. Rationale: F3 already established outer/inner layered decode with distinct error classes and F6 keeps the body **as received** with no re-encode path — if bundle validation opened the manifest, manifest-level failures would surface inside the bundle's error family, and D30 makes code families **permanent**. This is not layering taste: it fixes which error family cross-side rejections land in, and F8's error enum needs it before it is written (docs/decisions/D78-bundle-schema-manifest-opacity.md — record written wave 7, Q57)
- [x] **D79** OTS upgrade group (`block_height`/`block_header`/`fetch_date`): free-standing all-or-nothing vs conditioned on `status == attested` (F8, A-domain semantics) — surfaced 2026-07-28 (D8 §13 item 9); **Ratified same day: FREE-STANDING.** The group is structural — all three present or all three absent — and the **parse rule never depends on `status`**, because `status` is sealer-written and the design says a verifier must not trust it; conditioning parse on it would let a sealer choose which shapes decode. Same derived-not-declared principle as D28. **Scope: the parse rule only** — what the status values mean and how the verifier derives the real one stays A-domain at M2 (docs/decisions/D79-ots-upgrade-group-presence.md — record written wave 7, Q57)
- [x] **D80** Must a revealed unit's owning file appear in `touched_files`? (F8/R4, D8 §13 item 10) — surfaced 2026-07-28. **Resolved same day at F8: REQUIRED**, on spec evidence — line 95 says `path_salt` ships whenever any reveal **touches** the file (so the recipient can verify the path), and line 121 defines only two rendering states (revealed content, or unrevealed files with the path withheld); the permissive reading needs a third state the spec never defines. The privacy objection that framed this entry **overstated the stake**: the plaintext manifest already exposes file identity, size and unit boundaries by design (line 95), so path-hiding would have hidden only the *filename*. Tier `[R]` — it needs the manifest, so D78 keeps the check out of F8; recommended code `revealed-unit-file-not-touched`, spelling and enforcement owned by R4/R5 (docs/decisions/D80-revealed-unit-touched-file.md)
- [x] **D81** `unit-decrypt-failed` has no cause discriminator, so two spec-mandated M0 tamper rows are indistinguishable (R8/Q8) — surfaced 2026-07-28 by Q8's completeness registry applying distinctness to **pending** rows, i.e. before either row exists. *Flipped ciphertext byte* and *swapped unit* both land on `unit-decrypt-failed` because `VerifyError::UnitDecryptFailed` carries no cause, so R8 as specified **cannot have both rows**. Options: (a) mint a cause discriminator — this **changes the frozen code set**, so it must happen before Q14; (b) keep one as a row and demote the other to an R10 property. **RESOLVED 2026-07-28: option (b), NO discriminator minted** (docs/decisions/D81-unit-decrypt-cause.md) — **overturning this entry's own recommendation**. The premise was backwards: the AEAD is not the binding mechanism for either mutation (line 103 puts all content/identity binding in the salted commitments) and both are independently caught at stage 4; the code names the stage that fired, not the attacker's intent. Two real candidate discriminators were evaluated and rejected with costs stated — the manifest's BLAKE3 chunk address (makes an M3 storage-linkage computation verdict-bearing at M0, requires interpreting a deliberately-opaque `ContentAddress` in the WASM verdict path, and is evaded by swap-then-flip) and speculative re-decryption (O(n²) on hostile input, blind to non-revealed donors, and makes the reported code a function of *unrelated* bundle contents). R stays at 84 codes. Outcome: one row, three recorded non-rows — R8 found a **third** collision of the same shape (wrong `unit_salt`) → R33. It also forced Q8's registry to gain a third case state, without which Q14's gate would have stayed red forever. Two related hazards are already recorded in `testdata/tamper/MATRIX.json` and need no decision — a G19/R8 pair on `fine-root-binding-failed` and a G19/R7 pair on `fine-root-over-broad-cover` each yield **one** row, because R's wrapper arms surface the inner code unchanged (error-code contract §2)
- [x] **D82** Is a `touched_files` entry for a file with **no** revealed unit legitimate? (R5/R7) — surfaced 2026-07-28 at R5. D80 settled only the converse (a revealed unit's file **must** be listed). Today the disclosed-path-without-reveal direction is **allowed** and renders as a placeholder; rejecting it would mint a permanent code for a rule no decision has taken, so R5 deliberately did not. Note it is a real disclosure shape — proving *a file by this path was part of the work* without revealing any of its content — so the permissive reading may well be correct. **RESOLVED 2026-07-28: REJECT** (docs/decisions/D82-touched-file-without-reveal.md), minting `touched-file-without-revealed-unit` / `VerifyError::TouchedFileWithoutRevealedUnit { file_id }` (R 84 → 85 codes, 26 → 27 variants), firing at R5 coherence group 1b — additive to the frozen order, asserted. **Implemented same day.** Decisive evidence: D80 argued from an *absent* rendering state while line 121 states the opposite outright (unrevealed files render as committed placeholders), and line 95 makes it universal. It is also the D74 shape in a second section — `path_salt` is a per-work constant, so a relay holding any bundle of the work holds a genuine `{path, path_salt}` pair and can splice it into a bundle whose sealer withheld that file, passing R3's `path_commit` check because it is authentic
- [x] **D83** A leaf-level GGM cover seed's upper 16 bytes are inert (G20/G23) — surfaced 2026-07-28 by G20's proof-mutation property at `PROPTEST_CASES=1024`, counterexample committed. A cover node at `level == d` covers exactly one leaf, so the verifier descends zero levels and reads `salt_i = seed[..16]`: **bytes 16..32 of that node's disclosed 32-byte seed are never examined**, and a bundle may carry anything there and still verify. Not a soundness or confidentiality break — it is a canonical-form problem: two byte-distinct `.sealproof` files carry the same proof and both verify, a `bundle-cover-seed-bitflip` row could never be landed as written, and 16 bytes per node are wasted. Leaf-level nodes appear in most partial reveals, including G11's first normative KAT. Options: (A) accept + document in registry §5, (B) require a fixed canonical tail, (C) encode a `level == d` cover payload as **16 bytes**, keeping 32 for `level < d`. **Format event either way — must be settled before Q14.** Recommendation: C, fallback A (docs/decisions/D83-leaf-cover-seed-tail-malleability.md). Owned by **G23** — **RESOLVED 2026-07-28: option B, the canonical zero tail** (docs/decisions/D83-leaf-cover-seed-tail-malleability.md) — **overturning the recommendation of C**. A `level == d` payload is `salt_i ‖ 0x00·16`: still `bstr(32)`, still tier [P] on length, with the zero tail a tier-[R] value rule; zero decoder changes, zero tier changes, one code minted. C loses on a fact no record had checked — MVP-SPEC.md lines **96 and 121** both fix "every GGM covering seed = 32 B" outright, so C is a **spec amendment**, not a registry change — and it would drop an exact byte length (§0's archetypal [P] check) to [R] for two fields, the same argument that decided D76, for a saving measured at **<= 0.95 %**. Scope widened beyond what the record named: the rule also binds `full_reveal.s_root` at `n == 1`. **Implemented same day at G23**
- [x] **D84** Are A's M2 `.ots`/DER structural limits format-permanent? (A5/A11/Q14) — surfaced 2026-07-28 by D10's analysis. A5 already promises hard caps (max response size, cert count/size, signed-attribute count, nesting depth) and A11 "strict limits", but both land at **M2, after Q14 closes** — yet those limits decide accept/reject on a **v1-frozen** bundle, which is the same argument that pulled D10's opaque-artifact byte caps into M0. **Recommendation: scope them explicitly as post-freeze and document artifact-internal limits as a named exception to spec line 123** — the alternative is guessing DER limits at M0 with no recorded real token to check against. Blocks A5, A11 and Q14's freeze *scope*. (This is the question D10's author proposed as "D83"; that number went to G20's finding, which landed a doc the same day.) — **RESOLVED 2026-07-28** (docs/decisions/D84-anchor-artifact-limits-permanence.md): the limits are **NOT v1 format surface** and do not freeze at Q14 — scoping confirmed, **mechanism overturned**: there is *no* named exception to line 123, because line 123 is never at risk (M0/M1 render every anchor `absent`, pinned on disk by R10, so no released verdict depends on artifact internals). Carving a permanent exception into a normative stability rule to buy nothing is a bad trade. What **does** freeze, and had to be decided now, is structural: rules **F1–F4**. Also ruled that A5/A11's promised caps **are** the existing D10 constants, removing four from the open set. **Recorded at A27**
- [x] **D85** `unit_commit` mismatch cause discriminator (R33/Q8) — **RESOLVED 2026-07-28: no code minted** (docs/decisions/D85-unit-commit-cause.md), the non-row ratified and the rule generalised to **every** salted-commitment opening in R's pipeline. The asymmetry R33 named is real — the salt is bundle-side, the commit manifest-side, the manifest signed — but it yields no attribution and the split would be **unsound**, not merely unhelpful: the verifier never holds `W`, so anyone holding the bundle can re-encrypt arbitrary plaintext under the supplied `k_u` and pass stage 1 (line 103). "Signature invalid" names no field either, since one signature covers the whole body, and neither half separates a third-party edit from an adversarial sealer — which *is* the threat model. A cause belongs in **rendering** (D27's `VerifyFailures`), never in the code set. R stays put — D85 minted nothing. (2026-07-31 correction: the entry's original closing "85 codes / 27 variants" was stale at writing — G23/D83's `fine-root-leaf-seed-tail-not-zero` had landed hours earlier the same day, so the universe at the freeze is **86 codes / 28 variants**, 27 of them exemplar-represented; `verify/error.rs` `DISTINCT_CODES = 86` is the source of truth.)
- [x] **D86** Does the decode **layer** reach the user-facing verdict? (F26/R9/Q7) — **RESOLVED 2026-07-28: it never enters the report**, and the two `layer()` accessors harmonise **upward** — total on both types, `Option` dropped, manifest side derived from a new `ManifestError::map()` (docs/decisions/D86-decode-layer-in-verdict.md). **Overturned F26's own proposal** to harmonise downward. D27 §4 makes the report exist only for a bundle that *passed*, and `layer` occurs **zero** times across all 21 pinned report byte strings, so F26's "expensive after Q14" premise was unreachable. The real defect was elsewhere: `body-unknown-key` and `envelope-unknown-key` are **one observable**, a live counterexample to the error-code contract's own §2 claim (→ F27). **Implemented same day at F26**
- [x] **D87** Bit-match vector carriage before v2 doubles the tree (F21/Q5/Q6/R28) — **RESOLVED 2026-07-28: keep `include_bytes!`**, with a 2 MiB per-version ceiling enforced by a `build.rs` panic rather than a comment (docs/decisions/D87-bitmatch-vector-carriage.md). **Overturned F21's framing by measurement**: carriage is **0.73 %** of the lane and 2.32 % of the artifact, while *executing* the vectors is **90.1 %**. The alternatives optimise the 1 % and each destroys a structural property — the module's zero imports, the crate's zero unsafe, and decisively "both sides execute identical bytes" as a **link-time fact** rather than a claim about the JS runner's plumbing (→ F28, F29). **Implemented same day at F21**
- [x] **D88** Disposition of the un-zeroized HKDF/HMAC key state (C22/C21 R1) — **RESOLVED 2026-07-28: option (d), which C22 did not list — enable `sha2`'s non-default `zeroize` feature** (docs/decisions/D88-hkdf-hmac-zeroization.md). **C22's and the audit's shared premise "no feature fixes it" is FALSE**: `digest`'s `buffer_fixed!` `ZeroizeOnDrop` arm emits a *marker impl with no `Drop`*, so the trait was never the mechanism — the wipe is ordinary drop glue reaching `Sha256VarCore::drop`, gated on a crate neither C21 nor C22 examined. Measured: 114 of 144 bytes of `Hkdf<Sha256>` non-zero, and **`W` sitting verbatim** in the extract context's block buffer, which R1 had recorded as merely "`W`-equivalent". Option (b) would not compile; (c) is strictly weaker alone. Also closed an unrecorded gap — every `ggm::child_seed` left its **parent seed fully recoverable** in a dropped hasher. **Implemented same day at C22/C23/C24**; four prose errors amended back into the record, one of which would have aborted a correct implementation

### Due M1
- [x] **D32** Blob↔address model (data-map style vs per-chunk) per ant-core's real storage model (S2/S4/S6; informed by S1) — 2026-07-27 inputs captured in the S1 memo (recommended: blob = one chunk, 32-B BLAKE3 address; `get_data` ↦ `chunk_get`) — **Resolved 2026-08-01** (docs/decisions/D32-blob-address-model.md): **chunk-level** — every blob is exactly one Autonomi chunk, address = BLAKE3-256(ciphertext), `get_data` ↦ `chunk_get`, and **no data-map path in v1**: the S1 lean is confirmed but its open oversize door is closed — a blob whose ciphertext exceeds `MAX_CHUNK_SIZE` (4 MiB; max unit plaintext 4 194 047 B) is a **seal-time plan-validation hard error** before consent, anchors, and quote, a duty that is not optional because upstream quotes and pays for unstorable chunks without complaint. Spec deviation recorded (lines 34/51/110/119 "via self_encryption" → BLAKE3-256); the large-blob path is parked to v1.1
- [x] **D33** Block-number acquisition locus: antseal-net pay() vs antseal-anchor RPC client (S7/A17) — 2026-07-27 inputs captured: no upstream payment API returns a block number; own `eth_getTransactionReceipt` mandatory (S1 memo) — **Resolved 2026-08-01** (docs/decisions/D33-block-number-locus.md): **`antseal-net::pay()` — the register's anchor-side framing overturned**: the payment RPC is not anchor HTTP. In the primary external-signer flow the block number is a field of the very tx receipt `pay()` must await anyway; the native-fallback and crash-backfill paths read `eth_getTransactionReceipt` over the payment RPC S5's own `NetworkConfig` already defines (`ant_protocol` `http_provider` re-export — no dependency beyond the alloy `Provider` trait pay's submission machinery needs). `antseal-anchor` gets **zero M1 work**; A17 stays M2/advisory and consumes the S7 receipt, never produces it. Spec line 52 flagged for amendment (capture → confirmation)
- [x] **D34** Seal-pipeline crate placement (CLI lib target vs orchestration module; must be library-drivable) (S12/S10) — **Resolved 2026-08-01** (docs/decisions/D34-seal-pipeline-placement.md): **`antseal-cli` gains a `[lib]` target** — the register's lean confirmed, but on grounds it never stated (it recorded no rationale at all): pipeline-in-core is structurally impossible (`CostQuote`/`PaymentReceipt` are churn surface and core never depends on net), pipeline-in-net violates the storage crate's spec-written charter (anchor/consent/journal surface excluded), and a dedicated orchestration crate breaks the cargo publishability chain while D72 is still open. The orchestration layer (S12 pipeline, S10 journal state machine, S11 resume, S14 restore — and M3's R16 reveal flow, forced out of core by the same dependency direction) is `antseal_cli` library code over injected interfaces (StorageBackend, the A1 gate, lib-defined ConsentHook/SealJournal); `main.rs` stays thin. Forced sub-finding: `MockBackend` must be consumable across a crate boundary under EVERY option → S3 feature-export note
- [x] **D35** self_encryption in core: direct pinned dep vs vendored address-derivation subset (S4/P15) — **Resolved 2026-08-01** (docs/decisions/D35-self-encryption-dependency-mode.md): **option (d) — the register's option space overturned: NO antseal crate depends on `self_encryption` at all.** Under D32 (blob = one chunk) S4's `compute_storage_address` is `blake3` alone (new exact-pinned workspace dep, default-features off); `antseal-net` takes addresses from ant-core's own returns and cross-checks them against S4. P15 re-scoped from pin to **CI prohibition** (`self_encryption` only ever transitive under ant-core, linked only into net/cli binaries); D6's CRITICAL GPL flag discharged by deletion — no GPL code approaches core or the WASM page. Decisive evidence: 0.36.0 carries mandatory tokio/rayon/tempfile/rand, so it is wasm32-dead regardless of license. Revisit triggers recorded (P19 relicense watch; the v1.1 large-blob decision)
- [x] **D36** Pre-pay resume re-quote/re-consent policy on cost change (proposed: re-consent) (S11/U17) — **Resolved 2026-08-01** (docs/decisions/D36-prepay-resume-requote-reconsent.md): **always re-quote, consent per-invocation — S11's "re-consent if cost changed" conditional overturned; U17's unconditional wording ratified** (the task-text split arbitrated). A journaled quote is never paid: nothing in the pay path checks age before moving tokens, and storers enforce ~24 h `QUOTE_MAX_AGE_SECS` — so re-quoting is mandatory anyway, and "unchanged" is not even well-defined below the totals. One invariant falls out: **consent is a single-use spend authorization consumed by the `pay()` it immediately precedes, in the same process invocation** — subsuming D37's proofs-expired re-consent and the no-consent-post-receipt rule as corollaries. Decline → `incomplete`, never abandon; "changed" is display-only (prior consented totals + drift flag from a new S10 consent record `{total_ant_atto, gas_estimate, consent_time, channel}`); `--yes` affirms the fresh quote; D51 governs non-interactive uniformly
- [x] **D37** Multi-tx payment handling (per-tx chunk cap; plural tx hashes) (S6/S7) — 2026-07-27 inputs captured: 256 transfers/tx, 64-chunk waves, 256 merkle leaves; `PaymentReceipt` needs `Vec<TxHash>` (S1 memo) — **Resolved 2026-08-01** (docs/decisions/D37-multi-tx-payment.md): caps confirmed against source (the 64-chunk waves are upstream's driver shape, not a cap we hit), but the register's `Vec<TxHash>` conclusion is **overturned as materially insufficient** — `finalize_batch_payment` consumes a `HashMap<QuoteHash, TxHash>` and errors on any gap, so the receipt's core is the quote→tx **map** plus per-blob `{address, peer quotes, proof_bytes}` plus per-tx block-number slots; a bare hash list can neither rebuild proofs nor resume finalize. `pay()` emits ⌈blobs/256⌉ **sequential** txs journaled per sub-batch as each lands; merkle payment mode **excluded** from the paid path (its proofs carry no tx hashes — the spec's "one Merkle-batch EVM tx" is a recorded deviation: common case, not contract); `Client::batch_pay` **forbidden** in the native fallback (its error path discards the partial paid map — drive `Wallet::pay_for_quotes` directly). **New finding: node-side proof validity ≈ 24 h (`QUOTE_MAX_AGE_SECS`) time-boxes every post-pay resume** → new proofs-expired state with re-consent before any re-payment (rule supplied by D36)
- [x] **D38** Test-ANT acquisition mechanism on Arbitrum Sepolia 421614 (P17) — **Resolved 2026-08-01** (docs/decisions/D38-sepolia-test-ant-acquisition.md): an ordinary **ERC-20 `transfer` from an existing holder — nothing else exists**: the deployed test token (0x4bc1aCE0E66170375462cB4E6Af42Ad4D5EC689C; payment-vault proxy 0xd742E8CFEf27A9a884F3EFfA239Ee2F39c276522, evmlib-0.9.0 src/lib.rs:58-76) has no mint function, upstream operates no faucet, and no bridge is involved. Primary route is maintainer-owned per the external-actions rule (community transfer request + account-gated gas faucets). Contingency, scripted and dormant: self-deploy both verified contract artifacts on 421614 via upstream's Custom-network machinery — a recorded deviation that never substitutes for the canonical-contract M4 gate
- [x] **D39** `init` interaction model: wizard vs flags (U1/U11) — **Resolved 2026-08-01** (docs/decisions/D39-init-interaction-model.md): **the dichotomy is overturned as a false choice** — `init` is a TTY wizard whose **every question has a flag/fd equivalent** (a pure wizard violates three committed M1 Accept criteria; pure flags cannot carry the passphrase). The v1 `init` flag set is enumerated as a deliberate U1 surface amendment: `--wallet <generate|import>` (default `generate`), `--wallet-key-fd <n>` (D41 mechanism, must differ from `--passphrase-fd`), the global `--passphrase-fd <n>`, one D40 KDF selector slot and one `--wrap` selector slot (default declined) — nothing else. Existing-vault refusal is absolute: no `--force`, the error names the path and says move/remove it manually
- [x] **D40** KDF selection mechanism + low-RAM (cannot allocate 256 MiB) behavior (U6/U11) — **Resolved 2026-08-01** (docs/decisions/D40-vault-kdf-selection.md): **Argon2id (m = 262144 KiB, t = 3, p = 1) sole default; scrypt (N = 2²⁰, r = 8, p = 1) only as an explicit init-time selection; low-RAM = typed hard error at create AND unlock — U6's fallback-prompt note overturned**: the fallback it imagines does not exist, because scrypt at its spec floor needs 1 GiB, four times Argon2id's 256 MiB. Adds **pre-auth KDF-header caps** (D10 precedent — a substituted header demanding 1 TiB is a resource bomb *before* the AEAD authenticates; the same caps serve unlock AND D47 import). Crates: scrypt `=0.12.0` (resolves onto the already-wiped sha2 =0.11.0 stack); argon2 0.5.3 **rejected** (a second un-wiped blake2/digest-0.10 stack) → recommend `=0.6.0-rc.8` plus a D88-style blake2 residue probe at the pin PR. Spec line 143 completion flagged (it names no scrypt r/p)
- [x] **D41** Non-interactive passphrase supply channel (env var vs fd vs keystore) (U7/U13) — **Resolved 2026-08-01** (docs/decisions/D41-noninteractive-passphrase-channel.md): a global **`--passphrase-fd <n>`** (stdin is the degenerate case `--passphrase-fd 0`). The env-var channel is **rejected on a measured mechanism**: on Linux `/proc/PID/environ` serves the execve-time snapshot, which no userland `unsetenv` or zeroize can wipe — an env passphrase contradicts spec line 143's zeroization mandate for the whole process lifetime. The OS keystore is not a scripting channel (stays U8/D50). Byte semantics frozen: read-to-EOF with a 1 KiB cap, strip exactly one trailing LF/CRLF, reject empty and interior NUL/LF, prompt parity (fd bytes ≡ what the prompt would have produced); the fd path skips the create-time confirmation prompt
- [x] **D42** Vault encryption-boundary partition (config/anchor artifacts inside vs beside the AEAD; hook-without-unlock consequence) (U5; affects U24 at M2) — **Resolved 2026-08-01** (docs/decisions/D42-vault-encryption-boundary.md): lean confirmed and sharpened — beside the AEAD sits **exactly** `config.toml` + the vault header + the lockfile; **everything else lives inside, including all anchor artifacts** (`.ots`, TSA tokens, fetch dates). The U24 consequence becomes a positive rule: the opportunistic-upgrade hook runs **iff the host command already holds an unlocked vault handle** — on a locked or absent vault it is a silent no-op, never a prompt, and it never creates `~/.antseal`. Rider: every AEAD in the vault binds the KDF header + record identity as AAD (splice resistance); whole-vault rollback protection is recorded out of scope
- [x] **D43** Staged-ciphertext retention after successful finalize (local cache vs prune) (U9/U27/U28) — **Resolved 2026-08-01** (docs/decisions/D43-staged-ciphertext-retention.md): **RETAIN** — on finalize success the staged bytes reclassify journal → cache in place (same bytes, state tag only), stay inside the AEAD boundary, and are **excluded from `vault export`**. The journal/cache split is the load-bearing line: journal bytes (work incomplete) are resume-critical, never prunable, always exported; cache bytes (work complete) are integrity-rechecked (S4 recompute) on every use with silent `get_data` refetch fallback, and their loss is never an error. No prune surface at MVP (`vault gc` = v1.1 — R16 needs whole-file ciphertexts for boundary Merkle paths, so pruning would force full-file fetches). The proposed S14 amendment (network-first restore with verified-cache fallback) **ratified at integration 2026-08-01**
- [x] **D44** Wallet import formats (raw hex only vs +mnemonic) (U11) — **Resolved 2026-08-01** (docs/decisions/D44-wallet-import-formats.md): **raw hex private key only in v1** (64 hex digits, optional `0x`, case-insensitive, k256 scalar-validated), with the accepted set defined as **"whatever the pinned evmlib/alloy parse accepts"** so import validation and the payment path cannot drift (evmlib wallet.rs:72-75 / alloy private_key.rs:224-230 — validation ≡ payment path). Mnemonic rejected for v1: the pinned upstream stack does not compile it in, it pulls two new payment-path crates (coins-bip32/39) into the exact-pin class, and derivation-path ambiguity is a silent wrong-wallet failure mode. Revisit trigger recorded
- [x] **D45** Resume detection keying + consent-on-resume rule (U17) — **Resolved 2026-08-01** (docs/decisions/D45-resume-detection-consent.md): detection is **automatic, keyed on recorded invocation identity** (network + exact ordered lexically-absolutized input-path list + seal-shaping flag set): an exact match auto-resumes; **any overlap that is not an exact match, or a flag mismatch, is a hard error before consent/payment** (D24 pattern — two new resume-safety error classes); disjoint proceeds as a fresh seal with a notice. Interactive selection and a `--resume`/work-id flag rejected (not in the canonical surface; the crash-re-run path must be zero-interaction-safe). Consent: pre-pay resume re-fires the **full U14 gate over a fresh quote** (register lean confirmed — agrees with D36, whose cost-comparison rule this record deliberately defers to); post-receipt resume never re-consents and needs no `--yes`
- [x] **D46** Directory arguments to `seal`: recurse vs files-only error (U13) — **Resolved 2026-08-01** (docs/decisions/D46-seal-directory-arguments.md): **files only** — directory, non-regular-file, and lexical-duplicate arguments hard-error at plan validation, before consent and payment (D24 pattern), identically under `--dry-run`; shell globbing is the recursion tool. Rejected not because recursion is unwanted but because every one of its sub-rules (iteration order, hidden files, symlinks, non-regular files — POSIX `readdir` order is unspecified and would reach the manifest file-table order) would be frozen per seal into a permanent paid artifact, with no human checkpoint left under `--yes`; error-now → recursion-later is the additive, reversible direction. G14/G5 may consequently assume regular files
- [x] **D47** `vault export` format (verbatim archive vs re-encrypted single file) (U12) — **Resolved 2026-08-01** (docs/decisions/D47-vault-export-format.md): **re-encrypted single file** — the Q2-reserved magic `ANTSEAL VAULT EXPORT` + a versioned deterministic-CBOR header (own KDF block, fresh salt, AAD = magic‖header) + **one AEAD over the full serialized logical vault state** (config INCLUDED, cache EXCLUDED per D43); same vault passphrase, proven by the unlock the export performs; keyfile factor preserved; mandatory post-write self-verify; import validates everything in memory under D40's pre-auth caps before an atomic install. The register carried no lean; decided on two properties the verbatim archive cannot have — **whole-backup authentication** (per-record AEADs cannot detect *absence*: a backup missing works imports "successfully") and a deliberate interchange format that does not freeze U5's on-disk layout
- [x] **D48** `restore` default output dir + overwrite policy (U20) — **Resolved 2026-08-01** (docs/decisions/D48-restore-output-policy.md): default output = the work-scoped fresh `./antseal-restore-<work-id>/`; recorded paths re-rooted under it by a **defensive lexical rule** (absolute prefix stripped, `..` rejected, intra-work collisions abort pre-write); overwrite policy **per-file and byte-aware** — an existing identical file counts as already-restored success (idempotent re-run), an existing different file is a per-file refused-overwrite error and is never touched, and files failing verification are never written (S14). Temp+rename writes; restore never prompts; processing continues past per-file errors and the exit code reports the most severe class present. In-place restore stays possible explicitly (`-o`), never by default
- [x] **D49** `--dry-run` network semantics: real quote_batch (proposed) vs offline estimate (U16) — **Resolved 2026-08-01** (docs/decisions/D49-dry-run-network-semantics.md): **real `quote_batch`**, confirming the register's lean and S12's Accept, with four sharpenings: dry-run is the real pipeline **truncated after quoting** (a prefix, not a parallel mode); a live **S12↔U16 contradiction found and resolved** (S12:160's journal-before-backend invariant vs U16:201's zero-vault-mutation — the invariant is scoped to the **paid** path, dry-run runs journal-disabled with its own S16 matrix row); dry-run runs the S8 preflight and exits with the **real** insufficient-ANT/-gas codes, making it a scriptable preflight gate; and the quoted cost is labeled **indicative** — the fresh-nonce argument proving a dry-run leaks nothing linkable to a later real seal also means the real seal re-quotes different addresses from different peers
- [x] **D50** OS-keystore platform scope for the W wrap (U8) — **Resolved 2026-08-01** (docs/decisions/D50-os-keystore-scope.md): **the framing is overturned — NO keystore platforms at M1**; the keyfile wrap ships alone, with zero new dependencies. The register's own question ("which platforms?") is unanswerable before D72 (release target set, due M4) without pre-committing D72, so the correct M1 answer is "no platforms yet, and the header makes later ones additive": U8's AAD-bound **wrap-mode registry** (0 = none, 1 = keyfile, 2 = os-keystore RESERVED, with a distinct "wrap mode not supported" error). Evidence against shipping now: keyring 4.x is churn-active; linux-keyutils is reboot-volatile (would brick the vault); machine-bound factors break D47's clean-machine drill. Platform scope + implementation = a recorded post-D72 follow-up (see the D72 row)
- [x] **D51** `--json` × interactivity contract (hard-abort without --yes: proposed yes) (U3/U14) — **Resolved 2026-08-01** (docs/decisions/D51-json-interactivity-contract.md): hard-abort **confirmed and widened** — machine mode (`--json` ∨ non-TTY stdin ∨ stdin consumed by `--passphrase-fd 0`) never prompts for **anything**: consent, passphrase, wizard questions, or destructive confirms, each prompt class carrying a declared non-interactive channel or a deliberate absence of one (`vault import` over an existing vault has **no bypass flag by design** — scripted overwrite-import unsupported in v1). The consent report always renders (stderr under `--json`); declined ≡ unobtainable = **one** consent-not-obtained exit class, distinct from passphrase-unavailable and usage; interactivity gated on stdin `isatty`, `/dev/tty` rejected; post-receipt resume is not consent-bearing and proceeds without `--yes`. U3 gains the prompt-class registry + an abort-not-hang harness over every command × machine-mode flag
- [x] **D52** Devnet E2E venue: CI job vs self-hosted vs required local gate (Q15) — **Resolved 2026-08-01** (docs/decisions/D52-devnet-e2e-venue.md): **register lean overturned** — a **required LOCAL gate** (`scripts/e2e-devnet.sh`, mandatory before merging storage-touching changes, scripted evidence per the Q14 pattern) plus a **scheduled, non-required GitHub-hosted job** at reduced node count on the fuzz-nightly pattern, landing only if its measured runtime fits the minutes budget. A per-PR required CI job is rejected: branch protection 403s on Free-private (a 20th required context is unenforceable), the hosted runner is the same 2-core class against a 688-package graph vs the 10 GiB cache cap, and S18's SIGKILL matrix is flake-prone there; self-hosted rejected (the only candidate host is the 2-core dev machine itself — a standing runner daemon adds surface and contention for zero capability the local gate lacks). Q15's "cached node binaries" premise is obsolete — nodes are in-process (P16). Promote-to-required trigger recorded: plan upgrade or the Q65 public flip, after ≥20 clean scheduled runs with warm runtime ≤15 min. Scheduled-lane triage convention: a red scheduled run → a wave-bookkeeping note; two consecutive reds block storage-wave starts

### Due M2
- [ ] **D53** State for chain-invalid-at-genTime: `invalid` (recommended) vs `internally-consistent-only` (A9/A18/A21 — "A-OD1")
- [ ] **D54** Default OTS calendar set (≥2; liveness-verified) (A13/A25 — "A-OD2")
- [ ] **D55** Default Arbitrum RPC endpoint pairs per network (A17 — "A-OD3")
- [ ] **D56** Reconcile spec lines 133 vs 108/168 on the OTS `internally-consistent-only` trigger (mismatch → `invalid`; align R wording) (A18/A21/R18 — "A-OD4")
- [ ] **D57** Pinned root-store scope: defaults only vs + documented alternates (recommended: include alternates) (A6/A7 — "A-OD5")
- [ ] **D58** `opentimestamps` 0.2.0 viability on stable + wasm32; vendor/fork fallback (A11/P18 — "A-OD6")
- [ ] **D59** Request-nonce persistence semantics (capture-time check; bundles carry no nonce) — sign off (A8/A10 — "A-OD7")
- [ ] **D60** DER/CMS/X.509/signature crate pins; P-256 TSA support beyond normative P-384+RSA? (A5/A8/A9 — "A-OD8")
- [ ] **D61** Long-run fuzz venue: nightly CI vs OSS-Fuzz (Q9)

### Due M3
- [ ] **D62** Verifier-page host (domain from P3; canonical URL constant) (R26/P3/Q)
- [ ] **D63** Footer build-hash mechanism + exact artifact set the published SHA-256 covers (R25)
- [ ] **D64** Online-overlay ↔ headline presentation layout (frozen by R18 snapshots) (R17/R18)
- [ ] **D65** `--json` verdict-report schema stability commitment (R21/U30)
- [ ] **D66** CORS viability of pinned esplora/Arbitrum endpoints from browsers; fallback policy (R24)
- [ ] **D67** Disclosure-preview snippet length + binary-snippet format (R15/U27)
- [ ] **D68** `--units` syntax (ranges in MVP?) + default bundle filename (U28)
- [ ] **D69** Verify exit-code mapping per verdict class (UNANCHORED = 0 or distinct nonzero?) (U2/U30)
- [ ] **D70** Raw-mirror auto-include on whole-file reveals: always automatic vs future opt-out (R13/R16)

### Due M4
- [ ] **D71** Signing mechanism (minisign vs cosign vs both) + key custody (Q30)
- [ ] **D72** Release target set (linux-only vs +macOS/+Windows), distribution channel, crates.io publish scope (Q31/Q22) — 2026-08-01: resolving D72 also triggers D50's recorded keystore-scope follow-up (the OS-keystore wrap's platform choice + implementation, wrap-mode 2, was deliberately deferred to post-D72)
- [ ] **D73** Success-metric measurement method with zero telemetry (Q35)

---

## Joint/shared tasks (implement once — do not duplicate)

- **P10 ↔ F1** — one CBOR-crate evaluation+pin (P records the pin; F owns the technical evaluation + implementation).
- **P11/P12 ↔ C11** — C11's probe produces the evidence; P lands the pins.
- **P9 ↔ S1** — P9 re-verifies/lands the pin; S1 surveys the API surface. Both M0-start.
- **S15 ↔ R11** — one persistence primitive (S15 implements in antseal-net; R11 is the verify-side wrapper).
- **P19 ↔ Q36** — one weekly upstream-check job (P mechanics, Q operation).
- **P13 ↔ Q10** — one advisory lane (P stands it up; Q operates it permanently + licenses via Q29).
- **U31 ↔ Q20** — one copy lint (U owns CLI catalog, Q owns repo-wide guide; single lint implementation).
- **G19/C17/F15/A21/R7-R8 → Q7/Q8** — rows are domain-owned; the harness + completeness registry are Q's. Error-code names must be globally distinct — coordinate through Q7's contract before writing rows.

## Risk watchlist (spec §Risks → owning tasks)

| Risk | Standing mitigation tasks |
|---|---|
| ant-core churn (weekly-to-biweekly) | P7, P9, S2 (boundary), S20, P19/Q36 |
| Autonomi network youth / data-reset history | evidence never depends on storage: R5 layering, A-anchors; single mainnet exposure only at Q34 |
| Receipt not independently verifiable in MVP | A19 classification, S7 capture completeness (no reseal ever needed) |
| OTS pending window / calendar death; TSA churn + cert expiry | A20 gate, A15/U24 opportunistic upgrades, U25 nags, A6/A7/A26 pinned roots, A9 genTime LTV |
| PQC crate maturity (ml-dsa unaudited, 2 advisories) | P12, P13/Q10, C11 probe, C14 fallback slot |
| Vault loss AND theft | U6 KDF+AAD, U7 floor, U8 wrap, U12 export, U18 nags, Q24 docs, Q21 threat model |
| Wallet linkability | receipt opt-in (R13/U29), U10 sub-key, Q25 hygiene doc, Q21 |
| Malicious verifier host | R25 reproducible build + hash, R26 canonical URL, Q30 signing, footer + CLI cross-check |
| Hostile bundles (adversary CBOR/DER/.ots in the browser) | F3/F11 strict+caps, F17/A23/R10 fuzz, A5 strict DER, Q9 lanes |
| Sealer-as-adversary | R3/R4 structural invariants, R7 rows, C7/G11 by-construction guards, subordinate claimed time (R17/R18) |
| Free-TSA terms/rate limits | A10 spacing config, A24 mock-only CI, Q16 policy, Q26 caveats doc |

## Out of MVP scope — v1.1 parking lot (do not implement; capture obligations already covered)

Committed v1.1: arbitrary byte-range reveal *tooling* (`reveal --range` selection + rendering UX — the proof format + verification already ship in MVP via G12/G13; F8 reserves the bundle slots) · Arbitrum-receipt verification chain (capture is complete via S7 — no reseal will be needed) · heading-aware text splitting · grapheme-selection UX.

Registered by the 2026-08-01 M1 planning round: large-blob (> 4 MiB) storage path (D32/D35 record the revisit triggers) · `vault gc` cache prune + an everything-included export variant (D43/D47) · `--max-cost` non-interactive spend ceiling (D36 residual).

Later: eIDAS-qualified tier · hosted SaaS/org gateway · GUI/desktop app · C2PA export · Arweave mirror tier · ZK position-blinded reveals · key escrow/social recovery · re-anchoring service · public anchor-bundle uploads · external-signer wallets.

Scope discipline: if a task seems to require any of the above, stop and check the spec's Parking lot — the MVP obligation is only that these remain *possible* (reserved slots, complete capture), never that they ship.

## Change log

- 2026-08-01 — **Adversarial-review remediation wave** (3 staged lanes over 3 waves: F40+C28+Q67 → F41+R53 → R54; six seal agents, orchestrator-integrated, per the 2-core lane-staging rule; every lane-reported-unverified finding re-verified before action). **All six review tasks landed.** F40: the count rule argued into `FileEntry::new`'s frozen order, `manifest-multiple-raw-mirrors` minted, red-direction and planted-fault both executed, committed two-mirror fixture + rejecting-direction generator. C28: `verify_body`'s "safe on any input" made true (duplicate scan over both collections; `crypto-pubkeys-duplicate`/`crypto-signatures-duplicate` appended — with F40's mint, **universe 191 → 194**, union-only re-bless). Q67: D28's shape-totality claim rescoped at seven sources (consistent narrowing is honest narrowing; D74's decision basis corrected, R4's line here included). R53: `resolve_raw_mirror` is the sole mirror resolution (an `include_str!` structural pin catches any second path); report `raw_mirror` gated on `is_full()` — **zero frozen bytes moved**, proven by regenerating all 21 pinned reports byte-identically through the changed pipeline on both targets; the report-kind vector set found structurally closed (→ Q68). F41: the four false structural guarantees resolved — `Clone` gated to `test-util` behind a planted-fault-verified const guard; **six** constructors (the review knew four) now enforce the decode-side D10 caps; the `sig_policy` and domain-tag justifications corrected at source; D10 amended. R54: U1/U3 re-verified true, then index-once across stages 2–4, `reveal_set`, and the tier-[X] merge scans — at-cap no-reveal verify **29.0 s → 2.6 s debug** (3.11 s → 0.13 s release), scaling 3.7× → 2.0× per doubled input, machine-speed-free ratio guard proven red on the pre-fix code; stage order untouched (stage-5-last re-verified correct); D10 §5 row 2 made true, the cap product recorded as deliberately unbounded. Integration was real work, again: two 3-way merges (the contract §7 double-append hand-merged with per-commit-accurate arithmetic), and one stale *generated* fuzz-seed corpus re-emitted additively (wave 6's exact lesson). Gate at the wave head: **1243 tests**; fmt / clippy / wasm32 / format-freeze / ci-shell / ci-lanes / traceability / cross-check green; 12-vector bit-match byte-identical. Discovered work: **11 tasks** (F51–F53, C29, G29, Q68, Q69, R55–R58) + renderer notes on R18/R23; counts 309 → 320. **Pushed same day with express consent** (`d3345e1..62ea9d4`); **CI run 30697242826 green on all 19 contexts**, `traceability` cleared by Q66 as predicted — the first remote verdict recorded at a push since the freeze.

- 2026-07-31 — **Post-freeze verification audit + Q66 + bookkeeping sweep** (five read-only agents: spec-alignment · gate re-execution · internal consistency · F/C/P claims · G/R/Q claims). Alignment to MVP-SPEC.md confirmed (0 coverage gaps, 0 silent divergences, 0 stale spec citations); implementation claims confirmed (1221/1221 tests re-run; all 16 pins exact; 12-vector bit-match byte-identical; tamper 0 pending; cross-check 255 checks / 0 discrepancies; G16's 14 405/3 220/11 185 recomputed exactly). **One real defect: the `traceability` lane exited 1 at the tag and every commit since** — its self-test fixtures rotted when Q14 ticked the gate rows, invisible because `local-gate.sh` never ran the lane (→ **Q66**, fixed same day; the lane is in the local gate now). Sweep: header/table/section counts corrected (301 → 302 actual at audit, **303 with Q66**; every milestone header now equals its physical count); the 17 M0-declared R tasks (R29–R43, R51, R52) moved home from M1's section to M0's; the Maintainer-actions push clause superseded (the freeze push happened 2026-07-28 21:27Z per the local reflog — "tag is local only" was stale for three days); Q1's branch-protection remainder re-marked ⛔ BLOCKED BY PLAN; G3's stale ⛔ cleared; F27–F29 added to the residue list; wave-7's "18 contexts" corrected to 19; the R-universe count corrected to **86 codes / 28 variants** in D85's entry, `docs/decisions/README.md` and error-code-contract §7 (stale-at-writing); D29's README row updated RECOMMENDED → FROZEN; D29/D78/D79 register entries now cite their record files; **D83 amended to name the `n == 1` `s_root` value substitution as a recorded spec deviation** (line 96's conveyance rule — the record had labeled only option C a spec amendment); retroactive change-log stubs added for waves 4 and 6, which ran without entries. Minor residue left open deliberately: `all_code_exemplars` dead-code warning on wasm32 (`codec/decode.rs:354`), `testdata/error-codes/` has no README, stale runbook context-set copies (Q56's scope).

- 2026-07-28 — **M0 wave 7**, the freeze wave, ending at the **`format-v1-freeze` tag** (planning-first: 2 planning agents resolved **D8** and audited the gate → 7 parallel worktree lanes → rolling integration → the gate). Completed **28 tasks**: F4, F18, F20, F22–F25, F30, F33, G24, C27, R33, R36, R37, R41, R42, Q38, Q40, Q41, Q43, Q49–Q53, Q57, Q58, **Q14**. Froze **D8, D17, D29, D30**. Discovered **21 tasks**. Gate at the tag: **1221 tests** (1113 at wave-6 close), fmt/clippy/wasm32/bit-match over 12 vectors/vector freeze/**registry freeze digest**/tamper matrix COMPLETE/cross-check 0 discrepancies/traceability all-M0-covered/ci-shell/ci-lanes.
  - **The gate audit found the gate itself unrunnable, and proved it rather than arguing it.** Q14's `Accept` required both `--freeze-boundary` green *and* every normative row ticked — but rows N1/N2 sit **inside** the `FREEZE-BOUNDARY` markers, which are byte-compared against D84 §7, so `- [ ]` → `- [x]` was drift. Demonstrated on a scratch copy. **Q49** blanks the marker on both sides and bounds the tolerance with four self-test cases: a tick passes, a changed word *or a deleted marker* still goes red.
  - **Two freezes turned out to be declarations with no mechanism.** **D30**'s append-only code rule was enforced by review discipline alone — the three existing layers check distinctness and shape, neither of which a rename violates, and **53 of 191 codes (28 %) could be renamed with a fully green suite** (**Q52**, validated live: renaming one produced exactly one failure across 706 tests). And the **wire registry had no freeze digest at all** — the 19 D10 caps were pinned only by "code and JSON agree", which a coordinated two-file edit satisfies forever (**Q50**). A third: the pinned CBOR encoder is item 1 of Q14's own `Do` and was **asserted by no test** (**Q53**).
  - **D8 removed wire surface rather than confirming it.** `tsa_anchor.source` leaves v1 — zero producers, zero consumers, the only rendered `tstr` neither signed nor committed, and the only assigned key with no spec line. It also found `block_number` **under-determined** (multi-tx payments span blocks, so two honest sealers emit different bytes) and **D82 absent from the registry entirely**. F4 then moved 33 sections, 30 of them substantive — exactly D8's prediction.
  - **Every lane found its own task text wrong, and in the expensive direction more than once.** R36/R37's incidence rule said a leaf-level cover node appears iff `n` is odd; it needs an odd **unit boundary**, and acting on the stale rule yields a fixture that discloses nothing and the false conclusion that the class does not exist. F20's Accept was unachievable because F8 had since made those states unrepresentable. F18's premise was half stale — two of its four rows existed, and adding them would have collided. Q38's key regex would have failed 20 of 34 committed keys at a gate where the tempting fix is the bytes. F22 miscounted its own scope; F25's "recursion is already depth-bounded" was false (the bound is in a *private* method).
  - **F30's real numbers were worse than recorded, and its own test was vacuous.** The `files` site amplifies **157×** (3.1 MB from a 20 KB input), not 32× — fuzzing had simply reached the sigmap case first. Worse, every allocation fixture ended at its container head, so `remaining()` was **zero** at the allocation, and a mis-united clamp is indistinguishable from a correct one when one operand is zero.
  - **Reverse coverage earned itself immediately, twice.** F23 found **88 of 95** `bundle-`/`manifest-` codes unaccounted on the first run, 65 of them with no owner at all (**F39**). F24 found `cbor-int-out-of-range` is not merely unrowed but **unreachable** — no v1 schema slot decodes through `i64()`.
  - **The independent cross-check rejected three new fixtures and was right all three times** — a count-cap fixture that was truncated as well as over-cap (so which fault fired was an ordering accident), and two whose layer was invisible to the checker's descent. A second implementation catching three defects in the first, in the wave that freezes both.
  - **R36 did not go red as predicted — it generalised**, and the generalisation was proved: all 32 bytes of a real leaf-level payload flipped, rejection **class** asserted per half. The `88 + 67` authentication boundary still holds exactly, which is the property D83 option B was chosen to preserve.
  - **Ops findings.** Three integration defects existed **only on the merged tree**, all from D8's one deletion landing in a different lane from its three users — source code, an assertion's grep target, and committed fixture bytes — and none was visible to git, because no two lanes touched the same file. A fourth was avoided: lanes C and E both moved `bundle.json`, and the merged file's digest matched **neither** lane's `FROZEN.sha256` entry, so resolving that conflict by picking a side would have frozen a manifest that does not describe its own file; regenerating from the emitter reproduced git's merge byte-for-byte. Worktrees were placed **outside** the repo this wave, which structurally removed wave 6's `.cargo/config.toml` doubling. The machine has **2 cores**, so lanes were staged 4 → 2 → 1 rather than run all at once; wave 6's "agents stalled at ~600 s having produced nothing" is very likely the same constraint. Pre-allocated ID blocks per lane eliminated wave 6's three ID collisions entirely.
  - **Not pushed** *at the wave's close* — superseded the same day: the local reflog records the freeze push (`5f794ff → d3345e1`, 21:27Z), see the 2026-07-31 entry. `docs/ci-verification.md` carried **two contradictory** authoritative context sets (17 names omitting `traceability`, and a later "still 16"); the real set is **19 from 17 jobs** (this line originally said 18 — corrected 2026-07-31, after b8411e5's recount), and a branch-protection payload cut from any stale copy would have left required lanes unprotected.

- 2026-07-28 — **M0 wave 6** *(retroactive stub, added 2026-07-31 — the wave ran without a change-log entry; the 2026-07-31 audit found the gap via irreconcilable discovery arithmetic. Reconstruct detail from git history between the wave-5 and wave-7 entries if ever needed.)* 21 tasks completed, 10 decisions resolved, 34 tasks discovered; gate at close: **1113 tests** (cited by wave 7). Between waves: the wave-6 head (`5f794ff`) was pushed with express consent and CI ran **GREEN on all 18 then-contexts** (run 30376668778) — the first remote execution of `cross-check` and `traceability`, and the push whose first remote run caught the Q8 tamper-matrix counting defect (→ Q43).

- 2026-07-28 — **M0 wave 5**, the vectors + fixtures + rows wave (planning-first: 3 planning agents resolved D10, D76/D77, D81/D82 before any implementation → 5 parallel worktree agents → rolling integration → 4 stage-2 agents → F15 alone at the end). Completed **20 tasks**: R6–R10, F10–F13, F15, F16, G14–G17, G19, G20, C20, C21, Q12. Resolved **D10, D76, D77, D81, D82**; registered **D83, D84**; discovered **19 new tasks** (F18–F26, G21–G23, R29–R34, C22). Gate: **1028 tests** (776 at wave-4 close), fmt/clippy/wasm32 build/wasm32 execution/native↔WASM bit-match over 11 vectors/vector freeze/tamper completeness/cargo-deny all green. **Two Q14 gate conditions met**: vector must-exist set empty, M0 tamper matrix COMPLETE (0 pending).
  - **Three decisions overturned or corrected their own register entries.** D81 was recorded recommending a cause discriminator; the analysis found the premise backwards — the AEAD is not the binding mechanism for either mutation, both are caught independently at stage 4, and the two candidate discriminators (the BLAKE3 chunk address, speculative re-decryption) each break something worse. D76 confirmed its lean on entirely different grounds: the recorded reason (a new HKDF label) costs almost nothing pre-Q14, while the real arguments are a D78 presence-tier drop, a doubled permanent code surface, and the fact that **the split does not remove the leak** because the mirror's `true_length` already publishes raw size. D77's recorded shape was already closed by F5; the hole that actually survived was the **mirror-only file**, which no record had named.
  - **Findings that change future work**: C21 found the HKDF/HMAC key state is un-zeroizable on the current pins — a live contradiction of spec line 143 (→ C22). R9 found `REPORT_VERSION` 0 → 1 rewrites all 21 pinned report byte strings and must precede `status frozen` (→ R32), a coupling Q14's checklist does not name. G20's proof-mutation property found that a leaf-level cover seed's upper 16 bytes are never examined, so `.sealproof` bytes are not unique for a proof (→ D83/G23). R10 measured the M0 authentication boundary at exactly 88 + 67 bytes. F15 established that a wrapped codec rejection surfaces its inner code unchanged at every layer, so canonicality classes are many fixtures and one code — fixtures pin `(code, layer)` rather than forking the taxonomy per layer.
  - **Ops findings**: **every single agent worktree this wave was cut from a stale base** (`c85d963`, 71 commits behind) — not the occasional hazard wave 3 suggested, but the default; all 13 self-corrected only because the base check was mandatory in every brief. A new failure mode appeared: 4 of the first 8 agents hit a stream-watchdog stall at ~600 s having produced nothing, all recovered by resuming from transcript with an instruction to read-narrowly-and-commit-early. Worktrees sit *inside* the repo, so cargo merges both `.cargo/config.toml` files and the wasm runner doubles — `CARGO_TARGET_WASM32_UNKNOWN_UNKNOWN_RUNNER` is the workaround. Three ID collisions had to be reconciled at merge (two lanes each minting G21, R31/R32, F19/F20) — parallel agents cannot see each other's numbering. Two integration defects were visible only on the merged tree: F10's roster still listed G15's landed vector as pending, and an orchestrator conflict resolution collapsed two `#[cfg]`-gated module declarations under one attribute, un-gating R7's slice and breaking the wasm32 tier.

- 2026-07-28 — **M0 wave 4** *(retroactive stub, added 2026-07-31 — the wave ran without a change-log entry.)* Gate at close: **776 tests** (cited by wave 5). Completions not itemized here — reconstruct from git history between the wave-3 and wave-5 entries if ever needed.

- 2026-07-27 — v1: initial list generated from MVP-SPEC.md Rev 2 (9-agent decomposition, 219 tasks). Nothing started; phase = pre-M0.
- 2026-07-27 — pre-M0 execution (3-agent run: research + scaffold/CI + decisions/docs): P4–P7 ✅; P8 committed with all 5 lanes verified green locally, remote run blocked on gh `workflow` scope (maintainer device-flow); P1 provisional decision + propagation checklist + blessing draft committed; P2/P3 execution kits ready (reserve script dry-run green ×5), execution maintainer-blocked; D2–D6 resolved, D1 provisional with fallback + M0-start deadline. Evidence: docs/decisions/, docs/naming/, docs/ci-verification.md. Notable finding: `self_encryption` (mandatory in ant-core) is GPL-3.0 → D6 per-crate split + P15 flag.
- 2026-07-27 — **D1 FINAL: `antseal`** — maintainer confirmed ("I confirm the name is antseal"); no upstream blessing (deviation + residual risk recorded in the D1 doc); blessing request → optional courtesy; `sealstone` fallback retired. P1 ✅. Availability re-verified at confirmation (~18:58Z): 5 crates FREE + dry-run green, antseal.org/.dev FREE → P2/P3 executable, flagged prompt (availability decays). C12/F4/U1 unblocked on the name; Q14's D1 gate satisfied.
- 2026-07-27 — M0 wave 1 (6 parallel worktree agents + orchestrated integration): P9/P10+F1/S1/C1–C4/G1/R1 ✅, Q1 ⏳ (remote-blocked). Pins landed: ant-core =0.5.0 (declaration-only), minicbor =2.3.0 (D7), unicode-normalization =0.1.25/Unicode 17.0.0 (D25), sha2 =0.11.0 + hkdf =0.13.0 + hmac =0.13.0, serde =1.0.229 + serde_json =1.0.151 (D29). D11 (32-B BLAKE3 address), D12 (cbor2 ==6.1.3), D27 (hybrid error mode) resolved; D1 fallback re-anchored (request unfiled at M0 start — clock not started). Notable findings: evmlib 0.9.0 is a second GPL-3.0 crate in ant-core's graph (D6); no upstream payment API returns block numbers (D33); CI needed .gitattributes to keep golden fixtures CRLF-safe on windows runners. Full local gate green; push still blocked on gh `workflow` scope.
- 2026-07-27 — **M0 wave 2** (planning-first: D19/D20/D21 resolved before implementation → 6 parallel worktree agents + orchestrated integration → R2 follow-up): F2/F3 ✅, C5–C9 ✅, C11+P11/P12 ✅, G2 ✅, Q2–Q4 ✅, P13 local-complete ⏳, F4 draft ⏳, R2 ✅ (G13-rider). Pins landed: ed25519-dalek =3.0.0 (D13), ml-dsa =0.1.1 + fips204 =0.4.6 (D14; declaration-only until C12/C13), chacha20poly1305 =0.11.0, zeroize =1.9.0, subtle =2.6.1, rand_core =0.10.1; cargo-deny =0.19.8 (CI tool class). D13/D14/D16/D19/D20/D21 resolved; D9/D17 registry proposals drafted; D15 inputs captured. Notable findings: ml-dsa wasm32 **executed** bit-match via zero-import probe (no wasm-bindgen needed); dalek signature parse infallible on both lines → D16 = explicit C12 pre-validation; only 1 of 3 ml-dsa advisories has a RUSTSEC ID → GHSA/osv.dev sweep mandated (D19 amendment); strip-exactly-one-BOM violates the normative idempotence property → strip-all-leading (D21 recorded deviation); usize/u64 wire-length convention needed at R5/F5 (R2 finding); stable-code contract at 71 codes (D30). Gate: 223 tests, fmt/clippy/wasm32/deny green. Ops note: every agent worktree was cut stale at the pre-wave-1 commit (= the then-origin/main) and each agent fast-forwarded to main before starting — verify snapshot bases in wave 3.
- 2026-07-27 — **Uploaded**: hosting consolidated under the maintainer account per the D2 amendment (full identity rewrite + object-level reference scrub of all history, both repos verified zero-occurrence at the object-database level; head `c85d963`); `workflow` scope granted via maintainer device flow; main pushed to private `aed900/antseal`; **first remote CI run GREEN — all 14 contexts incl. the 3-OS corpus/vector lanes** (run 30309407509). Wasm-guard probe, branch protection, and the D19 advisory demo deliberately deferred by the maintainer (procedures recorded in docs/ci-verification.md).
- 2026-07-28 — **M0 wave 3** (planning-first: D15/D22/D23/D24 resolved before implementation → 3 parallel worktree agents for F5-F7 / C10+C12-C14 / G4+G5+G8, with G3, R3, Q7, C14 and the decisions done inline by the orchestrator → merge + integration fixes). Completed: F5/F6/F7, C10/C12/C13/C14, G3/G4/G5/G8, R3, Q7. Resolved D9, D15, D22, D23, D24; D17 ratified in code (freeze Q14); D30 formalized as docs/testing/error-code-contract.md. Gate: **428 tests**, fmt/clippy(-D warnings, all features)/wasm32/core-dep-graph/cargo-deny all green.
  - **Notable findings**: G3's independent Python oracle agreed with the pinned Rust crate on **every** fixture at first run — including all eight invalid-UTF-8 classes, so CPython's and Rust's decoders demonstrably implement the same §3.9 maximal-subparts rule D20 froze; NFC oracle safety across differing Unicode versions rests on the Normalization Stability Policy (fixtures confined to scalars assigned ≤ 6.0). C13 needs **no** pre-validation layer (C11's finding held up in implementation) but must never touch the crate's `Signer`/`Verifier` traits, which sign with the empty ctx. C14 found that `SigPolicyUnknownAlg` is only reachable through a wire-id entry point, since `SigAlg` cannot represent an unregistered id — hence `SigPolicy::from_ids`. D24 turned out to be expressible as a type witness rather than a runtime check. R3 needed a sortedness pre-pass so a descending unit table reports `Unsorted` instead of a misleading first-range `Gap`, and `RawMirrorInTilingSet` got a precise trigger (non-mirror ranges fail, but including the mirror's would tile). Two registries now describe the same algorithm-id assignment (C's semantic, F's wire) — pinned together by test; delegation is a future cleanup.
  - **Ops findings**: agent worktrees were correctly based this wave (the wave-2 stale-base hazard did not recur). Two process failures worth not repeating: the first launch of all six agents died together when usage credits ran out — nothing was lost only because they had committed incrementally — and later a broad `pkill -f "worktree-agent"` by the orchestrator matched the agents' own process command lines and killed all three mid-task. Incremental commits again limited the loss to one placeholder KAT digest and two unrun gates. **Rules for next wave**: never pattern-kill on a string that appears in agent paths, and keep the per-task commit instruction in every agent brief.
- 2026-07-27 — v1.1: adversarial verification applied. Coverage audit: **0 gaps** against the spec. Consistency audit: 12 defect groups fixed — R16/R21 redefined as library APIs (U28/U30 are the sole CLI owners of `reveal`/`verify`); A18 cedes verdict aggregation to R17; C17↔R7 tamper-fixture ownership resolved (R owns committed fixtures, C owns primitives+helpers); wallet keygen/import/address ops added to S5 (was an orphaned U11 dependency); A6→A7 sequenced; U13⇄U14 and eight consumer-reference cycles annotated (Deps-discipline rule 7 added); A5/A11/A12 real-fixture bootstrap via early A25 capture noted; multi-OS corpus lane added to Q1 (G3 was unexecutable); D31 scoped against D12.
