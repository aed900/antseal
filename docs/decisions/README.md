# Decision index

Numbered records of project-shaping decisions. `TODO.md`'s **Decision
register** is this project's decision register — it allocates the id space and
legitimately carries ids that have no file yet; **this file is not a second
register but the index of the records that exist**, carrying exactly one row
per `docs/decisions/D*.md`, in ascending id order, with each row's status and
date taken from that record's own `- **Status` and `- **Date` lines, and the
`decision-index` check in `scripts/check-traceability.py` is what holds all
three true. A decision is changed by editing its file
with a new dated entry, never by silently rewriting history.

*(Identity, domains and check ruled 2026-08-11 by
[D119](D119-decision-index-identity-and-the-index-row-sections.md) RULING 1
and RULING 3, which also rules the **record** authoritative for status where
the three homes disagree.)*

Evidence convention: several records cite the pre-M0 naming & pin research
memo (2026-07-27, retrievals 13:44Z–13:51Z via crates.io API / rdap.org /
api.github.com). The memo itself was working material; every load-bearing
finding is quoted inline in the records with its retrieval timestamp.

| ID | Title | Status | Date |
| --- | --- | --- | --- |
| [D1](D1-product-name.md) | Product name — **`antseal`, maintainer-confirmed final** (no upstream blessing — recorded deviation; blessing request now optional courtesy; fallback retired) | RESOLVED | 2026-07-27 |
| [D2](D2-hosting-ci.md) | Hosting + CI — GitHub + GitHub Actions | RESOLVED | 2026-07-27 |
| [D3](D3-repo-layout.md) | Repo layout — repo root IS the workspace root (no `antseal/` subdir) | RESOLVED | 2026-07-27 |
| [D4](D4-edition-msrv.md) | Edition 2024; toolchain pinned =1.92.0; MSRV = the pin | RESOLVED | 2026-07-27 |
| [D5](D5-cli-crate-name.md) | CLI crate name — spec tree normative (`antseal-cli` crate, `antseal` binary); bare-name publish deferred | RESOLVED (recorded deferral) | 2026-07-27 |
| [D6](D6-license.md) | Licensing — per-crate dual MIT/Apache-2.0; GPL-3.0 effect on net/cli via `self_encryption`; P15 flag | RESOLVED (files land M4/Q29) | 2026-07-27 |
| [D7](D7-cbor-crate.md) | Deterministic-CBOR crate — `minicbor = "=2.3.0"`; strictness lives in F3 on native probes; in-house-codec contingency trigger; D12 cross-check nominee `cbor2==6.1.3` (dev-only) | RESOLVED | 2026-07-27 |
| [D8](D8-wire-registry-final.md) | Complete v1 wire registry sign-off — **RESOLVED**, with four substantive changes to the draft, five ratifications and eleven corrections. Two of the four open items resolve by *removing* wire surface: the TSA `source` string leaves v1 (unbindable in an unsigned bundle, and its one named consumer had already refused it in writing), and the recorded `anchor_status` subset is ruled to be **no subset at all** — any narrowing would be a parse rule keyed on a field D79 forbids depending on. The receipt's `block_number` is under-determined under S1's verified multi-transaction payment path and is given a total definition (the minimum over key 0); `title`/`app_version` keep their shape on entirely replaced grounds, because the alternative the draft rejected is *also* single-encoding. Freeze mechanics defined (`frozen-v1` by equality, the pre-freeze markers prohibited outright) and the freeze-time assertion set named A + B + C1–C10 + D + E, of which C5, C6, C10 and D were gaps. Eleven errors recorded in §17 — three of which (a `D83`/`D84` mispointer, a retracted "proves no extra leaves" claim, and D82 missing from the registry entirely) would have been frozen into a normative document | RESOLVED (F4 implements) | 2026-07-28 |
| [D9](D9-ggm-node-address.md) | GGM node address — **`(level, index)`** pair of uints (not a bit-path): the index bits MSB-first ARE the root-to-node path, so it is the spec's own leaf rule generalized, and canonical-CBOR uints make it canonical for free; wire shape `[level, index, bstr(32)]` | RESOLVED (co-frozen with F4 at Q14) | 2026-07-28 |
| [D10](D10-parser-caps.md) | Parser resource caps — **19 frozen constants** (bundle 256 MiB, manifest 16 MiB, depth 8, 65 536 units, 16 384 files, 256 cover/path entries, …), **18 new `bundle-`/`manifest-` codes**, one universal clamp rule `min(claimed, remaining_input)`; all enforced in verify stage 1 ahead of every AEAD/hash/signature. Cover/path caps derived from the `uint` width of `size` (2·⌈log₂ n⌉ ≤ 128), not guessed | RESOLVED (F11 implements; freezes at Q14) | 2026-07-28 |
| [D13](D13-ed25519-dalek-pin.md) | `ed25519-dalek` pin — **`=3.0.0`** over 2.2.0 (identical strict-verify semantics probe-proven; single signature-3/sha2-0.11/getrandom-0.4 stack shared with ml-dsa); ZIP-215 pubkey gap → C12 pre-validation | RESOLVED | 2026-07-27 |
| [D14](D14-mldsa-crate.md) | ML-DSA crate — primary **`ml-dsa =0.1.1`** (wasm32 probe PASSED, executed native↔wasm bit-match; canonical rejection at decode), fallback `fips204 =0.4.6` (pinned, unconsumed, byte-compatible); Ed25519-only sig_policy fallback NOT shipped (trigger did not fire); 3 advisories assessed, all patched in pin | RESOLVED | 2026-07-27 |
| [D15](D15-mldsa-signing-mode.md) | ML-DSA signing mode — **deterministic** (FIPS 204 `Sign`, rnd = 0³²) everywhere in v1; hedged unused. Makes C16 vectors portable and the fips204 fallback swap byte-invisible; **not format-encoded**, so a future version may switch without a format bump | RESOLVED | 2026-07-27 |
| [D18](D18-wasm-bindgen-surface-location.md) | wasm-bindgen surface location — **a NEW WORKSPACE MEMBER, `crates/antseal-wasm`** (`cdylib`+`rlib`, `publish = false`), `wasm-bindgen` **target-gated** to `cfg(target_arch = "wasm32")`, a **closed** export list of at most four entries returning the report's canonical **bytes**, and `antseal-core` edited **not at all** — so `core-dep-graph`, `gate-features --check-partition`, `wasm32-core`, `wasm32-core-tests` and `wasm-bitmatch` are untouched **by construction**. The register's **dichotomy is overturned as a framing** (a feature is a compilation switch, an artifact is a package output; seven downstream consumers name the shipped `.wasm` **by package** and none can name a feature), while the **wrapper arm survives ON MEASUREMENT**: `core-dep-graph` runs on default features and so cannot see a feature-gated edge at all (proven with `proptest`), and the arm nobody named — target-gated *inside* core — dies because one `#[wasm_bindgen]` fn yields **4 imports** and both wasm runners refuse any import. *"Thin"* is closed by definition, the crate lives under `crates/` (`.cargo/config.toml`'s two-level runner path refuses `verifier-web/` as a package root, so **`verifier-web/` does not become a member**), and its **name is frozen** because it is inside the artifact whose digest D63's footer publishes. *"No I/O inside the WASM module"* becomes structural via a reviewed-set rule + an import allow-list riding **existing** jobs (19 contexts stays 19). Pins ruled and installed: `wasm-bindgen`/CLI **`=0.2.126`** (the lock's own version — coupled to the ant-core subtree, so S20 must name it) and `wasm-pack 0.15.0`. One **recorded deviation**: a fifth `crates/antseal-*` entry against MVP-SPEC lines 44–58, flagged at §9 with no spec edit. Zero frozen bytes | RESOLVED | 2026-08-12 |
| [D19](D19-advisory-lane.md) | Advisory lane — cargo-deny ONLY (`=0.19.8` CI pin); deny.toml advisories/bans/sources active, licenses stubbed → D6/Q29 (BlueOak-1.0.0 minicbor, GPL-3.0 self_encryption notes); per-PR `audit-deny` + weekly cron; every ignore carries justification + review date; **GHSA/osv.dev sweep mandatory** (RUSTSEC covers 1 of 3 ml-dsa advisories) | RESOLVED (FROZEN) | 2026-07-27 |
| [D20](D20-force-text.md) | `--force-text` on invalid UTF-8 — deterministic **lossy U+FFFD** (Unicode §3.9 maximal subparts), text canonicalization total, **no descriptor flag**: `kind=Text` alone determines verifier recompute semantics | RESOLVED | 2026-07-27 |
| [D21](D21-canonicalization-micro-semantics.md) | Canonicalization micro-semantics — pipeline decode → BOM strip → EOL → NFC; strip **all** leading U+FEFF (forced by the idempotence property); one-pass CRLF→LF then lone CR→LF | RESOLVED | 2026-07-27 |
| [D22](D22-split-blank-line-definition.md) | Blank line for `--split` — content is solely ASCII space/tab (empty included); separating run attaches to the **preceding** unit. Seal-time CLI semantics, **not** verification law (R checks only tiling), so changeable without a format bump | RESOLVED | 2026-07-27 |
| [D23](D23-raw-mirror-placement.md) | Raw-mirror placement — the mirror is its file's **last** unit, at most one per file. R must exempt by **kind**, never by position, so a hand-built manifest still verifies correctly | RESOLVED (frozen) | 2026-07-27 |
| [D24](D24-no-fine-tree-split-conflict.md) | `--no-fine-tree` × `--split` — **hard CLI error** at plan validation (before consent, before payment); silent single-unit would permanently discard requested granularity. Model-side the contradiction is unrepresentable (`SplitEligibleText`), not checked | RESOLVED | 2026-07-27 |
| [D25](D25-unicode-normalization.md) | Unicode/NFC — `unicode-normalization =0.1.25`, data version 17.0.0, descriptor string `unicode-17.0.0`; add-only version registry, shipped tables retained forever | RESOLVED | 2026-07-27 |
| [D26](D26-perf-memory-budgets.md) | Perf/memory budgets (G18) — **the budgets are exact closed forms, not tolerances: `C₁` does not exist and must not be minted** (`peak_seed_stack_len == d+1`, `peak_frontier_len == bit_width(n)`, `compressions == n + 2(n−1) + G(n)`, all verified exhaustively to n = 4096). Corpus set from measurement: the entry's "≥ 256 MiB" is 13.5 min release / **6.3 h dev** on the reference box — default lane tops out at 65 536, a `#[ignore]`d release lane runs 2^25. The `[5,7]` envelope as written **fails** (measured 4.999); floor is 4 990 for n ≥ 2^12. No wall-clock assertion in the default lane. **Explicitly NOT format-permanent — nothing here freezes at Q14, and G18 is not a Q14 dependency** | RESOLVED | 2026-07-28 |
| [D27](D27-verify-error-mode.md) | `verify_bundle` error mode — hybrid: fail-fast typed first-error is the sole **normative** mode (the tamper matrix binds to it); `VerifyFailures` is rendering-only, primary-first, first element ≡ the fail-fast error | RESOLVED | 2026-07-27 |
| [D28](D28-full-reveal-strictness.md) | Full-reveal strictness — **STRICT hard-fail**: revealing every non-mirror unit without `file_salt` (+`s_root` iff a fine tree exists) fails. `canon_commit`/`raw_commit` have no other verification path, so optionality would **delete** the check, not weaken it; strict is also the reversible direction | RESOLVED | 2026-07-28 |
| [D29](D29-report-byte-format.md) | Verification-report byte format (the bit-match contract) — byte-deterministic compact `serde_json`: declaration-order fields, Vec/BTreeMap only, no floats, lowercase hex, kebab-case wire names; serde/serde_json exact-pinned | RESOLVED (FROZEN at Q14 as report v1; `REPORT_VERSION = 1`, R32; Q38 independent check) | 2026-07-28 |
| [D31](D31-cross-check-vehicles.md) | Independent cross-check vehicles + permanence — one vehicle per surface, **tiered T0/T1/T2**; CBOR keeps `cbor2 ==6.1.3` for **decode only** (our own RFC 8949 §4.2.1 encoder is the encoding authority, retiring D12's ordering caveat); crypto/GGM/canonicalization reuse the landed Python references, each required to carry a T0 known-answer anchor; **ML-DSA-65 = NIST ACVP replayed against `ml-dsa =0.1.1` (T0)** and **`fips204` is T2, explicitly excluded from the independence claim** (retained as D14's fallback check). Permanence: **both** — dated one-shot report gating Q14 + permanent unconditional `cross-check` CI lane. Found a gap: the report byte format has no cross-check → Q38 | RESOLVED (Q11/F14 execute) | 2026-07-28 |
| [D32](D32-blob-address-model.md) | Blob↔address model — **chunk-level**: every blob is exactly one Autonomi chunk, address = BLAKE3-256(ciphertext), `get_data` ↦ `chunk_get`, **no data-map path in v1**. The S1 lean confirmed, its open oversize door closed: a ciphertext over `MAX_CHUNK_SIZE` (4 MiB; max unit plaintext 4 194 047 B) is a seal-time plan-validation hard error before consent/anchors/quote — mandatory because upstream quotes and pays for unstorable chunks without complaint. Spec's "via self_encryption" lines recorded as a deviation; large-blob path parked to v1.1 | RESOLVED | 2026-08-01 |
| [D33](D33-block-number-locus.md) | Block-number acquisition locus — **`antseal-net::pay()`**, the register's anchor-side framing overturned: the payment RPC is not anchor HTTP. External-signer flow reads the block number off the tx receipt `pay()` already awaits; native-fallback/backfill via `eth_getTransactionReceipt` over S5's payment RPC (`ant_protocol` `http_provider` re-export). `antseal-anchor` gets zero M1 work; A17 stays M2/advisory, consuming the receipt, never producing it. Spec line 52 flagged (capture → confirmation) | RESOLVED | 2026-08-01 |
| [D34](D34-seal-pipeline-placement.md) | Seal-pipeline placement — **`antseal-cli` gains a `[lib]` target**; the orchestration layer (S12/S10/S11/S14, and M3's R16 reveal flow by the same forced symmetry) is `antseal_cli` library code over injected interfaces. Lean confirmed on grounds the register never stated: core is structurally impossible (churn types; core never deps net), net violates its spec-written charter, a dedicated crate breaks the publishability chain while D72 is open. Forced sub-finding: `MockBackend` must cross a crate boundary (S3 feature export) | RESOLVED | 2026-08-01 |
| [D35](D35-self-encryption-dependency-mode.md) | `self_encryption` dependency mode — **option (d), absent from the register's option space: NO antseal crate depends on it at all.** Under D32, S4 is `blake3` alone (exact-pinned, default-features off); net cross-checks ant-core's returned addresses against S4. P15 re-scoped pin → **CI prohibition**; D6's CRITICAL GPL flag discharged by deletion. Decisive: 0.36.0 has mandatory tokio/rayon/tempfile/rand — wasm32-dead regardless of license. Revisit triggers: P19 relicense watch, v1.1 large-blob | RESOLVED | 2026-08-01 |
| [D36](D36-prepay-resume-requote-reconsent.md) | Pre-pay resume re-quote/re-consent — **always re-quote (a journaled quote is never paid: nothing in the pay path checks age; storers enforce ~24 h `QUOTE_MAX_AGE_SECS`), consent per-invocation** — S11's "only if changed" overturned, U17's unconditional wording ratified. The invariant: consent is a single-use spend authorization consumed by the `pay()` it immediately precedes, same process invocation — subsuming proofs-expired re-consent and no-consent-post-receipt as corollaries. Decline → incomplete, never abandon; "changed" is display-only over a new S10 consent record | RESOLVED | 2026-08-01 |
| [D37](D37-multi-tx-payment.md) | Multi-tx payment — `pay()` emits **⌈blobs/256⌉ sequential txs journaled per sub-batch**; the register's `Vec<TxHash>` overturned as materially insufficient: the receipt's core is the **quote→tx map** (`HashMap<QuoteHash, TxHash>`, finalize errors on gaps) + per-blob `{address, quotes, proof_bytes}` + per-tx block-number slots. Merkle payment mode excluded (spec's "one Merkle-batch EVM tx" = recorded deviation); `Client::batch_pay` forbidden (error path discards the partial paid map — drive `Wallet::pay_for_quotes`). **New finding: ~24 h node-side proof validity time-boxes every post-pay resume** → proofs-expired state with re-consent | RESOLVED | 2026-08-01 |
| [D38](D38-sepolia-test-ant-acquisition.md) | Test-ANT acquisition on Arbitrum Sepolia 421614 — an ordinary **ERC-20 `transfer` from an existing holder; nothing else exists** (no mint function, no upstream faucet, no bridge; token + vault-proxy addresses recorded from evmlib source). Primary route maintainer-owned per the external-actions rule. Contingency, scripted and dormant: self-deploy both verified artifacts on 421614 via Custom-network mode — a recorded deviation, never a substitute for the canonical-contract M4 gate | RESOLVED | 2026-08-01 |
| [D39](D39-init-interaction-model.md) | `init` interaction model — **the wizard-vs-flags dichotomy overturned as a false choice**: TTY wizard, every question with a flag/fd equivalent (a pure wizard violates three committed M1 Accept criteria; pure flags cannot carry the passphrase). v1 flag set enumerated as a deliberate U1 surface amendment (`--wallet generate\|import`, `--wallet-key-fd`, D40/D50 selector slots); existing-vault refusal absolute — no `--force` | RESOLVED | 2026-08-01 |
| [D40](D40-vault-kdf-selection.md) | Vault KDF selection + low-RAM — **Argon2id (m=262144 KiB, t=3, p=1) sole default; scrypt (N=2²⁰, r=8, p=1) only as an explicit init-time choice; low-RAM = typed hard error at create AND unlock** — U6's fallback-prompt lean overturned (the imagined fallback needs 1 GiB, 4× more). **Pre-auth KDF-header caps** added (D10 precedent; a substituted 1 TiB header is a resource bomb before the AEAD authenticates — same caps serve unlock and D47 import). scrypt `=0.12.0`; argon2 0.5.3 rejected (un-wiped blake2/digest-0.10 stack) → `=0.6.0-rc.8` + D88-style residue probe at pin PR. Spec:143 completion flagged (no r/p) | RESOLVED | 2026-08-01 |
| [D41](D41-noninteractive-passphrase-channel.md) | Non-interactive passphrase channel — a global **`--passphrase-fd <n>`** (stdin = fd 0). Env var **rejected on a measured mechanism**: `/proc/PID/environ` serves the execve-time snapshot no userland unset/zeroize can wipe — contradicting spec:143's zeroization mandate for the process lifetime. Frozen byte semantics: read-to-EOF, 1 KiB cap, strip one trailing LF/CRLF, reject empty/interior NUL/LF, prompt parity; fd path skips the create-time confirmation | RESOLVED | 2026-08-01 |
| [D42](D42-vault-encryption-boundary.md) | Vault encryption boundary — beside the AEAD sits **exactly** `config.toml` + vault header + lockfile; **everything else inside, including all anchor artifacts**. The U24 consequence sharpened into a positive rule: the upgrade hook runs **iff the host command already holds an unlocked handle** — locked/absent = silent no-op, never a prompt, never creates `~/.antseal`. Rider: every vault AEAD binds KDF header + record identity as AAD (splice resistance); whole-vault rollback out of scope | RESOLVED | 2026-08-01 |
| [D43](D43-staged-ciphertext-retention.md) | Staged-ciphertext retention — **RETAIN**: finalize success reclassifies journal → cache in place (state tag, same bytes), inside the AEAD, **excluded from `vault export`**. Journal bytes resume-critical, never prunable, always exported; cache bytes integrity-rechecked (S4 recompute) each use with silent refetch fallback, loss never an error. No prune surface at MVP (`vault gc` = v1.1; R16 needs whole-file ciphertexts for boundary Merkle paths). S14 network-first/verified-cache-fallback amendment ratified at integration | RESOLVED | 2026-08-01 |
| [D44](D44-wallet-import-formats.md) | Wallet import formats — **raw hex only in v1** (64 hex digits, optional `0x`, case-insensitive, k256 scalar-validated); accepted set ≡ **"whatever the pinned evmlib/alloy parse accepts"** so validation and the payment path cannot drift. Mnemonic rejected: not compiled in upstream, two new payment-path crates into the exact-pin class, and derivation-path ambiguity = silent wrong-wallet. Revisit trigger recorded | RESOLVED | 2026-08-01 |
| [D45](D45-resume-detection-consent.md) | Resume detection + consent — **automatic, keyed on recorded invocation identity** (network + exact ordered lexically-absolutized path list + seal-shaping flags): exact match auto-resumes; **overlap-not-exact / flag-mismatch = pre-consent hard error** (D24 pattern); disjoint = fresh seal with notice; `--resume` and interactive selection rejected. Pre-pay resume re-fires the **full U14 gate over a fresh quote** (agrees with D36, which owns the cost rule); post-receipt never re-consents, no `--yes` | RESOLVED | 2026-08-01 |
| [D46](D46-seal-directory-arguments.md) | Directory arguments to `seal` — **files only**: directory, non-regular, and duplicate arguments hard-error at plan validation pre-consent, identically under `--dry-run`; shell globbing is the recursion tool. Every recursion sub-rule (iteration order — POSIX `readdir` is unspecified and would reach the manifest file-table order — hidden files, symlinks) would freeze per seal into a permanent paid artifact under `--yes`; error-now → recursion-later is the additive, reversible direction | RESOLVED | 2026-08-01 |
| [D47](D47-vault-export-format.md) | `vault export` format — **re-encrypted single file**: magic `ANTSEAL VAULT EXPORT` + versioned det-CBOR header (own KDF block, fresh salt, AAD = magic‖header) + one AEAD over the full logical state (config IN, cache OUT per D43); same passphrase proven by the unlock the export performs; mandatory post-write self-verify; import validates fully in memory under D40 caps before atomic install. Archive rejected on a property it cannot have: per-record AEADs **cannot detect absence** — a backup missing works imports "successfully" | RESOLVED | 2026-08-01 |
| [D48](D48-restore-output-policy.md) | `restore` output policy — default `./antseal-restore-<work-id>/`; defensive lexical re-rooting (absolute prefix stripped, `..` rejected, collisions abort pre-write); **per-file byte-aware overwrite**: identical = already-restored success (idempotent), differing = refused-overwrite never touched, verification-failed never written. Temp+rename; no prompts; continues past per-file errors with a severity-ordered exit; in-place possible explicitly, never by default | RESOLVED | 2026-08-01 |
| [D49](D49-dry-run-network-semantics.md) | `--dry-run` network semantics — **real `quote_batch`**, confirmed and sharpened: dry-run = the real pipeline **truncated after quoting** (a prefix, not a parallel mode). Found and resolved the **S12↔U16 contradiction** (journal-before-backend vs zero-vault-mutation) by scoping the invariant to the paid path — dry-run runs journal-disabled, own S16 row. Runs the S8 preflight, exits the real insufficient-ANT/-gas codes (scriptable preflight gate); cost labeled **indicative** (fresh-nonce unlinkability cuts both ways) | RESOLVED | 2026-08-01 |
| [D50](D50-os-keystore-scope.md) | OS-keystore platform scope — **framing overturned: NO keystore platforms at M1**; keyfile wrap only, zero new deps. "Which platforms?" is unanswerable before D72 without pre-committing it; the header makes later platforms additive: U8's AAD-bound wrap-mode registry (0 = none, 1 = keyfile, **2 = os-keystore RESERVED** with a distinct not-supported error). keyring 4.x churn-active; linux-keyutils reboot-volatile (would brick the vault); machine-bound factors break D47's clean-machine drill. Platform scope = recorded post-D72 follow-up | RESOLVED | 2026-08-01 |
| [D51](D51-json-interactivity-contract.md) | `--json` × interactivity — hard-abort **confirmed and widened**: machine mode (`--json` ∨ non-TTY stdin ∨ stdin consumed by fd 0) never prompts for **anything**; every prompt class declared with a non-interactive channel or a deliberate absence (`vault import` overwrite: none in v1 — scripted overwrite-import unsupported by design). Report always renders (stderr under `--json`); declined ≡ unobtainable = one consent-not-obtained class; stdin `isatty` gate, `/dev/tty` rejected; post-receipt resume not consent-bearing | RESOLVED | 2026-08-01 |
| [D52](D52-devnet-e2e-venue.md) | Devnet E2E venue — **register lean overturned**: a **required local gate** (`scripts/e2e-devnet.sh`, mandatory before merging storage-touching changes, Q14-pattern evidence) + a **scheduled non-required hosted job** at reduced node count. Per-PR required CI rejected (branch protection 403 on Free-private makes a 20th context unenforceable; same 2-core runner class vs a 688-package graph and the 10 GiB cache cap; S18 SIGKILL flake profile); self-hosted rejected (only candidate host = the 2-core dev machine). Q15's "cached node binaries" premise obsolete — nodes are in-process. Promote-to-required trigger recorded (plan upgrade or Q65 flip + ≥20 clean scheduled runs, warm ≤15 min) | RESOLVED | 2026-08-01 |
| [D53](D53-chain-invalid-at-gentime.md) | State for a TSA chain that does not validate at `genTime` — the register's **binary framing is overturned**: the question is a six-way partition and MVP-SPEC.md:109/133 had already ruled two sub-cases the *opposite* way from the lean. Partition principle (shared with D56): **`invalid` iff the artifact makes a claim the verifier can refute from trusted material; `internally-consistent-only` iff it makes no refutable and no confirmable claim**. Temporal failure at `genTime` (**both** directions — the not-yet-valid/back-dating half is named in no spec line, task or matrix entry), failed path constraints, and a failed link signature on a chain **naming a pinned root** → `Invalid` with three new `anchor-` codes; no pinned root reached, bundle-supplied roots included → `InternallyConsistentOnly`. Decisive evidence for the lean's true scope is mechanical rather than editorial: error-contract §4b layer 3 refuses a pending row re-claiming `anchor-untrusted-root`'s `verdict:internally-consistent-only` key, and its only escape hatch is an explicit record asserting the two mutations are **one observable** — false for "unpinned TSA" vs "back-dated token". (The check is dormant today because both rows carry `expected: null` — itself the defect Q76 fixes.) Rules quantify over the candidate-path *set*, so the state is permutation-invariant and a relay cannot demote an honest anchor by appending a certificate to the **unsigned** bundle. Also found: **there is no `anchor-` error-code prefix** (§2's table has no A row, and A cannot borrow `bundle-`), and line 168's M2 enumeration is **six clauses / eight cases**, with two incompatible "sevens" in the tree. Zero frozen report bytes change — held so by keeping diagnostic codes out of `AnchorResult` (D86's ruling, one domain over) | RESOLVED | 2026-08-02 |
| [D54](D54-default-ots-calendars.md) | Default OTS calendar set — **upstream's own four defaults verbatim** (`a.pool.opentimestamps.org`, `b.pool.opentimestamps.org`, `a.pool.eternitywall.com`, `ots.btc.catallaxy.com`, `otsclient/cmds.py:186-189`), which are **aggregator aliases, not the calendar hostnames the A25 bootstrap used** — and the alias indirection is the whole point: the dead `finney.btc.calendar.opentimestamps.org` was never an upstream default, while every real default is live (measured, 18 submissions). Success is counted over **distinct pending-attestation URIs** (a pool and its calendar return the same one, measured 4/4 rounds), ≥2 normal / 1 thin / 0 absent — **none ever blocks a seal**, because A20's gate is TSA-only and a seal-time OTS anchor is `pending`. No calendar key exists to pin and none is needed: an impersonated calendar can deny service or return a *later* time, never an earlier one, so this is a liveness decision. What is pinned is an **append-only upgrade-URI host allowlist** (3 suffixes, strict dot-boundary, `https`-only), closing an SSRF the U24 every-invocation hook would otherwise expose. Replacing a dead default is **not** a format event — no byte of `antseal-core` names a calendar | RESOLVED (A13/A14/A20/A25 + new A42/A44/U44 implement) | 2026-08-02 |
| [D55](D55-arbitrum-rpc-pairs.md) | Default Arbitrum advisory-RPC pairs — **one = `arb1.arbitrum.io/rpc` + `arbitrum.drpc.org`; sepolia = `sepolia-rollup.arbitrum.io/rpc` + `arbitrum-sepolia.drpc.org`; devnet disabled**. Two measurements decided it and a liveness probe would have missed both: `publicnode` answers `eth_blockNumber` in 0.75 s and then rejects `eth_getTransactionReceipt` with **"Archive requests require a personal token"** (so endpoints are probed with the production method, never a cheaper one), and `1rpc.io/arb` returns `ACAO: *` on the **preflight** and none on the **POST** (so CORS is measured on the POST — which also answers D66's Arbitrum half affirmatively for all four ruled endpoints). "Must agree" is over an extracted tuple `(present, status, blockNumber, blockHash)` with **zero tolerance** and never over bytes or the whole object: the two ruled endpoints return different key sets for the same receipt (`timeboosted` vs `blobGasUsed`) while the tuple is identical. Head-height agreement — the brief's hard case — is not in A17's comparison and could not be (5 endpoints, **3 blocks apart inside 0.878 s**). Disagreement is **advisory-degraded, never an error**, locked by a test requiring the D29 report bytes to be identical across all five overlay outcomes. Adds a fifth outcome `Lagging` A17 lacks, an `eth_chainId` guard it lacks, and **overturns the landed `[verify] arbitrum_endpoints` config slot as unable to hold a per-network pair** | RESOLVED (A17 + new U44 implement) | 2026-08-02 |
| [D56](D56-ots-internally-consistent-trigger.md) | OTS trigger for `internally-consistent-only` — the alleged conflict is **real** (line 133 says the state, 108/168 say `invalid`, on the same input) and the register's line numbers are right, but its fix is half an answer: **108/168 win**, *and* the state stays OTS-reachable, because its true trigger is an `.ots` whose ops commit `anchor_digest` but whose every branch is an **unevaluable op/attestation type** (A11's "typed unverifiable results") — line 133's own definition, unedited. Line 133's *parenthetical* is corrected, not its definition. Two spec silences closed: `--online` attempted but endpoints **unreachable or disagreeing** stays **`attested`** (enforced structurally — `OnlineEvidence` has no failure variant, only `Header`/`NoSuchBlock` keyed by height, so "network weather changes the cryptographic verdict" is unrepresentable), and because A13/A14 make the **merged multi-branch `.ots`** normal while line 108 addresses a single one, per-branch precedence is **best-evidence-wins**: refutation-wins would hand any relay a downgrade-to-`invalid` primitive over the **unsigned** bundle, while best-evidence-wins concedes nothing (the forgeable ceiling is `attested`, already non-headline-eligible by design). Complete 10-rule order O0–O9 with every overlapping pair adjudicated; `anchor-ots-online-block-absent` is a new refutation the spec never named. Four `anchor-ots-*` codes minted; zero frozen report bytes change | RESOLVED | 2026-08-02 |
| [D57](D57-tsa-root-store-scope.md) | Pinned TSA root-store scope — **include the alternates, lean confirmed on a reason the register never gave, and the list replaced by a closure rule** (*a root is pinned iff antseal NAMES a TSA whose live chain closes at it*). The decider is the sealing side, not rendering: U26 makes alternates configurable and A20 aborts a seal on zero fully-verified tokens, so documenting an alternate whose root we withhold ships a **pre-payment abort**, not a weaker bundle. Store v1 = ****4 roots / 6 429 B**** (**0.31 %** of D87's per-version budget). Two findings from live tokens at all five TSAs, neither reachable with A24's test CA: **2 of 5 chains terminate at a CROSS-CERTIFICATE** (DigiCert→Assured ID, Sectigo→USERTrust; SPKI equality proven), so A9 must **stop at the first pinned match** or the production DigiCert default renders `internally-consistent-only`; and **3 of 5 tokens ship their own self-signed root**, in **3 different certificate orders**, so P2 needs a positive twin and P3 forbids any positional assumption. Also measured: FreeTSA signs **`ecdsa-with-SHA512`** (A8 names no digest) and SwissSign's leaf is **RSA-3072** (no size allow-list). The provenance procedure names four channel classes and was **executed** — Wayback snapshots from 2016 and 2019 corroborate FreeTSA, NSS corroborates DigiCert — with the three unfinished rows stated as A7 work; mandating root-programme membership would have been unexecutable, since **only 2 of 5 candidates are in Mozilla NSS**. Corrects A7's "DFN-PKI/T-TeleSec" (the live chain is a self-signed DFN Community Root 2022) and finds `crt.sectigo.com` serves its root over **plain HTTP only** (TLS handshake failure) | RESOLVED (A6/A7/A26 + new A43/A44 implement) | 2026-08-02 |
| [D58](D58-opentimestamps-viability.md) | `opentimestamps` 0.2.0 viability — **not adopted in any form; the `.ots` codec is in-house in `antseal-core` over the already-pinned `sha2`, at +0 packages against the crate's +13.** Both halves of the register's framing overturned: it **does** compile on `=1.92.0` and wasm32 (`cargo build` codegen green), so the stated test decides nothing; and "vendor/fork" is not a cheaper contingency because the crate is **edition 2015** and every line needing change is a line. Decided on measured behaviour instead — an **80-byte** `.ots` drives an uncatchable `SIGABRT` from `vec![0; attacker_varint]`, a **102-byte** one from an unbounded hexlify chain, an **87-byte** one panics in debug and *parses to a different answer* in release, and a **90-byte** one makes this crate and `python-opentimestamps` report **different attestation sets from identical bytes** (the crate ignores the declared attestation payload length; the reference sub-slices, caps at 8192 and asserts EOF). Unwrappable: `from_reader` fuses parse + op execution + recursion in one call, so any pre-filter enforcing A11's limits *is* the parser. Also: `env_logger` is a non-optional dep of a codec, and `core-dep-graph`'s forbidden list would not have caught it (→ Q74). Establishes the `.ots` container byte-by-byte from the A25 captures (65-B header, `0xff` before every branch but the last; 3-calendar merge = 664 B, round-trip byte-identical), and — by falsifying it against corrupted commitments — that the upgrade endpoint's discriminator is **three-way, not two**: `404 + "Pending confirmation in Bitcoin blockchain"` (re-poll) vs `404 + "Not found"` (hard error), separated by body alone, which also validates the op executor against a live calendar (→ A14). **Seven limits set with F4 rows** (ops 4 096, depth 1 024, width 64, attestations 256, operand 16 384 B, running value 32 768 B, attestation payload 8 192 B), two of them absent from A27 §4's "exhaustive" list and each closing a measured abort; measured, a 1 048 566-B `.ots` carries **74 893** attestations under `MAX_OTS_BYTES`. **P18 retired** | RESOLVED | 2026-08-02 |
| [D59](D59-request-nonce-persistence.md) | Request-nonce persistence semantics — capture-time only: nonce generated in `antseal-anchor` (8 B, OS CSPRNG, never from `W`), compared in core only when supplied, persisted in the vault, and **verdict-inert** on every bundle path. Register's conclusion kept, its reason corrected — the nonce *is* in the bundle, inside the signed `TSTInfo`. Bundle-side request-nonce field ruled a permanent non-rule | RESOLVED (A8, A10, A2) | 2026-08-02 |
| [D60](D60-der-cms-x509-pins.md) | DER/CMS/X.509 pins + P-256 — **seven pins into `antseal-core`, `p256` NOT among them**: `der =0.8.1`, `const-oid =0.10.2` (`db`), `x509-cert =0.3.0`, `cms =0.3.0-pre.2`, `p384 =0.14.0`, `rsa =0.10.0-rc.18`, `sha1 =0.11.0`, plus `oid` on `sha2`. Measured **55 → 82** packages, **zero** new duplicate pairs, zero `getrandom`/`rand`, wasm32 green. Both stable lines overturned: `rsa 0.9.10` drags `rand 0.8.7` into the normal graph and **fails the `core-dep-graph` regex outright** (+22 pkgs, 9 dupes incl. a second `signature`/`digest`/`der`); `cms 0.2.3` forks the DER stack and adds a **second `x509-cert`**. `sha1` is REQUIRED and was missing from the register's list — ESSCertID v1's `certHash` is SHA-1 by definition and FreeTSA sends v1 only; answered as a *selector*, never the trust decision. **P-256 out**: of 9 live TSAs surveyed, exactly one uses ECDSA and it is P-384; cost of adding later priced at +1 package. The `to_der(from_der(x)) == x` strictness rule **rejected on measurement** — 6 of 9 real TSAs, DigiCert included, emit a non-DER-sorted `certificates` SET. Limits: depth **63** consumed from `der` (no antseal walker — a recursive one stack-overflows to `abort`), `MAX_CHAIN_CERTS` 8, `MAX_CHAIN_CERT_BYTES` 16384 (`<= MAX_CERT_BYTES`), `MAX_SIGNED_ATTRS` 16. One mandatory RUSTSEC ignore (`rsa`, Marvin — private-key-only, antseal verifies only) | RESOLVED (A5/A8/A9 implement) | 2026-08-02 |
| [D61](D61-fuzz-venue.md) | Long-run fuzz venue — the existing `fuzz-nightly.yml`, re-cadenced twice-weekly at 600 s/target under a 700 min/month ceiling with a machine check. OSS-Fuzz rejected as not applicable — its criterion is adoption, not visibility, so Q65 alone does not unblock it; ClusterFuzzLite rejected (same minutes, more machinery). Found the committed daily × 4 × 900 s configuration spending ≥ 91 % of the free allowance, live and unobserved since 2026-07-28 | RESOLVED (Q9, Q17, Q65) | 2026-08-02 |
| [D62](D62-verifier-page-host-and-canonical-url.md) | Verifier-page host + the one canonical URL — **`https://antseal.org/`** (apex, `https`, trailing slash, no `www`, no subdomain, no path) on **GitHub Pages** from the private `aed900/antseal` via an Actions publishing source, and the register's vendor-pick framing **OVERTURNED on four measurements**. The vendor was never open: D2 already put source, CI and the release channel inside GitHub, so **party count** is the metric and every other host *adds* a party to the exact threat spec line 186 names. "Point antseal.org at it" is not a no-op — the domain answers plaintext today with a third party's scripted page and no HTTPS at all, while U28 begins printing that URL **this wave**. P3's *"serve HSTS with `preload` from day one"* — the compensation it paid for choosing `.org` — is **undeliverable** on the ruled host (Pages emits no security header of any kind, measured on two custom domains with Enforce-HTTPS on) and is **retired**, replaced by Enforce HTTPS plus an optional **CAA** record, which addresses mis-issuance, the stronger attack. `antseal.dev` is **refused**: 8 near-neighbours are free, the `.com` is unobtainable since 2004, and P3's `.dev → .org` redirect would have minted the second working URL line 139 forbids. R26's scripted check hashes the **decoded** body, with both encodings as a transform tripwire and a cache-window re-poll rule, because one Pages resource yields two transferred hashes and exactly one decoded hash. The general rule the host's header-lessness forces: **every security property the page relies on lives inside the hashed artifact, never in host configuration**. `VERIFIER_URL` lands in `antseal-cli` now, ahead of the deploy, consumed by U28 alone. The host choice moves line 186's residual **not at all**, and the record says so rather than dressing a convenience as a mitigation | RESOLVED | 2026-08-12 |
| [D63](D63-footer-build-hash-and-artifact-set.md) | Footer build-hash mechanism + the exact artifact set the published SHA-256 covers — **build-time injection**, with the register's dichotomy **half overturned**: real as the mechanism for the *digest*, where injection wins ON MEASUREMENT (the runtime self-hash cannot run under the `file://` mode R23's Accept requires, costs a second full read of the largest artifact, makes the footer async and silently degradable, and defends nothing extra — against a hostile host **both arms prove nothing**, because both are computed by the suspect); **false** as the mechanism for the *footer*, because R22's `Do` already commits the module to exporting the identity fields. Displayed value = **SHA-256 of the deployed `.wasm` as the browser loads it**, 64 ungrouped lowercase hex, never truncated; version / format versions / commit render from R22's export and never from injected HTML; the advice line is `MVP-SPEC.md` line 139's sentence verbatim with a drift test, and is **not** added to R18's frozen table. Published artifact = **`SHA256SUMS` over the complete served closure** (post-injection, content-decoded, byproducts deleted, itself excluded), authoritative **only** in its signed-release copy. The chicken-and-egg is cut by **two deliberately different sets** — the footer names the one artifact that structurally cannot contain it, the manifest sits outside its own list — and both escapes die on measurement: the two-pass terminal form is a ≈2²⁵⁶ preimage search, and a content-addressed filename is the same fixed point because the output filename is *inside* the artifact. The build script is fenced to a closed operation list, which is how line 137's *"no build beyond `wasm-pack`"* and line 139's mandated footer hash are held together. Preconditions measured absent 2026-08-12: D18 unruled, no `wasm-pack`/`wasm-bindgen(-cli)` pin, no `--remap-path-prefix` (the current wasm32 artifact embeds the checkout path 300× and `$CARGO_HOME` 1 274×), no optimizer pin. Zero frozen bytes | RESOLVED | 2026-08-12 |
| [D64](D64-online-overlay-headline-presentation.md) | Online-overlay ↔ headline layout — **two stacked blocks: the lean's structure survives ON MEASUREMENT, its overlay-content clause OVERTURNED on the spec's own sentence.** Offline block first, complete, **byte-identical to a run without `--online`** (R24's frozen Accept kills every composite/provenance-column arm), carrying no overlay-contingent marking; the overlay is a **full second aggregation** rendered as an impact against the offline verdict — heading with "online"+"advisory"+a non-replacement clause disclosing it can strengthen OR refute; exactly ONE headline-impact line (`Supplies` — the one spec template with a mandatory online-attribution marker / `Stands` — points at the offline headline, compares time AND source / `StillUnanchored`); always-rendered per-probed-anchor outcome lines (no TSA lines; state word only on refutation; disagreement lines carry no state word and suppress promotion for that anchor alone), overlay-only receipt-confirmation line, endpoints-disclosure line, change-only aggregate deltas. The promoted time exists nowhere in report v1 (`verified_time_unix` null on `attested`), so R17 ships `VerdictAggregate` + `OnlineOverlay` + a core-owned typed probe-outcome input (data-only, rendering-input never verdict-input, `evaluate_anchors` untouched), display strings embedded (R74/R22 pattern). **D105 adjudication: the overlay is a SIBLING document, never a report-v1 field** — `REPORT_VERSION` stays 1, carriage is U30's `--json` envelope beside verbatim report bytes (R11's `LiveCheckReport` precedent); never-overwriting becomes an equality: `--online` and offline runs emit **byte-identical report bytes**. Structural tokens are D64's; every sentence spelling is R18's. Zero frozen bytes | RESOLVED (R17/R18 implement; R21/R22/R24 consume) | 2026-08-11 |
| [D65](D65-json-schema-stability-commitment.md) | `--json` schema-stability commitment at M3 — the *"promise a stable versioned JSON schema from M3"* lean **OVERTURNED as a blanket** and replaced by a **three-tier partition, per member**, which hands a scripter *more* than the lean asked for on the tier that matters. **Tier A — `result.report` is FROZEN already, harder than M4 could freeze it**: carried **byte-verbatim**, pinned by 21 committed golden vectors, `EXPECTED_CANONICAL_JSON` and a native↔wasm32 bit-match, moving only through a `REPORT_VERSION` bump — with its one hole priced rather than hidden (an enum-valued key may gain a spelling with **no version moving**, so a consumer always writes the default arm). **Tier B — the envelope wrapper is STABLE-ADDITIVE from M3**, under the `ENVELOPE_VERSION` already in force and already exact-key-set asserted; inside the error object `class`/`exit_code` are stable identifiers while `message` stays freely rewordable, adopting the error-code contract's own sentence. **Tier C — everything else inside `result` is DECLARED UNSTABLE until U32**, reviewed by a blessable byte snapshot but promised to nobody, because that instrument returns the **same red** for an added key and a renamed one. Blanket-*instability* is refused too, on a string the shipped binary prints today. **`REPORT_VERSION` is never a `--json` schema version** — the two axes are independent and code deriving one from the other is wrong. The finding that changes code: `success_envelope`'s only signature takes a `serde_json::Value`, `serde_json::Map` is a `BTreeMap` in this build, so routing the report through it **alphabetizes its keys and destroys D29 rule 1's wire order** — the tree already demonstrates the loss on itself — which is why tier A's acceptance test is a **byte containment** and not a structural comparison, since the structural comparison passes the bug. `decode_layer` ruled **NO** for envelope v1 in both of its homes; U50's scope answered (tier C, with the `null`-is-a-value rule promised at every tier); `ok` = *a result document was produced*. Zero frozen bytes | RESOLVED | 2026-08-12 |
| [D66](D66-browser-endpoint-cors-and-fallback-policy.md) | Browser CORS viability of the pinned endpoint sets + the fallback policy when a fetch fails — all six endpoints are browser-usable today and **no pinned default changes**, but the *"compatibility checkbox a quick probe answers yes/no"* lean is **OVERTURNED ON MEASUREMENT, in a real browser**. Both esplora operators have **no `OPTIONS` route at all** — the preflight 404s with the router's generic `endpoint does not exist "…"` body — so the pair is reachable **only** by a request that never triggers one: measured in Chromium 150 against the real `blockstream.info`, the GET **succeeds** with `Accept: text/plain` and **throws** with one extra header, while a curl probe reads `ACAO: *` in both cases. So the cheap probe does not under-describe the answer; on a page that adds a header it reports the opposite of it — and the header set belongs to code R24 has not written. The Arbitrum half is viable for the **opposite** reason: its `application/json` forces a preflight and the preflight works on all four (D55 re-confirmed). CORS rejection, preflight failure, connection-refused and DNS failure are the **identical** `TypeError` / `"Failed to fetch"` / `cause: undefined`, so the page **may never name CORS or any cause** — and a thrown fetch is `ProbeFailureClass::Transport`, **never** `NoSuchBlock`, fail-closed **by construction** because D56 O7's refutation needs a 404 **body** and a throw has none. One endpoint permanently unreachable never promotes on the survivor and never silently substitutes a third. Overrides stay (spec line 137, verbatim) and gain the CLI's A49 rule — `https://` only, exactly two entries, rendered as text and never as markup. R18's three-class failure vocabulary **survives ON MEASUREMENT** and its coarseness is now **pinned**, because the shared table is renderable by both surfaces only while no class names a cause a browser cannot observe. The residual the framing hid: **nothing watches** — A44, itself still open, probes Arbitrum POST CORS by name and has **no esplora check of any kind**, covering the pair that can never move a verdict and omitting the sole route to `proven` and to `invalid` | RESOLVED | 2026-08-12 |
| [D67](D67-disclosure-preview-snippet-format.md) | Disclosure-preview snippet — text 64 B to a code-point boundary, binary `hex:` + 16 ungrouped lowercase pairs, marker `…` outside the quotes, form = manifest's committed domain (mirrors always hex), closed C0/C1/bidi escape set, absent arm `(snippet unavailable)`; no-snippet and length-only arms refused on measurement; zero wire impact, snapshot-frozen at M3 | RESOLVED | 2026-08-11 |
| [D68](D68-units-syntax-and-default-bundle-filename.md) | `--units` acceptance envelope (ranges refused) + `reveal`'s default output filename — **(1)** a comma-separated list of work-global unit ids and nothing else; the shipped parser (`value_delimiter = ','`, `Vec<u64>`) is **ratified verbatim** and ranges are refused for MVP — but on the id space's own properties, not on the spec's example, because line 149's canonical list spells the value `…` and expressly calls line 36 an example, so the register's stated ground does not survive its own citation. Ids are dense and gapless, so a range is *arithmetically* well-posed; it dies because raw mirrors sit at positions **invisible in range notation**, every disposition of a mirror-spanning range is bad, and a range's off-by-one at the upper bound **promotes a file to a full reveal** under D70/D28, disclosing `file_salt` and `s_root` — the offline confirmation oracle spec line 95 exists to deny. The refusal creates one obligation: a range-shaped value must be rejected by a **message naming the supported form**, not by clap's `invalid digit found in string`. **(2)** The *"trivial pick"* framing is **overturned**: the friendly title-derived name is a path-injection and audience-widening surface over free-form unbounded user text, the two in-house precedents (D48's work-scoped directory, U12's timestamped export) **disagree and neither transfers** — reveal's output is a function of an anchor set the product is designed to improve, so D48's byte-aware convergence is structurally unavailable — and the load-bearing clause is an **ordering rule, not a name**: resolve and check the target **before** the preview, before U29's consent gate and before any fetch, so no user is ever asked to consent to an irreversible disclosure that then cannot be written. Default `./antseal-reveal-<work-id>.sealproof` (stem maintainer-confirmed 2026-08-12), 89 bytes by construction, closed `[0-9a-f]` alphabet; collision = **refuse**, never overwrite, suffix, timestamp or prompt; `create_new(true)` for exclusivity, not temp+rename, which would clobber. Mints `BUNDLE_FILE_EXTENSION`. Zero wire surface | RESOLVED | 2026-08-12 |
| [D69](D69-verify-exit-code-mapping.md) | `verify`'s exit-code mapping — the register's **FRAMING is overturned** and the UNANCHORED sub-question settles as a consequence. There is no scalar verdict to map: `verify_bundle` returns `Result<Report, VerifyError>` and the `Ok` arm carries a `Vec<AnchorResult>` of independently-stated states, exercised as mixed sets in the shipped A1 suite. And the binding constraint is **carriage, not numbering** — shipped `main_entry` has exactly two arms, so under the machine as built *any* nonzero exit emits `ok:false` with **no `result` at all**. Ruling: a **third arm** (`Outcome.exit_class: Option<ErrorClass>`; `ok` re-read as *"a result document is present"*, maintainer-confirmed 2026-08-12; `ENVELOPE_VERSION` stays 1, no key added, removed or renamed), then **D48 §6's severity fold extended** over the anchor set — `verify-bundle-rejected` **40** (the `Err` arm; tamper and malformed are ONE class, the 248 rejection codes riding in the message per the two-namespace rule) > `verify-anchor-refuted` **41** > `verify-headline-divergence` **42** > `verify-unanchored` **43** > **0**, with 44–49 still reserved. **UNANCHORED is nonzero and distinct** so a caller who accepts undated bundles can opt back in — the `status`-exits-0-on-`invalid` precedent is confined by its own reason (*a fact about the vault*) to a sealer-facing command, spec line 137 mandates a **loud** banner exit 0 would silence for every non-human consumer, and line 137's minimum-anchor guarantee means a normal seal never reaches the rung. `--online` **may** move the code in all three directions and D64 is untouched (its equality is over report **bytes**, its inviolability over the offline block's **rendering**); probe failure and disagreement **cannot** move it, as a theorem off `OnlineBlockResult`'s missing failure variant. `--live` and storage-linkage **never** move it. **D48 §6's carriage is CORRECTED** — its own *"per-file detail always available in the `--json` result"* is unkeepable through `CliError`, and the committed `restore` fixture proves the house believed it. `reveal`'s 23 `RevealError` variants mapped onto shipped classes plus exactly one new — `reveal-inputs-unusable` **36**, outside the reserved band by construction | RESOLVED | 2026-08-12 |
| [D70](D70-raw-mirror-auto-include.md) | Raw-mirror auto-include on whole-file reveals — **automatic, on measurement: the spec permits-not-mandates** (line 114's mirror clause is conditional; the mirror-less full reveal is the frozen REQUIRED vector `single-text-with-mirror/full-no-mirror`, discharging R9's "full text reveal" row) **but kills both alternatives** — a v1 opt-out flag dies on line 149's canonical CLI list, never-include voids line 92's carve-out and leaves the M0-frozen rows 9–10 permanently dormant, recreating for `raw_commit` the unfalsifiable-commitment defect D28 refused. **Nothing reserved for the v1.1 opt-out — the reservation already exists as frozen verifier tolerance**: a `--no-mirror`-class flag is pure builder/CLI surface, zero format cost, no reserved key minted (absence-carries-shape, D28 rider 1). **Both mirror ⟷ full-reveal directions ruled BUILDER guarantees**: verify_bundle enforces neither (frozen full-no-mirror accept; R53 partial-with-mirror tolerance — the module doc's "needs its own decision first" answered with a refusal), so R13 asserts both internally, R14 (c) gains the converse arm, the verifier gains no duty. Mirror emission conditional on existence (`needs_mirror ⟺ raw ≠ canonical`, ≤1 per file by D23) — a no-op when absent; `--no-fine-tree` orthogonal. Honest residuals recorded: `--force-text` mirrors are genuine marginal disclosure (consented, previewed); the report does not surface a withheld mirror (fed to R18/R19's open mirror-wording policy). Zero frozen bytes | RESOLVED (R13/R16 implement) | 2026-08-11 |
| [D71](D71-binary-signing-mechanism-and-key-custody.md) | Binary signing mechanism, public-key pinning and custody for a key that can never be revoked — **minisign CONFIRMED as the tool, but two of the lean's supporting clauses OVERTURNED and cosign killed on a ground no brief named**. The trust-bootstrap case for pinning the key on the page falls to measurement: `antseal.org` and `objects.githubusercontent.com` (where release assets actually download) present the **identical chain** — Let's Encrypt YR1 → ISRG Root YR — with the asset host's leaf subject literally `CN = *.github.io`, so a page or README pin proves nothing TLS did not; and with the repo private the README pin is **unreadable** (anonymous GET → HTTP 404). **Cosign keyless dies on DISCLOSURE, not on offline**: Fulcio certs carry OID `1.3.6.1.4.1.57264.1.22` *"Source Repository Visibility At Signing"* and Rekor answers unauthenticated, searchable and append-only, so a keyless signature would publish a private repo's identity permanently and **decide Q65 by side effect**. Rekor's real gift — publicly detectable key substitution — is recovered instead by **OTS-anchoring the public key through this project's own A13/A14 machinery**, which publishes only an opaque digest. Verified rather than asserted: no signing tool is installed on this host, so minisign was implemented from its published spec and a **real third-party release** was tamper-matrixed — a data-byte flip and a trusted-comment edit both fail, but **an untrusted-comment rewrite still ACCEPTS and the 0.11 release verifies under its own signature**, so replay/downgrade is invisible to a content signature (`minisign.c:556` prints the trusted comment without checking it); closed by mandating `-Q`. Spec line 157's `sums + signature` is the conjunction, its `minisign/cosign` slash a **disjunction**. **The CLI is NOT reproducible** — `--remap-path-prefix` lives in one executable place, `scripts/wasm-pack-build.sh`. Size 271 043 B vs cosign's 141 178 250 B (520.9×); minisign is in Debian, an independent bootstrap root. **Addendum 2026-08-16**: the maintainer decided to make the repo public, withdrawing §3's deciding argument and four dependent clauses while every non-visibility measurement stands; re-ruled on the ground that **`cosign verify-blob` has no `--offline` flag** (trusted root over TUF) and this check sits upstream of everything — minisign still wins, *both* moves REFUSED → **DEFERRED** to v1.1, the OTS anchor is retained because **Rekor does not come without cosign**, and **§A R5.5 forbids producing any keyless signature until the repo is public in fact**, or the append-only log carries `private` for ever | RESOLVED (addendum 2026-08-16) | 2026-08-16 |
| [D72](D72-release-targets-distribution-and-crates-io-scope.md) | Release target set, distribution channel and what a crates.io publish would oblige — **Linux-only and GitHub-Releases-only both CONFIRMED, but on different grounds than the lean, and FOUR premises overturned on measurement**. **(1) "Names reserved but unpublished" is FALSE** — all five were really PUBLISHED 2026-08-11 by `aed900` at `0.0.0`, `MIT OR Apache-2.0`, unyanked: on crates.io, reservation **is** publication. **(2) "macOS/Windows stay CI-verified" massively overstates CI** — `scripts/ci-lanes.sh:935-944` is the whole cross-OS lane, `cargo test -p antseal-core --locked -- corpus_ vector_`: ONE crate's determinism slice, neither a build nor a test of the workspace, and `antseal-cli`/`net`/`anchor` have **never been compiled on macOS or Windows by any lane**. Linux-only therefore narrows nothing proven — the others are UNPROVEN. **(3) musl is not the escape hatch** — the target installs but both builds fail, default at `ring v0.17.14` and `--features ant-backend` at `aws-lc-sys v0.43.0`, both missing `x86_64-linux-musl-gcc`, exit 101, and `ring` is unremovable (rustls←ureq←antseal-anchor); measured gnu floor **`GLIBC_2.34`**. **(4) the release FEATURE SET, which D89 §Consequences 6 assigned here, was omitted from the brief** — a default build is 182 packages and **cannot seal**, the shippable one 677 including `self_encryption v0.36.0`, so D6's "distribution effectively GPL-3.0" is confirmed **in the resolved graph**. **R6, the ordering blocker nobody had**: the repo is PRIVATE, private-repo release assets need auth, and the M4 gate requires the clean machine to install with **"zero repo-checkout resources"** — so **"GitHub Releases only" is INOPERATIVE until Q65 resolves**, and Q65 is re-ruled from "M4 alongside Q22/Q28" to a **precondition of Q31 and Q34**. Sharpest single measurement: `cargo publish --dry-run -p antseal-core` → **exit 101**, *"depends on `antseal-core` with feature `test-util` but `antseal-core` does not have that feature"* — the self-dev-dep resolves against the **registry**, finds the empty placeholder, and refuses, so **the reservation artifact blocks the real publish of the name it reserved**. Cascade: **U33 closes** on a recorded stdin-only limitation, and **D50's keystore follow-up is answerable NO and inverts** — Linux-only picks the worst platform for a keystore — with wrap-mode 2 measured **reversible, not frozen** (no `docs/format/` hits) | RESOLVED | 2026-08-16 |
| [D73](D73-external-completions-without-telemetry.md) | Counting the ≥10 external completions with zero telemetry — **the bundle is the evidence, the third leg cannot be evidenced at all, and the metric is reported as TWO numbers, never one**. The register's own framing is overturned twice: it asks *"facilitated sessions vs self-report vs both"* and **both named arms count claims**, while the arm it never listed — **the participant hands over the `.sealproof`, which the maintainer verifies offline with the shipped binary** — is the only candidate producing something a non-participant cannot fabricate, and is adopted. The larger overturn is that **the metric is not uniformly measurable**: legs 1–2 (seal, reveal) leave signatures behind and are cryptographically evidenced, but **leg 3, third-party page verification, leaves nothing BY CONSTRUCTION** — D29 froze the report as a byte-deterministic function of the bundle and the `wasm-bitmatch` lane asserts the page emits those same bytes, so a pasted verdict or screenshot is **reproducible by anyone holding the bundle, the sealer included, and carries zero bits** about whether a third party ever looked. Q35's own *"optional shared verdict screenshots"* is that vacuous artifact and is **refused**. `pubkeys` (registry §7.2 key 5) makes distinct sealer keys an **upper** bound only — it can deflate an inflated count, never confirm a real one. **R8 is what makes the metric falsifiable at all**: the count is read on a fixed date, once, and the first reading is never revised; **R5 X7** requires the duplicate-vault check to have been run against a **planted** duplicate and recorded red before the count may be declared met. Methods rejected each on a stated cause of death: page-side counting (no backend to beacon to — **not** the CSP, which is `connect-src https:` and would permit one); pasted verdict or screenshot (D29 determinism, vacuous); GitHub issues (the venue does not exist — repo measured PRIVATE — retained as *transport*, never evidence); email (claim not evidence, plus PII); **on-chain payer counting** (no antseal marker exists, the project's own fresh-address-per-work hygiene moves the count in both directions, and it would build the very linkage table `MVP-SPEC.md:185` warns users about); facilitated sessions as primary (measures whether the product *can* be completed, not whether it *was*, by strangers) | RESOLVED | 2026-08-16 |
| [D74](D74-extraneous-full-reveal-s-root.md) | Extraneous `s_root` on a `FineTree::Absent` full reveal — **reject** (`full-reveal-s-root-without-fine-tree`), completing D28's present-set-==-required-set rule in the *unexpected* direction: adding material now fires too. One code — a `FileSalt` arm is unreachable. **Wave-6 ratification**: outcome stands and is now locked (reversal would delete a landed code, §3); the record's F8-provenance argument is corrected (it is about the *missing* direction), row 3 is reachable only by stripping the whole §7.14 entry, and the totality claim is re-audited — `.sealproof` bundles are **not union-closed** (→ R35) *(2026-07-31 correction, adversarial review finding 7: the D28 shape-totality clause this record inherited is corrected at source in both records — detection is total over **inconsistent** edits only; a consistent whole-file narrowing fires nothing, by design and harmlessly, because the unsigned bundle claims only what it discloses)* | RESOLVED (ratified wave 6) | 2026-07-28 |
| [D75](D75-full-reveal-cover-shape.md) | Full-reveal cover shape — **BOTH**: a full reveal ships per-unit covers *and* `s_root`. Keeps `covered_reveal.cover` at tier [P] so bundle validation stays decidable from the bundle alone (D78); on a full reveal the covers leak nothing. **Wave-6 ratification**: outcome stands, consequences corrected — the `s_root` rebuild is defence-in-depth for content binding *and* the **sole** check of the disclosed `s_root` itself (deleting it would recreate exactly the material D74 rejects), the "proves no extra leaves" claim is vacuous, and the R5 discharge of the routes-must-agree action holds only for bytes some check reads — the exception is **D83's inert leaf-level seed tails**, so closing D75 depended on D83. **Discharged 2026-07-28 (wave 7)**: D83 resolved as option B, so the exception is deleted — though not by the mechanism the contingency predicted. Agreement is now forced by **two** mechanisms, the folded-root comparison for the salt half and the canonical-zero rule for the tail, and the tail is caught by a **value** check rather than by deriving anything from it | RESOLVED (ratified wave 6; D83 rider discharged wave 7) | 2026-07-28 |
| [D76](D76-file-salt-split.md) | Split `file_salt` into raw/canonical salts — **NO for v1**: one salt keeps salting both `raw_commit` and `canon_commit` (line 95). Lean confirmed, its *reason* overturned — a 9th HKDF label is near-free pre-Q14 (the info encoding is injective by construction). The real costs: both salts' presence would become tier [R], making a §7.14 entry undecidable at F8 (D78); the reveal-shape code surface roughly doubles; and the split removes only a *confirmation* step, since the plaintext manifest publishes raw size anyway. Residual + 4 revisit triggers recorded | RESOLVED | 2026-07-28 |
| [D77](D77-zero-unit-file.md) | Zero-non-mirror-unit file — **reject at F5** (`manifest-empty-normal-units`). The registered shape (size-0 file with *zero* units) is already closed by `manifest-empty-units`; the live hole is the **mirror-only** file, which survives F5, R3 and R4 and permanently un-opens `canon_commit`/`raw_commit` — the condition D28 rationale 1 exists to prevent. Unreachable on the seal side (G5 always emits the `[0,0)` normal unit). D28's anti-vacuity clause stays as defence-in-depth | RESOLVED | 2026-07-28 |
| [D78](D78-bundle-schema-manifest-opacity.md) | May bundle schema validation open the embedded manifest? — **NO**: at schema time the bundle treats `manifest` as an opaque `bstr`. Not layering taste — it fixes **which permanent error family** a cross-side rejection lands in (D30 makes families append-only), and F8's error enum needed the answer before it was written. Consequence stated, not hidden: an `s_root` on a tree-less file is a *verify* failure, not a *schema* one (that is D74's code). Rules needing both sides are tier **[R]** by construction (D80 is the worked example). Enforced as a **type witness** — `BundleError` has no manifest arm | RESOLVED (record written wave 7; ratified wave 5) | 2026-07-28 |
| [D79](D79-ots-upgrade-group-presence.md) | OTS upgrade group (`block_height`/`block_header`/`fetch_date`) tied to `status == attested`, or free-standing? — **FREE-STANDING**. The all-or-nothing rule was never in question; the coupling was. `status` is **sealer-written and explicitly distrusted** (registry §6.1), so conditioning a *parse* rule on it would be the one place in v1 where a sealer chooses which shapes decode — a capability handed to the party the threat model treats as the adversary. Derived-not-declared, as D28. Scope is the parse rule only; D8 later ruled the recorded-value question the same way (all seven values legal, no subset ever). Unrepresentable rather than rejected: `Option<OtsUpgrade>` has no two-of-three state | RESOLVED (record written wave 7; ratified wave 5) | 2026-07-28 |
| [D80](D80-revealed-unit-touched-file.md) | Revealed unit's owning file in `touched_files` — **REQUIRED**, on spec line 95 ("`path_salt` ships whenever any reveal *touches* the file") plus line 121's binary revealed/unrevealed rendering. Path-hiding would hide only the *filename* — the plaintext manifest already exposes file identity, size and unit boundaries. Tier [R] (needs the manifest ⇒ D78 keeps it out of F8) | RESOLVED | 2026-07-28 |
| [D81](D81-unit-decrypt-cause.md) | `unit-decrypt-failed` cause discriminator — **none minted**. An AEAD authentication failure is one bit by construction, and outside the AEAD the verifier holds nothing separating a swapped unit from a flipped byte (it never holds `W`; the AAD binding is an input to the failing check; ciphertext length only separates 256-B padding buckets). The address-recompute and speculative-re-decryption discriminators are evaluated and rejected. One row (`verify-flipped-ciphertext-byte`); swapped unit and pipeline-level wrong `k_u` become recorded non-rows + tests + an R10 property, which needs a third case state in Q8's checker so Q14's zero-pending gate stays reachable | RESOLVED | 2026-07-28 |
| [D82](D82-touched-file-without-reveal.md) | `touched_files` entry for a file with no revealed unit — **reject** (`touched-file-without-revealed-unit`, R5 coherence group 1b), closing the converse D80 left open: the set now *equals* the revealed-file set. Line 121 does not merely omit "unrevealed file, path disclosed" — it states the opposite ("path withheld"), and line 95 says "only as committed placeholders". Also the D74 add-material hole in a second section: `path_salt` is a per-work constant, so a relay can splice a genuine entry into a bundle whose sealer withheld that file | RESOLVED | 2026-07-28 |
| [D83](D83-leaf-cover-seed-tail-malleability.md) | Inert upper 16 bytes of a `level == d` GGM payload — **option B, the canonical zero tail**: the disclosed value is `salt_i ‖ 0x00·16`, still `bstr(32)`, still tier [P] on length; the zero tail is a tier-[R] value rule. **C overturned** — a 16-byte payload contradicts MVP-SPEC.md lines 96 *and* 121 (which fix 32 B outright) and drops two exact byte lengths from [P] to [R] (the D76 argument) for a **≤ 0.95 %** wire saving; **A** is the irreversible direction and would turn R10's exact 88-byte authentication boundary into a per-bundle `88 + 16·k`. Scope **widened**: the rule also binds `full_reveal.s_root` at `n == 1`, which the register missed. Exposure re-measured — the `[0,n)` cover never decomposes (level-`d` only at `n == 1`); incidence is `d==0 ∨ a odd ∨ (b odd ∧ b<n)`, ~75 % of ranges, ≤ 2 nodes per cover. One code `fine-root-leaf-seed-tail-not-zero`; 4 hex strings in one vector file re-emitted | RESOLVED (G23 implements; freezes at Q14) | 2026-07-28 |
| [D84](D84-anchor-artifact-limits-permanence.md) | A's M2 `.ots`/DER structural limits — **NOT v1 format surface, do not freeze at Q14**; set at M2 against A25's real artifacts, as MVP-SPEC.md lines 153/155 already provide. **No exception to line 123 is created** (the register's mechanism, overturned): M0/M1 render every anchor `absent`, so no released verdict depends on artifact internals. What *does* freeze: the anchor envelope, D10 rows 6–9/16–19 over it, and rules **F1–F4** (limits evaluated only in the anchor stage; over-limit fails that anchor alone as `invalid`; raise-only afterwards). A5/A11's promised byte/count caps are ruled to *be* the existing D10 constants | RESOLVED (A27 records; A5/A11 consume at M2) | 2026-07-28 |
| [D85](D85-unit-commit-cause.md) | `unit-commit-mismatch` cause discriminator — **none minted**, and the rule generalised to **every salted-commitment opening** in R's pipeline (`unit_commit`, `path_commit`, `canon_commit`, `raw_commit`). The asymmetry R33 named is real — the salt is bundle-side, the commit is manifest-side, the manifest is signed — but it yields no attribution: "signature valid" leaves **two** bundle-side candidates, because the verifier never holds `W` and a bundle holder can re-encrypt arbitrary plaintext under the supplied `k_u` (line 103); "signature invalid" names no field, since one signature covers the whole body; and neither half separates a third-party edit from an adversarial sealer, which *is* the threat model. Obtaining even that coarse split needs signatures before files, re-binding every manifest-field row's expected code at once (§3). `path_commit` is the sharper case and the same rule covers it — **both** its inputs are bundle-side. A cause belongs in **rendering** (D27's `VerifyFailures`), never in the code set. R minted nothing *(2026-07-31 correction: the freeze universe is **86 codes / 28 variants** — the original "85 / 27" predated G23/D83's same-day code)* | RESOLVED | 2026-07-28 |
| [D86](D86-decode-layer-in-verdict.md) | Decode **layer** in the user-facing verdict — **it never enters `VerificationReport`**, and the two `layer()` accessors harmonise **upward**: total on both types, `Option` dropped, the manifest side derived from a new `ManifestError::map() -> MapId`. F26 proposed harmonising *downward* (bundle arm returns `None`) — **overturned**. D27 §4 makes the report exist only for a bundle that *passed*, so on the one path where a report exists there is no decode error and no layer; `layer` occurs **zero** times across all 21 pinned report byte strings, so F26's "expensive after Q14" premise was unreachable. The real defect was elsewhere: `body-unknown-key` and `envelope-unknown-key` are **one observable**, a live counterexample to the error-code contract's own §2 claim that the `(code, layer)` pair is pairwise informative (→ F27). No codes minted, no report vector re-emitted | RESOLVED (F26 implements) | 2026-07-28 |
| [D87](D87-bitmatch-vector-carriage.md) | Bit-match vector carriage before v2 doubles the tree — **keep `include_bytes!`**, with `MAX_EMBEDDED_BYTES_PER_VERSION = 2 MiB` per `v<n>/` enforced by a `build.rs` panic plus a testable second leg. F21 framed the growth as a cost to engineer away — **overturned by measurement**: carriage is **0.73 %** of the lane and 2.32 % of the artifact, while *executing* the vectors is **90.1 %**. Run-time loading and digest-streaming optimise the 1 % and each destroys a structural property — the module's zero imports, the crate's zero unsafe, and decisively "both sides execute identical bytes" as a **link-time fact** rather than a claim about the JS runner's plumbing. v1 uses 28.15 % of the budget (→ F28, F29) | RESOLVED (F21 implements) | 2026-07-28 |
| [D88](D88-hkdf-hmac-zeroization.md) | Un-zeroized HKDF/HMAC key state (C21 R1) — **option (d), not in C22's list: enable `sha2`'s non-default `zeroize` feature.** C22's premise "no feature fixes it" is **false** — `digest`'s `buffer_fixed!` `ZeroizeOnDrop` arm is a *marker with no `Drop`*, so the wipe is drop glue via `sha2`, not a trait bound on `Hmac`. Measured: the full 144-B `Hkdf<Sha256>` wiped, including the block buffer holding `W` **verbatim**; option (b) would not compile as specified and (c) is strictly weaker alone. Also closes an unrecorded gap — GGM covering seeds recoverable from dropped hashers. No format/vector/API impact | RESOLVED (C22 implements) | 2026-07-28 |
| [D89](D89-wallet-primitives-below-the-gate.md) | Wallet primitives below the `ant-backend` gate — the light half (secp256k1 keygen, D44 import validation, EIP-55 address derivation) moves **into the default graph** over `k256 =0.13.4` without `ecdsa` + `sha3 =0.11.0`: **120 → 129** packages, zero added to the heavy graphs, zero new lock entries. Feature-gating `init` instead (option a) fails on **coverage** — required CI is default-features-only, so four of U11's six Accept rows would be compiled by nothing, and cfg-inactive code is not type-checked. The measurement that decided it: `alloy-signer-local` costs **+71**, k256-arithmetic **+9**. D44 unweakened by construction (same `k256::SecretKey::from_slice`, same locked version). Also found: `balances()`/`preflight()` were inherent methods on the concrete backend, so U14 was un-mockable even with the feature on | RESOLVED (U37 implements) | 2026-08-02 |
| [D90](D90-anchor-http-substrate.md) | Anchor network substrate — HTTP-client pin, execution model, dependency-graph placement. **`ureq = "=3.3.0"`** (rustls, no default features), **blocking and UNGATED**, declared by `antseal-anchor` alone: default graph 129 → **144** names, `ant-backend` 447 → 451. No runtime is created, borrowed or required — the frozen `AnchorGate::run` `async fn` keeps its signature with a blocking body (measured: 1 poll under the project's own noop-waker executor), and endpoint independence comes from `std::thread::scope`. Both halves of A3's implied lean overturned: `reqwest` is in the *lock*, not the default graph (+64…+72 vs ureq's +15, and its `blocking` feature costs *more* than its async client), and no P task should own a pin nothing consensus-affecting flows through. Gating behind `ant-backend` refused on D89's measured ground: required CI passes no `--features`, so every Accept row of A3/A10/A13/A16/A17 would have been compiled by nothing. Three undocumented `ureq` behaviours found: `.limit(N)` accepts **N−1** bytes, `max_redirects(0)` **returns** the 3xx, and `Config::default()` reads `HTTP_PROXY` — **which would collapse A16's must-agree pair onto one origin** | RESOLVED (minted mid-round) | 2026-08-02 |
| [D91](D91-anchor-error-code-namespace.md) | The A-domain error-code namespace — **one prefix, `anchor-`**; the `ots-`/`tsa-` split rejected; the digest check is `anchor-ots-digest-mismatch` and `ots-ops-do-not-commit-anchor-digest` is never minted. Raised by the A2 lane, which found two decisions resolved the **same day** specifying different prefixes and two different codes for one equality — permanent, because codes are append-only. D58's own prediction was tested and failed the same day. The question was never two-way: seven already-ruled codes belong to *neither* namespace, so the split's honest count is **four** prefixes. The finding underneath: §2's headline rule — *"a domain never mints a code under another domain's prefix"* — **is enforced by nothing**, since a foreign-prefix code is distinct, well-shaped and new, so every distinctness layer passes it while `census` absorbs it into the `(unprefixed)` bucket. That is how sixteen codes came to be specified under an unregistered namespace with a fully green suite → **Q77** | RESOLVED (minted mid-wave) | 2026-08-02 |
| [D92](D92-proven-ots-anchor-identity.md) | What a `proven` OTS anchor contributes to the independence count — **the chain, not the calendars and not the block**. One opaque `AnchorIdentity::BitcoinChain` for every headline-eligible OTS anchor; `OtsCalendars` deleted. A40's recorded risk confirmed and sharpened twice over: the calendar set is a **sealer-chosen bundle-recorded string** (the input A40's own `TsaSigner` rustdoc forbids) and is **invariant across `pending → attested → proven`** because the merge *enforces* attestation retention; and the over-count needs **no adversary**, since the measured 2026-08-03 cycle put one digest's three calendars in **three blocks** (960767/960768/960771) — scored 3 by both the calendar key and the block key, 1 by MVP-SPEC.md line 19. The block key fails again on `merged.added.first()`: the recorded height is merge order, not evidence. Two heights of one chain are one identity and two data points. "No identity for OTS" rejected for breaking `count == 0 ⟺ UNANCHORED`. **A20's gate must never read the mixed scalar** (A40's `Do` said it should) → kind-scoped accessor added, A40's `Do` amended. Found: the eligibility filter has no OTS-side witness; the one existing OTS identity test cannot distinguish three of four candidates; two disagreeing TSA identity notions, the seal-side one over-counting → A72. Errs toward under-counting; §8's irreversibility restated as being about the **published claim**, not the code. Zero wire bytes, zero vectors, zero codes | RESOLVED | 2026-08-06 |
| [D93](D93-online-refutation-precedence.md) | Reconciling the M2 anchor tamper matrix with the shipped state machine — **the registry cell is right, the code is wrong, and D56 §5 is wrong in the place the code copied faithfully.** `MATRIX.json` row `anchor-forged-header` keeps `verdict:invalid`; **no `expected` cell moves** and the 6-family/8-case counts hold. D56 §5 placed **O4 above O6/O7**, which makes both online refutations unreachable whenever the ops commit the embedded header — so a forged header (trivial to build: the attacker picks the ops, reads the root, writes 80 bytes around it) renders `attested` against any online evidence, and an `.ots` claiming a height beyond the chain tip renders **`attested` for ever**, which is the defect **D56 §3 states verbatim as the reason O7 exists**. Five statements contradict the shipped order, four of them inside D56 (§3, §5's own summary table, §1's ruling that spec 108/168 win, A18's Accept row 2); the single argument for it — best-evidence-wins — is a **per-branch** rule and the D79 upgrade group is singular, while O8 already hands a relay the same downgrade primitive offline. Fix is one guard: **O4 fires only when no agreed online evidence refutes the recorded height**; O5 stays above O6/O7, so §4's anti-downgrade ruling and the A21 single-branch trap are untouched. Row 4's fixture is now **load-bearing in two directions** — single-branch *and* ops-committing — because the uncommitted forgery that satisfies the row today is already `invalid` offline and never runs the online gate; the row carries a vacuity latch so that mistake goes red naming itself. D56 §9's `nTime` row formally retired-and-replaced. Q75 confirmed in substance and corrected in two of its three instructions (the `pending` block **stays**; `EXPECTED_M2_UNMINTED` empties in the same commit). Zero frozen report bytes, zero codes minted | RESOLVED | 2026-08-06 |
| [D94](D94-anchor-verdict-vector-re-emit.md) | Frozen report vector's anchor state changes when R12 replaces the M0 `absent` stub — **RE-EMIT, and it is not report v2**. Report v1 already carries all seven states (`verify/report.rs:282-290`, `REPORT_VERSION = 1` unchanged), no field is added/reordered/renamed and no bundle byte moves, so this is a **VERDICT EVENT** — the third class the tree has no word for, between "the format changed" and "the targets diverged", which both failing assertion messages enumerate and neither covers. Full freeze ceremony, zero version bump. `"invalid"` is correct (D84 F3) and the fixture's synthetic bytes **stay**: R12's own Accept sources the seven-state fixtures from A, and swapping in A25 material would move frozen `bundle_bytes` in `bundle/bundle.json`, both bundle digests in all 21 report cases and the fuzz seed corpus — a FIXTURE EVENT for zero gain, when the meaningful home is the `anchor` kind already reserved for A22 as a legal-forever *append*. R30's in-tree re-pin is the same class with a lesser ceremony and one obligation the vector side lacks: **exactly one of 26 rows may move, and a second is a D84 F2 breach to fix rather than re-pin** — the only test of F2's blast radius the tree has. **D84 §7's report-**v2** row is stale** and never was true — R32 froze `REPORT_VERSION = 1` the same day D84 was written, so the row asserted a gap the file it was freezing did not have; amended byte-identically across three lint-checked files. **The finding is the third instrument the brief did not count**: `m0_anchor_artifacts_are_inert_until_r12_wires_the_anchor_stage` — named by D84 §2, `docs/security-assumptions.md`, the Q14 gate plan's row N1 (*"the row's own load-bearing evidence … as an equality"*) and `GraftAnchors` as the test **written to go red at R12** — observes only `verify_bundle`'s accept/reject bit, which D84's own rule F2 guarantees never moves. It stays green through R12 and through any correct implementation of it; it must **not** be inverted (F2 forbids what the inversion would assert, and with synthetic bytes every flip yields `invalid` either way) but re-titled as the permanent F2 guard it has always been, with P2's successor being A21 rows 1–2 against A25's real material. One claim, five copies, no lint — D84's own correction-to-the-correction recurring. Also found: the brief's *"the anchor-state change is the whole of the diff"* is **unestablished** (the assertion reports the *first* difference only, and `fetch_date` is a live second candidate the fixture supplies), and a vector's `pins` prose is compared only against the const that generated it, so `report_vectors.rs:137`'s *"each an absent M0 slot"* would stay green and false in a frozen file. Discovered R67–R70, Q107–Q110 | RESOLVED (R12 executes) | 2026-08-06 |
| [D95](D95-sealer-recorded-anchor-metadata-in-the-report.md) | May a refuted anchor render sealer-recorded metadata? — **RENDER, unconditionally, no state gate**, as decimal POSIX seconds (`u64::to_string()`), format-permanent for report v1. D94 §6's STOP fired, and fired on `fetch_date` alone: `source` is `null` six times out of six in case 19, and D53 §4 had already ruled it (*claimed* identity on `invalid`, rendered as claimed) — D95 does not reopen it. The field is sealer-written and bound by nothing **in every state** (unsigned bundle; `anchor_digest` covers only the manifest; D59 §6(a) normatively forbids the one available cross-check off a measured 129 s skew), so a state gate would encode a verification distinction that does not exist — and the only benefit anyone can name for the `proven` case is *itself* the inference D59 forbids, which inverts the harm. The precedent is frozen in case 19's own bytes: `"claimed_time_informational_only":"1767225600"` is the same construction over the numerically same value, rendered unconditionally in all 21 cases, and `"fetch_date":null` on a TSA slot would assert what registry §7.9 key 3 (`req`) makes impossible. Recorded so nobody re-runs them: **D29 permits suppression** (a state-conditioned `null` is legal and would have cost one line) — it loses on meaning, not mechanism; **no spec line mandates the render** (`fetch date` is at MVP-SPEC 108/114 only, both bundle content, and the doc citing "line 127+" is mispointed); and **registry §6.1 is not the suppression authority D94 read into it** — its rule is *do not consume, MAY display as a claim*, said about `anchor_status`, the sealer's own competing verdict. The label is owed and currently **unreachable**: `model.rs` drops the `AnchorSource::Verified`/`Claimed` discriminant, `is_verified` has three repo-wide hits all definition-or-test, and R22 hands the page only report bytes — dischargeable at M3 via R18's embedded strings, no version bump. Two further instruments go red **after** the emit in a file D94 never opened, both with wrong-cause messages, one carrying a **sixth** copy of D84 §7's stale report-v2 claim in a fourth file. Discovered R72–R75 | RESOLVED (R12 executes) | 2026-08-06 |
| [D96](D96-pending-case-fixtures-after-a21.md) | What happens to Q8's nine tests-of-the-test when A21 empties the tamper registry of pending cases? — **synthesize the pending case; stop borrowing a live one.** Two planners were briefed on opposite charges and **the one charged with keeping the real registrations rejected its own charge**: re-anchoring is not merely inconvenient but **structurally impossible**, because a case may exist only for a clause MVP-SPEC.md line 168 actually names, M0's set must stay empty since *that is Q14's gate condition*, and every M2 clause is A21's — so there is nothing left to re-point to and nothing that could create a replacement. A81/Q92's O7 row does not supply one (it is a `project_added[]` entry, whose schema has no `pending` key and which fails unless the row already exists). Holding one of A21's eight rows back to feed a fixture is rejected on three grounds, the decisive one being that it **inverts the dependency**: a fixture requiring a non-empty pending set is a fixture requiring the project never to finish. Measured rather than argued, by extracting the checker into a standalone `serde_json`-only harness that reproduces all 17 committed fixtures passing: post-A21, **8 of 17 break**, and **the two most dangerous are not the three panics** but the two that stay *red on the wrong rule* — where the cheapest repair is a needle edit that leaves the collision rule and the pending-arity rule permanently untested while the suite reads green. The remedy is the one D81 already forced one state down, recorded verbatim in this same file (*"a test that depended on one would have to be deleted or kept alive by leaving a collision in place"*). A **case** is appended, not a family or a root: `check_text` pins family counts but never a case count, so the section's contract — *"takes the COMMITTED registry, breaks exactly one thing"* — stays literally true, and converting a real implemented case is refused because it orphans that case's live row. **Riders**: `expect_only_red` pins each fixture to exactly one failure, closing the one thing borrowing genuinely bought (a stale synthetic block can no longer be carried by a second failure); and `assert_m2_anchor_set_is_armed` **follows the cases into their live state** rather than collapsing to `assert_eq!(8, 8)` — it computed its arming check, its print loop and its unminted set all from the set A21 was about to empty, which is the *"complete but comparing nothing"* failure its own doc was written to prevent, one level up. Discovered Q115 | RESOLVED (A21/Q18 execute) | 2026-08-06 |
| [D97](D97-ots-upgrade-group-vault-home.md) | Where the OTS upgrade group lives in the vault — **one record, not two: keys 4/5/6 on the anchor-artifact record in the existing `ots-pending` slot, with `SEAL_JOURNAL_VERSION` 1 → 2 in the same commit.** The register's lean (a second `ots-upgrade` slot) is overturned on four measured grounds, and the decisive one is not the crash window: the **ordinary resume path** rewrites `ots-pending` from a fresh submission and leaves a stale group beside it, the engine then reads `upgrade = anchor.upgrade.clone()`, `if upgrade.is_none()` is false, `confirm_header` is **skipped**, and the vault holds a genuinely upgraded `.ots` paired with a previous submission's header — verdict **`invalid`/`anchor-ots-header-uncommitted` on an honest work**. (a)'s advertised "no schema change" is false: it relocates the change onto `ArtifactKind::from_wire`, closed at 0/1, whose tag-2 refusal is `Corrupt` → **`VaultAuthFailure`** ("wrong passphrase, or the vault … has been modified or corrupted", exit 12) — the same false-tamper diagnostic (b) was rejected for. (c) was measured rather than feared: **two literal edits, zero regenerated artifacts** — no committed fixture, golden vector or snapshot pins a journal version byte, no test asserts `== 1`, no migration machinery exists to write, and **no vault exists to migrate**; the "whole-vault-format event" framing is wrong twice, since `SEAL_JOURNAL_VERSION` is one of **six** independent constants and is not `VAULT_FORMAT_VERSION`. (d) dies on a fact: `OtsCaptureRecord` has **no artifact field**, so adopting it is (a) again — and it has zero production callers. **Rider 1 answered stronger than asked**: the group is not merely convenient but mandatory, because an upgraded artifact with no group renders `internally-consistent-only` (O9), is **un-nagged** (`NagState::AttestedOnly`, whose own hint says `--online` can help) and is **structurally unrescuable** — `agreed` is derived from `upgrade` (`verdicts.rs:735`), so no online evidence can ever reach it; `OtsAnchor::new`'s `Option` means reveal does not force it, which is why the bundle does not settle the question. **Rider 2**: the upgrade's `fetch_date` must be pinned from the parameter, which partially discharges U48 and **sharpens** the rest — one record, two fetch dates, one pinnable and one not — and narrows U48 to threading the pinned date (`ctx.anchors.fetch_date` already exists; only `resume.rs:382` reads a clock). **Rider 3**: one write site in `pipeline/anchors.rs`, a read-modify-write, which makes **U47 a hard predecessor of U23**. Also corrected: the D43 cache exclusion does **not** cover the anchor area (the `complete` guard is scoped to journal entry keys; `tests/vault_export.rs` writes anchors only on `Complete` works and asserts they survive), and `put_anchor`'s *"nothing downstream has read them yet"* premise for the resume overwrite is **falsified by U24's hook**, which advances anchor slots on every invocation regardless of seal state — so a resume discards a completed attestation whose replacement submission carries a new, un-re-pollable commitment (U57). Discovered U53–U57, Q116 | RESOLVED (U23/U24/U47 execute) | 2026-08-06 |
| [D98](D98-status-anchor-state-vocabulary.md) | What state vocabulary does `antseal status` render, and how are the vault's records projected into it? — **A18's seven names, computed per artifact through the public evaluators; A15 keeps the nag; nothing is fabricated.** The register's lean assumed `AnchorStatus` had to be invented — **false**: `evaluate_ots_artifact`/`evaluate_tsa_artifact` are `pub` and both `*ArtifactView::from_parts` constructors take no status, naming A18 as their intended consumer; measured by evaluating all five real captures with no status anywhere. `vec![]` intermediates is **not** a weaker input — `validate_token_chain` pools the token's own bag, all four pinned-root TSAs reach `proven` with `&[]` in already-committed tests, and no CLI path has *ever* populated the field (`TsaAnchor::new` has zero call sites outside core), so it is the same input `verify` will get; SwissSign's `internally-consistent-only` is root quarantine, identical either way. The journal contradiction resolves **for** S29 — `export.rs:784` skips only `>= UNIT_ENTRY_BASE`, so entries 0–2 survive import and the `anchor_digest` recovery path works; **three** stale claims say otherwise, one of them inside the file that implements the opposite. `verify_at_unix` copies **`VerifyOptions`**, never `AnchorStageConfig.fetch_date`, whose own doc says it is safe to expose *because it gates no outcome* — `verify_at` gates the headline, is one-sided, and `verify_at = 0` renders an expired DigiCert chain `proven`, so it gets no user surface at all. Option (b) dies because A15 has **no TSA verdict**; option (c) — a projection table — is the one design that guarantees `status` and `verify` diverge. `status` emits six of seven states (OTS is never `proven`; `absent` is never an outcome, R70) and adds no core API. **A fifth gap nobody named blocks U23**: `AnchorArtifact`'s key-0–3 schema cannot store the D79 upgrade group, so `attested` is unreachable under **both** machines and U23's Accept is unmeetable — reached independently from the render side here and the write side in **D97**, which rules it (keys 4/5/6, journal v2) and whose §1.2 adds that `--online` can then neither promote *nor refute*, and that `list` **stops nagging** on exactly the work whose evidence was destroyed. Discovered U58, A101 | RESOLVED (U23/U25 execute) | 2026-08-06 |
| [D99](D99-upgrade-hook-placement-and-test-seam.md) | Where U24's opportunistic OTS upgrade hook lives, and how it becomes executable — **at the end of `main_entry`, after the exit code is a value and all output is flushed, over a handle each vault-holding handler moves up into a dispatcher-owned slot.** The register's lean (the shared dispatch path) is right about the location and wrong about the mechanism: measured, **neither point that sees all ten subcommands holds a vault** (`run::run` takes `&Cli`; every handler unlocks for itself; `UnlockedVault` is deliberately `!Clone`), so **U24's Accept row 4 is factually unsatisfiable as written** and is rewritten into a per-subcommand assertion over the real binary's debug trace, which proves D42's rule in *both* directions — more than the original asked. Unlock-time placement is rejected on four grounds, the fatal one being that it would fire in ~40 test call sites. The placement is **forced three times**: it is the only point post-output in both plain and `--json` mode, the only point at which the host command's `VaultLock` is released (and the lock is a **try-lock** that refuses a second same-process acquire), and the only point at which the exit code is already a value. **Highest-risk finding, measured not read**: `cargo test -p antseal-cli` runs with Q16's gate **disarmed** (`deny_reason() == None`), and the CLI's existing defence is a *source* scanner that cannot see URIs living inside a stored `.ots` — the Q16 §2 violation exactly, one crate over. The obvious compile-time fix is **measured dead**: `cargo test -p antseal-anchor -p antseal-cli --test no_real_network` links the `test-util` rlib, so any dev-edge feature arm would permanently redden the workspace's only proof that the environment arm works. The arm therefore stays the environment variable and the *harness* is made to guarantee it — one spawn helper replacing 12 bare `CARGO_BIN_EXE_antseal` sites, a structural ban on in-process `main_entry` (a test **cannot** set the variable: `set_var` is `unsafe` in edition 2024 and the workspace denies `unsafe_code`), and a new `check-anchor-net.py` R4 with planted faults. `list` keeps its lock-free read untouched — **the hook, not `list`, takes the lock**, only when it has something to persist, declining silently when contended, with a compare-and-set against D97's stale-group hazard. The test seam is opened by promoting `upgrade_pending_with` + `UpgradeTarget::loopback_for_tests` from `#[cfg(test)]` to `feature = "test-util"`, **measured** not to enter any shipped binary (`cargo build` links `["default"]`, `cargo test` links `["default","test-util"]`, and the two `target/debug/antseal` binaries differ). Also corrected: **D42's own consequence paragraph is wrong about `init` and `vault import`** (neither holds a handle at the dispatch layer); **D97 R3's `apply_upgrade` must take the store, not the journal**, because `VaultJournal::new` is scan-restricted to `seal_session.rs`; and the hook's budget **must rotate** or the first work by seal-id order starves every other for the life of the vault. Discovered U57, U58, Q117, Q118 | RESOLVED (U23/U24/U47 execute) | 2026-08-06 |
| [D100](D100-damaged-anchor-record-surface-behaviour.md) | What `list` and `status` do with an anchor record they cannot decode — **the refusal stops being an error and becomes a datum; no `ErrorClass` is minted and `list` exits 0.** The register's lean (d) is overturned in half and inverted in the other half. The error-class half dies on a measurement recon did not take: `status` **already renders the identical damage one layer down at exit 0** — `.ots` bytes that do not parse render `invalid` (D98's own cross-walk, `verdicts.rs:694-707`), and the `status` handler has **no verdict → exit-code branch at all** (`commands.rs:390-402`) — so a new class would put exit 0 and exit N on two sides of a boundary the user cannot see and that carries no severity difference. And recon's (c), which it recommended **refusing**, is the load-bearing half: its grounds are **measurably false for `status`** (D99 R6 assigns it the loud per-work report and it answers exit 12 *"wrong passphrase"* on a work the user named by id), **vacuous for the hook** (D99 R6 already lists the decode refusal as a per-work skip), and **unmeasurable for `reveal`**, which does not exist and which gets whole-failure back as one explicit call. **The decisive finding is not in `list`**: `list` and `status` already **contradict each other in production** on the same vault state — a work with anchors and no journaled manifest renders *"anchors: unclassified"* at exit 0 (`listing.rs:616-619`, tested at `list_command.rs:839`) and exits 12 accusing the passphrase (`status.rs:856-863`), each documenting itself as obviously correct and neither citing the other — so the "fail whole" rule has **two contradictory authorities, not one**. Recon's *"five `corrupt(…)` producers"* is measured wrong: **twelve** call sites, **fourteen** distinct strings, plus three in `journal.rs`. The split is placed **exactly at the AEAD boundary** — a genuine auth failure is `CipherError::AuthFailure` → `VaultAuthFailure` **directly** and never through `JournalError`, so no unauthenticated caller can observe the distinction and U6's *"insofar as safe"* is preserved; codes **18** (*"NEVER the generic auth failure"*) and **19** (an unimplemented registry tag) are the committed precedents. An eighth `AnchorState` is impossible (**frozen at seven**, D98) and a verdict is unstatable anyway because the artifact's **kind lives inside the record that will not open**, so the damage gets a field, not a verdict and not a code. Damage carries **three reasons** — `Undecodable` / `NewerRecord` / `SlotMoved` — because *"upgrade antseal"*, *"your vault is damaged"* and *"another antseal is running"* are three different sentences and only one is anyone's fault. **A102's Accept row is overturned**: its literal *"nags"* would print `ANCHORS PENDING: 0` and an unsatisfiable `--upgrade`, which is why the committed counter-argument at `engine/tests.rs:552-570` is **right about the boolean and silent about the name** — the fifth `NagState::Unreadable` keeps `nags() == false`, so three of that test's four assertions survive untouched and the surviving one is the load-bearing one. **A103's ordering claim is refuted**: the compile error that forces a name already exists at `listing.rs:293-301` by design; what A103 does *not* close is `each_nag_class_has_its_own_name`'s hand-written five-element array, and `work_status`'s classifier is an **if/else chain**, so a fifth variant can be named and **never constructed**, silently — hence `NagState::ALL` and a reachability test with a planted fault. **D97 K1 is weakened and survives**: D97 itself names K2 as decisive, K1's relocation-of-the-schema-change leg is untouched, and a rider records it rather than leaving it to be re-derived. Also corrected: **U19 execution note 6's `list_works` analogy compares an enumeration failure with a per-record one**; **the vault exit-code band 12–19 is full**, though U20 note 2 already ruled adjacency irrelevant. `tests/upgrade_hook.rs`'s `Shape::renderable` workaround **must die, and its death is the acceptance criterion**; `tests/machine_mode.rs:715` and `listing.rs:1085-1107` are named as **too narrow to redden**. Discovered five items (orchestrator allocates) | RESOLVED (U65/A102/A103 execute) | 2026-08-07 |
| [D101](D101-anchor-golden-vector-home.md) | Q109 — which home A22's anchor golden vectors take, and what their `expect` may contain — **HOME B, the reserved `anchor` kind under `testdata/vectors/v1/`**, and the home half of the lean is not overturnable: five committed sources against one, and one of the five is the frozen manifest that reserves the kind by name. The second half — *"verdict-only `expect` plus a binding test"* — **is** overturned, on three measured grounds. Verdict-only is **too narrow**: `evaluate_*` returns `AnchorOutcome`, which carries `suppressed` (A39) and `identity` (A40) beside the verdict, and D92 recorded in writing that A40's OTS identity half *"has never been pinned by a falsifiable row"* — an `anchor` vector is exactly that row. It is simultaneously **too wide**, in a place the brief did not look: `AnchorIdentity::TsaSigner { subject_dn_der }` is what **A72** is open to reconcile against A31's `(issuer_der, serial)`, so freezing the payload forever this wave pre-empts a live register row. And the standing binding test is **replaced**, because an equality assertion between a **frozen** file and an **unfrozen** one can only ever go red for a reason the frozen side is forbidden to fix; it becomes a committed generator plus a self-binding `artifact_sha256` inside the frozen document. On the crux `root_store_version` is **OUT**, and not as a loss: **A26 Accept row 2 is structurally undischargeable by golden vectors at all** — it needs two stores and a vector executor reaches exactly one — so it is already discharged today by `chain::tests::appending_a_root_leaves_every_existing_verdict_unchanged` over five real tokens with a non-vacuity guard, while `store_version_appears_in_verdict_data` pins the field in code, where a bump may legally move it. Structural finding the brief circles but never names: **`anchor` is the second vector kind whose pinned bytes are a function of the verifier rather than of the format** — D94's VERDICT EVENT class — and `verdict_event_ok`'s hard scope to `/report/` is an accident of `report` having been the only such kind on 2026-08-06, not a principle. The hatch is **not** extended now; the one event that will need it is named and handed off | RESOLVED | 2026-08-07 |
| [D102](D102-parser-structural-allocation-cost.md) | A100 — which allocation rule governs the `.ots` parser — **neither of the two it names.** The peak is `walk.rest`, the iterative work stack (`parse.rs:437`): `Frame` is **40 B** (measured), `Vec` doubles 4→…→128, and the 65th push allocates the **5 120 B** CI saw four times. It is **count-bounded** by `MAX_OTS_DEPTH`, and D10 §4's clamp — imported here by D58 §10.3 rule 4 — is a rule about **length headers**. The third class was never written down. **The finding recon flagged for verification is confirmed and worse than stated**: `MAX_OTS_DEPTH = 1_024` means `rest` reaches **40 960 B from a 1 145 B legal artifact** — **35.77× the input, 7.82× the guard** — and `tests.rs:1186` **already builds that input and asserts it parses, green today, silently**. Decisive new measurement: the **real mainnet `LARGE_TEST.ots` allocates the identical 5 120 B** as all four failures and passes only because 1 768 > 1 024, so the guard's verdict on identical allocator behaviour is settled by input length — no longer an inference from four points. Second site confirmed: `attestations` at `256 × 48 = 12 288 B`, reachable from 1 869 B. **Recon's lean (derived bound, task-level, no D-number) is overturned**: a derived bound *moves with the limit*, so the objection's 65 536 raise stays green under it. Clause **(c)** — structural cost `≤ MAX_OTS_BYTES`, **zero new constants**, as a `const` assert so a bad raise fails `cargo build` — is what goes **RED at 2.51×** on exactly that scenario, and it is an **F4 precondition**, which makes this a decision. **Option (B) refuted** (executor allocates 32 B; `MAX_OTS_VALUE_BYTES` as a budget would hide the whole D58 §3.1/§3.2 class it was built to catch). **Option (E) as briefed is measured dead**: a frame costs **one** op byte not two, so `frames ≤ L` not `L/2` — an L/2 rule rejects the tree's own green test — and `frames ≤ L` gives `bytes ≤ 40 L`; the tail-call elimination that fixes deep chains is refuted by an **open-fork spine** (40 960 B from 14 399 B). fuzzing.md §4 step 3 is **not amended**: the work stack is not a member of the class `MAX_CLAMPED_ELEMENT_BYTES` is about, the fix leaves the guard strictly stronger in three places, and the target's D58-crasher resolution is preserved (residual 7 KB window stated, not buried). `MAX_OTS_DEPTH` **not lowered** — but F4's monotonicity is scoped to *after first release*, so the window is **open now and closing**, recorded because it expires; D58 §9.2's *"depth costs a `Vec` entry"* is corrected — nobody ever costed the entry. `anchor_token.rs:71` is the **same class, silent only because its count limit is 8 and not 1 024**; it takes the derivation now. Regression test confirmed: own test binary (a global allocator is process-wide), **equality** per D26 §2 on the pinned toolchain, **inequality** in the fuzz lane so it cannot flake. **A100's reproducer is corrupt** — 328 base64 chars → **245 B, sha1 `08df86ba…`**, not 248 B / `b5aec24c…`; four characters lost; it still trips the budget but at a **different terminal error**, so the row is unverifiable rather than dead. Replaced by a **generated 142-byte** minimal witness, under a standing rule: reproducer bytes never live in prose | RESOLVED (A11/A23/A5 execute) | 2026-08-07 |
| [D103](D103-upgraded-ots-vector-provenance.md) | A22's upgraded-`.ots` cases — **the lean dies on its premise, not its merits.** D101 §9 rows 4/5 name an artifact that does not exist (`A-*.upgrade` are calendar response bodies, no magic, no header; the quoted sizes are HTTP `bytes=` fields), but the repair needs no network and no `antseal-anchor` call, because `merge_upgrade`'s whole byte effect is `file[..off] ‖ 0xff ‖ body ‖ file[off..]` — no hash, no judgement. Reproduced in Python this round, with the walker **validated first** against the tree's own committed shapes (merged-A 34/12/3, LARGE_TEST 100/67/4, exact). The three-way splice is **3 808 B, 244 ops, depth 85, 6 attestations, sha256 `c2bf8b2c…`**, and the measurement nobody had made: **all three Bitcoin branches derive merkle roots byte-matching `header[36..68]` of the three committed mainnet headers** (960767/960768/960771), each re-verified offline by `double-SHA256 == claimed hash`. So `check_embedded_header` is `Committed`, and **O4 precedes O5** — the surviving pending branches do *not* demote it, the inverse of the trap `tasks/A.md:265` records for the *forged* row. **K5 never fires**, and its reading is ruled anyway: it is about a committed re-runnable procedure, not a language — the narrow reading is **already false of the tree**, since `report`/`bundle`/`manifest`/`content-model`/`sig-reject` are frozen with **no generator at all**. For `anchor` the generator is a **derivation record, not a cross-check** (D101 §7.3 is right that no RFC 3161 reference exists); the real cross-check is one **A14** test running the shipped `merge_upgrade` over already-wired fixtures to the same sha256 — A13's precedent, and *not* D101 6b's refused archive-equality lane. **D101 §9.2's fallback was CONDITIONAL and expired** when the headers landed in `6f69e1a`; it offered 1–4, never 1–3. **Two defects found in D101's own set**: §3.2 rules `suppressed` IN on a witness the set does not contain (`ots_refutation` returns `None` for all five cases — five empty lists), and §3.5 assertion 6 is **vacuous** (`absent` is kind-level on `absent_verdict`, unreachable per-artifact — D94's blind instrument inside a document whose §4.3 cites it). Case set is **seven**: +case 6, agreed `no-such-block` over the **same artifact bytes**, which delivers §3.2's witness *and* the first committed witness for D56 §4's anti-downgrade rule, at zero artifact cost; +case 7, `merged-B` under digest A → `invalid`, promoting D56 §9's sharpest defect from *same-verdict* to *byte-identical*. Frozen artifact is the **full** splice, order pinned, on four grounds — the terminal protocol state, three headers from one artifact, non-vacuous permutation-invariance, and case 6's expressibility. **A107 RESOLVED here, not by §7.4** — which carries no ruling marker (6c → 7) and whose "~129 s" is **wrong about both frozen tokens: measured 128 s each**; recorded instants, per artifact, per log line; the synthetic `FETCH_DATE` refused as unreachable *and* as the one unrecorded value in a document of records; cases 4–6 take the **header** campaign's instant because D79 makes the group all-or-nothing. Accept becomes **four explicitly numbered rows** so the D53/Q109 miscount cannot recur by counting. Feature tier pinned at `anchor/mod.rs:77`: the executor may name no `anchor::testing` item, and the failure is a **wasm-bitmatch build break**, not a red test | RESOLVED (A22 executes; A14 owns §3.3; A107 closed) | 2026-08-09 |
| [D104](D104-max-ots-depth-lowering-window.md) | A109 — does the `MAX_OTS_DEPTH` lowering window exist, and should it be used — **it exists, and the lowering is empty rather than declined.** Both sides of the brief are overturned. Recon's *"§5a governs, the window never existed"* fails on **provenance**: the Update rule at `:331-338` is **A27's, commit `1620543`, 2026-07-28**, not the D102 lane's — D102's §5a heading was inserted *above* it and now files two A27 paragraphs under a banner reading "Added 2026-08-07 by D102", a corruption that **already produced one measured error, recon's own finding 1**. And it is a **procedure, not a permission**: it prices a lowering and never permits or forbids one. F4 sentence 1 governs *when* — sentence 2 is the harm model and names a release, and sentence 3's *"format-version event"* would contradict **D84 §3** (*"not format surface"*) if applied before any release exists. A third statement neither the brief nor D102 cites — the **drift-checked** freeze-boundary copy at `:127-128`, *"raised, never lowered"* with no condition — is a parenthetical summary and loses. **But D102 is wrong the other way**: *"available today at no cost"* is refuted by the same unqualified sentence, since a lowering still spends Q27's ceremony and kills `lowered: never` forever. **On the merits the option is EMPTY.** Cost is `next_power_of_two(MAX_OTS_DEPTH) × size_of::<Frame>()`, so **every cap in (512, 1 024] reserves identical bytes on every target** and rejects more artifacts — a strict loss; the first value that saves anything is 512, and the tree's own margin discipline (`ots/tests.rs:280`, *"asserted rather than trusted"*) demands ≥ **680**. **Disjoint bands**, so **1 024 weakly dominates every admissible value** and is the cost-minimal admissible cap, not a generous guess. **Invariant to the depth dispute** — holds at 85, 73 and the discredited 67 (floor 536 > 512); would move only below 64, and every artifact in the tree is 12/67/70/73/83/85. **A109's four load-bearing numbers are all wrong**: 40 960 B is x86-64's, and the browser tab it invokes as the constrained venue costs **24 576 B** and is the **cheapest** place the parser runs (`limits.rs:155-157` already says so, total 32 768 B wasm32 vs 53 248 B native); "depth 67" is a third-party crate's 2017 test constant A48 exists to retire, while the project's own fragments are 67/70/73 and the **spliced artifact a verifier parses is 85** — depth is attachment depth **plus** fragment depth, a structural fact nobody had written down; the margin is 12.05×, not 15.28×; and the cost is a rejection threshold, not bytes. Three defects fall out, **none absorbed**: the margin column is the **only numeric column in §5 no test cross-checks** (→ **A112**) — exactly how A109 came to reason from it; **§6 has taken zero entries across seven document-changing commits** while §5's Update rule routes F4's auditability through it (→ **Q126**, and it conditions any future lowering); and the wasm32 figures are asserted by nothing because `scripts/wasm-tests.sh` is `--lib`, so **`D102:517-519` is false** and clause (a)'s *"a number typed by hand survives a raise"* is being violated one file over from where it is written — the same confusion that reddened `wasm32-core-tests` at `873a1cf` one commit ago. Re-measurement is **A48's** (artifact from **A63**), with A48's own *"every margin is ≥ 15×"* falsified by the depth row; the §5a capture and the wasm32 cells are **A106's**. **Zero constants move** | RESOLVED (A48/A63/A106 execute; A112/Q126 registered) | 2026-08-09 |
| [D105](D105-post-freeze-report-value-additions.md) | R69 — may a new *value* enter frozen report v1, and what must exercise it — **yes, and R20's arm is permitted; but the lean's remedy is REFUSED by two mechanisms in the tree.** D29 rule 1's subject is **struct** fields and reaches no enum variant, and rule 4's *"shape of the bytes depends only on the report version"* cannot mean the document's key set because **frozen v1 already breaks that reading** — an anchorless bundle renders `"anchors":[]` and five `AnchorResult` keys vanish (`pipeline.rs:3098`, pinned in 20 of 21 cases). Recon's crux — *nested per-blob data is not two scalars* — **dissolves on three grounds**, the sharpest being an axis recon never named: both enums were a **single unit variant rendering a bare string** and both become `string \| object`, so the transition is identical and `ArbitrumReceipt` is already whatever R20's arm would be. D29 **does** name R20's arm as forbidden — and the same bullet's other example, *"R12's anchor detail"*, was measured wrong at R12 by D94 §2, so the bullet's **rule** survives verbatim while its two 2026-07-27 guesses about undesigned tasks do not bind: D84 §7's prediction-inside-a-delimiting-block defect, recurring one document over. The test, applicable in one sentence: **a report change is a VALUE ADDITION iff no struct report v1 could already serialize gains, loses or reorders a field** — corroborated free, since a FORMAT EVENT moves all 21 cases and all 26 R30 rows (R32) and a value addition nothing emits moves zero. **The vector is not expensive, it is refused**: a 22nd case dies on `vector-freeze.sh`'s *first* verdict-event check (*"the case count moved (21 -> 22); a verdict event re-values existing cases and adds none"*), and a second `report` document dies on `check_shape_coverage`'s **per-document** demand for all 13 `REQUIRED_SHAPES` — neither the lean, nor D94 §11's R69 sketch, nor R69's own Accept row had read either. Recon's costing was also wrong permissively: a catalogue Case does **not** force a report-vector case (26 shapes, 21 cases). **The right instrument is the one D29 named in its own Consequences bullet 1** — the fixed-fixture snapshot `EXPECTED_CANONICAL_JSON`, hand-built (which is why it still renders `AnchorState::Absent`, a state the pipeline no longer emits at all) and **already enumerating all seven anchor states in one document**, with `supporting_evidence` the single slot where the tree's value-space exhibit stops short. A receipt-bearing twin, `+63 B` at one value, dual-target by A90's `--lib` route, **zero frozen files touched — so R69 is fully decoupled from A22** and `TODO.md:50`'s *"shares D101's ruling"* is false (D101 has zero "R69"). **The reframing finding: R12's heading *"Adding this arm moved no pinned byte"* is simultaneously the proof it was not a format event and the statement that nothing checks it** — zero moved pins is a correct format signal and an empty coverage signal, the tree has no second signal, and R69 is a coverage defect wearing a format defect's clothes. Also found: R12's *"Q14 froze report v1's field list"* drops rule 1's subject and, read literally, refutes itself — harmless where used, the wrong answer where R20 will read it; and D94's three-class vocabulary has **no class for adding a case to a frozen document**, which Q107 as scoped does not add. Discovered **Q127** (report enums are not enumerable: only `AnchorState` has the `ALL` + wildcard-free-match + sweep triple that makes a new variant a compile error) | RESOLVED (R69 executes) | 2026-08-09 |
| [D106](D106-work-enumeration-existence-predicate.md) | U66 — what a whole-store scan does with a work that is being created — **the defect is neither atomicity nor tolerance: `WorkStore` holds TWO definitions of an existing work and enumerates by the wrong one.** `create_work`, `store_meta` and `require_work` all define existence as *the `meta` record is present* (`store.rs:367-370`, `:389-391`, `:748-754`); `list_works` alone defines it as *a 32-hex directory name* (`:501-502`), and `create_work` is `create_dir_all` then `write_meta`. **Both halves of the lean are overturned.** Atomicity dies on four kills, the sharpest being **circularity**: `std::fs::rename` of a directory onto an existing directory is not portable (POSIX replaces an empty target; Windows `MOVEFILE_REPLACE_EXISTING` cannot name a directory at all) and **Windows is a required `cross-os` lane**, so a uniform `AlreadyExists` needs a pre-check on *directory existence* — the very predicate that causes the bug — while `import_vault` already publishes atomically **one level up** (`export.rs:1173-1185`), and an atomic `create_work` would fix the window U66's row does **not** name and leave the one it does. Per-caller tolerance dies on placement: it fixes **one of six** production scans, and on D100's `SlotMoved` model would render a row whose every `WorkRow` column comes from the record that does not exist. **Recon's "a window no user can reach" is right about the window and wrong about the reach: TWO commands are broken.** `antseal status <work-id>` resolves ids through `restore.rs:520-531` — the same `list_works`→`load_meta` scan, equally lock-free — so a user who types a valid work id is told *that work does not exist*. **Measured on the real binary**: five on-disk shapes in `store/works/` produce **three** exit codes, and the three that must be told apart (mid-`create_work`, `atomic_write` residue, and a genuinely meta-less work) collapse onto one sentence — *"no work with this id exists in the vault (see `antseal list`)"*, exit 2, `list` telling the user to run `list`, `--json` emitting **no `works` array at all**. A fourth shape nobody had recorded: a **regular file** named 32-hex passes `parse_hex32` and leaks an absolute in-vault path at `ErrorClass::Io`, so `list_works`' *"loud, never skipped silently"* has only ever held for names that fail the parse. **The silent-drop dilemma is dissolved by construction, not by policy**: `require_work` gates every journal, receipt and anchor write on the `meta` record, so a work directory with no meta and no non-dot content has **never held a byte** — it is the writer's intermediate, and skipping it is the same act as skipping the dot-prefixed `atomic_write` residue four lines above (`store.rs:497-500`). **Ruling**: `list_works` adopts the store's own predicate — enumerate iff the entry is a directory **and** (`meta` present **or** non-dot content present) — and `incomplete_works` stops asking `state()` (whose refusal must stay hard for the state machine) and reads the fine tag through `recorded_state` with U9's coarse mirror as the fallback `list` has had since `a_work_without_its_fine_state_record_still_lists`. Two production files; **no atomic publish, no new type, door, error variant, `ErrorClass`, exit code, JSON key, snapshot or source scan**, and `listing.rs` is not touched. **U66's row named the wrong window** — it describes `begin`'s meta→`STATE_ENTRY` gap while every observed red is `WorkNotFound` from the mkdir→meta gap — and its evidence, a load-based reproduction, did not reproduce; replaced by six deterministic instruments that construct the state on disk. **S34 and S38 close with it**: S34's *"Production-safe (read-only listing)"* is false and is the sentence that let this sit across three recordings, and **S38's root cause is false — nothing in the codebase removes a work directory**, so the race is a work *appearing*; implementing S38's Accept literally ("skip on `WorkNotFound`") would produce exactly the silent data loss D100 R3/R4 forbids. **The four shared-vault whole-store scanning tests STAY**, overturning S38's second Accept: after the fix they are the tree's only witness that a store-wide scan is total under real concurrent mutation, and isolating them trades a load-dependent red for a permanently green vacuum (D100 R10.2, A104). Also corrected: `store.rs:65-66`'s *"Readers are safe against a concurrent writer because every write is an atomic rename"* is a **per-file** guarantee offered as a **per-work** one, and is the root cause of all three recordings. Concurrency is ruled a **supported configuration** — derived from five shipped behaviours, since MVP-SPEC.md never mentions the lock and U5 calls it *"inferred hygiene"* — and a locked `list` is refused because `VaultLock::acquire` is a **try-lock** (Q117), so it would exit **15** for the whole duration of every seal rather than wait. Six items discovered, including that **D100 §9's five discovered items were never registered as rows at all** | RESOLVED (U66/S34/S38 execute) | 2026-08-09 |
| [D107](D107-f4-registry-limit-change-log.md) | Q126 — the F4 registry's §6 changelog has never recorded anything — **§6 stays and its obligation MOVES; both arms of Q126's `Do` are refused and Q126's own premise is false.** Measured: **zero of the eight document-changing commits owed a §6 entry** — all eight `lowered` cells read `never` and no diff in the file's history has ever written otherwise — so the *"100 % miss rate"* measures misses against an obligation of **zero**, and **Accept clause 2 is met on arrival**: *"has this ever been lowered?"* is answered by eight cells in one table at a glance, with seven of them pinned to their constants by `ots_limits_match_the_f4_registry`. §6 was never that question's answer; it carries the *why*. The decisive finding neither side reached: **§6's duty is attached to the branch F4 makes nearly impossible and forbidden on the branch F4 makes routine** — a lowering's window closes at first release (D104 §2.1) and its only proposed exercise is **empty** (D104 §3), while a raise is *"always available and never expires"* (D104 §10) with 16 384 of measured headroom and is routed **away** from §6 to the `initial value` cell. That is the whole explanation of the empty positive arm: the aim is wrong, and (b) deletes the section while (c) enforces the aim harder. **The lean dies on K2**: a `lowered` cell is written **only by a lowering**, so moving the note there is auditable for the case that will not happen and silent for the case that will — and on **K1**, since Q125's own answer to a guard with an empty positive arm was to **supply the arm** (*"pinned to `anchor/caps.rs` by name"*), not delete the guard. (c) is refused on **aim, not vacuity** — the brief is right that a planted change-set is a real arm — because an instrument whose only lifetime exerciser is its own fixture is **A104**'s lesson repeating. **And the branch that will happen is already broken against a committed test**: §5's *"a raise is recorded by appending the new value and its date to the `initial value` cell"*, executed literally, makes `ots_limits_match_the_f4_registry` red, because its needle `\| \`{name}\` \| A11 \| {value} \|` (`limits.rs:268`) requires the value cell to hold **one bare value and a closing pipe** — invisible because no raise has occurred. Ruling: the column becomes **`value`** carrying one bare current value (which keeps the existing needle exact through a raise), the history and reason move to §6, and §6 is **retitled `Limit-change log`** because a section called *Changelog* holding one *"document created"* entry tells a reader a **seven-times-changed** document is unchanged — the exact misreading **D104 §1.2 measured a recon lane making about this file**. One checker in `limits.rs`'s existing `mod tests`, bidirectional (the reverse arm is D104 §5's fabricated-provenance guard made mechanical), with **four planted fixture strings** as the positive arm — no script, no gate lane, no temp files, because the checker is a pure function of two `&str`s. Also corrected: the Update rule's *"`lowered` … is the only cell that may change"* is **falsified three ways** — by its own next clause, by D102's eighth column, and by D104 §4's order to A48 to rewrite two cells. D104 §5's clause conditioning the open lowering window is **discharged**; D104 §2.2 survives verbatim. **Zero limit values move.** Discovered: §5's **eighth row (A42's) is pinned by nothing** — the needle asserts `ALL.len() == 7` | RESOLVED (Q126 executes) | 2026-08-09 |
| [D108](D108-unanchored-in-frozen-registry-prose.md) | Q129 — a fourth sense of UNANCHORED in frozen normative text — **the frozen bytes are NOT edited; the correction lands at every editable site and the in-place edit is queued as an erratum.** The lean (*annotate or retire the word at `registry-v1.md:664`*) is refused because annotate, retire and correct are **one act**: a byte change to a SHA-256-pinned registry. **Recon's freeze analysis is wrong on the mechanism**: it reports *"there is no bless flag; re-pinning requires hand-editing `FROZEN.sha256`"*. There **is** an in-band re-bless — `#! status frozen` → `pre-freeze` → `--update` → back, inside one commit, which `format_freeze.rs:189-192` forces to be transient by reddening a committed `pre-freeze` — **and it has been used once, for exactly this class**: `622f5fe` corrected §14's overstated claim about the project's own assertions and re-blessed with the reason in the message. But that commit **set its own limit — *"done in one commit with the reason stated, before any tag exists"*** — and `format-v1-freeze` (`d3345e1`) now exists, so a byte change today is a **tag event**, a repository-identity act and not a lane's. Weighed against a gloss in a `len/shape` cell that no parser reads and that the registry's **own `:459-460` already defers past**, naming MVP-SPEC.md line 137 by name as the authority for this vocabulary, the trade fails. **Recon's strongest premise is inverted**: *"no test reads that prose"* is not a licence — every mechanical fact in registry-v1 is pinned **twice** (cell comparisons **and** the digest), so **prose is the only content the digest alone covers**, and *"no assertion reaches these bytes"* is the same sentence as *"the freeze is all that covers them"*. `FROZEN.sha256:12-23` says it in its own words: **consistency is not a pin**. *"Correct it to the spec's predicate"* is independently dead on **D78** — headline-eligibility is an [R]-tier verdict and §7.6 is tier **[P]**, *"decidable from the one entry being decoded"*. Ruling: `schema.rs:1449` (unfrozen, and the site an implementer of this codebase reads) **retires** the word and states the shape fact; **D98 rider 3c is extended from three senses to four** with a *layer* column and is **not renumbered**, so every existing citation resolves; the three editable senses each name the fourth; both frozen sites are recorded in a new **`docs/format/frozen-registry-errata.md`** — deliberately named **outside** the `registry-v` prefix, because `EntryPolicy` would otherwise **accept** it into the manifest — whose every entry is pinned to its frozen sentence by a test, so a later re-bless turns a stale erratum **red** instead of quiet. **The row understates the site count and the divergence**: `registry-v1.json:356` carries the same claim in **different words** (no bold, plus a clause the `.md` lacks), and **nothing compares the two prose surfaces in either direction** — a new row, scoped as a **closed-vocabulary** check because `622f5fe` §1 already ruled against pinning prose 1:1. **No new test was needed for the separation**: `verdicts/tests.rs:1862-1896` already builds a **non-empty** `ots_anchors` holding one `pending` OTS and asserts `is_unanchored()`, so rewriting sense (c) as sense (d) goes **red today** — in a test named for identity counting that cites none of this. Coverage without a claim; R6 makes it a claim by renaming the binding and citing the rider, rather than writing a duplicate. **Q129's Accept is amended** — both arms required frozen bytes. Also found: **`MVP-SPEC.md:123` is cited by the freeze machinery as byte-immutability and does not say that** (it is a *released*-conditioned compatibility rule, and D104 §1.5 measures that nothing has been released), so the freeze should cite **Q14's own act**. **Zero frozen bytes, zero wire bytes, no tag, no format-version event.** §8 puts the alternative — re-bless plus a tag event — to the maintainer with its exact procedure and price | RESOLVED (Q129 executes; §8 open) | 2026-08-09 |
| [D109](D109-task-id-traceability-scope.md) | Q85 — the real scope of the task-ID traceability gap — **two checks under two flags, both landing green; the mirror-`check_decisions()` lean is refused on a measurement.** **D1–D108 has zero holes; the nine task domains have 71**, so the sibling's `n > max(allocated)` bound is nearly free for a dense namespace and is a 71-number-wide false-positive surface for sparse ones — and all three of today's unresolved hits land in holes, which is the mechanism, not luck. The bound becomes a **floor** (`n >= 1` — `A0`/`C0`/`F0` are **hex bytes in UTF-8 comments**, not labels as recon had them), a **ceiling**, and **marked-form-only above the ceiling**, which closes the "a newly minted id is by definition above max" hole at **zero measured cost** (0 tier-2 failures, with `F64`, `P384`, `U256` all present and never written marked; `F64` is `Type::F64`, IEEE-754 — **not** the genuine id recon called it). Marked-form-only *everywhere* was measured and rejected: **218 of 352** cited ids appear bare only. `docs/decisions/` is **OUT**, as it already is for decisions — including it fails **13** rows for ids D92/D93 merely *proposed*, punishing the one document type whose job is to propose; `scripts/` + `.sh`/`.mjs` are **IN**, because the one genuine finding is there: **`scripts/fuzz.sh:98-103` specifies real fuzz work and names it `A71`, and no row defines it** — missed twice by the code this was asked to mirror (`scripts/` not in `DECISION_SCAN`, `.sh` not in its suffixes). `A71` gets a **row**, not a suppression, so the check earns its keep on its first run; `R46` (a **Sectigo CA subject DN** sitting in one of R's 12 holes) and `Q72` (**mention, not use** — the cited sentence *records* that Q72 was never issued) go into `TASK_ID_NOT_A_CITATION`, keyed **(id, path)** with stale entries failing both ways. **`P18` needs no exemption at all**: the "standing offset" is a **regex bug** every ad-hoc script has repeated — protocol rule 1 mandates the checkbox-less struck shape `- ~~**P18**`, and once parsed, 497 live + 1 struck = **498** = the header total and *entries with no row* = **0**. The 21 rows without entries go into a **date-locked, closed** `ROWS_PENDING_ENTRY` (a 22nd entry needs a false date in a diff or a constant move that invalidates all 21), drained by two rows split **13 open** (live hazard, M2) from **8 done** (archaeology, M3) — refused as Q85 lane work because writing a `Do`/`Accept` for a row you did not execute is defining it second-hand, the defect Q85 exists to close. **Accept bullet 1 was unsatisfiable the day it was written**, not merely stale: `A68` and `Q87`, two of its seven, are cited in **zero** files anywhere. Also found: the decision half shares the surface hole and closing it costs **zero** new failures; project memory is **wrong** that S33/S35 lack entries (both have them, and neither has a row); `TODO.md`'s twelve self-reported counts are all correct today and verified by nothing, but must stay **advisory** because counts drift by design mid-wave. Eleven derived self-test cases, **three green arms** pinning the tier-2 blind spot, the struck-row shape and `Q14`'s `#### ` sub-heading. Zero frozen bytes, zero new required contexts, no `cargo` run | RESOLVED (Q85 executes) | 2026-08-10 |
| [D110](D110-calendar-response-margin-reply-class.md) | A115 — which reply class `MAX_OTS_CALENDAR_RESPONSE_BYTES`' margin divides by — **the UPGRADE reply, 59.31x, and the question was already ruled.** A115 and the brief both frame the keying as *"a substantive ruling D54 §6.1 assigned"*; read in full, **D54 §6.1 made it** on 2026-08-02 — *"the F4 row must be completed with that figure before M2 closes"* — and D54 §9 finding 3, `OTS-BOOTSTRAP.md`, `ots/mod.rs:89-91` (*"it is the figure the F4 row takes"*) and `replay.rs:141-142` all agree, so **the registry cell is the only artefact in the tree keyed to the submit reply** and one of the five it contradicts is a code doc asserting what the registry currently falsifies — A106's *"a correction landed in one of two places has not happened"*, one file over. Keeping submit would require **amending a ratified decision**, which nothing argues for. Re-measured from bytes, not from the row: submit 207/170/**220**/207/170/185, upgrade 1 000/1 036/**1 105** ×2 digests, **both pinned by the same test** (`the_measured_upgrade_response_sizes_are_the_f4_row`, per file) — and the upgrade's sample is **complete and committed** where the submit's *"largest of 18"* is prose-backed, six committed of eighteen. Arithmetic re-derived with the checker's own integer expression: 297.8909 → **297.89x**, 59.3086 → **59.31x**, both of A115's figures correct. The limit bounds **any single calendar reply**, not a request class (`submit.rs:343`, `upgrade.rs:213`, no third site) — which *strengthens* the ruling: a shared cap is only as generous as its worst shape. The class set is **exactly two**: non-2xx bodies, including A14's two 404 discriminators, read under `HTTP_ERROR_BODY_CAP_BYTES` (4 KiB) at `http.rs:820-835`, **not** `receive_cap_bytes`. **"Record both" is refused on §5's own rule**: `printed_margin` reads only the leading `N.MMx`, so a second margin is *"a number that reads like the checked ones and is checked by nothing"* — A113's words for the defect it landed to remove, and the 15.28x shape that injured A109; both figures are still recorded, the submit one **unbolded** and not written as a margin. **Value KEEPS 65 536** — worst shape is **1.69 %** of the cap, structural cost genuinely none, a lowering saves ≤ 256 KiB of transient heap, and **zero of the four `DEFAULT_OTS_CALENDARS` endpoints has ever been measured** (the A25 captures used `*.btc.calendar.opentimestamps.org`, which `calendars.rs:6-13` says the default list deliberately excludes). **§6 owes no entry** — and the brief's *"D107 fires on lowering alone"* is the rule **D107 replaced**; D107 R1's exclusion table names *"correcting `measured against`, `margin` or `structural cost`"* as owing none, and writing one anyway reddens the bidirectional log checker. **Ships an instrument**, because A113's cross-check is document-internal and a *consistent* re-key back passes every test today: `the_f4_row_is_keyed_to_the_upgrade_reply_not_the_submit_reply` lands in `antseal-anchor`, the only crate that can see both operands, literal-free on both sides. Also repaired: the §5 margin band's ceiling (297.89x → **178.09x**) and its floor claim, which has said `MAX_OTS_DEPTH` at 12.05x since A110 put `MAX_CHAIN_CERTS` at **2.0x** in the same table. **No constant moves** | RESOLVED (A115 executes, with A114) | 2026-08-10 |
| [D111](D111-registry-prose-cross-check.md) | Q131 — does a prose cross-check between the registry's two surfaces exist, and what does it compare? — **the prose⟷prose closed-vocabulary check is REFUSED, and four strictly mechanical document⟷mirror checks land in its place**, all four GREEN on the tree as it stands, so no frozen byte moves, no erratum is needed and no tag event fires. Refused on **measurement, not principle**, and it fails the two ways that matter most. **It is green at its own motivating instance**: `UNANCHORED` occurs in the prose of *both* surfaces at registry §7.6 key 3 and nowhere else in either, so a term-presence check there is satisfied by construction, and the divergence Q131 was minted to catch — the mirror dropping the emphasis and adding *"Anchor kind is positional"* — is not a vocabulary event at all. **And it is red at roughly thirty sites that are not defects**: the tier letters are md-only at **25** occurrences, `may be empty` is absent from the `.md` `len/shape` prose at **7** of the 8 sites the mirror carries it, and `reserved` splits 1 md-only / 1 json-only. The root cause is that **the two cells are not the same field** — the `.md`'s `len/shape` cell merges byte length, shape pointer, tier tags, conditions and prose, while the mirror splits those across `length`, `type`, `rule`, `tier` and `notes`, so **66 of 68 pairs differ literally**: `622f5fe` §1's *"the two are not row-comparable"* finding, at a second table. The real gap is mechanical rather than prose: the mirror carries `type`, `presence`, `rule` and `tier` as structured fields, D108 §3.1 C1 names all four **format surface**, `grep` over `crates/` returns **zero** readers of `tier` and `rule`, and the `.md`'s `type` column — read by no D-family test either — agrees with the mirror at **68 of 68**. The motivating instance is already covered by D108 R4's `every_erratum_quotes_its_frozen_sentence_verbatim`, which pins both copies of the §7.6 sentence byte-for-byte, just not comparatively | RESOLVED | 2026-08-10 |
| [D112](D112-cbor-cross-check-scope-rule.md) | Q130 — how the CBOR cross-check decides scope — **the registry flag is the right answer, is built, and is NOT Q130's.** It has an owner with a better specification and a *measured* instance: **F31**, open since 2026-07-28, whose `Do` already names `FROZEN.sha256`'s `#! kind` directives as the home and already specifies the must-exist biconditional; **Q54** then binds them — *"two columns on one directive line and one parser, not two mechanisms over the same registry"* — so a one-column flag here forces the second directive edit Q54 exists to forbid. **The brief's decisive question is answered against its own premise, twice.** There is **no freeze event**: `testdata/vectors/v1/FROZEN.sha256`'s bytes are pinned by nothing (its `EntryPolicy` admits `.json` only, so it cannot list itself; `wasm-bitmatch/build.rs` and `vector_runner.rs` classify it as an auxiliary; `docs/format/FROZEN.sha256`, the tag-bound one under D108 §3, does not list it and carries no `#! kind` lines), `vector-freeze.sh`'s frozen guard iterates `grep -v '^#'` and cannot see a directive line, and the `#! kind` block has been appended to **three times post-freeze** — S4, R12, A22, the last a pure addition. And the threat is **not hypothetical**: the false *positive* is, but the false *negative* is `crypto/manifest-aead.json`, whose `manifest_bytes` is CBOR-shaped and **not well-formed** (`5820` declares 32 bytes over a 30-byte ASCII stand-in; `581e` was meant) — `strict_render` says `truncated: no head byte at offset 36` — sidecar-less, frozen, checked by nothing, and measured by F31 twelve days before Q130 was written. The row has the two directions the wrong way round. **Q130's own subject is the duplication, and it is still live**: the row claims *"Both now share one `_in_scope` helper"*, and `_in_scope` is a **closure inside `self_test`** while `run` carries its own inline copy, held in step by a comment — the exact instrument the brief rules out. It closes on **one `scope_of`, two callers**, a self-test that sweeps **every** in-scope document rather than the first (which dissolves the sort-order dependence that caused the incident, and honours D31 §6's ban on hard-coding a vector or case name), computed fault counts, an explicit non-vacuity `CheckFailure`, and `the_cbor_scope_predicate_is_written_exactly_once` in the file that already bans `cbor2.dumps` *"prose included"*. Three defects found on the way, none in the row: **the self-test plants each fault inside the `try` that judges it**, so `reorder_a_map`'s own abort prints as `RED as required`; there is **no positive non-vacuity assertion** — three bare `next(...)` whose `StopIteration` never reaches `main`'s handler, which is why the 2026-08-09 red was a traceback; and **five of thirteen kinds carry no `expect.cases`** (`commitments`, `hkdf-labels`, `manifest-aead`, `unit-aead` use `expect.vectors`; `signatures` uses named sub-objects), so the neutral out-of-scope line is **false about its reason** for a third of the tree. The flag's design is fixed here for F31 — `key=value` attributes so Q54's column is an append, `cbor=yes\|no` **mandatory with no default** (mandatory dominates both defaults: a silent `yes` is the loud A22 incident, a silent `no` is `manifest-aead` for ever), semantics *"commits format CBOR"* not *"has a sidecar"*, the biconditional asserted **in Rust** so the Python gains no third parser of the directive vocabulary and no coupling to the freeze manifest. Also corrected: the row's census (**23** cases carry `diagnostic`, 16 dict-valued, 7 being `anchor`'s — not *"only bundle ×10 and manifest ×6"*), and `#! kind` is measured to be **the only directive arm that swallows arbitrary trailing content** — `#! kind bundle F13 cbro` parses clean, and `since` is asserted by nothing. **Zero frozen bytes move.** | RESOLVED (Q130 executes; F31 inherits R5–R9) | 2026-08-10 |
| [D113](D113-orphaned-decision-owner.md) | Q145 — a resolved ruling orphaned by its named owner's closure — **the proposed ticked-owner join is REFUSED and a keyed back-citation lands instead.** The row asked for *"a resolved decision naming an owner whose row is ticked"*, reported for human confirmation, on the premise that the script *"already parses the register and the checkboxes, so it is a join over data it holds"*. It holds two of the three columns. **The third is prose in 94 files**: `Owner:` occurs **20 times in 16 documents in five spellings**, **2 of the 20 are D8 quoting a tail it is deleting**, 2 name a domain letter, and the anchored per-ruling form exists in **one** document; the real convention, `- **Owning tasks:**` (55 of 94), is a related-tasks bibliography — D104's names six, including one that *"does not move"* — and **27 documents have no owner field at all**. Precision, measured: **1 of 42** over every `Owner:` line, **1 of 318** over the front-matter field, against a hand-verified base rate of **12/12** owners closed *and* executed; and the report is **monotonically non-decreasing**, so it has no green state and no drain and D109's date-locked register cannot be transplanted. **The killing measurement is on the rule's own two-row domain**: D104 §4 → A48 is executed (registry `:222` reads *"re-measured by A48 2026-08-10"*, margin `12.05x`) and §6 → A106 is **not** (`limits.rs:361` still gates on `size_of::<usize>() == 8`; the cost cell holds zero occurrences of `24 576`) — **both owners ticked, so the predicate fires on both and separates nothing**. One fact separates them: **A106's row cites no decision at all**, the only D104 owner that does not. So: **`--decision-owners`, hard-fail** — every `**Owner: <ID>**` line in a RESOLVED decision names a registered task whose row names that decision back. Run against the tree it is **red on exactly D104 §6 → A106, zero false positives**, silent on §4 in the same file, green on the one-sentence repair — **it sees its own motivating instance**, which is the test D111 failed. **No exemption register**: the backlog is one line and it is the defect, so registering it would make the first run certify what it was written to find (D109 §3.4's A71 precedent); and with the tree red at baseline the red fixtures are unfalsifiable, so **the A106 repair must ride the same commit**. **A premise of Q145 is falsified**: A106 *did* leave a forwarding pointer — *"filed as **A114**"* — and what was lost was the **decision id**; A114's row cites **D102** as the *subject* of its defect and never D104, so *"cites some decision"* would have been green. Blind spot named — prose assignments — and bounded by a protocol clause plus a loud vacuity guard; every wider surface was measured and refused with its own artifact: the front-matter bullet mis-parses 2 in 12, and RULING headings read **D107's Ruling 1 as task R1** | RESOLVED (Q145 executes; A121 unchanged) | 2026-08-10 |
| [D114](D114-line-123-citation-authority.md) | Q148 — which of the tree's line-123 citations are actually wrong, what each one's authority is, and why the lint for them cannot exist — **the defect is real and the row's arithmetic is wrong in both directions**: the sweep finds **189** citation sites, not nine, and **ten** are defective, not nine. **One of the row's nine is not a defect at all** — `format_registry_freeze.rs` already names Q14 for permanence and cites line 123 only for the compatibility break, exactly the shape Q132 prescribed, so it is a class (b) and must be left byte-identical. **Two sites the row never named are defective**, and the second changes the wave: the misquotation is in **`docs/format/registry-v1.md`'s §0 freeze blockquote**, inside the SHA-256-pinned normative document a third-party verifier implements from, and therefore **unreachable by Q132's fix** — its disposition is D108's landed errata instrument, not an edit. So the wave is **9 prose edits + 1 erratum entry**, not *"nine more sites, same fix as Q132"*. The row's `Do` is wrong about the vector family in one place: the `#! status frozen` flip is **Q14's**, not Q6's, so at that one site the authority the row wants substituted is already the one the site names. The row's Notes premise is false as well — both citations at `vector-freeze.sh` are right for retention, because **line 123 states retention itself** in its own second clause; line 123 is wrong there only for the *byte pin*, a third claim in the same sentence. **The instrument is REFUSED on three independent measurements**, the last structural rather than a tuning problem: a prose lint over this class is red on the **corrected** text, because the corrected text must say *"this is **not** under line 123"* and a regex cannot read negation. The strictest usable variant is already red at two sites **Q132 made correct last wave** and at one of this wave's own proposed replacement blocks; the variant narrow enough to avoid that is **green at 6 of the 9 fixable sites the row exists to fix**. What lands instead is one entry in an existing, tested gate | RESOLVED | 2026-08-10 |
| [D115](D115-entryless-row-reconstruction.md) | Q134 — what an entry must contain when the row is its only source — **the set is confirmed exactly and every claim about it is refused**. 20 entryless rows, **12 open / 8 done**, `Q130` confirmed drained, register neither stale nor grown. But **"substantially a reformat except two" is 3/9, not 10/2**: only `Q123`, `R76` and `U39` carry both a `Do` and an `Accept`; 5 carry a `Do` alone; 4 carry neither — **nine of twelve need an `Accept` authored**, which is exactly what D109 §2 (b) forbids a lint lane to do silently. **The reason A105/A108 were singled out does not exist** — no Current-focus sentence says it; the phrase *"defined second-hand in three places that disagreed"* is **A106's** (`TODO.md:462`), misattributed at **D109 §8 (iii)** and inherited by Q134's row and entry, which get A106 right one clause earlier. Membership is wrong too: **A108 is better specified than `A72`, `A74` and `R60`**. **Atomicity is per row, not per batch** — measured on a full tree copy: 10-of-12 drained is green on `--task-entries`, the no-flag run **and** `--self-test`, while both halves of one row's drain are red — so the twelve **can** be split; the real constraint is that twelve register lines share one file. **A105**: both cited line ranges verify exactly, but `chain.rs` already names three chain-level `proven` tests (the gap is the **verdict** level), and matrix row **`V7.2`** already exists for the property reading **`NONE at M0`**. **A108**: the **manifest half already landed** (`FROZEN.sha256:82-90`), leaving one bullet in a **closed** task's `Do`; and its *"two frozen files, one with a hatch and one without"* is **false** — `report/`'s three anchor verdicts are all `invalid` with `source: null`, so **all three events move only `anchor/anchor.json`, the file with no hatch**, which strengthens D101 RULING 3c. Provenance convention: a **dated bolded bullet naming the row by ID, never by line number**, separating transcribed from authored; where the row offers a menu, **the `Do` is the choice**. Correcting D109 §8 (iii) and D101 §3.4 is **blocked on `Q122`**, one of the twelve. Also found: `--matrix --milestone M2` names **11** deferred rows incl. **six M1 rows**; the `report` kind pins the anchor surface only in its degenerate case; `Size`/`Milestone` live in two places unchecked. Character range corrected **386–2 193 → 386–1 735** (2 193 was `Q130`'s, the row that left). Zero frozen bytes, no `cargo` run | RESOLVED (Q134 executes) | 2026-08-11 |
| [D116](D116-gate-and-sweep-coverage.md) | Q133, Q141, Q140 — the gate and the sweeps are blind to three things, and **all three rows state the wrong number.** **Q133**: D109 §6.4 cmd 8's *"zero new unresolved decisions — the widening is free"* measured **one** of the two failure classes `check_decisions()` raises from one sweep; the **Q58 line-citation** class has no ceiling, no allocation bound and no suppression register, and widening turns the lane **RED** immediately — on `scripts/check-traceability.py:1245`'s `registry-v1.md:1097`, the RED arm of the Q58 case, **not** on the `D77000` at `:1229` that the row and D109 §8 (i) both name, which is the GREEN arm of the ceiling rule and stays correctly silent under every arm. The self-exclusion is therefore mandatory and its comment must name the literal that fires. **One** constant, not two: the only honest comment on two would be *"these are the same, one is stale"*, which is D112's defect one file over. Proven by planting in both directions — on a staged tree, a `D<n>` cited only from a `.sh` with its record deleted is **green today** and **red under the proposal, naming the file**; on the real tree the same surface goes red via the Q58 class, then green on deletion, with `git status` unchanged. The new self-test case must be **class 2**, because D1–D112 is measured **100 % dense with zero holes and zero failing ids**, so no single-file mutation can make a `D<n>` fail and the harness applies exactly one. **Q141**: *"measured cost ~4 s"* is wrong by ~12× — `cross-check.sh --self-test` is **55.66 s then 46.41 s** against `--check`'s 24.63/22.48 s; the 4 s figure belongs to `crosscheck_cbor.py --self-test`, measured at **0.10 s**. The lane lands anyway, on a better argument: 46 s fires planted faults on **six** surfaces, five of which no local lane exercises. **Q140** reproduces verbatim (`cargo check --workspace` **rc 101**) and exceeds its row twice: **two** walkers carry the identical fatal rule — `vector_runner.rs`, which is CI's `golden-vectors`, `cross-os` and `test` contexts, fails the same way — and the trigger is **import, never run** (measured: running the checker creates nothing), so the producer-side fix has **no producer in this repository to fix**. A vim `.swp` bricks the build identically, so the class is wider than Python. `vector_freeze.rs`'s `collect_json` is immune because it filters **positively** for `.json` instead of asserting a closed classification — the tolerant shape is already in the tree. Ruling: prune `__pycache__`, classify the git-ignored droppings ignorable **after** the `*.json` arm, one list in both walkers with a byte-identity test, and a test asserting `.gitignore`'s patterns are covered — so one rule **derives** from the other instead of two disagreeing. **The general measurement found Q125's shape a third and fourth time, in the direction nobody has looked**: **five gate lanes have no CI counterpart at all** — `gate-features.sh --self-test`/`--check-partition`/`--heavy` (so the HEAVY features are compiled by **zero** CI lanes), `wasm-bitmatch.sh --trigger-self-test`, and `e2e-devnet.sh --self-test`, whose cron sibling runs the lane **bare** — violating `docs/ci-verification.md`'s own *"a lane that has never run on the remote is not evidence"* five times with nothing saying so, while the gate header enumerates only the opposite direction. Plus **256 vs 1024 proptest cases**, undeclared. Four further argument divergences measured **not** substantive: `fmt` (101 files each, diff clean), `clippy` (virtual manifest → `--workspace` is a no-op), `test` (**2440 = 2440**; the LIGHT features are already unified through dev-dependency self-edges), `wasm-bitmatch` (bare and `--check` are one branch). **35 comparison points; 15 CI steps absent from the gate, 5 gate lanes absent from CI, 5 argument divergences, 3 undeclared classes.** | RESOLVED (Q133, Q141, Q140 execute) | 2026-08-10 |
| [D117](D117-resolved-decision-corrections.md) | Q122 — does a resolved decision's body take dated corrections, or does only the register row move? — **BODIES TAKE DATED CORRECTIONS; the lean survives and every reason given for it is wrong.** The row cites *"D84 §2's mechanism"*: §2 is the permanence argument and holds none; the mechanism is D84's two trailing sections, and **they are two incompatible mechanisms** — the first **deleted three body lines** (`5994365`, +27/−3), the second was a **pure append** (`3d47363`, +37/−0) — which three wave-13 amendments then cite as *"the appended-correction mechanism D84 established"*, false of the first. **The rule was already written, on the lean's side, in the third sentence of the file the row cites for something else**: `docs/decisions/README.md` opens *"A decision is changed by editing its file with a new dated entry, never by **silently** rewriting history"* — the prohibition is on silence, not change. **The other arm is unimplementable three times over**: *"the register row"* is **three** rows (`TODO.md`'s **116, complete, machine-read**; `README.md`'s **69 — 29 records missing, D53–D61 and D97–D116, and read by zero scripts**; **33** in-body `## Index row` sections of which **27 were never applied**); on Q122's own forcing case **the row is inside the body** (D95's row is verbatim at `README.md:81`, `TODO.md:788` *and* `D95:349`); and *"bodies are immutable"* would reverse **30 executed resolved-body edits** — of 48 modifying commits **28 deleted lines**, 132 in total, and nine decisions carry standing *"orchestrator applies at source"* sections, one of which (**D93 §13.3**) ordered a ground **deleted** from D56 and it was. **Accept bullet 3's count case was solved in wave 11 and nobody registered it**: `7bc746b` edited resolved D98's rider 3c *"Three"* → *"Four"* in place, with an inline dated attribution and an explicit **"This rider is not renumbered"** clause naming its six citation sites. **Wave 13's four amendments never amended a committed body** — `1c702d4` **created** all four files (880/0, 899/0, 873/0, 1209/0), so *"the body above is byte-unchanged"* is vacuous as evidence. Ruled: six rules — the correction is a **dated appended section** that is the single home of the new fact and quotes the original verbatim; the wrong sentence is **REPLACED if a lane would act on it** and **STRUCK and left standing if it is a measurement, census or argument**; the marker carries **at most one scalar**; **section, ruling and rider identifiers never move** (**2938** `D<n> §<n>` citations across **63** decisions); a **count** correction states the original, the new figure, **the predicate and the command**, whether it was *wrong when written* or *went stale*, and whether the conclusion survives, with its **epoch**; and a correction **never cites a line number** — measured: `tasks/R.md`'s `is_verified` correction has the right figure and **all five locators stale in three days**. **Scope: the `is_verified` sites are a FOLLOW-ON**, and the row's domain is wrong both ways — **six** statements in four files, three of them one register row in three homes, one named site (`TODO.md`'s R74 row) **already corrected**, and both of the row's `TODO.md` line numbers moved. **`Q167` lands under REPLACE**, D114 §7 and D101 §3.4 under STRIKE. **A fifth parked correction the row never named**: `D94:554-555` still reads *"three of A22's four Accept rows"*; **D101 §2.2 and D103 §7.2 RULING 6a each ordered it corrected and neither ran**, because `tasks/Q.md:1407` asserts *"a ruled record is immutable"* — registered nowhere, citing nothing, contradicting README's opening, and now refused. **Exercised**: D109 §8 (iii) corrected under STRIKE — five wrong claims (count 13→12, `Q130`'s membership, range 386–2 193→**386–1 735** at `5fbc48d`, A106's history misattributed to A105/A108, and per-batch atomicity), `(iii)` not renumbered, every ruling standing. Zero frozen bytes, no `cargo` run, traceability green before and after | RESOLVED (Q122 executes) | 2026-08-10 |
| [D118](D118-deferred-matrix-rows-and-the-gate-constant.md) | Q165 — the eleven `deferred` matrix rows adjudicated one by one, and **both readings refused: it is ten and one**. Ten (`V2.3`, `V3.5`, `V6.1`–`V6.6`, `V7.2`, `V7.3`) describe work that landed 2026-08-01…08-06 and are now `covered`; references resolved go **55 → 97**. **One is real and the row does not name it**: `V7.1`'s real calendar cycle ran 2026-08-02 → 08-03 with six `200`s — **performed by `curl`, before the OTS client existed** (`CAPTURE.log` carries curl's own resolve error), so antseal's own path has never spoken to a calendar; runbook §0 says the protocol *"is not yet executable end-to-end"* and A25 row 1 is **PARTIAL**. `V7.1` → **`gap`**. **`CURRENT_MILESTONE` `M0` → `M1`, not `M2`**: it names the last review that **passed**, and `M2` would force `V7.1` into `ACCEPTED_NON_COVERED` — a register reserved for *"a milestone shipping with a known hole"* — and mute it at the very review meant to read it; the register stays `{}`. Gated rows **16 → 22**; flagless run green; `--milestone M2` now fails on **one** row, not eleven. **M1's gate is not implicated in its verdict but is in its evidence**: every M1 bullet had a real test the day M1 passed, while this matrix said six were owned by a later milestone and the checker agreed because it was pointed at M0 — **Q51's fix was correct and unarmed**, and *nothing in the project makes a milestone review move the constant*. Also: **both status-gate `--self-test` fixtures were pinned to `V6.1`'s cell** and broke on the no-op guard the moment it went `covered` — retargeted to `V9.2` (M4), whose expiry lands on Q34's release gate. Both section preambles were false in three clauses each for eight days. Corrects **D115 §4.1** twice (`:241` for `:244`; `V7.2` mis-routed to A25 row 1) — both blocked on `Q122`. Zero frozen bytes | RESOLVED (Q165 executes) | 2026-08-10 |
| [D119](D119-decision-index-identity-and-the-index-row-sections.md) | Q180 + Q186 — what `docs/decisions/README.md` is, and what the `Index row` sections mean — **the file is the decision directory's FRONT PAGE, not a third register; its opening claim is made TRUE by a check over the glob; the sections are DEMOTED into the edit sets they always belonged to.** Both of Q180's arms are refused by a defect in neither row: the index and `TODO.md`'s register both record **D29** as *FROZEN 2026-07-28 at Q14*, while **D29's own record still reads `RECOMMENDED (R1)`, dated 2026-07-27**, telling a reader the format may still change — so landing the 29 missing rows leaves it wrong and *"curated selection"* licenses it. Q180's *"the index only ever falls behind"* is an invalid inference and false: presence is one-directional, **status runs the other way**, and `dfe7071` corrected the row while D29's file has one commit ever. The domains differ, which is why neither supersedes: `TODO.md`'s register is **118 rows, D1–D118, dense, machine-read**, holding **18 fileless ids** (5 homed, **13 open**) — its domain is the **id space**, the index's is the **files on disk**, so the obvious register-keyed check is red on 47 ids for documents nobody has written. **Deletion refused on measurement**: D117's ruling turns on the file's third sentence, D91 and `docs/security-assumptions.md` cite the convention by name, the *Evidence convention* paragraph has no other home, and the table is no register duplicate (median similarity **0.25** over 71 ids); measured over 17 citing files, **nothing depends on the table's content** — every binding citation is to the prose. **Q186's census is a parsing artefact**: *"exactly one spelling"* is **two over 37 documents** — D86 §10 and D87 §8 carry a **numbered** `Register row (… added at merge)` heading, so those two cannot be deleted without moving an identifier D117 forbids moving. Three more of its particulars fail: **1 of the 35 is fenced** (D90), **D111 holds no table row at all**, **D118 carries a header to strip**. **The merge never happened**: 83 merges, last `8a6cd96` 2026-08-05, **31 commits since, all direct to main**, and **zero** applied rows landed at one — 6 same-commit, D90/D91 swept 4 days late. But the sections are not pointless: D86/D87's own `Index note` records the rationale as **concurrent planners sharing one tree**, still true, so the hand-off is kept and only the false event-claim is dropped. **D101 and D114 have no pre-written row at all**, so the backfill is 27 copies and 2 compositions, not 29. Counts re-derived: **71 of 100**, not 69 of 98; **8 applied of 35**, **10 of 37**, not 6. Check cost measured at **0.03 s / ~35 lines**, red today on 32 findings. Generation declined with its precondition recorded: it would overwrite the correct index status with D29's stale record. The heading-form retrofit is handed to **Q188**, whose own inventory of eight is measured at **four spellings over 40 documents**. The briefed command `--check` **does not exist**; the full form takes no flags | RESOLVED (Q180/Q186 execute) | 2026-08-11 |
| [D120](D120-self-test-staging-and-differential-arms.md) | Q191 — `--self-test` **keeps the working tree** and stages it **as git sees it** (`ls-files` ∪ `ls-files --others --exclude-standard`, minus the `*.tmp.*` shape Q150 named), **and its arms become deltas** against a baseline measured on that same tree. **The row's question is aimed one arm to the left**: 27 cases are **19 red + 8 green**, and **every one of the six checks has a green arm**, so `--self-test` silently re-runs the whole check suite over a copy and inherits every unrelated red. Reproduced twice — a sibling lane's unentered row reddens it with two messages naming *green-case mutations* of `TODO.md` and `tasks/P.md` and never the row, and this lane reddened its own harness by leaving two scratch `.py` files in `scripts/`. **At the gate it is worse**: `lane_traceability` runs the self-test first and returns, so the flagless half never prints the accurate finding (planted id: **0 occurrences** in the lane output) and the lane's annotation claims *a check stayed green over corrupted input* — the inverse of what happened. **`git archive HEAD` refused on measurement, not argument**: on one working tree carrying an uncommitted edit that rots a fixture, the snapshot exits **0 with 28 ok lines**, the live copy exits 1 with the finding **plus 2 spurious green-arm failures**, and the ruled design exits 1 with **exactly one message**; in CI the checkout **is** `HEAD`, so the snapshot's only effect is to move fixture-rot detection off the developer's machine — the opposite of wave 14's two local catches. Index snapshot refused as `HEAD` plus an unobservable third state; **synthetic minimal tree refused on counted cost** — **21 of 27 targets are fixed** (17 literals over **8 distinct paths** + the matrix constant, confirming Q184's nine) and the other 6 are derived from a register/entry/decision/matrix corpus a fabrication would have to reproduce — and because it would **permanently disarm the no-op guard**, the instrument that has caught every fixture rot so far. Measurements: **922** files confirmed, but **~5.1 MB corrected** — that is `du` of `fuzz/corpus` alone; the two directories hold **2,312,609 B (2.21 MiB)**. The copy stages **1,983 files / 21.42 MiB** where **at most 474 are readable by any check** — **1,509 staged and unread**, 925 untracked (921 corpus + 1 artifact + 2 `.pyc` + **1 lock file**, the last ignored only by `.git/info/exclude`, which a `.gitignore`-derived ignore set cannot see). **git does NOT ignore this project's atomic-write shape** (`check-ignore` rc 1, both spellings), so Q150's glob is kept on top of git's set — neither closes the class alone. Saturated writer **reproduced 5 of 5** exhaustions (bursty writer only 1 of 3), against **3 of 3** clean stages for the ruled mechanism; a vanished **tracked** path is an error, a vanished **untracked** one an announced skip (1/31/49 observed). Cost **+0.4 s on 6.3 s**. `--check` **is not a flag** (rc 2). Zero frozen bytes | RESOLVED (Q191 executes) | 2026-08-11 |
| [D121](D121-der-structural-cost-figures-normative-home.md) | A126 — where the seven DER structural-cost figures normatively live. **The row's lean (move the four into §5) is refused on the document's own text**, which already ruled twice and was never cited: *"**Where the DER path's 7 488 B goes, and why no one row carries it**"* and *"§5's two numeric `structural cost` cells … **this table is the derivation they point at**"*. §5 is keyed by **F4 limit**, and three of the four figures belong to non-F4 bounds — `MAX_PATH_NODES` (classified out in `caps.rs`'s own `NOT_F4_LIMITS`), `MAX_INTERMEDIATE_COUNT` (**frozen**, D10 §2 row 8) — while `7 488 B` is a **sum across two containers**; §5's preamble names four owners and *"Nothing outside those four may"*. **Ruling 3/4**: §5's `MAX_CHAIN_CERTS` cell is authoritative for `4 288 B`, `1 024 B`, `664 B`; §5a for `3 200 B`, `7 488 B`, `2 048 B`, `4 096 B`. **Nothing moves.** **The brief's alternative is refuted too**: A123's fix is **three arms, not one** — formulas document-wide, rowed figures cell-scoped, and the one figure with **no row** pinned by `matches().count() == 1` — so §5a needs **no parse**. Shape (a) also fails on its own promise: `mod limits;` is declared **without `pub`**, so `caps.rs` cannot reach `registry_rows` and would have had to **duplicate ~90 lines**, the second parse A126's `Accept` forbids. **Defect confirmed by mutation, not report**: §5's cell → `**4 289 B**` leaves `1056 passed; 0 failed; 1 ignored` — identical to baseline, DER test `ok`, **blast radius zero** — because the needle still occurs **twice** (§5a table + A111 prose); document restored byte-for-byte. Shape (c) (assert from code) refused as a **category error** — already true under D102 rule 6 clause (a), and it answers a different question; shape (d) (delete duplicates) refused — no needle duplicates another. **A122's ground is about the per-target label, not row scoping** (verified, and its *"x86-64 and nowhere else"* is narrowed: §5a does carry `**3 008 B**`). **Corrects A126**: the *"1056 lib tests"* figure it dropped as unverifiable is exact. Zero frozen bytes | RESOLVED (A126 executes) | 2026-08-11 |
| [D122](D122-what-moves-the-gate-constant.md) | Q183 — what moves `CURRENT_MILESTONE`. **The row's favoured shape (b) is refused because as worded it demands the `M2` D118 §5 refused**: the Current-focus block names the milestone *in progress*, the constant the last one *passed*, so an equality check is **red against a correct constant** and a lane obeying it would force `V7.1` into `ACCEPTED_NON_COVERED` and mute it at the M2 review. The predecessor repair fails too — that line has used **five grammars**, the same token meaning the current milestone in three and the finished one in two, and two revisions carry no milestone token at all. **The row's four-file sweep is wrong in both directions** (measured at `1c702d4`: five files, D118 absent, `TODO.md` and `tasks/Q.md` dropped) — **and `TODO.md` is the checklist it supposed missing**: rule 4 already names the exit criterion and the closing acts and omits only the constant. **The defect under the defect: the bump had no owner because the review had no row.** Only `Q14` (M0) and `Q34` (M4) execute a gate; **M1, M2 and M3 have none**, and the one milestone whose review was a row is the one whose constant was right at the end of it. **No check is added**: deriving the constant from the matrix makes the status gate **provably unable to fail** (it would test a predicate true by construction), and deriving it from task counts is refuted by measurement — **M1 passed 2026-08-02 with 11 of 71 rows still open today**, most discovered *after* the gate, so a passed milestone accretes rows forever and `TODO.md` rule 4 says so itself (*"the gate, not the task count, is the exit criterion"*). Ruling: the bump is an act of the milestone-gate row, rule 4 gains it as the third of three closing acts, and the missing gate rows are registered. **Four sites lose the fiction** — the constant's comment, the `--milestone` argparse help that **prints** it, `verification-matrix.md`, and Q51's entry by dated correction (D117) — plus the failure message at `:390` that tells readers a review finished on 2026-08-02 is in progress. Gate figures re-measured exactly (16 at M0, 22 at M1); zero frozen bytes | RESOLVED (Q183 executes) | 2026-08-11 |
| [D123](D123-literal-scan-roots-and-the-suffix-filter.md) | Q190 — **the suffix question is refused as the wrong question**. `CITATION_SUFFIXES` keeps its six members; `CITATION_SCAN` gains a **per-entry rule** — a literal file entry is read **unfiltered** (naming is the deliberation), a directory entry is walked and filtered. **Measured: adding `.toml` reaches SIX `crates/*/Cargo.toml` and NOT `deny.toml`** (a root file is reached only by being named), and the full suffix route (`.toml`+`.txt`) costs **359 → 377 files, 15 collateral**, and **still cannot reach `.gitattributes`/`.gitignore` — the empty suffix is in no suffix set**. The per-entry rule costs **+5, zero collateral**. **The row scopes three root files; the census finds five**: the root `Cargo.toml` alone carries **57 task ids / 100 occurrences / 19 decision ids**, more than the three combined, plus `requirements-crosscheck.txt`. All five named; **359 → 364 files, 391 → 393 ids, both halves green on landing**. **Proven by construction**: a bare hole id planted in `deny.toml` leaves the lane `ok — … all resolve`, exit 0, and is caught by name under the ruled semantics; file restored byte-for-byte (sha256 unchanged, 8654 bytes). **Corrects the row twice**: `.gitignore` carries **six** distinct ids in **seven** occurrences, not four, so the three-file total is **19 (id, file) pairs / 18 distinct / 29 occurrences**, not *"seventeen"*. **Refutes the row's direction of risk**: ids are struck, never deleted, so there is no decay path — the failure is *wrong when written*, and rarity **preserves** it rather than causing it, which also makes these files the cheapest possible thing to sweep. The false-positive fear is refuted by measurement (62 manifest ids and 19 snapshot decision ids, **all resolving, zero would-fail**; license expressions and `RUSTSEC-…` ids match nothing). Two self-test cases added, one per half, the task-half fixture **bare** because all 29 occurrences in these files are bare. Hazard sentence **deleted, not expanded**, with a dated note (D117). Zero frozen bytes | RESOLVED (Q190 executes) | 2026-08-11 |
| [D124](D124-asserting-the-cargo-free-job-property.md) | Q182 — the `traceability` job's cargo-free property **becomes a machine assertion, expressed at the job as a runtime fact, not in `check-ci-shell.py`**. The row's home is refused for **vacuity, not cost**: Q153's near-miss line `./scripts/gate-features.sh --check-partition` carries **no `cargo` token**, and neither do any of the job's five `run:` lines — **0 of 6** — so a per-job text predicate reads green on the incident that produced the row; recursing into the callee reds **all five green steps** (`ci-lanes.sh` 31 `cargo` hits, `wasm-bitmatch.sh` 4). **"The larger half of the work" is refuted**: job attribution is **9 lines, 65/65 blocks, zero unattributed**, matching the shipping checker's own count — measured, then declined as unneeded. **The row's failure mode does not exist**: no workflow installs rustup (every step is `rustup show … \|\| rustup toolchain install`), so **cargo is on `PATH` there now** and nothing fails; there is no cache step, so *"whatever is cached"* is empty. The real cost is a **silent implicit install of the pinned 1.92.0 + rustfmt + clippy + wasm32 std on a green job**, every PR and push, on a repo that exhausted its Actions minutes — which `ci-verification.md:1768-1771` already recorded correctly and the row garbled. **Prose sites measured at 16 across 6 files, not 3 across 2** (6 of them in `ci-verification.md`, which the row does not count; a 17th sits in `check-ci-shell.py`'s own docstring). **The converse is loud AND misdiagnosing**: cargo masked, `--check-partition` prints an unhandled `JSONDecodeError` then *"the extractor is broken"* — so `core-dep-graph` gets `--require` in the same instrument. Mechanism: new `scripts/cargo-free.sh` (`--arm`/`--verdict`/`--require`/`--self-test`); `--arm` is the job's **first** step and installs failing `cargo`/`rustc`/`rustup` shims via `$GITHUB_PATH`, covering **steps not yet written**; `--verdict` is the **last** and reds on a marker file — needed because `gate-features.sh:116`'s `2>/dev/null` **measurably swallows the shim's stderr**; the two steps guard each other. **All five current steps measured byte-identical with cargo masked.** Rejected with measurements: PyYAML (present as 6.0, changes nothing, and would put a hash-pinned `pip install` in the job whose cheapness is the property), `env: RUSTUP_TOOLCHAIN:` poison (works, rc 1, instant — but its message is swallowed by the same `2>/dev/null`), per-step wrappers (opt-in cannot catch a step not yet written), and do-nothing (refused on five counts). **Zero new jobs, zero new required contexts — set stays at 19. Zero frozen bytes** | RESOLVED (Q182 rules; edit set for an implementing lane) | 2026-08-11 |
| [D126](D126-a25-rerun-clause-and-row4-handoff.md) | A25's re-run clause + row-4 hand-off — **the `Do`'s closing clause is DISCHARGED on the `A25-wave16-cycle` campaign and STRUCK** (no replacement Accept row: the future-trigger set is empty — runbook §5, A26, A44, A48 ✅, D54 each own their step); **row 4 closes on the hand-off EXECUTED into Q26's `Do`** ((a)–(e) verbatim-by-citation, A25 into `Deps`, (d) as "~2 minutes"+citation per A107's 128 s, (e) forcing published-not-measured labels; (f) discharged by standing homes: runbook §3.1, D54, A14, A44); **A25 CLOSES on its four rows** (row 1 conjunct-by-conjunct — protocol completed twice, the bootstrap's literal two-day cycle alone meeting "at least once"; row 2 now also pinned by a run named for the property). Three overturn arms refused on measurement: empty trigger set; A26-closed vs A31-open hand-off precedent, the verb "handed" decides; row-1 re-read dies on its own script conjunct, spec line 173's text, and the D117 replacement living in `V7.1`. **Submit residue single-homed in `V7.1` (`gap`) + Q236's gate; disposition D127's — explicitly not adjudicated.** Zero frozen bytes | RESOLVED (registrar executes the edit set) | 2026-08-11 |
| [D127](D127-m2-gate-v71-disposition.md) | Q236 — the M2 gate's disposition of V7.1, the one gap at or before M2 — **ruled exception now, lean survives on measurement, sharpened: the residue is BIGGER than framed.** Submit targets are the four `DEFAULT_OTS_CALENDARS` **pool** hosts (day-1 log) — zero overlap with the calendar hosts every real-endpoint proof spoke to, so the product's client has never contacted the pool set in any mode; marginal proof enumerated (pool-host acceptance of the client-built `POST /digest`, fresh-response validation in situ, the composed client-submitted→client-upgraded cycle that has never run). All three overturn directions die: the slide is mechanically impossible (gated `gap` sans entry = red), the entry is **bump-atomic by construction** (unused-exemption arm reds it before the bump — one change, both directions, and closure likewise), and D118 §5's refusal was time-scoped by its own comment ("the one moment it exists to speak" = Q236's review). Arm (b) refused on cost: does NOT block R16–R27 (cumulative rule), but parks a review whose every other input is measured done on an absent third party and leaves V2.3/V3.5/V7.2/V7.3 outside the flagless gate. Register's first entry priced: decision record + named trigger + machine-enforced removal + third-party-only residue; non-extension stated. Stands under either D126 outcome. Zero frozen bytes | RESOLVED (Q236 executes) | 2026-08-11 |
| [D128](D128-storage-linkage-default-and-the-frozen-report-exhibits.md) | R20 — what `verify_bundle`'s default is, and how the frozen report exhibits survive it. **The default RUNS the storage-linkage layer**, and the committed exhibits are pinned to an *explicitly suppressed* options value at the two executors that **already build their own `VerifyOptions`** (`vectors_report.rs`'s `build_expect`; `bundle_fixtures.rs`'s `verify()` helper) — so **zero frozen bytes move, zero `REPORT_DIGEST_BY_SHAPE` rows move, and no freeze event of any class occurs**. The register's three-arm enumeration is **overturned by a fourth shape it missed**: *the default* and *what the exhibits pin* are **two knobs, not one**, and nothing but habit bound the exhibits to `VerifyOptions::new()` — the sibling `anchor` vector kind has declared its own `verify_at_unix` as an input since A22, so the tree already knew a verify option is a vector input, in one kind of two. **(a) FIXTURE EVENT** is refused on a measured blast radius — 3 of 14 frozen files, 37 of their 47 cases, all 26 R30 rows, and `verify_fuzz.rs`'s content locator losing two of its three markers — for a **negative** payoff: every committed bundle would then link, and the tree would lose its only evidence that an **unlinked** bundle still verifies. **(b) design D105 §10's fourth freeze class** is refused **and is mis-aimed**: that class admits an *added case* (21 → 22) and this change adds none, so designing it would leave this change exactly as refused. **(c) permanently opt-in** is refused on failure-mode asymmetry — it makes the spec's own configuration the thing two future rows must remember, both forgetting is **silent and self-consistent** (21 vectors agree, R30 agrees, R18's `not evaluated` row renders a grammatical sentence), and R27's parity gate cannot see an omission both surfaces share. The crux is spec **line 38** — *"the page's storage story is the offline address-recomputation of the storage-linkage layer"* — read against R22's `verify(bundle_bytes)`, which takes **no options**: the page cannot opt in, so the default must already be in. Consequences: `with_storage_linkage` is **deleted** and replaced by `without_storage_linkage` with its caller list **closed at two** by a literal scan (D123's pattern); `Default` is hand-written so the derive cannot silently disagree with `new()`; three source assertions are re-expressed, not re-blessed; R21/R22 may not expose suppression, so **U30 gets no `--no-storage-linkage` flag** and R23's page no toggle. `REPORT_VERSION` stays 1, no struct gains or loses a field, and the freeze instrument is left standing with its `all cases moved ⇒ FORMAT EVENT` heuristic intact and now witnessed. Zero frozen bytes | RESOLVED (R20 implements) | 2026-08-12 |
| [D129](D129-verifier-page-file-shape-and-the-in-artifact-csp.md) | R23/R25 — the verifier page's file shape, the packaging step, and the CSP that decides it. **ONE FILE, `index.html`**: the `wasm-pack --target web` glue inlined verbatim into one inline `<script type="module">`, the module inlined as base64 in one inline `<script>`, `initSync({ module })` and never the default init, under `default-src 'none'; script-src 'sha256-<H1>' 'sha256-<H2>' 'wasm-unsafe-eval'; style-src 'sha256-<H3>'; img-src data:; connect-src https:; base-uri 'none'; form-action 'none'`. **The wave owner's CONCLUSION survives ON MEASUREMENT while EVERY REASON FOR IT IS OVERTURNED.** `--target no-modules` is refused and was never needed: the `web` glue has **no static `import` at all**, so what `file://` blocks is *fetching a sibling*, not the module-script **type**, and an inline script is not fetched; `no-modules` additionally drops the Safari `TextDecoder` guard on the function every report string crosses and is glue no committed check has ever run (its export surface is measured intact, 21/21 vectors byte-identical, so it dies on what it drops, not on what it cannot do). **The flagged CSP weakness INVERTS**: an all-inline page does not force `'unsafe-inline'` — hashes admit inline classic **and module** scripts in both engines from `file://`, and `'unsafe-inline'` is measured ignored beside a hash — and because the module sits inside the hashed script the hash covers the **99.40 %** of the payload SRI structurally cannot (D63 §4 way 5), so a one-byte payload edit makes the browser refuse to run the page: D63 §2.2's accident job becomes a hard fail-closed. **Still no defence against a malicious host** (D63 §2.1 stands, and the record says so). Measured in **Chromium 150 and Firefox 140.13.0esr**: a real `.sealproof` verified from a real `file://` origin, report **byte-identical to native**, anchors evaluated, drag-and-drop and file-picker working, **one network request — the page itself**; the multi-file arm fails in both engines in their own words. **R23's offline Accept row is KEPT VERBATIM** — answered by measurement, not by scoping. `connect-src` is scheme-only because a six-host allowlist forbids spec line 137's user overrides **undiagnosably** (identical `TypeError`, and D66 §3 R4 forbids naming a cause). `SHA256SUMS` covers **one line**; the pre-packaging `.wasm` is a **signed-release** artifact — two names for two things, not D63 §4 way 2's two numbers. Refused: the hybrid (both artifacts would show one footer digest), inline compression (a compressor version and level would enter the artifact's identity), a nonce, a Worker (blocked by the hash-locked `script-src`, so R54's busy affordance stays on the main thread), `'unsafe-eval'`. Two riders at D63 §5 R5: **base64** named beside *"compute digests"*, and the *"one-token diff"* clause **narrowed** and replaced by a mechanical check — 29 ms to decompose the artifact into a **2 858-byte** authored residue plus two byte-exact tokens. R26's set-equality check runs over served **responses**, never requests (an automatic `GET /favicon.ico` 404s and is not a served artifact). Cost stated: **+43 %** on the wire, permanently. Zero frozen bytes | RESOLVED (R23/R25 implement) | 2026-08-12 |
| [D130](D130-r18-wording-route-to-the-verifier-page.md) | R23 — how R18's frozen wording reaches the verifier page: **a FIFTH wasm-bindgen export, `verify_rendered(bundle_bytes) -> String`**, returning a core-built rendered document (`redaction`, `rendered`, `report` byte-verbatim, `verdict`); D18 §5 R4's closed list opens to five and closes again there, `REPORT_VERSION` stays 1 and zero frozen vectors move. **The blocked row's own premise was FALSE and is corrected here**: there are no *"report's R18 strings"* — all 21 committed report cases carry **zero** rendered prose, the only multi-word strings being the sealer's own fixture titles — and the same false sentence sat in two shipped source files. The cost arm collapsed on a finding nobody had noticed: `RenderedVerdict` already exists, is already `Serialize`, is built **unconditionally on every verification**, and its sentences are already in the shipped module, computed on every drop and thrown away at the boundary. **Two of this record's own numbers are PREDICTIONS the implementing wave then measured, and the measured ones govern**: the module grows **+15 251 B raw (+0.828 %), +5 881 gzipped** — not §1 (d)'s predicted +31 772 / +11 028 — and §1 (j)'s unescaped-value count is **FOUR, not three**, because `headline_line` embeds `wording::source_slot(headline.source())`, artifact-authored and unneutralised, escaped by the implementing lane under §3 R5's own *"every sealer- or artifact-authored value"* rule and documented on `RenderedVerdict::new`. **§3 R8.2 is SUPERSEDED, not applied**: D131 §5 R1 renames the page `verifier-web/index.template.html`, whose extension is already walked, and §5 R2 replaces the vacuous `sources.len() > 10` guard with per-root floors — so adding a `template` suffix would mint a filter entry for a file that no longer needs one. **Amended by D132 §9.5**: the document is **five** members, `plan` joining, and §3 R2's *"R24 needs no further export"* is true of the online wording and false of the online inputs | RESOLVED (R23 shipped; two figures superseded on measurement, §3 R8.2 superseded by D131) | 2026-08-12 |
| [D131](D131-the-verifier-web-directory-footprint.md) | R23/R25 — what `verifier-web/` contains, what is committed, what scans it, and where the page's tests live: **a SOURCE-ONLY directory holding exactly one committed file, the template `verifier-web/index.template.html`**. The 484-byte placeholder `verifier-web/index.html` is deleted in R23's commit; the built 2.47 MB page is **never** committed and never written into `verifier-web/` but to `target/verifier-web/index.html`, which `/target` at `.gitignore:8` already covers — so `.gitignore` is not edited at all. **D129 §7's template name is overturned on a planted fault**: `Path::new("index.html.template").extension()` is `Some("template")`, which `verdict_wording.rs`'s four-extension filter does not walk, so a frozen verdict sentence planted under that name leaves all nine tests **green** while the same sentence under the ruled name turns the scan **red**. **The name is the symptom; the guard is the defect** — with the directory holding only an unwalked template the scan reports 54 files, every one from `crates/antseal-cli/src`, page coverage exactly **zero**, and the anti-vacuity guard `sources.len() > 10` cannot fire now or ever because the CLI alone supplies 54; it is ruled **per-root** (CLI ≥ 40, verifier-web ≥ 1), so the class closes rather than the instance. **A second, opposite defect the survey did not name**: the residue assertion reads the **union** of both roots, so a page-side copy of a residue sentence **masks** its deletion from the CLI — measured red, then green, with the deletion unchanged — and is therefore scoped to the CLI root alone. The two file-walking tests want opposite domains and may not share one source list. Traceability gains `verifier-web` as a `CITATION_SCAN` root and `.html` as a suffix, measured **420 → 421 files with zero collateral**. The base64 is **ONE LINE**, ruled on **determinism** — wrap width is a free parameter entering the hashed script text and five plausible encoders split two ways on it — and not on the silent-skip footgun (real, 6.2× the prediction, and inert against `wording.rs`'s 131 literals) nor on runtime (`atob` accepts wrapped base64 in both engines, decoding to identical bytes). Answers and **closes D63 §10 (iv)**. Zero frozen bytes | RESOLVED (R23/R25 shipped) | 2026-08-12 |
| [D132](D132-the-pages-probe-plan.md) | R24 — how the verifier page learns what to probe: **a FIFTH MEMBER, `plan`, on the document D130's fifth export already returns**, not a sixth export; `verify_rendered` becomes five members (`plan`, `redaction`, `rendered`, `report`, `verdict`) and D18 §5 R4's list **stays at five**. **The register's framing is wrong in both directions**: the Bitcoin half was never the question — reachable three ways today, the cheapest of them returning every height — while the receipt half is reachable **zero** ways, because the transaction hash `eth_getTransactionReceipt` needs is in no document the page can see, proven at the value level rather than by grep. So the decision is about **one 32-byte value**, and every *"no new surface"* arm solved only the half that was already solved. The cost intuition is wrong too: a sixth export costs **146 bytes** and a decode-only `probe_plan` runs in **0.0199 ms, 1.2 % of a `verify()`**. What decides is that a decode-only export would hand a plan to a bundle that **failed verification**, while a member on `verify_rendered` cannot exist without a passing offline verdict — and D130 §7.1's own recorded risk, *"the surface is open by one, and the next request will be easier to make"*, comes due one wave later and is answered by refusing. **Two corrections a wave-19 brief proposed against this record were themselves measured WRONG and are refused rather than recorded**: §1 (g)'s *"Zero cases carry a receipt"* is **correct**, its own heading scoping it to the **R9 report corpus** (`supporting_evidence` is `"none"` in all 21, and `arbitrum-receipt` occurs zero times in the report vectors), so pointing at the F13 **bundle** fixture `every-anchor-kind-with-receipt` refutes a claim §1 never made; and **§3 R5's premise was accurate when written** — at HEAD `SiblingEncodeError`'s tuple field is genuinely private, and it reads `pub(crate)` today **because the implementing lane took R5's own first option**, so R5 was applied, not overtaken. The plan-value cost predicted **+11 368 B** measures **+10 377 B (+0.563 %)** by controlled A/B; the naive before/after comparison reads **−3 689 B** and is a trap, because the *"before"* build recompiled nothing — the same class as **R83**. **Residue named rather than closed (R85)**: the page pins Arbitrum One's chain id **42161** itself (a page-side constant citing `antseal_net::network::ARBITRUM_ONE_CHAIN_ID`; no §5 rule states the number, and the guard is §5 R4's, not R5's) and no document the page can see carries a network datum — so a **Sepolia**-sealed receipt is not merely unconfirmable: the pinned mainnet pair answers the chain-id guard correctly, `eth_getTransactionReceipt` then returns `null` from both, and the page renders the frozen **agreed-refutation** line *"both RPC endpoints agree the recorded transaction is not on chain"*. That is a **false refutation, not a fail-closed absence**, and it is the sharper form of the residue | RESOLVED (R24 shipped, with residue R85) | 2026-08-12 |
| [D133](D133-the-attested-anchor-fixture.md) | R84 — how the corpus gains a bundle that verifies OFFLINE to `attested`, so R27's four online cases can be written: **the fixture is MATERIALISED into `target/verifier-web-fixtures/`, never committed**, assembled from two documents already frozen in the tree. **The row's own problem statement is OVERTURNED at its root**: *"the four cases cannot be written at all, on either surface"* is false as measured — all four are already written and green in `crates/antseal-core/src/verify/orchestration/tests.rs` (native **and** `wasm32-unknown-unknown`) and again in `crates/antseal-cli/tests/verify_command.rs`, whose disagreement row drives **two real loopback `StubServer`s** through the production collector `probe_online`; what was missing is the **browser venue's fixture**, because `verifier-page-browser.mjs` receives bundles as **file paths** and no file on disk verified to `attested`. **The material is real, already committed and already frozen, and it binds by construction**: A25's calendar campaign stamped `083f87df…69df`, which **is** the `anchor_digest` of the F13 case `empty-anchor-unanchored`, so the merged-and-upgraded `.ots` inside the frozen `anchor` vector (`ots-upgraded-offline`, 3 808 B, expected state `attested`, upgrade group at real block **960767**) binds to that bundle's manifest without any digest being chosen. Measured end to end: rebuilt bundle **4 846 B**, `anchor[0] state = Attested`, `plan.blocks = [960767]`, `headline_eligible_count = 0`; both captured endpoint responses for 960767 are **byte-identical to the embedded header**. **The lean — build around the A25-bootstrap upgraded `.ots` — is REFUSED on a mechanism rather than on cost**: it stamps the digest of a **third-party crate's `LARGE_TEST` file**, and `anchor_digest` is SHA-256 over the bundle's own manifest envelope, so **no bundle can be built that it binds to**. The ruling is the venue of arm (e) with the material of arm (b), a combination the register's list did not contain. **ONE fixture serves all four cases** (they differ only in the mocked response); a second is emitted solely as the wide-versus-narrow witness. **No freeze event of any class**: `testdata/vectors/` is read, never written, and `vector-freeze.sh` must be green **without** `--update` — the check that proves the classification. Adding a case to `bundle/bundle.json` is refused as D105 §5.1's *"red gate with no key"*; editing `every-anchor-kind-*` is refused three times over; the receipt twin is **deferred to R85**, because its `tx_hash` target is exactly what R85 leaves open. Zero frozen bytes | RESOLVED (R84 shipped 2026-08-15; two of the record's own rulings corrected in the same act) | 2026-08-14 |
| [D134](D134-paper-key-locked-bundle-surfaces.md) | Paper-key locked-bundle surfaces — the transform pair, the header that shrinks, and what U32 actually has to reserve. **The reason D134 sits before U32 is NOT the reason the register recorded**: the register assumes a lock mode collides with the frozen CLI, but measured on the built binary a file carrying the proposed magic already yields a deterministic refusal (`exit=40`, `bundle: expected map, found byte string at byte 0 [cbor-unexpected-type]` — `'A'` is `0x41`, CBOR bstr(1)), and `machine.rs:56,64` place `message` in D65's **freely rewordable** tier, so the honest refusal costs **ZERO frozen surface**. What U32 owes is three **policy** reservations, not a surface. **(1) U1's shape OVERTURNED** — not a `reveal --lock` mode but a **vault-free transform pair**: `reveal` opens the vault and a lock mode would mutate its frozen default-output line, while the pair mutates nothing and keeps `verify` the single verdict authority, preserving the `40..=43` band. Keys never transit argv; `wrap`/`unwrap` are refused (D50 owns `--wrap`) and bare `unlock` is refused (it already means the *vault* unlock); **D51's four prompt classes do not fit — a recipient with no vault would abort as `passphrase-unavailable`, so a fifth class is required**. **(2) header — two of three overturned**: `label` becomes a bounded **uint card number**, not a `tstr` defaulting to `"card 1"`, which makes the round's own red-team finding (a plaintext label leaking recipient identity) **unrepresentable** rather than mitigated by rubric; **no padding**, on a cost table over ten real bundles (932–9 397 B) where C8's own formula gives +4.6 % for 6 classes, power-of-two +55.3 % for 4 and a 16 KiB floor +590 % for 1, and none closes the magnitude leak against unbounded bundle size; **no key commitment**, argued not inherited — there is no sanctioned derivation and equivocation gains an attacker nothing when the plaintext must be a fully verifying bundle. **(3) page key entry — shape ruled, authorisation DEFERRED**: widening an existing export is refused because `scripts/wasm-boundary.mjs:86-103` compares **names only**, so a signature change is invisible to the very guard policing the boundary; hence a sixth entry with D18 §5 R4 amended. A **new blocking question** the round never asked: may the unlock flow live in a document whose CSP is `connect-src https:` — **a paper key would be the first secret this page has ever held**. **(4) BIP39-24 RATIFIED on recomputed arithmetic** — 256 + 8 = 264 = 24 × 11 exactly, all 24 indices reproduced, wrap key measured at exactly 256 bits; 12 words would need expansion and a frozen-registry format event. Not verified and stated as such: **no wordlist exists in this repo or its 772-package lock**, so no word string and no wordlist hash is claimed. **Both probes the round's addendum pinned as D134's evidence are mis-specified**: probe 2 cannot discriminate its arms, and **probe 3 would have licensed a false promise** — a failed tag check MACs the ciphertext rather than decrypting it, so repair is Θ(192 × \|file\|), ~2 ms for the largest golden vector but **~19–64 s for a 100 MB reveal**, and its kilobyte benchmark samples only the safe end | RESOLVED | 2026-08-16 |
| [D135](D135-the-two-environment-build-comparison-and-its-tier.md) | R86 — what CI job enforces R25's *"two clean CI builds produce byte-identical artifacts"*, and at what tier, against a metered private-repo Actions budget: **DEPLOY-GATED — a third build inside `pages.yml`'s existing `publish` job**, invoked from `scripts/pages-publish.sh --build` and positioned so a red aborts before `actions/configure-pages`. Not a required context, not a `ci` job, not a dispatch lane. **The brief's own arm (b) is OVERTURNED at its premise**: a second build has been in `pages.yml` since `9317a35` and is worthless as reproducibility evidence — R83's `stale_guard` already builds twice and already fails naming both digests, measured in run `31873422229` at **0.81 s wall** with cargo's own `Finished in 0.14s`; not one crate recompiled. **The missing thing is a second ENVIRONMENT, not a second build**, so the two axes proven load-bearing — checkout path and `$CARGO_HOME` — must differ in content AND length, and R83's identical-environment rebuild must be KEPT or its red becomes ambiguous. **"Different runners" asks for the WEAKER property**: two hosted `ubuntu-latest` runners both compile from `/home/runner/work/antseal/antseal` under `/home/runner/.cargo`, holding both axes constant, so two environments on one runner is the stronger test and R25 Accept row 1 is re-worded on the record (§4 R3). **R86's own Accept row 3 names a plant that cannot work** — `check_build_paths` runs in `--build-only` too, so removing the remap reds upstream at the path scan and the comparison is never reached; §7 rules the plant that does, a differing `ANTSEAL_SOURCE_COMMIT`, already demonstrated by this repository's history (`08c074c` and `9317a35` produce modules of identical size 1 853 031 B with different digests). **The measurement nobody had taken: the whole-repository bill** — 2026-08-01→15, 538 dispatched jobs, per-job round-up, multipliers applied: **3 154 weighted minutes**, 105 % of a 3 000 allowance with 16 days left, `ci` alone 2 787 — which is what refuses every push-tier arm on a number rather than on plausibility. Price of the ruled arm: **≤ 7 weighted minutes per month, zero in a month with no deploy**. Residue named rather than hidden: nothing between deploys, one runner and one image, and the signed release path uncovered. The unlisted arm — a separate workflow with a workflow-level `on: push: paths:` filter, evaluated before any runner is assigned, 52 % hit rate over 33 real push ranges, ~81 weighted min/month — is **pre-priced for the promotion (Q238)** so that decision need not re-derive it | RESOLVED | 2026-08-15 |
| [D136](D136-the-page-browser-lane-without-playwright.md) | Q19 — what the page-verification lane actually is, now that playwright is refused: **one `workflow_dispatch`-only `browser` job driving raw CDP over `ubuntu-latest`'s stock chromium**, plus a provisioning step, a diagnostic artifact set and a corrected header, with `scripts/verifier-page-browser.sh` joining `scripts/local-gate.sh` behind an exit-2-as-SKIP wrapper. **The lane had NEVER run on the remote — zero runs — and could not have gone green as committed**: `verifier-page.yml` never installs `wasm-pack` or `wasm-bindgen-cli`, `ubuntu-latest` ships neither, and on a cold checkout it dies earlier still in the packaging self-test with an unhandled node `ENOENT` stack trace and no `::error::` annotation, after which `if: failure()` uploads **nothing**. Committed and entirely unwitnessed — the Q153/R82 class. **`ubuntu-latest` DOES carry `chromium` on `PATH`**, so the header's one environmental premise is correct and it is the only one that is. **Q19's `Do` has THREE false clauses, not one** — beyond playwright, the page loads from `file://` rather than "served from localhost", and the disagreement case uses CDP `Fetch.fulfillRequest` rather than "local stub endpoints"; `screenshot` survives via `Page.captureScreenshot` while `trace` is ruled dead with no CDP counterpart. **`workflow_dispatch`-only is CONFIRMED on measured cost** (83.7 weighted min per `ci` push, 30 runs since 08-01 projecting ~5 000/month) but **"unchanged" is OVERTURNED**, and the inversion that killed the `schedule:` arm is that **a dispatch-only lane is always a COLD lane** — first-ever `pages` run 8.30 min, the second nine hours later 1.72 min. **R25's Accept row 3 asks for a comparison that can never hold**: the footer renders `sha256(module)` and `SHA256SUMS` publishes `sha256(page)` — different objects, never equal — and the split is D129 §5 R7/R8's own ruling, so the **code is right and the row's wording and the visible label are wrong**; the label becomes `module build` and `MVP-SPEC.md` line 139's *"its own build hash"* is flagged as the source of the conflation. The hash clause becomes two assertions and one echo; the seed is an environment variable verified BY ITS MESSAGE, since `--self-test` is a positive control on one row and does not cover it. **R27 owns the ASSERTIONS, Q19 owns the VENUE, and neither may tick citing the other** | RESOLVED | 2026-08-15 |
| [D137](D137-what-the-page-may-say-about-an-unresolvable-receipt.md) | R85 — what the verifier page may say about a receipt whose chain it cannot establish: **arm (c′)**, the arm the row did not list. The two receipt-echo lines that assert *where* a transaction is become **chain-scoped from a `u64` the caller supplies**, and the machine-readable token changes in the same act (`NotOnChain` → `NotOnChain { chain_id }`), because `ReceiptEcho` has two fields and the row's arm (c) reaches only one. **Arm (a) is not "a format question to be routed" — it is a CLOSED one**: `docs/format/registry-v1.md` §7.6.1 lists a receipt `chain_id`/network name among six **checked absences** and `format_registry_freeze.rs` bans the three names by test, because a sealer-written chain id steers the verifier's RPC choice — so arm (a) would convert a false refutation into a **purchasable false confirmation**. **A different chain datum IS reachable**: both surfaces call `eth_chainId`, compare, and discard it three lines before the receipt query, and the guard *passing* is exactly what licenses naming the chain. **Arm (b)'s stated obstacle is misattributed** — D56 O7 is an OTS-anchor rule producing `AnchorState::Invalid`, while the receipt echo produces no state at all (`is_headline_eligible()` is hard-`false`), so there is no refutation strength to lose; (b) still fails, on information destruction. **A SECOND defective line was unrecorded**: `receipt_confirmed_line`'s network-blind "on Arbitrum" mis-scopes a *confirmation*, an error favouring the seal. **The frozen sentence was not scan-protected** — `receipt_not_on_chain_line()` was absent from `single_sourced()`, so a renderer could copy it and nothing reddened. **The CLI shares the defect**: its guard is network-parametric but the network comes from the operator's global `--network`, never from the bundle, so a third party verifying a Sepolia-sealed bundle with defaults prints the byte-identical false sentence. Both surfaces ruled together; the illegal state made unrepresentable rather than merely unwritten; three snapshot rows change and two Sepolia twins are added, because with one instance each an implementation that interpolated a constant would produce a byte-stable file forever. Reasoning recorded beside all three constants, in three crates, which is what forces the number-not-name form. The receipt-bearing fixture twin's `tx_hash` is taken whole from the frozen F13 case 5, so the emitter invents no bytes and, after this ruling, a hash pointing at nothing renders a **true** sentence | RESOLVED | 2026-08-15 |
| [D138](D138-ci-path-filter-and-cross-os-cadence.md) | Q239 — path-filtering docs-only pushes and re-cadencing the billed cross-OS arms, against an exhausted allowance: **`ci.yml`'s `push` gains `paths-ignore: [TODO.md, tasks/**, docs/ci-verification.md]` and `pull_request` stays unfiltered permanently**, because a workflow skipped by path filtering never reports and leaves a required context pending for ever. **The naive shape is OVERTURNED**: `traceability` and `secret-guard` had to MOVE to a new unfiltered `ci-always.yml`, since a path filter is workflow-level and `traceability`'s inputs — `TODO.md`, `tasks/*.md`, `docs/decisions/D*.md` — ARE the excluded files, making a docs push its busiest case, not its emptiest. **The SCOPE of "docs" is overturned on evidence**: `docs/**` and even `docs/decisions/**` are UNEXCLUDABLE, because `D18-wasm-bindgen-surface-location.md` lines **752** and **976** carry `wasm-bindgen-cli --version 0.2.126` and `scripts/wasm-toolchain-audit.sh:161-162` greps `docs` for that literal inside `wasm32-core-tests` — two lines of a decision record are live CI inputs — so the exclusion is **three entries, not a tree**. **`paths` and `paths-ignore` are NOT complements** (both quantify existentially over the changed set, so a mixed push fires both and duplicates every shared context), which kills the mirrored-workflow shape. `cross-os-macos` (13.2 weighted min, ×10) and `cross-os-windows` (9.7, ×2) move to a weekly `cross-os-extended.yml` that KEEPS `pull_request` so neither required context can hang a PR; the Linux arm (2.2) stays per-push, and the cost — up to seven days' blindness to OS-conditional divergence — is recorded in `local-gate.sh`'s header rather than buried. **A path filter is an assertion that cannot fail**, so `scripts/check-ci-paths.py` guards it with nine rules and eighteen arms each matching its own rule tag; its reader set is CLASSIFIED, not blindly closed, after a first version dragged `check-traceability.py` in through the `ci-lanes.sh` dispatcher and made every exclusion unprovable (D124 RULING 2 in a new suit), and its own `--self-test` caught it reporting GREEN over a real read of `MVP-SPEC.md` because both readers anchor on `CARGO_MANIFEST_DIR`. Saving **~2 450 weighted min/month** at the observed cadence of 64 pushes/month — the brief's ~1 375 assumes ~25 — taking the `ci`-push bill from ~5 360 to ~2 900 against 3 000. `concurrency` measured at **1 superseded run in 35** and refused. 19 contexts unchanged; `docs/ci-verification.md` and `CONTRIBUTING.md` owe a provenance update, and no lane here has yet run on the remote | RESOLVED | 2026-08-16 |
