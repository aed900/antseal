# Instrument-finding ledger

**Authority**: `TODO.md` protocol rule 8 (**D125**, ruled 2026-08-11). Append-only — a correction is a new entry naming the entry it corrects, never an edit.

**What belongs here**: any finding whose *subject is the verification apparatus itself* — checker/self-test defects, decision-document prose or arithmetic, stale locators, tracker-count drift, gate-instrument honesty. It takes **no task ID and no `TODO.md` row**.

**What does NOT belong here**: a *product* defect found *by* an instrument — the Q112 class (shipped code that fails to compile under a feature gate, a policy violated in code, a wrong byte on the wire). Those mint a row immediately, per protocol rule 2, exactly as before D125.

**The line to draw when unsure**: does fixing it change what a *user's* seal/verify does? Row. Does it change only what a *checker* says or how a document reads? Ledger.

**Promotion**: an entry becomes a task (protocol rule 2 — the registrar assigns the next free ID; lanes never self-number) only when it **blocks an acceptance on the ship path**, which is the Current-focus queue in `TODO.md`. Record the promotion as a new ledger entry pointing at the minted ID.

**Pre-D125 rows**: open instrument-class rows minted before 2026-08-11 stay in `TODO.md`, frozen as a class — they are NOT migrated here and NOT struck. This ledger starts empty and covers findings from wave 16 onward.

**Entry format** (one line each):

    - YYYY-MM-DD · <finder: lane / instrument / session> · <one-line finding> · <pointer: file / section / run id>

---

## Entries

- 2026-08-11 · D67 planning lane + R15 lane · `tasks/R.md` R15's Do/Deps prose predates D43's journal→cache reclassification ("local staged bytes or fetched ciphertext"; Deps "S: `get_data` when no local copy") — measured in-wave: no R15 network surface exists or is needed (fetch is R16's seam; sourcing is cache-first and degrades, never errors) · tasks/R.md R15 · D67 §9(i) · wave-16 R15 report
- 2026-08-11 · D67 planning lane · the snippet escape discipline D67 mints has no counterpart on the existing consent surfaces — seal consent prints titles and file paths with no control-character handling anywhere in `antseal-cli` (grep-measured); exposure is self-injection only (values are the user's own args); U31's copy catalog is the natural owner if ever promoted · docs/decisions/D67-disclosure-preview-snippet-format.md §4
- 2026-08-11 · D70 planning lane · executed-past phrasing on unstarted work: `tasks/R.md` R13 Notes ("implemented as automatic") and the open-decisions mirror row ("(implemented default)") read as executed acts while R13 was unstarted — the referent was R6's constructor default; superseded the same day by the dated D70 entry notes · tasks/R.md R13 Notes
- 2026-08-11 · D70 planning lane · `verify/file_stages.rs` records the "`raw_commit` opens to canonical bytes when no mirror exists" check as a decision candidate in a module doc only — no register row carries it, and the Q14 freeze forecloses it for v1 (adding the rejection fails bundles that verify today; the restore path checks the equivalent vault-side) · crates/antseal-core/src/verify/file_stages.rs · D70 §10(ii)
- 2026-08-11 · Q208 lane · `decision-index` has the same routine mid-act red window `task-entries` has: a record file landing before its registrar-applied index row (D119 RULING 6 splits the halves across actors) reddens every concurrent lane's flagless run — observed on D67/D70 this wave, diagnosed by mtime + git-status; one sentence at the check or in Q235's Notes would stop the next lane misdiagnosing it · scripts/check-traceability.py decision-index
- 2026-08-11 · Q208 lane · D122 §7.3's prescribed rule-4 paragraph asserts "Each gate is a task row", a universal the same-wave M1 disposition (no retrospective row, with reasons) falsifies permanently — landed rule 4 enumerates the four owners (Q14/Q236/Q237/Q34) instead, so no future reader should "correct" it back to the universal · TODO.md rule 4 · D122 §7.3
- 2026-08-11 · Q208 lane · Q231 (frozen class) is mostly discharged already: `under_review` → `gated_through` executed, the `--milestone` help and the :405 failure message corrected in wave 15; "under review" survives at three comment lines only — re-measure at any promotion; "nine places, ten occurrences" no longer describes the tree · scripts/check-traceability.py · TODO.md Q231
- 2026-08-11 · A25 lane · the typed anchor-client returns (`OtsSubmission`, `UpgradePoll`, `TsaCapture`, `TsaFailure`) discard the exact 2xx status and, for classified 404s / unverifiable TSA replies, the response bodies — capture logs honestly record `http=2xx` and `bytes=- sha256=-` for those classes (documented in the log headers and runbook §4); exact capture end-to-end would need widened typed returns or a response observer on `HttpClient` · scripts/anchor-smoke · crates/antseal-anchor
- 2026-08-11 · A25 lane · `fetch_and_agree` surfaces no per-request wire capture (by design — D55 §3 agreement is over extracted tuples) — must-agree rounds log the round outcome + extracted-tuple hash instead of four-field per-request records; runbook §4 documents the two notations · docs/anchors/real-smoke-runbook.md §4 · A16
- 2026-08-11 · A25 lane · `the_test_seam_has_no_production_call_sites`'s exemption mechanism is filename-based: a second `test-util`-gated bin would need its own manifest-pinned exemption or it reds the guard · crates/antseal-anchor/src/ots/engine/tests.rs
- 2026-08-11 · A25 lane · `check-anchor-net.py`'s `dependency_sections` treats a `[[bin]]` table (startswith `[[`) as body of the preceding section — benign as placed (after `[lints]`, with a manifest comment saying so), but a `[[bin]]` with `required-features = ["test-util"]` placed after a normal dependency table would false-positive R5 · scripts/check-anchor-net.py
- 2026-08-11 · R13 lane · R6's catalogue case `split-multi-unit/full-via-enumerated-units` is output-equivalent to `FullNoMirror` (R6's `Units(all indices)` never adds the mirror) while D70 ruling 1 defines a `--units` set completing a file as mirror-including product behavior — the case's name reads like the honest promotion shape but builds the fixture-only mirror-less one; R13's parity test normalizes the reference to `Full` and pins the normalized set to exactly that case · crates/antseal-core/src/test_util/bundle_fixtures.rs · R13 parity test
- 2026-08-11 · R15 lane (process) · mid-wave sibling cargo builds hold the build lock for minutes at a time, and a `head`-capped observation pipe on a long cargo run caps the evidence and can SIGPIPE the run — lanes verifying with `cargo test` should expect lock stalls and never truncate suite output they intend to cite · wave-16 R15 report
- 2026-08-11 · R14 lane · R6's in-module ancestor test (`no_disclosed_cover_seed_is_an_ancestor_of_an_unrevealed_leaf`) checks each cover span against ONE revealed range rather than the union of revealed ranges — equivalent today because per-unit covers are leaf-exact, but it would false-flag a legitimate union-straddling node if one ever became emittable; R14's independent checker uses union containment · crates/antseal-core/src/test_util/bundle_fixtures.rs ~2030
- 2026-08-11 · R14 lane · `Force`/`Tweak` are `#[non_exhaustive]`, so functional-record-update is unavailable outside the crate — external seam consumers construct via `Force::none()` + field assignment (documentation-grade; in-crate tests may keep FRU) · crates/antseal-core/src/builder/forcing.rs
- 2026-08-11 · D126 lane · `testdata/anchors/README.md`'s preamble counted campaigns as "Two of the three" while its own table lists four — stale since `A25-wave16-cycle` landed; the truthful count is three of the four (only `A16-A17-live/` lacks recorded consent). Corrected in the D126 closure act. The class: a provenance README states one fact in a preamble and again in a table, with nothing relating them, so appending a campaign rots the prose half silently · testdata/anchors/README.md
- 2026-08-11 · D126 lane · `tasks/A.md` §A48's `Deps` still reads "not before 2026-08-04", flagged stale by the 2026-08-07 A25 adjudication; the limits-doc half of that flag was executed (A106 ✅ 2026-08-10), the `Deps` half never was. A48 is closed, so this is historical residue on a closed row — recorded, no edit ordered · tasks/A.md A48
- 2026-08-11 · D127 lane · `check-traceability.py`'s `ACCEPTED_NON_COVERED` is **bump-atomic by construction**: an entry for a row not at a gated milestone reds on the unused-exemption arm, and a gated `gap` without an entry reds on the status arm — so the register's first entry and the M1 → M2 bump cannot land in different changes in either order, and closure (row → `covered` + entry removal) is forced into one change by the covered-but-registered arm. Nothing to fix; recorded so no later lane re-derives the sequencing or fears it · scripts/check-traceability.py ACCEPTED_NON_COVERED · D127 §1e
- 2026-08-11 · R16 lane · `BundleV1::revealed_unit_ids()` returns **wire-section order** (covered section, then non-covered), not globally ascending — found by R16's parity test comparing it against manifest-ordered previews; any consumer comparing it to a manifest-ordered set must sort first. Doc-gap on a production accessor, no behaviour change · crates/antseal-core bundle accessor
- 2026-08-11 · R16 lane · `ManifestBodyV1` is `Clone` only under `test-util`, so the `disclosure_preview.rs` test idiom `.body().clone()` compiles solely through dev-feature unification — a trap for production orchestration, which must keep the `Manifest` borrow alive instead. Noted for U27's lane · crates/antseal-cli/tests/disclosure_preview.rs
- 2026-08-11 · R17 close (session) · the R17 lane died mid-verification on an API credit exhaustion, leaving code + tests on disk and no report; the work was completed by measuring it rather than by trusting it — targeted suite, clippy, wasm32 lane and a rustdoc attribution count, which is how the `>=` defect (now U-less, fixed in place and recorded on R17's row) was found still live. The transferable half: an interrupted lane's tree state is evidence of nothing until it is run · TODO.md R17
- 2026-08-11 · R18 lane · the verdict-wording snapshot (`crates/antseal-core/tests/snapshots/verdict-wording.txt` vs `render_document()`) makes any change to any verdict string a red — the freeze is now mechanical rather than a convention. Red-capable, demonstrated by a planted one-character drift; `ANTSEAL_BLESS=1` regenerates, matching `cli-surface.help.txt`'s pattern with **no snapshot dependency added** · crates/antseal-core/tests/verdict_wording.rs
- 2026-08-11 · R18 lane · `every_anchor_state_has_a_snapshotted_row_and_a_slot_block` is a **wildcard-free match over `AnchorState`**, so an eighth state fails the build rather than silently lacking a row, and a deleted snapshot row fails the test by name (demonstrated red). This is D64's proposed exhaustive-rendering guard, landed at the state level · crates/antseal-core/tests/verdict_wording.rs
- 2026-08-11 · R18 lane · `no_renderer_source_spells_a_frozen_verdict_string` scans `antseal-cli/src/**` + `verifier-web/**` for 21 needles, so "shared verbatim by CLI and page" is asserted rather than assumed — paired with `every_recorded_residue_is_still_there`, which pins the six declared-residue sentences **present** so the exclusion list cannot rot into silence. Both proven fallible on planted corpora; the scan control also proves it catches code literals and ignores prose mentions · crates/antseal-core/tests/verdict_wording.rs
- 2026-08-11 · R18 lane · test files are excluded from the source scan **by ruling, recorded in the test header**: a test naming a frozen string is an independent pin, and rewriting those pins to read the table would make them tautological. Recorded so no later lane "fixes" the exclusion and deletes the independence · crates/antseal-core/tests/verdict_wording.rs
- 2026-08-11 · R18 close (session) · `wording::slot_name` is 1-based `kind-ordinal` (`ots-1`) while shipped `status` uses vault artifact names (`ots-pending`, `tsa-0`) — a **third** surface, not a divergence to repair; R21 must use `slot_name` for the offline block and the overlay together or D64 §6.2's alignment breaks. Recorded before R21 starts, since the alignment is invisible until both halves render · crates/antseal-core/src/verify/wording.rs · D64 §6.2
- 2026-08-12 · D62 planning lane · `tasks/R.md:673` states the canonical-URL constant is *"consumed by R16/R25"* and `docs/waves/wave-18-brief.md:13` inherited it as *"the constant R16/U28/Q consume"* — R16's own `Do` disclaims it (*"owned by R26 and printed by U28"*, and its opening clause is *"this task ships no CLI surface"*) and R25's Do/Accept never mention the URL; the real set is R26 owning, U28 the only Rust consumer, Q22/Q28 the docs. Both corrected in the D62 registration act · tasks/R.md open decisions · docs/waves/wave-18-brief.md:13 · D62 §10 (ii)
- 2026-08-12 · D62 planning lane · `TODO.md`'s maintainer-actions item 5 gave branch protection's blocked cause as *"403 on a private repo on **GitHub Free**"* while item 4 of the same line says the account is on Pro and `docs/ci-verification.md:1978` records the upgrade — the stale cause reads as a **Pages blocker**, which is what made it worth correcting rather than leaving; the blocked *status* is unmeasured since the upgrade and is not ruled here · TODO.md maintainer actions item 5 · D62 §1 (j), §10 (iii)
- 2026-08-12 · D62 planning lane · the project can seal its own verifier page, and it is the one move that breaks the shared-trust-domain dependency in which the page's bytes and the hash attesting them are published by the same party — anchoring `SHA256SUMS` itself via A13/A14 + TSA (exercised for real 2026-08-11) plus a DNS TXT record at a different party costs zero new cryptography and makes the product its own first customer; natural owners D63 (what the sum covers) and Q31 (release pipeline). Described, not registered · D62 §10 (i)
- 2026-08-12 · D62 planning lane · `verifier-web/index.html` carries no CSP meta — correct today (a 16-line placeholder with no script, and the tree carries **zero** verifier-URL literals, so R26's grep test starts from a clean slate) and recorded only so R23 lands the meta-CSP **with** the page rather than after it, when adding it would move a hashed artifact · verifier-web/index.html · D62 §10 (v)
- 2026-08-12 · D63 planning lane · `tasks/F.md:399`'s Discovered-by paragraph and `TODO.md`'s F29 row both assert *"the root `Cargo.toml` declares no `[profile.*]`"*, false since `68d2fa4` (2026-08-01, U6) landed ten `[profile.dev.package.*]` stanzas at `opt-level = 3` — the `--release`-in-CI half still holds, and two consequences follow: the wasm32 bit-match already runs the hot crypto at opt-level 3 so F29's codegen gap is **narrower** than the row says, and its *"order of magnitude"* timing estimate predates the overrides and is stale in the same direction · tasks/F.md F29 · TODO.md F29 row · D63 §10 (i)
- 2026-08-12 · D63 planning lane · R25's Accept row *"Footer shows the build hash matching the published sum, plus the advice string (snapshot/playwright-asserted)"* asserts a snapshot that **does not exist** — measured, no snapshot anywhere in the tree contains the advice string, and the committed R18 wording snapshot has zero hits for it; D63 §5 R6 tells R25 to create the pin, recorded here so the Accept row is not read as describing something already in place · tasks/R.md R25 · D63 §10 (v)
- 2026-08-12 · D63 planning lane · `verifier-web/` has no lane, no workspace membership and no scan root of its own — not in `Cargo.toml`'s `members`, in no workflow, and its only live reference is as a **ban** scan root in `verdict_wording.rs:710`, which asserts the page does not re-spell frozen table rows and asserts nothing about strings the table does not contain; when R23/R25 land, the injector tests and the advice-line drift test need a home. Adjacent to Q210's three-directories-without-a-scan-root observation · verifier-web/ · D63 §10 (iv)
- 2026-08-12 · D65 planning lane · the `--json` fixture registry cannot see two commands' result shapes, and they are the only two: `machine_mode.rs`'s `render_fixture` renders eight of ten documents **through the real producers** (with comments stating the rule three times), while `vault export`/`vault import` are hand-written `json!` literals duplicating `commands.rs` — measured, four of their eight keys (`file`, `bytes`, `vault_dir`, `config_restored`) are asserted only against the copy, so renaming any of them in the handler moves no test · crates/antseal-cli/tests/machine_mode.rs:415-431 · D65 §13 (i)
- 2026-08-12 · D65 planning lane · tier C's per-document presence enforcer exists for `restore` and `list` and for nobody else — `status`, `seal` (three documents), `init`, `vault export` and `vault import` have no key assertion beyond the blessable byte snapshot, so tier C's honesty rests on a reviewer reading a snapshot diff; `verify`'s half rides U30's Accept and `seal`'s is U50's, and the rest is apparatus with no ship-path acceptance blocked by it · crates/antseal-cli/tests/{restore_output.rs:550,list_command.rs:361} · D65 §13 (ii)
- 2026-08-12 · D65 planning lane · the R76/R77 class (*a doc stating an imperative its named consumer cannot honour*) is not confined to `report.rs`: `docs/testing/error-code-contract.md:19` says the verifier's `code()` is what *"`--json` output carries"* while envelope v1's error object is exactly `class`/`exit_code`/`message` and `namespace_disjointness.rs` forbids the workaround, and `machine.rs:33-34` calls the committed fixture *"the schema-registry fixture the harness enforces"* when the harness enforces the envelope, not the schema — four instances across three files and two layers, same one-line fix at each · D65 §13 (iii)
- 2026-08-12 · D68 planning lane · `RevealError::EmptySelection`'s doc describes a CLI path that cannot exist — *"`--units` was given with no ids"* is unreachable from the binary because clap rejects every empty spelling first (`--units` bare, `''`, `3,,5`, `3,5,` all die at exit 2); the variant is correct as a library-API defensive arm, since R16 is a library API with non-CLI callers by design, so this is doc-prose accuracy only and no Accept row should be spent exercising it · crates/antseal-cli/src/pipeline/reveal.rs:272-275 · D68 §9 (ii)
- 2026-08-12 · D68 planning lane · the `.sealproof` extension has 210 uses across the tree and **no constant**, while `.sealvault` has `EXPORT_FILE_EXTENSION` — D68 §3 R10 mints `BUNDLE_FILE_EXTENSION` as part of U28, and the pre-existing literals in `crates/antseal-cli/tests/*.rs`, `crates/antseal-core/tests/*.rs` and `machine.rs:287` are **not** required to migrate by that ruling; recorded so a future tidy-up starts from a list rather than a search · crates/antseal-cli/src/vault/export.rs:185 · D68 §9 (iii)
- 2026-08-12 · D69 planning lane · `tests/exit_codes.rs`'s `codes_are_nonzero_distinct_and_avoid_reserved_ranges` is a correct guard that will fail for the right reason at the wrong time: it asserts `!(40..=49).contains(&code)` for **every** class, so the first correct implementation of D69 turns it red and the amendment (narrow to 44–49) must ride the same change or the failure reads as a mystery · crates/antseal-cli/tests/exit_codes.rs:114-135 · D69 §9 (ii)
- 2026-08-12 · D69 planning lane · `reveal-inputs-unusable` sits one letter from the code universe's existing `revealed-` stem (`revealed-unit-file-not-touched`) — not a collision, since exit classes and rejection codes are ruled disjoint namespaces, but the fourth near-miss of the `path-commit-mismatch` kind that `error-code-contract.md` tracks and the second *across* namespaces; Q77 is the row that would machine-check it · testdata/error-codes/v1/CODES.txt · D69 §9 (iii)
- 2026-08-12 · D69 planning lane · U30's Accept row 3 (*"`--live` without network → clean error"*) and D69 §3 R6 are reconcilable but not identically worded — "clean error" means *the message is clean and the verdict is unaffected*, never *the exit code is the live layer's*; the reading is now explicit in U30's entry so a later implementer does not re-derive the opposite. Not a defect; a wording sharpening · tasks/U.md U30 · D69 §9 (iv)
- 2026-08-12 · wave-18 registrar · `tasks/R.md`'s `Open decisions (R)` bullets for the online-overlay layout and the disclosure-preview snippet still read as open, though both resolved 2026-08-11 (D64, D67) and both have register rows and index rows — the footer list has no mechanism relating it to `TODO.md`'s register, so a resolution lands in three homes and misses the fourth silently; found while landing four resolution pointers into the same list · tasks/R.md Open decisions (R)
- 2026-08-12 · R19 lane · `verdict_wording.rs`'s header claims it *"renders every row of `antseal_core::verify::wording`"* and **nothing enforces that claim** — the document builder is hand-written, so a new `pub` row can ship on the frozen table, be reachable by both renderers, and never enter the snapshot, leaving it unfrozen while the file asserts the opposite; measured and recorded in the test header rather than closed, since a source-scan over `wording.rs`'s `pub fn`/`pub const` names is instrument work R19 does not own. Not currently realised: R19's nine rows are all in the document · crates/antseal-core/tests/verdict_wording.rs · found while adding rows to the frozen table
- 2026-08-12 · R19 lane · D67 §9 (ii)'s standing observation (*"the escape discipline minted here has no counterpart on existing consent surfaces"*) now has a cheaper remedy than when written: R19 promoted the escape set to `pub fn escape_for_terminal` in the same crate, so `seal_consent.rs`/`seal_warnings.rs`/`listing.rs` could adopt it one line each. Still self-injection-only exposure, so still an observation and not a defect — but the reason not to do it has shrunk, and U31's copy-catalog lane is the natural owner · crates/antseal-cli/src/preview.rs · D67 §9 (ii), re-measured
- 2026-08-12 · R20 lane · **`scripts/wasm-test-runner.mjs:119` cannot report which wasm32 test failed** — it does `Buffer.from(new Uint8Array(memory.buffer)).toString("latin1")` over the *whole* linear memory, which the real module grows from 2,949,120 B to **1,363,542,016 B** during `main()`, against node's max string of 536,870,888 B; so any failing `antseal-core` wasm32 test yields `Error: Cannot create a string longer than 0x1fffffe8 characters` instead of R41's promised `::error::wasm32 test runner: test '…' FAILED`. The lane still exits 1, so it is **red for the wrong reason and a reader learns nothing** — the "nonzero is not the message" failure mode exactly. **`--self-test` is structurally unable to see it**: its throwaway crate's memory is a few MiB, and it passed while the real lane was crashing. Fix measured at one function — 64 MiB windows with an 8 KiB overlap recovered the test name and the full panic record · scripts/wasm-test-runner.mjs:119 · found by using the runner as a witness, not by reading it
- 2026-08-12 · R20 lane · `bundle_fixtures::fixture_address`'s rustdoc premise — *"Fixtures never fetch, so the address only has to be a well-formed 32-byte value"* — is falsified by R20, which recomputes addresses **offline** with no fetch involved; corrected in place with `StorageAddresses::Real` named as the mode for a bundle that links. Related and deliberately not corrected (another test's prose to own): `verify_fuzz.rs`'s `the_unauthenticated_region_at_m0_is_exactly_the_storage_record` still passes and its assertion is still true (it compares `Outcome` only), but after R20 those 88 bytes are storage-linkage-bearing while remaining verdict-free, so "unauthenticated" now needs the qualifier the test body already implies · crates/antseal-core/src/test_util/bundle_fixtures.rs · found while building the first bundle in the tree whose linkage can pass
- 2026-08-12 · R20 lane · R18 froze **one** storage-linkage failure row, taking `(mismatch_count, checked_count)`, so a bent *unit* address and a bent *manifest* address render the byte-identical CLI sentence although they are distinct values in the report; the distinction survives in the report bytes for `--json` and the page. Reported rather than resolved — R18 authored those rows the day before with R20 in view, and moving a frozen snapshot is not a lane's call; the equality is asserted explicitly so it is documented rather than discovered · crates/antseal-core/src/verify/wording.rs · R20 Accept work
- 2026-08-12 · U27 lane · `tasks/U.md` U27's `Do` carries a rung that cannot exist — *"silent refetch fallback on mismatch"* — inherited from R16's Notes where it is correct; `show` has no S dependency and holds no backend, and D67 §1 (j) rules the ladder is exactly cache-first → current-file-marked-may-differ → absent. Implemented as D67's three rungs with the divergence documented in `show.rs`'s module doc; the entry's parenthetical wants a correction so the next reader does not go looking for a fetch · tasks/U.md U27 · found by implementing the rung
- 2026-08-12 · U27 lane · `--title` is echoed unescaped by `list`, `status` and now `show`, while only paths route through `escape_for_terminal`; self-injection-only (the user's own argument, D67 §9 (ii)'s distinction) and **uniform across three commands**, so fixing it in one would manufacture the very divergence U27 declined to add elsewhere. U31's catalog work is the owner · crates/antseal-cli/src/{listing,status,show}.rs · observed while routing paths
- 2026-08-12 · U27 lane · the stub-exemplar rotation moved for the sixth time and now points at `verify` in all four suites that carry it (`cli_surface.rs`, `exit_codes.rs`, `config_file.rs`, `upgrade_hook.rs` — `config_file.rs` had literally written *"`show` is the next stub; when U27 lands, move this to whichever one is left and say so here"*). **`config_file.rs` runs out of stubs at U30**, so the last M3 CLI lane inherits a suite whose premise expires with it · crates/antseal-cli/tests/config_file.rs · handed forward to U28/U30
- 2026-08-12 · D18 planning lane · **`core-dep-graph` does not guard what its own violation message claims.** It runs on **default features** (`scripts/ci-lanes.sh:314`), so a feature-gated dependency edge is invisible to it — proven on today's tree with `proptest`: 84 names, 0 hits by default, 7 hits under `--features test-util` — while the check's failure text asserts the set *"ships as the verifier page WASM"*. D18 routed around it (the wasm edge is target-gated in a separate crate, so the check is untouched by construction), but the blind spot stands · scripts/ci-lanes.sh:314 · measured while pricing the feature-gated arm
- 2026-08-12 · D18 planning lane · `tasks/A.md:270` and `tasks/Q.md:57` still instruct *"wasm-bindgen-test (or equivalent)"*; both predate P14's deviation, both are satisfied by the harness that exists, and D18 §5 R10 now **forbids** adopting the named tool, because `.cargo/config.toml`'s `runner` key is per *target*, there is one `[target.wasm32-unknown-unknown]` table, and `wasm32-core-tests` owns it — a live prohibition sitting behind stale prose · tasks/A.md:270, tasks/Q.md:57 · D18 §10
- 2026-08-12 · D18 planning lane · `tasks/P.md:166` (P14's `Do`) still says *"Install and exact-pin wasm-pack and wasm-bindgen-cli … and wire wasm-bindgen-test"*; the execution deviated on all three and `TODO.md`'s P14 row records why, but the row's own `Do` was never annotated, so a reader arriving at P14 today is told to do three things the project decided not to do · tasks/P.md:166 · D18 §10
- 2026-08-12 · D18 planning lane · `.github/workflows/ci.yml:211-214`'s comment carries the overclaim `scripts/ci-lanes.sh:202-234` explicitly retired (*"must never gain an async runtime, network, or I/O crate"*) — harmless today, misleading the moment a second dep-graph rule lands under the same job name · .github/workflows/ci.yml:211-214 · D18 §10
- 2026-08-12 · D18 planning lane · **line-number citations into `crates/` minted during a wave with implementation lanes running are a coin flip** — D63 §1 (g) cites `crates/antseal-core/tests/verdict_wording.rs:710` for the `verifier-web` scan root; that file was `+102` lines dirty at the time and the entry now sits at `:812`. Offered as a caution, not a defect: the house already bans line-number citation into the frozen registry, and this is the same hazard one directory over · docs/decisions/D63-footer-build-hash-and-artifact-set.md §1 (g) · D18 §10
- 2026-08-12 · D128 planning lane · the `report` vector kind's `inputs` block declares **no verification input and cannot be made to** — it names `app_version`/`cases`/`seal_id`/`seed`/`w`, all bundle-construction, while its `expect` is equally a function of `verify_at_unix`, `tsa_roots` and (since R20) `storage_linkage`, so a second implementation could not reproduce these vectors from the document alone; and the declaration cannot move into the document, because adding an `inputs` key moves the digest with **no case moved**, which `verdict_event_ok` refuses at `scripts/vector-freeze.sh:149-150` (*"no case moved, so the digest changed for some reason this check cannot see"*). Predates this decision by **two milestones**. Contrast, measured on the sibling: A22's `anchor` kind **does** declare `verify_at_unix` in its own `inputs`, because a verify option determines its pinned bytes — D128 §3 R3 names the tuple in the *executor* instead, the strongest form available under the freeze · testdata/vectors/v1/report/verification-reports.json · D128 §1.4, §10 (i)
- 2026-08-12 · D128 planning lane · `verdict_event_ok`'s *"all cases moved ⇒ FORMAT EVENT"* step (`scripts/vector-freeze.sh:151-153`) is a **sufficient-condition heuristic doing a necessary condition's job** — it affirms the consequent on a warrant of one observation (R32 at `report_version` 0 → 1), so a legitimate verdict event that re-values every case is refused by cardinality alone. **Left standing deliberately** and recorded as a known limit rather than repaired: it is the freeze instrument's only defence against an *undisciplined* format event (a field added to a frozen struct without D29 rule 1's bump leaves `report_version` unmoved, moves all 21 cases, and is caught by this check and nothing else inside the instrument), and unlike the source-side pins it cannot be re-blessed by the same hand in the same commit as the change it guards. Q107 owns the freeze-vocabulary table and per D105 §6 is already short one class; this is a second · scripts/vector-freeze.sh · D128 §1.10, §2.2, §10 (ii)
- 2026-08-12 · D128 planning lane · `docs/decisions/D105-post-freeze-report-value-additions.md` §1.5 and §5.2 both cite `scripts/wasm-tests.sh:48` for the wasm32 `--lib` route, and the citation is stale — measured on the committed script, `:48` is the toolchain-read guard, the route is `cmd_check` (opens `:50`) and its `cargo test -p antseal-core --lib --target wasm32-unknown-unknown` is `:52`; D128 §10 (vi) names `:51`, which is the `note` line one above the command, so the record's own replacement pointer is one line high. **Reported, not edited** — D105 is a resolved record, the same disposition D105 §7 itself gave the three stale citations it found · docs/decisions/D105-post-freeze-report-value-additions.md §1.5, §5.2 · D128 §10 (vi)
- 2026-08-12 · D128 planning lane · **D105 §5.5 is still open, and now has a second instance**: must every report v1 value eventually live in a **retained** `testdata/` artifact? If it is ever ruled yes, `StorageLinkageResult::Evaluated` joins `SupportingEvidenceResult::ArbitrumReceipt` as a report v1 value pinned only in source, and the route for both is a new `storage-linkage` vector kind through the sanctioned append path (S4 2026-08-01, A22 2026-08-09 — an append moves no existing digest). D128 §2.4 refuses that kind **for now** on "nothing requires it" (R20's Accept asks for no vector, D105's R-VAL is discharged, wasm32 parity comes by A90's `--lib` route) and records the route rather than pre-empting the question — flagged here because answering it is **a decision that will need an id**, and it is owned by nobody today · docs/decisions/D105-post-freeze-report-value-additions.md §5.5 · D128 §2.4, §10 (iii)
- 2026-08-12 · R21 lane · **the wasm32 test binary's linear memory has grown 770×** — `docs/ci-verification.md:552` records `memory: 1769472 B`; the current tree reports `1363607552 B` (1.36 GB). The lane attributed **its own** share by gating its two test modules off and rebuilding: **exactly 65,536 B, one wasm page**, so the remaining 1.36 GB pre-dates R21 — and that attribution is independently corroborated by this ledger, since the R20 lane's entry above measured `1,363,542,016 B` and the difference is **exactly** that one page. Still under wasm32's 4 GiB ceiling and nothing fails on it, but a recorded figure now three orders of magnitude stale, and it is the **root cause of the `wasm-test-runner.mjs` defect already on this ledger** — that runner stringifies the *whole* linear memory against node's 536,870,888-byte string limit, so the growth is what turns a named test failure into `Cannot create a string longer than…`. Further attribution (which wave grew it, and by how much) needs git operations the lane's rules forbid · docs/ci-verification.md:552 · scripts/wasm-test-runner.mjs:119
- 2026-08-12 · R21 lane · **the anchor stage now runs twice per offline verify and three times under `--online`** — `verify_bundle`'s D27-frozen signature returns only the report, while R17's aggregate needs the **verdicts** (kind and source, not just time — D64 §9), so the orchestration evaluates the anchors a second time and the online path a third; **TSA chain validation is the expensive half**. A `verify_bundle_with_verdicts` variant would remove the duplication but changes a frozen signature, so it is a decision and not a lane's call. Mitigated meanwhile by a private `AnchorInputs` making both evaluations share one digest derivation, so they cannot drift even while they duplicate. Cost stated rather than hidden · crates/antseal-core/src/verify/orchestration.rs · D27 · D64 §9
- 2026-08-12 · R21 lane · **`attested` renders no block-H row, and cannot from the report alone** — MVP-SPEC line 108's sentence names block H, but the height lives on the artifact's upgrade group, not on `AnchorResult`, which is precisely why `build_online_overlay` takes `artifacts` separately. Taking artifacts into `RenderedVerdict` would make the offline block underivable from report bytes and break the R22/R27 parity story, so the omission is the cheaper of two wrongs. R18's `RESIDUE` list already carries the open question **verbatim** and pins it *present*, so R21 rendered nothing rather than render it inconsistently — no new residue was created, and the existing one now has a second witness · MVP-SPEC.md line 108 · crates/antseal-core/tests/verdict_wording.rs `RESIDUE` · R18
- 2026-08-12 · U28 lane · **`tasks/R.md` R16's `Deps` states the upgrade-hook ordering backwards** — it read *"the opportunistic upgrade hook (A15) is invoked by U's command dispatch **before** this API runs"*, while the shipped hook is `upgrade_hook::run_after_output`, called from exactly one place — `main_entry` — **after** dispatch, per **D99 R1** (*"after the answer, never before it"*). The product consequence is by design but is worth recording where someone will actually look for it, which is the task entry and not the hook's own doc: **a `reveal` never benefits from the upgrade its own invocation triggers — the next one does**, so a commitment that matured since the last run is embedded at its pre-upgrade capture state and the freshening lands behind the output. D68 §1 (j)'s argument is unaffected either way. **Corrected in place** as part of this entry, with the previous wording quoted on the line so the change is visible rather than silent · tasks/R.md R16 `Deps` · crates/antseal-cli/src/lib.rs:164 · D99 R1
- 2026-08-12 · U28 lane · **`backend::unavailable`'s `ant-backend` arm names the wrong task** — *"connecting it to this command's argument handling lands with U13"* — which is already false for `restore`, landed by **U20**, and will be false a second way once `reveal`'s dispatch flips at U29. Pre-existing, and reachable **only in an `ant-backend` build**, so no committed fixture carries the sentence and correcting it moves no snapshot: the fix is one literal, and its owner is whichever lane next has that arm open. Recorded rather than taken, because U28 owned no `ant-backend` compile this wave and a literal edited without its lane's build is an unverified edit · crates/antseal-cli/src/backend.rs:113
- 2026-08-12 · U28 lane · **a new form of the shared-build-lock hazard: `TaskStop` stops the task *wrapper*, not the compiler that task spawned.** A stopped lane's `cargo test` kept running and held the cargo build lock for **~15 minutes**, stalling the other lanes, until its PID was killed directly. The standing "parallel lanes share one working tree" rule covers `git` conflicts and multi-minute lock stalls, but it does not cover **orphaned children of a stopped task** — and that is the worst version of the case, because once the wrapper is gone there is no report to read, no exit to wait for and nothing that will ever announce the process, so the stall presents as an unexplained slowdown in lanes that did nothing wrong. The protocol gap is on the *stopping* side: a stop must be followed by a process check, never assumed to have taken the build with it · wave protocol · sibling of the "parallel lanes share one working tree" rule
- 2026-08-12 · U28 lane + wave-18 registrar · **a lane's account of *another* lane's tree state is a snapshot of a moving target, and must be re-measured rather than arbitrated.** U28 reported `envelope_fixtures_match_the_committed_snapshot` **red** in the shared tree before it began work, while U27 had reported the whole crate **green**; registrar-side direct measurement after both had landed showed `machine_mode` **green, 7 passed** — so the discrepancy **resolved** rather than needing a verdict on which lane was right, and both reports can have been true of the tree at the moments they were taken. The general rule is the entry: in a concurrent wave the only admissible answer to "is that other lane's suite green?" is a fresh run, and a registrar who adjudicates between two such reports is deciding a question that no longer has a fixed subject · crates/antseal-cli/tests/machine_mode.rs · the "interrupted lane's tree is not evidence" rule, one step weaker
- 2026-08-12 · D128-implementation lane · **`tests/doc_pointer_liveness.rs` recognises no pointer written with an ELIDED PREFIX, which makes an abbreviated citation unfalsifiable.** The checker worked as designed during this lane — it caught two renamed tests still cited by their old names, *"2 dangling doc pointer(s) — each reads as evidence and is not"* — but `normalise` requires `token.starts_with(|c: char| c.is_ascii_lowercase())`, so a backtick span written `` `..._has_exactly_two_callers` `` (or with a leading `…`, or a leading `_`) is rejected as **not a pointer at all** rather than checked and found dangling, and passes in silence. The asymmetry is the sharp half: **trailing** elision is a first-class supported form — a `*` suffix sets `glob` and resolves by prefix — while **leading** elision is invisible, and leading is the natural way to shorten a long test name in prose. So an abbreviated citation reads as evidence and is never checked · crates/antseal-core/tests/doc_pointer_liveness.rs `normalise` · found while repairing the pointers the checker had just caught
- 2026-08-12 · D128-implementation lane (sites re-measured by the wave-18 registrar) · **`scripts/vector-freeze.sh`'s `unchanged` verdict exists only in the `--update` branch**, so a brief or a record that asks for it from a *flagless* run asks for a string that cannot appear: `${version}: unchanged` is printed only where `--update` compares its regenerated manifest against the committed one, while the flagless lane's verdict is `vector-freeze: GREEN` plus `v1: N frozen vector(s) OK (independent … check)`. **D128 §8's gate list names this as D105's recorded slip and tells the lane to run flagless and expect `GREEN` — and D128 §3 R3, five sections earlier in the same record, demands *"`scripts/vector-freeze.sh` must report `v1: unchanged`"* anyway**, which is D105's own gate sentence (*"must report `unchanged`* — if it does not, kill criterion 4 has fired") carried forward with a version prefix added. R20's TODO.md result line carried the same string until this act rewrote it. The one correct use in the tree names its branch: `docs/ci-verification.md` records `v1: unchanged` under `./scripts/vector-freeze.sh --update`. Recorded because the slip has now propagated twice out of one wrong sentence, and one of the two lives inside the record whose own gate list warns about it · scripts/vector-freeze.sh `--update` branch · D128 §3 R3 vs §8 · D105 gate list
- 2026-08-12 · U29 lane · **D51's stdout/stderr routing now has FOUR unshared implementations, and the fourth was added by the lane that reported it.** `commands::Ui::line` (`commands.rs:48`), `SealConsent::emit` (`seal_consent.rs:423`), `seal_run`'s emitter (`seal_run.rs:588`, behind its own `to_stderr` field at `:576`) and now `DisclosureConsent::emit` (`reveal_consent.rs:418`) each hold their own `if to_stderr { eprintln } else { println }` and each independently decides where human copy goes under `--json`. D51's machine-mode matrix is the single rule all four are meant to obey — *human copy to stderr under `--json` so stdout carries exactly one document* — so this is four copies of one routing rule and four chances to diverge, in the exact shape D100 §1.2 names: each site documented as obviously right, none citing the others. Nothing is wrong today (all four agree, and `machine_mode.rs`'s real-binary harness catches a stdout violation end to end), which is why this is a ledger line and not a row: fixing it changes no byte a user reads. The natural owner is **U31**'s copy-catalogue row, which already owns the "one production per user-visible thing" work at the copy layer; the remedy is one shared emitter taking the bit, not a fifth site with a better comment · crates/antseal-cli/src/{commands.rs:48, seal_consent.rs:423, seal_run.rs:588, reveal_consent.rs:418} · D51 · sibling of U70 (four spellings of one work state) at the routing layer instead of the wording layer
- 2026-08-12 · U30 lane · **D69 §9 (ii) under-counted its own blast radius, and only one of the five was greppable.** The record predicted exactly **one** instrument would redden — `tests/exit_codes.rs`'s reserved-band guard, which asserts `!(40..=49).contains(&code)` for every class — and was right about it: the amendment rode the same change, narrowing the band to **44–49** and adding a `verify-`-naming assertion on **40–43**, so the failure never read as a mystery. **Four more broke on the same edit and none of the four was predicted**: `namespace_disjointness.rs`'s `PINNED_CLASS_COUNT` (**29 → 33**, whose doc now records that the class table is no longer the same thing as the error-variant table — three of the four new classes are severity rungs of a run that *succeeded*, so a reader counting `CliError` variants would get 30), plus the three stub-exemplar sites (`config_file.rs`, `cli_surface.rs`, `machine_mode.rs`), whose subject was `verify`-as-a-stub and expired the moment `verify` shipped. **This is the "gate finds what analysis misses" shape, in its sharpest form yet**: the one instrument the record foresaw is the one that spells the constant the record named, and the other four are related to the change by *meaning* rather than by text — no grep over `40..=49` reaches a count of classes or a suite's choice of exemplar, and only running them did · crates/antseal-cli/tests/{exit_codes.rs, namespace_disjointness.rs, config_file.rs, cli_surface.rs, machine_mode.rs} · D69 §9 (ii)
- 2026-08-12 · U30 lane · **a decision's *reservation* has become an executable check for the second time, which is what makes it worth naming as a pattern rather than recording as an event.** `namespace_disjointness.rs::the_reserved_verify_stem_never_appears_in_the_code_universe` enforces **D69 §3 R8**'s `verify-` reservation against the **248-code** committed universe — no rejection code may take the stem D69 gave to verify's exit classes — mirroring §2's `anchor-gate-abort` reservation **in the other direction** (a class name that must never become a code, versus a stem no code may take). It carries a non-vacuity assertion that exactly **four** classes own the stem, so the guard cannot pass by having no subject, and its failure text sends a future R or A lane to `docs/testing/error-code-contract.md` §2 for a distinct spelling rather than leaving it to invent one. The transferable half: a reservation is normally a sentence in a record nobody re-reads, and the two ingredients that turn one into a red lane are cheap and already present here — a **committed universe** to scan and a **count** proving the scan had something to find · crates/antseal-cli/tests/namespace_disjointness.rs · D69 §3 R8, §2
- 2026-08-12 · R22 lane · **`scripts/wasm-toolchain-audit.sh`'s pin extractor greps `docs/`, so prose *proposing* a pin counts as an install line.** The audit reads its expected `wasm-bindgen-cli` version out of documentation rather than out of the installed binary, and its glob reaches `docs/wasm-toolchain.md` **and D18's own §7**, both of which merely *discuss* the pin — so a decision record can redden the lane before anything is installed, and the red then reads as a toolchain state when it is a documentation state. R22's install closed it, but the closure was accidental: the lane went green because the docs and the binary happened to converge, not because the extractor learned to ask the binary · scripts/wasm-toolchain-audit.sh · `wasm-tests.sh --check`
- 2026-08-12 · R22 lane · **`without_storage_linkage_has_exactly_two_callers` cannot tell a citation from a call.** It is `text.matches("without_storage_linkage()")` counted over every `.rs` file with no comment skipping, so a **doc comment** quoting the suppressed tuple registers as a third suppressing caller and reddens D128 §3 R2's exact-count assertion. Found by it firing on R22's own doc comment, which was explaining the rule rather than breaking it; the scan's value is real (it caught a planted production caller in the same wave) and the fix is to skip comment lines, not to stop scanning · crates/antseal-core, the D128 two-caller scan · D128 §3 R2
- 2026-08-12 · R22 lane · **D18 §5 R3's "P20 rule 1's count stays at 162" is off by one — measured 163**, because a workspace member is itself a node in `cargo tree --workspace`, so adding `crates/antseal-wasm` adds a package to the tree even though it adds no *dependency*. No lane asserts the figure, so nothing went red and nothing was repaired; recorded because the record states it as a prediction that survives the change, and it does not · docs/decisions/D18-wasm-bindgen-surface-location.md §5 R3
- 2026-08-12 · R22 lane · **the R18 wording-ban scan does not cover the new page-facing crate.** `crates/antseal-core/tests/verdict_wording.rs` scans `crates/antseal-cli/src` and `verifier-web/` only, so `crates/antseal-wasm` — the crate that feeds the page its bytes — sits outside the scan that exists to stop a second spelling of a frozen verdict string. R22 covered its own crate with a local `tests/carriage.rs` rather than editing core (D18: core is not edited at all), so the property holds today; the scan's **root list** is now short by one, and it is short for exactly the crate the page loads · crates/antseal-core/tests/verdict_wording.rs · R18
- 2026-08-12 · R22 lane · **an exclusion-list check was unreachable behind a generic one, and the ordering sent the reader to the wrong repair.** With the unknown-arrival check running first, a package that was *present but excluded* reddened as an "unreviewed arrival" — a true failure with a false diagnosis, pointing at the reviewed-set table instead of at the exclusion list. Reordered so the specific check runs first, with the measurement recorded at the site · scripts/ci-lanes.sh `dep-graph`
- 2026-08-12 · R22 lane · **a resolved decision's "not due yet" prose scatters further than its edit set predicts: five further sites said "D18 is open / not due until M3"** beyond the two named in R22's brief — `crates/wasm-bitmatch/src/lib.rs`, `scripts/wasm-test-runner.mjs` (×2), `.cargo/config.toml`, `.github/workflows/ci.yml` and `CONTRIBUTING.md` — all corrected with dated notes. Recorded as a class rather than as six repairs: a brief that enumerates the sites of a decision's *pending* prose from the decision's own edit set will under-count, because the prose was written by whoever needed to explain the absence, not by whoever owned the decision · seven sites, corrected in-wave · D18
- 2026-08-12 · wave owner · **a `TODO.md` row can sit under the wrong milestone *sub-heading* and no instrument sees it.** `check_task_entries` verifies that a row's detail entry is in the right **domain file**, and nothing in the checker relates a checkbox row to the `###` heading above it — so `R77` sat under `### CI & copy (Q)` inside the M3 block, correctly filed in `tasks/R.md`, and every check was green. Found by eye while queueing wave 18's close; the row was moved to `### Reveal & page (R)` in the same act, but the **gap stands**: sub-heading placement is unwitnessed for all 635 rows, and the misfile is invisible to the count reconciliation too, since the row is counted by domain letter and not by the heading it sits under · TODO.md M3 block · scripts/check-traceability.py `check_task_entries`
- 2026-08-12 · D129 planning lane · **D66 §1 (d)'s Firefox claim is an artefact of its observation method, not of Firefox.** That record states the harness *"was attempted under Firefox 140.13.0esr headless and did not run — its GFX initialisation failed in this environment (`RenderCompositorSWGL failed mapping default framebuffer`) and no beacon was returned"*; this lane drove **the same Firefox 140.13.0esr, in the same environment, successfully** — it still prints the same `[GFX1-]: RenderCompositorSWGL failed mapping default framebuffer` line and **its JS engine is unaffected**, running the ruled single-file page from `file://`, instantiating the module to the same SHA-256 and honouring the whole CSP with zero violations. What made the difference was a profile with `gfx.webrender.software=true` (plus `LIBGL_ALWAYS_SOFTWARE=1`), a **POST beacon** instead of DOM inspection, and enough wall time before termination. **D66's ruling is untouched** — its indistinguishability finding was established *within* one browser and does not depend on a second — but the belief the note leaves behind is not harmless: **R27 would otherwise have inherited a belief that this project has one testable engine when it has two**, which is exactly the kind of claim a parity gate gets designed around · docs/decisions/D66-browser-endpoint-cors-and-fallback-policy.md §1 (d) · D129 §1 (i), §1 (l), §10 (i) · tasks/R.md R27
- 2026-08-12 · D129 planning lane · **a browser's automatic `GET /favicon.ico` is not a served artifact**, so R26's set-equality check must be written over served **responses** and never over **requests**. Observed on the multi-file arm over http: `GET /favicon.ico → 404 (File not found)`. R26's Accept asserts *"no artifact is served under the page's own paths that is absent from `SHA256SUMS`"* — a 404 serves no artifact, so nothing is violated, but a check written naively over requests goes **red on a 404 that violates nothing**, and the red would read as a deploy defect. Recorded so the scripted check is written the right way round the first time; R23 declaring a `data:` icon removes the request entirely, but the check must be correct without that · tasks/R.md R26 · D129 §7 (R26), §10 (ii)
- 2026-08-12 · D129 planning lane · **the R9 corpus exercises exactly one anchor-state class** — across all 21 committed report cases the anchors are `{"ots/invalid": 1, "tsa/invalid": 2}` — so R23's Accept *"completes verification incl. anchor checks"* is demonstrable **today only against anchors that evaluate to `invalid`**. The anchor path does run and does produce verdicts, which is what the row asks, and D129 §1 (g) satisfies it in its own terms; but `pending`/`attested`/`proven`/`valid-at-stamping-cert-since-expired` reach the page for the **first time** with **R27**'s A25 fixtures, which its own `Deps` already name. Recorded so nobody reads D129 §1 (g) as broader coverage than it is, and so R27 knows it owns the first page-side exercise of the richer states · testdata/vectors/ (R9 `report` cases) · tasks/R.md R23 Accept, R27 · D129 §10 (iii)
- 2026-08-12 · D131 planning lane · **a scan root that silently contributes zero files, behind a guard that cannot notice.** `crates/antseal-core/tests/verdict_wording.rs` walks two roots and guards itself with `sources.len() > 10` over their **union**; `crates/antseal-cli/src` supplies 54, so the guard is satisfied forever no matter what the other root contains. Measured with a planted frozen sentence: under the template name D129 §7 minted (`index.html.template`, `extension()` = `Some("template")`) page coverage is **zero** and all nine tests pass. **A guard over a union of roots is not a guard on any of them** · crates/antseal-core/tests/verdict_wording.rs:863 · D131 §1 (c)
- 2026-08-12 · D131 planning lane · **an assertion that a second root can satisfy on the first root's behalf.** `every_recorded_residue_is_still_there` asserts six sentences **present** across the union of `crates/antseal-cli/src` and `verifier-web/`; measured, deleting the CLI's copy turns it RED, and adding any `verifier-web` file carrying the same string turns it GREEN with the deletion still in place. The ban and the residue tests want **opposite** domains and shared one source list · crates/antseal-core/tests/verdict_wording.rs:884 · D131 §1 (e)
- 2026-08-12 · D131 planning lane · **`atob` accepts wrapped base64 in both engines, and a build page's cost is where you put it.** Chromium 150 and Firefox 140.13.0esr both decode newline- and space-bearing base64 to identical bytes (forgiving-base64), so line-wrapping breaks no runtime; what it does break is determinism — five plausible encoders split two ways on the default wrap, and the width enters the hashed script text. Separately, the wording suite's runtime moves 0.62 → 0.87 → 1.13 s purely with the contents of `verifier-web/`, which is why build-artifact assertions belong to a build script and not to `cargo test` · scripts/ · D131 §1 (h)(i)(k)
- 2026-08-12 · registrar (wave 19) · **an assertion constructed to pass: the one-line `SHA256SUMS` check.** `scripts/verifier-page-pack.mjs` builds the sums string as a single line and then asserts that it splits into one line, so it documents D129 §5 R7 rather than testing it and cannot fail for any input. Same class as the two D131 entries above, found in the wave that repaired them · scripts/verifier-page-pack.mjs · R25 Notes
- 2026-08-12 · registrar (wave 19) · **a required drift test that was routed, recorded as routed, and never written.** D131 §9.2 routes D63 §7 rule 5's advice-line drift test into `crates/antseal-wasm/tests/page_template.rs`; that file carries five tests and none of them is it, and nothing in `crates/` compares the page's advice line to `MVP-SPEC.md` line 139. The line itself is present and correctly spelled — which is why nothing is red, and why the absence is the hazard rather than the string · crates/antseal-wasm/tests/page_template.rs · R25 Accept row 3
- 2026-08-12 · registrar (wave 19) · **a `--check` mode that certifies an artifact it did not build, and a sibling that does not.** `scripts/verifier-page-build.sh` rebuilds only when `target/wasm-pack/`'s two files are absent, so its `--check` packages and asserts over a stale module while every D129 §5 R9 assertion still passes — each compares the page against *the module it was built from*. Its sibling `scripts/wasm-pack-build.sh --check` `rm -rf`s and rebuilds unconditionally; the staleness surface there is `--self-test`. **A wave brief named the wrong one of the two**, which is why the row was re-aimed before minting · scripts/verifier-page-build.sh · R83
- 2026-08-16 · U73 lane · **`CARGO_BIN_EXE_antseal` is ONE path shared by both feature configurations**, so a spawned-binary test answers differently depending on which build last wrote `target/debug/antseal`. An earlier `--features ant-backend` run in the same session had left the two U73 rows **green** — a check passing for the wrong reason, which is the [[assertions-that-cannot-fail]] family with the sign flipped. The registrar re-measured both builds in sequence for this reason and restored the default binary afterwards · crates/antseal-cli/tests/seal_command.rs `antseal_bin()`
- 2026-08-16 · U67 lane · **`seal_session::production_files_naming` scans `src/<mod>/tests.rs` files AS PRODUCTION.** Its own rustdoc claims *"test modules are exempt by construction (`production_files_naming` cuts each file at its first `#[cfg(test)]`)"* — but a `src/<mod>/tests.rs` file contains **no** `#[cfg(test)]`; the attribute is on the parent's `mod tests;`. **Six such files exist in `crates/antseal-cli/src/` today** and none happens to name a hunted needle, so the gap is **live but unexposed**. Found the hard way: the lane's first draft used that layout and reddened two unrelated checkers. Resolved in-lane by inlining the test module rather than by widening a shared checker, which is the correct call — the widening belongs to whoever promotes this line · crates/antseal-cli/tests/ `seal_session::production_files_naming`
- 2026-08-16 · U71 lane + U67 lane · **A scan that counts PROSE, twice in one wave, which is what makes it a pattern rather than an anecdote.** `the_reveal_consent_gate_has_exactly_one_production_caller` went red at `run_reveal( 4 time(s), not 3` — the fourth "call" was a **rustdoc paragraph** spelling the call form literally (`run_reveal(.., |_| Ok(()))`), because the scan matches text. One layer over, `the_raw_backend_type_is_named_in_exactly_one_file` caught a rustdoc paragraph naming `AntCoreBackend`. **Neither expected count moved**; both prose sites were reworded and both rustdocs now record that this happened, so the next lane does not re-find the trap. A check that reddens for a reason unrelated to its property is as corrosive as one that cannot redden at all · crates/antseal-cli/tests/reveal_consent.rs:651 · crates/antseal-cli/src/backend.rs
- 2026-08-16 · R27 lane + Q19 venue lane · **D136 §2 R9's ~12.5 s marginal cost for the browser lane is superseded by measurement: ~38 s warm (R27's suite), ~50 s warm with R85's rows.** Not a re-decision — the ruling that the lane rides `local-gate.sh` is untouched — and the growth is not overhead: it is 13 real verifications replacing a race (see the next entry). Re-recorded so a reader budgeting the local gate is not working from a number the wave that wrote it had already invalidated · docs/decisions/D136-the-page-browser-lane-without-playwright.md §2 R9 · §1 h · §7
- 2026-08-16 · R27 lane · **A twelve-bundle green that was vacuous for eleven of them.** `dropBundle` polled *"is `#result` hidden?"*, but the page unhides `#result` on the first success and **never re-hides it**, so from the second drop the exit condition was already satisfied by the **previous** bundle's DOM. Proven by measurement rather than by reading: the first parity run reported one fixture's row against another fixture's anchor block. Fixed with an explicit reset before each `setFileInputFiles`. **Wave 19's and wave 20's "twelve bundles rendering in Chromium" claims are retroactively qualified to one bundle witnessed**, and the arm got slower because it now actually waits · scripts/verifier-page-browser.mjs `dropBundle`
- 2026-08-16 · R27 lane · **D133 §3 R8's perturbation table and §5.2 promise an outcome no committed material can produce.** §5.2 predicts *"overlay row `Refuted` with code `anchor-ots-online-header-mismatch`"* and §3 R8's row is headed *"mismatch → invalid"*; measured, both endpoints serving 960768's real header render `NotPromoted{RefutationSuppressed}`, class `outcome`. `classify_ots`'s **O5 out-ranks O6/O7** on A25's merged `.ots`, and the frozen corpus had already recorded the mechanism (`ots-upgraded-online-block-absent` pinned `state: pending, suppressed: [...]`). The **product** half is the row R88; this line is the document half · docs/decisions/D133-the-attested-anchor-fixture.md §3 R8 · §5.2
- 2026-08-16 · R85 act-2 lane · **D137 §5 point 3 names a string the page never renders, and §6.1's assertion ordering makes its own assertion 3 unfirable.** §5 point 3 asks the rendered receipt element to carry the failed line naming `wrong-chain`; the rendered label is `"wrong chain"` — with a **space** — and the hyphenated form is the evidence-document class token. And §6.1 fixes its assertions *"in order of what they catch"*, with assertion 2 pinning each of the four renders to its **full expected string**; once that passes, assertion 3 (*"no render may contain the unqualified clause"*) can never fail without assertion 2 having already aborted the test. Assertion 3 is still worth having; it guards a narrower case than §6.1 implies · docs/decisions/D137-what-the-page-may-say-about-an-unresolvable-receipt.md §5 point 3 · §6.1
- 2026-08-16 · R27 lane · **D130's parity sentence is not where two documents cite it, and it is not the comparison that holds.** It lives at §5's `### R27 / Q19 — the parity gate` clause 1, not §7.3 — the section D133 §5.2 and R27's own Notes both point at, and where no such clause exists. And *"string for string after the CLI's indentation is stripped"* understates the difference: the CLI prints `"{slot}: {state_line}{tag}"` as **one** line while the page puts `"{slot} {tag}"` in one `<p>` and `state_line` in the next. **A lane taking that sentence literally builds a gate that reddens on a correct page** · docs/decisions/D130-r18-wording-route-to-the-verifier-page.md §5 (parity gate) clause 1
- 2026-08-16 · registrar (wave 21) · **D136 §1 (k) is stale by one in the direction it was written to correct.** It measures `grep -c '#[test]' crates/antseal-wasm/tests/page_template.rs` → **6** and calls the advice-drift test *"the sixth"*; measured at the wave-21 close the file carries **7**, the seventh being `the_footer_names_the_module_not_the_page_as_the_subject_of_its_digest` at `:244`, which R85 act 1 added later the same day under that record's own §2 R12. **The locator holds — line 145 is still the drift test — and only the count moved.** Recorded because §1 (k) exists precisely to correct a stale count in R25's Notes, and a correction that goes stale the same day is worth one line · docs/decisions/D136-the-page-browser-lane-without-playwright.md §1 (k) · crates/antseal-wasm/tests/page_template.rs:145
- 2026-08-16 · registrar (wave 21) · **A method sentence left standing beside the dated correction that contradicts it.** D135 §1.1's method paragraph still reads *"jobs with an empty `runner_name` were excluded — they were never dispatched and billed nothing"*, while the 2026-08-16 parenthetical eleven lines below reports that a query for jobs with no runner assignment returns **zero** across the whole window and that all 45 carry real `started_at`/`completed_at` stamps. The **figure is unaffected** (excluding an empty set changes nothing) and no ruling moves; what is left is a reader meeting the old mechanism first. Named rather than edited, because the record's argued body is not the registrar's to rewrite · docs/decisions/D135-the-two-environment-build-comparison-and-its-tier.md §1.1
- 2026-08-16 · registrar (wave 21) · **`dep-graph` and `core-dep-graph` are BOTH correct and name different things, and a wave brief tried to collapse them.** `bash scripts/ci-lanes.sh --list` names the **script lane** `dep-graph` (`scripts/ci-lanes.sh:69`, dispatched at `:1443`); **`core-dep-graph` is the GitHub Actions job id and required-status context** (`.github/workflows/ci.yml:222`). The brief instructed the registrar to "correct" R80's Accept row 4, which names the required context and names it right — applying the instruction would have made a correct clause wrong. **The house form, already used in this ledger:** cite the script lane as ``scripts/ci-lanes.sh `dep-graph` `` and the CI job as `core-dep-graph` · scripts/ci-lanes.sh:69 · .github/workflows/ci.yml:222 · tasks/R.md R80
- 2026-08-16 · registrar (wave 21) · **R86 is a STEP, not a job, and the register must not cite a job that does not exist.** Searched at the wave-21 close, `.github/workflows/` contains **no** two-build byte-identity job: the enforcement D135 §3 R1 ruled is the step `build and check the page` (`./scripts/pages-publish.sh --build`) inside `pages.yml`'s pre-existing `publish` job, positioned before `actions/configure-pages` so a red stages nothing. Deliberate — a new job is a new metered runner — but R25's Accept row 1 was briefed to *"cite R86's job by name"*, which would have named nothing. Also recorded: `scripts/reproducible-build.sh` is **untracked** at the moment R86 is ticked · the .github/workflows/pages.yml `publish` job, steps 78-79 · tasks/R.md R25, R86
- 2026-08-16 · registrar (wave 21) · **`CHANGELOG.md` carries no wave entries and by its own text never will.** A wave brief directed the wave-21 entry there; the file's lines 5-8 say *"this file starts at the format-v1 freeze … the per-wave record of how the project got here lives in `TODO.md`'s change log and in `docs/decisions/`"*, and its only `##` entry is `[format-v1] — 2026-07-28`. Wave 21 froze no format version, so a `CHANGELOG.md` entry would have been the first thing in that file that is not a permanence claim. The entry went to `TODO.md`'s `## Change log` · CHANGELOG.md:5-8 · TODO.md `## Change log`
- 2026-08-16 · registrar (wave 21) · **`docs/decisions/D65` §1 (d) describes `json-envelopes.txt` as "38 lines" with `show`/`reveal`/`verify` as "not-implemented stubs".** Stale before this wave (44 lines, none a stub) and **48** at its close, after R82 made the file feature-parameterised so both `backend::unavailable` arms are documented in every build. The count is not load-bearing for any ruling; the *stub* claim is, because a reader pricing D65's tier partition against three stubs is pricing the wrong surface · docs/decisions/D65-json-schema-stability-commitment.md §1 (d) · crates/antseal-cli/tests/snapshots/json-envelopes.txt

- 2026-08-17 · registrar (wave 22), from the pre-public scrub's history lane · **`secret-guard`'s PEM rule cannot match an OpenPGP private key, and its `*.md` exclusion hides a third of everything ever committed.** The single generic rule `[-]{5}BEGIN[ A-Z0-9]*PRIVATE KEY[-]{5}` (`scripts/ci-lanes.sh:1075`) anchors the closing dashes **immediately after** the literal `PRIVATE KEY`, and OpenPGP interposes ` BLOCK` — so `-----BEGIN PGP PRIVATE KEY BLOCK-----` never matches while RSA/EC/plain/OPENSSH/ENCRYPTED all do (5 of 6 flavours; `[ A-Z0-9]*` also carries no `-`, so `X25519-PRIVATE KEY` shapes miss too). Separately, `--exclude='*.md'` (`:1072-1073`) blinds every pattern to **1 062 of 3 379 reachable blob paths — 31.4 %** of everything ever committed (at HEAD the tracked-file figure is a milder **224 of 1 226, 18.3 %**; the two numbers measure different things and the 31 % one is the honest one for a history scan). **Rule 8 clause: the subject is the checker's own coverage, and neither blind spot blocks an acceptance on the ship path** — no M4 row's Accept turns on detecting a PGP block or scanning Markdown. The third blind spot in the same guard **was** promoted, and is the next entry. · scripts/ci-lanes.sh:1072-1075 · docs/reviews/pre-public-scrub-history.md:387-397, :419
- 2026-08-17 · registrar (wave 22) · **PROMOTION — `secret-guard` is blind to an unencrypted `-W` minisign secret key, and that becomes `Q245`.** The guard's only minisign rule (`ci-lanes.sh:1093`) is the literal `minisign encrypted secret key`; a `-W` key has no passphrase, no XOR mask, the raw Ed25519 secret in cleartext base64, and a comment line without the word `encrypted`, so **no rule in the set matches it** (reproduced: shipped pattern `*** NO MATCH (blind) ***`, `minisign( encrypted)? secret key` matches). **Rule 8 clause: promoted because it blocks an acceptance on the ship path — `Q30` is an M4 queue row and it generates precisely the artifact the guard cannot see**, which D71 `:866-870` states in as many words. Its two siblings above stay unpromoted, which is the rule working in both directions inside one finding. · minted **Q245** · scripts/ci-lanes.sh:1093 · docs/reviews/pre-public-scrub-history.md:399-406 · docs/decisions/D71-binary-signing-mechanism-and-key-custody.md:866-870
- 2026-08-17 · registrar (wave 22) · **`secret-guard`'s self-test plants five fakes for six rules and balances anyway, so one rule has never been exercised.** `ci-lanes.sh:1109-1128` writes five fakes, sets `planted=5`, and fails if the scan does not return five — but the minisign rule (4b) has **no planted fake at all**; the age fake at `:1117` fills the "(4)" slot and the arithmetic closes while a rule sits untested. Nor does the self-test plant a PGP block or any `.md`-named fake, so **all three blind spots above are invisible to the instrument that exists to make the guard's failures visible.** This is the [[assertions-that-cannot-fail]] class inside a fault-planting harness. **Rule 8 clause: the subject is a self-test's own coverage; the ship-path consequence is carried by `Q245`, whose Accept requires `planted` to be derived per rule rather than per slot** — so the finding is recorded here and its teeth are on the row. · scripts/ci-lanes.sh:1109-1128
- 2026-08-17 · registrar (wave 22) · **`docs/ci-verification.md`'s branch-protection section is stale in its heading, its evidence block and its rationale — and the 403 it records no longer reproduces.** `:1168` reads *"Branch protection is BLOCKED BY PLAN — verified 2026-07-28 (wave 7)"* over an evidence block at `:1176-1181` showing both endpoints returning 403 *"Upgrade to GitHub Pro or make this repository public"*. Re-measured read-only 2026-08-17: `rulesets` returns **`200 OK []`** (also with `includes_parents=true`, and `rules/branches/main` → `[]`), and `branches/main/protection` returns **404 `"Branch not protected"`** — GitHub un-gated rulesets for private repositories on Free, so the load-bearing sentence is not the heading but `:1184-1195`'s *"GitHub gates both classic protected branches and the newer rulesets behind a paid plan"*. Substantively: **nothing protects `main` today, and `format-v1-freeze` has no protection either** — the legacy tag-protection endpoint is retired and `rulesets` is empty, leaving the freeze enforced only by the in-repo digests `:1203-1205` names. Whether a `PUT` now succeeds is **untested**. **Rule 8 clause: the subject is a runbook's own recorded verification, and the file is outside the registrar's write scope; the consequence for the ship path is registered on `Q65` and struck in `TODO.md`'s Current focus, where the same stale claim had a second home the brief did not name.** · docs/ci-verification.md:1168, :1176-1181, :1184-1195
- 2026-08-17 · registrar (wave 22) · **Three lanes were briefed with a premise the shipped page contradicts: *"the verifier page runs with zero network access by ruling."*** It does not. `docs/decisions/D129-…:15-17` sets **`connect-src https:` — scheme-only, deliberately NOT the six pinned hosts** — because §1 (m) at `:352-360` measured that a host allowlist silently breaks spec line 137's user-supplied overrides with a violation the page cannot report; the ruling is at `:705-718`, the shipped meta tag is `verifier-web/index.template.html:6`, and R24/D66 give the page two live `fetch()` sites (`:534`, `:574`). **Zero-network is a test-lane assertion about the offline path**, at `scripts/verifier-page-browser.mjs:692-699` (*"the offline run fetches NOTHING but the page itself"*, positive-control planted at `:1446-1475`), not a property of the artifact. D134 `:32-41` already carries the corrective. **Rule 8 clause: the subject is a wave brief's own premise — it changes what a lane believes, not what a user's verify does.** · docs/decisions/D129-verifier-page-file-shape-and-the-in-artifact-csp.md:15-17, :352-360 · scripts/verifier-page-browser.mjs:692-699
- 2026-08-17 · registrar (wave 22) · **D134 re-specified two probes the OBOL round's addendum had pinned as its resolution evidence, and the addendum is the round document's rather than D134's — a distinction a citation will get wrong.** The probes live at `docs/research/paper-key-transport-design-round.md:491-497`, pinned as evidence at `:899-903`. **Probe 2 cannot discriminate its own arms**: all three declared observables (page-size delta, CSP-hash churn, browser-lane pass) are arm-invariant — both arms compile the same ~13 kB wordlist and differ by one export's glue, **146 B measured by D132**, ~0.008 % of a 1.84 MB module, and both edit the inline script so both produce exactly one recomputed hash (`D134:548-557`). **Probe 3 would have licensed a false repair-timing promise**: specified *"over kilobyte bundles"*, it would have measured only the trivial rows of a **Θ(192 × \|file\|)** cost — ~2–6 ms for the largest golden vector but **~19–64 s for a 100 MB reveal** — and D134 `:559-576` rules that no page or CLI copy may promise repair timing, and that the benchmark must sweep sizes. **Rule 8 clause: decision- and research-document prose about how a future measurement is specified; no shipped behaviour is wrong today.** · docs/decisions/D134-paper-key-locked-bundle-surfaces.md:25-31, :548-576 · docs/research/paper-key-transport-design-round.md:491-497
- 2026-08-17 · registrar (wave 22) · **Tracker-count drift, third occurrence, and this one was structurally undetectable: the header sentence and the domain table disagreed by one and nothing compares them.** `985ad05` corrected the total 647 → 648 from the script's parse and left the table's **Q** cell at 217 against 218 physical Q rows (Q239, minted at the wave's start), so the table summed to 647 under a sentence saying 648. None of `check-traceability.py`'s seven checks reads the domain table — it pairs rows against entries, never against the header — so the drift could not go red. **The same registration left the same drift in a third counter**: the `## M4` section heading read *"20 tasks"* over **21** physical rows. So one row minted mid-wave went unrecorded in three places at once — the domain table, the M4 heading, and (until `985ad05`) the header sentence — because each is maintained by hand and none is compared to the parse. Recomputed at this registration from one parse: R 78 → **81**, U 73 → **75**, Q 217 → **224**, M3 heading 45 → **48**, M4 heading 20 → **29**, total **659**. **Rule 8 clause: tracker-count drift is named in the rule's own text as a ledger item.** **Worth promoting if it recurs**: three hand-maintained counters that no check reads is the [[assertions-that-cannot-fail]] shape applied to bookkeeping, and the fix — have `check-traceability.py` compare the domain table and section headings against its own parse — is small. It blocks no acceptance today, so it stays here. · TODO.md:6, the domain table, `## M3`/`## M4` headings · scripts/check-traceability.py (no check covers any of them)
- 2026-08-17 · registrar (wave 22) · **`the_fetch_failure_vocabulary_is_finite_and_static` now sweeps a core-owned type from `antseal-net`'s suite, and its own doc comment is false as written.** After R89 the test at `crates/antseal-net/tests/live_display_channel.rs:296` asserts `FetchFailureClass::ALL.len() == 2`, label shape and label/token collision-freedom — all `antseal-core` facts, and all already covered core-side at `tests/verdict_wording.rs:288-298` and `tests/r89_live_outcome_json.rs:54-73` — while its doc still claims *"the value `antseal-net` produces has no free-form slot at all"*. One genuine `antseal-net` property survives in it: it fails to compile if the re-export at `live.rs:74` is dropped. The neighbouring totality test over `StorageError` at `:331+` is correctly homed and should stay. **A caution for whoever traces its history, and a worked example of the [[interrupted-lane-tree-is-not-evidence]] trap**: a verification lane run before `1830618` reported from `git log -S "enum FetchFailureClass"` that the type was *born* in `antseal-core` and had *never* lived in `antseal-net` — a confident inversion, because `git log` cannot see an uncommitted working tree, and at that moment `git show HEAD:crates/antseal-net/src/live.rs` still put the enum at `:129` with `ALL` at `:147`. The truth is the opposite: R79 (`b253ae8`, which touched **only** `crates/antseal-net/src/live.rs`) created it there, and R89 moved it. **Settled by reading the committed blob rather than by arbitrating between two lanes' accounts.** **Rule 8 clause: the subject is a test's home and its own prose; nothing a user runs behaves differently.** · crates/antseal-net/tests/live_display_channel.rs:296
- 2026-08-17 · registrar (wave 22) · **Four documents cite locations R89 deleted or moved, in three classes with three different owners.** (a) `docs/decisions/D130-…:390` cites `orchestration.rs:231-234, on LiveBlobOutcome::FetchFailed::reason` and block-quotes a doc comment that no longer exists — field and text both gone. (b) `tasks/R.md`'s R79 entry carried pre-R89 locators (`antseal-net/src/live.rs:129`, `ALL` at `:147`, an `of()`); **corrected in this act** by an appended dated pointer rather than by rewriting a closed row's evidence. (c) `docs/decisions/D72-…`'s manifest citations are stale by roughly eighty lines — `:347` cites `antseal-core/Cargo.toml:133-134` for `:134-135`, `:353` cites `Cargo.toml:53` for `:64`, `:359` cites `antseal-net/Cargo.toml:55-59` for `:113-117`, `:761` cites `Cargo.toml:49-52` for `:60-64` — noted on **Q241** so a lane does not follow them. The **shipped-crate** doc sentences in the same class are a different animal and mint a row (**R92**), on R87's precedent. **Rule 8 clause: stale locators in decision records and in the tracker are named in the rule's own text; the same drift inside `crates/` is product doc-currency and takes an id.** · docs/decisions/D130-r18-wording-route-to-the-verifier-page.md:390 · docs/decisions/D72-release-targets-distribution-and-crates-io-scope.md:347,353,359,761 · tasks/R.md R79
- 2026-08-17 · registrar (wave 22) · **`scripts/ci-lanes.sh:1386` still tells its reader that licences are stubbed, one line above the command that enforces them.** The comment reads *"Explicit check list, never a bare `check`: licenses are STUBBED until Q29"* while `:1387` runs `cargo deny --locked check advisories bans sources licenses` against a `deny.toml` whose licenses section Q29 landed with 14 allow ids and five exceptions; `deny.toml:9-11` was updated in the same act and the script comment was not — and the commit that landed Q29 (`1830618`) touched **four lines of this very file**, correcting the lane's header summary at `:28` to `advisories/bans/sources/licenses` and the command at `:1387`, while leaving the contradicting sentence between them untouched. **Rule 8 clause: a comment inside a CI lane, about that lane's own state — it changes what a reader believes, not what the lane does; the lane is green either way.** Recorded on Q29's tick as a named residue rather than left to be rediscovered. · scripts/ci-lanes.sh:1386-1387 · deny.toml:9-11
- 2026-08-17 · registrar (wave 22) · **D73 §3.5.4's own sentence about the allowance fingerprint is stale about itself, and the way it is stale would have mis-scoped the row it asked for.** It reads *"It is documented nowhere as an observability property, nothing pins it, and a refactor would silently destroy it"* — but D73 §3.5.4 and §7 item 3 **are** that documentation, at length and with the upstream comparison measured. What remains true is the second and third clauses: **nothing pins it.** A lane briefed on the sentence as written would have found the property already documented and closed the row without writing the test, which is the only half with teeth; **Q240** is therefore scoped to the pin and the threat-model §2.3 entry, not to "document it". **Rule 8 clause: decision-document prose, corrected at the point it would have misdirected a row.** · docs/decisions/D73-external-completions-without-telemetry.md:380-394, :743-750, :782-796 · minted Q240
- 2026-08-17 · orchestrator (wave 22, post-close) · **D52's own TRIAGE rule was not followed for the only scheduled devnet run that has ever existed, and the process row that exists to prevent exactly this is ticked.** `.github/workflows/devnet-e2e-cron.yml:27-29` states the rule verbatim: *"a red scheduled run gets a tracking note in the next wave's bookkeeping; **two consecutive reds block storage-wave starts** until diagnosed."* Run `31571938292` (`event: schedule`, `conclusion: failure`, 2026-08-12 on `3c8095a`) sat unrecorded for **five days** across two wave closes. Verified absent, not assumed: `grep -rn '31571938292'` over all `*.md` returns **one** hit and it is `docs/reviews/pre-public-scrub-github-side.md:498`, which enumerated the artifact while auditing retention and did not read its verdict; the *"red at `3c8095a`"* already in `docs/ci-verification.md:2423` is a **different lane** (`machine_mode` under `--features ant-backend`, owned by R82) and reading it as this one is the [[verify-the-field-that-carries-the-claim]] trap. **The sharper half: `Q79` — *"read the scheduled lanes as a wave-close step"*, minted precisely because `fuzz-nightly` ran five times unobserved — is `- [x]` ticked.** A ticked process row is not a running process, and nothing in the tree re-reads it: no checker enumerates scheduled workflows and compares their last conclusion against the tracker. Found only because a maintainer asked whether the evidence artifact should be deleted or archived, which caused someone to open `evidence.txt` for the first time. **Rule 8 clause: gate-instrument honesty — a wave-close process step that did not run. The product defect it should have surfaced is separately a row (Q246), and the retention arithmetic that makes its evidence store unusable is another (Q247); what stays here is that the reading step itself has no teeth.** **Worth promoting if it recurs**: the fix is small and is the [[assertions-that-cannot-fail]] shape applied to process — have the wave-close step be a script that lists every `schedule:` workflow with its latest conclusion, so "I read the scheduled lanes" becomes a thing that can go red. · .github/workflows/devnet-e2e-cron.yml:27-29 · TODO.md Q79 (ticked) · minted Q246, Q247

- 2026-08-17 · Q245 lane (counts re-measured by the wave-23 ledger lane) · **`scripts/ci-lanes.sh --self-test` writes a copy of the script into the TRACKED tree, and `secret-guard` fires on it.** `self_test()` writes `$repo/scripts/.ci-lanes-selftest.sh` (`:1516`) — in `scripts/` deliberately, because the script derives `repo` from its own location — and removes it on a `RETURN` trap a killed shell never runs. Measured: `git check-ignore` exits 1 on that path and `git status` reports it `??`, so it is stageable; and the guard's exclusion is the **basename** `ci-lanes.sh`, which the copy does not carry. The lane's own fixture-planting `printf` lines are literal secret-shaped strings, so a scan of the copy fires **six of the seven rule arms** — (1) PEM `:1176`, (2) the keystore conjunction `:1177`, (3) vault-export magic `:1178`, (4a) age `:1179`, (4b) header `:1183-1186`, (4c) body `:1192-1193` — everything but (5), whose 64-hex value is generated at run time rather than written in the source. **The finding was queued as "all five rules fire, raised 4 → 5"; measured, it is six of seven now and five at HEAD, so Q245's (4c) raised it 5 → 6.** Pre-existing; the failure mode is a false red indistinguishable from a real hit · scripts/ci-lanes.sh:1516 · the basename exclusion at :1072-1073
- 2026-08-17 · Q245 lane · **`docs/ci-verification.md`'s `secret-guard` description is stale in its exclusion list, its fixture count and its recorded result — three claims, one bullet.** `:333-341` gives the exclusions as `.git/`, `target/`, `.github/` *"(the patterns themselves live there)"* and `*.md`; the lane excludes `.git`, `target`, `.devnet`, the basename `ci-lanes.sh` and `*.md`, and has never excluded `.github/`. The same bullet says *"four fakes planted"* and `:419`'s wave-2 evidence table records *"self-test detected 4/4 planted fakes"*. Stale before this work (HEAD planted five), and **eleven** after it · docs/ci-verification.md:333-341, :419
- 2026-08-17 · Q245 lane (list completed by the wave-23 ledger lane) · **Q245's +118-line shift invalidated every locator into `lane_secret_guard`, and the queued list of the casualties was short by four files.** `git diff --numstat` is `126/8`; everything below the old `:1128` moved by 118. Confirmed stale: `D71:330` (`ci-lanes.sh:1075-1093` — `:1075` is still the PEM rule but `:1093` is now a comment; the minisign arm is `:1111`), `docs/instrument-ledger.md:126` (`:1093`) and `:127` (`:1109-1128`), `docs/instrument-ledger.md:134` (`:1386-1387`, now `:1504-1505`), `TODO.md:808` (`:1386`, `:1387`), `TODO.md:1035` plus `docs/research/paper-key-transport-design-round.md:326` and `:562` (`:1054-1139`), `:123` (`:1139`), `:676` (`:1042-1142`), and `D140:287`, `:502`, `:700` (`:1387`). **Four the queue missed**: `docs/reviews/pre-public-scrub-worktree.md:95-97`, three exact-line quotes of which `:97` block-quotes the *retired* rule text verbatim, plus `docs/reviews/pre-public-scrub-history.md:379`, `tasks/Q.md:2999` and `TODO.md:823` (`:1055-1144`, `:1109-1128`). Separately **already stale at HEAD and not caused by this change**: `D124:506` and `scripts/cargo-free.sh:88` both cite `ci-lanes.sh:1204` for a `command -v cargo` guard the file has never contained (its only such line is `command -v cargo-deny`, `:1501`), and `D109:318` cites `:1270` for the `traceability` wiring, which is `:983` and did not move. The one that is still right is `docs/instrument-ledger.md:125`'s `:1072-1075`. **No checker validates any of them** · scripts/ci-lanes.sh
- 2026-08-17 · Q245 lane (figures re-measured by the wave-23 ledger lane) · **Recording a finding inflates the population the finding is about, and here it inflated it past the point where the recorded figure means anything.** Q245's row observed that the file count for `minisign encrypted secret key` had moved because the row and the ledger text about it now contain the literal. Re-measured at this filing: **8 tracked files carry the old literal — the same 8 at HEAD — and 10 match the widened `(minisign|rsign)([ ]encrypted)?[ ]secret[ ]key`**, so neither queued figure (4 → 7, → 9) reproduces; the tree moved again under the registrar while the row was being written. What does reproduce, and is the point: **six of the eight are documents *about* the finding** (`docs/instrument-ledger.md`, the three `docs/reviews/pre-public-scrub-*.md` halves, `TODO.md`, `tasks/Q.md`), and only two are the subject itself (`scripts/ci-lanes.sh`'s rule and `D71`'s design record). A count over a pattern this project writes about is not a measurement of the tree; it is a measurement of the tree plus the wave · scripts/ci-lanes.sh:1111 · the `*.md` exclusion at :1072-1073
- 2026-08-17 · D140 planning lane · **PROMOTION — `init`'s human report has no committed golden rendering, and both freezes pass over MVP-SPEC line 143's mandated nag, so it becomes `U78`.** Measured: `crates/antseal-cli/tests/snapshots/` holds ten files and none of them is `init`'s report (`cli-surface.help.txt` is the help text, `json-envelopes.txt` is the machine document); and `scripts/check-copy-style.py` reaches CLI copy **only through the rendered golden snapshots** by its own module doc at `:28-33`, so a surface with no snapshot is a surface the copy lint has never read. U32's CLI-surface freeze passes over it for the same reason. **Rule 8 clause: promoted because it blocks an acceptance on the ship path — `U78` is ordered before `U32`, whose snapshot freeze would otherwise close over a surface nobody has pinned** · minted **U78** · crates/antseal-cli/tests/snapshots/ · scripts/check-copy-style.py:28-33 · D140 §2 R3
- 2026-08-17 · D140 planning lane · **The generalised "stale forward reference" lint is unbuildable in unregistered form, and the measurement is what kills it rather than the cost.** D140 §2 R8 measured **117 forward references** in the tracker, **98 of them already ticked** and near-all correct past-tense narration — so a lint that flags "row X refers to work Y that has not landed" would spend its whole output on sentences that are true. The only surviving shape is the one `OWED_PRESENCE` already uses: a **register** of the references that are genuinely owed, with named owners, so the check has a finite subject it can go red on. Recorded rather than promoted: it blocks no acceptance, and the register that would carry it does not exist · docs/decisions/D140-readme-front-door-and-init-doc-pointer.md §2 R8
- 2026-08-17 · D140 planning lane (interval re-measured by the wave-23 ledger lane) · **`tasks/Q.md` Q65's registrar Note asserts a closure that the same sentence's other half falsified.** The Note reads *"**B1 is closed by Q29 this wave; B2 was fixed in `4cae455`.**"* B2, as `docs/reviews/pre-public-scrub-worktree.md:178` states it, has **five** components — three table rows (the `pre-M0 scaffold` status line, *"219 tasks"*, and `:21`'s `verifier-web/ … (M3)` reading as future) and the two registered `OWED_PRESENCE` debts. `4cae455` fixed **two** of the three table rows and neither debt, and it added a `## License` section reading *"all rights reserved — until Q29 lands"*. **That sentence was falsified by `1830618` — Q29 — which is the very commit the same Note calls B1's closure.** The queued form of this entry said the claim *"went stale in 7 minutes"*; measured from the commit timestamps, the licence sentence stood for **103 minutes** (`4cae455` 00:04:06 → `1830618` 01:47:05) and the *"648 tasks"* figure for **110** (`dee7b2a` 01:54:19, *"648 to 659 rows"*). Seven minutes is the gap between the two falsifying commits, not the lifetime of either claim. Amend by dated addendum · tasks/Q.md Q65 Notes · docs/reviews/pre-public-scrub-worktree.md:178-215
- 2026-08-17 · D140 planning lane (re-measured by the wave-23 ledger lane) · **`crates/antseal-cli/src/init.rs:327-328` attributes to MVP-SPEC line 143 both a venue and a frequency, and line 143 states neither.** The doc comment reads *"The two standing warnings MVP-SPEC.md line 143 requires `init` to give **once**…"*. Line 143's mandate is *"**Docs and CLI must nag on both failure modes**"* — a venue pair, not `init` in particular — and the word *"once"* does not appear on it at all. **The queued form said line 143 "contains neither"; it does contain the token `init`, once, for a different obligation** (*"`init` enforces a passphrase-strength floor and offers a high-entropy keyfile / OS-keystore wrap"*), which is exactly the kind of near-miss that makes a wrong attribution survive a spot check. `U18` is the citation for both the venue and the frequency · crates/antseal-cli/src/init.rs:327-328 · MVP-SPEC.md line 143 · tasks/U.md U18
- 2026-08-17 · D139 planning lane + Q25 lane (locator re-measured by the wave-23 ledger lane) · **`check_spellings` locates P4/P5 violations with `text.find(anchor)`, so the rules are wrap-fragile in both directions and the blind direction is silent.** D139 §1.2 swept the wrap positions: **3 of 6 (P4) and 3 of 5 (P5)** intra-phrase wraps redden *correct* copy, and **3 of 3 / 2 of 2** wraps falling inside the *anchor* make the rule blind to a genuine mis-spelling, because a wrapped anchor is never looked at while the identical paraphrase on one line reddens. Q25 measured both directions independently and named the mechanism: the difference between "caught" and "invisible" is purely where the line break falls. The check now lives at `scripts/check-copy-style.py:408-431`, **not the `:380-404` this was queued as** — the central `docs/user/` admission moved it — and D139's live false red at `docs/threat-model.md:450` was fixed by Q21 in this same wave (the phrase now sits unwrapped at `:699`) · scripts/check-copy-style.py:408-431 · D139 §1.2, §5.4
- 2026-08-17 · D139 planning lane (locator re-measured by the wave-23 ledger lane) · **P6's tail-exclusion class omits `.`, so the canonical URL ending a sentence is reported as non-canonical.** `PRODUCT_URL` is `https?://[A-Za-z0-9.-]*antseal\.[a-z]+[^\s)\"'<>\],`*]*` — the class excludes whitespace, `)`, `"`, `'`, `<`, `>`, `]`, `,`, a backtick and `*`, and not the full stop. Probed at this filing: `https://antseal.org/` inside a sentence and in parentheses both compare equal; the same URL followed by `.`, `;` or `!` is captured with the punctuation attached and fails the equality against `CANONICAL_URL`. **The constant is `:227`, not the `:199` this was queued as** · scripts/check-copy-style.py:227 · D139 §1.2
- 2026-08-17 · D139 planning lane · **`TODO.md:302`'s *"21 465 bytes byte-identical"* is a character count wearing a byte label.** Measured on the marker-delimited block in `docs/threat-model.md`: **21 465 characters, 21 612 UTF-8 bytes, 371 newlines.** The gap is 147 bytes of em and en dashes. Q12's row is where a later lane would go to learn what the freeze covers, and a lane checking it with `wc -c` disagrees with the row by 147 and can conclude it has found drift · TODO.md:302 · docs/threat-model.md, `<!-- BEGIN/END frozen-security-assumptions -->`
- 2026-08-17 · D139 planning lane (locator re-measured by the wave-23 ledger lane) · **`security-assumptions.md`'s classification reason states a mechanism the check does not use.** The `DOCS_CLASSIFICATION` entry says a finding inside the frozen text is *"unfixable without breaking that drift test"*; the drift test pins the block by **equality between the copies**, not by a digest, so a synchronised edit to both would pass it. The real blocker is Q21's own `Accept` clause and the M0 sign-off, which is a decision and not a mechanism. D139's Edit 3 corrected the wording for the `threat-model.md` entry; the `security-assumptions.md` entry still carries it. **The entry is at `:109-114`, not the `:101-106` this was queued as** — the same central admission edit that moved `check_spellings` moved it · scripts/check-copy-style.py:109-114
- 2026-08-17 · D139 planning lane · **`docs/positioning-copy-style.md` was carrying a live false forward promise into a public flip, in a file that is itself scanned product copy.** §12 item 3 promised the `docs/threat-model.md` `ENG` → `PRODUCT` promotion that D139 §2 R3 **refuted as unbuildable**; §12 item 1 described the refused per-page admission arm and under-named its own set (Q26 and Q27 belong to it, and Q22's README rewrite is not a widening because `README.md` has always been a scan root); §6 read as though every M4 user page becomes a P7 presence surface, which D139 §2 R9 refused on a measurement of **18** P7 findings; and §11 finding 3 went stale the moment Q21 landed. **Corrected centrally in this wave by four dated amendments rather than by rewriting**, with the guide's stale *"Line 14"* locator called out as the reason it now cites markers and section numbers instead of line numbers. Recorded because the interesting half is the venue: the document a wave's lanes read as their normative guide is the one place a stale promise is hardest to see, and it was inside the lint's own scan root the whole time · docs/positioning-copy-style.md §6, §11 finding 3, §12 items 1 and 3
- 2026-08-17 · Q23 lane · **An assertion that cannot fail, because the value under test is produced by the function that produces the expectation.** `crates/antseal-cli/tests/init_command.rs:698-702` asserts that the rendered report `contains` every line of `funding_lines(network, &report.address)` — but `render()` *calls* `funding_lines()` (`crates/antseal-cli/src/init.rs:263`), so the loop compares a function against itself and no wording change can redden it. Independent confirmation of D140 §1.2, found from the test side rather than the render side. Adjacent, found in the same read: `funding_lines` takes `address` and discards it (`init.rs:323`, `let _ = address;`) · crates/antseal-cli/tests/init_command.rs:698-702 · crates/antseal-cli/src/init.rs:251, :263, :323
- 2026-08-17 · Q23 lane · **`docs/testing/error-code-contract.md:362` records both of its numbers from a measurement that has moved.** It reads *"(measured 2026-08-02: 28 class names, zero intersection with the 194-code universe)"*; measured now, `ErrorClass::ALL` is `[ErrorClass; 33]` (`crates/antseal-cli/src/error.rs:327`) and `testdata/error-codes/v1/CODES.txt` carries **248** codes. The **disjointness** the sentence exists to record is unaffected — it is a property, not a count — which is precisely why nothing went red and why both figures drifted unobserved. Sibling of the wave-18 entry on `PINNED_CLASS_COUNT` 29 → 33, from the document side · docs/testing/error-code-contract.md:362 · crates/antseal-cli/src/error.rs:327
- 2026-08-17 · Q23 lane · **The covering test for a dry-run shortfall asserts the class, the code and that nothing was paid, and never that the report printed — which is the whole of why `U79` survived.** `crates/antseal-cli/tests/seal_command.rs:814-858` (`a_dry_run_with_a_drained_wallet_exits_with_the_real_shortfall_code`) checks `err.class()`, `err.exit_code()`, `calls(Method::Pay) == 0` and `calls(Method::FinalizeBatch) == 0`. D49 §Ruling 3 requires the complete report to print *first*; the shipped path returns the error before the render, and this test cannot see the difference. **The product half is the row `U79`; what stays here is that a test naming the right failure can still be silent about the ruling it was written under** · crates/antseal-cli/tests/seal_command.rs:814-858 · U79
- 2026-08-17 · Q248 lane (count re-measured by the wave-23 ledger lane) · **No local gate parses workflow YAML at all, so D124's defect is structurally open rather than fixed.** `git grep -l 'import yaml\|yaml.safe_load\|ruamel'` over `scripts/`, `.github/` and `crates/` returns **nothing**; every script that reads `.github/workflows/` reads it as text, `check-ci-shell.py` by `WORKFLOWS_DIR.glob("*.yml")` and line scanning, `check-ci-paths.py` the same. An unparseable workflow therefore passes `check-ci-shell.py` and `check-ci-paths.py` green — the Q248 lane had to bring its own PyYAML parse to prove its two workflow edits were well-formed, and the D124 colon-space fault it planted is caught by nothing in the tree. **The queued form said "eight tracked scripts read `.github/workflows/`"; a literal-path grep over `scripts/` returns 11, and the two scripts that actually read the directory reach it by `Path` join and are not among them** — the count is not the finding and does not reproduce · scripts/ · .github/workflows/ · D124
- 2026-08-17 · Q248 lane · **PROMOTION — `cargo doc` is run by no lane, so 13 rustdoc warnings sit on the exact surface a crates.io publication would ship, and that becomes `Q249`.** The lane's `cargo doc` (run by hand, to prove a `[D89]` link resolved) surfaced 12 broken intra-doc links in `antseal-net`, six of them `[`crate::evm`]` — a module `ant-backend` gates out of the default build — and one dead relative decision link in `antseal-core`, the only dead one of the five in crate sources and in a **publishable** crate. None of it is visible to any gate. **Rule 8 clause: promoted because the fix must land *with* its lane or the warnings return — the Q27 precedent, that a copy added without its lint is the drift** · minted **Q249** · crates/antseal-core/src/anchor/request.rs:2
- 2026-08-17 · Q248 lane · **`check-copy-style.py`'s P8 reddens the whole gate the moment any lane creates a new `docs/` subtree, which is correct and makes "my lint is red" uninformative for the rest of a parallel docs wave.** `DOCS_CLASSIFICATION`'s own comment states the rule — *"A path in neither is a FAILURE — that is the whole point of the register"* — so the first `docs/user/` page reddens P8 for **every** concurrent lane, none of which owns the register file, and it reddens on the **directory**: measured by the Q23 lane, the pre-admission run scanned 14 files against 17 after, and `--inventory` did not list the new page, so the flagless run before admission is not a weaker check but *no* check of that page. Behaviour worth keeping and worth knowing: during a docs wave the P8 red must be read as "the register has not caught up", and a lane that stops there has not measured its own copy at all · scripts/check-copy-style.py:98-101 · D139 §2 R5
- 2026-08-17 · Q248 lane · **`docs/ci-verification.md:329-330` states a `cargo deny` command that is not the one the lane runs, and the missing word is `licenses`.** The runbook transcribes it as `cargo deny --locked check advisories bans sources` (repeated in the wave-2 evidence table at `:418`); `scripts/ci-lanes.sh:1505` runs `cargo deny --locked check advisories bans sources licenses`. D140 §1.6 swept six sites for stale *labels* over a correct command and missed this one, which states a **wrong command** in the document a contributor runs the lane from. Instrument-class by rule 8 — the CI runbook is apparatus — and the sharpest of the batch, because a reader who copies it gets a green that skips the check Q29 landed · docs/ci-verification.md:329-330, :418 · scripts/ci-lanes.sh:1505
- 2026-08-17 · Q248 lane · **Two `ci.yml` comments describe Q29's licence work in the same phrasing `TODO.md`'s Q10 row uses, so neither tells a reader that the lane skips licences — and the honest reading weakens the case for changing them.** `.github/workflows/ci.yml:69-70` reads *"Q10 owns permanent operation + the Q29 licenses completion"* and `:700` *"permanent operation + licenses completion = Q10"*; `TODO.md:836` (Q10's own row) reads *"extends P13; licenses completed by Q29"*. Left deliberately unexpanded and recorded so the next sweep does not re-derive it as a defect · .github/workflows/ci.yml:69-70, :700 · TODO.md:836
- 2026-08-17 · Q27 lane · **The freeze-boundary check reports a `str` length and calls it bytes, and the gap is 22.** `scripts/check-traceability.py:201` prints `f"… ({len(source_text)} bytes of rule text; checkbox state excluded, Q49)"`; measured through the script's own extractors, the block is **2 175 characters and 2 197 UTF-8 bytes** in all three copies. Same defect class as `TODO.md:302` above and as D139 §1.2's file sizes, in the instrument itself: a lane measuring the block with `wc -c` disagrees with the lint by 22 and can conclude it has found drift where the check says none. Q27's own D139 §1.2-derived figure had the same shape — 2 179 raw against 2 175 stripped, and neither is a byte count · scripts/check-traceability.py:201
- 2026-08-17 · Q27 lane · **The freeze-boundary check's vacuity floor is a literal `2`, and the register it guards now has three entries.** `scripts/check-traceability.py:191-195` fails only when `len(seen) < 2`, so with `BOUNDARY_COPIES` at three the check would stay green on a copy that silently stopped being found — the floor no longer scales with the constant it protects. Nothing is wrong today (all three are found and identical, verified at this filing); the guard against vacuity is simply one copy behind the thing it makes non-vacuous · scripts/check-traceability.py:191-195 · BOUNDARY_COPIES at :69-72
- 2026-08-17 · Q27 lane (remediation state measured by the wave-23 ledger lane) · **Landing a third copy of the freeze boundary made four statements incomplete at once, and every one of them was a count written in prose beside a constant that holds the truth.** The two `FREEZE-BOUNDARY:BEGIN` comments each named only the other copy and ended *"then both copies"*, `scripts/check-traceability.py:75` said *"Both copies are cut from here"*, and Q14's `Accept` named two locations. **Three of the four were corrected in-wave**: the script's comment now says *"Every copy in `BOUNDARY_COPIES`"* with the reason the word went stale recorded at the site (`:75-78`), `docs/format/anchor-artifact-limits.md:115`'s marker comment now names all three and **defers to the constant rather than to its own list**, and `tasks/Q.md`'s paragraph was corrected by the registrar with the correction dated in place. **`tasks/Q.md:166` — Q14's `Accept` row — still names two locations**, and it is the one a freeze gate reads. The transferable half is the fix the other three took: a prose list that can go stale should point at the constant, never restate it · tasks/Q.md:166 · scripts/check-traceability.py:69-72, :75-78
- 2026-08-17 · Q27 lane (locator re-measured by the wave-23 ledger lane) · **`testdata/README.md` describes a lane that no longer exists in that shape.** *"the three-OS CI lane proves byte-identical behavior"* — since D138/Q239 `ci.yml`'s matrix has a single linux arm, with macOS and Windows moved to `cross-os-extended.yml` on a different trigger set. The property the sentence asserts still holds; the mechanism it names does not. **The sentence is at `:41-43`, not the `:43-45` this was queued as** · testdata/README.md:41-43 · .github/workflows/cross-os-extended.yml
- 2026-08-17 · Q26 lane · **A real gap is recorded in shipped rustdoc against a row that does not hold it.** `crates/antseal-anchor/src/tsa.rs:74` says honouring a TSA spacing rule **across** invocations *"needs a persisted last-contact time, which is vault state this crate does not have; recorded as owed work (U49) rather than faked here"*. `U49` (`tasks/U.md:756-763`) is the **resume slot-overwrite** row — a re-run gate writing `tsa-<n>` by index without clearing the area — and says nothing about spacing. So the gap is real, stated honestly, and pointed at the wrong owner; the row that does own cross-invocation pacing is `A137`, minted this wave · crates/antseal-anchor/src/tsa.rs:74 · tasks/U.md:756-763
- 2026-08-17 · Q26 lane (sites re-measured by the wave-23 ledger lane) · **Five sites cite `MVP-SPEC.md` line 108 for the TSA defaults and alternates; line 108 is the OTS bullet and the TSA bullet is line 109.** Measured: `crates/antseal-core/src/anchor/roots/mod.rs:264`, `:274`, `:285`, `:295` and `crates/antseal-core/src/anchor/roots/PROVENANCE.md:95` and `:260`. **Three of the four `mod.rs` numbers in the queued form were wrong** (`:265`, `:279`, `:296`) and **`PROVENANCE.md:95` was missed entirely**, which is the second time in this batch that a sweep of a wrong-citation class under-counted itself. The one site in the directory that cites correctly is `mod.rs:23`, which names line 109 for the intermediates rule · crates/antseal-core/src/anchor/roots/mod.rs:264, :274, :285, :295 · roots/PROVENANCE.md:95, :260
- 2026-08-17 · Q26 lane (range re-measured by the wave-23 ledger lane) · **A quarantine record says *"both"* one line below a list that holds one.** `crates/antseal-core/src/anchor/roots/PROVENANCE.md:260-261` reads *"MVP-SPEC.md line 108 documents **both** TSAs as configurable alternates"* over the `## Quarantine` list at `:258`, which since A26's promotion carries a single entry (`swisssign-signature-services-root-2020-2`). The consequence the paragraph draws is still right and is the one a user meets — configure only a quarantined TSA and A20's gate produces a **hard pre-payment abort** — which is exactly why the count beside it should not be a word. **The claim is at `:260-261`, not the `:259-261` this was queued as** · crates/antseal-core/src/anchor/roots/PROVENANCE.md:258, :260-261
- 2026-08-17 · Q26 lane · **The P4/P5 wrapped-substring class is not a property of the copy lint; it is a property of any substring check over this corpus.** Q26's page-local citation checker — written for one lane, never committed — produced **two** false reds of exactly the D139 §1.2 shape, on citations broken across a source line. Every checker in this tree that locates a claim with `find`/`in` over prose inherits the same two-sided failure: a wrap outside the anchor reddens correct text, a wrap inside it makes the rule silent. Recorded as a class so the next lane writing a private checker plants a wrapped fixture before trusting a zero · scripts/check-copy-style.py:408-431 · D139 §1.2
- 2026-08-17 · Q21 lane · **The P4/P5 wrap defect has three LIVE specimens inside one uncovered file, and they show the whitespace-normalising fix to be necessary but not sufficient.** `crates/antseal-core/src/verify/report.rs:744-745` spells the dictated phrase correctly and wraps it after the em dash — **false red**. `:756-757` is a genuine comma paraphrase wrapped *inside* the anchor — **silently green**. `:1081` is ordinary assertion prose that happens to contain the anchor words — **false positive**, and normalising whitespace does not touch it. Neither `verify/report.rs` nor `verify/orchestration.rs` is in `COPY_SCAN`, which is why the three specimens sat unmet: the sweep that found them ran because a lane went looking, not because a gate did · crates/antseal-core/src/verify/report.rs:744-745, :756-757, :1081
- 2026-08-17 · Q21 lane + Q25 lane · **The comma paraphrase of the receipt class has two homes in shipped doc comments, and both are files the threat model cites as its authority for the class.** `crates/antseal-core/src/verify/report.rs:756-757` and `crates/antseal-core/src/verify/orchestration.rs:473` both spell *"supporting evidence, no independently proven time"* where MVP-SPEC lines 110 and 137 dictate the em dash. It seeded a P4 in the Q21 lane itself when the sentence was quoted into product copy, and Q25 found a third in `bundle/schema.rs` by a whitespace-normalised scan. Guide §10.3 records Rust sources as unscanned, so this is a known blind spot rather than a lint failure — **and the sharp half is the venue**: a maintainer following threat-model §2.3's citations lands on the softened spelling. Handed to `Q28`, whose `Do` already owns the full-corpus sweep · crates/antseal-core/src/verify/report.rs:756-757 · crates/antseal-core/src/verify/orchestration.rs:473
- 2026-08-17 · Q21 lane · **D139 §1.2's file sizes are character counts labelled as bytes — the exact defect D139 §1.3 documents about the frozen block, carried by D139's own header.** Measured against the pre-Q21 blob: `docs/threat-model.md` is **31 767 UTF-8 bytes / 31 547 characters** and `docs/security-assumptions.md` **28 601 / 28 427**; the record prints 31 547 and 28 427 as *"bytes"*. Nothing depends on either figure, which is why it survived a record whose own §1.3 is about this mistake · docs/decisions/D139-m4-documentation-venue-and-copy-lint-admission.md:116, :129
- 2026-08-17 · Q21 lane · **`every_m4_threat_section_is_enumerated` is a heading-presence check, and it passed identically over the stubs for a year.** `crates/antseal-core/tests/security_assumptions_drift.rs:272-315` asserts that `docs/threat-model.md` `contains` each of ten anchor phrases; it was green while every one of those sections said *"not written"* and it is green now that all ten are written. Correct for its stated purpose — Q12 enumerates the sections so a deletion is visible — and it must **never** be cited as evidence that a section has content. The lane that finished the sections is the one that refused to stretch it · crates/antseal-core/tests/security_assumptions_drift.rs:272-315
- 2026-08-17 · Q21 lane · **The copy lint's verdict flipped green → red → green inside one lane's run, under concurrent lanes in a shared tree — D139 §1.5 ORD-2 observed live rather than simulated.** The red was P8: another lane created a `docs/` subtree the register had not yet admitted. So *"the lint passes"* measured during a parallel docs wave is a statement about the minute it was taken, and a lane reporting it without a timestamp is reporting nothing. Sibling of the wave-18 rule that a lane's account of another lane's tree state must be re-measured rather than arbitrated · scripts/check-copy-style.py · D139 §1.5 ORD-2
- 2026-08-17 · Q26 lane + Q21 lane (settled by the wave-23 ledger lane) · **Two lanes reported the same shipped-rustdoc defect at two different lines, and the way to settle it was to re-measure rather than to pick.** `crates/antseal-core/src/anchor/roots/mod.rs` documents `ROOTS_V1` as *"Three roots, admitted under D57 §6's channel rule"* over a **four**-element array whose own test — `pinned_store_is_exactly_four_roots_at_version_1`, 101 lines below at `:360` — asserts four labels by name. Q26 reported it at `:257`, Q21 at `:259`; measured at this filing there is exactly one occurrence in the whole directory and it is **`:259`**. **The defect itself is `R92`/`F56`-class — a shipped rustdoc making a false factual claim about the item it documents, in a publishable crate — and belongs on a row, not here**; what belongs here is the settlement rule, which this project has now needed three times · crates/antseal-core/src/anchor/roots/mod.rs:259, :360
- 2026-08-17 · Q25 lane · **Nothing validates a `path:line` locator on a `docs/user/` page, and locators are the bulk of what those pages offer as evidence.** `CITATION_SCAN` admitted `docs/user` in this wave, so a decision or task **id** that stops resolving on a page a stranger reads now reddens the gate; a **line number** that stops resolving does not, and there is no check anywhere in `scripts/` that resolves one. Q25's page alone carries **76** of them, Q26's **64**. Both lanes wrote a private locator verifier and both found real defects with it on the first pass; **neither verifier is committed**, so the next page starts from nothing. The natural shape is the one `CITATION_SCAN` already has — a directory register plus a suffix filter — with the resolution step reading the cited line rather than the cited id · scripts/check-traceability.py CITATION_SCAN · docs/user/
- 2026-08-17 · Q252 lane (measured independently by the wave-23 ledger lane) · **A fixture that DERIVES its subject from project state is only as safe as that state's floor — and where the project's goal is to drive that state to zero, derive nothing from it: CONSTRUCT the subject.** `check-traceability.py --self-test`'s resolved-with-no-record case built its mutation target by searching `TODO.md` for a decision that was open, allocated, cited as normative, and holding neither a record nor a `DECISIONS_HOMED_ELSEWHERE` entry. Measured: `985ad05^` carried **4** open decision rows and `985ad05` carried **0** — wave 22's M4 round drained the register — so the fixture's search fell through to `raise AssertionError(…)` and the harness died before any case ran. `ci-lanes.sh:979-983`'s `lane_traceability` runs the `--self-test` half **first and returns on its failure**, so a required status context went red on the healthiest state the bookkeeping can reach, and the flagless half that names a real cause never ran. **The sharp half is that the authors had already met this once and stopped one step short**: the pre-repair docstring says the case *"used to name one decision and pin its UNRESOLVED STATE"* and was computed precisely so it would not disarm when that decision resolved — fixing the dependency on **one** decision staying open while keeping the dependency on **some** decision staying open, with the margin recorded in the same breath as *"Three decisions qualify today"*. The register consumed all three in one commit. This is `Q234`'s general form and the rule is the entry, not the incident: **the register's open set, the unticked-row count, the unresolved-decision count — every counter the project exists to drive down — is an unsafe surface for a fixture to stand on.** The repair builds the subject from an id the register does not allocate, which its state cannot reach · scripts/check-traceability.py `self_test` · scripts/ci-lanes.sh:979-983 · Q234
- 2026-08-17 · Q252 lane (probed independently by the wave-23 ledger lane) · **A check's own SUPPRESSION rule can be repurposed as a safe staging mechanism — and the same rule read on the sibling check does the opposite, which is what makes the trick specific rather than general.** `check_decisions` discards any cited id above the register's allocation ceiling (`:778`, `if number > max(allocated): continue`, and `:800` excludes it from the reported distinct count) — the rule that stops the sweep reporting surrogate ids. An above-ceiling decision citation is therefore **visible to the sweep and inert to the verdict**, which lets a two-file defect be staged one half at a time under a harness that applies exactly one mutation to one file: plant the citation before the baseline, and the case arms only when the mutation supplies the row. Probed at this filing on the live tree, planting `**D141**` (ceiling is D140) into an existing swept file: **the entire seven-check output is byte-identical to the baseline**, exit 0 — not merely "no findings", *no difference at all*, since no new file is created either. **The asymmetry is the finding**: the same plant shape one check over is a hard red. `**Q999**` in the same file yields `::error::… [task-citations] Q999 is cited in marked form … and is above domain Q's highest allocated number (253)`, `REAL_EXIT=1`. Above-the-ceiling means *"discard"* for decision ids and *"report"* for task ids, so this staging idiom is available to `check_decisions` alone and a lane that generalises it will redden the gate · scripts/check-traceability.py:778, :800 · task ceiling arm at :1033, :1071
- 2026-08-17 · Q252 lane (both counts recounted by the wave-23 ledger lane) · **Prose counts inside instrument code rot in clusters, and the two found in one file in one wave failed in two different ways — only one of them silently.** `scripts/check-traceability.py` carried `BOUNDARY_SOURCE`'s *"Both copies are cut from here"*, which **had** gone stale unnoticed when Q27 made it three; and the self-test's *"23 of the 32 cases below are red and 9 are green"*, which — recounted by AST against the committed blob — **was exactly right at HEAD (32 cases, 23 red, 9 green)** and was falsified by Q252's own edit in the same act, which replaced the decisions red arm with a red/green pair. **Correcting the queued form: the second was not silent rot but a count a change is obliged to carry and can silently drop, which is the more dangerous of the two because nothing about the edit announces the obligation.** The landed text now reads 23 of 33 red and 10 green, independently recounted here by AST: **33 elements, `{'red': 23, 'green': 10}`**. The sharpest half is the lane's own record of its first attempt: it wrote **24** by arithmetic — the natural error, since a case was added and the added case is red — before recounting and finding the *replaced* arm had been red too. **A count corrected by arithmetic instead of by measurement is the same defect the count represents**, and the comment now instructs its reader to recount rather than to trust it · scripts/check-traceability.py:3076-3078 · :75-78
- 2026-08-17 · orchestrator (wave-23 close), mechanism corrected by the wave-23 ledger lane · **`scripts/verifier-page-build.sh --check` reds exactly once after any commit that moves HEAD, and no reader of the red can tell which of the two stalenesses it is — by design, because the guard refuses the handle that would distinguish them.** The trigger is **not** the footer digest. `stale_guard()` rebuilds and compares the `sha256` of the **module** (`target/wasm-pack/antseal-wasm/antseal_wasm_bg.wasm`) before and after; what puts the commit inside those bytes is `crates/antseal-wasm/build.rs:24,40` emitting `cargo::rustc-env=ANTSEAL_SOURCE_COMMIT` (with `rerun-if-env-changed`) into `build_info.rs:26`'s `pub const SOURCE_COMMIT`, and `scripts/wasm-pack-build.sh:226` defaulting that variable to `git rev-parse HEAD`. Verified without building: `strings` over the cached module returns the literal 40-hex `59c47ad9…afc15`, which **is** HEAD — so the stamp demonstrably lives in the module's data section, exactly where D129 §1 located it by offset and D135 §1.5 diagnosed the same event. The footer digest is a **consequence** the error message lists, not the trigger, and citing it sends a reader to the wrong file. **The ambiguity claim needs the same correction**: `local-gate.sh`'s `run()` tails 25 lines of a failed lane and `stale_guard` returns immediately, so `STALE ARTIFACT`, both digests and *"re-run to package the current module"* **do** reach the log. What a reader cannot tell is **which** staleness — a benign HEAD move that changed only the 40-byte stamp, or a real source change packaged from an old module — because the script's own comment **refuses arm (c)**, *"an mtime or commit-stamp predicate … neither handle sees an uncommitted edit"*. The discriminator exists but is not in the message: `git diff --name-only <prev>..HEAD -- crates/antseal-core/src crates/antseal-wasm Cargo.lock Cargo.toml .cargo rust-toolchain.toml` being **empty** is how D135 §1.5 settled it. R83 working as designed; recorded so the next reader of a `verifier-page FAIL` re-runs and diffs rather than hunting a defect · scripts/verifier-page-build.sh `stale_guard` · crates/antseal-wasm/build.rs:24,40 · crates/antseal-wasm/src/build_info.rs:26 · scripts/wasm-pack-build.sh:226 · D135 §1.5
- 2026-08-18 · secret-guard lane (wave 24), found by the Q30 lane · **A guard widened "as defence" became a false positive on the very artifact class it exists to protect, and the widening is the regression.** `scripts/ci-lanes.sh` rule (4b) was widened by Q245 (`de49206`) from the literal `minisign encrypted secret key` to `(minisign|rsign)([ ]encrypted)?[ ]secret[ ]key`. Every `.minisig` minisign writes carries `untrusted comment: signature from minisign secret key` (upstream `minisign/src/minisign.h` `DEFAULT_COMMENT`; `rust-minisign/src/constants.rs` for rsign), so the widened rule reported **every signature this project will ever ship** — a file that is public by construction — and also ordinary PROSE in any non-`.md` file naming the tool and the key in one sentence. Fixed by ANCHORING on `untrusted comment: ` (`scripts/ci-lanes.sh:1145`): a secret key's comment BEGINS with the tool name, a signature's begins with `signature from`. An exclusion was refused on measurement — `ex()`'s `--exclude` flags apply to all seven rules at once, so `--exclude='*.minisig'` would blind the whole lane and make a real key renamed `foo.minisig` invisible; a fixture now reds BY NAME if a future lane tries that shortcut. Rule (4b) was kept, not removed: over real keys of every form D71 cares about its true positives are a subset of (4c)'s **but for one case** — a key whose body is absent and whose header survives. **Ledger and not a row** under rule 8: nothing a user seals, verifies or restores changes. The promotion argument is recorded and was weighed: `secret-guard` is an unfiltered required context and `sign-release.sh` emits `.minisig` into a `--dir` that will normally sit in the checkout — but promotion exists to SCHEDULE work, and the work landed in the same act, so a row would be minted only to be ticked. `TODO.md`'s Q245 row and `docs/instrument-ledger.md`'s earlier entry both quote the old pattern at the old locator and are corrected by the wave-24 registration and by this entry respectively.
- 2026-08-18 · secret-guard lane (wave 24) · **A lane that `cd`s to its own repo root cannot be exercised against a fixture tree by changing directory, and the run is GREEN for the wrong reason.** `scripts/ci-lanes.sh:64` does `cd "$repo"` derived from `BASH_SOURCE`, so the lane's first reproduction attempt ran from a fixture directory, scanned the real repo, and reported `OK: no vault-export/wallet-key signatures in the tree` with `REAL_EXIT=0` — an assertion that could not fail. A byte-identical copy of the script at `<fixtureroot>/scripts/ci-lanes.sh` is required so `repo` resolves to the fixture root. Propagate to any brief that asks a lane to exercise a `ci-lanes.sh` lane against constructed input.
- 2026-08-18 · Q244 lane (wave 24), predicted by D143 §1.7 and demonstrated in-tree · **A 40-hex-shape rule certifies the ANNOTATED TAG OBJECT, so the shape check alone is an assertion that cannot fail on the one action it matters for.** `gh api repos/Swatinem/rust-cache/git/ref/tags/v2` returns `49a0bdc70d2e1b713ca9e2869b211fcce03d3c1c` — 40 valid lowercase hex, and the WRONG value; the commit is `6323deb102c322ba6fcbdcafc7e3dddab59af2b6`. Disabling `check-action-pins.py`'s rule P2 (ledger equality) turns the whole run **green** over the tag object while P1 (shape) still passes. The discriminator that works is two endpoints, not the `.object.type` field: `git/commits/<sha>` returns 200 only for a commit, `git/tags/<sha>` only for a tag object; every landed pin is 200/404 and the trap value is 404/200.
- 2026-08-18 · Q243 lane (wave 24) · **A `--help` flag that prints its own header by hard-coded line count truncates silently the next time the header grows, and nothing asserts the window still covers it.** `scripts/e2e-devnet.sh`'s `-h|--help` was `sed -n '1,30p'`, bumped to `'1,33p'` when three usage lines were added, with the output verified byte-identical plus the new lines. That verification is a one-off; no check pins it. This project's dominant defect class sitting in a help flag.
- 2026-08-18 · D144 lane (wave 24) · **`scripts/check-copy-style.py --self-test` also stages a full tree copy and belongs on the standing banned-mid-wave list**, beside `scripts/check-traceability.py --self-test` and `scripts/local-gate.sh`. Separately, `scripts/check-ci-paths.py --self-test` **writes to four tracked files** (nine of its arms plant in the tree and restore in a `finally`), so it is unsafe to run while any parallel lane holds the tree. The safe shape is the one `check-anchor-net.py` and now `check-action-pins.py` use: plant in memory, never on disk.
- 2026-08-18 · D144 lane (wave 24) · **A load-bearing belief about what guards `MVP-SPEC.md` was false, and it made the situation look protected when it is not.** `tasks/Q.md:938` (carried into the `Q65` row) states that `scripts/check-traceability.py --matrix` resolves the traceability matrix against `MVP-SPEC.md`'s line numbers and that the lane is a freeze-gate condition. Measured: `check_matrix` mentions `MVP-SPEC` **nowhere** and performs **no file read of its own** — re-verified independently by the orchestrator with an AST-free source parse over the function body. `MVP-SPEC.md` is covered by no digest and no freeze lint (`docs/format/FROZEN.sha256` pins `registry-v1.*` only; `BOUNDARY_COPIES` is three other files). What actually protects it is **1 707 line-number citations**, of which F44 measures that nothing verifies a single one, with line 42 inside six cited ranges. A freeze fails loudly; this fails silently.
- 2026-08-18 · orchestrator (wave 24) · **The `/home/deb` tracked-file figure has now drifted FOUR times and should stop being written as a number.** The series is 5 → 18 files/39 lines → 22/53 → 24/61, the last two one day apart, and Q65 finding 1's whole-tree figures moved 434→441 files and 1 996→2 078 occurrences overnight. D144 §2 replaces the count with a machine check (`--machine-paths`) precisely so no wave re-transcribes a number. Any prose that quotes one of these figures is stale by construction.
- 2026-08-18 · orchestrator (wave 24) · **My own `after:`-graph parser was wrong and I reported a cycle scan run on an incomplete edge set.** It handled comma- and slash-separated lists but not en-dash RANGES, so it missed `Q34 after Q31–Q33` and the `+ R25,R26` continuation on `Q31`. The D141 lane parsed the real graph at 678 rows / 1 279 edges and corrected both. Separately, `TODO.md` rule 7's claim that the `after:` lists are the authoritative build order **"(verified acyclic)"** is false as a claim about a verification: the word `acyclic` occurs **once in the whole tree**, written in `0efdeee` when the list held 219 rows, and nothing has ever parsed an `after` list. D141 §2 R7 strikes the parenthetical; no checker is minted, under rule 8.
- 2026-08-18 · orchestrator (wave-24 close), from the D144 implementation lane's hand-off · **An eighth check exists in `check-traceability.py` and was not registered here.** `--machine-paths` (D144 §2 R6) bans absolute home paths outside the reserved vocabulary `(user, fixture, runner, u, x)` across the `CITATION_SCAN` live-surface partition minus `MVP-SPEC.md` and the checker itself, and pins `MVP-SPEC.md:42` as a registered divergence by **whole-line equality**, asserted present verbatim and exactly once, red in both directions. Nine planted faults, four `::error::` messages that interpolate the vocabulary from the constant so they cannot go stale, and — the arms that matter — `/Users/` and `C:\Users\` cases with **zero live occurrences**, which without their plants would be assertions unable to fail. It counts its OWN failures rather than reading the shared `Failures` object, so its ok line prints even when an earlier check has failed: the pre-existing `if not failures:` idiom in the other seven suppresses every later ok line, which is now more reachable because `check_decisions` can fail early. That suppression defect is recorded and NOT repaired — it edits seven checks and is nobody's subject yet.
- 2026-08-18 · orchestrator (wave 25), from the U78 verification pass · **`check-copy-style.py --inventory` prints a character count and calls it "bytes", and the gap is invisible in ASCII.** The line is `print(f"  {rel}  ({len(text)} bytes)")` (`scripts/check-copy-style.py:981`) over the **decoded string**, so wave 25's new golden `crates/antseal-cli/tests/snapshots/init-report.txt` was reported at **7 161** against **7 281** on disk — 120 bytes of em-dashes and box-drawing characters. Every file the scan reads today is mostly ASCII, which is why nobody has noticed; the mislabel only matters when someone reconciles an inventory figure against `wc -c`, which is exactly what happened here.
- 2026-08-18 · U82 lane (wave 25) · **D149 §2 R9 enumerated three comments its own ruling would falsify, and a fourth sat in the very file R1 edits.** `crates/antseal-cli/src/seal_plan.rs`'s module header claimed *"the checks that run before anything is read"* and *"Every function in this module is pure or `stat`-only"*. Amended in the same act because the file was in scope. R9's own method — *look for a claim about the process rather than about its own function* — predicts exactly this miss, and applying that method to R9's own list is what found it.
- 2026-08-18 · U82 lane (wave 25), pre-existing · **`seal_plan.rs`'s order diagram still lists *"→ the M1 anchor-stage gate (this build cannot anchor yet)"*** while `build_plan`'s own step-3 comment says U22 removed that gate. Falsified by U22, not by D149, so it was deliberately left rather than folded into an unrelated row.
- 2026-08-18 · U79 lane (wave 25) · **D146 §2 R1's mandated signature carries a parameter its own mandated body never reads.** `dry_run_outcome(&consent, &dry, ctx)` takes `ctx: &SealContext` and does not use it, which `-D warnings` makes a hard error; shipped as `_ctx` with the call shape and type preserved exactly as ruled, plus a doc paragraph saying why it is unread. R1 may want amending to drop it — a ruling that mandates both a signature and a body should be checked for agreement between them.
- 2026-08-18 · orchestrator (wave 25), refining a standing rule · **Bracketing the first character of a `pkill -f`/`pgrep -f` pattern is NOT sufficient when the unbracketed literal also appears elsewhere on the same command line.** A cleanup loop whose `for` list contained `'faults3.sh'` was itself matched by `pkill -f "[f]aults3.sh"` and killed its own shell (exit 144). The bracket defeats the *pattern* matching itself; it does nothing about a second, unbracketed occurrence in the same argv. Prefer not using `-f` matching in a script that names its own targets.
- 2026-08-18 · U81 lane (wave 25) · **D147 §2 R4's prescribed sweep returns four hits where the record says five, and the discrepancy is in the record's favour.** The literal `rg -n 'SealError::ProofsExpired' crates/ --glob '!target'` cannot match the definition line, which is spelled `ProofsExpired,` — so §1.10's count of five sites is right about the sites and wrong about what the command finds. Substance confirmed independently: no sixth site exists, and the deletion proceeded as ruled.
- 2026-08-18 · U81 lane (wave 25), re-measured at registration · **`docs/testing/error-code-contract.md` says the code universe *"stays 246"* while the file it describes holds 248.** `grep -vcE '^\s*(#|$)' testdata/error-codes/v1/CODES.txt` → **248**, against `:1286`'s dated census and the corroborating `:1229`/`:1262`. D147 §2 R12 named this as a residual it deliberately leaves open; recorded here so the next reader measures rather than inherits.
- 2026-08-18 · orchestrator (wave 25) · **U79's change moved five locators that other records and rows cite, and one of them is cited by the dated addendum written in the same act.** `seal_consent.rs:441 → :451` (the gate's emit, now inside `show` at `:438-441`), `:832 → :841`, `seal_run.rs:505-521 → :522` plus the new `dry_run_outcome` at `:556-583`, `SealContext::emit :586-594 → :646-654`, `ctx.emit(&notice) :365 → :373`. **D49's own new addendum cites `seal_consent.rs:441`** — verbatim-mandated by D146 §2 R4, so it was applied as written and is now stale by ten lines. Repairing it needs a **second dated line**, not a rewrite of the first (D117's form).
- 2026-08-18 · orchestrator (wave 25) · **Two lanes were killed mid-task by server-side API errors (a 500 and a 529) and a third died four times, and the recovery rule earned its place again: the interrupted tree was run, not believed.** One lane's last recorded intention was *"now restore init.rs and confirm the plant's removal returns T1 to green"* — i.e. a planted fault might have been live at the moment of death. It was not, but that was established by measurement (`git diff`, a `PLANT` sweep, then the suites) rather than by reading the diff and finding it tidy. The docs lane's own gate captures survived on disk in the scratchpad and were read back from files instead of re-run, which is the same principle from the other side: **evidence written to a file outlives the agent that produced it, and a transcript does not.**
- 2026-08-19 · orchestrator (wave 26) · **Protocol rule 7's own precedent for `with`/`before` cites two rows, and one of them never carried either word.** Rule 7 (written by D141 §2 R7 in `2cdf0fb`) says its ordering vocabulary is "wider than `after`" because "`with`/`before` already sit in the ordering position at `TODO.md:433` and `:482`". Measured **at `2cdf0fb`, the commit that wrote the claim**: `:433` was **P26**, whose clause is `— with a planted-fault self-test (M2, XS)` — a `with` governing the *deliverable*, not a co-timing edge; and `:482` was **A50**, clause `— after A3`, carrying **neither** word. The claim itself is TRUE — a parse of all 685 rows finds real instances at `R33` (`after R8, before Q14`), `U38` (`after U1; before U11's wizard half`) and `U84` (`before U32`) — but **none of them is what rule 7 cites**, and both locators have since drifted a line (P26 → `:434`, A50 → `:483`). A locator that goes stale within one wave of being written is the argument for citing rows by **id**; same class as the 1 707 unverified line-number citations into `MVP-SPEC.md` that F44 measures · TODO.md rule 7 · D141 §2 R7
- 2026-08-19 · orchestrator (wave 26), from the readiness survey · **`U77`'s ordering clause named its own successor as a predecessor, and under rule 7 that is authoritative.** The row read `— after U30, U29/U72, U32` while `tasks/U.md`'s `Deps` said U32 is "the freeze this must **precede**". Rule 7 makes the checkbox line the authoritative build order, so as written the row was blocked by a row it must precede — a two-row cycle that no parse would report, because `D141`'s graph only reads `after`. Corrected in the same wave to `before U32`; `U84`'s row already used the correct form, so the vocabulary was present in both the file and the rule. **The general shape**: a `Deps` gloss and a checkbox clause that disagree are not redundant copies, and only one of them is authoritative · TODO.md `U77` · tasks/U.md
- 2026-08-19 · U84 lane (wave 26) · **A ruling's enumerated plant table had two of six rows green, including the one the row's own `Accept` names by hand.** D151 §2 R10's **T1** — delete the export-side refusal — does not redden the test R10 designed, because the **mandatory D47 self-verify** (`export.rs:961-973`) re-reads the written file through full import-side validation, so `validate_payload`'s *own* wrap refusal fires and `export_vault` returns `ExportSelfVerifyFailed` rather than `Usage`: `is_ok()` is false in **both** worlds and only the *reason* changes. **T6** is green for a different reason — the rendered copy is a join of **three** producers and `contains` is an OR, so emptying one hides behind the others; partial **T4** likewise. Repaired by asserting the refusal's **verdict** per class (which is what R1 itself said the test does), then re-planted red by message. **Two propagating rules**: a plant table is evidence only once **each row has been run**, and an `is_ok()` assertion over a value with **two failure paths** tests the boolean, not the claim the copy was written against. This also falsifies D151 §4 item 4, which calls `validate_payload`'s guard "read-side-only" — it is reachable in the **write** direction, and that reachability is precisely what hid T1 · docs/decisions/D151-vault-export-backup-advice.md §2 R10, §4 item 4
- 2026-08-19 · U85 lane (wave 26) · **A ruling's tamper matrix named a plant that its target test cannot see, and the record stated the very fact that explains why.** D152 §2 R9 said "drop `size > 0` from R3 → (c) disagrees **in both callers at once**" would redden **T6**, the differential against `build_plan`. Executed, T6 stays **green** (`REAL_EXIT=0`): a differential whose two sides both read the *shared* predicate cannot observe a change to it — the "both callers at once" clause is the reason it is invisible, not the reason it is caught. The plant reddens **T4** instead. **Propagate to briefs**: when a plant lands in code that both sides of a differential share, name the test that does **not** share it · docs/decisions/D152-resume-hint-honesty.md §2 R9
- 2026-08-19 · U85 lane (wave 26) · **A biconditional derived from two field names was false, because the two fields count different domains.** D152 §1.6 argued `fine_tree_present` is false for exactly two reasons and that "the size" separates them. But `CanonDescriptor::describe_file` is handed the **`size` field** — canonical bytes for text (spec line 98) — while the separating term is the **raw** count. A lone-BOM file is raw 3 / canonical 0, so `fine_tree_present == false` with **no glob involved**. Read literally, `list` would have told the owner of a **resumable** work that it was dead — the exact false-positive direction the same record refuses the cheap trigger for. Found by building the differential row the record's own §4 parks for "a future lane". **Propagate**: when two records say "size", check which domain each means · crates/antseal-core/src/content/descriptor.rs · crates/antseal-core/src/content/assemble.rs
- 2026-08-19 · U77 lane (wave 26) · **A committed red-direction proof was itself vacuous, and only meta-testing the control found it.** U77's guard plants an interactive read into a synthetic `fn verify` on every run and requires the scanner to report it. The assertion was `expect_err` — which passes on a synthetic tree **regardless of the plant**, because the real inventory rows are all absent there and the scanner errs about a missing file instead. Caught by re-running the control with a **token-free** plant and observing it still pass; fixed with an explicit found-key assertion, reason recorded at the site. A proof that fires every run is still only as good as the thing it asserts · crates/antseal-cli/tests/verify_command.rs:1560-1573
- 2026-08-19 · Q22 lane (wave 26) · **A ruling specified a path that does not resolve from the file it governs, because the record and the file sit at different depths.** D150 §2 R3 item 3 spells the README's between-markers pointer as `[…](../signing/verifying-a-release.md)`; from `README.md` at the repo root `test -f` **FAILS**, while `docs/signing/verifying-a-release.md` passes. The record lives under `docs/`, so its relative frame is `docs/`, and the href was written in that frame. Landed as the resolving path with the deviation recorded. **Nothing in the tree validates a README markdown link** — `doc_pointer_liveness.rs` is rustdoc-only and `check-traceability.py`'s eight checks include no such check — so this was decidable only because R5 independently required `test -f`. **Baseline banked for any future checker**: all 23 relative link targets in the finished README swept 2026-08-19, **0 dead** · docs/decisions/D150-q22-release-docs-preconditions.md §2 R3
- 2026-08-19 · Q22 lane (wave 26) · **`check-copy-style.py` offers no way to run its self-test arms without staging the tree, so under the mid-wave staging ban the arms are unrunnable.** `--help` is not a flag (`usage: check-copy-style.py [--self-test | --inventory]`, exit 2); `--self-test` stages a full tree copy and `--inventory` runs no arms. The lane had to reconstruct the judgment block out-of-process against the live module to prove its new arm both reds and restores. It also proved the **restore obligation** load-bearing by planting a register leak — and its first attempt at that plant came back green, because the harness's own `restage()` cleared the register unconditionally, so the branch under test could not fail. Candidate: an arm-selection flag, or a stage-free P7 path · scripts/check-copy-style.py
- 2026-08-19 · Q22 lane (wave 26) · **Two documents narrate the `OWED_PRESENCE` register's contents in prose, and nothing mechanical compares them to it.** `docs/positioning-copy-style.md` §6 ("Two debts are registered today") and §11 items 1–2, and `docs/threat-model.md`'s "Delegated" paragraph ("the copy lint currently carries it as a named debt"), all describe a register that is now **empty** — and emptying it stayed green, because the guide's prose has no mechanical reader. Same shape as the count-drift class. Both files were outside the emptying lane's write scope · docs/positioning-copy-style.md · docs/threat-model.md
- 2026-08-19 · Q251 lane (wave 26) · **Three of eighteen re-measured locators were wrong the day they were written — none of them had rotted.** `crates/antseal-cli/src/error.rs:375` cited for exit 14 is an unrelated `ALL`-array entry (exit 14 is `:401`); `kdf.rs:288` cited as "the Argon2id arm" is the first range guard *inside* it (the arm begins `:283`); and D151's `D47:250-255` for §Spec-conformance is the D43-amendment discussion (§Spec conformance is `:172-178`, the sentence `:176`). This is the argument that decided the collection's citation style: a durable document adopting them would have published three wrong pointers as its first act, so shipped behaviour is named **by symbol** and the dated `path:line` form stays in the register, where it is a timestamped claim about one tree state · docs/testing/verification-matrix.md §"MVP-SPEC.md line 143"
- 2026-08-19 · Q240 lane (wave 26) · **A planted fault reddened the wrong assertion first, and the diagnostic the next maintainer needs was shadowed.** The rounding plant tripped a *sanity* assertion that fired before the rounding-specific message, so the failure said nothing about rounding. The test was restructured and re-run under the same fault to confirm the intended diagnostic actually surfaces. **A message the lane cannot see is a message the next maintainer will not get either** — planting proves a test *can* fail, and reading the message proves it fails *usefully* · crates/antseal-net/tests/allowance_policy.rs
- 2026-08-19 · Q240 lane (wave 26), correcting the orchestrator · **`U256::MAX` does not trip `[task-citations]`; a bare `` `U256` `` does.** The orchestrator relayed that the 256-bit type would be read as task id U256 and proposed a `TASK_ID_NOT_A_CITATION` entry. Measured against the real regex (`TASK_MARKED`), the id must be the **entire** content of the span: `` `U256::MAX` `` → no match, `U256::MAX` → no match, `` `U256` `` → match, `**U256**` → match. Additionally `docs/threat-model.md` is **not in `CITATION_SCAN` at all**, so EVM prose there is free. The two real hits were bare `` `U256` `` doc spans written the same wave, fixed by rewording. **No checker defect and no suppression entry** — the marked-form rule is doing exactly the job its own comment describes · scripts/check-traceability.py `TASK_MARKED`
- 2026-08-19 · U84 lane (wave 26) · **`rustfmt` invalidates locators written before it.** Two of fourteen `path:line` references the lane wrote into `docs/user/vault-loss.md` were stale by the time the tree settled, because formatting moved the lines after the prose was written. Reading each cited line back — rather than trusting the value captured while editing — is what caught them. Propagate to any lane that writes locators into docs in the same act as it edits code · docs/user/vault-loss.md
- 2026-08-19 · Q250 lane (wave 26) · **§2.3's growth rotted two locators that cite it from outside, and neither has a mechanical reader.** `docs/threat-model.md` §2.3 grew 185 → 313 lines, moving §2.10 from `:1899` to `:2027`. `TODO.md`'s R95 row and `tasks/R.md:1212` both cite `docs/threat-model.md:1958` for §2.10's M4 cross-check — now **`:2086`**; `docs/instrument-ledger.md:146` records the unwrapped P4 phrase at `:699` — now **`:703`**. Checked and unaffected: D73, D40/D42/D50, D139, D151 and the Q14 freeze-gate plan, all citing lines above 692 · docs/threat-model.md
- 2026-08-19 · D154's lane (wave 27) · **The tracker's only proposed ordering checker reports four cycles today and all four are parser artefacts — a decision id (`D-A8`), an inline consumer annotation (`(R11 consumes)`), rule 7's own example token (`U13(interface)`) and a prose sentence beginning "— after".** Measured over 838 rows / 1 393 `after` edges. Two further candidates were measured and refused: `Deps ⊆ after` yields **290 rows / 573 ids** on day one, and `Accept`-names-a-successor yields **62** (6 open, 1 real) and still misses the defect that raised the question, because Q65's `Accept` row 3 names a **file** rather than a row id. **No checker is minted; D141 §2 R7's refusal stands.** Separately measured and repaired by D154 §2 R1: **D141 §2 R5's four `TODO.md` edits were never executed** — the same corrections landed in `tasks/Q.md:2994/3009/3024` — so `Q243` and `Q244` are both `[x]` while the authoritative order records them `after` an open `Q65` · TODO.md rule 7 · D141 §2 R5/R7 · D154
- 2026-08-19 · D153's lane (wave 27) · **`README.md`'s `minisign-public-key` marker pair has two readers and no test.** D150 §2 R3 item 3 mandated the anchor as the stable locator §5 step 1 writes into, and `maintainer-key-procedure.md` §5 step 1 now points at it — but nothing asserts the pair exists, is well formed, or is still paired. The idiom's precedent (`docs/threat-model.md`'s `<!-- BEGIN frozen-security-assumptions -->`) is pinned by the freeze-boundary lint; this one is pinned by nothing, so a rename or an accidental deletion is silent until the key act fails · README.md:50,54 · D150 §2 R3 · D153 §4.3
- 2026-08-19 · D153's lane (wave 27) · **Nothing in the corpus can detect a status claim written outside `maintainer-key-procedure.md` §7.** D153 §2 R2 establishes that execution status lives in exactly one place and every other page points at it — the rule that stops the next state change repeating this wave's four false statements. It is enforced today only by that record: no check, lint or test can see a second page asserting what has or has not been run. The failure mode is silent and recurs on every maintainer act · docs/signing/**  · D153 §2 R2 · §5 item 20
- 2026-08-19 · U87's lane (wave 27) · **`machine-paths` is blind to committed `.txt` snapshots and reports green while one leaks.** `CITATION_SUFFIXES` (`scripts/check-traceability.py:676`) omits `.txt` and the walk at `:2067` filters on it, while `crates` IS a scan root — so the suffix set alone is the exclusion. Measured by plant: an absolute home path appended to a committed snapshot left the check printing *"ok — 0 absolute home paths … across 455 live file(s)"* with no check naming the file. **All 13 golden `.txt` files under `crates/**/tests/snapshots/` are outside the guard** that exists to stop a real `/home/<user>/` path shipping at the Q65 flip. Re-measured by the registrar 2026-08-19: **no live leak** — every path in all 13 is `/home/user`, inside the reserved vocabulary — so the guard is asleep and the room is clean. This is why U78's Accept row 2 had to demand a two-run proof by hand · scripts/check-traceability.py machine-paths · U78 · D144
- 2026-08-19 · U86's lane (wave 27) · **A ruling's redness table and its code sketch disagreed, and the sketch made the ruling's own mandatory plant unfalsifiable.** D155 §2 R8 ordered the D47 verdict guard ahead of the claim assertion, so under T1 the guard fired first and step 4 — the only assertion encoding U86's claim — never ran, degenerating the test to the bare `!contains()` form the record itself forbids. Reconciled by planting, not by transcribing · docs/decisions/D155 §2 R8 · tests/vault_keyfile.rs
- 2026-08-19 · U86's lane (wave 27) · **A specified test double could not fire.** D155 §2 R8's `NeverPrompts` was to panic if any prompt was reached, but its prescribed call (`machine: true`, no passphrase fd) answers `PassphraseUnavailable { NoChannel }` **before any `InitPrompt` method is called**, so the panic never happened and a different assertion caught the plant. Found only by planting T4 and reading *which* assertion fired. **A double is evidence only once you have watched it panic** · docs/decisions/D155 §2 R8 · tests/vault_keyfile.rs
- 2026-08-19 · U86's lane (wave 27) · **A wording-assertion census stopped four lines short and declared the field clear.** D155 §2 R5 item 2 measured `tests/vault_export.rs:520` and concluded *"No other test asserts either message's wording … Nothing else greps"*; `:524-527` asserted `contains("vault export")` and went red on the reword. **A census of who asserts a string must grep the string, not read the neighbourhood of the variant match** · docs/decisions/D155 §2 R5 · tests/vault_export.rs:524
- 2026-08-19 · U86's lane (wave 27) · **`check-copy-style.py`'s P1/P3 disclaimer heuristic structurally exempts refusal copy.** `DISCLAIMER` (`:242-246`) admits `refused|refuses|no|not|never|cannot|without`, so a refusal message satisfies the disclaimer test **with its own refusal verb** — a planted *"the notary refuses to import over the existing vault at …"* passed **green**. Any refusal string may therefore carry `notary` or `authorship` unchecked. Scan *reach* is fine (proved separately by a P6 plant naming `cli-errors.display.txt:14`); this is a standing blind spot in the rule, not in the walk · scripts/check-copy-style.py DISCLAIMER
- 2026-08-19 · U86's lane (wave 27) · **Two class-blind refusals sat in two frozen machine contracts and were exercised by no test at all** before this act — the freeze was their entire coverage, so any change to what they *claimed* (as opposed to how they read) was unobservable. Both gates now have behavioural tests that hold the claim against the named command's measured verdict per wrap class · D155 §4.5 · tests/vault_keyfile.rs
- 2026-08-22 · orchestrator (wave 28) · **The FIFTH count drift, and the largest — three surfaces disagreed and one of them was the sentence promising they do not.** The domain table summed to **685** against a parse of **691** (`R` 83/84, `U` 83/86, `Q` 236/238) — six rows accumulated silently through waves 25, 26 and 27 — which also falsified the header's own claim that *"the domain table below is the SAME parse rather than a second one"*. The M4 heading read *"690 rows total (689 live + 1 struck)"* against 691, while its other two figures were right. And three script figures quoted **in the present tense** (*reports*, *gives*, *now reads*) were a wave-26 measurement carried through two later registrations. Found by running the parse, not by reading. **Nothing in the eight checks compares the domain table to the row count** — the gap named at wave 15 and still open. Corrected at the wave-28 registration against a parse cross-checked with `check-traceability.py`'s independent count.
- 2026-08-22 · D156's lane (wave 28) · **Rule 7's two ordering exemplars were line numbers that had drifted, and they appear in no decision record.** `TODO.md:433` is a blank line and `:482` is `A40`; a grep over D154 returns neither number, so the registrar composed them. Replaced with **row ids** (`R32 — before Q14`, `A38 — before A5,A11`, `P11 — with C11`, `A7 — with A6`), all four re-verified against the tree at registration. A third site carried the same two locators — the `D141` register row — and was annotated rather than rewritten, since it records what D141 ruled.
- 2026-08-22 · D156's lane (wave 28) · **An ordering run is MIXED-VOCABULARY, and a parser that does not terminate on `before` manufactures phantom edges.** `U78` reads `— after U11/U18, before U32`: one comma-separated run carrying both words. This lane's own first parser produced **seven** phantom `after` edges from it, **every one pointing at `U32`, the release freeze** — reproducing wave 27's defect inside the record that was written to prevent it. Rule 7 gained the sentence.
- 2026-08-22 · Q31's lane (wave 28), correcting D156 · **A retraction landed on two surfaces of three, and the ruling then read the third.** `R26`'s `STANDING DEBT` was discharged 2026-08-16 — run `31873422229`, recorded at `tasks/R.md` and in `docs/ci-verification.md` — but the sentence survived on `TODO.md`, which is the surface D156 §2 R8 read, so it ruled a timing (`before Q34`) onto a debt that did not exist. Corrected at the wave-28 registration on all four rows that cited it. **The surviving rule is the claim's shape, not a debt**: `ANTSEAL_SOURCE_COMMIT` is itself a compile input, so the honest form is *"reproducible at the commit it was deployed from"*, never *"from HEAD"* — a checklist row demanding a HEAD match would be a row that can never go green.
- 2026-08-22 · D157's lane (wave 28) · **`check-traceability.py` suppresses GREEN evidence whenever the run is red, on six of its eight checks.** `matrix`, `decisions`, `task-citations`, `task-entries`, `decision-owners` and `decision-index` all guard their ok line on the **global** failure accumulator rather than their own contribution; only `machine-paths` uses the correct per-check form and `freeze-boundary` guards on its own condition. Reproduced twice: in a red run, three passing checks printed **nothing at all** — neither ok nor error. This is the mirror of an assertion that cannot fail: a pass that cannot be seen, precisely when a reader most needs to know which checks held. `Q235` recorded the shape; this is its re-measurement with the per-check count.
- 2026-08-22 · Q31's lane (wave 28) · **The publish-flip checklist's own action-pin count is short by eleven, and its regex commits the mirror of the bug its closing sentence warns about.** `docs/ci-verification.md`'s step A5 reports *"46 `uses:` invocations"* and instructs the reader to verify by running its command; the pattern `'(^|- )uses: \S+'` admits no leading whitespace on the `^` branch, so it misses every indented `uses:` without a list dash. Measured now: **57** invocations, **57** pinned, **0** unpinned — A5's conclusion survives, its count does not, and `Q244`'s original 57 was right.
- 2026-08-22 · Q31's lane (wave 28) · **`scripts/sign-release.sh:97` states a prohibition; it does not enforce one.** The line is prose inside the usage heredoc. The script's only mechanical refusal is the `-W` unencrypted-key check reading the key struct's KDF field; there is no automated host check, so *"the secret key must never be on a CI runner"* is a human attestation and the release checklist now marks it as one.
- 2026-08-22 · U76's lane (wave 28) · **Cargo's fail-fast made untested targets look green, and absence of a `FAILED` line was read as a pass.** A plant run showed the integration suite green when it had **never been run** — the bin target failed first and cargo stopped. Re-run with `--no-fail-fast`; every figure re-derived. The same lane then found a rule-6 defect in its own test: `read_salt(...).expect_err(...)` **renders the `Ok` value into the panic message**, printing all 32 secret salt bytes precisely when the guard is broken. Replaced with a `let Err(e) = … else { panic!(…) }` binding.
- 2026-08-22 · Q256's lane (wave 28) · **A planted fault that failed to apply produced a green run, and only the message caught it.** The plant's escaping matched zero times, so the "planted" run measured the unmodified program and returned `REAL_EXIT=0`. A nonzero exit is not proof a plant was found; **a zero exit is not proof a plant was absent**. Re-applied by line-targeted replacement. The same lane measured that the brief's `1857` and its own `1812` are both correct and count different things — D145's figure is **total** non-blank lines, the wave-28 survey's is **unique** — and that under a blanket-hex plant the *pre-existing* redaction arm stays green, because its only must-survive token is a 40-hex address that a 64-hex filter never touches.
- 2026-08-22 · D157's lane (wave 28) · **A ruling's own prescribed text falsifies the acceptance check the same ruling specifies.** D157 §2 R3's re-specified `Q259` `Accept` row 1 greps for `vault passphrase is`, expecting 0; R1's Site A wraps so that `passphrase` and `is` land adjacent, producing that exact string as a **contrast** sentence. Rows 1 and 2 cannot both be satisfied by one tree — satisfying row 1 means re-wrapping Site A, which breaks row 2's verbatim requirement. The lane refused to re-wrap, on the ground that bending a ruled artifact to make an acceptance green is the inversion this project names. The pattern is the registrar's to tighten. Two further locator defects in the same record: §5 item 4's line range is off by one at both ends, and R3 row 1's §7 coordinates are pre-edit.
- 2026-08-22 · D160's lane / R96's lane (wave 28) · **Two decision records cite locators that are wrong, and one census stopped six sites short.** `D155` §4.1/§2 R2 give `default_keyfile_path` as `:654-662`; it is `:655-663` (the wave-28 brief said `:650-663`, also wrong). `D73` §7 item 2 cites `verify/report.rs:283` for `signature_scheme`; the field is at `:299`. And D160's first, function-scoped wording census returned **12** sites where a parse of the whole corpus returns **18** in 6 files — `init_command.rs:998-999` are reachable only through the real binary.
- 2026-08-22 · orchestrator (wave 29) · **`check-traceability.py` now runs NINE checks, and the arrival of the ninth invalidates three entries above — recorded here rather than by editing them, per this file's append-only rule.** D159 §2 R1 was RESOLVED at wave 28 and **never executed**; wave 29 executed it, so the count moved **8 → 9** (`doc-links`). (a) The **2026-08-19 entry (wave 26, Q22's lane)** says *"nothing in the tree validates a README markdown link — `doc_pointer_liveness.rs` is rustdoc-only and `check-traceability.py`'s eight checks include no such check"* and banks a *"baseline for any future checker"*: **that checker now exists, and the banked baseline has been drawn on** — the check validated the 23 README links Q22's lane wrote in the same wave. (b) The **2026-08-22 entry on suppressed GREEN evidence** reads *"six of its eight checks"*: the **numerator 6 still holds** — `doc-links` uses the correct per-check form and does not join the suppressed set — only the denominator moved. (c) The **wave-28 count-drift entry**'s *"nothing in the eight checks compares the domain table to the row count"* is **still true of all nine**; the gap named at wave 15 remains open, and only its denominator is stale. · `scripts/check-traceability.py`; `docs/decisions/D159-relative-link-check-and-the-gated-module-links.md` §2 R1
- 2026-08-22 · D159's implementing lane (wave 29) · **A ruling's own "one for one" estimate understates its exclusion by two orders of magnitude, and the refusal it supports is far better founded than it claims.** D159 §1.8 justifies restricting `doc-links` to `.md` targets on the ground that it excludes exactly **one** non-`.md` relative link. Measured at execution: dropping the `.md` restriction takes the corpus **53 → 338** links and produces **279 findings**, of which **266 are rustdoc intra-doc item paths** written `[text](crate::Foo)` — not file links at all. §1.8's "one" is correct only for *path-shaped* targets (**2** today). The ruling's conclusion stands; its arithmetic was off by 278. · `docs/decisions/D159-…` §1.8, §3 item 7
- 2026-08-22 · D159's implementing lane (wave 29) · **Reference-style link definitions are outside the frozen `doc-links` subject, and one of them is a fifth `crates/*/src/**` link the ruling's own four-row census does not list.** R1 froze the subject as the inline `](TARGET)` form; three reference-style definitions exist and all resolve — `crates/antseal-net/src/wallet.rs:52`, `crates/antseal-cli/tests/seal_plan_conflict.rs:40`, `crates/antseal-cli/tests/common/spawn.rs:83`. The subject was **not** widened (R1 froze it deliberately) and the floors are unaffected, but D159 §1.1's *"exactly four"* census is a count of one link **form**, not of relative `.md` links under crate sources. · `docs/decisions/D159-…` §1.1, §2 R1
- 2026-08-22 · orchestrator (wave 29) · **`DOC_LINK_TOTAL_FLOOR`'s ruled headroom was consumed by another lane in the same wave, and the constant was deliberately NOT raised.** D159 §2 R1 set the floor at **45** against a measured **49**, calling the headroom *"exactly four — the size of the subject"*. Q22's lane grew `README.md` from 19 to 23 links mid-wave, so the corpus is **53** and the headroom is now eight. Raising a ruled constant is a decision act, not an implementer's convenience (the `FUZZ_BUDGET_CEILING_MINUTES` idiom), so **45 stands** and what it still buys was re-measured instead: `README.md` + `CONTRIBUTING.md` alone yield **39**, so a walk that collapses to the two root literals still reds. The trap the headroom targeted is owned by `DOC_LINK_CRATES_SRC_FLOOR`, which remains at the boundary. · `scripts/check-traceability.py`
- 2026-08-22 · orchestrator (wave 29) · **`CHANGELOG.md` names a per-wave venue that has been dormant for seven waves, and the surface that replaced it is a commit message no checker reads.** `CHANGELOG.md:6-8` says *"the per-wave record of how the project got here lives in **`TODO.md`'s change log** and in `docs/decisions/`"*. Measured: `TODO.md`'s `## Change log` newest entry is **2026-08-17, "Wave 22's registration"** — a scan of that section for *"wave 23"*–*"wave 28"* returns **nothing**. The de-facto per-wave record for waves 23–29 is the **Current focus** block plus the wave **commit message** (`bebc850` *"Wave 28: …"*, `5c8d1ca` *"Wave 27: …"*, and predecessors). Wave 29 followed the six-wave-old convention (Current focus) rather than the documented one, deliberately and for consistency; **the divergence is recorded here rather than resolved**, because choosing between the two venues is a decision and no row owns it. Relevant to `docs/ci-verification.md` **A7**, which instructs *"write that sentence into the wave record"* — an instruction whose named venue a reader would find six waves stale. · `CHANGELOG.md:6-8`; `TODO.md` `## Change log`; `docs/ci-verification.md` A7
- 2026-08-22 · orchestrator (wave 29) · **`scripts/e2e-devnet.sh --plan` prints `PASS — the D52 gate is discharged for this commit` on a run that executes nothing.** Measured verbatim: `e2e-devnet: PASS commit=bebc850 nodes=14 suites_run=4 failed=[] pending=[] secs=0 anvil=1.5.1-stable evidence=(plan only — nothing executed)`, followed by the unqualified human-facing line *"PASS — the D52 gate is discharged for this commit."* Three of those fields are the ones a reader checks for honesty and **all three read green from a no-op**: `suites_run=4` counts suites *registered*, not run; `failed=[]` and `pending=[]` are empty because nothing was attempted; `secs=0` and the `evidence=` field are the only disclosures, and neither is in the sentence that states the verdict. This is the project's dominant defect class (a green that nothing reachable could redden) sitting on the **one gate whose whole purpose is that it must actually boot a devnet** — and D52's own trigger list ends *"a change to the gate is a change the gate must survive"*. `commit=` is also a **label**: it prints HEAD while the suite runs the working tree, the same trap recorded for `local-gate.sh:270`. **Not promoted to a row** under protocol rule 8 — it blocks no acceptance today, and wave 29 answered the gate by **running the real suite** rather than by reading a plan. Promote if `Q32` (script the M4 gate) or `Q34` adopts `--plan` output as evidence. · `scripts/e2e-devnet.sh`; `CONTRIBUTING.md` "Devnet E2E gate (D52)"
- 2026-08-22 · D158's lane (wave 28), written by the wave-30 registrar · **`tasks/Q.md` and `TODO.md` both cite `D72:730-737` as "§6"; it is §5 item 1.** `## 5.` is at `D72:728` and `## 6.` at `:755`, with item 1 at `:757-763` — so the cited span sits in the section before the one named. Recorded per D158 §2 R9 item 1. **This line is two waves late**: D158 resolved at wave 28 and its edit set named the ledger; nothing wrote it until D162 measured the gap. · `tasks/Q.md` `### Q241`; `TODO.md` `Q241` row; `docs/decisions/D72-…`
- 2026-08-22 · D158's lane (wave 28), written by the wave-30 registrar · **`tasks/Q.md`'s `Q241` entry cited `docs/decisions/D19-advisory-lane.md:51-53` as co-author of the wildcard collision, and that passage is evidence AGAINST it.** `D19:51-53` records `allow-wildcard-paths = true` — the very setting that exempts all nine dev edges — so the citation asserted the opposite of what its target says. Corrected at source in this wave per D158 §2 R9 item 2. · `tasks/Q.md` `### Q241` `- Problem:`
- 2026-08-22 · D158's lane (wave 28), written by the wave-30 registrar · **D72 §2 R8 reason 4's *"`cargo install antseal` would also be an unsigned acquisition path"* does not describe this tree.** No workspace member is named `antseal`; the reserved crates.io name maps to no crate, and the binary called `antseal` is produced by `antseal-cli` (`crates/antseal-cli/Cargo.toml:20`), so `cargo install antseal` today installs the empty placeholder. **The reasoning survives — a second acquisition path is still refused — but the mechanism named is not the live one**, and the naming gap belongs to whoever pays §2 R8's review date. Recorded per D158 §2 R9 item 3; the record itself is resolved and is corrected by citation, not by editing. · `docs/decisions/D72-…` §2 R8
- 2026-08-22 · D162 (wave 30) · **A resolved ruling's edit set went unexecuted for two waves because its `with`-chain's ordering root sat in a write scope no lane held.** D158 §4 item 10 was written `with` 1–9 **and `after` item 11**; item 11 was `Cargo.toml`, assigned to *"whichever lane holds the manifest write scope this wave"* — a lane never dispatched, so **the chain could not start**. Item 12 executed **alone**, in violation of its own `with`, because `decision-index` reds without it. Measured cohort: waves 26/27/29 executed **3/3, 3/3, 1/1**; **wave 28 ruled five records and executed two**. **48 of 155 records carry an edit set.** The fix is protocol, not mechanism — rule 3 now says *resolved is not executed*, and an item assigned to *"whichever lane holds"* a scope is assigned to nobody. · `docs/decisions/D158-…` §4; `TODO.md` rule 3
- 2026-08-22 · D162 (wave 30) · **A record's *"Mandatory, not optional"* edit landed only on the half a checker could see, and the two halves are one paragraph apart.** D160 §5.3 names two edits under that heading: one that `check_decision_owners` reds without (the row must name `D160`) and one it cannot see (a verbatim headline replacement). **The checked half landed; the unchecked half did not**, leaving `U88`'s row asserting a headline its own closing note refutes ~4 000 characters later, with `check-traceability.py` **9/9 green** over it. Executed in wave 30 by D162 §4 items 16–17. **This is evidence for a weaker claim than *checkers force execution*, which D162 §1.4 falsified 15–1**: a checker is sufficient, never necessary. · `docs/decisions/D160-…` §5.3(a), §5.4; `TODO.md` `U88` row; `tasks/U.md` `### U88`
- 2026-08-22 · D162 (wave 30) · **A decision record's own line-number locators drifted +10 before any lane read them, and D154's row-id-plus-quoted-string form was available and not used.** D158 §4 anchored eleven of thirteen registrar edits to `tasks/Q.md:2986-2989` and `TODO.md:837`; `Q241`'s entry had moved to `:2987` by the time wave 30 executed them. **The class then reproduced inside this wave**: D162 §4 item 1's own "explicitly NOT edited" locators `Cargo.toml:64/70/76` became `:70/76/82` the moment the item landed, because the replacement comment grew from 4 lines to 10. D162 §2 R5 now rules that edits execute by row-id plus quoted string; line numbers are a convenience. · `docs/decisions/D158-…` §4; `docs/decisions/D162-…` §2 R5; `Cargo.toml`
- 2026-08-22 · D162 (wave 30) · **`Q249` carried an `OWED` marker and its own `EXECUTED` note on one line, unstruck — protocol rule 1 clause (b) breached in the wave that read D156 on what a box and its annotations each mean.** The row read *"Ruling landed; the check is OWED and is the next act on this row"* immediately beside `✅ 2026-08-22 — D159 §2 R1/R2/R5 EXECUTED`, so one line asserted the check both owed and delivered. Retracted in wave 30 per D162 §2 R9; the check had in fact landed as `doc-links`. · `TODO.md` `Q249` row
- 2026-08-22 · D163 (wave 30) · **`Q71`'s problem statement rests on a *"branch protection 403s on this plan"* premise that measurement has retired.** D141 §2 R8 measured `gh api repos/aed900/antseal/rulesets` returning **`200 []`** where it once returned 403, and `branches/main/protection` returning **404 "Branch not protected"** — an absence, not a plan refusal; the account is on **Pro**. Rule 8: a problem statement about the apparatus takes no row. The same stale premise was carried by `Q238`'s `Accept` row 3 parenthetical, which **is** on a row and was corrected there in this wave. · `tasks/Q.md` `### Q71`; `docs/ci-verification.md` "Branch protection is BLOCKED BY PLAN"
- 2026-08-22 · D163 (wave 30) · **D156 §2 R7's `TODO.md:98` locator now resolves to `:99` — the SEVENTH drift of its class, and the reason this wave's edit lists name the Maintainer-actions block by heading instead.** The block is addressed by the line beginning *"**Maintainer actions** (all external)"*, which is stable; its line number is not, moving with every registration paragraph inserted above it. · `docs/decisions/D156-…` §2 R7; `TODO.md` `## Current focus`
- 2026-08-22 · orchestrator (wave 30) · **Two of D162 §4's own post-edit verification predicates are unsatisfiable without damaging correct text, and both were caught by executing them rather than reading them.** (1) Item 1's `grep -c 'path deps included' Cargo.toml → 0` also matches `Cargo.toml:53`, an unrelated and **true** sentence about where versions are declared; the discriminating predicate is `grep -c 'rejects wildcard' → 0`, measured 1 → 0. (2) Item 16's `grep -c 'no backup route mentioned anywhere' TODO.md tasks/U.md → 0 0` cannot reach 0 on `TODO.md`, because `U88`'s closing note **quotes the false headline in order to refute it** (*"The row claimed …; six shipped surfaces already state"*) — text D160 §5.3(b) authored deliberately. The predicate's intent is met: the only surviving occurrence is a quotation under refutation. **A verification predicate is itself an assertion and can be wrong**; neither was narrowed to fit. · `docs/decisions/D162-…` §4
- 2026-08-22 · Q263's lane (wave 30) · **A planted fault found two failure classes collapsed under one assertion, in a check written in the same act.** The `secret-guard` deny-by-default arm asserted `grep -F "FAIL $p: expected IGNORED"` — a string the *wrong-source* failure message also satisfies — so neutering the no-pattern branch left the check **GREEN** (`REAL_EXIT=0`) where it should have reddened. Split into two greps (`"expected IGNORED ("` then `"but NO pattern matches it"`); the re-run reds naming `.env` and `.env.local`. **Found only because every arm was planted, including the arms of the check itself.** Also load-bearing there: `git check-ignore --no-index`, since by default a **tracked** path reports as not-ignored whatever the patterns say, which would have made every `visible` row unfailable. · `scripts/ci-lanes.sh` `secret-guard`
- 2026-08-22 · Q261's lane (wave 30) · **A new check's unregistered-quote detector was unreachable from its own self-test — an assertion that could not fail, found by neutering it.** `run()`'s detector for §11 exceptions that match no §10 row was extracted as `unused_exceptions()` and given both a red arm and a green control. The same lane caught a neuter that **failed to apply** (an unexported shell variable) and returned `0`, which would have read as *plant not caught*; the apply-assertion now lives inside the plant. **`custody-log` runs in no CI job**: it is registered in `LANES` and dispatchable locally, but `.github/workflows/**` was outside the lane's write scope, so nothing invokes it — owed to whoever holds that scope. Relatedly `docs/decisions/D131:423` quotes the old 12-lane `LANES=` string as a historical measurement and is now stale by one. · `scripts/check-custody-log.py`; `scripts/ci-lanes.sh`; `.github/workflows/`
- 2026-08-22 · orchestrator (wave 30) · **A lane died to the 600-second watchdog having written nothing, and the tree proved it rather than the report.** The first `decision-ledger-debt` lane stopped mid-task; `git diff --stat scripts/check-traceability.py` was **empty** and `grep -c decision-ledger-debt` was **0**, so there was no complete-looking residue to mistake for progress. It was relaunched with a staged brief ordering early incremental writes to the real file instead of assembling the whole check in scratch. **An interrupted lane's tree is measured, never read.** · `scripts/check-traceability.py`
- 2026-08-22 · Q262's lane (wave 30) · **A lane ran one command its brief forbade and disclosed it rather than burying it.** Reaching the guard's key-plant arm it ran `dig +short TXT antseal.org` — read-only DNS, no publish and no write, but outside the *no network commands* instruction it was given. Recorded because a disclosed deviation is the cheap case and an undisclosed one is not; the lane's other refusals in the same report (six of seven plants staged rather than live, because the target files were outside its write scope) are the same discipline working. · wave-30 lane briefs
- 2026-08-22 · orchestrator (wave 30) · **The tracker header and wave-29's registration prose say *nine checks* and *63/63*; both are stale as of this wave.** `check-traceability.py` now runs **ten** (`decision-ledger-debt` joined) and `--self-test` reports **70/70**. Corrected in the wave-30 registration rather than by rewriting wave 29's block, which was true when written. **The substantive claim under the stale figure survives all ten**: nothing compares the domain table's per-domain cells to the parsed row count, the gap named at wave 15 and still open. · `TODO.md` `## M4` heading; `scripts/check-traceability.py`
- 2026-08-22 · orchestrator (wave 30) · **Widening `.gitignore` silently armed a build-bricking classifier, and only the gate could see it.** `Q263` added 24 deny-by-default patterns; `crates/antseal-core/tests/vector_runner.rs`'s `vector_gitignore_expected_names_cannot_brick_the_build` reddened because a file matching any of them under `testdata/vectors/v<n>/` would be **unclassifiable and would FAIL THE BUILD** — the Q140 defect, which is precisely *two rules that disagree about whether a file is expected*. The lane's own checks were all green: `git check-ignore` probes 7/7, `git ls-files -i -c` = 0, `secret-guard` self-test 21 probes with 18 plants. **The coupling is invisible from either side** — the `.gitignore` editor has no reason to read a vector-runner test, and the vector runner has no reason to be consulted when a safety pattern is added. Resolved by registering all 24 in `GITIGNORE_PATTERNS_DELIBERATELY_FATAL`, which is the arm the guard's own doc comment reserves for patterns that exist because the file is DANGEROUS. **The guard is working as designed; the entry records that a safety edit has a second reader nobody names.** · `crates/antseal-core/tests/vector_runner.rs`; `.gitignore`
- 2026-08-22 · orchestrator (wave 30) · **A new checker script entered the tree unclassified, and `check-ci-paths`'s R4e control is what noticed.** `Q261` added `scripts/check-custody-log.py` and named it from `scripts/ci-lanes.sh`; the edge sat in neither `FOLLOW_EDGES` nor `UNREACHED_EDGES`, which the check reds on by design because *leaving it unclassified means this check silently guesses at the reader set it is asserting over*. Measured: **zero** invocations across `.github/**` and `scripts/local-gate.sh`, so `UNREACHED` is the honest classification — following it would have counted the script's reads of `docs/signing/key-custody.md` toward the `DOCS_ONLY` exclusions **on the strength of a reader that never runs**. Registered with the residue stated in the entry itself: it moves to `FOLLOW_EDGES` in the same commit that wires the lane into a job. **The lane had disclosed this in its own report and the disclosure changed nothing until the gate failed on it** — which is the argument for the gate, not against the lane. · `scripts/check-ci-paths.py`; `scripts/ci-lanes.sh`; `scripts/check-custody-log.py`
- 2026-08-22 · orchestrator (wave 30) · **`verifier-page`'s STALE ARTIFACT guard fired a second consecutive wave, on a wave that touched no page source.** `target/wasm-pack/antseal-wasm` did not match a rebuild from this tree (`was: 74373b09…`, `is: 573b49cc…`), so the page, its CSP hashes and the footer digest the page publishes **about itself** all described an artifact the sources do not produce. Wave 29 recorded the same failure from a wave that DID touch the wasm surface; this wave's diff is CLI and net crates plus tracker prose, which means **the staleness is not caused by touching the page — it is a property of a stale build directory persisting across waves**. The guard rebuilds and corrects, so the cost is one re-run; the entry exists because *a page that publishes a digest about itself is the one artifact where a stale build is a false statement rather than a slow build*. · `scripts/verifier-page-build.sh`; `target/wasm-pack/antseal-wasm`
- 2026-08-22 · orchestrator (wave 30) · **A planted fault returned `REAL_EXIT=101` and proved nothing, because cargo exits 101 for a compile error and for a failing test alike.** Verifying `U89` after its lane died, the obvious plant — reverting `from_env_file_optional_wallet` to `from_env_file` — did not compile (`E0308`: `CliDevnetEnv` is `DevnetEnv<Option<SecretBuf>>`, the ten-key constructor returns `DevnetEnv<SecretBuf>`). Read by exit status alone that 101 is indistinguishable from a caught plant, and banking it would have recorded a **false proof that the tests detect the defect**. The second plant reproduced the defect *behaviourally* — a walletless export resolving to `NoDefinition` — and was caught by MESSAGE: `a nine-key devnet selects a network: NoDefinition(DevnetEnvironmentRequired)`, `7 passed; 1 failed`. **Generalisable: a plant that changes types is not a plant, it is a compile check.** The plant must be the smallest edit that makes the CODE wrong while keeping it buildable. · `crates/antseal-cli/src/devnet_env.rs`
- 2026-08-27 · D164 planning round (wave 31) · **The sentence four surfaces rest on to defer a ship-path obligation names no mechanism, and the file it lives in contradicts it twice.** `docs/ci-verification.md:3835` says a required status context that has never reported green on `main` *"blocks the next push, including the maintainer's own"* — quoted at `docs/decisions/D163-…:179` and load-bearing under D163 §1.6, D163 §2 R4, Maintainer actions entry (8) and `Q238`'s replaced `Accept` row 3. The same file prescribes `"enforce_admins": false` at `:277` and states at `:292-294` that it *"leaves the repo admin an explicit bypass for emergencies"*, and again at `:1216` that it *"leaves the admin a bypass regardless"*. Classic protection with that flag does **not** block an admin push; a **ruleset** has no implicit admin bypass — and the register never distinguishes the two mechanisms anywhere. So *"arming early is actively unsafe"* is **UNPROVEN**, which removed the reason to defer without supplying a reason to hurry. **The propagating rule: an unqualified claim quoted onto four surfaces is not four witnesses, it is one claim with three copies** · `docs/decisions/D164-the-ordering-term-that-named-the-wrong-event.md` §2 R2
- 2026-08-27 · orchestrator (wave 31) · **A flip-checklist step that reads a pin count was blind to 11 of 57 invocations, noticed the discrepancy, and blamed the row that was right.** Runbook step **A5**'s `grep -rhoE '(^|- )uses: \S+'` anchors on line-start-or-`- `, so every **bare-indented** `uses:` was invisible: `verifier-page.yml:124,138`, `pages.yml:85` (`actions/deploy-pages`, the highest blast radius in the tree), `ci.yml:677,694,723`, `fuzz-nightly.yml:93,121,166`, `devnet-e2e-cron.yml:247`, `advisory-cron.yml:63`. All eleven are pinned, so the step **read green by luck**. Its own prose then named the *mirror-image* bug (*"`^\s*uses:` alone silently misses every `- uses:` step"*) while carrying the inverse, and explained the 46-vs-57 gap against `check-action-pins.py` as `Q244`'s row being stale — **the row was right**. Its fault plant used the `- uses:` form only, so the blind spot was **un-plantable by construction**. Corrected to `'^[[:space:]]*-?[[:space:]]*uses: \S+'`: 57 seen, 57 pinned, 0 unpinned, agreeing with the checker · `docs/ci-verification.md` A5
- 2026-08-27 · history-delta lane (wave 31) · **`printf … | grep -q` under `set -o pipefail` turns a SUCCESSFUL lookup into the failure branch, intermittently, and the construction is live in `secret-guard`'s self-test.** `grep -q` exits the instant it matches, `printf` takes `SIGPIPE`, and the pipeline status becomes the signal — so the branch taken depends on whether the writer had finished, which it usually has. Found because the new `scrub-history.sh` self-test failed on it before it ever passed. **The same shape is in `scripts/ci-lanes.sh`'s `secret-guard` self-test, in the `missing`/`unexpected` loops** — flagged, not touched, being outside that lane's write scope. A self-test that can spuriously red is a self-test nobody will trust the third time · `scripts/ci-lanes.sh`, `scripts/scrub-history.sh`
- 2026-08-27 · Q35 lane (wave 31) · **A new depth-1 `docs/*.md` page gets zero link, citation and machine-path checking, and the lane proved it rather than assuming its green.** Depth-1 `docs/` is in no `CITATION_SCAN` root, so `task-citations`, `machine-paths` and `doc-links` never read `docs/success-metric.md`. The lane planted a deliberately dead relative link, watched `doc-links` stay green at `REAL_EXIT=0`, restored byte-identically, and then verified all 13 links by hand with `test -f`. **A green that never read the file is not evidence about the file** — the same gap D131 §5 R7.4 and D139 §2 R1 closed for `verifier-web` and `docs/user`, still open one directory up · `scripts/check-traceability.py` `CITATION_SCAN`
- 2026-08-27 · check-ci-paths lane (wave 31) · **A guard would have accepted a witness that can never testify, and it was found only because fixing R3 forced the same predicate to be read twice.** R6's witness set was `"push" in events and not (paths or paths-ignore)`, so a **tag-only** release workflow producing `traceability` would have *satisfied* R6 — a docs-push guard witnessed by a workflow that no docs push can ever run. Latent today (no workflow is tag-triggered) and armed the moment `Q31` lands. Fixed in the same act as R3, with its own planted arm; the plant's red names the rule that did not fire, because an exit status alone would have passed it · `scripts/check-ci-paths.py` R6
- 2026-08-27 · U32 survey lane (wave 31) · **A test hand-maintains an enumeration axis beside the constant that exists to be one, under a false completeness comment.** `crates/antseal-cli/src/machine.rs:159-160` documents `ALL_COMMAND_NAMES` as *"the harness's enumeration axis and the fixture registry's key set"*; `crates/antseal-cli/tests/secret_hygiene.rs:501` ignores it for its own `[&[&str]; 9]` labelled *"Every command the surface has, with a minimal valid argv."* An eleventh command would join `ALL_COMMAND_NAMES` and the harness would stay green without scanning it. **The content gap — `init` unscanned — is `U32`'s and is closed this wave; the MECHANISM gap is nobody's**, which is why it is here · `crates/antseal-cli/tests/secret_hygiene.rs`
- 2026-08-27 · orchestrator (wave 31) · **A workflow comment described an unfixed state for five days after the fix landed, in the present tense, naming its own fix as future work.** `.github/workflows/devnet-e2e-cron.yml:233-238` said six of eight evidence files *"are duplicated VERBATIM into the job log"* and that fixing it at the write *"is Q256"*. `Q256` landed `capture_through() { redact -u | tee "$1"; }` on 2026-08-22 (`scripts/e2e-devnet.sh:140`, used at `:349` and `:366`), and `--self-test` now asserts the function is defined exactly once and that no capture forks outside it. `Q256`'s tick note handed the comment over (*"For the registrar and not done here"*) and a grep found that hand-off in **exactly one place — that ticked row's own note** — and not in this ledger either. **The D162 class verbatim: a write scope assigned to nobody.** Corrected, and the step name at `:132` now names the arm `Q256` added · `.github/workflows/devnet-e2e-cron.yml`
- 2026-08-27 · check-ci-paths lane (wave 31) · **Five count assertions in prose went stale in one commit, and nothing mechanical can red on any of them.** `scripts/check-ci-paths.py` moved from 9 rules / 18 planted arms to **10 rules / 23 arms / 1 green arm / 8 classifier cases / 3 refusal cases**, and the surfaces that state the old figures are `TODO.md`'s `Q239` row, `TODO.md`'s `D138` register row, `docs/decisions/README.md:156`, `docs/decisions/D138-…md`, and `scripts/lib/red-arm.sh:83` (*"eighteen arms requires its own tag"*). **No mechanical count assertion exists between the script and any of them**, which is why this is a ledger line and not a red lane — and why the same class has now drifted six times · `scripts/check-ci-paths.py`
- 2026-08-27 · orchestrator (wave 31) · **A green gate needs TWO runs after any change to the wasm build inputs, and nothing tells a wave planner that.** `verifier-page`'s STALE ARTIFACT guard fired for the **third consecutive wave** — this time legitimately, because `D158 §2 R4`'s ninth manifest line is `antseal-wasm/Cargo.toml:113` and the packaged module predated it. The guard **rebuilds and then refuses**, printing *"The rebuild has corrected it; re-run to package the current module"* — a deliberate choice to fail loudly rather than self-heal silently, and the right one. The consequence is not written anywhere a planner reads: **any wave touching a wasm build input will fail its first gate and pass its second**, so a lane that budgets one gate run has mis-budgeted by a full run (~18 min here, of which the devnet E2E is ~460 s). Worth stating in the gate's own header beside the `ANTSEAL_GATE_*` opt-ins rather than being rediscovered · `scripts/verifier-page-build.sh`, `scripts/local-gate.sh`
- 2026-08-27 · orchestrator (wave 31) · **A wrapper's exit status reported the opposite of the gate's, and only the recorded value was true.** The gate was invoked as `./scripts/local-gate.sh > log 2>&1; echo "GATE_REAL_EXIT=$?" | tee file`; the harness reported the command as **exit code 0**, because the shell's status came from `tee`. The value inside the file was **1**, and the gate was genuinely red with `verifier-page` FAIL. **A pipeline's status is its LAST element's**, so any `… | tee` wrapper silently discards the status it was written to preserve. This register already refuses to read `cargo`'s 101 as a test failure and refuses `--plan`'s green fields as a run; this is the same class one layer out — **read the recorded `REAL_EXIT`, never the wrapper's**. `set -o pipefail` or `${PIPESTATUS[0]}` is the fix where a pipe is wanted at all · `scripts/local-gate.sh` invocation convention
- 2026-08-27 · orchestrator (wave 31, post-registration) · **A line-level exclusion in a redaction sweep hid two real hits on the line it excluded, and the sweep reported CLEAN.** Verifying a personal-identifier redaction, the check was `grep -rnI -E '<patterns>' . | grep -v 'substituta'` — the exclusion existing because `substitutable` contains the mail provider's name as a substring. `grep -v` filters **LINES**, and this register's lines run to thousands of characters: one benign `substitutable` in a `tasks/Q.md` note suppressed **six** real occurrences on that same line, including the account name the redaction existed to remove. The sweep printed nothing and was believed. Caught only because an independent walk of the historical blobs enumerated string forms the worktree sweep had said were absent. **The propagating rule: an exclusion must operate at the same granularity as the thing it excludes.** A match-level sweep (`grep -o` over the pattern union, then count by form) cannot hide a same-line hit, and is what every later verification here used. This is the *assertions-that-cannot-fail* class wearing a new coat: the predicate could not report the defect it was written to find · redaction verification, `tasks/Q.md`
- 2026-08-27 · orchestrator (wave 31 follow-up) · **The personal-data guard's first act was to catch its author breaking the rule the same commit had just written.** `Q269` landed `scripts/check-personal-data.py`, `.githooks/pre-commit` and a `CONTRIBUTING.md` section stating the rule — *name the class, never the literal* — and the very same commit spelled the accepted home path **twice**, in `TODO.md`'s new row and `tasks/Q.md`'s new Notes. The guard reported `[HOME-CEILING] … grew to 133 … against an accepted ceiling of 131` and refused. **The commit had already landed**, because `core.hooksPath` had not yet been pointed at `.githooks` — the guard existed, the hook existed, and neither was armed, which is precisely the gap `CONTRIBUTING.md` now opens with a one-line install command. Fixed by de-literalising the two sites rather than by raising the ceiling: **raising a ceiling to accommodate the text that describes the ceiling is how an accepted exposure becomes an unbounded one.** Three lessons compound here and all three are the same lesson — a guard that names its subject leaks it (its own docstring, fixed earlier), a self-test that plants literals leaks them (fixed earlier), and a row that documents a guard leaks what the guard forbids · `scripts/check-personal-data.py`, `Q269`
- 2026-08-28 · orchestrator (wave 31 follow-up) · **A force-push removed the identifiers from the branch and left a working index to them in plain sight, and `clone --mirror` reported CLEAN throughout.** After `+ c2709ea...68d7a88 main -> main (forced update)`, a fresh `git clone --mirror` of the remote scanned to **0** occurrences — the obvious predicate, and the wrong one. GitHub does not delete unreferenced objects on a force-push: `gh api repos/aed900/antseal/commits/496f1a6…` still returns **8** occurrences of the identifier and the file at that ref returns **3**. They are unreachable from any ref, invisible to `clone`, and **fetchable by SHA**. **The compounding half is the finding**: the rewritten history *publishes those SHAs* — `496f1a6` in 3 tracked files, `1830618` in 6, `4cae455` in 5 — because the records documenting the disclosure cite the old commits as evidence of when it entered. A public reader would not have to guess a 40-hex value; the repository hands over the pointer and the pointer resolves. **The propagating rule: when a removal's success predicate is *"the thing I can reach is clean"*, verify by the route an adversary would take, not the route the tool offers.** The closing predicate here is a **404** from a direct-SHA fetch, obtainable only from GitHub Support's garbage collection · `Q268`
- 2026-08-29 · orchestrator (wave 32) · **`cargo update --dry-run` UNDER-REPORTS, and a whole row's refusal had been priced on it.** `Q267`'s recorded refusal said the minimal advisory fix "also moves" a set of Windows-target crates. Re-measured on an isolated `git archive HEAD` copy: `--dry-run` prints **one** changed package; the **real** run changes **32 diff lines** — the dry run is not a preview of the write, it is a different question. But the ripple is also not what it was read as: `[[package]]` count is **772 before and 772 after** and the affected crate's version *set* is **identical** on both sides, so it is an **edge repoint among versions already resolved**, not a downgrade and not a new dependency. Two lessons, opposite directions: *an "is this minimal?" judgement resting on `--dry-run` is an assertion that cannot fail*, and *a diff line count is not a graph change*. The fix that landed touches **two** lines. Pointer: `Q267`'s tick note.
- 2026-08-29 · orchestrator (wave 32) · **A NON-RECURSIVE `ls` reported two 200–400 line documents absent, and a survey lane concluded from it that a ship-path row's `Do` was "measurably unbuilt".** The command was `ls docs/ | grep -iE 'release|version'`; the files were one directory down at `docs/user/`, **225 and 403 lines**, landed four waves earlier in `0ae2c96`. The wave's lean — write the missing artifacts — would have re-created work already in the tree. This is the `machine-paths` lesson in a new venue: **the instrument answered a different question than the one asked, and its silence read as absence**. A negative result from a non-recursive listing is not evidence about a tree. Pointer: `D166` §0, `Q31`'s wave-32 note.
- 2026-08-29 · orchestrator (wave 32) · **A `--depth 1` clone SAILS THROUGH a history scanner's reachability invariant, and the summary line it prints is textually identical to a real one.** Measured on a real shallow clone of this repository: `objects=1384 commit=1`, `reachable=1384 unreachable=0` — the arithmetic **balances**, so the existing invariant had nothing to complain about, and the script printed `mode=full` with a confident verdict over **1 of 718 commits**. All 24 `actions/checkout` steps in the 8 workflows run at the default `fetch-depth: 1`, so wiring the scanner into CI as it stood would have produced a permanent, cheerful, meaningless green. **An invariant that balances over a truncated corpus is not an invariant over the corpus you meant.** Fixed under `D165` §2 R6 by refusing a shallow store outright — keyed on shallowness, never on `commit=1`, which is legitimate for a new repository. Pointer: `Q266`'s tick note.
- 2026-08-29 · orchestrator (wave 32) · **A green `cargo build --locked --workspace` never compiled the crate whose version the wave had just changed, and the green was real.** After bumping one transitive crate in `Cargo.lock`, the workspace build exited **0** — while `Compiling <that crate>` appeared **nowhere** in the log and its two build fingerprints were **eleven days old**. The cause was not a stale artifact: under **default features the crate is not in the graph at all** (`cargo tree -e normal -i <crate> --workspace` → *package ID specification did not match any packages*). The honest run needed `--all-features`, which did compile it and stamped a **new** fingerprint. **A build that succeeds is evidence about the graph it built, not about the change you made** — and the two can be disjoint. Discriminator, cheap and worth making routine: grep the log for the `Compiling <crate>` line and check the fingerprint mtime before believing a build verified a dependency change. Pointer: `Q267`'s tick note.
- 2026-08-29 · D165 implementation lane (wave 32) · **The `printf … | grep -q` SIGPIPE race recurred in four new sites inside the very file being written to document it — and one of them would have failed OPEN.** `grep -q` exits on first match and closes the pipe, so `printf` can take SIGPIPE and the pipeline reports **141**. Three of the four were baseline-field validators, which fail loudly; the fourth was the anti-vacuity suppression guard itself, where a spurious 141 would have **silently disabled the check that stops the baseline degenerating into a marker allowlist** — a guard that fails open is worse than no guard, because it reports PASS. Caught only because a real run printed a validator error naming a field value that was plainly in the accepted list. All four are here-strings now. The 2026-08-27 entry recorded this class; **recording a hazard does not immunise the next file against it**, and the next file was this one.
- 2026-08-29 · orchestrator (wave 32) · **The gate's heavy-tier selector and the D52 devnet-E2E trigger are described as one path list and are not, and the divergence decides whether a lane runs.** `scripts/local-gate.sh`'s heavy-features comment says the tier fires on *"the same storage-touching path list the D52 devnet E2E gate uses"*. Measured: `scripts/gate-features.sh`'s `HEAVY_TRIGGER_PATHS` carries **`Cargo.toml` and `Cargo.lock` wholesale**, while `CONTRIBUTING.md`'s D52 list names **a pin move in four specific crates** (`ant-core`, `ant-protocol`, `alloy`, `evmlib`) and no lockfile at all. This wave changed **only** `Cargo.lock`, so heavy fired and `e2e-devnet` printed `SKIP` — two instruments reaching opposite verdicts on one diff while one of them claims to be the other. Neither is wrong for its own purpose; the **comment** is, and a reader answering the SKIP by trusting it would answer it backwards. The SKIP was resolved by running the lane, not by reading the comment.
- 2026-08-29 · orchestrator (wave 32) · **An archive exclusion matched at one depth while the measurement it was checked against matched at every depth, and the two disagreed by a factor of nine.** The wave-close backup was written with `tar --exclude='./target'` and sized against `du -sh --exclude=target`. `du`'s pattern matches a component **at any depth** and so silently dropped `fuzz/target/` as well; `tar`'s anchored pattern dropped only the top-level one. Expected ~53 MB, got **463 MB** — 400 MB of rebuildable fuzz build artifacts, including a 74 MB rlib and 60 MB incremental dep-graphs. Caught only because the size was implausible and was checked against the estimate instead of accepted. Rebuilt with `--exclude='*/target'`: **26.5 MB, 2 539 members, zero `/target/` members**. This is the wave-31 ruling restated for a third venue — *an exclusion must operate at the same granularity as the thing it excludes* — and the twist worth keeping is that **the estimate and the archive used different tools whose flags look identical and are not**. The archive was then verified by **extraction and recursive `diff -r` against the live tree** (`DIFF_REAL_EXIT=0`, zero differences), not by trusting the write, because a `cmp` straight after a copy can be answered from the page cache.
- 2026-09-02 · orchestrator (wave 33) · **A closing predicate ruled a status code the endpoint does not use for the gone-state, and reading the verdict by code alone would have kept a satisfied row open.** `Q268`'s closing predicate, written 2026-08-27, requires *"a 404"* from `gh api repos/aed900/antseal/commits/496f1a60…`. Measured 2026-09-02 after GitHub's Support GC ran: that endpoint answers a well-formed missing SHA with **HTTP 422** *"No commit found for SHA"* — byte-identical to a never-existed control SHA fetched the same minute — while the literal 404 appears on the neighbouring contents-at-ref and tarball routes. The predicate's INTENT (the object is no longer served) is what closed the row; the code it named was an unmeasured prediction about GitHub semantics, the same class as D162 §4's unsatisfiable post-edit predicates. Rule applied forward: a closing predicate that names a status code names the expected message too, and the reader closes on message plus a live control — never the code alone. The controls ran first for the mirrored reason: on a private repository an auth failure also reads as not-found, so a 404/422 without a 200 control proves nothing.
- 2026-09-03 · orchestrator (wave 33) · **A history rewrite silently killed a checker's pinned baseline, and the red sat invisible for six days because the check ran in no ritual — the exact residue shape its own row had recorded.** `check-custody-log.py` pins `docs/signing/key-custody.md`'s birth commit and reds on drift rather than re-baselining. The 2026-08-27 identifier rewrite (72 commits) rewrote every SHA in its range, so the pin stopped existing and the check went red BY DESIGN — but `custody-log` ran in no CI job and no local-gate step (`Q261`'s registered residue, "dispatchable-by-hand only"), so the first eyes on the red were a wave-33 planning sweep. Repaired by DELIBERATE re-derivation (sole `--diff-filter=A` commit; drift arm re-proven red on a planted pin after the fix, verified by its message) and wired into `local-gate.sh` in the same commit, green on arrival per D158. The general form: **a SHA pin is an input to every history rewrite, and a rewrite's edit list must enumerate SHA pins** — the one-command sweep is a 40-hex grep over `scripts/` (measured here: 1 live pin, the broken one; 3 fixture literals excluded by inspection). Composes with the wave-31 lesson that local backups are "anywhere": the enumeration surface of a rewrite is every consumer of the old SHAs, and checkers join backups and removal records on that list.
- 2026-09-03 · orchestrator (wave 33) · **`e2e-devnet.sh --plan` prints `PASS`, `pending=[]` and "the D52 gate is discharged for this commit" with nothing executed — a confident verdict textually identical to a full run's, on the gate's own most-quoted line.** Found by comparing the new `e2e-sepolia.sh`'s plan output against its sibling's convention: the new lane had faithfully copied the defect. The only truthful fields on the plan line are `secs=0` and `evidence=(plan only — nothing executed)` — the standing reader rule (*read `secs=`/`evidence=`, never the verdict word*) is what has kept this from biting, and D165 reddened exactly this shape in the history scanner the same week. The NEW driver diverges deliberately: its plan states **`PLAN`** with `stages_planned=9 stages_run=0`, so a grep for `e2e-sepolia: PASS` can never match a plan-only invocation. `e2e-devnet.sh` is left untouched THIS wave with a number behind the refusal: editing it is itself a D52 trigger (*a change to the gate is a change the gate must survive* — a ~13-minute mandatory run) and the defect sits behind a guarded reader rule. OWED: the same PLAN-state fix in `e2e-devnet.sh`'s verdict emitter. OWNER: the next lane that touches any D52 trigger surface. TIMING: with that wave's mandatory `e2e-devnet` run, so the fix and its witness land together.
- 2026-09-03 · orchestrator (wave 33, post-registration) · **A recorded remedy was priced as a payment the maintainer cannot make, when the actual remedy is free — the CI billing gate clears by going PUBLIC, not by paying.** The register (Maintainer action 11, D166 §2 R6, ci-verification.md) had the hosted-CI refusal DIAGNOSED correctly (cause = billing on a private repo) but its REMEDY wrong: *raise the spending limit / clear the failed payment*, and the ship-sequence note *"separable, clear it first, cheaper"*. Corrected at the maintainer's word + a docs check (docs.github.com: **standard GitHub-hosted runners are free and unlimited on PUBLIC repositories**; measured here: all 19 jobs use `ubuntu`/`macos`/`windows-latest`, 0 larger/self-hosted). The remedy is **the public flip itself**, at $0, and the hosted-witness cascade unblocks as a free side-effect of the flip rather than a paid prerequisite before it — sequencing inverted from billing→flip to flip→(free Actions). The general form: **a diagnosis can be right while the remedy attached to it is wrong; a remedy that assumes a cost is itself an assertion, and one this project could not read (the token lacks `user` scope to see billing) — so it went four waves as "clear billing first" without anyone pricing the free path.** The failed-payment disjunct is the only residual: watched by a single signal — Actions still refusing AFTER the flip.
- 2026-09-03 · orchestrator (wave 33, post-registration) · **The billing block's monthly reset was OBSERVED, not just predicted — the private free-minute allotment refilled around Sept 1 and hosted runs EXECUTE AGAIN, measured by step count.** Entry 254's rider (a) said a private run *"MIGHT already succeed"* in a new month; measured, it DOES. Scheduled runs that had failed at `steps:0` (the billing signature) all through 2026-08-20…31 SUCCEEDED on Sept 2–3 with real executions — `fuzz-nightly` **18 steps / 64 min** (run 33732476854), `devnet-e2e-cron` **11 steps / 30 min** (run 33616516399) — against the discriminator that a billing block is `conclusion=failure` with **`steps:0`** (Aug 29 `ci-always`: `traceability`/`secret-guard` both `steps:0`). The block is **currently LIFTED** on the private repo. Two lessons kept: **(1)** `steps:0` vs `steps>0` is the machine-readable discriminator between a billing block and a real test failure — read it, never the bare `conclusion`, which is `failure` for both; **(2) the drain is USAGE-DRIVEN within a cycle, not a clean calendar boundary** — the same annotation cleared 2026-08-10, then re-blocked 2026-08-27, so minutes exhaust and refill mid-record and a run green today can block next week if the allotment drains before the next reset. The push-triggered `ci`/`ci-always` suite and the macOS/Windows `cross-os-extended` legs have NOT executed since the block (all `steps:0`), so their HOSTED state is still UNVERIFIED; the weekly `cross-os-extended` (Sundays) will now execute for free and reveal it. See [[ci-refusal-is-billing]], ledger 254.
- 2026-09-03 · orchestrator (wave 33, post-push) · **Once billing cleared, the first real `ci` execution exposed a `test`-job red that had been standing for weeks and that the register had folded wholesale into "billing" — a real test-artifact failure, not a payment block, distinguishable only by `steps>0`.** At `b529013`, billing clear, `ci`'s `test` job failed for real (`steps=8`) on `the_built_page_carries_the_footers_signing_key_element_byte_for_byte` (`crates/antseal-wasm/tests/page_template.rs`): the job runs `cargo test --workspace --locked` with **no page build**, so `target/verifier-web/index.html` is absent on the always-clean hosted runner and the test — which by R96's design reads the BUILT artifact and refuses to skip — panics. `ci` was green ≤2026-08-15 and red every run since 2026-08-16 (18 successes, all pre-Aug-16). **Three things this teaches:** (1) `steps:0` = billing, `steps>0` = a real failure — the register asserted "the provider refused every job since Aug 15" and never separated the two, so a real month-old red hid inside the billing story; observation itself was blocked, which is why nobody had read the step count. (2) The **local gate masks it**: `local-gate.sh` runs `cargo test` (line ~283) BEFORE `verifier-page-build.sh` (line ~730), relying on a leftover `target/verifier-web/` from a prior run — a cross-lane bootstrap that an isolated hosted `test` job does not have, so `cargo clean && local-gate.sh` would red locally too (the test file's own header documents this). (3) `ci.yml` and the test are UNTOUCHED by this session — pre-existing. **OWED: build the verifier page in `ci.yml`'s `test` job before `cargo test --workspace` (needs the wasm toolchain there), mirroring the local gate's cross-lane order; the test must not be softened to a skip (R96). OWNER: the go-public-prep session/CI lane. TIMING: before the visibility flip — a public repo must not show a red `ci`.** macOS/Windows (`cross-os-extended`), `advisory-cron`, `traceability` and `secret-guard` all PASS on hosted, so this is the SOLE hosted-CI blocker to a clean public launch.
- 2026-09-12 · leak-audit lane (wave 34) · **A PARTIAL clone is not a SHALLOW clone, and the guard that refuses shallow stores could not see it — D165 §2 R6's own defect class through a door R6 does not cover.** `git clone --filter=blob:none` reports `is-shallow-repository = false` and writes **no `shallow` file**, while carrying `remote.origin.partialclonefilter blob:none`; `git cat-file --batch-all-objects` then enumerates **only locally-present objects**, so a lazily-fetched blob is simply ABSENT from the corpus the scan walks, the reachability arithmetic still **balances**, and the verdict reads CLEAN. Reachable in practice, not theoretically: `actions/checkout` takes a `filter:` input. R7b's claim that after R6 *"a shallow CI wiring cannot report a false clean"* was therefore **incomplete** — it named one truncation mechanism and the store has two. Closed in-wave by a dedicated arm (b) in `lane_scrub_history`, keyed on `partialclonefilter` rather than on any object count, so a third truncation mode does not silently inherit the green. **The transferable rule: when a guard refuses a MODE, enumerate the other modes that produce the same corpus truncation — a corpus-completeness invariant must be keyed on how the store was BUILT, never on whether its own arithmetic closes over whatever it was handed.** · `scripts/ci-lanes.sh` `lane_scrub_history` (b); `docs/decisions/D165-history-scan-recurrence-and-the-digest-baseline.md` §2 R6/R7b
- 2026-09-12 · orchestrator (wave 34) · **A wasm build's staleness was ruled BENIGN twice on precedent; this wave ESTABLISHED it by measurement, and the difference between those two acts is the whole entry.** The gate's first run failed `verifier-page STALE ARTIFACT` (`was: e51d9d36…`, `is: 568277a2…`). From the message alone the benign reading (HEAD moved under a leftover `target/`) is **indistinguishable** from the dangerous one (a wasm build that is not reproducible from its sources), and the 2026-08-22 and 2026-08-27 entries each read it by precedent. Three measurements separated them here: **(1)** the wave's diff at `d31f81d` contains **zero** `crates/` paths, **zero** `.rs` files and **zero** `Cargo.lock` change, so no wasm input moved; **(2)** a **FORCED** recompile (`touch crates/antseal-wasm/src/lib.rs`, **2** `Compiling` lines) reproduced `568277a2` **byte-for-byte**, where the two prior cached no-op rebuilds could have proved nothing — a rebuild that recompiles nothing cannot disagree with the artifact it has just re-hashed; **(3)** **no tracked file records either digest** (`git grep` over both: 0 hits), so the page, its CSP hashes and the footer digest the page publishes **about itself** are all `target/` products, and nothing published ever described the stale module. **Two rules: a cached no-op rebuild is an assertion that cannot fail, and *benign by precedent* is not a measurement — the discriminator is the diff walked for wasm inputs plus a forced recompile that reproduces the digest.** · `scripts/verifier-page-build.sh`; `target/wasm-pack/antseal-wasm`
- 2026-09-12 · orchestrator (wave 34) · **The gate's exit status lied from a wrapper for the THIRD consecutive wave, in a new direction: the harness reported the BACKGROUNDED gate as exit 0 while the gate was still running.** `gate.exit` did not yet exist at the moment the success was reported, so the only true value was the recorded `GATE_REAL_EXIT` — **1** on the first run (`verifier-page` FAIL), **0** on the second. Wave 31 recorded the same lie through `… | tee`, wave 32 through a planted run reported as exit 0 against a recorded 1; this is the same class one layer out again, and the direction is always the dangerous one. **The sharpening worth keeping: the ABSENCE of the recorded exit file means *still running*, never *passed* — a wrapper's status for a backgrounded command is a fact about the LAUNCH, not about the run, so a gate verdict is read from a `GATE_REAL_EXIT` that must be proven to EXIST before it is read.** · `scripts/local-gate.sh` invocation convention
- 2026-09-12 · fuzz-verification lane (wave 34) · **`scripts/fuzz.sh selftest` DESTROYS un-triaged local crash artifacts, and it did so to the only local copy of one.** `cmd_selftest` runs `rm -rf "$fuzz_dir/artifacts/$t"` (`:217`, `:231`) **before** its own run, and it deleted the maintainer's only copy of an un-triaged `anchor_ots` reproducer; the lane had backed the directory up first and restored it, which is the only reason this is an entry and not a loss. Harmless on a clean hosted runner, where `artifacts/` is empty by construction — **so the hosted green says nothing about it, and the foot-gun sits in precisely the one lane whose purpose is preserving reproducers.** **The rule: a self-test that clears state to get a deterministic start clears a TEMPORARY directory of its own, never the shared path where evidence accumulates; and until it does, a lane touching a fuzz tree backs `artifacts/` up before it runs anything at all.** · `scripts/fuzz.sh` `cmd_selftest`
- 2026-09-12 · fuzz-verification lane (wave 34) · **`scripts/fuzz.sh repro` drops `LIBFUZZER_FLAGS`, so the documented single-input triage path runs under libFuzzer's DEFAULT timeout — 1200 s where the project's is 25.** `LIBFUZZER_FLAGS=(-timeout=25 -rss_limit_mb=2048 -print_final_stats=1)` is declared at `:154` and passed by the smoke path (`:191`) and the runs path (`:203`); `cmd_repro` (`:245`) passes none of it. The consequence is not a missing flag, it is an INVERTED verdict: a hang the smoke lane calls a finding at 25 s becomes a **twenty-minute wait** under `repro`, and the RSS ceiling the smoke lane enforces is absent there too — so the command a triager reaches for is the one command that will not classify what it is triaging. **The rule: a shared flag set is a CONTRACT, and every entry point claiming to reproduce what another entry point found must pass the same set; an option list assembled once and used at two of three call sites is a divergence with no red arm and no reader.** · `scripts/fuzz.sh` `cmd_repro`
- 2026-09-12 · identifier-checker lane (wave 34) · **Two rules in a brand-new checker arm were DEAD ON ARRIVAL and the arm's own self-test caught both — the plant-every-rule discipline paying for itself inside a single act.** In `check-personal-data.py`'s new bare-name arm, `CHOWN` (`:257`) matched the phrase *"chown pair"* **in its own docstring** — the guard-that-names-its-subject leak again, the shape this same file has now produced three times — and `USERGROUP` (`:266`) sat behind a prefilter containing **no anchor for a bare `<n>:<n>` pair**, making it a rule that could never fire on anything. Neither was visible to reading; both were visible to a planted line. The same gate ran inside the lane's own measurement harness, so **~245 MB had to be re-measured after the fix** — that is the cost of finding it late in one's own act, not the cost of the defect. **The rule stands where it stood, with a clause added: a new rule is not a rule until a planted fault reddens it, and a PREFILTER is a rule too — a cheap pre-match that lacks an anchor for the pattern behind it silently converts that pattern into dead code.** · `scripts/check-personal-data.py` bare-name arm
- 2026-09-12 · identifier-checker lane (wave 34) · **A checker's header claimed a property its own registry violates, and the repair was to assert the EXCEPTION rather than to soften the claim.** `check-personal-data.py` states it holds no identifier of its own and matches denied values **by digest** — while `REGISTERED_HOME_NAMES`' **five** keys are plain text, one of them naming the maintainer's own account. Converted into a new `SELF-LITERAL` arm (`:807`, `:905`) asserting **EQUALITY**, not a floor: the set of self-naming tokens in the file must be **exactly** those five keys, so a sixth literal reds on arrival and the accepted exception is **named rather than silent**. **The rule: when a guard must carry the very thing it forbids, the honest form is an equality against an enumerated allowance — and a header sentence that is false about its own file is worse than no sentence, because it is the sentence the next reader trusts instead of measuring.** · `scripts/check-personal-data.py` `SELF-LITERAL`
- 2026-09-12 · leak-audit lane (wave 34) · **A lint's green was structurally blind to most of the tree, and the two instruments disagree by 131 to 0 on the question the flip turns on.** `check-traceability.py --machine-paths` reports *"0 absolute home paths … across 465 live file(s) in 14 scan root(s)"*, while the tracked tree at `d31f81d` carries **131** occurrences of the maintainer's home path across **33** files — because the evidence, decision and research directories sit **outside `CITATION_SCAN`**. Both instruments are correct for their stated purpose: the lint polices LIVE INSTRUCTIONS a contributor executes, and `check-personal-data.py` walks `git ls-files` whole and bounds that same **131** as a registered, accepted ceiling. Only one of them answers *"what publishes"*, and it is not the one printing the zero. **The rule: a scan-root allowlist makes a green an assertion that cannot fail for everything outside it, so the ROOTS are read before the verdict is believed — and two greens from two instruments are ONE measurement unless their corpora have been compared.** The 2026-08-27 depth-1 `docs/*.md` entry is this finding at one directory's scale; this is it at the tree's · `scripts/check-traceability.py` `CITATION_SCAN`; `scripts/check-personal-data.py`
- 2026-09-12 · CI-arm lane (wave 34) · **A recorded refusal signature was too narrow by 4×, and a reader applying it would have gone hunting a code defect that is not there.** `docs/ci-verification.md` gave the Actions billing refusal as an empty `steps` array *"in 3–5 seconds"*; measured across **all 39** zero-step refusals in the **102**-run archive, the range is **4–20 s**, with one run at a full **20 s** and all **15** jobs at `steps == []`. A 20-second failure read against a 3–5 s rule is classified as a real execution, which is the expensive direction of the error — it sends the reader into the test suite for a defect the provider caused. **The discriminator is `steps == []`; wall time is a hint and nothing more**, and the corrected text now says exactly that at `docs/ci-verification.md:4563-4566`. **The rule: a signature derived from the first instances you happened to see is an unmeasured RANGE — before it is written into a runbook it is measured over the whole archive, and the field that DECIDES is named apart from the fields that merely correlate.** · `docs/ci-verification.md` billing-refusal signature
- 2026-09-12 · CI-arm lane (wave 34) · **`ci` has no `workflow_dispatch`, so there is no way to ask a hosted runner for a verdict on `main` on demand — and a correction lane nearly shipped *"dispatch `ci`"* as the advice for doing so.** Measured: **0** occurrences in `ci.yml` and **0** in `ci-always.yml`, against **6 of 8** workflows that carry one (`advisory-cron` 2, `cross-os-extended` 1, `devnet-e2e-cron` 2, `fuzz-nightly` 3, `pages` 2, `verifier-page` 2) — so the mechanism is so routine in this tree that its absence in the two that matter reads as present. The only routes to a `ci` verdict are a `pull_request`, which reports on the **merge commit** rather than on `main`'s tip, or a push touching a path outside `paths-ignore`. **The rule: advice that names a mechanism is a claim about a file and is measured in that file before it ships; and a trigger set is part of a workflow's INTERFACE — a suite that cannot be asked for a verdict cannot serve as an on-demand pre-flip witness, however green its last run was.** · `.github/workflows/ci.yml`; `.github/workflows/ci-always.yml`
- 2026-09-12 · CI-arm lane (wave 34) · **A self-test that mutates and reverts a TRACKED file in place has a silent clobber window, and it opened while a sibling lane held that file with 913 uncommitted insertions.** `check-ci-paths.py --self-test` mutates `docs/ci-verification.md` (`:1458`) and then writes back a snapshot it read **milliseconds** earlier. Nothing was lost, and that was verified three ways rather than assumed — by diffstat, by the planted literal's absence afterwards, and by the sibling lane's new content still being present — but a concurrent write **inside that window would have vanished with no error and no red**, because the revert is a whole-file write of a stale buffer. **The rule: a self-test exercises a COPY, never the live tracked path; where it cannot, it is as unsafe to run mid-wave as a full-tree stager and belongs on the same forbidden list. *Nothing was lost* is a measurement of luck, not of safety.** · `scripts/check-ci-paths.py` `--self-test`
- 2026-09-12 · orchestrator (wave 34) · **A brief's liveness claim went stale the instant the next lane was dispatched, and the lane holding it spent real budget re-establishing its own inputs.** A verification lane was briefed that it was the only lane in the tree; another lane was then dispatched and rewrote a file underneath it. The lane **detected the advancing mtime**, flagged that its brief was false, and had to re-measure that its own inputs were untouched before it could trust anything it had already read. Nothing was corrupted; the cost was paid in that lane's budget and in the credibility of every other sentence in its brief, which is the durable damage. **The rule: a brief states what MAY MOVE and who holds it — never that nothing will. A liveness claim is a promise about the future of a shared tree that the dispatcher cannot keep and the lane cannot verify; the checkable form is a write-scope list, which is true at every instant.** · wave-34 lane briefs
- 2026-09-12 · orchestrator (wave 34) · **An archive exclusion silently failed by 42×, exited 0, and only a cross-check against the PRIOR artifact caught it.** The pre-flip backup came out at **447 MB** against the convention's **10.59 MB**, because `fuzz/target` (**1.5 G**) escaped an anchored `--exclude='./code0/target'`. This is the granularity ruling's **fourth** venue, and the twist is the one worth keeping: the fix recorded on 2026-08-29 (`--exclude='*/target'`) **had not travelled** to this neighbouring invocation, because that one archives the repository and this one archives its PARENT directory, so the anchored spelling looked correct and matched one path of two. Exit status was **0** and nothing was printed. Repaired with a component-matching pattern and verified by a **path-set diff between the two archives**, itself proven able to fail before it was believed — a planted entry produced **2** differing lines where the real comparison produced **0**. **Two rules: an archive's success predicate is a comparison against the previous archive (size and member set), never a zero exit; and a fix written into one invocation's convention does not propagate to the next — recording a hazard does not immunise the neighbouring command against it.** · wave-34 pre-flip backup; the 2026-08-29 exclusion-granularity entry
- 2026-09-12 · fuzz-verification lane (wave 34) · **A record asserted PERMANENT unknowability that was false at the moment it was written, and the evidence sat in two places nobody had looked.** `docs/decisions/D102-parser-structural-allocation-cost.md:451` — inside §7, the section whose own title is *"The record defect"* — says of a corrupt transcribed fuzz reproducer that *"we cannot know what the recorded 248-byte input did, and never will"*. The authentic bytes were in the CI artifact **and** un-tracked on the maintainer's own disk the whole time; measured this wave they yield **the same error code, the same 5120 B peak and the same 10112 B total** the record attributes to its corrupt transcription. So the metadata was right, only the prose base64 was damaged, and **the damage was behaviourally inert** — the record's substantive ruling is untouched, which is why this is an addendum and not a retraction. **The rule: *and never will* is a claim about the world's evidence stores rather than about the record, and it is the one claim a record must not make over an unsearched corpus — CI artifacts, local scratch and backups are all enumerable, and an un-tracked file is not a lost one.** · `docs/decisions/D102-parser-structural-allocation-cost.md` §7
- 2026-09-12 · fuzz-verification lane (wave 34) · **A register under-counted its own evidence by half, and the uncited half sharpens the inference the counted half produced.** `A100` and `D102` §7 both reason from *"four"* `anchor_ots` CI reds; there are **eight** `fuzz-smoke-artifacts`, and the sizes **389, 823, 840 and 915 B** appear nowhere in `TODO.md` or `docs/` as artifact sizes — the tree's sole `840` is `tasks/A.md:1167`'s `4 840 B` relative cap, a neighbouring number rather than this one, which is exactly how a count like this stays wrong through two records. The four uncited runs also test `A100`'s own inference, that a second input *"peaked at the same 5120 B — evidence the peak is a fixed step, not input-proportional"*: the **840 B** input peaks at **10240 B**, the next rung of the doubling ladder `D102` §1.1 prints at `:69` (`160 320 640 1280 2560 5120 10240 …`). The peak is therefore a fixed **LADDER**, not a fixed step — a **sharpening, not a refutation**, and the D58-versus-relative-cap disagreement the record rules on is unaffected. **The rule: a count of reds is a count of ARTIFACTS and is re-measured from the artifact store rather than carried forward in prose; and an inference about a quantity that can only double, drawn from two observations, is under-determined by construction.** · `TODO.md` `A100`; `docs/decisions/D102-parser-structural-allocation-cost.md` §1.1, §7
- 2026-09-12 · registrar (wave 34) · **Recording the finding above TRIPPED the arm it describes: the guard's prose rule read this ledger's own sentence as naming an account, and the token it flagged was the new arm's own name.** `NAMEPROSE` is `\b(?:user|account|login)[ \t]?names?\b[^\n]{0,72}?` followed by a QUOTED `IDENT_NAME`, so an entry that must state in prose what `REGISTERED_HOME_NAMES`' keys are, and then name the arm in backticks a few words later, emits `[USER-NEW] … (1 occurrence(s), digest 54ee7225d5bac4ca)` with `SELF-LITERAL` as the supposed account. This is the **SECOND** measured instance of the class the rule's own comment already records — the first, `match-level` at `tasks/Q.md:3492`, is why `**bold**` is not admitted as a quote — and it was repaired by DE-ANCHORING the ledger prose, never by widening the rule. **How it was attributed is half the entry**: the tree was concurrently red from another lane's in-flight edits, and `[USER-NEW]`/`[HOME-CEILING]` are AGGREGATE findings that name **no path**, so *"no finding names my file"* (`grep -c` = 0) was not evidence of innocence — the per-file answer, obtained by importing the module and calling its pure `identity_names()` on the added lines alone, **inverted** it. **Three rules: a guard whose anchors are ordinary English is tripped by its own documentation, so every surface that describes it must be written around those anchors; an aggregate finding cannot be attributed by grep, and a checker with a pure per-text function can always be asked the per-file question directly instead; and the entry recording a guard is itself scanned by that guard.** · `scripts/check-personal-data.py` `NAMEPROSE`; this wave's `SELF-LITERAL` entry
- 2026-09-12 · orchestrator (wave 34) · **The pre-public wave's OWN registration reproduced all three identifier classes it was registering, and the guard extended earlier in the same wave caught every one — at occurrence 132 of an accepted 131.** Writing the wave-34 Current-focus block into `TODO.md` emitted `PD_REAL_EXIT=1` with three findings: `[DENY/handle]` (the personal address's username stem, written out while recording that it measures zero), `[HOME-CEILING]` (*"a registered home directory name grew to **132** occurrence(s) against an accepted ceiling of 131"* — the registration added the 132nd while explaining why the 131 are accepted), and `[USER-NEW]`, where the prose arm extracted the English article **the** from the phrase *"names the username in prose"* and treated it as though it were an account. That was the **third** over-broad match measured in this checker in one wave: the other two were a rule that read its own docstring, and a rule that sat behind an anchorless prefilter and so could never fire at all. **A fourth arrived while this very entry was being written** — the arm matched its own subject again, extracting a rule's backticked name from a sentence that had to say what the arm does, which makes the false-positive class **structural**: this rule cannot be documented in prose without firing, so every record about it must be de-anchored by hand. Four instances, one wave, all in documents describing the rule; a narrowing (POSIX account names are conventionally lower-case, and English articles are not accounts) is **owed as a follow-up and was deliberately NOT taken here**, because widening or narrowing a guard in the hours before an irreversible publication is the one edit whose blast radius nobody can measure in time. **This is `Q268`'s exact defect class — a document about an identifier reproducing that identifier — recurring in the tracker rather than in a scrub report, in the one wave whose purpose was to prevent it, and authored by the orchestrator who had just briefed two lanes on the rule.** The transferable lesson is not *be careful*: it is that **the only reason this was caught is that the bare-name arm and the ceiling had landed hours earlier**, so the instrument built this wave immediately convicted its own author. Repaired by naming every class and reproducing no literal — the rule `Q269` states — never by widening a rule or raising a ceiling on the eve of an irreversible publication. **Raising the ceiling to 132 would have been the tempting fix and it is the wrong one**: the ceiling's whole function is that accepted is not a licence to add more, and the first thing that asked to be exempted from it was the wave's own prose.
- 2026-09-12 · orchestrator (wave 34) · **HEAD IS A WASM INPUT, so the gate can never be green on the commit that is about to be made — and the register's recorded reason for three waves of `STALE ARTIFACT` was wrong in its conclusion while right in its verdict.** `scripts/wasm-pack-build.sh:226` reads `commit="${ANTSEAL_SOURCE_COMMIT:-$(git rev-parse HEAD 2>/dev/null || printf 'unknown')}"` and `crates/antseal-wasm/build.rs` stamps it into the module, with `cargo::rerun-if-env-changed` making the stamp non-stale. Measured on the artifact the gate had just built: the current commit is present (**1**), the immediately previous commit **absent (0)**, and a fabricated 40-hex control **absent (0)** so the search was live — the first control tried, forty ASCII zeros, matched the binary's data section and had to be discarded as unable to fail. **The consequence is procedural and it inverts the wave-close order**: every commit invalidates the packaged module, so a gate run immediately after a commit rebuilds and FAILS on that rebuild, and only a second run over an unmoved `HEAD` can pass. The standing rule *"a wasm-input change means the gate needs TWO runs"* is therefore the weaker form of *"a COMMIT means two runs, and every wave commits"* — which is why waves 32, 33 and 34 each needed an extra run and each explained it differently. **The wave-33 register's stated ground — that the check's commit-stamp arm being refused makes the module "a pure function of the source tree" — is FALSIFIED**: the refused arm is the *checker's*, and it says nothing about the *builder*, which supplies the commit by default. **A diff-based wasm-input test cannot see this input at all**, because the input is not a file; this orchestrator ran exactly that test three times, twice reached the right answer for the wrong reason, and was corrected only when a third run reddened on a tree whose diff contained no Rust, no lockfile and no template.
- 2026-09-12 · orchestrator (wave 34) · **COMMITTING BEFORE GATING SILENTLY DISARMS THE HEAVY-FEATURES AND DEVNET-E2E TRIGGERS, and the two wave-34 findings about the gate pull in OPPOSITE directions.** `scripts/gate-features.sh:187` defaults its base to **local `main`** (`base="${ANTSEAL_GATE_BASE:-main}"`) and line 194 unions `git diff --name-only "$base"...HEAD` with `git status --porcelain`. On `main` with a clean tree **both terms are empty**, so `heavy-features n/a (no storage-touching path changed vs main)` is **guaranteed rather than measured** — the dominant defect class of this project, sitting inside the gate's own trigger. Measured at the wave-34 close: default base **0 files**, `origin/main...HEAD` **9 files**; re-derived against the published base the verdict *holds* (`no storage-touching path changed vs origin/main`, `REAL_EXIT=1`), so this wave's `n/a` and its `e2e-devnet SKIP` are both honest — **but they were about to be right for a reason that would have held no matter what the wave contained.** The interaction is the entry: **`HEAD` being a wasm input pushes the gate AFTER the commit, while the trigger base being local `main` requires it BEFORE** — so **no single run on `main` satisfies both**, and a wave that gates only post-commit gets an unfalsifiable heavy verdict while a wave that gates only pre-commit gets a `verifier-page` red it will misattribute. **Remedy, and it is one environment variable: close the wave with `ANTSEAL_GATE_BASE=origin/main`** — the right base for the question a wave close actually asks, *what does this wave change relative to what is published* — after rebuilding the module for the final commit. Both halves then measure something that can fail.
