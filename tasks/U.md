# U — CLI, vault, config, UX

> Part of the antseal MVP task breakdown (generated 2026-07-27 from MVP-SPEC.md Revision 2 by a 9-agent decomposition).
> **Status tracking lives in `../TODO.md`** — do not add checkboxes here. Treat Do/Accept as normative until deliberately revised; spec line references are into `MVP-SPEC.md` as of 2026-07-27.
> Dep prefixes: P=setup/toolchain/pins, F=CBOR+manifest/bundle codecs, C=crypto primitives, G=canonicalization+units+GGM fine tree, S=storage/payments/journal/restore, A=anchors, R=reveal/verification/web page, U=CLI/vault/config/UX, Q=test-infra/CI/threat-model/docs/release.
> Note: several U milestones are inferences (the spec assigns no milestone to init/vault/config) — placed in the milestone whose deliverables need them (init/vault before M1's E2E, seal orchestration M1, status M2, reveal/verify M3), as the agents noted per task.

### U1 — Scaffold `antseal-cli` crate with full canonical clap surface
- Milestone: M1
- Size: M
- Deps: P: workspace scaffold + pinned clap/tokio/tracing versions; P: final product name decision (binary name, `~/.antseal/` dir name, crate prefix)
- Spec: Working name / pre-M0 (line 3); Architecture (lines 44–58); CLI surface (lines 147–149)
- Do: Create `crates/antseal-cli` (binary `antseal`, clap derive + tokio main) — and, per **D34**, a `[lib]` target: the orchestration layer (seal/resume/restore pipelines) is `antseal_cli` library code over injected interfaces, `main.rs` a thin driver. Define the complete canonical command tree up front — `init`, `seal <path>…`, `list`, `show <work-id>`, `status <work-id> [--upgrade]`, `restore <work-id> [-o dir]`, `reveal <work-id> (--all | --units …) [-o file] [--include-receipt] [--yes]`, `verify <bundle> [--online] [--live]`, `vault export|import` — with all `seal` flags (`--title`, `--split blank-lines`, `--force-text`, `--no-fine-tree <glob>`, `--dry-run`, `--yes`, `--force-degraded`, dev-only `--no-anchor`) and globals `--network arbitrum-one|arbitrum-sepolia|devnet` (default `arbitrum-one`), `--json`, and — **deliberate surface amendments, decided 2026-08-01** — the global `--passphrase-fd <n>` (D41) and the D39 `init` flag set: `--wallet <generate|import>` (default `generate`), `--wallet-key-fd <n>` (must differ from `--passphrase-fd`; equal fds are a usage error), one D40 KDF selector slot, one `--wrap` selector slot (default declined, values owned by U8/D50). Nothing else joins the surface; the snapshot enumerates exactly this. Later-milestone handlers return a clean "not implemented until M<x>" error, so the surface is frozen from day one and never drifts from spec. Initialize `tracing`/`tracing-subscriber` writing to stderr only, env-filter driven (no new verbosity flags — the canonical surface is closed).
- Accept:
  - `antseal --help` and every subcommand `--help` snapshot-tested; snapshot enumerates exactly the canonical surface plus the recorded D39/D41 amendments, nothing extra
  - `--network` rejects unknown values; `--split` accepts only `blank-lines`; `--no-fine-tree` takes a glob value; `reveal` enforces exactly-one-of `--all`/`--units` at parse time; `--wallet-key-fd` equal to `--passphrase-fd` is a usage error
  - `--no-anchor` is present but hidden from release help output (dev-only), and gated as in U13
  - tracing output goes to stderr; stdout stays reserved for command output
- Notes: If the pre-M0 rename lands, binary/dir/env names must be single-constant parameterized here. The D39/D41 additions are recorded spec-surface amendments (the spec's canonical list names no `init` flags and no passphrase channel), justified in their decision docs. **[D68, 2026-08-12]** The `reveal` arg surface is ratified as shipped (docs/decisions/D68-units-syntax-and-default-bundle-filename.md §3 R1–R2): no clap attribute on `--units` changes. Two amendments land with U28, both re-freezing `cli-surface.help.txt`: (i) a `value_parser` on `units` that discriminates range-shaped values (`3-5`, `3..5`) and rejects them with a message naming the comma form and `show`, replacing clap's generic `invalid digit found in string` (row **U68**); (ii) reveal's `-o <FILE>` help line must name its default, as restore's `-o <DIR>` and `vault export`'s `[FILE]` already do (snapshot lines 241 and 361 vs 279-280).

### U2 — Define exit-code scheme and thiserror CLI error taxonomy
- Milestone: M1
- Size: M
- Deps: U1; C: library error enums; G: canonicalization errors; S: storage/payment error classes (incl. insufficient-token vs insufficient-gas); F: codec errors
- Spec: Core user flows (line 34); CLI surface (lines 147–149); Seal journal (line 145)
- Do: Design a documented, stable exit-code scheme: 0 success; distinct codes for usage error, user-declined consent / non-TTY-without-`--yes` abort, insufficient ANT token, insufficient ETH gas, anchor-gate abort, vault authentication failure (bad passphrase / AAD-detected tamper), network failure, resume-safety abort (changed source / missing staged bytes), and verification-failure classes (finalized in U30). **[2026-08-01 planning round] The taxonomy additionally carries the decided classes**: D51's **consent-not-obtained** partition (declined ≡ unobtainable — one class, distinct from passphrase-unavailable and from usage); D41's **passphrase-unavailable/abort** (no TTY and no `--passphrase-fd`; fd read failure/empty — dedicated code, prompt-parity); D40's two vault-KDF classes (`vault-kdf-memory` — cannot allocate the floor, at create AND unlock — and `vault-kdf-params-out-of-range` — the pre-auth header-cap rejection); D45's two **resume-safety usage errors** (overlap-not-exact, flag-mismatch); D46's **invalid-seal-argument** (directory / non-regular file / duplicate path, pre-consent); D48's **refused-overwrite** and **malformed-restore-record** (restore-side per-file classes); D47's export/import classes (`export-self-verify-failed`, `import-auth-failed`, `import-newer-version`, `import-refused-existing-vault`). Implement a top-level `thiserror` CLI error type that wraps every library-domain error (C/G/S/A/R/F) into user-facing messages carrying the possession-language style and never any secret material. Wire `main()` so every error path maps deterministically to its code and, under `--json`, to a structured error object.
- Accept:
  - exit-code table committed and unit-tested (each error class → asserted code), including every 2026-08-01 decided class above
  - insufficient-token and insufficient-gas produce different messages and different exit codes
  - error `Display` output for every wrapped class is snapshot-tested; no secret-bearing fields exist in any CLI error variant (enforced by U21 harness)
- Notes: Verify-verdict exit mapping ruled 2026-08-12 by **D69** (docs/decisions/D69-verify-exit-code-mapping.md), which consumes the reserved 40–49 band as **40** `verify-bundle-rejected` (the `Err` arm of `verify_bundle` — tamper and malformed are one class; the specific `VerifyError::code()` rides in the message, never in the integer), **41** `verify-anchor-refuted`, **42** `verify-headline-divergence`, **43** `verify-unanchored`, with **44–49 still reserved** (reasons in D69 §3 R9) — and adds **36** `reveal-inputs-unusable` **outside** the band, because a reveal failure is not a bundle-verification verdict. `ErrorClass::ALL` goes 28 → 33; the module-doc table, `tests/exit_codes.rs::TABLE` and `class_index` move together (compile-forced), and `codes_are_nonzero_distinct_and_avoid_reserved_ranges` must narrow its `!(40..=49)` assertion to 44–49 in the same change or it fails for the wrong reason. D69 also adds the **third arm** the table needs: `commands::Outcome` carries `exit_class: Option<ErrorClass>` so a nonzero exit can still emit the success envelope — `ok` means "a result document is present", not "exit 0". Class names remain U2's to finalize; D69 fixes the partitions and the rank.

### U3 — Build `--json` output framework covering every command
- Milestone: M1
- Size: M
- Deps: U1, U2
- Spec: CLI surface (lines 147–149)
- Do: Define the machine-output contract: with `--json`, stdout carries exactly one JSON document per invocation (versioned envelope: `command`, `network`, `ok`, `result`|`error`), all human copy/prompts/nags/warnings go to stderr, and exit codes are unchanged. Build a schema-registry harness in which every command registers its `--json` result schema and CI validates emitted output against committed schema fixtures — so a command cannot ship without a defined JSON shape. Implement the interactivity contract per **D51**: **machine mode** = `--json` ∨ non-TTY stdin ∨ stdin consumed by `--passphrase-fd 0` — detected via stdin `isatty` (never `/dev/tty`, rejected) — and in machine mode **nothing prompts, ever**: consent, passphrase, wizard questions, destructive confirms all abort with their dedicated codes instead of hanging. Extend the schema registry into a **prompt-class registry**: a command registering its `--json` fixture must also declare its prompt classes (none / consent / auth / config), each with its declared non-interactive channel (`--yes`, `--passphrase-fd`, a D39 flag) or a deliberate absence (`vault import` overwrite — none in v1); an **abort-not-hang harness** drives every command × machine-mode flag combination and asserts abort with the right code.
- Accept:
  - envelope schema committed; harness fails CI when any command lacks a registered `--json` fixture or an undeclared prompt appears (prompt-class registry enforced)
  - `--json` output parses as a single JSON document with zero stray stdout bytes (nags/warnings verified on stderr)
  - non-TTY + no `--yes` on `seal`/`reveal` aborts with the defined code, in both plain and `--json` modes; the abort-not-hang harness covers every subcommand in machine mode
- Notes: Accept criteria extend per command as U11–U30 land (each adds its fixture). Open decision 15 closed by **D51** (2026-08-01) — the contract above is decided; U14 consumes U3's machine-mode determination rather than probing on its own. **[D69, 2026-08-12]** The envelope's `ok` field means **"a `result` document is present"**, not "the exit code is 0" — **maintainer-confirmed in session 2026-08-12**, answering D69's one open question YES: `ok:true` is re-read, no key is added, removed or renamed, and `ENVELOPE_VERSION` stays **1**. The two were coincident until M3 and are separated deliberately: `verify` reports a bad verdict at a nonzero code while still emitting its full result (D69 §3 R1), and `restore` does the same once U69 lands. The U2 error object is untouched; the module docs in `machine.rs` carry the restatement, and D51 invariant 2 (same code in both modes) is preserved because the code is computed before the mode branch. `verify`'s registered fixture must include at least one **nonzero-exit success envelope** — the shape nothing in the tree exercises today. **[D65, 2026-08-12]** The `--json` surface's commitment level is D65's three-tier partition (docs/decisions/D65-json-schema-stability-commitment.md §3): tier A `result.report` carried byte-verbatim and already frozen by 21 vectors, tier B this envelope's wrapper stable-additive from M3 under `ENVELOPE_VERSION`, tier C everything else inside `result` reviewed but unpromised until U32 — and the fixture registry is what tier C's honesty rests on, which is why every command's `result` wants a presence-only key assertion, not just the blessable byte snapshot.
- **[2026-08-01 execution note (U3 landed; wave 2 lane η)]** Module `src/machine.rs` (envelope + registry + single detection point) + suite `tests/machine_mode.rs`; committed fixture `tests/snapshots/json-envelopes.txt` (ANTSEAL_BLESS regeneration, one entry per command + success examples — the "committed example docs + shape test" reading of the fixtures requirement). Choices + interpretations, argued in the module docs:
  1. **Envelope v1** = `{v, command, network, ok, result|error}` — `v` is the version marker the entry's "versioned envelope" requires; the inner `error` object is **U2's provisional shape finalized UNCHANGED** (`class`/`exit_code`/`message`; `to_json` renamed `error_object`, envelope embeds it verbatim — asserted). `command`/`network` are null only on the unparseable-argv path.
  2. **Prompt-class registry** rides the same exhaustive `match` as `command_name` (a new subcommand fails compile until named AND registered); rows per D51's table — consent(--yes) on seal+reveal, authentication(--passphrase-fd) on every vault-touching command, configuration(D39 flags) on init, destructive-confirm(channel NONE, v1 disposition refuse-always) on vault import, verify = zero prompts. Channel truthfulness is tested (declared --yes must parse on its command); the only channel-less prompt is asserted to be exactly the import overwrite.
  3. **Abort-not-hang harness**: every command × {plain, --json} × closed stdin against the REAL binary under a real watchdog (kill + loud failure at 120 s — the harness can lose); asserts the typed class per cell (stubs → 3; export → 11; import-over-vault → 10), stdout emptiness in plain errors, and exactly one parseable envelope under --json. Stubs therefore emit valid machine-mode JSON errors by construction (main_entry composes the envelope for every error).
  4. **Two documented envelope exemptions**: `--help`/`--version` stay clap text on stdout (intrinsic surfaces, not command invocations); unparseable argv emits a best-effort null-command envelope only when the literal `--json` token is present in argv (token scan — the U2-noted refinement closed; false-positive-as-flag-value documented harmless). Machine-mode detection is the single point `machine_mode_for` (delegating to ε's stdin-isatty primitive); commands.rs's passphrase seam now routes through it.
  5. Envelope `network` = flag-or-default at U3; U4 upgrades it to the flag > config > default resolution in its own commit.

### U4 — Implement config file with network mapping and override slots
- Milestone: M1
- Size: M
- Deps: U1, U5; S: network → contracts/RPC mapping schema (S5); A: TSA list + endpoint config schema (slots only in M1)
- Spec: Vault (line 143, "config + per-work records"); MVP scope decision 4 / networks (line 20); Anchoring (line 109); Verifier page online mode (line 137)
- Do: Implement `~/.antseal/config.toml` (plaintext-readable, **beside the AEAD per D42** — exactly `config.toml`, the vault header, and the lockfile sit beside it; everything else is inside): default network, per-network contracts/RPC endpoints (schema from S), and reserved sections for the TSA list override (wired M2, U26) and pinned online-endpoint overrides for `verify --online` (wired M3, U30). Precedence: CLI `--network`/flags > config > built-in defaults (`arbitrum-one`). `init` writes the initial config (U11). Loader tolerates missing file (defaults) but hard-errors on malformed content with a precise message.
- Accept:
  - precedence tested: flag beats config beats default; `arbitrum-one` is the built-in default with no config present
  - `--network devnet` and `arbitrum-sepolia` resolve to distinct endpoint/contract sets supplied by S's schema
  - malformed config → distinct exit code; unknown keys warn (forward-compat) rather than crash
- Notes: Milestone inference — config is unassigned in spec; M1 needs network mapping for the devnet E2E.
- **[2026-08-01 execution note (U4 landed; wave 2 lane η)]** Module `src/config.rs` + suite `tests/config_file.rs` + user doc `docs/config.md`. Choices + deviations, argued in the module docs:
  1. **No contract/RPC-default duplication into config** — the entry's "per-network contracts/RPC endpoints (schema from S)" is implemented as: config consumes S5's pure `NetworkId`/`NetworkConfig` (antseal-net default features; containment asserted — no alloy/ant-core in cli's normal tree), chain ids/contracts/default-RPCs stay net's pinned constants, and config carries only a per-network `rpc_url` override SLOT (`[networks.<id>]`; consumer S6). Deviation per the updated lane direction: a hand-edited contract address is the drift hazard S5's evmlib byte-equality test exists to prevent.
  2. **Precedence made mechanically possible**: `--network` now parses as `Option` (a clap-level default would make an explicit `--network arbitrum-one` indistinguishable from absence) — a deliberate parse-semantics amendment to the frozen surface, help snapshot re-blessed (the visible change: the default note moves into the flag's help text). `effective_network` = flag > config > built-in arbitrum-one; the table test includes the explicit-flag-equals-default row only Option makes expressible; the U3 envelope's `network` field now reports the config-resolved value (proven end-to-end by spawned-binary tests).
  3. **Hand-parsed strict TOML subset** (no new deps, per the lane brief): sections, string keys, single-line string arrays, both quote forms, no escapes/ints/multiline/inline-tables — everything outside the subset is a per-line typed error, never a misparse; grammar documented in-module and in docs/config.md. **New U2 class `malformed-config` = exit 17** (the Accept's "distinct exit code"; table/ALL/snapshot updated in the same commit): missing file = defaults, malformed = hard error for EVERY command (silently dropped overrides are the config-tamper failure mode D42 names). Unknown top-level keys/sections WARN on stderr (forward compat); invalid values for recognized keys (unknown network id, non-http(s) URL, wrong type, duplicates) are hard errors.
  4. Reserved slots landed with validated shapes: `[anchors] tsa_urls` (U26/M2 — the URL-shape check the U26 Accept wants exists now), `[verify] bitcoin_endpoints`/`arbitrum_endpoints` (U30/M3). `initial_config_text` ships for U11's init writer. The D42/D47 cross-test: an exported config survives import byte-identically AND still loads + resolves.

### U5 — Design vault store layout, versioned header, and crash-safe file discipline
- Milestone: M1
- Size: M
- Deps: U1; P: dir/file naming constants
- Spec: Vault (lines 141–145)
- Do: Design the on-disk `~/.antseal/` layout: config file, vault header + encrypted store, per-work record area, and anchor-artifact area. Header is versioned (format version, KDF block per U6) to permit future migration. All mutating writes are atomic (temp file + fsync + rename) and a single-writer lockfile prevents concurrent `antseal` processes from corrupting the journal. The encryption-boundary partition is **decided — D42 (2026-08-01)**: beside the passphrase AEAD sit **exactly three things** — `config.toml`, the vault header, and the lockfile; **everything else lives inside**, including all anchor artifacts (`.ots`, TSA tokens, fetch dates) and every per-work record (`W`, journal, receipts, addresses, paths, costs) — the encrypted-at-rest mandate holds with no carve-outs. **D42 rider**: every AEAD in the vault binds the serialized KDF header + the record's identity as AAD, so a record spliced between slots (or between vaults) is an authentication failure, not a silent swap; whole-vault rollback protection is recorded out of scope.
- Accept:
  - layout documented in-crate with the D42 partition table (the three beside-items named exhaustively); header round-trips with version field; unknown future version → clean "vault created by newer antseal" error
  - kill-during-write test leaves the store readable (old or new state, never torn)
  - second concurrent invocation fails fast with a clear lock-held message
  - record-splice test: a valid AEAD blob moved to another record slot fails authentication (the AAD identity rider)
- Notes: Lockfile and atomicity are inferred hygiene (the M1 pay/finalize kill-tests depend on journal integrity). The boundary decision (D42) also fixes U24's hook rule at M2: runs iff the host command already holds an unlocked handle — silent no-op otherwise.

### U6 — Implement vault encryption: Argon2id/scrypt KDF with header-bound AAD
- Milestone: M1
- Size: M
- Deps: U5; C: XChaCha20-Poly1305 AEAD primitive + zeroizing key types; P: argon2/scrypt crate pins
- Spec: Vault (line 143)
- Do: Implement vault-at-rest encryption: passphrase → KDF → vault key → AEAD over the store, per **D40**: default KDF **Argon2id m = 262144 KiB, t = 3, p = 1** (the sole default); alternative **scrypt N = 2²⁰, r = 8, p = 1** — selectable only as an explicit init-time choice (U11/D39 selector), never automatically; random 16-B salt; the header records algorithm id + full parameters. Bind the entire serialized KDF header into the vault AEAD as AAD so any parameter downgrade (e.g. lowered m/N) or header edit is an authentication failure, never a silent weakening. **Pre-auth KDF-header caps (D40 §3, D10 precedent)**: parse-time upper bounds on m/t/p/N/r/p are enforced *before* any KDF allocation runs — a substituted header demanding 1 TiB is a resource bomb before the AEAD can authenticate; the same caps serve unlock AND D47's import path. A machine that cannot allocate the KDF's memory gets the **typed hard error at create and at unlock — no fallback, no prompt** (`vault-kdf-memory`, U2). Bad passphrase and tampered header are distinguishable from corrupt store only insofar as safe (all surface as vault-auth failure exit code).
- Accept:
  - test: decrypt with correct passphrase succeeds; wrong passphrase fails with vault-auth code
  - downgrade test (spec-mandated): lower `m` (and `N` for scrypt) in the header of a valid vault → AEAD authentication failure, not a successful open with weak params
  - parameter floor enforced at creation; **header-cap test**: an over-cap header (absurd m/N) is rejected pre-allocation with the distinct params-out-of-range error, no KDF run, no large allocation attempted
  - low-RAM behavior: simulated allocation failure at create and at unlock each yield the typed `vault-kdf-memory` error — no fallback path exists (asserted: no prompt, no silent scrypt substitution)
  - default-params vault creation measured (Argon2id 256 MiB) and documented; scrypt path covered by the same tests
- Notes: The former "may need a documented fallback prompt" lean is **overturned by D40** — the imagined fallback does not exist (scrypt at its spec floor needs 1 GiB, four times Argon2id's 256 MiB); low-RAM is a typed hard error by design. Crate pins per D40: scrypt `=0.12.0`; argon2 0.5.3 rejected (un-wiped blake2/digest-0.10 stack) — `=0.6.0-rc.8` recommended with a D88-style blake2 residue probe at the pin PR (P owns the pin landing).
- **[2026-08-01 execution note (U6 landed; wave 2 lane ε)]** Modules: `vault/kdf.rs` (block schema, caps, derivation), `vault/cipher.rs` (record AEAD, D42 identity AAD), `vault/session.rs` (create/unlock + `UnlockedVault`); suites `tests/vault_encryption.rs` + `tests/kdf_residue.rs`. Deviations + recorded choices, each inline in the code docs:
  1. **The literal Accept "lower `m` → AEAD authentication failure" is unreachable by design**: D40 §3 floors sit AT the frozen creation values, so every lowered `m`/`N` dies at the pre-auth caps with the distinct `vault-kdf-params-out-of-range` error — *before any KDF or AEAD runs* — which is strictly stronger than the auth failure the sentence imagined. Both directions are still covered: lowered params → cap rejection (tested); raised-within-window params → KDF runs → auth failure (tested); pure-AAD header edits (wrap-mode flip, KDF inputs held fixed) → auth failure at the cipher layer (tested). Recorded as a deviation-in-mechanism, not in property.
  2. **Key schedule recorded** (entry silent): one vault key = the 32-B KDF output used directly as the XChaCha20-Poly1305 key; fresh random 24-B nonce per record write; AAD = serialized header ‖ canonical-CBOR record identity; no per-record sub-keys (U10's wallet sub-key stays the recorded exception).
  3. **blake2 `=0.11.0-rc.6` became a direct declaration** (not in D40's pin list): argon2's `zeroize` feature does NOT forward blake2's, and blake2's own feature gates the chaining-state wipe — proven red-first (65 bytes of passphrase-derived `h` survive without it, buffer wiped either way via sha2→digest/zeroize). The residue probe + a manifest-declaration guard are committed.
  4. **Wrong-passphrase detection is a key-check record** (`store/check`: AEAD over a fixed public marker, identity-bound) — makes bad-passphrase/tampered-header/corrupt-store collapse uniform and immediate at unlock without touching work records.
  5. **Wrap modes ≠ 0 are refused at unlock as `internal` until U8 lands** its keyfile path and D50's distinct mode-2 error (nothing can write mode ≠ 0 yet; U8 re-maps).
  6. **Measured (release, 2-core reference host)**: Argon2id create/unlock ≈ 0.68 s; scrypt ≈ 2.4 s — the D40-predicted tax. Debug-mode KDF crates ride `[profile.dev.package.*] opt-level = 3` overrides (9.2 s → 0.7 s per derivation; justification with measurements in the workspace Cargo.toml).

### U7 — Build passphrase UX: prompts, strength floor, non-interactive supply, zeroization
- Milestone: M1
- Size: M
- Deps: U6; C: zeroizing buffer types (C5)
- Spec: Vault (line 143, "`init` enforces a passphrase-strength floor", zeroize)
- Do: Implement no-echo passphrase prompting (entry + confirmation at creation), a passphrase-strength floor enforced at `init`/`vault import`-to-new-passphrase (choose and document the measure, e.g. zxcvbn-class estimation with a minimum score/entropy), and the non-interactive supply channel **decided by D41: the global `--passphrase-fd <n>`** (stdin is the degenerate case `--passphrase-fd 0`; the env-var channel is rejected on the measured `/proc/self/environ` snapshot mechanism — unwipeable for the process lifetime; `--yes` covers confirmations only, not passphrases). **Frozen fd byte semantics (D41)**: read to EOF with a 1 KiB cap, strip exactly one trailing LF (or CRLF), reject empty and interior NUL/LF with the dedicated abort code, prompt parity (the bytes accepted over the fd are exactly what the prompt would have produced); **the fd path skips the create-time confirmation prompt** (the double-entry exists to catch typos, and fd input is not typed). All passphrase buffers are held in zeroizing types (C) and never logged.
- Accept:
  - weak passphrase at `init` rejected with actionable message; floor value documented
  - non-interactive path drives a full scripted `init`+`seal` without a TTY via `--passphrase-fd` (pipe and fd-redirect variants)
  - fd semantics tested: trailing-LF/CRLF stripped once, 1 KiB cap enforced, empty and interior-NUL/LF inputs abort with the dedicated code; fd-supplied create skips confirmation
  - passphrase bytes appear in no log/error/JSON output — nor in `/proc/self/cmdline` or environ (exercised by U21 harness)
  - prompt aborts cleanly (dedicated exit code) when no TTY and no `--passphrase-fd`
- Notes: Strength-floor metric choice is an open decision; must land with U11. Channel + byte semantics are closed (D41, 2026-08-01).
- **[2026-08-01 execution note (U7 landed; wave 2 lane ε)]** Module `src/passphrase.rs` (+ 3 additive `PassphraseFailure` reasons: `PromptEmpty`/`PromptFailed`/`ConfirmMismatch`, all class 11; exit-codes snapshot re-blessed). Choices + deviations, argued inline in the module docs:
  1. **Strength floor decided and documented: 12 bytes, length only** (`MIN_CREATE_PASSPHRASE_BYTES`), enforced at create on BOTH channels, never at unlock. zxcvbn-class estimation rejected for v1 (new scoring dependency + gameable locale-sensitive judgments; the real mitigations are D40's memory floor + length). Bytes not characters — the measure is exactly the KDF input. U11 consumes `enforce_create_floor` at init.
  2. **Prompt mechanism: `rpassword =7.5.4` exact-pinned** (policy §1 row + workspace rationale): no-echo needs termios, std has none, and first-party libc would need the crate-root `unsafe` opt-out — worse than two small vetted crates. Prompt text goes to `/dev/tty` (never stdout); termios restored on drop, panic included. Prompting is GATED by our own stdin-`isatty` check, so D51's "never /dev/tty" detection rule holds — /dev/tty is only ever the read/write device once stdin-tty already said interactive.
  3. **fd mechanism is std-only, no unsafe**: fd 0 = locked stdin; fd n = reopen via `/dev/fd/<n>` (how shell `<(…)` works; verified against real pipes in tests). Recorded platform limit: Windows has no fd namespace → non-stdin fds fail as `FdReadFailed` there (the D41 design is the gpg POSIX convention).
  4. **Parity hardened both directions**: the prompt channel applies the same interior-NUL/LF/CR rejection as the fd channel, so the accepted byte spaces are identical; the parity Accept is executed against a real vault (create via prompt-collected buffer, unlock via fd-collected buffer, one Argon2id vault).
  5. **Accept items owned by consumers, deferred by construction**: the full scripted `init`+`seal` drive belongs to U11/U13 (neither command exists yet — the channel is proven at the library level over pipe AND fd-redirect variants); the `/proc/self/cmdline`+environ scans are U21's harness (the in-suite sentinel test covers rendered errors/JSON today). Machine-mode primitive (`machine_mode`/`machine_mode_from`, stdin-isatty only) shipped for U3 to consume.
  6. Zeroization: capacity pre-reserved before the fd read (no realloc strands a partial copy), every rejection path wipes before returning, prompt String moves into `SecretBuf` (no copy), confirmation buffer wipes on drop.

### U8 — Implement the optional keyfile wrap for `W` (OS-keystore mode reserved, not implemented)
*(re-scoped 2026-08-01 by D50 — M1 ships the keyfile wrap only, zero new dependencies; the OS-keystore wrap is format-reserved and lands as a post-D72 follow-up.)*
- Milestone: M1
- Size: M
- Deps: U6, U7 (offered during U11's init flow — interface only, not an ordering dep); C: key-wrap primitive (AEAD) + zeroizing types
- Spec: Vault (line 143, "offers a high-entropy keyfile / OS-keystore wrap for `W`"); D50 (docs/decisions/D50-os-keystore-scope.md)
- Do: At `init`, offer wrapping the master-secret material with a generated high-entropy keyfile (stored wherever the user directs, required alongside the passphrase). Record the chosen wrap mode in the vault header via the **D50 wrap-mode registry**, bound through U6's AAD: **0 = none, 1 = keyfile, 2 = os-keystore RESERVED** — mode 2 is a registered id with no M1 implementation, and a vault presenting it fails with a **distinct "wrap mode not supported" error** (not generic auth failure), so a later keystore implementation is purely additive. The `--wrap` selector (D39) defaults to **declined**. Unlock path combines passphrase KDF with the wrap; absence of the keyfile yields a precise, distinct error. No keyring/keystore dependency enters the graph at M1 (keyring 4.x is churn-active; linux-keyutils is reboot-volatile; machine-bound factors would break D47's clean-machine drill — D50's evidence).
- Accept:
  - round-trip tests for the keyfile mode: create → lock → unlock; missing keyfile → distinct error, not generic auth failure
  - wrap mode recorded in header; header tamper (mode flip) → AEAD auth failure; a header claiming mode 2 → the distinct "wrap mode not supported" error (tested)
  - declining the offer leaves a passphrase-only vault (mode 0) byte-compatible with U6 tests
  - no keystore crate in the workspace graph (dependency assertion)
- **[2026-08-02 EXECUTED (M1 lane m1-ux)]** `antseal_cli::vault::keyfile` + `create_vault_with_wrap` / `unlock_vault_with_keyfile`, the header's optional keyfile-path key, two new error classes, `init --wrap keyfile` honoured, and `docs/vault-keyfile.md`. Execution notes:
  1. **The combination is a KDF, not a XOR**: `vault_key = HKDF-SHA256(salt = keyfile bytes, ikm = kdf_key, info = "antseal-cli vault v1: keyfile wrap")`. A XOR would have been cheaper and wrong — it makes the two factors additively related, so a leak of the final key plus either factor discloses the other. **Zero new dependencies**: `hkdf`/`sha2` are already in the graph for U10's wallet sub-key, which is D50's "zero new dependencies" satisfied literally.
  2. **The keyfile path rides in the vault header as an OPTIONAL v1 key 2**, not in `config.toml` and not only in the environment. The header is read first at unlock (pre-auth, which is exactly where the path is needed) and is AAD for every record, so a tampered path is an authentication failure for the whole vault instead of a silent redirect at an attacker's file. It is a **pre-release extension of v1**, not a version bump — the same rule `vault/export.rs` already records for its payload — and it is optional, so a vault may carry mode 1 with nothing in its directory saying where the second factor lives. What recording it costs (an attacker with the vault directory learns *where* to look) is stated in the module docs and in `docs/vault-keyfile.md`.
  3. **`ANTSEAL_KEYFILE` is the non-interactive channel and is not a D41 violation.** D41 rejected the environment for *secrets* on a measured mechanism (`/proc/PID/environ` retains the execve-time snapshot). A **path** retains nothing worth having, which is the rule `ANTSEAL_DIR` has always followed. The canonical surface has no `--keyfile` flag and this lane did not add one.
  4. **Two new error classes, in the vault band's free slots.** 18 `vault-keyfile-missing` — absent, unreadable or wrong-sized — is emphatically **not** U6's generic auth collapse: that collapse exists to hide *which* secret-dependent step failed, and nothing secret is involved in noticing a file is not there. Telling a user whose USB stick is unplugged that their passphrase is wrong sends them to re-type, re-derive and eventually re-create, which destroys the vault. A *wrong* keyfile does collapse into 12, correctly. 19 `vault-wrap-mode-unsupported` is D50's distinct mode-2 refusal and says the vault is intact.
  5. **DEVIATION — the entry's "D47's export carries the keyfile factor unchanged" is overturned.** A v1 export's AEAD key is `KDF(passphrase, fresh salt)`: the passphrase **alone**, as `vault/export.rs`'s own format block says. Writing one from a two-factor vault would silently convert two factors into one and make the backup the weakest link — the same silent downgrade U12 refused on the import side. So `vault export` **refuses** a vault with a non-zero wrap mode and `vault import` refuses such a payload, both naming the reason; making the export two-factor is a D47 format event, which is a recorded decision rather than an implementation detail (D50's own reasoning for keeping the keystore out of M1).
  6. **DEVIATION — Accept row 4 was already false before U8.** `keyring` sits in `Cargo.lock` as a transitive dependency of `ant-quic` under the `ant-backend` feature. The assertion is narrowed to the two statements that are true and load-bearing (the CLI manifest names none; the **default** graph contains none, checked against `cargo tree`), recorded as **U43**.
  7. `init --wrap keyfile` is honoured and U11's typed not-implemented refusal is gone. The wizard asks for the path through a new `InitPrompt::ask_path` **with a default implementation that refuses**, so every prompt double written before U8 keeps compiling and one asked for a path it was never scripted for fails loudly rather than inventing an answer. Machine mode requires `ANTSEAL_KEYFILE` and says so **before any passphrase is collected** — the position U11's refusal held, kept.
- Notes: Not on the M1 E2E critical path — may land late-M1. The OS-keystore platform scope + implementation is D50's recorded post-D72 follow-up (see the D72 register row); D47's export carries the keyfile factor unchanged. **[2026-08-01, from U12]** `vault import` currently REFUSES any export with wrap mode ≠ 0 (an `internal`-class error — never a silent mode-0 downgrade of a wrapped backup; `vault/export.rs::validate_payload`): U8 must extend the export payload with the keyfile-wrap parameters it defines, teach import to reconstruct a mode-1 vault, and re-map that refusal to its distinct error — the same re-map obligation U6's unlock left it.

### U9 — Implement per-work record store (vault records + journal persistence API)
- Milestone: M1
- Size: L
- Deps: U5, U6; S: journal record shapes (S10 supplies the content schema — input, not an ordering dep; S10 in turn builds on this store); A: `.ots`/TSA artifact shapes (slots exercised M2); C: zeroizing `W` type
- Spec: Vault (lines 141–145); Architecture receipt contents (line 69); Seal journal (line 145)
- Do: Implement the encrypted per-work record store: for each work — `W`, `seal_id`, title, network, file paths (paths live in the vault, not the manifest), per-work cost, completion state, the seal journal area (staged ciphertext bytes + per-unit nonce + target address; content shapes owned by S), the journaled `PaymentReceipt` (D37 map shape), the **D36 consent record** `{total_ant_atto, gas_estimate, consent_time, channel}`, the **D45 invocation-identity block** (network + exact ordered lexically-absolutized input-path list + seal-shaping flag set — the key resume detection matches on), and anchor-artifact slots (`.ots` files, TSA tokens, fetch dates). Every record AEAD binds the KDF header + record identity as AAD (D42 rider — splice resistance). Provide the write API the seal pipeline uses, with the receipt write being a single atomic, fsync'd operation so "journaled the instant the tx lands" is real under crash. Provide read/query APIs for `list`/`show`/`status`/`reveal`/`restore`.
- Accept:
  - record schema versioned and round-trip tested with fixture works (complete, incomplete-pre-pay, incomplete-post-pay/pre-finalize, degraded, UNANCHORED) — including the consent record and invocation-identity block
  - receipt-write atomicity test: simulated kill immediately after the write API returns leaves the receipt readable on reopen (feeds S's no-double-pay E2E)
  - all record content unreadable without vault unlock; `W` exposed to callers only as C's zeroizing type; a record blob moved between slots fails authentication (D42 AAD)
  - staged-ciphertext retention per **D43**: the journal → cache reclassification at `complete` is a state-tag write (same bytes); cache-state entries are **excluded from `vault export`** and their loss is never an error, while journal-state entries are always exported
- Notes: This is the storage substrate for S's resume semantics — S owns *when/what* to journal, U9 owns *where/how durably*.
- **[2026-08-01 execution note (U9 landed; wave 2 lane ε)]** Module `vault/store.rs` + suite `tests/work_store.rs`. Shape: one directory per work (`store/works/<seal-id-hex>/` — seal_id is the store key, drawn at seal start; work_id is a meta field set once the manifest exists) holding `meta` (versioned canonical-CBOR WorkRecord: `W`, seal_id, network, state + degraded/unanchored flags, both D45 path lists, the D45 shaping-flag set, optional work_id/cost/D36-consent), `journal/<entry>` (opaque S10 bytes keyed by canonical-decimal u64), `receipt` (opaque bytes; single atomic fsync'd write), `anchors/<slot>` (opaque A bytes; slot grammar `[a-z0-9][a-z0-9._-]{0,63}`). Deviations + recorded choices, inline in the module docs:
  1. **Receipt/journal content is opaque bytes, not typed structs**: `PaymentReceipt` has no serde today — "deterministic serde for vault persistence" is S7's recorded deliverable, exactly as journal-entry shapes are S10's — so the store API takes/returns bytes and the U9/S boundary stays where the task text drew it ("S owns when/what, U9 where/how durably"). No antseal-net dependency enters antseal-cli at U9. The entry's "store the journaled PaymentReceipt" is satisfied as the durable slot + atomicity contract; the byte contract lands with S7/S10.
  2. **Enumeration without decrypting bodies = readdir + small meta decrypts**: no plaintext index exists (D42 puts indexes inside the AEAD); the directory listing of random-hex names is the index. Executed proof: with every journal/receipt file flipped to garbage, `list_works` + `load_meta` still answer. Honest limit re-recorded from U5/U6: file-count/size/mtime metadata of a locked vault is visible; content and linkage are not.
  3. **D43 reclassification = `mark_complete` (a meta state-tag write)**: proven byte-identical journal files across the transition. The cache-exclusion consumer is U12's export; the tag is what it reads.
  4. **The D42 splice matrix U5 deferred is executed at file level**: meta↔meta across works, journal entry across works, journal entry across entry keys, receipt→meta (class confusion), anchor slot→slot — every swap is an authentication failure; intact neighbors still open (falsifiability).
  5. **Record-schema versioning**: `[version, body]` envelope like the vault header; a future-version meta refuses distinctly (never parses the body) and maps to the vault-newer-version class (code 16 — message says "vault format", record version noted as the same family; U2 owns any wording split).
  6. **UTF-8-only paths in v1 records** (`utf8_paths` helper for U13): non-UTF-8 spellings refuse via D46's invalid-argument class pre-consent. Recorded v1 limitation.
  7. Journal-entry filenames are canonical decimal (leading-zero names are alien — `"007"` beside `"7"` would alias one AAD entry key onto two files); alien entries anywhere in the store are loud (`VaultAuthFailure` collapse), never silently skipped; inert `.tmp` dotfiles from the U5 atomic writer are the one skipped shape.
  8. Consent record carries D36's exact field set `{total_ant_atto, gas_estimate_wei, consent_time, channel: interactive|yes-flag}` (u128 amounts as 16-byte BE bstr — CBOR uint stops at u64); latest-wins single field per D36 rule 4.

### U10 — Store Arbitrum wallet key under its own vault sub-key
- Milestone: M1
- Size: S
- Deps: U6, U9; C: HKDF-based sub-key derivation + AEAD; S: wallet key material type
- Spec: Vault (line 143, "wallet key lives inside the encrypted vault but under its own sub-key")
- Do: Derive a dedicated sub-key from the vault master key (label-separated) and encrypt the Arbitrum wallet key record under it, separately from work records, so the wallet's blast radius is decoupled and future external-signer support can remove it cleanly. Expose a narrow accessor used only by S's payment path.
- Accept:
  - wallet record decrypts only via the sub-key path; corrupting the wallet record leaves work records readable, and vice versa
  - wallet key bytes never appear outside C's zeroizing types; U21 harness covers wallet-path errors
- **[2026-08-01 execution note (U10 landed; wave 2 lane η)]** Module `vault/wallet.rs` + `UnlockedVault::wallet_subkey` (session.rs); suite `tests/wallet_record.rs` + in-module key-isolation tests. Choices, argued inline:
  1. **Sub-key = HKDF-SHA256(salt = "", ikm = vault key, info = `"antseal-cli vault v1: wallet record sub-key"`)** — same construction family as C's derivations; label frozen by a tripwire test (changing it orphans stored wallet records). The record rides the existing cipher.rs AEAD (`RecordIdentity::Wallet`, id 1, reserved by U9) so both splice defenses hold: key AND identity; the key half proven in isolation by a crate-internal forgery test (main-key blob, correct identity → auth failure).
  2. **"Sign-capable handle" for M1 = `WalletKeyHandle`** (no Clone/Display, redacted Debug, zeroize-on-drop, single `secret_bytes()` accessor): the raw-bytes-under-narrow-scope accessor S's pay path consumes. NO evmlib/ant-core edge enters antseal-cli — the seam is documented in the module rustdoc: scalar validation happens at `init` against the pinned evmlib parse (U11, S-side, behind `ant-backend`) and at seal time in the backend adapter; this module stores/returns 32 opaque secret bytes only.
  3. New crate edges: `hkdf`/`sha2` (existing workspace pins; sha2 carries the D88 zeroize feature via the single declaration). The end-to-end sentinel scan remains U21's; in-tree coverage = Debug redaction + error classes (auth-fail/absent-is-None/wrong-length-collapses-to-auth).

### U11 — Implement `init`: vault create, wallet generate/import, funding instructions, network config
- Milestone: M1
- Size: M
- Deps: U4, U6, U7, U8, U9, U10; S: wallet keygen + address derivation + import-format validation (S5)
- Spec: CLI surface (lines 147–149, `init`); Vault (line 143); Networks (line 20); Positioning (line 28)
- Do: Implement `init` per **D39's hybrid model**: with a TTY, a short wizard — passphrase entry + confirmation (no-echo), wallet source (generate / import with no-echo paste), the U8 wrap offer, and the D40 KDF question — in that order, where any value already supplied by flag/fd is not asked again; without a TTY (or under `--json`), nothing prompts — missing required inputs abort with the dedicated codes (D51/U3 contract). Create the vault (passphrase per U7/D41; KDF choice per **D40** — Argon2id default, scrypt only as this explicit selection, low-RAM = typed hard error with no fallback prompt; optional wrap per U8/D50). Wallet key **generate** (OS CSPRNG via S; the safe default branch) or **import** (**D44: raw hex only**; key material via `--wallet-key-fd` non-interactively, never argv/env), stored per U10; a mnemonic phrase gets the distinct **"mnemonic import is not supported in this version"** copy (D44), not a generic parse error. Print the wallet address plus per-network funding instructions (Arbitrum One: ANT token + ETH for gas; sepolia/devnet: faucet/Anvil notes per P16/P17 runbooks); write network choice into config (U4). Re-running `init` over an existing vault **refuses absolutely — no `--force` exists (D39)**: the error names the vault path and says move/remove it manually (after `vault export` if the contents matter).
- Accept:
  - E2E: fresh machine → `init` → funded devnet wallet → U13 seal succeeds (feeds M1 devnet E2E)
  - both generate and import paths tested; invalid import material rejected per the D44 set (wrong length, non-hex, out-of-range/zero scalar, whitespace); the mnemonic case shows the distinct not-supported copy
  - wizard order snapshot-tested; every wizard question answerable by its D39 flag/fd equivalent (flag-supplied values not re-asked); fully-flagged non-TTY `init` completes with zero prompts
  - address + funding instructions printed for all three networks; copy uses possession language, no "notary"
  - `--json` fixture registered (address, network, vault path; no key material)
  - existing-vault refusal tested (refusal copy names the path; no override flag parses)
- Notes: Milestone inference — `init`/vault must precede M1's E2E (spec assigns no milestone). Q23 (M4) later harmonizes the printed funding copy with the docs — consumer, not a dep. Open decisions 1/2/6 all closed 2026-08-01 (D39/D40/D44).
- **[2026-08-02 EXECUTED (M1 lane m1-cli2)]** Landed as `antseal_cli::init` (`run_init`, `InitReport`, `InitPrompt`/`TtyInitPrompt`, `WizardStep`, `funding_lines`, `standing_warnings`, `existing_vault_refusal`) plus the `init` handler in `commands.rs`; `run.rs` dispatch goes live. Execution notes:
  1. **The wizard is a seam, and the order is a value.** `passphrase.rs` reserved this ("U11's wizard will define its own interaction seam over this module's public functions"); `InitPrompt` is it. `InitReport::asked` records the questions this invocation actually walked, so D39's order is asserted as data rather than inferred from control flow — which is what lets the order test run without a pty. The passphrase step stays `obtain_passphrase`'s (U7 owns its prompt and its floor); `init` owns only its *position*, first.
  2. **Deviation — `--wrap keyfile` is a typed not-implemented refusal.** D50 registers the mode and D39 reserves the flag, but **U8's engine has not landed**: `create_vault` hardcodes `WRAP_MODE_NONE` and `unlock_vault` refuses non-zero modes. Creating a vault whose header claimed a wrap it does not have would be the worst of the three options. The refusal fires **before the passphrase is collected** (the U36 rule: asking for a secret and then refusing is rude as well as pointless), and the test pins that ordering by passing *no passphrase channel at all* — a late refusal would surface as passphrase-unavailable and fail. The wizard's wrap question consequently offers only `none` in this build rather than offering a choice the vault cannot honour; the flag still parses and still refuses loudly. When U8 lands, both come back together.
  3. **Deviation — `read_passphrase_fd` split into a shared `read_secret_fd`** returning a bare `PassphraseFailure`. `--wallet-key-fd` must reuse D41's exact byte semantics (1 KiB cap, one trailing LF/CRLF stripped, NUL/LF/CR rejected) while reporting its **own** error class: a wallet key is a different secret on a different channel, and reporting passphrase-unavailable would send the user to the wrong flag. One implementation of the bytes, two classes.
  4. **Machine mode answers the three defaulted questions silently** rather than aborting (D51 read with D39/D40/D50): `generate`, Argon2id and declined are answered *by decision*, so they are not "missing required inputs". What does abort: no passphrase channel (passphrase-unavailable), and `--wallet import` without `--wallet-key-fd` (usage, naming the flag).
  5. **Nothing is written until every refusal has had its chance.** The existing-vault check, the wrap refusal, the whole wizard, and the D44 import validation all run before `create_vault`, so every failure path leaves the filesystem as it found it — asserted in the rejection-class test, which checks `vault.header` still does not exist after each of the five D44 classes.
  6. **The address is EIP-55 checksummed** (`antseal_net::checksummed`, in the default graph since D89) — the form wallets and explorers show, and the form that catches a mistyped address. The import test pins the whole chain paste → validate → store → derive against scalar 1's published generator address, an answer nothing in this repo computed.
  7. `machine_mode.rs`'s registry moves `init` off the not-implemented catch-all to `(2, "usage")` — over the fixture vault it hits the D39 refusal, which is first by design — and the envelope fixture gains an `[init] result` line. Both are rendered by the real producers (`existing_vault_refusal`, `InitReport::json`), never hand-copied: U19's rule. `cli_surface`'s stub exemplar moves from `init` to `seal`, with a comment saying it must always name a command that is *actually* still stubbed.

### U12 — Implement `vault export` / `vault import`
- Milestone: M1
- Size: M
- Deps: U5, U6, U9
- Spec: CLI surface (lines 147–149, `vault export|import`); Vault (line 143); M1/M4 verification (lines 172, 175)
- Do: Implement `vault export` producing a single encrypted backup file per **D47**: the Q2-reserved magic `ANTSEAL VAULT EXPORT` + a versioned deterministic-CBOR header (its own KDF block, **fresh salt**, AAD = magic‖header) + **one AEAD over the full serialized logical vault state** — all records, journal, anchors, wallet, and config INCLUDED; the D43 cache EXCLUDED — under the same vault passphrase (proven by the unlock the export performs; keyfile factor preserved). **Mandatory post-write self-verify**: the written file is re-read, decrypted, and structurally validated before export reports success. `vault import` reconstructs `~/.antseal/` on a clean machine: parse the header under **D40's pre-auth caps** (same KDF-bomb rule, second call site), decrypt and validate the **entire** logical state in memory, then perform an atomic install — never a partial write into a half-built vault. Import refuses to overwrite an existing vault; per **D51** there is **no bypass flag by design** (scripted overwrite-import unsupported in v1 — the refusal copy states the manual workaround, catalogued via U31). This pair is what the M1 "restore from vault backup on a clean tree" E2E and the M4 disk-loss drill exercise.
- Accept:
  - round-trip test: export → wipe `~/.antseal/` → import → `restore` of a fixture work succeeds byte-identically
  - export file is unreadable without the passphrase (and wrap factor if configured); self-verify failure (injected write corruption) → loud distinct error, nonzero exit
  - import validates fully in memory before touching `~/.antseal/` (kill-during-import test leaves no partial vault); an over-cap import header is rejected pre-allocation (D40 caps)
  - import-over-existing-vault refusal tested — and no bypass flag parses; `--json` fixtures for both subcommands
  - incomplete works (journal state) survive the round trip so a resumable seal stays resumable; cache-state entries are absent from the export (asserted) and their absence post-import is not an error
- Notes: Milestone inference — needed by M1 E2E's vault-backup restore (S19); M4 drill (Q32) consumes it. Q2's secret-guard gains its exclusion event when the magic literal lands (D47 flags it — same PR).
- **[2026-08-01 execution note (U12 landed; wave 2 lane η)]** Module `vault/export.rs` (format + both engines) + handlers in `src/commands.rs` (run.rs dispatch goes live for `vault export|import`); suite `tests/vault_export.rs` + in-module failpoint tests. Interpretations + deviations, each argued in the module docs:
  1. **Payload = plaintext logical state, re-encrypted at import into a NEW vault** (fresh vault salt/key, records re-encrypted through the current store APIs) — the reading D47's layout-independence argument compels; "round-trip byte-identical" is satisfied at the logical layer (meta fields, journal/receipt/anchor/wallet/config bytes) and by S19's restore at integration. Schema: uint-keyed maps + sorted arrays-of-pairs only (the house codec is deliberately uint-map-only); works sorted by seal_id; strict v1 reader (unknown keys reject). D43 enforced BOTH ways: writer exports journal entries only for state ≠ complete; reader rejects cache bytes in a claimed-complete work. Cache exclusion proven by arithmetic in the flagship test (3×2×200 kB planted cache; file < one blob).
  2. **Self-verify runs on the synced temp file, then renames** (deviation-in-mechanism from D47's "write then verify", recorded): identical bytes verified (rename is metadata-only), and a re-export onto an existing backup path can never replace a good backup with a bad file. The verify is the FULL import-side validator (`validate_export_bytes` — one rule, two call sites) incl. a fresh KDF from the file's own header, + SHA-256 digest compare vs the in-memory payload.
  3. **Import order**: existing-vault refusal FIRST (before even reading the file — tested), then bounded read (1 GiB v1 cap; over-cap collapses into import-auth as outside-the-format), then pre-auth header parse under the D40 caps **before the passphrase is collected** (a bomb header is rejected without prompting — proven by a panicking passphrase closure), then KDF → AEAD → strict decode → cross-validation, then temp-dir build + one atomic directory rename + post-install verify (fresh unlock + full walk). Existing-vault refusal is absolute in every mode incl. TTY (the task text + D51's "no bypass in v1" read over D47's looser "without explicit confirmation" phrasing; the D51 prompt-class registry will record the destructive-confirm class with channel *none, refuse-always*).
  4. **Non-zero wrap modes refuse at import** (never a silent mode-0 downgrade of a wrapped export); U8 re-maps to its distinct error — the session.rs unlock precedent. Post-auth payload malformation = `internal` (only our writer or the passphrase holder can author it); post-release payload schema changes are format_version bump events (recorded for U18's bookkeeping flag — v1 ships no bookkeeping slot; pre-release U18 may extend v1 while both sides move together).
  5. Default filename `antseal-vault-export-YYYYMMDD-HHMMSS.sealvault` (UTC, std-only Hinnant civil-date; timestamped so repeated exports never silently replace a backup); extension advisory, identification by magic. Export holds the U5 single-writer lock for a consistent multi-record snapshot. Machine mode at the passphrase seam honors `--json` (the ε primitive covers non-TTY/fd-0; commands.rs adds the `--json`-with-TTY clause) — U3 folds this into its framework.
  6. The secret-guard exclusion landed same-commit, exact-path (`crates/antseal-cli/src/vault/export.rs` only); tests reference `EXPORT_MAGIC`, never the literal — the guard itself caught the first draft's literal in the test file, which is the system working.

### U13 — Orchestrate the seal pipeline in exact normative order (M1 scope, `--no-anchor`)
- Milestone: M1
- Size: L
- Deps: U2, U3, U9, U11, U14, U4; G: canonicalization + unit split + fine-tree build (G14); C: unit encryption, manifest encryption, hybrid signing; F: manifest body build/encode, `work_id`; S: address computation, `quote_batch`/`pay`/`finalize_batch`, journal/resume semantics (S12)
- Spec: Core user flows 1 (line 34); Cryptography DAG (line 90); Seal journal (line 145); M1 (line 154); CLI surface (line 149)
- Do: Implement `seal <path>…` driving the exact normative order: canonicalize → encrypt units (local, journaled via U9 as S directs) → build + sign manifest → encrypt manifest → compute all ciphertext addresses (units + encrypted manifest) → `quote_batch` over the full blob set → permanence consent (U14) → [anchor stage: M2, U22 — in M1 reachable only as `--no-anchor`] → `pay` with the receipt journaled via U9's atomic write **before** `finalize_batch` is invoked → `finalize_batch` → print work-id + cost (and in `--json`). Plumb `--title` (embedded in manifest via F), `--split blank-lines`, `--force-text`, and `--no-fine-tree <glob>` (CLI-side glob → per-file opt-out set handed to G). Enforce statically that `--no-anchor` is rejected when the effective network is `arbitrum-one` (this guard is born in M1 with the flag), and that M1 seals without `--no-anchor` fail with "anchoring arrives in M2" rather than silently sealing unanchored.
- Accept:
  - integration test (MockBackend) asserts the step order — no payment call before consent returns affirmative, no `finalize_batch` before the receipt is persisted, no upload before payment
  - **D46 argument validation**, mock-ordered: a directory argument, a non-regular file (fifo/symlink-to-dir/device), and a lexical-duplicate path each hard-error at plan validation **before** any consent render, journal write, anchor submission, or backend call — same behavior under `--dry-run`; distinct invalid-seal-argument class (U2)
  - kill-between-pay-and-finalize and kill-mid-upload scenarios pass S's M1 E2E via this orchestration (no double payment; byte-identical re-upload)
  - `--no-anchor` + `--network arbitrum-one` (flag or config default) → rejection with dedicated exit code, tested
  - multi-file seal with `--split` produces expected unit set (G asserts content; U13 asserts wiring); `--no-fine-tree` glob matches recorded per-file in the descriptor
  - scripted-seal harness drives the full flow non-interactively with the passphrase over a `--passphrase-fd` pipe (D41) — the shape S17–S19 reuse
  - final line prints `work_id` + actual cost; `--json` fixture registered; cost persisted to the work record (U9)
- Notes: Directory-argument handling **decided — D46 (2026-08-01): files only**, recursion via shell globbing; G14/G5 may assume regular files downstream. Committed path normalization is G/F's; U13 passes original paths and stores them in the vault (the D45 invocation-identity block records the lexically-absolutized list). The command handler drives the `antseal_cli` lib pipeline (D34) — no orchestration logic in the binary. Candidate folded here by the planning round (no new ID): a seal-time guard when an input path lies inside a `./antseal-restore-<work-id>/`-shaped default restore target (D48's default output) — decide warn-vs-nothing at implementation.
- **[2026-08-02 EXECUTED (M1 lane m1-seal)]** Landed as four modules — `seal_plan.rs` (D46 + the flag guards + `--no-fine-tree` matching), `seal_resume.rs` (D45's matching table + the resume-plan render), `seal_consent.rs` (U14), `seal_run.rs` (the orchestration) — plus `commands::seal` and live `run.rs` dispatch. Execution notes:
  1. **Order of refusals, decided and asserted**: `--no-anchor` × `arbitrum-one` (flag-only, no filesystem access) → D46 argument validation → the M1 anchor-stage gate → `--no-fine-tree` matching. The mainnet guard is first because it mirrors `Pipeline::seal`'s own first statement, and the CLI **constructs `SealError::NoAnchorOnMainnet` and converts it** rather than paraphrasing — so the two layers are the same sentence with the same exit code by construction, not by review. D46 precedes the milestone gate deliberately: a directory argument is wrong permanently and the anchor gap is temporary.
  2. **D46 is one pass, and rule 4 rides inside the report.** All four rules are checked together (the entry's own reason for listing rule 4). Rules 1–3 → `InvalidSealArgument` (27) naming **every** offender; a missing path alone → `Io` (4); a missing path *beside* a D46 violation is reported inside the 27, because a user with both should fix both in one round trip. `stat`-not-`lstat` per D46 §1 — a symlink to a file is accepted, a symlink to a directory is a directory (both tested).
  3. **New error variant `AnchorStageUnavailable`** (class `not-implemented`, code 3 — no new class, no new code). Deliberately not `NotImplemented{command:"seal"}`: `seal` **is** implemented and saying otherwise sends a user hunting a milestone that already arrived (`backend::unavailable`'s distinction, applied a second time). It fires at *plan validation* so `--dry-run` refuses identically — D49 makes a dry-run a faithful prefix, and the anchor stage sits after consent, so a truncated run would otherwise rehearse happily for a seal the real command declines.
  4. **`--no-fine-tree`: the previous lane's recommendation implemented and strictly extended by two metacharacters, with no dependency.** A pattern with no metacharacters *is* exact-path matching. `*` and `?` are added (segment-scoped, never crossing `/`), matched against the as-given spelling, the basename, then the absolutized path. Argued: the frozen help says `<GLOB>` and cannot be re-blessed; a glob crate would import a *semantics* frozen into permanent paid artifacts without ever being written down (D46 rule 2's own argument); thirty documented lines are the smaller frozen surface. **And the silent-miss hole is closed from the other side**: a pattern matching zero files is a hard `Usage` error, so `--no-fine-tree '*.bin'` can never quietly buy a fine tree the user asked not to have.
  5. **`..` is kept, not popped** (D45 §1 verbatim). Popping is unsound through symlinks — `link/../x` is not `./x` — so it would refuse legitimate seals. The cost is a duplicate-detection miss on `a/../a.txt`; the alternative refuses correct invocations, which is worse.
  6. **The `ant-backend` build was RED at base** (`b11be2f`): D89/U37 added `balances()` to the trait and the delegating impl but not to `mod sealed`'s `use super{…}`, and no CI lane type-checks the feature's library, so nothing caught it. One identifier added, no behaviour; recorded as **U39** (the missing lane) and flagged as a cross-lane touch of `backend.rs`.
  7. **The live path is wired** (`SealBackend::connect` → `run_seal`), including the pre-passphrase `NetworkConfig::select` refusal for a devnet with no exported environment. Its D37 sink (`SealReceipts`) is in-memory and says why: the backend is constructed **before** the pipeline draws the `seal_id`, so no receipt can be written to a work record whose key does not exist yet. The receipt that matters is durable through the pipeline's own `put_receipt`; the residual >256-blob multi-tx window is **U40** (S27's remaining half). — **[2026-08-02 SUPERSEDED by S36/U40 (lane m1-s36)]** `SealReceipts` is **deleted**. Its stated reason was answered by S31's `arm_receipts`, which names the work at the one moment the `seal_id` exists, so the backend being built first never was the obstacle. `commands::seal` now unlocks straight into a `SealSession` (`Arc`-owned vault + the one `VaultReceiptSink`), hands `session.receipts()` to `SealBackend::connect`, and `run_seal` takes the session — so the sink the backend fires is the sink the pipeline arms, by construction. The window was **not** >256-blob-only in practice: a forced cap reaches it with five blobs, and the devnet row proves a real `antseal seal` re-paid before this landed. Two ungated source scans pin the bypasses (one sinkless journal site, one sink-minting site, both `seal_session.rs`), each proven red.
  8. **Two defects found by re-reading the code before committing, both would have been expensive.** (a) `--dry-run` over an invocation that exact-matches a resumable work fell straight into `Pipeline::resume`, which **pays and uploads** and takes no dry-run parameter — a rehearsal that spends money. D45 §5 asks only for the plan and zero side effects, so that is what it now returns, and it returns *before* the balance read, so the rehearsal makes no backend call at all; the regression test asserts the whole vault is byte-identical afterwards. (b) The consent gate keyed the resume plan on `ConsentRequest::resume`, which the pipeline computes as `proofs_expired || anchor.is_none()` — **false** for a work killed before the anchor gate, i.e. the commonest resume there is. D45's "one merged render" would have quietly become no render. The gate now keys on D45's own detection (a non-empty plan means D45 matched, and nothing else can make it non-empty); pinned by a unit test that passes the exact `ConsentRequest` a `Staged` resume produces.
  8. Superseded ground survey (kept — every item was accurate):
  1. **D46's error variant exists and is still constructed by nothing.** `CliError::InvalidSealArgument { problems: Vec<String> }` (`crates/antseal-cli/src/error.rs:537`, exit 27) is declared, `Display`-formatted and plural-aware, and `grep` finds no constructor anywhere. Same for the two D45 classes `ResumeOverlapNotExact` (:520, exit 25) and `ResumeFlagMismatch` (:528, exit 26). The previous lane's finding is re-confirmed at HEAD.
  2. **The pipeline half is complete and needs no changes for M1 scope.** `Pipeline::seal` (`pipeline/seal.rs:232`) already walks the normative order, already enforces `--no-anchor` × arbitrum-one beneath the CLI (`SealError::NoAnchorOnMainnet`), and already implements D49's truncation as a `PostQuote` return rather than a parallel mode. What is missing is entirely above it: reading files, building `SealFile`/`SealRequest`, D46 validation, D45 matching, and rendering the outcome.
  3. **The `--no-fine-tree` glob decision is still open and is a dependency-policy event either way.** `SealArgs::no_fine_tree` is `Option<String>` (one occurrence, per U1's recorded interpretation) while `SealShapingFlags::no_fine_tree` is `Vec<String>`, and **no glob matcher exists in the dependency graph** — verified against the workspace manifest. `FileFlags::with_no_fine_tree_matched` takes an already-decided bool, so the matching is entirely U13's. Recommended, and not taken here: exact-path matching against the given argument list first (no new dependency, no policy event), with the glob crate raised as a §1 event only if the entry genuinely requires shell-glob semantics.
  4. **`SealRequest` needs one clock and one app_version from the handler** — both injected by design (`claimed_time_unix_secs`, `app_version`), which is where U13 decides the one place a clock is trusted.

### U14 — Implement the permanence-consent gate
- Milestone: M1
- Size: M
- Deps: U2, U3, U13 (interface only — the consent-hook signature; U13 invokes this gate); S: ANT + ETH balance queries (S8); S: quote totals; G: file list + byte totals
- Spec: Core user flows 1 (line 34, "permanence consent")
- Do: Before anchoring/payment, render the consent report: file list, byte totals, the *true, complete* quote (every blob including the encrypted manifest), and current ANT + ETH balances displayed beside it; the exact warning "upload is permanent, public, and irreversible"; then require interactive confirmation, with `--yes` bypassing for scripts. On a **resume** invocation the render additionally shows the prior consented totals from the S10/D36 consent record with a visible drift flag when the fresh totals differ (either direction) — display-only, never gating; absent record → render without the prior-consent line (defensive). Pre-flight balance-vs-quote comparison and pay-time failures must surface as **distinct** insufficient-ANT-token vs insufficient-ETH-gas errors (U2 codes). Machine-mode behavior is an **input**: the gate consumes U3's machine-mode determination (D51) rather than probing TTY-ness itself — machine mode without `--yes` aborts with the consent-not-obtained code.
- Accept:
  - consent text snapshot-tested and contains the exact permanence wording, file list, byte totals, quote, and both balances
  - resume-render snapshot fixture: prior consented totals + drift flag beside the fresh quote (one added fixture, D36)
  - declining aborts before any anchor submission or payment (asserted via mock call log)
  - insufficient-token and insufficient-gas each tested → distinct message + exit code
  - `--yes` path and machine-mode-abort path both tested (the mode arriving via U3's hook input, not a local isatty probe); consent report reproduced on stderr under `--json`
- **[2026-08-02 EXECUTED (M1 lane m1-seal)]** Landed as `antseal_cli::seal_consent` (`PERMANENCE_WARNING`, `ConsentReport`, `SealConsent`, the `ConsentPrompt` seam and `TtyConsentPrompt`), injected into the pipeline as its `ConsentHook`. Execution notes:
  1. **`ConsentRequest` was NOT extended — a deliberate non-change, and the survey's prediction is overturned.** The file list, byte totals and balances are **per-invocation** facts; `ConsentRequest` carries the **per-call** ones. A `ConsentHook` implementation may hold its own invocation context — that is what the trait seam is for — so U14 lands with **zero edits inside `pipeline/`**, the pipeline stays balance-unaware, and lane ι's landed contract survives byte for byte (which also removes the whole merge hazard with the concurrent S31 lane).
  2. **The permanence wording is a constant with one author**: `PERMANENCE_WARNING = "upload is permanent, public, and irreversible"`, verbatim from MVP-SPEC.md line 34, asserted against **both** the constant and its literal text so a well-meant rewording fails.
  3. **The gate takes data, never a backend** (D89 §3): `run_seal` reads `backend.balances()` once and hands the report in as a value, so the render is a pure function and its assertions are cheap. The shortfall rule is `antseal_net::preflight` **called**, never restated — a second implementation of "which shortfall fires first" is exactly how a user gets told to bridge ETH when they need ANT.
  4. **Render first, then refuse.** A shortfall shows a `** SHORT by N **` marker beside the balance *and* returns the typed class (20/21). Both audiences are served by one pass: the report explains it to a person, the exit code to a script.
  5. **D36's drift flag is bidirectional and display-only** — `[CHANGED: +N]` / `[CHANGED: -N]` beside the prior consented total; an absent record renders without the line and is never a reason to skip the gate. The resume-plan lines and the consent screen are **one** render (D45's merged pass), because there is only one `ConsentHook` in the process.
  6. `--yes` grants through `ConsentChannel::YesFlag` **and still prints the report**; machine mode without it returns `MachineModeWithoutYes`; a `NeverAsked` prompt double turns any D51 violation into a failure rather than a hang. The consent time is injected, not read from a clock, so the journaled `ConsentRecord` is deterministic under test.
  7. Superseded ground survey (item 2's prediction is the one overturned above):
  1. **Both balances are now reachable through the seam.** `StorageBackend::balances()` exists and `MockBackend` implements it with a settable canned report (`with_balances`/`set_balances`), so U14 Accept row 1's "both balances" and row 4's insufficient-token/insufficient-gas split are default-lane rows with no network and no feature. The shortfall rule is the pure `antseal_net::preflight(&BalanceReport, &CostQuote)` — already unit-tested for the S8 ANT-first order and the u128 boundary — so U14 must *call* it, never restate it.
  2. **`ConsentRequest` still carries no file list, no byte totals and no balances** (`pipeline/consent.rs:38-57`: seal_id, quote, blob_count, prior, resume, proofs_expired). Extending it touches lane ι's landed contract; do it **additively** — new fields with the existing ones untouched — and update the two construction sites in `pipeline/resume.rs::consent_anchor_pay` plus `tests/common/mod.rs::ScriptedConsent`. The pipeline will need to call `backend.balances()` before rendering consent, which is the "new wire" U37 named and D89 §Consequences shapes.
  3. **The gate takes data, never a backend** (D89): a `BalanceReport` and a `PreflightReport` as values, so the render is a pure function and its snapshot is cheap.
  4. The permanence wording is MVP-SPEC.md line 34's "upload is permanent, public, and irreversible", verbatim; `--yes` is `SealArgs::yes`; machine mode arrives from `crate::machine::machine_mode_for` (never a local isatty probe) and without `--yes` aborts with `ConsentOutcome::MachineModeWithoutYes` (`error.rs:121`), which already exists.
- **[2026-08-12 — U29 EDITED THIS ROW'S SHARED COPY, deliberately and on the record]** `ConsentOutcome::describe()` — minted here, and until U29 read only by this gate — now says *"nothing was paid, anchored, uploaded, **or disclosed**"* in **both** arms (declined and machine-mode-without-`--yes`), moving two lines of `crates/antseal-cli/tests/snapshots/cli-errors.display.txt`. The reason is that U29 made this class's **second** consumer: `reveal`'s disclosure gate returns the same `ConsentOutcome`, and the old sentence was **true but silent on the one thing a declining `reveal` user is deciding about** — a person who declines a disclosure and is told only that nothing was paid, anchored or uploaded has not been told the thing they refused did not happen. The seal gate's own meaning is unchanged (a declined seal still discloses nothing, so the added clause is true of it too), and no `ErrorClass`, exit code or `--json` key moved. Recorded here rather than only on U29's row because this row owns the copy: a future edit to either arm now has two audiences, and neither may be reworded for one command alone. The eventual single owner of the *spelling* is U31/Q20's copy catalogue; what is fixed here is that the sentence has **two** consumers, not one.
  - **The extraction that came with it**: `seal_consent::ask_on_tty(question)` is now the **one** `/dev/tty` implementation and the **one** "EOF / blank / unrecognised = no" rule for both consent prompts, called by `TtyConsentPrompt` (this row) and `TtyDisclosurePrompt` (U29). The `#[cfg(feature = "ant-backend")]` construction site was **deliberately not touched** — U29 could not compile that feature, and a literal edited without its lane's build is an unverified edit (the standing ledger rule).

### U15 — Implement seal warning moments: title visibility, `--no-fine-tree`, fine-tree cost estimate
- Milestone: M1
- Size: S
- Deps: U13; G: fine-tree cost estimator (G10)
- Spec: Core user flows 1 (line 34, title warning); Canonicalization (line 85)
- Do: When `--title` is set, warn that the title is embedded in the plaintext manifest every proof bundle carries and will be visible to all bundle recipients. When `--no-fine-tree` matches files, warn those files are **permanently** whole-file-reveal only. For large files, print G's estimated fine-tree cost before consent so the user sees it ahead of the quote. All three surface before the consent prompt.
- Accept:
  - each warning snapshot-tested; emitted on stderr; present in the pre-consent flow (mock-ordered)
  - `--no-fine-tree` warning names the matched files; cost estimate appears above a configurable size threshold and is absent below it
- Notes: Threshold for "large files" is a U-owned constant — document it.
- **[2026-08-02 EXECUTED (M1 lane m1-ux)]** `antseal_cli::seal_warnings` — `warnings_for(&SealPlan)`, `title_warning`, `no_fine_tree_warning`, `fine_tree_cost_lines`, `FINE_TREE_ESTIMATE_THRESHOLD_BYTES`. Execution notes:
  1. **One author, one render — the warnings live on `ConsentReport`, not in a second emitter.** The obvious alternative (an `eprintln!` from `run_seal` ahead of the encryption pass) was rejected because a second place that prints pre-consent copy is a second place that can drift, and U13 had just paid for exactly that (the resume plan vanished from the merged render because two layers disagreed about which flag meant "resume"). `SealConsent::new` therefore takes the whole `SealPlan` rather than its file list: the displayed files and the warnings now have one source and cannot disagree. Cost, stated: for a very large file the estimate is read *after* the trees were built, because the consent gate sits downstream of encryption in the normative order — what the number is for is the decision in front of the user, and that decision is made on the consent screen.
  2. **The threshold is argued against `MAX_CHUNK_SIZE`, not against a feeling about "large".** Autonomi caps one blob at 4 MiB (D32/D10), so a *binary* file — always a single unit — is unsealable above that, and any threshold near the ceiling leaves the line unreachable for exactly the case where `--no-fine-tree` is the relevant lever. 1 MiB is a quarter of the cap and ≈ 5.2 M compressions, around where the build becomes a noticeable share of seal time. The first draft used 16 MiB and the acceptance test caught it: the pipeline refused the fixture with the over-cap error.
  3. **A file the pattern opted out of gets no cost line**, because it builds no fine tree and a cost for it would be false; and the line says "before canonicalization" and names the byte count it used, because a text file's tree is built over the canonical rendition rather than the raw bytes and an estimate that claimed exactness it cannot have is worse than none.
  4. The title warning quotes the title back (a user who typed `--title "Q3 layoffs — draft for Legal"` needs to see those words beside the sentence, not just the flag name) and names the **single-unit reveal** as a recipient — the case a user is most likely to assume is safe.

### U16 — Implement `seal --dry-run`
- Milestone: M1
- Size: S
- Deps: U13, U14, U15
- Spec: CLI surface (line 149, `--dry-run`)
- Do: Define and implement `--dry-run` per **D49**: it is the real pipeline **truncated after quoting** (S12's post-quote truncation barrier — a prefix, not a parallel mode): run every cheap, failable, local step (D46 argument validation, canonicalize, encrypt in memory, manifest build/sign, address computation, plan-time cap checks) plus the real `quote_batch` **and the S8 preflight**, then print the full consent report and all warnings (U14/U15) and exit — no consent prompt, no anchor submission, no payment, no upload, and **zero vault mutation** (no journal entries, no work record — the journal-before-backend invariant is scoped to the paid path, D49). The quoted cost is labeled **indicative** (fresh nonces mean the real seal re-quotes different addresses from different peers — same argument that makes a dry-run unlinkable to a later seal). A failing preflight exits with the **real** insufficient-ANT/-gas codes, making dry-run a scriptable preflight gate. On a resumable work, dry-run renders the resume plan without consuming or mutating it (D45 §5).
- Accept:
  - after `--dry-run`, the vault byte-state is unchanged (hashed before/after in test); MockBackend records only `quote_batch` + balance reads
  - shortfall exit test: drained ANT (resp. ETH) under dry-run exits with the same distinct code the real seal would use
  - output includes files, byte totals, cost with the indicative label, and all applicable warnings; `--json` fixture registered
  - `--dry-run` combined with `--yes`/`--force-degraded`/`--no-anchor` still performs no side effects; dry-run on a resumable work shows the resume plan and mutates nothing
- Notes: Semantics decided by D49 (2026-08-01) — documented in help text.
- **[2026-08-02 EXECUTED (M1 lane m1-ux)]** The mechanism landed with U13 (D49's truncation is a pipeline *prefix*); this closed the residual rows. Execution notes:
  1. **Both shortfalls, not one.** ANT-short and ETH-short each assert their distinct code (20/21) and zero pay/finalize calls. One arm was not the row: a script branching on which asset is missing is branching on different remedies (acquire ANT vs bridge ETH), which is the whole reason S8 keeps them separate.
  2. **`--dry-run --yes --force-degraded`.** `--yes` is the flag that makes a real seal pay without asking, so a rehearsal that honoured it would buy the thing it was rehearsing. The prompt is `NeverAsked` and the mock's pay counter is the evidence: neither the gate nor the payment path is reached, no work record is created, and the vault fingerprint is byte-identical afterwards.
  3. **Two registered `--json` documents**, because a consumer must branch on them: the truncated rehearsal carries a quote, and D45 §5's rehearsal over a resumable work deliberately carries none (`quoted: false`). Both are rendered by the **real** producers — the rehearsal document through the actual U14 gate over a fixture plan, the resume document through `resume_plan_lines` — and the fixture plan earns all three U15 warnings on purpose, because `warnings` is a field consumers branch on and a fixture that only ever showed it empty would document nothing about its shape.
  4. `cli-surface.help.txt` was **not** re-blessed here — and that was the error, not the economy. **[2026-08-18, corrected by U80]** The claim *"`--dry-run`'s help already documents D49"* was true of the *citation* and false of the *content*: the help named D49 while contradicting D49 §Ruling 4, claiming a *"true cost quote"* — the exact phrase that ruling refuses by name (`docs/decisions/D49-dry-run-network-semantics.md:109-116`). **Citing a decision is not complying with it.** The snapshot then froze the non-compliance and it propagated into `docs/user/funding-your-wallet.md` **twice** — once as the quoted help block and once as a paragraph that existed only to gloss the refused phrase. U80 re-blessed the line to D49's own contract wording — *"the plan + quote (true pricing mechanism, indicative figure)"* — under **U1's** freeze (`crates/antseal-cli/tests/cli_surface.rs:84`, *"The surface is frozen (U1)"*), not U32's; U32 is the release freeze that would otherwise have ratified the phrase for shipping.

### U17 — Implement seal resume and abandonment UX
- Milestone: M1
- Size: M
- Deps: U9, U13, U14; S: resume/abandon decision logic (S11)
- Spec: Seal journal (line 145); M1 (line 154)
- Do: Wire the CLI entry to S's resume logic: re-running `seal` detects a matching incomplete work per **D45's matching table** — keyed on the recorded invocation identity (network + exact ordered lexically-absolutized path list + seal-shaping flag set): **exact match → auto-resume; overlap-not-exact or flag-mismatch → hard error before consent/payment** (two distinct resume-safety classes, U2, with near-miss error text naming the differing paths/flags); **disjoint → fresh seal with a notice**. No `--resume` flag, no interactive selection (D45). **One merged render on pre-pay resume (D45 + D36)**: the resume plan (what's staged, whether a receipt is journaled, what remains) and the re-consent render are a single U14 gate pass over the fresh quote — prior consented totals + drift flag included — never two prompts; re-consent is unconditional (D36 — money not yet spent), and declining leaves the work **incomplete, never abandoned**. No re-consent when a receipt is journaled (money already spent, finalize only, no `--yes` needed — D45/D51). **User-initiated pre-pay abandonment is in scope**: an explicit way to abandon a matching incomplete pre-pay work and start fresh (a deliberate command/flagged action, never a side effect of declining consent). Surface S's safety aborts loudly: changed source file → abort explaining re-encryption under a journaled nonce is forbidden; staged bytes unavailable → the work is **abandoned**, message states any payment is forfeited as a deliberate safety choice and a fresh seal will use a new `seal_id` and fresh nonces.
- Accept:
  - resume-pre-pay path re-prompts consent unconditionally through the ONE merged plan+consent render (snapshot); resume-post-receipt path skips consent and calls only `finalize_batch` (mock-asserted)
  - D45 matching table exercised: exact-match auto-resume; overlap-not-exact and flag-mismatch each produce their distinct error naming the mismatch; disjoint runs fresh with the notice
  - declining the merged re-consent leaves the work incomplete and resumable; the explicit abandonment action marks it abandoned (two different outcomes, both tested)
  - changed-source and missing-staged-bytes scenarios produce the two distinct messages/exit codes; neither ever re-encrypts (asserted via C/S mock)
  - abandoned work is marked in the record store and shown by `list` as incomplete/abandoned
- Notes: Consent-on-resume decided per **D36** (unconditional pre-pay re-consent) and **D45** (detection/plan) — no longer a U inference; recorded in help/docs (`seal` help documents implicit resume and the exact-match rule).
- **[2026-08-02 EXECUTED (M1 lane m1-ux)]** The resume half landed with U13 (D45 detection, the plan render, the merged consent pass). This closed the **abandonment** half and the aborts. Execution notes:
  1. **`seal_resume::abandon_pre_pay`, three rules in order.** A **complete** work is never abandoned — its ciphertexts are permanent, so discarding the record would only destroy the keys that read them. A **paid** work is never abandoned on request, and the authority is the **journaled receipt, not the state tag**: S16 found a real double-payment defect that came from trusting the tag, because D37's capture hook journals the receipt *inside* `pay()` and a crash in that window leaves a durable receipt on a pre-pay tag. S11 does abandon paid works, but only as a safety outcome when the staged bytes are gone and the alternative is re-encrypting under a journaled nonce; forfeiting a payment because someone typed a flag is a different thing. Everything else transitions through the S10 machine's own `set_state`, and already-abandoned is an idempotent **outcome** rather than an error (a script re-running an abandon must not fail).
  2. **The surface token is deliberately absent — U41.** D45's residual-risk section says the CLI verb-or-flag is "a surface addition to be decided deliberately, not smuggled in here"; `cli.rs` and `tests/snapshots/cli-surface.help.txt` were outside this lane's containment, and the brief forbade re-blessing the frozen help. The engine, its safety rules and its tests are what needed review; the flag is one line once U41 decides which.
  3. **The two D45 errors were instructing an impossible action.** Both ended "or abandon it first", with no command behind the words. They now name only exits this build can perform (finish it, or seal a copy under a different path) and `ResumeFlagMismatch` says plainly that discarding a staged work is not possible in this build, rather than implying a command exists.
  4. **`SourceChanged` is constructed by nothing, and that is the stronger outcome.** U17's Accept row wanted an abort when a source file changed; S11 implemented something better — resume never opens a source file at all, so a changed source is a non-event (S18 case (3) proves a resume with edited sources restores the *original* bytes). Documented on the variant; keep-vs-remove recorded as **U42** to be decided with R57.
  5. `StagedBytesMissing`'s copy now states what the entry asked it to: forfeiture as a deliberate safety-over-cost choice, **and** that a fresh seal draws a NEW `seal_id` and freshly generated nonces. Without that sentence a reader is left wondering whether the retry reuses the old keys — the one thing it must never do.

### U18 — Implement vault loss/theft double nag and first-seal export nag
- Milestone: M1
- Size: S
- Deps: U12, U13
- Spec: Vault (line 143); Risks (line 184)
- Do: After the first successful `seal` in a vault with no recorded export, nag: run `vault export` and store the backup safely. The nag (and `init`'s closing message) states both failure modes in possession language: **loss** — lose vault + backup and reveal/restore are gone forever, the sealed data itself stays safely unreadable; **theft** — retroactive, permanent decryption of public undeletable ciphertexts with no rotation possible; treat passphrase and exports as long-term high-value keys. Track "export performed" in the vault so the nag stops.
- Accept:
  - first seal without prior export → nag on stderr (also under `--json`); after `vault export`, subsequent seals don't nag
  - nag copy snapshot-tested, contains both loss and theft framings, no "notary"/unqualified-"priority" wording
- Notes: Q24 (M4) later harmonizes the doc pages with these nags — consumer, not a dep; CLI nag copy is U's.
- **[2026-08-02 EXECUTED (M1 lane m1-ux)]** Landed **with U34**, and the choice between landing that slot and degrading honestly is the entry's own open question, answered: a nag that cannot know whether an export happened fires forever, and a warning that cannot be satisfied teaches the reader that antseal's warnings are noise — which is expensive two screens later at the permanence gate, where the one warning that must not be skimmed lives. Execution notes:
  1. `SealReport::export_nag` is read from the bookkeeping record **after** the seal succeeds. A nag before anything was sealed is advice about a risk not yet taken, and MVP-SPEC.md line 143 places it after the first successful seal. It renders on the D51 human channel with everything else and rides the `--json` document, so a scripted seal that never reads stderr can still notice it.
  2. **One author for the LOSS/THEFT copy**: `vault::bookkeeping::{LOSS_WARNING, THEFT_WARNING}` are constants shared with `init`'s closing message, asserted against the real producer rather than a hand-copied string. A user who read them at `init` must recognise them at `seal`, not parse a second phrasing of the same risk.
  3. `vault export` records the export **after** the file is written and self-verified, never before: the flag that stops the nag must mean "a backup exists", and a bookkeeping write that ran first would silence the nag for an export that then failed.
  4. U31 positioning asserted in the test: no "notary", no unqualified "priority". **[2026-08-01, from U12]** "Track 'export performed' in the vault" has no substrate yet: the store has no vault-global bookkeeping slot and the v1 export payload no bookkeeping key — that machinery is **U34** (discovered; land with or before this task).

### U19 — Implement `list` (works, incomplete state, per-work cost)
- Milestone: M1
- Size: M
- Deps: U3, U9
- Spec: CLI surface (line 149, `list`); Seal journal (line 145, "`list` shows `incomplete` works")
- Do: Implement `list`: every work in the vault with work-id, title, seal date, network, completion state (`complete` / `incomplete` with resume hint / `abandoned` / `UNANCHORED`), and per-work cost from the record store. The **resume hint prints the recorded invocation** (from the D45 invocation-identity block — the exact `seal` re-run that will exact-match), and for incomplete works states the applicable clock of the **two-clocks rule**: a pre-pay work's journaled quote is stale by design (resume always re-quotes, D36), and a post-pay work's stored proofs are PUT-usable only ~24 h (`QUOTE_MAX_AGE_SECS`, D37) — **resume promptly** nag on incomplete-post-receipt works. Pending-anchor nag column arrives M2 (U25) — leave the slot. Registered `--json` fixture with the full record set.
- Accept:
  - fixture vault with complete, incomplete-pre-pay, incomplete-post-receipt, abandoned, and `--no-anchor` works renders all states distinctly (snapshot); the post-receipt row carries the time-boxed-resume nag, the pre-pay row the re-quote note
  - resume hint reproduces the recorded invocation identity (paths + flags) verbatim
  - per-work cost matches the value journaled at seal; `--json` fixture registered
- Notes: Milestone inference — journal section (M1) requires `list` to show incomplete works. — **[2026-08-02 EXECUTED (wave-3 lane ε)]** Landed as `antseal_cli::listing` (`WorkListing`, `WorkRow`, `ResumeHint`/`ResumeClock`, `ListingCounts`, `seal_invocation`) plus the `list` handler in `commands.rs`. Execution notes:
  1. **"Seal date" is the D36 consent timestamp**, not the manifest's `claimed_time` — deviation, and deliberate. `claimed_time` lives in the journaled manifest, which a `vault import` does not carry for a complete work (the S29 gap); the consent time rides in the always-exported meta record, is set in the same invocation, and is the moment the user actually agreed to make the seal permanent. A work killed before consent has no date, which is honest — nothing was sealed.
  2. **Both state tags are read, and the fine one is optional.** The entry says fine-in-journal-0 / coarse-in-U9; what it could not know is that an imported complete work has *no journal entries at all*, so requiring the fine tag would make `list` fail on exactly the machine a user reaches for it. `pipeline::journal::recorded_state` encapsulates "absent is a state, not a failure" (a *malformed* record still errors). Asserted by a test that deletes entry 0.
  3. **No vault lock.** U5's lock is single-writer; making a read-only listing wait on a running seal would turn the diagnostic command into the hanging one. Cost: a listing taken mid-seal shows that work mid-transition, which is what it is.
  4. **Order** is newest-first by date, undated last, seal id as the total tie-break — deterministic for snapshots and scripts.
  5. **`--json` amounts are decimal strings.** atto-ANT is 18 decimals; real values exceed the 2^53 most consumers survive. Same exactness-over-convenience call the house already made for EVM addresses.
  6. **A corrupt record is not softened into a row** — `list` reports the damaged vault rather than rendering a partial picture that looks complete (and `list_works` already fails wholesale on an alien entry, so partial tolerance would have been inconsistent anyway). — **[2026-08-07, D100 §1.9]** the `list_works` comparison this note rests on is between an **enumeration** failure (after which no honest listing exists) and a **per-record** failure (after which the work, the slot and the survivor count are all known). The rule the note states — a damaged work may not pretend to be healthy — is preserved by D100 R3/R4; the blast radius is not. What still fails the whole listing is exactly the enumeration and the AEAD; a per-slot schema refusal is now a datum on the row, at exit 0.
  7. `--network` appears in the resume hint only when the work's network is not the built-in default: it *is* part of D45's identity, and the config file could resolve it differently on the next run.
  8. Four sibling suites had used `list` as their "a stub command" exemplar (`cli_surface`, `exit_codes`, `config_file`, `machine_mode`'s abort table); each now points at a still-stubbed command, and `list`'s registered fixture is rendered by the real renderer so it cannot drift from what the command emits.

### U20 — Implement `restore <work-id> [-o dir]` CLI wiring
- Milestone: M1
- Size: M
- Deps: U2, U3, U9; S: restore engine (S14)
- Spec: Core user flows 4 (line 37); M1 (line 154); CLI surface (line 149)
- Do: Wire `restore`: resolve the work record, invoke S's restore engine, write recovered original files per **D48**: default output the work-scoped fresh `./antseal-restore-<work-id>/`; recorded paths re-rooted under it by the defensive lexical rule (absolute prefix stripped, `..` rejected, intra-work collisions abort pre-write); overwrite policy per-file and byte-aware — existing identical file = already-restored success, existing different file = refused-overwrite and never touched, verification-failed never written; temp+rename; **restore never prompts**; processing continues past per-file errors and the exit code reports the most severe class present (idempotent re-run exits 0). Report per-file verification results; any commitment mismatch fails loudly with a distinct exit code. This is the door of the permanent vault — the M4 clean-machine drill runs `vault import` (U12) then this command.
- Accept:
  - devnet/MockBackend E2E: seal → wipe originals → `restore` reproduces byte-identical files (raw bytes via raw-mirror where applicable); immediate re-run reports every file already-restored and exits 0
  - **three-state collision tests (D48)**: pre-existing identical file → already-restored success; pre-existing different file → per-file refused-overwrite, file untouched, severity-ordered nonzero exit; verification-failed file → never written; unknown work-id → clean error
  - `..`-bearing and absolute recorded paths re-root safely (lexical rule tested); intra-work collision aborts before any write
  - commitment-mismatch (S-injected) → distinct failure exit code; `--json` fixture registered with **per-file status** (restored / already-restored / refused-overwrite / verification-failed / write-error)
- Notes: — **[2026-08-02 EXECUTED (wave-3 lane ε)]** Landed as `antseal_cli::restore_out` (`run_restore`, `write_verified`, `reroot`, `default_output_dir`, `RestoreOutput`/`FileReport`/`FileStatus`) plus the `restore` handler. Execution notes:
  1. **Deviation, and the one thing that is NOT done: the network seam.** The command cannot reach Autonomi from the binary, because the CLI has no backend-construction path at all — S6's backend is behind the non-default `ant-backend` feature, it is async while this crate declares no runtime, and it must carry D37's capture hook (S27). All three are U13/S17's construction path for `seal`, which needs the identical object; joining them here would mean a dependency-policy event and a feature decision taken by the wrong lane. Recorded as **U36**; `crate::backend` declares the seam and documents the obligations, and the handler refuses there with a specific message **before** collecting a passphrase (asking for a secret and then refusing is rude as well as pointless). The refusal is reported in the network class, deliberately *not* not-implemented: the command is implemented, and saying otherwise sends users hunting for a milestone that already arrived. Everything else in this entry runs over any `StorageBackend` and is driven end to end against `MockBackend` — which is how D34 says the M1 E2E drives the pipeline.
  2. **Two severity rungs D48 §6 does not name** were placed and documented: `malformed-record` just under `verification-failed` (a record problem is no more transient, but it is about the vault rather than the sealed content), and `write-error` between `refused-overwrite` and `fetch-failed` (a local, non-transient obstruction the user can clear). The rank is an explicit `Ord`, never a comparison of numeric exit codes — which is what lets S14's `restore-verification-failed` live at 35 while U30's 40–49 band stays reserved.
  3. **An unreadable-but-present target refuses** rather than being replaced: we cannot prove it is the same file, so the conservative reading of "exists, differs" applies. Same for a **directory** sitting at a target path — refused, never cleared.
  4. **The temp file lives in the target's own directory**, not at the output root: still inside the output tree (D48 §4), and guaranteed same-filesystem so the rename is atomic rather than a copy.
  5. The `..`/collision refusals fire **before** the output directory is created, not merely before the writes — asserted (`!dir.exists()`), which is a stronger statement than §2 asks for and costs nothing.
  6. `-o /` reproducing recorded absolute paths in place (D48 §5's stated consequence of the prefix strip) is implemented by the same rule and needs no special case; the re-rooting test pins the absolute-path shape.

### U21 — Build the secret-hygiene test harness (no secrets in logs/errors/output)
- Milestone: M1
- Size: M
- Deps: U2, U7, U9, U10, U13; C: zeroizing types with redacted `Debug`
- Spec: Vault (line 143, zeroize); project rule 6; CLI surface domain mandate
- Do: Build an automated harness that runs representative flows (init, seal happy + every error class, resume aborts, restore, vault export) with **known sentinel secret values** injected (`W`, unit keys, salts, passphrase, wallet key) and scans all stdout, stderr, tracing output at max verbosity, `--json` documents, and persisted non-vault files for any sentinel bytes (raw, hex, base64). Per **D41**, the scan additionally covers the spawned process's **`/proc/<pid>/cmdline` and `/proc/<pid>/environ`**: the fd channel exists precisely because argv/env are inspectable process metadata, and the harness proves no sentinel passphrase or wallet key ever appears there. Assert CLI error types and record-store `Debug` impls redact. Runs in CI on every change.
- Accept:
  - harness fails when a deliberately-leaky log line is introduced (self-test); passes on the real CLI
  - **env-leak self-test**: a sentinel deliberately passed via an environment variable is caught by the environ scan (proving the D41 assertion can fail); the cmdline scan proven the same way
  - covers trace-level logging; covers panic messages (panic hook output scanned)
  - wired into CI as a required check
- Notes: Extended by U32 as new commands land (M2/M3 surfaces get added to the flow list).
- **[2026-08-02 EXECUTED (M1 lane m1-seal)]** Suite `tests/secret_hygiene.rs`. Execution notes:
  1. **A real sentinel vault, not a mocked one**: created through `create_vault`/`store_wallet_key`/`create_work` with a sentinel passphrase, a sentinel wallet key (a valid secp256k1 scalar), and a work record whose `W` is a sentinel. A guard test asserts the vault *actually holds* them, so a clean scan means "nothing leaked" rather than "nothing was there" — and the same test scans the on-disk `meta` record, which is where the sentinels are allowed to be, encrypted.
  2. **Four spellings** per sentinel: raw, lowercase hex, uppercase hex, base64 (a 15-line test-only encoder — no dependency for four lines of arithmetic, pinned against RFC 4648 §10's vectors so the base64 arm cannot be silently unable to match). Salts are covered as a *measurement*, not a claim: one concrete `derive_file_salt(W, 0)` value joins the sentinel set.
  3. **The matrix**: 9 commands × 3 verbosities (`RUST_LOG` unset / `debug` / `trace`) × {plain, `--json`} = 54 real invocations, each with the passphrase over `--passphrase-fd 0`, every stdout and stderr scanned. Stubs are included deliberately — a stub that grows a secret-bearing message is caught when it lands, not when someone remembers. Files written outside the vault (the `vault export` backup) are scanned too.
  4. **D41 measured on a live process**: a child is held on an unclosed stdin pipe so `/proc/<pid>/cmdline` and `/proc/<pid>/environ` can be read *while it runs*, then fed and finished.
  5. **The self-tests are the point.** Four of them prove the scanner can fail: a sentinel planted in the child's environment (D41's env-leak self-test), one planted in argv, one in each encoding, and a positive control that ordinary output passes. Plus the panic channel closed at its source — `WalletKeyHandle`/`SecretBuf`/`MasterSecret` `Debug` output scanned, and a real caught panic naming a handle scanned as well — and every plausible `CliError`'s `Display` and JSON object.

### U22 — Wire the anchor stage: ≥1-TSA-or-abort gate, `--force-degraded`, degraded reporting
- Milestone: M2
- Size: M
- Deps: U13, U9, U2; A: OTS calendar submit + TSA submission engine (A20); A: minimum-anchor policy primitives
- Spec: Core user flows 1 (line 34); MVP scope decision 3 (line 19); M2 (line 155); Risks (line 189)
- Do: Insert the anchor-submission stage into U13's pipeline between consent and `pay`: submit `anchor_digest` to ≥2 OTS calendars (pending `.ots` journaled) and ≥2 configured TSAs; enforce the gate — if zero TSA tokens return, **abort before any payment** (cheap, reversible, distinct exit code) unless `--force-degraded` is passed, which proceeds with a loud warning and records the degraded anchor set in the work record. Anchor failures always downgrade the seal report explicitly, never silently. Remove the M1 "anchoring arrives in M2" stub so plain `seal` now requires the gate; `--no-anchor` continues to skip the stage entirely (still mainnet-rejected per U13).
- Accept:
  - mock-A matrix: all-TSAs-fail → abort pre-payment (mock asserts zero `pay` calls) with dedicated exit code; one-TSA-succeeds → proceeds; `--force-degraded` with zero tokens → proceeds with snapshot-tested warning
  - journaled artifacts (pending `.ots`, TSA tokens, fetch dates) land in U9 slots
  - seal summary output names each anchor's outcome; degraded seals labeled as such in `list`/record

### U23 — Implement `status <work-id> [--upgrade]`
- Milestone: M2
- Size: M
- Deps: U3, U9; A: per-anchor verdict-state machine + OTS upgrade engine (A15/A18)
- Spec: Core user flows 2 (line 35); M2 (line 155); Verifier verdict taxonomy (lines 127–137)
- Do: Implement `status`: load the work's anchor artifacts, evaluate each anchor's current state via A (offline evaluation; states worded consistently with R's taxonomy), and render per-anchor status plus the Arbitrum receipt as "supporting evidence — no independently proven time". `--upgrade` drives A's OTS calendar polling to completion where possible, persisting upgraded `.ots` (with embedded header + fetch date) back into the record store. Pending anchors render with the "not yet independently provable" hint. **Specified 2026-08-07 by D97 §6 amendment 4**: "back into the record store" was underspecified and had three candidate shapes, one of which D97 rejected on four grounds. It means **one record** — the same `ots-pending` slot, the upgrade group riding keys 4/5/6, written by **exactly one `put_anchor`** through the single `apply_upgrade` site (R3, as amended by D99 R10 to take the store rather than a journal). Not a second slot: a two-slot representation is torn apart by the *ordinary resume path*, which rewrites `ots-pending` from a fresh submission and strands the sibling, after which the engine skips header confirmation and the vault holds an upgraded artifact paired with a previous submission's header — `invalid` on an honest work.
- Accept:
  - fixture works covering `pending`, `attested`, `proven`(TSA), degraded, and UNANCHORED render distinct snapshot-tested output; receipt never rendered as an anchor
  - `--upgrade` against A's mock calendar transitions pending → attested and persists the upgraded `.ots` (re-run shows the new state) — **[2026-08-06, AMENDED by D99 R5]** this row is **executable only from D99 R5**: `upgrade_pending_with` and `UpgradeTarget::loopback_for_tests` move from `#[cfg(test)] pub(crate)` to `feature = "test-util"`, because A42's allowlist requires `https` + a bare host + no port and can therefore never admit a loopback stub through the real constructor. Before R5 the transition had no venue at all; the row read as satisfiable and was not.
  - `--json` fixture registered (machine-readable per-anchor states)
- Notes: State names come from A18 at M2; when R18 freezes the authoritative wording at M3, re-align `status` output (alignment check, not an ordering dep). — **[2026-08-06 CORRECTION, recorded by D98]** the first Accept row above **mixes three vocabularies**, and they are not interchangeable: `pending` and `attested` are `AnchorState`s, `degraded` is `WorkRecord.degraded`, and UNANCHORED is rider 3c's three-way ambiguity (the `--no-anchor` shaping flag, `NagState::Unanchored`, and MVP-SPEC.md line 137's "zero headline-eligible anchors" are three different predicates that disagree on one work — see **Q120**). The row's intent stands and the text is left as written; what it must **not** be read as is a single taxonomy. D98 also records that this entry implies A18's evaluator is reached the bundle way — it is reached **per artifact**, through the public `evaluate_ots_artifact` / `evaluate_tsa_artifact` over `*ArtifactView::from_parts`, which take no `AnchorStatus`, so `status` fabricates nothing.

### U24 — Wire opportunistic OTS upgrade hook into every CLI invocation
- Milestone: M2
- Size: M
- Deps: U1, U5, U9, U23, **U47** (the shared reader the hook decodes through), **Q16** (the no-real-network gate must be armed in the test harness *before* a hook that dials calendars exists — D99 R4); D97 (the record it writes), D99 (placement, lock discipline, test seam) — *added 2026-08-07, D99 §6*; A: bounded-time opportunistic upgrade API (A15)
- Spec: Core user flows 2 (line 35); Anchoring (line 108); Risks (line 182)
- Do: Add a shared hook in the command dispatch path so **every** invocation opportunistically attempts pending OTS upgrades for all works: enumerate pending `.ots`, call A's engine with a short time budget, persist any upgrades, and never fail or delay the primary command on hook errors (silent-degrade, debug-level trace only). Locked-vault behavior is **decided — D42's positive rule (2026-08-01)**: the hook runs **iff the host command already holds an unlocked vault handle** (anchor artifacts live inside the AEAD, so there is nothing to upgrade without one); on a locked or absent vault it is a **silent no-op — never a prompt, and it never creates `~/.antseal`**. Hook output (if any) goes to stderr and never corrupts `--json` stdout.
- Accept:
  - ~~running unrelated commands (`list`, `show`) upgrades a pending fixture via mock-A and persists it; primary command output unchanged~~ — **REWRITTEN 2026-08-06 by D99 §6.** The original named the **wrong mechanism**: `list` does not take the vault lock, and D99 R3 keeps it that way — the **hook** takes it, late, only when it has a transition to write, declining silently when contended. Replaced by: an **invocation** of a vault-holding command (`list` at M2; `show`/`status` as they land) upgrades a pending fixture via a `127.0.0.1:0` calendar stub and persists it, **after** the command's own handler has returned. `list` still takes no lock (`commands.rs:297-305` stands, subject to Q117's wording fix). Primary command output and exit code unchanged, asserted by byte-comparing stdout.
  - hook failure (mock network error) leaves the primary command's exit code and output untouched
  - vault-less `verify` performs no vault access, no prompt, and creates no `~/.antseal` (asserted); a command that did not itself unlock performs no hook work (D42 rule tested both ways)
  - ~~hook lives in the shared dispatch path — a test enumerates subcommands and asserts each passes through it~~ — **REWRITTEN 2026-08-06 by D99 §6.** The original was **factually unsatisfiable**: neither point that sees all subcommands holds a vault (`run::run` takes `&Cli`, every handler unlocks for itself, and `UnlockedVault` is deliberately `!Clone`), so a hook placed at either could not satisfy D42's rule at all — the row asked for a test of an arrangement that cannot exist (D99 §1.1). Replaced by: the hook is invoked from **exactly one place** — `main_entry`, after the exit code is a value and every byte of output is written (D99 R1) — and a test enumerates `machine::ALL_COMMAND_NAMES` against the **real binary** with `RUST_LOG=antseal_cli::upgrade_hook=debug`, asserting the hook's trace appears for every subcommand: in its *armed* form for the commands that unlocked, its *not-armed* form for the rest. That proves D42's rule in **both** directions, per command, and about the shipped dispatch path rather than a library path — more than the original row asked for.

### U25 — Add pending-anchor nags to `list`
- Milestone: M2
- Size: S
- Deps: U19, U23; A: per-work anchor-state summary (A15)
- Spec: Core user flows 2 (line 35, "`list` nags"); Risks (line 182)
- Do: Extend `list` to nag on every work whose **only strong anchor is still pending** (no headline-eligible TSA token or proven OTS): a visible marker plus "run `antseal status <id> --upgrade`" hint. Reflect degraded/UNANCHORED works distinctly from pending ones.
- Accept:
  - fixture matrix: work with valid TSA token → no nag; `--force-degraded` work with only pending OTS → nag; UNANCHORED `--no-anchor` work → labeled UNANCHORED, not "pending"
  - nag appears in human output (stderr or marked column) and as a structured field in the `--json` fixture

### U26 — Wire TSA-list override from config into the anchor stage
- Milestone: M2
- Size: S
- Deps: U4, U22; A: TSA client accepting a configured list (A10). Q26 (M4) documents the alternates — consumer, not a dep.
- Spec: Anchoring (line 109); Config mandate (domain scope)
- Do: Activate the U4 config slot: a user-supplied TSA URL list overrides the built-in defaults (FreeTSA + DigiCert) for `seal`'s anchor stage; empty/absent config uses defaults. Validate entries at load (URL shape) and pass through to A. The ≥1-TSA-or-abort gate applies to the effective list.
- Accept:
  - config with two custom TSAs → A's mock receives exactly those; no config → defaults
  - malformed TSA URL in config → clean config error before any pipeline work

### U27 — Implement `show <work-id>` unit preview
- Milestone: M3
- Size: M
- Deps: U3, U9; G: unit/byte-range metadata semantics; C: decrypt (snippet sourced from the D43 cache — open decisions 5/14 resolved 2026-08-01 by D43/D50); R: preview computation (R15)
- Spec: CLI surface (line 149, `show`); Core user flows 3 (line 36); M3 (line 156)
- Do: Implement `show`: for every unit of the work — `unit_id`, file (path from the vault), kind (raw-mirror units clearly marked as not selectable via bare `--units`), byte-range, size (`true_length`), and a local snippet when available — **cache-first per D43**: decrypt from the retained journal→cache ciphertext (integrity-rechecked via S4 recompute, silent refetch fallback on mismatch) for exact sealed bytes, else current local file marked "may differ if modified since sealing", else omitted. Cache loss is never an error here — the snippet degrades, per D43. This is the documented preview surface users consult before `reveal --units`.
- Accept:
  - fixture work with text (multi-unit via `--split`), binary, `--no-fine-tree`, and raw-mirror units renders all fields; raw-mirror marked with the not-directly-revealable note
  - snippet provenance labeled; missing local data degrades gracefully (no error)
  - `--json` fixture registered (full unit table, snippet omitted or flagged by provenance)
- Notes: **[D67, 2026-08-11]** Snippet value + form are D67's (docs/decisions/D67-disclosure-preview-snippet-format.md): 64-byte text window to a code-point boundary / `hex:` + 16-byte lowercase-pair window; form from the manifest's committed domain (raw mirrors always hex — a BOM must not render invisibly beside the not-selectable mark); escaping per D67 §3 R3; marker `…` outside the quotes; absent arm renders `(snippet unavailable)`. The current-file fallback slices in the unit's commitment domain (re-canonicalize a Text file before applying the canonical range, spec line 83) or degrades to absent — never an approximate raw-offset slice. In `--json`, snippet fields carry the raw window value + form + truncated + provenance; quotes, `hex:` and `…` are terminal rendering only. Schema shape/stability stays with the D65/U50 family.

### U28 — Implement `reveal <work-id>` command core
- Milestone: M3
- Size: L
- Deps: U2, U3, U9, U27; R: reveal-flow library API + disclosure preview (R16/R15, over R13); S: `get_data` ciphertext fetch when no local copy exists (via R16)
- Spec: Core user flows 3 (line 36); Raw mirror selection rule (line 92); Reveal bundle (lines 112–114); M3 (line 156); CLI surface (line 149)
- Do: Implement `reveal` wiring: parse selection (`--all` | `--units` comma list per **D68**; exactly one required — clap-enforced in U1), resolve the work, reject a bare `--units <raw-mirror-id>` with a message explaining mirrors are includable only via whole-file reveal or `--all` (enforced belt-and-braces here and in R's builder), fetch any missing ciphertexts from Autonomi via S, invoke R's builder, and write the bundle to `-o file`, default `./antseal-reveal-<work-id>.sealproof` per **D68**. Pass `--include-receipt` through to R. Print the closing lines: bundle path + the canonical verifier page URL (URL constant owned by R).
- Accept:
  - `--all` and `--units` paths produce R-verified bundles (R's verify API round-trip in test); unknown unit ids and mirror-id rejection each produce **distinct messages, each naming its own remedy** — **amended 2026-08-12 (U28)**: as drafted this row said "distinct errors", which **D69 §5** — ruled after the row was written — makes false at the layer a reader would check first, because both refusals are group 1 (`usage`, exit **2**) and their exit **class cannot** differ. What is distinct, and what is asserted, is the sentence (the mirror refusal names whole-file reveal/`--all`; the unknown-id refusal names the id) and the typed `RevealError` variant behind it (`RawMirrorNotUnitSelectable` vs `UnknownUnitId`) — the variant-level distinction is R16's suite's and stays there
  - local-copy-absent path exercises S's fetch (mock asserts `get_data`)
  - `--include-receipt` present/absent toggles receipt embedding (asserted via R)
  - output includes the canonical verifier URL; `--json` fixture registered (bundle path, revealed unit ids, receipt-included flag)
  - default output path is `./antseal-reveal-<work-id>.sealproof` (D68 §3 R5, worked example pinned by a unit test referencing `BUNDLE_FILE_EXTENSION`); an existing target is **refused with nothing written**, the existing bytes and mtime unchanged, and the refusal fires **before** the U29 consent gate and before any ciphertext fetch (test asserts the gate was never asked)
  - `--units 3-5` is rejected by a message naming the comma form and `show` (asserted **by message content**, not exit code — a bare clap digit error also exits 2)
- Notes: **[D68, 2026-08-12]** Both halves ruled by docs/decisions/D68-units-syntax-and-default-bundle-filename.md. **Syntax**: the shipped `--units` parser (`value_delimiter = ','`, `Vec<u64>`, `cli.rs:371-379`) is ratified verbatim — comma list only, 0-based work-global ids, ranges refused for MVP (mirrors sit at positions invisible in range notation, and a range's off-by-one promotes a file to a full reveal under D70/D28, disclosing `file_salt`/`s_root`); the parser validates lexis only and every manifest-relative judgment stays in R16's resolution. `RevealError::EmptySelection` is **unreachable from the CLI** (clap kills every empty spelling first) — do not spend an Accept row on it. **Filename**: `./antseal-reveal-<work-id>.sealproof` in the invocation cwd, work-id in D29's 64-lowercase-hex form via `hex32`, closed `[0-9a-f]` alphabet, 89 bytes by construction; **never** title- or source-path-derived (free-form unbounded user text authoring a path). Collision = refuse (never overwrite/suffix/timestamp/prompt); resolve and check the path **first**, per spec line 34's normative order; write with `create_new(true)` (exclusivity, not temp+rename — a rename would clobber), removing the partial on write error. Home: a new `reveal_out` module beside `restore_out.rs`, minting `BUNDLE_FILE_EXTENSION` (the repo has 210 uses of `.sealproof` and no constant). Error **class** is D68's (D48 §6's `refused-overwrite` rung); the **number** is U2's. `-o` is taken verbatim — no extension coercion; the default is identical under `--json`. The stem `antseal-reveal-` is **maintainer-confirmed 2026-08-12**, closing D68 §9 (iv)'s recorded naming question; the range-rejection Accept row above is also carried as row **U68**, so the defect can go red on its own. **[D69, 2026-08-12]** `From<RevealError> for CliError` is this row's to write, and D69 §5 fixes the classes it must produce for all 23 variants: **`usage` (2)** for the eight request-shaped refusals (unknown work/unit id, empty selection, bare mirror id, work not complete, and the three `--include-receipt` refusals) — the shipped `StoreError::WorkNotFound → Usage` precedent; **`network-failure` (23)** for the two fetch failures; **`reveal-inputs-unusable` (36, new)** for the ten cases where a manifest, record, ciphertext or anchor artifact disagrees with the manifest and no argv change or retry fixes it; **delegate** `AnchorsUnreadable` to the existing `From<JournalError>` (preserving D100's `NewerRecord` ≠ `Corrupt`) and `Store` to the existing `From<StoreError>`; **`internal` (1)** for the documented-unreachable `Preview` seam. One exemplar per variant in the `exit_codes.rs` style; the new class's `Display` joins `tests/snapshots/cli-errors.display.txt`.

### U29 — Implement reveal disclosure consent and receipt-exposure warning
- Milestone: M3
- Size: S
- Deps: U28; R: disclosure-preview data (R15)
- Spec: Core user flows 3 (line 36); Anchoring receipt opt-in (line 110); Risks wallet linkability (line 185)
- Do: Before writing any bundle, render R's disclosure preview — exactly which units will be **irreversibly disclosed**: file, byte-range, size, snippet — and require interactive confirmation (`--yes` for scripts; machine mode without `--yes` aborts per U3 — this prompt is a registered consent-class prompt and **inherits D51's machine-mode matrix** unchanged: `--json` ∨ non-TTY ∨ fd-0-consumed stdin never prompts, declined ≡ unobtainable, one consent-not-obtained class). When `--include-receipt` is passed, add a warning that the receipt exposes the paying wallet and links the user's seals. Copy uses possession language throughout.
- Accept:
  - preview snapshot-tested (unit rows with file/range/size/snippet + "irreversibly disclosed" wording); declining writes no bundle file
  - `--include-receipt` warning snapshot-tested; absent otherwise
  - `--yes` and non-TTY-abort paths tested; the preview goes to **stderr** under `--json` — **amended 2026-08-12 (U29)**: as drafted this row reads as a byte observation on fd 2, and that is **not** what is asserted, because a process cannot read its own streams back without an unstable API or a `libc` dependency and no new dependency was permitted for an `S` row. What is asserted is the **routing decision** the emitter obeys — `DisclosureConsent::to_stderr()` is `true` under `--json` and `false` otherwise, tested in both crates' suites — plus the fact that the emitter has exactly one branch on that bit. The **end-to-end statement**, that the two streams are actually separated in a real process, is `crates/antseal-cli/tests/machine_mode.rs`'s real-binary harness, which drives the shipped binary and asserts **exactly one document on stdout** under `--json`; the reason is repeated in the test's own rustdoc so the weaker local assertion cannot later read as an oversight
- Notes: **[D67, 2026-08-11]** The preview's rows render per D67 (docs/decisions/D67-disclosure-preview-snippet-format.md); U29's snapshot tests are the M3 freeze vehicle for that rendering; the absent arm is near-unreachable under this gate (R16 already holds the plaintext to build at all).

### U30 — Implement `verify <bundle> [--online] [--live]` CLI
- Milestone: M3
- Size: M
- Deps: U1, U2, U3, U4; R: full offline verification API + verdict rendering + online must-agree checks (R21); S: `--live` re-fetch (S15); A: online Bitcoin-block confirmation path (via R's API)
- Spec: Core user flows 5 (line 38); Verifier page online mode (line 137); M3 (line 156); CLI surface (line 149)
- Do: Implement `verify`: load the bundle file, run R's full offline verification, and render R's verdict output (one headline, per-anchor states, redaction view data — wording owned by R). `--online` activates R's two-pinned-must-agree endpoint confirmation (endpoints from U4 config overrides or R's pinned defaults; disagreement renders as R specifies). `--live` (CLI-only) drives S's re-fetch + byte-compare for the storage-linkage layer. `verify` must run with **no vault present** and never prompts. Finalize the verdict-class → exit-code mapping (open decision 13) so CI/scripts can gate on results; register the `--json` verdict fixture (machine-readable verdict data from R). **[D65, 2026-08-12]** The `--json` document is an envelope of members, not a report: `result` carries `report` (the canonical report bytes, **byte-verbatim**), `overlay` and `live` (D64's siblings, `null` when the mode was not requested — always present, never omitted), and `verdict` (D69's class datum). `ok` means *a report was produced*, never *exit code 0*: a verdict mapped to a nonzero code by D69 still emits a **success** envelope carrying the report, because D27 §4 means an error envelope has no report at all. The report member must **not** be routed through `serde_json::Value` — `serde_json::Map` is a `BTreeMap` in this build, so a round trip alphabetizes the report's keys and destroys D29 rule 1's declaration order (`success_envelope`'s current `Value` signature cannot express verbatim carriage; a typed envelope for `verify`, or `serde_json`'s `raw_value` feature, are the two acceptable routes). Commitment level per D65 §3: tier A `result.report`, tier B the wrapper, tier C everything else until U32.
- Accept:
  - runs on a vault-less machine against R's golden bundles (valid, tampered, UNANCHORED, pending-only) with correct verdict rendering and mapped exit codes; tampered → nonzero
  - `--online` endpoint-disagreement fixture renders the advisory distinctly (M3 verification item); `--live` mismatch (mock) fails the storage-linkage layer distinctly from the evidence layer
  - `--online`/`--live` off by default; `--live` without network → clean error, offline verdict unaffected
  - `--json` fixture registered; CLI verdict output bit-consistent with R's snapshot-tested wording (no U-local paraphrase)
  - the bytes of `verify --json`'s stdout **contain `VerificationReport::to_canonical_json()`'s bytes as a contiguous substring** for a golden bundle — a structural `serde_json::Value` comparison does not discharge this row, because it is green against the key-reordering bug the row exists to catch
  - a presence-only key assertion over `result` (the `restore_output.rs:550` pattern) covering `report`, `overlay`, `live`, `verdict` in **all four** mode combinations — every key present in every combination, `null` where the mode was off
  - the envelope's `v` is asserted against `ENVELOPE_VERSION` and the report's version is read from `antseal_core::verify::REPORT_VERSION`, never a literal and never derived from each other (D65 §4)
- Notes: **[D65, 2026-08-12]** Schema-stability level ruled at docs/decisions/D65-json-schema-stability-commitment.md: a **three-tier partition per member**, not a blanket — tier A `result.report` (already frozen by 21 golden vectors, `EXPECTED_CANONICAL_JSON` and the wasm32 bit-match; its one hole is that an enum-valued key may gain a spelling with no version moving, so consumers always write the default arm), tier B the envelope wrapper (stable-additive from M3 under `ENVELOPE_VERSION`, with `class`/`exit_code` stable and `message` freely rewordable), tier C everything else inside `result` (reviewed by the committed fixture, promised to nobody until U32). **`REPORT_VERSION` is not a `--json` schema version and must never be used as one** — the two axes are independent, `v:1` beside `report_version:2` is legal, and code deriving one from the other is wrong. The documentation home at M3 is `machine.rs`'s module doc (the tier table plus D65 §7's null/type/key-order rules), **not** a new file under `docs/`, and `verify --help`'s `--json` text carries the one-sentence form — **amended 2026-08-12 (U30)**: the module-doc half landed in full (§11.6's tier table with tier A's enum-spelling hole priced, §5's carriage rule, §7's null/no-float/decimal-string/type-stability/provenance rules with the key-order carve-out, the `ok` restatement, and §4 under `ENVELOPE_VERSION` itself). The **second surface did not**, and it could not at this price: `--json` is declared **once**, on `GlobalArgs` with `global = true`, so a single help string serves all ten commands, and giving `verify` its own sentence means either putting it on all ten — a frozen-surface event for nine commands that have no `result.report` to promise — or mutating clap's tree after `CommandFactory`, a new mechanism minted for one sentence. What landed instead is `verify_out::JSON_STABILITY_NOTE`, carrying §3's one-sentence form with a unit test pinning its content: the text exists, is asserted, and has exactly one author; only its **delivery into `--help`** is outstanding, and it belongs to **U32**, which re-reads the whole help surface to freeze it. Amended rather than declared green, and amended *here* rather than at an Accept row, because D65 §11.2's edit set asked for three Accept rows and none of them is this surface — no Accept row reads false, this Notes clause did. U32 lifts both into release docs. **D65's per-member enforcement half lands here rather than as a row** — the presence-only Accept above is that enforcement for `verify --json`; the same gap on the five already-shipped commands is apparatus, recorded in `docs/instrument-ledger.md` (2026-08-12), and `seal`'s is U50's. `preserve_order` must not be enabled for `serde_json` anywhere in the workspace. **[D69, 2026-08-12]** Open decision 13 is closed (docs/decisions/D69-verify-exit-code-mapping.md). Exit code = the severity-ordered rung over the **whole anchor set** — `verify-anchor-refuted` (41) > `verify-headline-divergence` (42) > `verify-unanchored` (43) > 0 — with `verify-bundle-rejected` (40) on the `Err` arm of `verify_bundle`, one class for every rejection code. **UNANCHORED is 43, deliberately nonzero and deliberately distinct** so a caller who accepts undated bundles can opt back in. The rung is computed by **one** core-side pure function over the same `VerdictAggregate` that serializes into `--json`, and `result.<verdict slot>.exit_code == $?` is an asserted equality (field names/stability stay D65's). `--online` **does** move the code — promotion 43→0, agreed refutation →41, newly-flagged divergence 0→42 — and probe failure or endpoint disagreement **cannot**, structurally (D64/D56: `OnlineBlockResult` has no failure variant, so the augmented verdicts equal the offline ones). Accept row 3's *"`--live` without network → clean error"* is satisfied **by the message**: the full offline verdict still renders, the live section reports R11's `Inconclusive`, and neither `--live` nor storage-linkage may ever move the exit code (spec line 118 — *"storage is the product's bonus, not its proof"*); only a build that cannot attempt `--live` at all refuses at the backend seam as `network-failure` (23), before verification. Requires U2's third arm (`Outcome.exit_class`) so a nonzero verdict still emits `ok:true` + `result` — **maintainer-confirmed 2026-08-12**: `ok:true` is re-read as *"a result document is present"*, no key is added, removed or renamed, and `ENVELOPE_VERSION` stays **1**. The third arm is also **U69's** dependency, which is the `restore` half of the same correction. **[D128, 2026-08-12]** **U30 gains no flag: there is no `--no-storage-linkage`, and this row must not mint one.** `verify_bundle`'s default **runs** the storage-linkage layer (D128 §3 R1), R21 constructs its own `VerifyOptions` and never calls `without_storage_linkage` (§3 R5), and the only two suppressing callers in the tree are the committed-exhibit generators, whose caller list is closed at two by a literal scan — a third caller is a red arm. So the offline linkage rows render on **every** `verify`, in the storage layer's section, **whether or not `--live` was requested** (spec line 119 is one layer with two questions), and `--live`'s per-blob results join that same section rather than constituting it. A user-facing suppression would be **a new decision and a new CLI surface**, not a default (§3 R5, §4) — and there is nothing to escape by suppressing, because the layer gates no verdict, which is the same reason the exit-code sentence above already forbids it moving `$?`.

### U31 — Continuous positioning-and-copy conformance
- Milestone: continuous
- Size: S
- Deps: U1; R: verdict wording ownership boundary (R18); Q: docs positioning language (Q20)
- Spec: Positioning constraints (line 28); Verifier page (lines 127–137)
- Do: Maintain a single catalog module of all user-facing CLI strings (consent texts, warnings, nags, errors, help copy) so copy is reviewable in one place, and enforce discipline on every addition: possession language ("holder of key X possessed this content by time T"), never "notary", never unqualified "priority". Add a CI lint that greps the catalog and help output for banned tokens (`notar`, standalone "priority" flagged for review) and a checklist gate for new strings. Applies to every task above that adds copy (U11, U14, U15, U18, U22, U25, U29).
- Accept:
  - banned-token lint in CI; seeded violation fails
  - all snapshot-tested copy sourced from the catalog; new-command PRs adding uncataloged user-facing strings fail the lint
- Notes: Verdict wording stays R's — the catalog embeds R's strings verbatim, never rephrases them. Complementary to Q20's repo-wide style guide + lint — implement the lint once, apply to both scopes. **[D51, 2026-08-01]** The catalog carries the workaround copy for the deliberately-bypass-less prompts: `vault import` over an existing vault has no flag by design, and its refusal copy (like `init`'s per D39) states the supported manual path — export first if the contents matter, then move/remove the existing vault — so "unsupported in v1" never reads as a dead end.

### U32 — Freeze CLI UX for release: help/JSON/exit-code freeze and final audits
- Milestone: M4
- Size: M
- Deps: U1–U30, U21, U31; Q: docs cross-check + release/CI gates (Q28/Q31); S/Q: clean-machine + disk-loss drill execution (Q32)
- Spec: M4 (line 157); Verification M4 gate (line 175); Positioning (line 28)
- Do: Final release pass on the CLI surface: freeze and document the exit-code table, every `--json` schema, and all help text (snapshot baselines become compatibility promises); confirm release default `--network arbitrum-one` and that `--no-anchor` remains hidden and mainnet-rejected in the release build; run the final positioning + secret-hygiene audits (U21/U31 extended over M2/M3 surfaces: status, show, reveal, verify); polish the `init` → `vault import` → `restore` path that the M4 clean-machine and disk-loss drills (Q/S-run) exercise, fixing any UX defects they surface.
- Accept:
  - exit-code and JSON schema docs shipped with the release; CI schema-freeze check active
  - hygiene harness green across the full command set at trace verbosity
  - drill feedback items closed; release binary's `--help` snapshot matches the frozen canonical surface exactly
  - **[2026-08-18, wave 25 — U78's Accept row 5 requires this row to cite it, and here is why it matters to the freeze.]** The snapshot set this row ratifies now includes `crates/antseal-cli/tests/snapshots/init-report.txt` (**U78**), the golden rendering of `init`'s human report — the one CLI surface MVP-SPEC.md line 143 mandates by name, and the one that had no snapshot at all until wave 25, so `check-copy-style.py` (which reaches CLI copy *only* through committed snapshots) could not see it and this row's freeze would have inherited the hole. Ratify it deliberately: **U84** records that its keyfile section currently pins a claim `vault export` refuses, so freezing this surface for release without dispositioning U84 freezes a false sentence.
- Notes: Binary signing, sha256sums, and distribution channel are Q's (Q30/Q31).
- Notes: **[Registrar, 2026-08-17 — wave 22. D134 §2 R3's three reservations, added to the `Do` above and repeated here so a lane reading only the Accept list cannot miss them. D134's own header names U32 as its owning task (`docs/decisions/D134-…:52`).]** The reservations exist because the v1.1 planning round runs **after** this row — U32 is M4, implementation is v1.1 — so U32 cannot freeze a surface that does not exist yet; it can, and must, freeze promises that make the later addition **purely additive**. (1) **Append-only exit codes**, with `44–49` off-limits (D69 §3 R9's reserved verdict band, asserted in `tests/exit_codes.rs`); the lock/unlock classes are ordinary process outcomes and belong outside the verdict band. (2) **Tier C promised stable-additive rather than exact**, in D65 tier B's own words. (3) **A surface-may-grow sentence in the release docs**, because U1's *"Nothing else joins the surface"* is an MVP-scope statement that reads as a closed set once it is frozen. **Declaring the v1.1 commands early is explicitly refused** (`docs/decisions/D134-…:324-330`). **A fourth obligation D134 places near this row is NOT U32's and is registered separately**: the `verify`-never-prompts guard must be strengthened **before** any word-prompt vocabulary exists to slip past it (D134 §4 item 1) — that is **U77**.

### U33 — Windows `--passphrase-fd` for non-stdin descriptors (or a formally recorded release limitation)
*(discovered 2026-08-01 by U7's implementation; gated on D72 — no work before the release-target set is decided)*
- Milestone: M4 (with D72; drop with a recorded reason if Windows is not a target)
- Size: S
- Deps: U7 (the channel + frozen byte semantics); D72 (release targets — **resolved 2026-08-16**; was "open, due M4")
- Spec: D41 (docs/decisions/D41-noninteractive-passphrase-channel.md); U7's platform-limit note (crates/antseal-cli/src/passphrase.rs module docs)
- Do: U7's std-only fd mechanism reaches non-stdin descriptors through `/dev/fd/<n>`, which Windows lacks — today every non-stdin `--passphrase-fd` on Windows fails with the typed `FdReadFailed` reason, while `--passphrase-fd 0` (the degenerate stdin case, and the primary scripting shape `printf | antseal …`) works everywhere via `std::io::stdin`. IF D72 names Windows a release target: either implement non-stdin fd reading there (inherited-handle plumbing — likely requires a deliberate `unsafe`/platform-crate decision, argued like U7's rpassword row) or formally record "stdin-only on Windows" in the release docs + `--passphrase-fd` help text, with the D41 byte semantics unchanged either way. If Windows is not a D72 target, close this task with a one-line register note.
- Accept:
  - D72-resolved branch taken explicitly (implementation OR recorded limitation, never silence)
  - if implemented: the U7 byte-semantics suite passes on a Windows CI lane over a real non-stdin handle; no change to the frozen semantics or the fd-0 path
  - if recorded: help text + release docs state the limit; the typed `FdReadFailed` behavior is snapshot-covered
- Notes: **[Registrar, 2026-08-17 — wave 22 closing. Closed, and the two Accept rows below row 1 are INAPPLICABLE rather than met — said out loud because "closed" plus an unmet Accept row is how a gap gets inherited.]** D72 §2 R1 (`docs/decisions/D72-…:423-426`) rules Windows and macOS **not release targets**, so the `Do`'s third branch governs — *"If Windows is not a D72 target, close this task with a one-line register note"* — and D72 §3.1 (`:615-639`) is that note, with **no code changes**. Accept row 1 is satisfied by §3.1 explicitly. Accept rows 2 and 3 both sit under the `Do`'s *"IF D72 names Windows a release target"* conditional and do not apply. **Read out of context, row 3 is satisfied nowhere**: the limitation lives in `crates/antseal-cli/src/passphrase.rs:153-157`, `crates/antseal-cli/tests/passphrase_channel.rs:9-12`, `tasks/U.md:118` and D72 §3.1 — all of them internal — while `cli.rs:80-81` and the frozen `tests/snapshots/cli-surface.help.txt:30` mention neither Windows nor `/dev/fd`, and `README.md` contains *"passphrase"* zero times. **The user-facing half is Q22's**, landed there by D72 §3.1 `:634-637`. **Also corrected**: the register's summary *"closes on a recorded stdin-only limitation"* uses a phrase **D72 never uses** and names the wrong branch; the accurate sentence is *"closes because Windows is not a release target, with the `/dev/fd` platform limit recorded in the channel's own rustdoc."*

### U34 — Vault-global bookkeeping record slot (store + cipher registry + export carriage)
*(discovered 2026-08-01 by U12's payload design — the prerequisite machinery for U18's export-performed flag)*
- Milestone: M1 (with or before U18)
- Size: S
- Deps: U9 (store), U12 (export payload); U18 is the consumer
- Spec: Vault (line 143); D42 (bookkeeping is enumerated INSIDE the AEAD); D47 (payload contents)
- Do: The record store has no vault-global slot besides the wallet, and the v1 export payload deliberately ships no bookkeeping key (U12 note 4). Add: a new `RecordClass` id (next free: 6) + AAD identity for a vault-bookkeeping record; a store slot (`store/bookkeeping` beside `store/check`/`store/wallet`); read/write APIs; and the D47 payload key that carries it through export/import. Pre-release this extends payload format v1 while writer and reader move together (recorded in `vault/export.rs` module docs); if any release has shipped, it is an export `format_version` bump instead.
- Accept:
  - bookkeeping record round-trips create → reopen; splice into/out of the slot fails authentication (the D42 matrix extends by one row)
  - export → wipe → import carries the record; a payload WITHOUT the key still imports (older exports stay valid)
  - U18's export-performed flag implemented over it stops the nag after `vault export` (its own Accept — may land in the same change)
- **[2026-08-02 EXECUTED (M1 lane m1-ux, with U18)]** `RecordClass::Bookkeeping = 6` (the next free id, as predicted) + a singleton `RecordIdentity::Bookkeeping` shaped like `KeyCheck`; the `store/bookkeeping` slot beside `check` and `wallet`; `vault::bookkeeping` with a versioned canonical-CBOR body `{0: version, 1: exports, 2: last_export_unix_secs?}`; D47 payload **key 5**, optional on read, with `EXPORT_FORMAT_VERSION` unchanged at 1 (the pre-release writer-and-reader-move-together rule already recorded in `vault/export.rs`). Execution notes:
  1. **Absent is a state, not a failure.** Every vault predating the slot and every freshly created one has no such file, so `load` returns the default; only a *present but unreadable* record is an error, and the vault-auth class already covers that.
  2. **An import always ends with at least one recorded export**, even from a payload carrying none. Reaching that line means an export file was just consumed, and that file *is* a backup — a vault restored from one must not greet its owner with "NO BACKUP YET" while they are holding the thing. The count is the honest minimum and the timestamp stays absent, because when that backup was taken is genuinely unknown to an export predating the slot.
  3. The record holds a count and a timestamp — no key material — and is encrypted anyway, because D42 puts everything that is not the header, the lockfile or `config.toml` inside the AEAD: "how often has this person backed up, and when" is metadata about a user.
  4. The D42 splice matrix extends by one row exactly as predicted, and `decode` is total over adversarial input (the import payload's copy is attacker-chosen up to the export's own authentication, so no shortcut).

### U36 — The CLI has no storage-backend construction seam, so no command can reach Autonomi
- Milestone: M1
- Size: M
- Deps: after U13, S6, S17, S22, S27
- Discovered by: **U20** (lane ε, 2026-08-02).
- Problem: three things have to be joined before any command can reach the network, and none of them is a single command's business. S6's `AntCoreBackend::connect(&NetworkConfig, &WalletKey)` sits behind the non-default `ant-backend` feature (S22's gate); it is `async` over ant-core's tokio client while `antseal-cli` declares **no runtime** — and the dep-graph lane keeps the *normal* graph free of async and network crates, so even an optional runtime under the feature is a dependency-policy event; and the constructed backend **must** carry D37's per-sub-batch capture hook or a post-pay crash costs a second payment (S27). `seal` (U13) and `restore` (U20) need the identical object, so it is one function.
- Do: declare the seam once — `crate::backend` owns it and documents the three obligations — and let U13/S17 fill it. Until it exists, every network-touching command refuses **there**, with a specific message, rather than implying an outage.
- Accept: one construction path exists and both paying and non-paying commands go through it; S27's hook obligation is discharged structurally rather than by convention, in both the *forget* and the *bypass* directions; the optional runtime lands under the feature per `docs/dependency-policy.md` with its graph cost measured; and the refusal message tells the truth in a build that has the feature and in one that does not.
- Notes: the S27 half is the one that cannot be satisfied by documentation — the type system closes *forgetting*, and something else has to close *bypassing*, because the raw backend type is public and satisfies the trait on its own.
- **[2026-08-02 DONE]** The seam is built, and **S27's obligation is discharged by the type system rather than by remembering**. `AntCoreBackend` is a builder whose `with_capture_hook` can simply be forgotten; `SealBackend` cannot be. It lives in a private module behind a private field, so the only expression in the crate that produces one is `SealBackend::connect(config, wallet_key, receipts)` — the sink is an ordinary required argument, there is no `Default`, no builder and no second constructor, and `connect` installs D37's hook itself.
  - **Two layers, because the type system alone does not close the *bypass*.** The raw `AntCoreBackend` is public and satisfies `StorageBackend` on its own, so a hurried command could name it directly, compile, pay, crash and pay again. An **ungated** source scan asserts it is nameable in exactly one file, and the detector was proven **red** against a planted mention (`left: ["backend.rs", "rng.rs"]`) before its verdict was believed. Commands hold the wrapper and reach the network through its four delegating `StorageBackend` methods.
  - **Deviation on the hook's argument.** It takes a `ReceiptSink` trait object, not `&VaultJournal` — upstream's `CaptureHook` is `'static + Send + Sync` and the journal handle is none of the three (it borrows the unlocked vault and `RefCell`s the RNG). The journal *write* stays the pipeline's; this is only D37's timing seam, which is what the type already said. Proven to deliver **every** capture, not just the first. `ReadOnly` lets the never-paying commands through the same door with *"this path does not pay"* written down instead of omitted.
  - **tokio lands per `docs/dependency-policy.md`**: `optional = true`, activated only by `ant-backend`, exactly `devnet-launcher`'s shape — **the lockfile delta is one line** (tokio added to this package; zero new packages), the dep-graph still measured **120** default packages at this landing, and the runtime is proven to drive a real timer. **Deviation on the §1 row**: §1 is *the exact-pin class* and tokio fails its own membership test — nothing it produces is hashed, signed, stored or paid for — while *"one tokio in the tree, the same one ant-core resolves"* is exactly what a competing `=` requirement here would break. Recorded as a dated §1 note saying why it stays caret + lockfile, with the workspace comment (which claimed tokio was for `devnet-launcher` **ONLY**) corrected to enumerate all three edges.
  - Also lands the vault→payment-key bridge (U10's 32 bytes → hex → D44's single acceptance gate, the refusal carrying no byte of the input) and a **two-armed** refusal message, because the old one-armed text becomes a lie in a build that *has* the feature.
  - **Discovered en route and fixed at source**: U3's registered `--json` restore fixture hand-copied the refusal string instead of rendering it, so the committed snapshot documented text no build emitted the moment the message changed. `backend` is now `pub` so the fixture calls the real producer (U19's *"rendered by the real renderer"* rule, one snapshot line re-blessed). 4 tests.
- **Entry authored 2026-08-10 under Q135 from `TODO.md`'s U36 row alone — no other source existed.** Transcribed from the row: `Size`, `Deps`, `Discovered by`, the `Problem`'s three obligations, and the whole outcome including both deviations. **Authored here, not transcribed: the whole `Do`, the whole `Accept`, and the `Notes`** — the row is closed and states an outcome, not an acceptance criterion. `Milestone` is not in the row and is taken from the `## M1 — Storage` block. Checked rather than copied on 2026-08-10: `SealBackend::connect` resolves in `crates/antseal-cli/src/backend.rs` and `SealBackend`, `AntCoreBackend` and `with_capture_hook` all still exist. **The `120` default-package figure is left as written and dated to this landing on purpose**: `docs/dependency-policy.md` carries a dated amendment moving it to **129** at D89 the same day, which is **U37**'s work below — so `120` is correct for U36's moment and wrong for today's tree, and the two rows read together are the record of why.

### U37 — `init` and the consent gate cannot be completed or tested in the default lane
- Milestone: M1
- Size: M
- Deps: after U11, U14, S5, S8, S22
- Discovered by: **U36** (lane m1-cli, 2026-08-02), while building the backend seam.
- Problem: the wallet and balance primitives are all behind `ant-backend`. `antseal-net/src/evm.rs` is `#[cfg(feature = "ant-backend")]` in full, so a **default** build has no `WalletKey::generate` (U11's safe-default branch), no `WalletKey::import` (D44's single acceptance gate, and therefore no way to reject the wrong-length / non-hex / zero-scalar set U11 Accept row 2 enumerates), and no `address()` / `checksummed()` (U11 Accept row 4's *"address + funding instructions printed for all three networks"*). `BalanceReport` and `PreflightReport` are gated identically, which is U14 Accept row 1's *"both balances"* and row 4's insufficient-token / insufficient-gas split. Both commands' Accept rows are **snapshot** rows, and every suite that would carry them runs in the **default** lane — the one the S22/S23 containment exists to keep light.
- Do: choose, and say so out loud, because the choice changes what *"a default build of antseal"* is. Either (a) `init` and the consent gate are feature-gated too and the default build is an inspect-and-verify build that cannot make a wallet — which then belongs in U31's copy and in the docs — or (b) the **pure-scalar half** of the wallet (generate / import / validate / derive-address) moves below the gate, which costs the default graph a secp256k1 + keccak edge and is exactly the 120-package measurement dep-graph rule 1 pins.
- Accept: the chosen branch is recorded as a decision rather than executed as a refactor; if the wallet half moves, the default-graph cost is **measured** against the pinned figure and the policy amended at source; the pure primitives are exercised in the default lane, including the rejection set; and the balance surface is reachable by a test that does not need the feature.
- Notes: the pipeline never calls `preflight` / `balances` today, so U14's balance display is a new wire in **either** branch — this row does not create that work, it only stops hiding it.
- **[2026-08-02 DONE — D89 executed, branch (b).]** The wallet **light half** is now the ungated `antseal-net/src/wallet.rs` — keygen from the pinned `getrandom =0.4.3`, D44 import validation, address derivation, EIP-55 — over exact-pinned `k256 =0.13.4` (**`arithmetic` only, no `ecdsa`**) plus `sha3 =0.11.0`. `evm.rs` keeps only `to_evm_network`, the re-exports and `WalletKey::evm_wallet`.
  - **Measured after the move: 129 default / 447 `ant-backend` / 475 devnet — exactly D89's prediction, and the `Cargo.lock` delta is three dependency-list lines with zero new package entries.**
  - `BalanceReport` / `PreflightReport` moved to `backend.rs` at zero cost, and `balances()` joined the `StorageBackend` trait — **D89's finding: they were inherent methods on the concrete backend, so U14 was un-mockable *with the feature on too***. The shortfall rule is now the pure `backend::preflight(&BalanceReport, &CostQuote)` that both `AntCoreBackend::preflight` and U14 call, with S8's ANT-first order pinned by a default-lane unit test. `MockBackend` gains `with_balances` / `set_balances` and `Method::Balances`.
  - **dep-graph rule 5** added (additive, at the end of `lane_dep_graph`): one k256, the `=` pin literal equals it, `alloy-signer-local`'s lock entry stays bare — self-test in both directions, and **five planted faults proven red with distinct messages**. `wallet.rs` was deliberately **kept off** the S6 allowlist: a planted `ant_core::` line in it was proven to turn dep-graph red.
  - Policy: §1 rows for both pins, the dated rule-1 amendment (**120 → 129**, with the note that rule 1's verdict is a five-name scan and no assertion changed), and the S20 bump-checklist line. Lane δ's agreement test survives byte-for-byte and is joined by an address/EIP-55 agreement test against alloy's own `Display`.
- **Entry authored 2026-08-10 under Q135 from `TODO.md`'s U37 row alone — no other source existed.** Transcribed from the row: `Size`, `Deps`, `Discovered by`, the `Problem` including its (a)/(b) framing, and the whole outcome. **Authored here, not transcribed: the whole `Do`, the whole `Accept`, and the `Notes`** — the row is closed and states an outcome; the `Do` restates the row's own two branches rather than inventing criteria, and the `Accept` is reconstructed from what the outcome reports as delivered. `Milestone` is not in the row and is taken from the `## M1 — Storage` block. Checked rather than copied on 2026-08-10: `crates/antseal-net/src/wallet.rs` exists and is ungated; `k256`, `sha3` and `getrandom` are all workspace edges of `antseal-net`; and `docs/dependency-policy.md` carries the dated amendment *"the default figure is now 129 … 475 with `devnet-launcher/devnet` and 447 with `antseal-net/ant-backend`"*, so all three figures in the outcome are still the tree's own at `95fcee0`.

### U38 — D39's "a value supplied by flag is not asked again" is not implementable from the parsed `Cli`
- Milestone: M1
- Size: S
- Deps: after U1; before U11's wizard half
- Discovered by: **U36** (lane m1-cli, 2026-08-02).
- Problem: `InitArgs::wallet`, `kdf` and `wrap` all carry `default_value_t`, so by the time `Cli::parse_checked` returns, *"the user chose `generate`"* and *"nobody said anything"* are the **same value**. D39 Decision 1's rule, and U11 Accept row 3 (*"every wizard question answerable by its D39 flag/fd equivalent; flag-supplied values not re-asked"*), both turn on telling them apart. clap already knows: `ArgMatches::value_source` distinguishes `CommandLine` from `DefaultValue`. `parse_checked` discards the matches.
- Do: thread the value sources out of the parse alongside the typed struct. The change is additive and must **not** touch the frozen U1 help surface — no flag added, removed or renamed, so `cli-surface.help.txt` stays byte-identical — but it lands in the one entry point every command's argv passes through, so it goes in deliberately with its own red-then-green test rather than inside `init`'s handler.
- Accept: *supplied by flag* and *left at its default* are distinguishable downstream of `parse_checked`; the frozen U1 help snapshot stays green **without re-blessing**, asserted rather than assumed; the test is proven red against the pre-fix behaviour, in a shape a hard-coded `true` could not pass; and the safe default for hand-built args is *ask*.
- Notes: the entry point is the right home precisely because it is the only one — a second, source-aware entry point would let a caller take the source-less one and silently lose the distinction.
- **[2026-08-02 DONE]** `Cli::parse_checked` now spells out the two steps `Parser::try_parse_from` performs internally — `command().try_get_matches_from` + `from_arg_matches`, with the `.map_err(|e| e.format(&mut Self::command()))` that carries usage context, **dropping which is the one observable difference between the spellings** — keeping the `ArgMatches` long enough to fill a new `#[arg(skip)] provided: InitProvided` (three bools) from `value_source(..) == Some(ValueSource::CommandLine)`, before `cross_validate`.
  - **Landed in the single entry point on purpose**, and carrying the bools *inside* `InitArgs` keeps the return type and all six existing call sites unchanged.
  - **The frozen U1 help snapshot stayed green with no re-blessing** — asserted **twice**: by the existing snapshot test, and by a new test that enumerates `init`'s clap argument ids and asserts `provided` is not among them.
  - **Red-then-green proven** by planting the pre-fix behaviour (discard the matches): the new test fails on `provided.wallet` while the *value* still agrees, which is the shape a hard-coded `true` could not pass. `InitProvided::default()` is all-false = *"ask"*, the safe direction for hand-built args.
- **Entry authored 2026-08-10 under Q135 from `TODO.md`'s U38 row alone — no other source existed.** Transcribed from the row: `Size`, `Deps`, `Discovered by`, the `Problem`, and the whole outcome. **Authored here, not transcribed: the whole `Do`, the whole `Accept`, and the `Notes`** — the row is closed and states an outcome, not an acceptance criterion; the `Do` restates the row's own prescription, which is unusually explicit for a closed row, and the `Accept` is reconstructed from the outcome's four proven properties. `Milestone` is not in the row and is taken from the `## M1 — Storage` block. Checked rather than copied on 2026-08-10: `crates/antseal-cli/src/cli.rs` carries `pub provided: InitProvided`, the `InitProvided` struct, `record_value_sources(&matches)` and the `value_source(name) == Some(ValueSource::CommandLine)` test; and `crates/antseal-cli/tests/snapshots/cli-surface.help.txt` is present and still the frozen surface.

### U39 — No CI lane type-checks `--features ant-backend`, so the feature's library rots between devnet runs
- Milestone: M1
- Size: S
- Deps: after U36, U13, S22
- Discovered by: **U13** (lane m1-seal, 2026-08-02), and again by **S36** the same day.
- Problem: nothing in CI type-checks the `ant-backend` feature, so it rots silently between devnet runs. Proven twice, not predicted.
  - **First instance.** `cargo check -p antseal-cli --features ant-backend` was **red at `b11be2f`**: D89/U37 added `balances()` to the `StorageBackend` trait and to `SealBackend`'s delegating impl but not to `mod sealed`'s `use super::{…}` list, and because every item in that module sits behind a non-default feature, a default `cargo check --workspace` never compiled the mistake. The devnet suites would have caught it, but they are environment-gated (`ANTSEAL_DEVNET_ENV`) and skip by default — so the only thing between a broken live path and a green gate was someone happening to run a devnet.
  - **Second and worse instance, found by S36 on 2026-08-02.** `cargo check -p antseal-cli --features ant-backend` builds **lib + bin only**, so it never saw that `tests/common/devnet.rs` had stopped compiling: it names `antseal_net::evm::WalletKey`, valid when S17 wrote it and demoted to a private `use` by the same `b775e02` that caused the first instance. Every devnet suite — S6–S8, S17, S18, S19 — was unbuildable on the mainline from `b11be2f` onward, so the required storage gate could not have run at all. Fixed at S36. The test targets are where the expensive failures live, which is why the lane's spelling must be `--all-targets`.
- Do: add a cheap **compile-only** lane — `cargo check -p antseal-cli --features ant-backend --all-targets`, ~1 min warm, no network and no devnet — to `scripts/ci-lanes.sh` and to the local gate, and prove it red against the exact fault this found.
- Accept: the lane exists in `scripts/ci-lanes.sh` and runs from the local gate, and it is **shown red against the exact fault this row found** — the missing `mod sealed` import, or the `WalletKey` demotion in `tests/common/devnet.rs` — before it is trusted. The spelling carries `--all-targets`: without it the second instance is invisible, which is the whole reason the row was re-opened.
- Notes: both instances were found by lanes doing something else, which is the argument for the lane rather than for care. `scripts/ci-lanes.sh` carries its lane list in one `LANES` string plus a dispatch arm, so adding one is two edits and a function; the local gate reaches lanes through `scripts/ci-lanes.sh <name>`.
- **Entry authored 2026-08-10 under Q134 from `TODO.md`'s U39 row alone — no other source existed.** Transcribed from the row: `Milestone` (its M1 section), `Size`, `Deps`, `Discovered by`, the whole `Problem` including both instances and their commits, the whole `Do`, and the `Accept` — the row supplies the acceptance criterion itself (*"and prove it red against the exact fault this found"*) and names the fault twice, so **nothing in this entry is authored except the `Notes` and the arrangement**. The two mechanical facts in `Notes` (`LANES` plus a dispatch arm; the gate's `ci-lanes.sh <name>` call) were read from the scripts on 2026-08-10 and are not in the row. No `Spec` field appears because the row names no spec line, no decision and no doc section.

### U40 — D37's capture hook cannot be journal-backed as the seam currently stands
- Milestone: M1
- Size: S
- Deps: after S12, S27, U36, U13
- Discovered by: **U13** (lane m1-seal, 2026-08-02) — S27's remaining half, made concrete.
- Problem: `SealBackend::connect` requires a `ReceiptSink` **by signature**, so U36 discharged the *forgetting* half. But the backend is constructed **before** `Pipeline::seal` draws the `seal_id`, so a sink has no work-record key to write a receipt into, and `seal`'s production sink (`seal_run::SealReceipts`) is therefore in-memory. The receipt that matters is made durable by the pipeline's own `put_receipt` immediately after `pay` returns; the residual window is a payment split across **more than one transaction** (>256 blobs, `MAX_TRANSFERS_PER_TRANSACTION`) crashing between sub-batches, which loses the partial receipt and costs a second payment on resume — exactly the direction S16 asserts.
- Do: build the pipeline-side seam — register the drawn `seal_id` with the sink before the first `pay` — so a sink has a work-record key by the time it is called. It belongs to whoever owns `pipeline/`, not to the command layer.
- Accept: a crash between sub-batch transactions costs the partial payment **once**, proven live in both directions rather than argued; the sink a paying path uses cannot be the in-memory one; and while the window is open it is **visible when entered** rather than only documented.
- Notes: `SealReceipts` logs a `warn!` naming this task the moment a second sub-batch actually fires, so the window announces itself until the seam lands.
- **[2026-08-02 DONE — closed by S36 (lane m1-s36); the entry's diagnosis was right about the symptom and wrong about the obstacle.]** *"The backend is constructed before `Pipeline::seal` draws the `seal_id`"* is **true and is not a problem**: S31's `SealJournal::arm_receipts` names the work at the one moment the id exists, immediately before every `pay`, on every path that can pay. What was missing was the **attachment** — and the fix is the pipeline-side seam this entry predicted, already built.
  - `SealSession` (new, **ungated**) mints the vault `Arc` and the one `VaultReceiptSink` together; `commands::seal` hands `session.receipts()` to `SealBackend::connect`, and `run_seal` takes the session, whose `journal()` cannot omit the sink. `SealReceipts` is **deleted**, with its `warn!` and its two wrong U39 citations.
  - **The residual window was NOT >256-blob-only in practice.** The forced cap reaches it with **five** blobs, and a live devnet row shows a real `antseal seal` stranding **23 437 500 000 000 000 atto-ANT** and re-buying all five sub-batches before this landed — **82 031 250 000 000 000 spent against 58 593 750 000 000 000** the completed seal accounts for. After the fix: 5 txs = one per sub-batch, delta = the quote **once**.
  - Numbers and the two red-direction scans are on **S36**.
- **Entry authored 2026-08-10 under Q135 from `TODO.md`'s U40 row alone — no other source existed.** Transcribed from the row: `Size`, `Deps`, `Discovered by`, the `Problem`, and the whole outcome including the devnet figures (digit-grouped here for legibility; the row's unspaced forms are the authoritative ones). **Authored here, not transcribed: the whole `Do`, the whole `Accept`, and the `Notes`** — the row is closed and states an outcome, not an acceptance criterion. `Milestone` is not in the row and is taken from the `## M1 — Storage` block. Checked rather than copied on 2026-08-10: `crates/antseal-cli/src/seal_session.rs` exists and is ungated, and **`SealReceipts` is genuinely deleted** — the only surviving mentions are a headstone comment at `crates/antseal-cli/src/seal_run.rs` recording what was there and why, and two references in `crates/antseal-cli/tests/e2e_kill_resume.rs` describing the pre-fix behaviour. **S36's entry carries the fuller account of this work and does not duplicate it**: the two rows are the same obligation seen from the sink's side and the command's, which is why S36's entry says *"U40 closes with it"*.

### U41 — The user-initiated abandon's CLI surface (engine landed, token missing)
*(discovered 2026-08-02 by U17 — D45's deferred surface decision, now the only thing between a user and the flag-mismatch trap)*
- Milestone: M1
- Size: S
- Deps: U17 (engine landed); U32 freezes whatever this adds
- Spec: Seal journal (line 145); D45 §residual risk
- Do: Add one CLI token that reaches `seal_resume::abandon_pre_pay`, and give the two D45 error messages their real way-forward sentence back. Candidates, in the order U17 would argue them: (a) `seal --abandon` — the invocation that hits the trap is already a `seal`, so the fix is where the problem is, at the cost of overloading a paying verb with a destructive one; (b) `vault abandon <work-id>` — `vault` is already the housekeeping namespace and `list` already prints work-ids, at the cost of a second place to learn about; (c) `list --abandon` — rejected on sight, a read-only command must stay read-only. Register it in D51's prompt table as a **destructive-confirm** class; decide its non-interactive channel deliberately (`vault import`'s precedent is a channel-less refuse-always, which would be wrong here — abandoning is how a script gets unstuck).
- Accept:
  - the flag-mismatch error names the command, and running that command lets the previously-refused invocation proceed (one test, end to end)
  - a paid work is still refused through the surface, not only through the engine
  - `cli-surface.help.txt` re-blessed deliberately, with the diff justified
- Notes: The engine, its three safety rules and its tests already exist and are green; this is the surface event D45 asked to be taken on its own.

### U42 — Decide `ResumeSafetyReason::SourceChanged`'s disposition (keep-and-document vs remove)
*(discovered 2026-08-02 by U17)*
- Milestone: M2 (decide with R57)
- Size: XS
- Deps: R57 (the same question about `proof_unit_refs`' tautological arm)
- Spec: line 145
- Do: The variant is constructed by nothing, and that is the *stronger* outcome rather than a gap: S11 made resume never open a source file, so a changed source is a non-event and S18 case (3) proves a resume with edited sources restores the original bytes. It is currently kept, documented on the variant, and exemplified in the frozen `cli-errors.display.txt` as the class any future path that reads plaintext against a journaled nonce would need. Decide whether a frozen, exemplified, never-constructed class earns its surface — the same question R57 asks — and apply the same answer to both.
- Accept: a recorded decision; if removed, the class table, `ALL`, the exemplar and the snapshot move together in one change.

### U43 — Amend D50's dependency assertion to something true
*(discovered 2026-08-02 by U8)*
- Milestone: M1
- Size: XS
- Deps: U8
- Spec: D50
- Do: D50's Accept row "no keystore crate in the workspace graph (dependency assertion)" is already false and was before U8: `keyring` is in `Cargo.lock` as a transitive dependency of `ant-quic`, reached through the `ant-backend` feature's ant-core graph. Nothing is wrong with the vault — U8 added zero dependency edges — but the row cannot be satisfied as written, so U8's suite asserts the two true statements instead (the CLI manifest names no keystore crate; the **default-feature** graph contains none, checked against `cargo tree`). Amend the D50 record to say what it means, and decide whether `scripts/ci-lanes.sh dep-graph` should additionally pin "no keystore crate is reachable from any vault key-schedule path" (the property that actually protects `W`).
- Accept: D50's row reads as something the suite can and does check; the dep-graph lane's coverage of it is either extended or explicitly declared out of scope.

## Open decisions (U) — each: the decision, blocking task IDs, milestone it must land by
1. `init` interaction model — pure interactive wizard vs convenience flags (canonical surface enumerates no `init` flags) — blocks U1, U11 — M1 — **[2026-08-01]** RESOLVED (D39): the dichotomy was false — TTY wizard with a flag/fd equivalent for every question; v1 flag set enumerated as a deliberate U1 amendment; existing-vault refusal absolute, no `--force` (docs/decisions/D39-init-interaction-model.md)
2. KDF selection mechanism — how the scrypt N ≥ 2²⁰ alternative is chosen at vault creation, and behavior on machines that cannot allocate Argon2id's 256 MiB — blocks U6, U11 — M1 — **[2026-08-01]** RESOLVED (D40): Argon2id (m=262144 KiB, t=3, p=1) sole default; scrypt (N=2²⁰, r=8, p=1) explicit init-time choice only; low-RAM = typed hard error at create AND unlock, no fallback prompt (U6's lean overturned); pre-auth KDF-header caps added (docs/decisions/D40-vault-kdf-selection.md)
3. Non-interactive passphrase supply for scripts/E2E (env var vs fd vs keystore; `--yes` covers confirmations only) — blocks U7, U13 — M1 — **[2026-08-01]** RESOLVED (D41): global `--passphrase-fd <n>` (stdin = fd 0); env var rejected on the measured unwipeable-environ mechanism; frozen byte semantics (docs/decisions/D41-noninteractive-passphrase-channel.md)
4. Vault encryption-boundary partition — whether config and anchor artifacts (`.ots`, TSA tokens; no key material) sit inside or beside the passphrase AEAD, and therefore whether the every-invocation upgrade hook can run without an unlock (hook must never add a passphrase prompt) — blocks U5 (M1), U24 (M2) — **[2026-08-01]** RESOLVED (D42): beside = exactly config.toml + header + lockfile; everything else inside, incl. all anchor artifacts; hook runs iff the host command already holds an unlocked handle — silent no-op otherwise; AAD binds header + record identity (docs/decisions/D42-vault-encryption-boundary.md)
5. Staged-ciphertext retention after successful `finalize_batch` — keep as local cache (serves `show` snippets, reveal/restore without network) vs prune (lean vault) — blocks U9, U27, U28 — M1 — **[2026-08-01]** RESOLVED (D43): RETAIN — journal → cache reclassify at complete; integrity-rechecked on use with silent refetch fallback; excluded from export; loss never an error; no prune surface at MVP (`vault gc` = v1.1) (docs/decisions/D43-staged-ciphertext-retention.md)
6. Wallet import format(s) — raw hex private key only vs also mnemonic — blocks U11 — M1 — **[2026-08-01]** RESOLVED (D44): raw hex only in v1; accepted set ≡ what the pinned evmlib/alloy parse accepts; mnemonic rejected with a revisit trigger (docs/decisions/D44-wallet-import-formats.md)
7. Resume detection keying (match incomplete work by input paths vs interactive selection) and whether resume-before-pay re-runs the consent gate (proposed: yes) — blocks U17 — M1 — **[2026-08-01]** RESOLVED (D45): automatic, keyed on recorded invocation identity; exact match auto-resumes, overlap-not-exact/flag-mismatch = pre-consent hard error, disjoint = fresh; pre-pay resume re-fires the full U14 gate over a fresh quote (cost rule = D36's) (docs/decisions/D45-resume-detection-consent.md)
8. Directory arguments to `seal <path>…` — recurse vs files-only error — blocks U13 — M1 — **[2026-08-01]** RESOLVED (D46): files only; directory/non-regular/duplicate args hard-error at plan validation pre-consent, same under `--dry-run`; shell globbing is the recursion tool (docs/decisions/D46-seal-directory-arguments.md)
9. `vault export` format — verbatim encrypted-store archive under the existing passphrase vs re-encrypted single file with optional separate passphrase — blocks U12 — M1 — **[2026-08-01]** RESOLVED (D47): re-encrypted single file (magic + det-CBOR header + one AEAD; config in, cache out; same passphrase; post-write self-verify; validate-then-atomic-install import); archive rejected — per-record AEADs cannot detect absence (docs/decisions/D47-vault-export-format.md)
10. `restore` default output directory and overwrite policy — blocks U20 — M1 — **[2026-08-01]** RESOLVED (D48): `./antseal-restore-<work-id>/` default; defensive lexical re-rooting; per-file byte-aware policy (identical = already-restored, differing = refused-overwrite, verification-failed never written); no prompts; severity-ordered exit (docs/decisions/D48-restore-output-policy.md)
11. `--dry-run` network semantics — call `quote_batch` for a true cost (proposed) vs fully offline estimate — blocks U16 — M1 — **[2026-08-01]** RESOLVED (D49): real `quote_batch` — dry-run is the real pipeline truncated after quoting; S12↔U16 invariant contradiction resolved by scoping to the paid path; real shortfall exit codes; cost labeled indicative (docs/decisions/D49-dry-run-network-semantics.md)
12. `--units` syntax (comma list per spec example; ranges like `3-5` in or out for MVP) and `reveal` default output filename when `-o` absent — blocks U28 — M3 — **[2026-08-12]** RESOLVED (D68): comma-separated work-global unit ids only — the shipped parser (`value_delimiter = ','`, `Vec<u64>`) is ratified verbatim and **ranges are refused for MVP**, though not on the spec's example (line 149's canonical list spells the value `…` and calls line 36 an example): raw mirrors sit at positions invisible in range notation, and a range's off-by-one promotes a file to a full reveal under D70/D28, disclosing `file_salt`/`s_root` — the confirmation oracle spec line 95 exists to deny. The refusal owes a legible message (row **U68**): a range-shaped value is rejected naming the comma form and `show`, never by clap's `invalid digit found in string`. Default output `./antseal-reveal-<work-id>.sealproof` in the invocation cwd (stem maintainer-confirmed 2026-08-12), 89 bytes, closed `[0-9a-f]` alphabet, never title- or source-path-derived; collision = **refuse** (no overwrite, suffix, timestamp or prompt), checked **before** the U29 consent gate and any fetch, written with `create_new(true)` (docs/decisions/D68-units-syntax-and-default-bundle-filename.md).
13. Verify exit-code mapping per verdict class — in particular whether UNANCHORED (integrity-only) exits 0 or a distinct nonzero — blocks U2 (scheme reserves the space), U30 — M3 — **[2026-08-12]** RESOLVED (D69): the framing is overturned — there is no scalar verdict to map, and the binding constraint is **carriage**, since shipped `main_entry` has two arms and any nonzero exit today emits `ok:false` with no `result` at all. A **third arm** lands (`Outcome.exit_class: Option<ErrorClass>`; `ok` = *"a result document is present"*, maintainer-confirmed 2026-08-12; `ENVELOPE_VERSION` stays 1), then D48 §6's severity fold extended over the anchor set: `verify-bundle-rejected` **40** > `verify-anchor-refuted` **41** > `verify-headline-divergence` **42** > `verify-unanchored` **43** > 0, with 44–49 still reserved. **UNANCHORED is nonzero and distinct**, so a caller who accepts undated bundles can opt back in. `--online` may move the code in all three directions; probe failure and disagreement cannot, as a theorem; `--live` and storage-linkage never do. `reveal`'s 23 `RevealError` variants map onto shipped classes plus one new — `reveal-inputs-unusable` **36**, outside the reserved band (docs/decisions/D69-verify-exit-code-mapping.md).
14. OS-keystore platform scope for the `W` wrap (Linux Secret Service / macOS Keychain / Windows at MVP?) — blocks U8 — M1 — **[2026-08-01]** RESOLVED (D50): the framing overturned — NO keystore platforms at M1; keyfile wrap only, wrap-mode 2 format-reserved with a distinct not-supported error; platform scope = post-D72 follow-up (docs/decisions/D50-os-keystore-scope.md)
15. `--json` × interactivity contract — prompts on stderr, and whether consent-bearing commands under `--json` without `--yes` always hard-abort (proposed: yes) — blocks U3, U14 — M1 — **[2026-08-01]** RESOLVED (D51): confirmed and widened — machine mode never prompts for anything; prompt-class registry with declared channels (import overwrite deliberately has none); declined ≡ unobtainable = one class; stdin-isatty gate, `/dev/tty` rejected (docs/decisions/D51-json-interactivity-contract.md)

## Cross-domain expectations
- P: final product name decision (binary, `~/.antseal/` dir, crate names) before M1 code lands; workspace scaffold with `antseal-cli` crate and pinned deps (clap, tokio, tracing, argon2, scrypt — no keystore crate at M1 per D50)
- C: zeroizing secret types (`W`, unit keys, `k_m`, wallet key, passphrase buffers) with redacted `Debug`; XChaCha20-Poly1305 AEAD + HKDF sub-key derivation usable for vault encryption and wallet sub-key; unit/manifest encryption and hybrid signing invoked by the seal pipeline
- G: canonicalization + `--split blank-lines` / `--force-text` / per-file `--no-fine-tree` option semantics; unit tables and byte totals for consent and `show`; fine-tree cost estimator for U15
- F: manifest body build/sign/encode, `work_id`/`anchor_digest` computation, deterministic-CBOR codecs the pipeline calls
- S: batch-first `StorageBackend` (`quote_batch`/`pay`/`finalize_batch`/`get_data`); ciphertext-address computation for all blobs; ANT + ETH balance queries with distinct insufficient-token/insufficient-gas error classes; journal/resume/abandon business logic incl. the `(k_u,nonce)` single-use guard; restore engine with commitment verification; network → contracts/RPC mapping schema for config; wallet keygen/import validation/address derivation
- A: TSA submission client with per-TSA results and configurable list; OTS calendar submit + pending `.ots` production; bounded-time opportunistic upgrade API; per-anchor verdict-state machine consumed by `status`/`list` nags
- R: bundle builder with selection rules (raw-mirror inclusion rule, `--include-receipt`) and disclosure-preview computation; full offline/online/live verification APIs with verdict taxonomy wording and machine-readable verdict data; canonical verifier page URL constant printed by `reveal`
- Q: docs (vault loss/theft, funding, wallet hygiene, positioning limits, compelled disclosure); M1 devnet kill-test E2E harness and M4 clean-machine/disk-loss drills that drive U11/U12/U13/U20; CI wiring for snapshot, schema-freeze, hygiene, and copy-lint checks; threat-model doc consuming U's degraded/UNANCHORED behaviors

### U44 — Land the M2 anchor/verify config slots (OTS calendars; per-network verify RPCs)
- Milestone: M2
- Size: S
- Deps: U4, U26; A13 (calendar list consumer), A17 (RPC pair consumer); D54, D55
- Spec: Vault (line 143), Anchoring (lines 108–110), Verifier page online mode (line 137)
- Do: Two additive changes and one correction to the U4 config surface, in `crates/antseal-cli/src/config.rs` and `docs/config.md`. (1) **New key `[anchors] ots_calendars`** — the slot does not exist today (`grep -n "calendar\|ots_" crates/antseal-cli/src/config.rs docs/config.md` is empty), so D54's "user-overridable calendar list" has nowhere to live. Add it beside `tsa_urls`, in the same section, with the same `expect_url_array` validation, so no second convention is invented. An override replaces the defaults wholesale, exactly as U26 gives `tsa_urls`. (2) **Reshape the verify RPC slot**: the landed `[verify] arbitrum_endpoints` is a single flat list and **cannot express A17's Accept requirement of per-network endpoint sets** (arbitrum-one vs arbitrum-sepolia; disabled on devnet). Replace it with `[verify.<network>] rpc_endpoints`, parsed through the existing path-array matcher and `NetworkId::from_str`, with `devnet` a hard error (there is no overlay to configure). `[verify] bitcoin_endpoints` is unchanged — Bitcoin has one network. (3) A verify-RPC override MUST supply exactly two entries: one endpoint cannot satisfy "results must agree" (spec line 137) and a silent single-endpoint overlay is the failure this rule exists to prevent.
- Accept:
  - `[anchors] ots_calendars` with two entries reaches A13's mock as exactly those; absent config uses `DEFAULT_OTS_CALENDARS`; a malformed URL is the existing `malformed-config` exit 17 before any pipeline work.
  - `[verify.arbitrum-one] rpc_endpoints` and `[verify.arbitrum-sepolia] rpc_endpoints` resolve to distinct pairs; `[verify.devnet]` is a hard error naming the reason; an unknown network name reuses the existing `[networks.<id>]` error wording.
  - A one-entry or three-entry verify-RPC override is a config error at load, not a silent overlay.
  - The **removed** flat `[verify] arbitrum_endpoints` key produces the existing unknown-key **warning**, not exit 17 — so an early adopter's config degrades rather than breaking. Asserted by test, because this property is the whole reason the removal is safe.
  - `initial_config_text` (U11's init writer) and the D42/D47 export-round-trip test both cover the new shape.
- Notes: The U4 execution note (2026-08-01, item 4) records `[verify] arbitrum_endpoints` as landed "with a validated shape"; the shape is wrong for its only named consumer. Amend that note rather than leaving it to contradict this task. Discovered by D54/D55.

### U46 — Persist the TSA request nonce in the anchor capture record, and mark it public-by-construction
- Milestone: M2
- Size: S
- Deps: U9 (the record store), U22 (the anchor stage that writes it), A2 (the struct), A10 (the producer); D59, D47
- Spec: Vault (MVP-SPEC.md lines 141–145), Anchoring (line 109), Core user flows 2 (line 35)
- Discovered by: **D59 (2026-08-02, M2 planning round)**
- Do: Carry A2's `request_nonce: [u8; 8]` through U9's `anchors/<slot>` payload for every TSA capture, successful or failed, and expose it in `status` detail and the `--json` anchor object. Three riders from D59 §3 are normative and must appear as doc comments at the field and at the U9 write site: (a) the nonce is **not secret material** under project rule 6 — it is echoed into the signed `TSTInfo` and therefore ships inside every bundle carrying that token, so it must not be wrapped in a zeroizing type, redacted from `--json`, or excluded from a bug report; (b) it MUST NOT be derived from `W`, from any HKDF label, or from any vault secret — a deterministic nonce would publish a PRF output under the vault master secret inside every bundle, once per anchor, forever; (c) it needs **no `secret-guard` exclusion** (no magic literal is introduced). Add the `status` re-check path: for a stored TSA anchor, re-run A8 with `expected_nonce = Some(stored)` and surface agreement as a vault-integrity detail, never as part of the evidentiary verdict (D59 residual risk 1).
- Accept:
  - `capture_records_the_nonce_and_status_can_recheck_it`: round-trips a capture record through U9's store and re-runs the `Some(nonce)` comparison over the stored token; fails if the field is dropped, truncated, or stored in the DER encoding where the raw draw was expected (or the reverse).
  - A failed capture (nonce mismatch) still records the nonce and the endpoint in the outcome list, and the degradation report names the endpoint.
  - `vault export` → `import` round-trips the field (D47 payload; no export format-version change — the nonce rides inside the anchor record the export already carries).
  - `--json` fixture for `status` includes the nonce; the possession-language/copy audit (U31) confirms no wording implies the nonce check is part of the proof.
- Notes: D59 Evidence 5 establishes the marginal disclosure of persisting this is **zero** — `anchor_digest`, the endpoint URL and the fetch date already in the same record dominate it, and in the ordinary case the value is already inside the shipped token. No D47 amendment is required.

### U47 — A shared reader for the U9 anchor slots
- Milestone: M2
- Size: S
- Deps: U22 (which writes them)
- Discovered by: **the U22 lane** (2026-08-06).
- Do: `pipeline/anchors.rs` writes `AnchorArtifact` records into the U9 slots and **nothing reads them back except a test**. U23 (`status`) and U28 (`reveal`) both need the decode path; without a shared reader each will grow its own, and two decoders of one versioned record is how they drift.
- Accept: one reader, used by every consumer; a record written by the seal path round-trips through it; an unknown version is refused by name, not skipped.
- Notes (added 2026-08-07, D97 §6 amendment 3 + D98): **this stopped being a tidiness task.** D97 R4 makes it a **hard predecessor of U23**, because the upgrade write is a read-modify-write — key 2 must keep the original submission's fetch date while key 6 carries the upgrade's, so the writer cannot construct its record without first decoding the one already in the slot. D98 arrives at the same conclusion by a second route: the reader must also build A15's `PendingWork`, which has **no production constructor**, so every consumer that skipped a shared reader would grow that projection too — the identical drift one layer up. Landed 2026-08-07 as `StoredAnchors`, which also owes the `tsa-<n>` numeric re-sort once rather than four times (**U61**).

### U48 — The OTS artifact's fetch date cannot be pinned, so no golden vector is possible
- Milestone: M2
- Size: S
- Deps: U22, A13
- Discovered by: **the U22 lane** (2026-08-06).
- Do: the OTS slot record's `fetch_date` comes from `SystemTime::now()` inside the pipeline; `with_fetch_date` reaches TSA captures only. A record whose bytes change every run cannot be a golden vector, which A22 will want. ~~Thread the stage's pinned date through, or record A2's per-calendar `submitted_date` instead.~~ — **NARROWED 2026-08-06 by D97 R5:** thread the stage's pinned date through, and **only** that. `ctx.anchors.fetch_date` already exists and already reaches the gate, and `artifacts_of` already takes the date as a parameter; only the resume path passes a clock read instead. Recording `OtsCalendarSubmission::submitted_date` in its place is **rejected** — it lives on `OtsCaptureRecord`, which has no artifact field at all (D97 §2(d); the type's own disposition is **U55**).
- Accept: two runs over identical inputs with a pinned date produce byte-identical OTS slot records; the pinning is asserted, not merely available.
- Notes:
  - **[2026-08-06, D97 R5] Partially discharged, and what remains is sharper than what this entry recorded.** The upgrade write pins its own date from the parameter (`applied.upgrade.fetch_date()`, never a clock), so after D97 **one record carries two fetch dates**: key 6, the upgrade's, pinnable; key 2, the original submission's, still `resume.rs:404`'s `SystemTime::now()`. That is a *worse* inconsistency than one unpinnable date, not a better one, and it now sits inside a single record that a golden vector would have to cover whole. **U48 blocks A22's OTS-slot vector.**
  - **[2026-08-07, the U23 lane] There is a live honesty gap until this lands.** `status` renders **no OTS capture date at all** today: `evaluate_ots_artifact` derives the verdict's date from the **upgrade group** (key 6, pinned by D97 R5), so key 2 — the unpinnable one — never reaches the render. D95 rules a sealer-recorded `fetch_date` *"rendered unconditionally"*; on the OTS side that is true of the upgrade's date and false of the capture's, and it stays false until this task threads the pinned date into key 2. Small, but D95's sentence should not be read as describing the product until then.

### U49 — A re-run gate leaves slots from two different submissions
- Milestone: M2
- Size: S
- Deps: U22, D45 (resume)
- Discovered by: **the U22 lane** (2026-08-06).
- Do: a resume killed at `Staged` re-runs the anchor gate and writes `tsa-<n>` **by index without clearing the area**. If attempt 1 verified two TSAs and attempt 2 verified one, `tsa-1` survives from the abandoned attempt beside an overwritten `tsa-0` — the slot set is then a mixture of two submissions, and nothing says so. Clear the area before re-submitting, or key slots by endpoint rather than by index.
- Accept: the kill-then-resume matrix covers a **narrowing** second attempt (2 tokens → 1) and asserts the resulting slot set contains no artifact from the abandoned attempt; the fault is planted by restoring index-overwrite and proven red.
- Notes: **[2026-08-06, D97 R11] The OTS twin of this defect does not exist — and that is a consequence of a decision, not of the code.** Under D97's one-record ruling a resume overwrites the whole `ots-pending` record, upgrade group included, so no mixture of two submissions is representable on the OTS side, and this entry's remedy stays free to choose *either* of its two options for the `tsa-<n>` family. **Under the rejected two-slot design it would have existed**: an `ots-upgrade` record surviving from an earlier hook would have been orphaned by keying slots by endpoint, so only *"clear the area"* would have worked. Recorded as a counterfactual so that a later reversal of D97 does not silently re-impose a constraint this entry no longer states. What a resume still discards is a different loss — a completed attestation whose replacement submission carries a new, un-re-pollable commitment — and that is **U57**, not this.

### U50 — `seal --json`'s anchors object has no stated contract
- Milestone: M2
- Size: S
- Deps: U22; consumed by D65
- Discovered by: **the U22 lane** (2026-08-06).
- Do: the `anchors` object is documented by exactly one committed example. A consumer branching on `failure_class: null` versus a string has nothing to rely on. Give it a shape assertion or a documented schema, and decide whether it falls under D65's stability commitment. **[D65, 2026-08-12]** Scope answered: `seal --json`'s `anchors` object is **tier C** — reviewed by the committed fixture, not promised until U32. The null question is answered *now* and at every tier: `failure_class` is **always present**, `null` when the endpoint succeeded and a kebab-case string otherwise; absence is never a missing key (D65 §6 rider 1, §7). This row's remaining deliverable is the **presence-only key assertion** over the `anchors` object and its `endpoints[]` rows.
- Accept: every field's presence rule is asserted by a fixture, including the null/absent distinction; the D65 scope decision is recorded (tier C, D65 §9.2); the presence rule is asserted by a fixture including the `null`/string distinction on `failure_class`.

### U51 — The seal summary prints the seal-side TSA count A72 says over-counts
- Milestone: M2
- Size: XS
- Deps: U22, A72
- Discovered by: **the U22 lane** (2026-08-06).
- Do: the summary renders A20's `degradation_report()` verbatim — deliberately, rather than inventing second wording — and that report prints `distinct_tsas`, the seal-side identity count A72 records as over-counting a key-rotating TSA against the verify-side notion. When A72 resolves, re-read this copy.
- Accept: after A72, the rendered number and the verify-side number agree for a rotated-key fixture, or the wording says which one it is.

### U52 — `ots_calendars` will be silently ignored unless wired with U44
- Milestone: M2
- Size: XS
- Deps: U22, U44
- Discovered by: **the U22 lane** (2026-08-06).
- Do: `AnchorStageConfig::from_config` passes `None` for calendars. When U44 lands the `[anchors] ots_calendars` key, a user setting it will be silently ignored unless that second argument is wired in the same change. A config key that parses and does nothing is worse than one that does not exist.
- Accept: a configured calendar list reaches the stage, asserted on the stub's request counters the way U26's TSA list is; the empty/absent case still yields D54's defaults.

### U53 — The `--upgrade` write path's kill-and-resume matrix
- Milestone: M2
- Size: S
- Deps: U23, U47; D97
- Discovered by: **D97 §2 K2/K3** (2026-08-06).
- Do: D97's ruling makes the torn state — an upgraded artifact whose group did not land, or a group whose artifact did not — unrepresentable *by construction*, because one record is one `put_anchor` and `put_anchor` is temp + fsync + rename. That claim is worth exactly as much as the test that plants the fault against it. Kill between the read and the write, and after the write, over the `--upgrade` path U23 drives; assert the record comes back wholly old or wholly new, never mixed. Plant the fault by restoring the **two-write** representation the ruling rejected (write the artifact, then the group) and prove the matrix goes red naming `internally-consistent-only` — the state D97 §1.2 measures as un-nagged and structurally unrescuable, which is why a silent tear here is worse than a loud one.
- Accept: the planted two-write fault reddens; the honest path never produces `OtsAnchorState::AttestedHeaderMissing`; native only — this is CLI-side, so the wasm32 half of the usual matrix does not apply.

### U54 — `open_envelope` throws away the version it just validated
- Milestone: M2
- Size: XS
- Deps: D97 R8
- Discovered by: **D97 §1.3** (2026-08-06).
- Do: `open_envelope` returns only the body, so no decoder below it can branch on the version it just checked. That is safe today, and it is safe by luck of shape rather than by construction: every journal schema change so far has been additive-optional, so v1 bytes read correctly under the v2 parser (absent keys 4/5/6 mean `None`, which is the correct v1 semantics). The guard is a **ceiling, not a dispatch** — the first non-additive change feeds v1 bytes to a parser that cannot tell it is looking at them. Return `(version, body)`, or a small `Versioned<'_>`, and have each decoder state in one line why it accepts every version at or below the constant.
- Accept: a v1 record and a v2 record are distinguishable at the decoder; the four existing decoders compile with their behaviour unchanged.
- Notes: D97 §7 names this task's trigger — the next journal schema change. If that change is not additive-optional, U54 lands first, or a second bump is as invisible as the first nearly was.

### U55 — `OtsCaptureRecord` has no production caller
- Milestone: M2
- Size: XS
- Deps: none
- Discovered by: **D97 §2(d)** (2026-08-06).
- Do: `antseal_core::anchor::model::OtsCaptureRecord` is `{anchor_digest, calendars, upgrade}` and has **zero production callers workspace-wide** — it is constructed only by `model.rs`'s own test module. D97 ruled it is *not* the home for the upgrade group (it has no artifact field at all, so adopting it would mean a second record beside `ots-pending` and would inherit that option's kills wholesale), but the type survives the ruling with its `upgrade` field intact, which leaves a third home standing for a fact that now has a settled one. Decide: give it a caller, or delete it. A caller is available if the type is wanted — the per-calendar `submitted_date` is the one datum nothing else records; if it is not wanted, fold `OtsCalendarSubmission` into whatever does record submissions.
- Accept: either a production caller exists, or the type is gone and A2's `Do` row is amended to say why.

### U56 — Nothing asserts the `put_anchor` call-site count, which is the whole atomicity argument
- Milestone: M2
- Size: XS
- Deps: U23, U47; D97 R3, D99 R10
- Discovered by: **D97 R3** (2026-08-06); its count corrected by **the D97 foundation lane** (2026-08-07).
- Do: D97's single-write-site rule is what turns D79's *"together or not at all"* from a property of a comment into a property of the filesystem — and it is currently a comment. **D97 R3's "exactly two `put_anchor` call sites" is factually wrong, and was wrong before this wave began:** there are **four textual sites** — the seal/resume loop, `apply_upgrade`, the trait forwarder at `vault_journal.rs:299`, and the D47 import at `vault/export.rs:1146`. The landed test asserts the classified four-element set. What must stay true is the **semantic** rule: exactly two *authoring* sites, the two that decide what an anchor record contains; the forwarder and the importer carry bytes someone else authored. Keep the classified assertion honest as the file set moves.
- Accept: adding a third **authoring** call site reddens the test by name; adding a forwarder-class site forces a deliberate classification rather than passing silently. A test that merely counted would have been wrong on the day it was written, which is the fact this entry inherits.

### U57 — A resume discards a completed OTS upgrade, and the replacement is not re-pollable
- Milestone: M2
- Size: S
- Deps: U22, U24, U47; D45, D97
- Discovered by: **D97 §2 K2** (2026-08-06).
- Do: the resume gate rewrites `ots-pending` from a **fresh** submission, and `put_anchor`'s stated justification for that overwrite — *"nothing downstream has read them yet"* — is false once U24's hook exists, because the hook advances anchor slots on every invocation, for any work, independent of seal state. So a resume throws away a Bitcoin attestation the hook already obtained; and because the replacement submission carries a **new** commitment, the discarded work is not re-pollable at all — it is a fresh multi-hour wait rather than a resumed one. Decide whether the hook skips works below `SealState::Paid` (their anchors are provisional and the gate will re-run them anyway), or whether the resume gate reuses an existing verified submission instead of re-submitting. Either answer is a rule; the current behaviour is neither, and D97 deliberately left it open.
- Accept: the kill-then-resume matrix covers a work whose OTS anchor was upgraded between the kill and the resume and asserts the chosen rule; the opposite behaviour is planted and proven red.
- Notes: D97 rules only that the overwrite is *clean* under one record — whole record, group included, no mixed state reachable. It does not rule the policy. D97 §6 amendment 8 rewrites the `put_anchor` doc comment that carried the false premise; this entry owns what that rewrite leaves open.

### U58 — Three stale "an imported complete work carries no journal entries" claims, now load-bearing
- Milestone: M2
- Size: XS
- Deps: none; S29 (which falsified them)
- Discovered by: **D98 gap 3** and **D99 §1.6** (2026-08-06) — two lanes found the same defect independently, and the two rows are merged here.
- Do: `listing.rs:12-18`, `journal.rs:973-975` and `export.rs:515-516` all state that a `vault import`ed **complete** work carries no journal entries at all, on the grounds that U12 applies D43 §3's cache exclusion to the whole journal area. S29 falsified that on 2026-08-02: the export skips only `entry >= UNIT_ENTRY_BASE`, so entries 0 (state), 1 (plan) and 2 (manifest blob) survive import. The third copy sits **inside the very file whose write path implements the opposite**. TODO.md's U19 row carries the same sentence, so there are four sites. **This is no longer cosmetic.** U24's hook recovers `anchor_digest` from journal entry 1, and U25's nag depends on that recovery working — the surviving entries are exactly what makes a restored vault nag correctly. A doc that says the opposite is the one a future lane will "fix" the code to match. Correct all four, keeping `list`'s coarse-mirror fallback (still right defensively, and pre-S29 exports exist in principle) but stating the real reason for it, and settle whether the fallback is still reachable now that entry 0 survives.
- Accept: no doc claims the exclusion covers entries 0–2; the S29 amendment is cited at each site; the fallback's reachability is asserted, or its removal is recorded.
- Notes: D99 §9 files its half of this as **Q117**; the ids were reconciled at bookkeeping (three lanes allocated from overlapping blocks and none of the rows had been registered yet). A reader arriving from D99's Q117 or D98's U58 wants this entry. **One of the four sites is already flagged in place, not silently wrong**: the U25 lane left a dated marker at `listing.rs:20-27` — *"The paragraph above is stale and is U58's to correct, not U25's"* — citing S29, the surviving entries 0/1/2, and the fact that the anchor nag recovers `anchor_digest` from the plan record and therefore works on a restored vault. That marker also **raises the question this entry must settle**, in sharper form than the `Do` row states it: with entry 0 surviving, `list`'s stated *reason* for the coarse-mirror fallback is itself now unclear, so the fallback needs its reachability decided rather than just its prose corrected.

### U59 — The hook re-polls routes whose state cannot change
- Milestone: M2
- Size: S
- Deps: U24; D99 R9
- Discovered by: **D99 §1.6** (2026-08-06); scope widened by **the U24 lane** (2026-08-07).
- Do: two route classes cost a poll on every invocation and can never repay it. (1) `UpgradePoll::is_repollable()` is `false` for `NotFound` — *"the calendar does not know this commitment — it will never upgrade"* — and nothing persists that fact. (2) An **already-merged** route is the same shape from the other end: A104's `already_merged` stops the artifact growing, but it **cannot be evaluated before the poll**, because it needs the body the poll returns. So a fully upgraded vault burns its whole 4-poll budget every invocation, for ever, and gets nothing. D99 R9's rotation spreads the waste across the work list rather than removing it — it bounds *starvation*, not *cost*. Decide whether a remembered per-route state is worth a vault write: it is a write on every invocation, which means the lock on every invocation, which is exactly what D99 R3 spends its effort avoiding — so "no, and here is why" is a legitimate answer, provided it is the recorded one.
- Accept: either the memory exists and a `NotFound` or already-merged route is polled at most once per calendar per work, or the decision not to have one is recorded with its reason in `ots/engine.rs`'s module docs.
- Notes: D99 §9 files this as **U57**; reconciled to U59 at bookkeeping. D99 §8 lists the un-widened half as a standing residual risk (*"starvation is bounded, not removed"*) with this task as its trigger.

### U60 — No way to turn the opportunistic hook off
- Milestone: M2
- Size: XS
- Deps: U24, U4
- Discovered by: **D99 R8** (2026-08-06).
- Do: the hook adds up to `budget.total` plus one in-flight call to every vault-holding invocation's **exit**, in every mode. The post-output placement is what makes that invisible to a human, and it buys a program nothing: a script's `$(antseal list --json)` waits for process exit regardless. U1's canonical CLI surface is frozen, so no flag is available. Add an `[anchors] opportunistic_upgrades = false` key through U4's override-slot mechanism, validated at load the way `tsa_urls` is, defaulting to on. **A config key, not an env var** — D41's channel discipline puts operator preferences in config.
- Accept: the key parses, defaults to on, is honoured by the hook, and appears in the config documentation; the hook's debug trace names the key when it is what suppressed the pass.
- Notes: measured rather than feared, and the measurement cuts both ways. On a four-work vault the U24 lane measured median exit latency at **1378 ms with the hook against 1377 ms without** (nine runs each) — a delta inside noise, because the ~1.35 s Argon2id unlock dominates. The ~6 s figure is the **budget ceiling**, which an invocation reaches only when polls actually go out; that is the case a scripted consumer hits and cannot opt out of. **The measurement's conditions matter and are now recorded** (they were left unstated when this entry was first written): those nine runs exercised the **A42-refused path** — the stored artifact's upgrade URI was refused by the allowlist, so no request was issued and no socket opened. The 1 ms delta is therefore the cost of *deciding not to poll*, not the cost of polling, and it is the right figure for the common case of a vault whose works are already attested. It is **not** evidence about the ceiling, and no offline measurement can be: with the Q16 gate armed a poll is refused before DNS, so a test suite can never observe the case this task exists to let a user switch off. D99 §9 files this as **U58**; reconciled to U60 at bookkeeping.

### U61 — `WorkStore::list_anchors` sorts lexically, and only one of its two consumers repairs it
- Milestone: M2
- Size: XS
- Deps: U9, U47
- Discovered by: **the D97 foundation lane** (2026-08-07), while landing U47.
- Do: `WorkStore::list_anchors` sorts slot names **lexically** (`store.rs:744`), so `tsa-10` precedes `tsa-2`, and every caller inherits that order. U47's `StoredAnchors` repairs it for anchor readers — but `vault/export.rs:793-800` walks `list_anchors` → `get_anchor` directly and gets the lexical order raw. It is harmless today because export re-keys by slot **name** rather than by position; the defect is that the repair now lives in **one of two consumers**, and the one without it is the one that decides what a `.sealvault` contains. Either push numeric ordering into `list_anchors` so no consumer can inherit the wrong one, or document at the sort that the order is lexical and that a positional consumer owes itself a re-sort. Do not leave the repair implicit in one reader.
- Accept: a work with enough TSA slots to reach `tsa-10` orders correctly at every consumer whose correctness depends on position, and the consumer that does not depend on position says so at its call site; whichever route, the property is asserted rather than inferred from today's slot counts.

### U62 — `list` performs a full CMS verification per stored TSA token
- Milestone: M2
- Size: S
- Deps: U25; D98, D60
- Discovered by: **the U25 lane** (2026-08-07), by measurement.
- Do: D98's ruling is that the nag **re-derives** headline eligibility rather than trusting the seal-time `verified` bit, and the reason is stronger than the hand-edit argument: the seal-time invariant is **stale by construction, no attacker needed**. A capture was verified against the root store *of its day*; headline eligibility is a claim about the store *now*. A root-store bump silently turns a stored token into `internally-consistent-only` while the stored bit keeps saying `true` — so the nag goes quiet on exactly the work that just lost its only strong anchor. Correct and necessary; the price is one full CMS verification plus path build **per stored TSA token**, measured at **14.8–21.9 ms each** in a debug build, and `list` is the command a user runs to see *everything*. Keep the re-derivation and make it cheaper. Two shapes are available: short-circuit the per-work loop once one token is headline-eligible (the nag predicate needs existence, not a census), or cache a **re-derivable** verdict keyed on the root-store version, so a bump invalidates the cache by construction rather than by someone remembering to.
- Accept: a vault whose works each hold several tokens costs measurably less than today, measured on the same host before and after; the committed `D60-tsa-globalsign-resp.tsr` fixture — whose root sits deliberately outside store v1 — still renders `internally-consistent-only`, so the optimisation cannot have quietly reintroduced the stale-bit shortcut.
- Notes: the nag predicate is **invariant under `verify_at`** — measured over five real captures × `{0, capture + 1 day, 2060}`, because `verify_at` is one-sided (D53 5(b)) and separates only `proven` from `valid-at-stamping-cert-since-expired`, both of which are headline-eligible. That is why `list` passes a constant and reads **no clock**. Any cache must not become the place a clock creeps back in.

### U63 — `WorkRow` is a public struct with public fields and no `#[non_exhaustive]`
- Milestone: M2
- Size: XS
- Deps: U19, U25
- Discovered by: **the U25 lane** (2026-08-07), which hit exactly this.
- Do: `WorkRow` is `pub` with `pub` fields and carries no `#[non_exhaustive]`, so **every future column is a workspace-wide compile break** at every construction site. U25 added two fields (`pending_anchors`, now a real count, and `nag`) and paid it; M2's remaining `list` work will pay it again. Decide between `#[non_exhaustive]` plus a constructor or builder, and leaving the type as it is on the grounds that it is crate-internal in practice. This may warrant a decision rather than a patch — the same question applies to the other `--json`-shaped structs, and answering it once is cheaper than answering it once per struct and differently each time.
- Accept: either the type can gain a field without touching its construction sites, or the reason it should not is recorded at the type.

### U64 — Two spellings of one calendar-list policy
- Milestone: M2
- Size: XS
- Deps: U23, U44; adjacent to U52
- Discovered by: **the U23 lane** (2026-08-07).
- Do: `UpgradeConfig::from_config` hardcodes `DEFAULT_OTS_CALENDARS` for the allowlist-widening parameter, while `AnchorStageConfig::from_config` passes `None` for the same list. Two constructors reading the same config and answering the same question differently is how the `[anchors] ots_calendars` key U44 will land comes to mean one thing at seal time and another at upgrade time. Give both sites one expression of the policy, and land it **with** U44/U52 rather than separately — a third spelling arriving in the same window is the failure mode this entry is trying to prevent.
- Accept: a configured calendar list reaches both the anchor stage and the upgrade path, asserted at each; absent config yields D54's defaults at both.

### U65 — One undecodable anchor record makes the whole vault unlistable, with a message that accuses the passphrase
- Milestone: M2
- Size: S
- Deps: U19, U25, U47
- Discovered by: **the U24 lane** (2026-08-07).
- Do: `StoredAnchors::read` fails the **whole** read when one record does not decode — deliberately, because a partial anchor picture that looks complete is worse than none. But the failure travels: `JournalError::Corrupt` → `CliError::VaultAuthFailure` → exit 12, *"wrong passphrase, or the vault store or header has been modified or corrupted"*. So **one damaged slot in one work makes `list` refuse the entire vault**, and the diagnostic it refuses with names the passphrase and tamper — on a vault whose passphrase is fine and whose other works are intact. That is the same sentence D97 §2 rejected its option (a) for, arrived at from the other direction: there it was a decoder refusing an unregistered kind, here it is a reader refusing a damaged slot. U24's hook already does the right thing on the same input — per-work skip, debug level, never stdout (D99 R6). `list` does not. Decide `list`'s per-work failure policy and make it distinguishable from an authentication failure; the hook's shape is the obvious candidate, but `list`'s whole job is a complete picture, so a per-row damaged marker is the more honest form.
- Accept: a vault with one undecodable anchor record still lists its other works; the damaged work is **visibly marked**, never silently dropped; the exit code and message are not `VaultAuthFailure`'s; **exit 0, and the damage is a per-slot datum with three distinct reasons (D100 R1), not a badge**.
- Deps (D100): U19, U25, U47, **A102**, **A103**, **U63**.
- Notes: U19's execution note 6 ruled that a corrupt record is **not** softened into a row — `list` reports the damaged vault rather than rendering a partial picture that looks complete. That ruling and this entry can both hold: the question here is the **blast radius** and the **wording**, not whether a damaged work may pretend to be healthy. The tension is real and whoever decides this must name it rather than route around it. — **[2026-08-07 RULED by D100, and the tension resolved by a measurement neither side had taken]**: `list` and `status` **already contradicted each other in production** on the identical vault state — a work with anchors and no journaled manifest rendered a soft *"anchors: unclassified"* row at exit 0 (`listing.rs:616-619`) and exited 12 accusing the passphrase (`status.rs:856-863`), each documenting itself as obviously correct and neither citing the other. So the "fail whole" rule had two contradictory authorities, not one. The refusal becomes a **datum**: `StoredAnchors::read` collects damaged slots, the artifact accessors move onto `IntactAnchors` behind two named doors (`require_intact()` = the old whole-fail, `intact_and_damaged()` = the reporting door), and **no `ErrorClass` is minted** — because `status` already renders the identical damage one layer down at exit 0 (`.ots` bytes that do not parse render `invalid`). The split sits exactly at the AEAD boundary, so U6 is preserved. — **[2026-08-07 EXECUTED (wave-9)]** together with A102/A103 in one commit; `status`'s no-manifest refusal converted in the same change (D100 R6), and `tests/upgrade_hook.rs`'s `Shape::renderable` workaround deleted with its four spawned rows re-pointed.

### U66 — The work store holds two definitions of an existing work, and enumerates by the one its writers do not use
- Milestone: M2
- Size: M
- Deps: S10, U65, U5, U19
- Discovered by: **the wave-10 gate run** (2026-08-09), as an intermittent `test`-lane red on `seal_journal::incomplete_works_are_enumerable_with_their_state`. Pre-existing — wave 10 touched no `seal_journal.rs` and all 52 changed lines in the three CLI/anchor production files it did touch are comments, verified by diff.
- Problem: `WorkStore` answers *"does this work exist?"* two different ways. `create_work` (`store.rs:367-370`), `store_meta` (`:389-391`) and `require_work` (`:748-754`) all ask *is the `meta` record present*, and `require_work` gates **every** write that can put content inside a work directory — `put_journal_entry`, `put_receipt`, `put_anchor`. `list_works` alone asked *does a 32-hex directory name exist* (`:501-502`). `create_work` is `create_dir_all` then `write_meta` (`:372-376`), so for the duration of one file write the two answers differ, and every whole-store scan reads the directory and then fails `load_meta` with `WorkNotFound`. **Two lock-free production commands break**: `antseal list` (`listing.rs`) and `antseal status <work-id>`, which resolves ids through the same `list_works` → `load_meta` scan in `resolve_work_id` (`restore.rs:520-531`). Both exit **2** with *"no work with this id exists in the vault (see `antseal list`)"* — `list` instructing the user to run `list`, and `status` denying the work id the user typed — with `list --json` emitting an error envelope carrying no `works` array at all. It is not only a concurrency defect: the same directory is what a `seal` killed between the two syscalls leaves behind, and nothing sweeps it, so one killed seal made a vault **permanently unlistable** until the user found and deleted an empty directory the error never mentions. A fourth shape nobody had recorded: `parse_hex32` tests only the *name*, so a hex-named **regular file** was admitted as a work and died later on `ENOTDIR` at `ErrorClass::Io`, leaking an absolute in-vault path.
- Do: make `list_works` adopt the store's own existence predicate — enumerate an entry iff it is a directory whose name parses as a seal id **and** (its `meta` record is present **or** it holds at least one non-dot entry) — leaving its name, signature, ascending order, zero-decryption property and loudness on an alien name unchanged. A work-shaped entry that is **not** a directory becomes `AlienEntry`; that is a safety obligation of the skip, not a bonus, because a hex-named file has no `meta` and no content and would otherwise be swallowed silently. Separately, `SealJournal::incomplete_works` stops asking `state()` — whose refusal must stay **hard**, its other callers being `set_state` and the seal pipeline, which run under the U5 lock, and a state machine that cannot read the current state must not advance — and reads the fine tag through `recorded_state`, falling back to U9's coarse mirror exactly as `list` has since `a_work_without_its_fine_state_record_still_lists`. Do **not** make `create_work` atomic and do **not** put tolerance in the callers: D106 §3 kills both.
- Accept: an empty (or dot-only) work directory planted in `works_dir` is absent from `list_works()`, and `antseal list` and `antseal status <work-id>` both succeed with it present, asserted by constructing the directory rather than by hoping for a race; a work directory holding records with **no** `meta` record is still enumerated and still refused, asserted in the same file, so the skip cannot widen into a silent drop; a work-shaped entry that is not a directory is `AlienEntry` with no in-vault path in the message; a work begun but not yet state-tagged enumerates as `(id, None)` rather than failing the whole scan; `list_works`' zero-decryption property and ascending order are unchanged, and no `ErrorClass`, exit code, `--json` key or snapshot is minted.
- Notes: U66, **S34** and **S38** are three recordings of one defect, and its root cause is a single false sentence — `store.rs:65-66`'s *"Readers are safe against a concurrent writer because every write is an atomic rename"*, which is a **per-file** guarantee offered as a **per-work** one. Every individual write is old-or-new; a work is many files and no primitive in the tree makes a set of them appear together. That sentence is why the defect sat seven days across three recordings with nobody positioned to catch it. — **[2026-08-09 RULED by D106]**: the defect is neither atomicity nor tolerance. Atomic publish dies on four kills, the sharpest being circularity — a portable directory `rename` needs a pre-check on *directory existence*, the very predicate that causes the bug — with Windows a required `cross-os` lane and `import_vault` already publishing atomically one level up (`export.rs:1173-1185`). Per-caller tolerance dies on placement: it fixes one of **six** production scans and, on D100's `SlotMoved` model, would render a row whose every `WorkRow` column comes from the record that does not exist. The silent-drop objection is answered **by construction rather than by policy**: `require_work` gates every content write on `meta`, so a work directory with no meta and no non-dot content has never held a byte. Concurrency is ruled a **supported configuration**, derived from five shipped behaviours since MVP-SPEC.md never mentions the lock; a locked `list` is refused because `VaultLock::acquire` is a **try-lock** (Q117), so it would exit 15 for the whole duration of every seal rather than wait. The maintainer ruled on 2026-08-09 that the supported-configuration statement stays **in-code only** — MVP-SPEC.md is the frozen source of truth for scope and the adaptive protocol's rule 5 makes any spec revision force a re-derivation of that section's tasks, which is not worth spending on a sentence that changes no behaviour. — **[2026-08-09 EXECUTED (wave-11)]** two production files, +171/−20 with tests. Measured on the real binary before and after: empty directory and `.meta.tmp` residue move from exit 2 with no `works` array to exit 0 with `"works":[]`; the hex-named regular file moves from exit 4 leaking `…/works/00…ff/meta: Not a directory (os error 20)` to `AlienEntry` with no path in the message; the meta-less-with-records case is unchanged at exit 2, deliberately (D106 R3), and D100 §9(i) is its named follow-up. Six instruments, five seen red before green; the sixth — `a_work_directory_holding_records_without_its_meta_is_still_enumerated` — is the anti-vacuity pin D106 names as one that *cannot* redden, and it is the only thing standing between the skip and a silent drop. The four shared-vault scanning tests **stay where they are** and `incomplete_works_are_enumerable_with_their_state` gains a docstring saying so, because after the fix they are the tree's only witness that a whole-store scan is total under real concurrent mutation and isolating them would trade a load-dependent red for a permanently green vacuum. **Residual (D106 R7)**: a `create_work` killed between `create_dir_all` and `write_meta` leaves an inert empty directory for ever; nothing sweeps it and nothing now reports it. Revisit on any report of `works_dir` accumulating entries, or when a `vault fsck`-class command arrives.

### U67 — D33's block-number backfill has no production caller, so a receipt whose enrichment failed can never be embedded
- Milestone: M3
- Size: S
- Deps: D33 (the enrichment slot), S7/S16 (the payment record), R16 (`--include-receipt`, the surface where it bites), U28 (the flag's CLI wiring)
- Spec: Reveal bundle — opt-in Arbitrum receipt (line 114); Verifier web page — supporting evidence class (line 137)
- Discovered by: **the R16 lane** (2026-08-11), surfaced as the third of its three ledger candidates and **promoted to a row on measurement** rather than on the report: this is the Q112 class (a product defect found by an instrument), not an instrument finding, so rule 8 mints it immediately.
- Problem: `TxRecord::block_number` is `Option<u64>` and its own rustdoc calls it the D33 **enrichment slot** — `None` when the receipt read failed, *"backfilled idempotently by later invocations via `eth_getTransactionReceipt` over the payment RPC"*. The backfill is implemented: `antseal_net::ant_backend::backfill_block_numbers`. **Nothing in the product calls it.** Measured 2026-08-11: the only callers anywhere outside its own module are two lines in `crates/antseal-net/tests/devnet_backend.rs`, so *"later invocations"* names an event that never happens — no command re-enters the payment path after `Complete`, and the opportunistic hook (A15/U24) walks anchors, not receipts. The consequence is now user-visible at M3: registry §7.10 key 1 is **required** and defined as the minimum block number across the receipt's transactions, so R16 refuses `--include-receipt` with the typed `ReceiptBlockNumberUnknown` when every record's slot is `None` — correctly, since fabricating `0` would put an unverified number in a display field. A work whose payment landed during an RPC read failure therefore carries a receipt it can **never** embed, permanently, with no path to repair. This is the M1-named dominant defect class exactly: a recorded guarantee the code does not implement — and here it is worse than unimplemented, because the implementation exists and is merely unreachable.
- Do: give the backfill a production host. The natural one is the existing every-invocation opportunistic hook's shape (U24's precedent) or `status`, which already reads the payment record and is where a user looks after a warning; whichever is chosen, it must obey U24's rules — never delay or fail the host command, never prompt — and must be gated exactly as the payment RPC is (no network in the Q16-armed lanes). Enrichment stays non-gating (D33: *"enrichment never gates finalize"*), so a failed backfill is silent and retried, never an error. Record at the call site why the slot can be `None` and what re-fills it, so the next reader does not have to measure the caller set to find out.
- Accept:
  - A completed work whose `TxRecord`s all carry `block_number: None` reaches a filled slot after the hosting invocation runs against a mock/devnet RPC, and `--include-receipt` then succeeds on the same vault — the two halves asserted together, since either alone leaves the defect standing.
  - The backfill is proven **idempotent** (running it twice changes nothing) and **non-gating** (an RPC failure leaves the host command's exit code and output unchanged), each with its own negative control.
  - A test names the production caller, so the caller cannot be deleted back into test-only reachability without a red — the property this row exists for.
  - No `ErrorClass`, exit code, `--json` key or report field is minted; `REPORT_VERSION` and the frozen registry are untouched.
- Notes: R16's `ReceiptBlockNumberUnknown` refusal is **correct and stays** — it is the honest arm and remains reachable for a work whose backfill has not yet run. This row does not soften it; it removes the state's permanence. The narrower alternative (make registry §7.10 key 1 optional) is **refused here rather than left open**: it is frozen wire surface, and D105's rule makes a field's optionality a format event, so the repair belongs on the enrichment path and not on the format.
- Notes: **[Registrar, 2026-08-16 — wave 21 closing. CLOSED: Accept rows 1, 3 and 4 met; Accept row 2 met with a stated limit.]** **Host: `status <work-id>`, in `commands::status`, before the gather** — the handler is at `crates/antseal-cli/src/commands.rs:512`, the reasoning block at `:537-576`, and the call at `:577` (`crate::pipeline::receipt_backfill::enrich_recorded_receipt(&vault, &seal_id)`), argued additionally on that function's rustdoc.
  **This row's own `Do` is measured FALSE where it calls the A15/U24 hook "the natural" host.** The only door to `backfill_block_numbers` is `AntCoreBackend::connect` — a full Autonomi `Client::connect` over the bootstrap set plus the `eth_chainId` guard — while U24's pass is budgeted at **3 s across every work in the vault** for cheap calendar GETs, and D90 §3.3 makes `antseal-anchor` blocking precisely so that hook runs in a build with **no tokio compiled in**. Hosting a payment-RPC session there would put an Autonomi connect at the end of every `list`. `status` is instead **the only command in the surface that renders a block number at all**, so the repair sits beside the display of the thing repaired. `status --upgrade` was refused as host because its frozen help would have to be re-blessed — a U1 surface event, not this row's.
  **Shape.** The policy is **ungated** — `crates/antseal-cli/src/pipeline/receipt_backfill.rs`, in the default build and every CI job: empty-slot test, idempotence, compare-and-set under the U5 lock, silence, and `enrich_recorded_receipt` returns a `BackfillPass` with **no `Result`**, so structurally nothing in it can reach an exit code. Only the RPC constructor is gated (`backend::payment_rpc`, inside `#[cfg(feature = "ant-backend")] mod ant`). **One precision the row must carry**: "no `Result` anywhere" is true of the entry point and the pass, and **not** of the `BlockNumberSource::backfill` trait method (`receipt_backfill.rs:112`, `-> Result<usize, String>`), whose error is swallowed inside the module. That is the design, but a reader grepping for `Result` will find it.
  **Design change beyond the row, recorded here because it is a promise to a user**: `PAYMENT_RPC_BUDGET` = **10 s per leg** (connect, then reads; worst case 20 s) via `tokio::time::timeout`, at `crates/antseal-cli/src/backend.rs:634`. Because the pass sits *before* `status` renders, it can delay the **answer**, not merely the exit — a stricter position than U24's post-output hook — so *"never delay the host command"* needed a bound rather than a hope.
  **Twelve planted faults, each RED by its message**, two of them catching classes rather than bugs: plant **D** proved the compare-and-set re-reads under the lock (`the concurrent writer's transaction was erased — the pass wrote its pre-RPC snapshot back`), and plant **I** proved a `println!` from the pass is caught by the D51 stdout hazard (`run A is json: Error("expected value", line: 1, column: 1)`). **Machine: this local host**, `cargo test -p antseal-cli --test receipt_backfill`, in the wave-21 gate's `test` lane. The single production caller is pinned by `the_block_number_backfill_has_exactly_one_production_caller`.
  **Accept 2's stated limit, and what is witnessed nowhere new.** Through the real binary the pass reaches only the **no-source** arm; the **refused-RPC** arm is proven at library level over a scripted source. A **successful** `eth_getTransactionReceipt` still needs a live devnet and remains covered only by two pre-existing lines in `crates/antseal-net/tests/devnet_backend.rs`.
  **A correction to the wave's own brief.** It said *"`backfill_block_numbers` is already `pub` so you should not need to widen anything"*. The conclusion held — no `antseal-net` write — but the premise did not: `pub` on an **inherent method of a type the CLI may not name** is unreachable, because `the_raw_backend_type_is_named_in_exactly_one_file` forbids naming `AntCoreBackend` elsewhere. A delegating `SealBackend` method was required and was added inside scope.

### U68 — `--units 3-5` has no legible rejection
- Milestone: M3
- Size: XS
- Deps: R16 (the resolution layer whose typed errors this must not duplicate), U28 (the command being wired); D68 (the ruling), U1 (owns the clap surface and the frozen help snapshot)
- Spec: CLI surface — `reveal <work-id> (--all | --units …)` (line 149); Core user flows 3 — the `--units 3,5` example (line 36); raw-mirror selection rule (line 92)
- Discovered by: **the D68 planning lane** (2026-08-12), measured against the prebuilt binary while ratifying the shipped parser. This is the **Q112 class** — a product defect found *by* an instrument, so rule 8 mints it immediately rather than sending it to the ledger: it changes what a user sees, not what a checker says.
- Problem: `antseal reveal w1 --units 3-5` answers `error: invalid value '3-5' for '--units <UNIT>': invalid digit found in string` and exits 2; `--units 3..5` answers the same. The message names the offending value and nothing else — not the supported form, not `show`, not the fact that ranges are unsupported by decision rather than by oversight. And a range is the *expected* mistake, not an exotic one: the spec's own flow example is a comma list (line 36), `show` prints unit ids as a column of consecutive integers, and D68 §1 (b) measures the id space as dense and gapless, so `3-5` reads as obviously well-formed to the person typing it. D68 §2.1 refuses ranges for MVP on the mirror-interleaving and file-completion risk; that refusal is what creates this obligation — a refusal a user cannot read is indistinguishable from a parser bug.
- Do: implement D68 §3 R3. Add a `value_parser` on the `units` arg that parses `u64` and, **on failure only**, discriminates the range-shaped case (a `-` or `..` between digits) and returns a message naming the offending value, the fact that ranges are not supported, the comma form, and `show` as the source of ids. **No other clap attribute moves** — `value_delimiter = ','`, `Vec<u64>` and `value_name = "UNIT"` are ratified verbatim by D68 §3 R1, so `--units 3,5` keeps its shipped behaviour byte-for-byte and every other rejection in D68 §3 R2's envelope (`''`, `3,,5`, `0x3`, `-1`, oversize) keeps its current disposition. The exact spelling of the copy is U31/R18's domain, not this row's; the **content** is fixed above.
- Accept:
  - `--units 3-5` and `--units 3..5` are each rejected by a message that **contains the comma form and names `show`**, asserted **by message content, never by exit code alone** — a bare clap digit error also exits 2, so an exit-code assertion is green against the defect this row exists to remove.
  - `--units 3,5` still parses to `vec![3, 5]` and `--units 3 --units 5` still appends, both asserted against the unchanged parser — the ratification half, which a `value_parser` addition is exactly the kind of change to break silently.
  - The `cli-surface.help.txt` snapshot is re-frozen in the same change if any help byte moves, and the usage/error fixtures that carry the old text move with it (D68 §3 R3 records this as a frozen-snapshot event).
- Notes: **[D68, 2026-08-12]** Ruled at docs/decisions/D68-units-syntax-and-default-bundle-filename.md §3 R3, with the same Accept row drafted at §8.1 for U28 — landing it here rather than inside U28 keeps the defect visible as a defect; either home satisfies the ruling, and the row is the one that can go red on its own. The parser validates **lexis only**: an unknown id and a raw-mirror id stay R16's resolution-layer refusals with their own messages, and nothing here may start judging ids against a manifest the parser cannot see. Do **not** spend an Accept row on `RevealError::EmptySelection` — D68 §1 (g) measured it unreachable from the CLI, since clap kills every empty spelling first. Zero wire surface: no bundle key, no manifest field, no HKDF label, no domain tag, nothing for Q14/F4.

### U69 — The registered `restore --json` fixture documents an envelope no build can emit
- Milestone: M3
- Size: S
- Deps: U30 (implements D69 §3 R1's third arm — the mechanism this row consumes), U20 (`restore`'s CLI wiring and its per-file report); U3 (the envelope and the fixture registry), U36 (the backend seam that keeps the defect latent), D48 (the output policy whose §6 promise this restores), D69 (the ruling)
- Spec: CLI surface — `--json` (lines 147–149); Core user flows — restore (line 40); Seal journal / restore output policy (line 145)
- Discovered by: **the D69 planning lane** (2026-08-12), while measuring what carriage a nonzero `verify` verdict could have. This is the **Q112 class** — a product defect found *by* an instrument, minted immediately under rule 8: it changes what a user's `restore --json` returns.
- Problem: `crates/antseal-cli/tests/machine_mode.rs:409-411` composes the registered `restore` fixture as `success_envelope("restore", "arbitrum-one", fixture_restore().json())` over a `RestoreOutput` whose rows include `FileStatus::RefusedOverwrite` **and** `FileStatus::VerificationFailed`, so the committed line at `crates/antseal-cli/tests/snapshots/json-envelopes.txt:34` reads `"ok":true` beside `"verification-failed":1`. But D48 §3 requires that run to exit **35**, `RestoreOutput::into_error()` returns `Some(CliError::RestoreVerificationFailed{..})` for exactly that shape, and `crates/antseal-cli/src/lib.rs:140-151` emits `success_envelope` **only** on the `Ok` arm at `ExitCode::SUCCESS` — there is no third arm. **No build can produce that envelope.** It is latent only because `commands::restore` is still the U36 seam stub and `into_error()` has **no production caller** (its only callers outside its own module are eight lines in `tests/restore_output.rs`), so U20's wiring will reach the fork with nothing ruling it and the cheapest resolution at that moment is to drop the per-file array — which is precisely the half D48 §6 promised to keep (*"per-file detail always available in output and in the `--json` result"*). The fixture is not merely wrong; it is the house having believed a promise the machine forbids.
- Do: land the `restore` half of D69 §6.1. Replace `RestoreOutput::into_error() -> Option<CliError>` with `RestoreOutput::exit_class() -> Option<ErrorClass>` folded by the same `FileStatus` severity rank D48 §6 froze, and route it through the third arm U30 adds (`Outcome.exit_class`), so the human per-file report and the per-file JSON array both render **in full** while the run exits on the most severe class present. D48's rank and its classes (30, 31, 35) are untouched — **only the destination of the fold changes**. Then correct the committed fixture so it documents the envelope the build now emits: a **success envelope at a nonzero exit**, which is the shape nothing in the tree exercises today.
- Accept:
  - A `restore` run containing a `verification-failed` row emits a **`success_envelope` with the complete per-file array** and exits **35**, asserted together — either half alone leaves the defect standing, and the exit code must be read from the process, not from the document.
  - `json-envelopes.txt`'s `restore` entry is regenerated from the **real producer** and matches the run above; the fixture is no longer hand-composed around a pairing the machine forbids.
  - An all-`restored` run still exits 0 with `ok:true`, and a run whose worst row is `refused-overwrite` exits its own D48 class — the rank is asserted over a mixed set, not over one example.
  - `ENVELOPE_VERSION` stays **1** and the error object is untouched: no key is added, removed or renamed by this row (D69 §3 R1; the `ok` re-reading is documentation, confirmed by the maintainer 2026-08-12).
- Notes: **[D69, 2026-08-12]** Ruled at docs/decisions/D69-verify-exit-code-mapping.md §6.1, which **corrects D48's carriage without touching its severity order** — the order is adopted verbatim and extended to anchors for `verify`. The blocking dependency is real and one-directional: the third arm is U30's to build (`commands::Outcome` gains `exit_class: Option<ErrorClass>`, `main_entry`'s `Ok` arm computes the code before the mode branch so D51 invariant 2 holds), and this row is the second consumer of it. `ok` means **"a result document is present"**, never "the exit code is 0" — the two were coincident until M3 and are separated deliberately, so a reviewer must not "fix" this row by making `ok` follow the exit code again. Whoever lands it should know `restore`'s per-file human report already exists and already renders; the change is that it stops being *replaced* by a one-line `error:` when a file failed.
- Notes: **[Registrar, 2026-08-16 — wave 21 closing. CLOSED, with a named half that this row CANNOT close and must not be read as having closed.]** **The fold landed**: `RestoreOutput::into_error()` is **gone** from the tree (zero occurrences of `fn into_error`) and is replaced by `pub fn exit_class(&self) -> Option<ErrorClass>` at `crates/antseal-cli/src/restore_out.rs:232`, whose rustdoc at `:198-231` names the impossible fixture in its own words — *"a `success_envelope` over a `RestoreOutput` carrying a `verification-failed` row … which **no build could emit**"*. The registered fixture now renders through the real fold (`crates/antseal-cli/tests/machine_mode.rs:543-550`, `"[restore] result (at exit {} — the most severe row present)"`), so the committed document is produced by the producer rather than composed by the test.
  **The citation this row arrived with has moved.** `machine_mode.rs:409-411` no longer holds the restore fixture — that range is now a `seal` comment — and the fixture lives at `machine_mode.rs:525-550`. Recorded rather than silently re-pointed, because a lane sent to `:409` would edit the wrong document.
  **Accept row 1's unclosable half, stated plainly.** `RestoreOutput::exit_class` has **no production caller**: measured 2026-08-16, its only callers are `tests/machine_mode.rs:546`, `:1545` and `:1576`. The generic third arm exists and works — `crates/antseal-cli/src/lib.rs:173` computes the code from `outcome.exit_class` — and its one production consumer is **`verify`** (`commands.rs:778`), not `restore`, because `commands::restore` is still the seam stub (`commands.rs:602-612`, body `Err(crate::backend::unavailable("restore"))`). **So no process reaches this fold until U36 wires `restore`.** The row ticks because the fold is built, correct and asserted at the only venue that exists; it does **not** claim an end-to-end run, and `restore_out.rs:213-221` admits the same thing at the code. **U74** now carries the adjacent question of whether `restore`'s seam refusal should stay where it is at all.
  **Machine: this local host**, `cargo test -p antseal-cli --test machine_mode` and `--test restore_output`, in the wave-21 gate's `test` lane. **Taken with R82 as its Notes require** — both blessed `json-envelopes.txt` in the same act, from different premises, without collision.

### U70 — One vault work state, three spellings, three sites (and a fourth the census missed)
- Milestone: M3
- Size: S
- Deps: after U27 (which promoted `status::work_state_name` to `pub(crate)` and is what makes this consolidation cheap); U19/U23 (`list`/`status`, the two read surfaces that render a state), U20/U28 (`restore`/`reveal`, the two engines carrying the wordier copy), D48 (`restore`'s output policy), U31/Q20 (own the eventual *spelling*; this row owns the *count*)
- Spec: CLI surface — `list`, `status`, `restore`, `reveal` are one canonical surface (MVP-SPEC.md lines 147–149); seal journal and the pay/finalize split that creates the two incomplete states in the first place (lines 69, 145)
- Discovered by: **the U27 lane** (2026-08-12). `show` needed a word for a work's state, the lane found the tree already carried several, and it **declined to author another**: it promoted `status::work_state_name` to `pub(crate)`, reused it, and recorded the reason in that function's own doc. Reusing is not consolidating, and the consolidation is not U27's row to do — so it is minted here. This is the **Q112 class** (a product defect found *by* an instrument), which rule 8 mints immediately: it changes what a user reads, not what a checker says.
- Problem: `WorkState` has four variants and `antseal-cli` renders it through **four private functions in three vocabularies**, so the same vault state answers in different words depending on which command was typed. `crates/antseal-cli/src/pipeline/restore.rs` holds a private `work_state_name` rendering `"incomplete (nothing paid)"` and `"incomplete (paid, not finalized)"`; `crates/antseal-cli/src/pipeline/reveal.rs` holds a **byte-identical twin** of it whose own doc says it copies the restore engine's spelling *"so `reveal` and `restore` name states identically"* — true of those two and of nothing else in the tree; `crates/antseal-cli/src/status.rs` holds the `pub(crate) work_state_name` that collapses both incomplete arms to `"incomplete"` (consumed by `status` and, since U27, by `show`); and `crates/antseal-cli/src/listing.rs` holds `WorkRow::state_name`, a third implementation producing the same three words as `status`'s, beside `detail_state_name`, which carries the pre-pay/post-pay distinction in kebab (`incomplete-pre-pay` / `incomplete-post-pay`) and is what `list --json` publishes as `detail_state`. A user who runs `status` is told `incomplete`; the same work under `restore` is told `incomplete (paid, not finalized)`. **That is the D100 §1.2 class exactly** — one vault state, more than one command, more than one production, each documented as obviously right and none citing the others — and the citation is not decorative: D100 §1.2 is the finding that a divergence cannot be fixed in one of its homes without leaving the disagreement standing. **The census in `status.rs`'s own doc is off by one**: it names the restore spelling and `listing::WorkRow::state_name` and calls a new one *"a fourth"*, but `pipeline/reveal.rs` already carried one, so the tree holds four functions, not three. Measured 2026-08-12 and load-bearing for the fix's cost: **neither wordy string is pinned by any test, fixture or snapshot anywhere** (`grep -rn` over `crates/` and `testdata/` returns only their two definition sites apiece), while `"incomplete"` is pinned by `crates/antseal-cli/tests/snapshots/list-report.txt`, `list-anchor-nags.txt` and `json-envelopes.txt`. So the cheap direction and the honest one may not be the same direction, and the distinction the wordy pair carries is real — the difference between *"you owe nothing"* and *"you have paid and the proofs expire"* — which is why `restore` and `reveal` reach for it: at that moment it is the actionable fact.
- Do: reduce the coarse work-state word to **one** production called by every renderer. The natural home is the existing `status::work_state_name`, already `pub(crate)` and already shared by `status` and `show`; delete the private copies in `pipeline/restore.rs` and `pipeline/reveal.rs` and route `WorkRow::state_name` to the same table, so a fifth cannot arrive by being written next to a fourth. Where a message genuinely needs the pre-pay/post-pay distinction, it takes it from **one** finer production — `detail_state_name`'s existing kebab pair, or a single explicitly named sibling of it — and never by re-spelling the coarse word with a parenthetical, which is what created this row. Keep every match wildcard-free so a fifth `WorkState` variant breaks at the table rather than rendering as something plausible. The exact copy is U31/Q20's domain; what this row fixes is the **count**, and a rewording that leaves four producers has not satisfied it.
- Accept:
  - **Exactly one** function in `antseal-cli` maps a `WorkState` to a user-visible coarse state word, asserted by a test that **fails if a second appears** — a source scan over `crates/antseal-cli/src/` for a second site matching a `WorkState` variant to a string literal, proven fallible by planting one and recording the failure message. An assertion that the words currently *agree* is explicitly not enough: it goes green again the moment somebody adds a fourth that happens to agree, which is how this row's subject arrived.
  - Every command that renders a work state — `list`, `status`, `show`, `restore`, `reveal` — renders it through that one function, asserted **at the render sites**, not at the definition.
  - Committed fixtures move or stay deliberately: `list-report.txt`, `list-anchor-nags.txt` and `json-envelopes.txt` carry `incomplete` today and the two wordy strings are pinned by nothing (measured 2026-08-12), so whichever spelling survives, the fixture delta is stated in the change and re-blessed in it rather than discovered afterwards.
  - If the pre-pay/post-pay distinction survives anywhere, it has exactly **one** production too, and the coarse and fine words are not two spellings of one fact: `detail_state`'s kebab pair is the shipped machine form and stays it. `ENVELOPE_VERSION` stays **1** and no `--json` key is added, removed or renamed.
  - No `ErrorClass`, exit code, report field or wire key moves; `REPORT_VERSION` and the frozen registry are untouched.
- Notes: R18's frozen wording table is **not** the home for any of this — it freezes *verdict* wording for `verify` and the page, and a vault work state is neither; do not solve this row by extending a frozen set. The row is deliberately a consolidation and not a rewording: if the surviving word turns out to be wrong copy, that is U31/Q20's catalogue-and-lint work and it is far cheaper against one production than against four. U27's decision to reuse rather than author was correct and is not being corrected here — it is the reason this row is `S` and not `M`. Whoever lands it should also correct `status::work_state_name`'s doc comment, whose *"a fourth is what this visibility avoids"* undercounts the tree by one and would otherwise keep a wrong census in the most-read of the four sites.
- Notes: **[Registrar, 2026-08-16 — wave 21 closing. CLOSED; the consolidation is to TWO functions, not one, and that is the ruling rather than a shortfall.]** **Exactly two productions remain, adjacent in one file**: `pub const fn work_state_name(state: WorkState) -> &'static str` (the coarse word, 3 arms) at `crates/antseal-cli/src/status.rs:1130`, and `pub const fn detail_state_name(state: WorkState) -> &'static str` (the finer distinction, 4 arms) at `:1159`. Every renderer now delegates: `listing.rs:421` and `:435`, `show.rs:486` and `:540`, `status.rs:475` and `:614`, `pipeline/restore.rs:571`, `pipeline/reveal.rs:599`. The wordy strings `"incomplete (nothing paid)"` / `"incomplete (paid, not finalized)"` survive **only as prose inside the corrective rustdoc** at `status.rs:1105`, which is where a reader should meet them. That is the row's own rule honoured exactly — *"one production called by every renderer, with the finer distinction taken from one finer function and never from a re-spelled coarse word"*.
  **The Accept clause that had to fail, and does.** `exactly_one_production_maps_a_work_state_to_a_word` (`crates/antseal-cli/tests/work_state_word.rs:79`) carries a **closed list of one file with an expected hit count**, so a second production anywhere in `crates/antseal-cli/src` reddens it — agreement between two spellings is not enough to go green, which was the row's stated requirement.
  **A correction to this row's own headline.** It reads *"three spellings, three sites"* and then names a fourth in its own body; the tree's record (`status.rs:1096-1107`, `tests/work_state_word.rs:14`) says there were **four producers in three vocabularies**. The census in the title undercounts by one, in the direction the row itself already corrected — noted so a reader does not reconcile the two by deleting the fourth.
  **Machine: this local host**, `cargo test -p antseal-cli --test work_state_word` plus the pinned renderings (`list-report.txt`, `list-anchor-nags.txt`, `json-envelopes.txt`) unchanged, in the wave-21 gate's `test` lane.

### U71 — The reveal consent gate has no production caller, so nothing structurally forces it
- Milestone: M3
- Size: S
- Deps: after U29 (which built `reveal_consent` and flipped the dispatch), U28 (which minted the `consent` seam in `run_reveal`); U36 (the backend seam whose unconditional refusal is what keeps the gate unreached), U72 (the row that decides whether that refusal stays unconditional — a resolution there changes which production path this row must pin, not whether it must pin one)
- Spec: Core user flows 3 (line 36) — the irreversible-disclosure confirmation, which the sentence makes part of what `reveal` *is*; CLI surface (lines 147–149)
- Discovered by: **the U29 lane** (2026-08-12), **self-reported** — the lane that built the gate measured that nothing calls it outside tests and said so rather than closing on green. This is the **Q112 class** (a product defect found while building, not a defect of an instrument), so rule 8 mints it immediately: it changes what a user is asked before an irreversible disclosure.
- Problem: `crates/antseal-cli/src/reveal_out.rs`'s `run_reveal` takes `consent: G` where `G: FnOnce(&PreparedReveal) -> Result<(), CliError>` — an arbitrary closure, with no type, trait bound or construction rule tying it to `DisclosureConsent`. `crates/antseal-cli/src/commands.rs`'s `reveal` returns `crate::backend::unavailable("reveal")` before the engine is entered, so **every caller of `run_reveal` in the tree is a test** (`tests/reveal_output.rs` and `tests/reveal_consent.rs`), and `DisclosureConsent` is exercised **only over `MockBackend`**. In shape this is `restore`'s documented deviation (U20 note 1: the engine is proven at library level while the command refuses at the seam), and that precedent is why U28 and U29 were both allowed to land — but the **forget-direction is not the same**. For a restore policy, a caller who supplies the wrong argument gets a worse restore. Here, spec line 36 makes the confirmation **constitutive of the command**: `run_reveal` called with `|_| Ok(())` is not a degraded `reveal`, it is a *different command* — one that writes a `.sealproof` full of irreversibly disclosed plaintext without asking — and it compiles, passes every test in the tree, and reads as ordinary. That is the **U67 class one turn earlier**: U67's `backfill_block_numbers` is implemented, documented and unreachable; this gate is implemented, documented, and reachable only by a caller that has not been written yet, with nothing that will notice if the caller that is finally written omits it. The distinction the row turns on: the suite proves the gate **works**; nothing proves it is **called**.
- Do: make the gate's presence structural or asserted, not conventional, and prefer structural. Two shapes, either acceptable, and the choice is the lane's on measurement of what U72 leaves standing: **(a) close the seam** — the production entry point stops accepting an arbitrary closure for this argument (the gate is constructed inside, or the parameter is narrowed to a type only `reveal_consent` can produce), leaving the free-closure form to a `#[cfg(test)]`- or `test-util`-gated sibling so the existing suites keep their scripted gates; or **(b) pin the call site** — a red-capable source assertion that the one production path constructs the real `DisclosureConsent`, in the shape of `without_storage_linkage_has_exactly_two_callers` (D128 §3 R2 / its 2026-08-12 Correction (2)): a table of admitted call sites, each carrying its warrant, with the count asserted over the `src/` partition so a second production caller with a permissive closure reddens. Whichever is taken, the assertion must fail on a **plant** and the failure message must be recorded, per the standing rule that a nonzero exit is not proof the fault was found.
- Accept:
  - A planted production call to `run_reveal` with a permissive consent closure **fails a check**, and the failure is verified **by its message** (not by a nonzero exit); the plant is reverted and a grep for the planted token exits with zero hits.
  - The check is not satisfiable by a passing suite alone: state explicitly, at the check, why an assertion that `DisclosureConsent` *behaves* correctly does not discharge this row.
  - The existing scripted-consent test seams still compile and run — the fix must not cost the suites their ability to drive a declining gate, a granting gate and a never-asked gate.
  - No `ErrorClass`, exit code, `--json` key or committed snapshot moves; `ENVELOPE_VERSION` stays **1**. If shape (a) changes `run_reveal`'s public signature, the change is stated in the row's execution note with its callers enumerated.
- Notes: This row is **about reachability, not about correctness** — U29's gate is correct, snapshot-frozen and 19-tests covered, and nothing here re-opens it. Read it beside U67: the M1-named dominant class is *an implementation that exists and is merely unreachable*, and the two rows are the same defect at two distances from the user. The dependency on U72 is real but one-directional and not blocking: if U72 rules that a fully-cached `reveal` completes without a backend, the production path this row must pin **exists sooner** and the row gets cheaper; if U72 rules the refusal stays unconditional, the row still stands, because the pin must be in place *before* the first production caller is written, not after. Do not discharge this row by waiting for the `ant-backend` build to make the path reachable — a gate that is only forced in a feature-gated build nobody in CI compiles (U39) is forced by nothing.
- Notes: **[Registrar, 2026-08-16 — wave 21 closing. CLOSED, and it closed the right way round: U72 gave it a real production path, and the pin was written against that path rather than against a hypothetical.]** `the_reveal_consent_gate_has_exactly_one_production_caller` (`crates/antseal-cli/tests/reveal_consent.rs:651`) asserts a **count**, not a presence: exactly one call to `run_reveal(` under any `src/`, at `crates/antseal-cli/src/commands.rs`, carrying the real `gate.gate()` argument, and exactly one production `DisclosureConsent::new(`. The production caller is `commands.rs:684-689`, reachable because U72 moved `reveal`'s refusal off the entry — so the gate a real user meets is now supplied by production code and pinned by a red-capable check, which is the row's whole subject.
  **A finding worth keeping beyond this row: the scanner counts PROSE.** The check went red at `run_reveal( 4 time(s), not 3` — and the fourth "call" was a **rustdoc paragraph** that spelled the call form literally (`run_reveal(.., |_| Ok(()))`), because the scan matches text. **The expected count did not move**; the prose was reworded and the rustdoc now records that this happened, so the next lane does not re-find the trap. This is the [[assertions-that-cannot-fail]] family seen from the other side — a check that goes red for a reason unrelated to the property, which is just as corrosive as one that cannot go red at all. The same trap caught `the_raw_backend_type_is_named_in_exactly_one_file` on a rustdoc paragraph in the U67 lane the same day: **two independent instances in one wave**, which is what makes prose-counting a pattern rather than an anecdote. Both are recorded in `docs/instrument-ledger.md` under rule 8.
  **Machine: this local host**, `cargo test -p antseal-cli --test reveal_consent`, in the wave-21 gate's `test` lane.

### U72 — `reveal` refuses at the backend seam unconditionally, though a cached work needs no network
- Milestone: M3
- Size: S
- Deps: after U28 (the CLI wiring whose handler holds the refusal), R16 (the reveal engine whose D43 cache-first gathering is the fact the refusal ignores); U36 (the seam and its `backend::unavailable` message), D43 (the retention ruling), D68 (§3 R8's step order, which the change moves the refusal within); U71 (the sibling — if the featureless build gains a real production path, that path is the one U71 must pin)
- Spec: Core user flows 3 (line 36) — `reveal` as a user-facing command; storage retention and the local cache (D43's ground in the seal/restore flows); CLI surface (lines 147–149)
- Discovered by: **the U29 lane** (2026-08-12), while flipping the dispatch and measuring what the flipped path actually returns. **Q112 class** — it changes what a user's `reveal` does, not what a checker says.
- Problem: `crates/antseal-cli/src/commands.rs`'s `reveal` returns `crate::backend::unavailable("reveal")` **unconditionally**, before a work is resolved, before a selection is read and before any determination of what the work needs. But R16's gathering is **cache-first by ruling**: `crates/antseal-cli/src/pipeline/reveal.rs` documents it as *"cache-first, integrity-rechecked, never an error"* — every retained cache copy is integrity-rechecked and used, only a missing or corrupt copy is silently refetched, and `RevealSummary` carries the split as `units_from_cache` beside `units_from_network`. So a work whose retained cache is intact can be revealed with **zero** network access, and the deliberate asymmetry with `restore` (network-first, cache-fallback — D43 §5) exists *precisely because* reveal is the interactive consent moment retention was kept for. The consequence is user-visible in the build most people will run: without the `ant-backend` feature, `antseal reveal` refuses on a work whose every byte is on the local disk, with a message about a network it never needed. The refusal is not wrong in general — a work with an evicted cache genuinely needs a fetch — it is wrong **unconditionally**.
- Do: **rule it, then implement the ruling.** The question is not "move the refusal down"; it is *what does a featureless build promise for `reveal`?* Enumerate and decide at least: (i) does a fully-cached reveal complete in a build with no backend at all, or does it complete only in a build that has one and could not reach it? (ii) what happens when the cache is **partial** — refuse the whole reveal at the first required fetch (which is D68 §3 R8-compatible, since the refusal then lands after path resolution and collision check but before the consent gate), or disclose the cached subset (which changes what the user consented to and must be refused unless ruled otherwise)? (iii) does the refusal move **before or after** the consent gate, given D68's own clause that *nobody consents to a disclosure that cannot be written* — the analogue here is that nobody should consent to a disclosure that cannot be gathered; (iv) does `backend::unavailable`'s message change, and does the lazy path need a distinct class from `network-failure` (23)? Then implement: the handler resolves and prepares as far as it can, and the seam refuses at the point a fetch is actually required, not at entry.
- Accept:
  - A work whose cache is complete is revealed **end to end in a build without the backend feature** (or the ruling explicitly refuses this, with its reason on the record) — asserted by a run that makes **zero** backend calls, measured at the seam and not inferred from a green result.
  - A work whose cache is partial behaves exactly as the ruling says, asserted at the boundary: the first genuinely-required fetch is where the refusal lands, and the D68 §3 R8 step order is re-asserted around the moved refusal (output path resolved and collision refused **before** anything else, gate asked only when a bundle could actually be produced).
  - The refusal's message and class are stated: whatever it becomes, `exit_codes.rs` carries an exemplar and `cli-errors.display.txt` is re-blessed in the same change rather than after it.
  - `RevealSummary::units_from_cache` / `units_from_network` are the evidence used, not a proxy — the test reads the split the engine already reports.
  - No `REPORT_VERSION`, `ENVELOPE_VERSION`, report field or frozen wording moves; this row is a control-flow and policy change, not a format one.
- Notes: The reason this is a *ruling* and not a patch is the blast radius: the refusal's position is the difference between a build that can reveal and a build that cannot, in the default feature set, for the command spec line 36 puts at the centre of the product — and it interacts with two frozen orders at once (D68 §3 R8's step order and D43's asymmetry with `restore`). Note the direction of the asymmetry when ruling (ii): `restore` is network-first *because* its job is to reconstruct what may be gone, while `reveal`'s job is to disclose what the user already has, which is the argument **for** the lazy refusal and also the argument **against** disclosing a partial subset silently. Whoever lands this should record whether the same latency exists on any other command that refuses at the seam — `show` already has no backend at all (U27) and is the existing proof that a vault-local read path is a coherent shape.
- Notes: **[Registrar, 2026-08-16 — wave 21 closing. CLOSED: the featureless build reveals from cache, and the refusal is now lazy.]** `commands::reveal` (`crates/antseal-cli/src/commands.rs:657-694`) no longer returns `backend::unavailable("reveal")` at entry. It unlocks through the same `unlock_for_command` expression as `list`, `show` and `status`, then drives `run_reveal` over a `VaultLocalBackend` (`crates/antseal-cli/src/backend.rs:235+`) — a backend that serves nothing — so a work whose retained cache is intact completes with **zero** network access, and a partially-cached one still refuses at the first genuinely required fetch. The D68 §3 R8 step order is re-asserted around the moved refusal.
  **A third independent witness of the wiring, and no grep would have found it.** `crates/antseal-cli/tests/upgrade_hook.rs`'s `every_subcommand_either_arms_the_hook_or_is_a_documented_non_armer` **reddened** on the change, because unlocking the vault means `reveal` now **arms** D42's opportunistic-upgrade hook; the table's `reveal` row said `false` with the reason *"like `restore`, whose seam it shares — it reaches the storage-backend seam before the vault"*. Flipped to `true` at `upgrade_hook.rs:803-816` with the new reason recorded in the row itself, including that — like `show` and `status` — it arms **before** resolving the work id, so a minimal argv with an unknown id still leaves the invocation armed. **The gate found this, not the analysis** ([[gate-finds-what-analysis-misses]], a fourth instance): it was found by running the full suite, and no grep for the seam would have reached it.
  **Two consequences registered rather than absorbed.** **U71** closed on the production path this row created — the consent gate now has a real caller and a count-based pin. **U74** and **U75** carry the census this lane made while enumerating which other seam-refusing commands share `reveal`'s latency: `seal` no, `status --upgrade` no, `show` already has none (U27), **`restore`** has a latent offline-capable path with no recorded choice (U74, and D43 §5 makes its answer legitimately different), and **`verify --live`** refuses unconditionally with its collector reachable only from tests (U75).
  **Machine: this local host**, `cargo test -p antseal-cli` including `--test reveal_vault_local`, `--test reveal_consent` and `--test upgrade_hook`, in the wave-21 gate's `test` lane (2 871 tests, PASS).

### U73 — `seal` answers differently in the two feature builds, and nothing had ever seen the second answer
- Milestone: M3
- Size: XS
- Deps: after U13 (the wiring that made the feature-ON arm construct a backend at all), U36 (the seam and its two `#[cfg]` arms), U72 (the sibling ruled hours earlier, whose ordering this must not contradict), R82 (the feature-parameterised snapshot, arm (b), whose shape this borrows for behaviour), Q153 (the ruling that keeps `heavy-features` local-only, which is why nothing has ever witnessed it), D89 (a default build is an inspect-and-verify build), D48 §6 / D69 §3 R3 (the severity fold — named because it looks like precedent here and is not)
- Spec: Core user flows 1 — `seal` (MVP-SPEC.md line 34); CLI surface (lines 147–149); M3 milestone (line 156)
- Discovered by: **the wave-21 full local gate** (2026-08-16), `ANTSEAL_GATE_HEAVY=1 ./scripts/local-gate.sh`, whose `heavy-features` lane went red on two `seal_command.rs` rows. **Pre-existing, not the wave's** — R82's class one command over, and the red had stood for an unknown number of commits because Q153 makes `heavy-features` local-only.
- Problem: **a spawned-binary test hard-coded one build's answer, and the other build's answer had never been looked at.** `antseal seal a.txt --network devnet` with a **valid** plan into a vault-less `HOME` exits **23** on the default build — the seam has no adapter — and **2** under `--features ant-backend`, because `seal_over_backend`'s feature-ON arm (`crates/antseal-cli/src/commands.rs:326`) opens the vault first and this `HOME` has none. Two registered tests asserted 23 and the word `ant-backend` unconditionally, so under the feature they failed with `left: Some(2) right: Some(23)`: `the_binary_refuses_bad_plans_without_a_vault_a_network_or_a_passphrase` and `a_no_fine_tree_pattern_matching_nothing_is_refused_at_the_surface` (default build: 23 passed, 0 failed; feature build: 21 passed, 2 failed). **Test and code disagreed, and which of them was wrong was the whole question.**
- Do: find the third witness rather than picking a side, then make the test arm-aware if the code is right. Four witnesses were available and all four point the same way: **(1) U72**, hours older and the same shape, had just ruled vault-before-seam for `reveal` and paid the cost out loud; **(2)** `seal`'s own recorded rule is *"the cheapest thing that can say no goes first"* and `open_layout` is one `stat` against a tokio runtime, a wallet-key read and a connect; **(3)** `BackendArm::Compiled`'s sentence — *"does not construct one yet"* — is **false** for a `seal` U13 already wired, so satisfying the old assertion would have meant printing a falsehood; **(4) D89** settles the general form, since the two builds are capability-divergent by design. So: **rule the divergence correct**, record the reasoning where the code is, and make the expectation select per arm through `BackendArm::THIS_BUILD` rather than `#[cfg]`, so the default build still compiles and still carries the feature build's answer.
- Accept:
  - `cargo test -p antseal-cli --features ant-backend --test seal_command` is green, and so is the default build — both asserted, because the defect is precisely that one of the two was never checked.
  - The expectation is selected by a **runtime** arm read, not `#[cfg]`-ed out: both answers are compiled into both builds, so the only CI job that ever runs (the default one, per Q153) still type-checks the feature build's expectation.
  - The reasoning is recorded at `commands::seal` with its four witnesses, and the two witnesses that are *code-local rather than ruled* are labelled as such — a reader must not mistake a comment's own rule for a decision record.
  - The refusal messages are unchanged and no `--json` envelope key moves: `ENVELOPE_VERSION` stays 1 and both arms of `backend::unavailable` are byte-identical to before.
- Notes: **CLOSED in the act that minted it** (2026-08-16) — the row exists because the finding must be registered, not because work is outstanding. **The ruling is recorded at `crates/antseal-cli/src/commands.rs:215-275`** (the rustdoc on `seal`) and the arm-aware expectation at `crates/antseal-cli/tests/seal_command.rs:1370-1425`, in `next_stage_for_a_valid_seal_plan`, which additionally asserts that `BackendArm::Compiled`'s message names no feature flag — so witness 3 is fallible rather than remembered. **The two records that look like precedent and are not** are named at the code: D48 §6 / D69 §3 R3 are one severity-fold rule over facts established *together in one run*, and these two refusals are **sequential gates** with no second fact to fold; D65 partitions one build's `--json` schema by stability over time and rules on no cross-build question. **This row's citations were written bare on purpose** while U's ceiling stood at 72 — `check-traceability.py --task-citations` fails a *marked* citation of an unallocated id, and bare above the ceiling is its documented tier-2 blind spot. Allocating this row advances the ceiling and drops every one of them into tier-1 checking, which is the intended self-heal. **One correction this row owes R82** is recorded at both sites: R82's Accept reads *"only the `error.message` string of the feature-ON arm is in scope"*, which is exactly right about the **envelope** — both arms are `network-failure`, exit 23, and `json-envelopes.txt` carries both — but is incomplete as a description of the two **builds**, because for a vault-less `seal` the feature build never reaches that envelope and exits 2 instead.

### U74 — `restore` has a latent offline-capable path and nothing in the tree says so
- Milestone: M3
- Size: S
- Deps: after U20 (the restore engine and its policy), U36 (the seam the handler refuses at), D43 §5 (the ruling that makes `restore` network-first, which is why this is not U72 a second time), U72 (the worked precedent for ruling a seam-refusal's position), R16 (the cache-first gathering `reveal` has and `restore` does not)
- Spec: Core user flows 4 — `restore` (MVP-SPEC.md line 36); storage retention and the local cache; CLI surface (lines 147–149)
- Discovered by: **the U72 lane** (2026-08-16), while enumerating which other seam-refusing commands share `reveal`'s latency. The census result is part of the finding: `seal` no (it needs the network by definition), `status --upgrade` no, `verify --live` no (that one is **U75**), `show` already has none (U27).
- Problem: **`restore` refuses at the seam before anything is determined, and unlike `reveal` that is not wrong unconditionally — but it is unrecorded.** `crates/antseal-cli/src/commands.rs:602-612` is a body of one line, `Err(crate::backend::unavailable("restore"))`, and its rustdoc argues the position deliberately: the seam is reached *"before the vault is opened and before any passphrase is asked for"*. **D43 §5 makes `restore` network-first by ruling**, so the entry refusal is defensible in a way `reveal`'s was not. What is missing is the other half: a work whose cache is complete could in principle be restored with no backend at all, and **the tree neither does it nor records the choice**. A reader comparing the two handlers after U72 finds one lazy refusal and one eager one with no statement of why they differ — which is how the next lane "fixes" the asymmetry in the wrong direction.
- Do: **rule it, then record the ruling wherever the refusal lives.** The question is not "move the refusal down"; it is *does D43 §5's network-first rule mean `restore` must always ask the network, or only that it must prefer it?* Decide at least: (i) whether a fully-cached restore completes in a build with no backend, or is refused on the ruling's authority; (ii) if it completes, what a **partial** cache promises, given that `restore`'s job is to reconstruct what may be gone and a silent partial restore is a worse failure than a refusal; (iii) whether `restore`'s refusal keeps its position relative to the vault and the passphrase, which its own doc currently argues for on a reason unrelated to caching. If the answer is "refuse, always", the row closes on a **comment and a test** — the cheapest possible outcome and still worth the id, because the absence of the statement is the defect.
- Accept:
  - The chosen answer is recorded at `commands::restore` with its reason, naming D43 §5 and U72 so the asymmetry with `reveal` reads as a decision rather than an oversight.
  - Whichever arm: a test drives it. If refuse-always, a test asserts the refusal happens for a **fully-cached** work too — otherwise the ruling is a comment nothing defends. If lazy, the first genuinely-required fetch is where the refusal lands, measured at the seam and not inferred from a green result.
  - The refusal's message and class are unchanged unless the ruling moves them, in which case `exit_codes.rs` carries an exemplar and `cli-errors.display.txt` is re-blessed in the same change.
  - No format cost: no `REPORT_VERSION`, `ENVELOPE_VERSION`, report field or frozen wording moves.
- Notes: **this is U71/U72's shape a third time and the third is what makes it a class** — a seam refusal standing in front of work the build can actually do. It is registered separately rather than folded into U72 because **the answer may legitimately differ**: D43 §5 gives `restore` a ruling `reveal` did not have, so the same reasoning can land on the opposite arm without either being wrong. **Blocked by nothing and blocking nothing at M3.** Take it with **U75**, which is the same census's other finding and touches the adjacent handler.

### U75 — `verify --live` refuses unconditionally, so its collector has no production caller
- Milestone: M3
- Size: S
- Deps: after U30 (the `verify` command and its `--live` flag), U36 (the seam that must be wired before the flag can do anything), R81 (the row whose Accept row 1 this bounds), R11/R21 (the live check and its composition), D43 (the retention ruling), D69 §3 R6 (why the live layer reaches no exit code)
- Spec: Verifier — the storage layer and `--live` (MVP-SPEC.md line 119); core user flows — verify (line 38); CLI surface (lines 147–149)
- Discovered by: **the R81 end-to-end lane** (2026-08-16), while proving the manifest row at the deepest venue that exists and finding the deepest venue is not a process invocation.
- Problem: **the flag is wired to a refusal, and the code it would drive is reachable only from tests.** `crates/antseal-cli/src/commands.rs:740-745`, inside `verify` (`:721`), is `if live { … return Err(crate::backend::unavailable("verify")); }` — and it is **not** `#[cfg]`-gated, so it fires in the `ant-backend` build too; the arm selection happens inside `backend::unavailable` at runtime, not around this return. Measured 2026-08-16, a whole-tree grep finds `collect_live` in its own definition (`crates/antseal-cli/src/verify_host.rs:412`), in `crates/antseal-cli/tests/verify_command.rs:459`, and in `crates/antseal-cli/tests/verify_live_manifest.rs` — and **nowhere in `commands.rs`**. So **R81's Accept row 1 read literally — *"`verify --live` over a work whose manifest blob was replaced on the network renders a manifest row"* — is not satisfiable by any process invocation, by anyone**, until the U36 seam is wired into the `verify` handler. R81's own `Deps` calls U30 *"the `--live` driver"*, while the driver refuses before verification.
- Do: **rule whether `verify --live` gets a real path in this milestone, and if not, say so where the flag is defined.** Arms: **(a)** wire the seam into the `verify` handler, which makes every `--live` row in the tree end-to-end reachable and is the only arm that closes R81's Accept row 1 as written; **(b)** rule the refusal correct for M3 and **make the flag say so** — `--live` that refuses unconditionally should be documented at the flag and in `--help`, not discovered at run time, and the row that depends on it should carry the bound; **(c)** the U72 shape — refuse only when a fetch is actually required — which is coherent here because the live check's whole subject *is* the network and therefore reduces to (a). Whichever arm, the `commands.rs` refusal and R81's Accept must end up agreeing, because today they do not.
- Accept:
  - The chosen arm is recorded at `commands::verify`'s `live` branch with its reason, naming R81 and U36, so a reader of the refusal finds the ruling rather than re-deriving it.
  - If (a): one test drives the manifest-divergent case **through the binary**, and R81's Accept row 1 is discharged by citation to it rather than to a library-route test.
  - If (b): `--live`'s own help and doc state that it refuses in this build, `cli-errors.display.txt` carries the exemplar, and R81's Accept row 1 is amended on the record to name the route it is actually proven at.
  - `collect_live` gains a production caller or a recorded reason for having none; a `pub` function reachable only from tests is the exact shape U67 and U71 were minted for, and this row must not leave a fourth.
  - Exit codes and frozen wording untouched: D69 §3 R6 keeps the live layer exit-code-free at the type level.
- Notes: **U71/U72's shape a fourth time**, and the reason the class is now worth naming: a seam refusal standing in front of work the build can do (`reveal`, ruled), work it may or may not be allowed to do (`restore`, **U74**), and work whose only purpose *is* the network but whose collector is nonetheless fully built and fully tested (this row). **R81 still ticks and this row does not un-tick it**: R81 owed a manifest row in the live section and the manifest row is there at every hop that exists — production collector (`verify_host.rs`'s `collect_live`), production projection (`antseal_net::live_inputs`), production renderer, production machine document — and is proven at the `antseal-net` route and at the CLI route through `crates/antseal-cli/tests/verify_live_manifest.rs`. What this row records is that the **process** hop does not exist yet, which is a bound on R81's evidence and not a defect in R81's work.

### U76 — The value the success metric's own falsifier reads is in every bundle and printed by no command
- Milestone: M4
- Size: S
- Deps: after U23/U28 (`show` and its `--json` view, the natural venue), D29 (the frozen `show` field table this row must not move), D65 (the tier any new machine key lands in), D73 §4 R5 (the falsifier set that needs the value), Q35 (the row that cannot run its own instrument without it)
- Spec: CLI surface — `show` and `--json` (MVP-SPEC.md lines 147–149); manifest key 5 `pubkeys` (`docs/format/registry-v1.md:573`); Milestones M4 success metric (line 157)
- Discovered by: **D73's lane** (2026-08-16), which named it at §7 item 2 and corroborated it at §6 item 7; re-measured whole-crate by the wave-22 registrar 2026-08-17.
- Problem: **a falsifier that cannot fire is a falsifier that certifies whatever it is pointed at.** D73 R5 **X2** is the check that two rows claiming distinct participants but carrying the **same sealer public key** are one vault and must collapse to one row; D73 R5 **X7** then makes the instrument falsify itself — *if X2 has never fired, the count may not be declared met*. X2 reads manifest key 5 `pubkeys`. That value is in **every** bundle and `ManifestBody::pubkeys()` exists to reach it (`crates/antseal-core/src/manifest/body.rs:1213`), but a whole-crate sweep of `crates/antseal-cli/src` for `pubkeys|pubkey|public_key|verifying_key` finds it **constructed** at `pipeline/seal.rs:534`, stored into the body at `:540`, and read back for display **nowhere**: `show`'s `--json` view begins `"work_id": …` at `show.rs:536` and never reaches the keys, `VerificationReport` carries `signature_scheme` but no key bytes, and the only other hits in the crate are two `#[cfg(test)]` fixtures. **So D73 R5 X2 cannot be run with the shipped binaries, by anyone**, and by X7 the ≥10-external-users metric may not be declared met while that is true. This is the U67/U71 shape from the other direction: not an unreachable implementation, but a value with no reader at all.
- Do: **give the value one route, and take the cheapest one that D29 permits.** A maintainer-side tool over the existing library accessor is sufficient — X2 is run by the maintainer against submitted bundles, not by participants — so **no format change is needed, no `--json` key need move, and D29 stays frozen**. Decide explicitly between (a) a `test-util`/dev binary or an `xtask`-shaped helper that prints a stable fingerprint of `pubkeys` for a bundle, and (b) a real `show` field, which is the friendlier surface but moves D29's frozen table and lands a new key in D65's tier C. **(a) is the lean and (b) must not be taken by accident.** Whichever is chosen, the printed form should be a **fingerprint, not raw key bytes** — D73 R7's tracking table calls the column `vault_fp` and *opaque*, and a metric sheet that publishes participants' raw public keys is a worse instrument than one that publishes a digest.
- Accept:
  - X2 is runnable end to end from shipped or maintainer-side tooling: two bundles sealed from one vault collapse to one row, and two from different vaults do not — proven by a test that exercises both directions, not just the collision.
  - D29's frozen `show` table is unmoved, or the move is a recorded format event with its tier stated; `REPORT_VERSION`/`ENVELOPE_VERSION` are explicitly named moved or unmoved.
  - The rendered form is a fingerprint with its derivation recorded, and **no raw secret material and no raw key bytes reach a log, an error or a fixture** (project rule 6).
  - Q35's entry cites the route by name, so a later reader of the metric can see how X2 was actually run.
- Notes: **Registered as its own row rather than folded into Q35** because Q35 is a plan-and-run row and this is a CLI-surface gap with a format question attached; a plan row silently acquiring a format decision is how frozen tables move. **The row is small and the trap is not**: the tempting fix — printing `pubkeys` in `show --json` — is the one that costs a frozen surface, and it should be chosen deliberately or not at all.

### U77 — `verify` is asserted never to prompt by a test that three English words could walk past
- Milestone: M4
- Size: S
- Deps: after U30 (the `verify` command), U29/U72 (the consent-gate idiom this borrows), D134 §4 item 1 (the ruling that nominates this row), U32 (the freeze this must precede)
- Spec: CLI surface — `verify` is the key-free third-party entry point (MVP-SPEC.md lines 147–149); core flows — verify (line 38)
- Discovered by: **D134's lane** (2026-08-16), §4 item 1, which nominated it as *"New row candidate (U domain, S)"*; re-measured by the wave-22 registrar 2026-08-17, which found D134's own citation off by thirteen lines.
- Problem: **the only behavioural check that `verify` never asks for a secret is a substring search over already-captured output.** `crates/antseal-cli/tests/verify_command.rs:785-789` — inside `verify_runs_on_a_vault_less_machine_without_prompting()` at `:773`, **not** at `:772` as D134 cites, which is the `#[test]` attribute — loops over `["passphrase", "Passphrase", "no vault exists"]` and asserts none appears in `stdout` or `stderr`. A prompt reading *"Enter word 1 of 24"*, or *"Key?"*, or *"Type the 24 words from your card"* contains none of those three tokens and passes. The test is also blind to prompting **as a behaviour**: `run_binary` collects output after exit, so a binary that blocked on a read from a closed stdin would present as a hang, not as a failed assertion. **Today nothing can slip past it because no key-entry vocabulary exists in the product; D134 rules that a paper-key `reveal` lock mode and an unlock surface are coming, and the guard must be strengthened BEFORE that vocabulary exists** — after it exists, the strengthening is a change made against a live counterexample instead of a rule made in advance.
- Do: **give `verify`'s handler the source-shape assertion `reveal` already has.** The idiom is `crates/antseal-cli/tests/reveal_consent.rs:651-660` (`the_reveal_consent_gate_has_exactly_one_production_caller`): a closed, exhaustively enumerated table of `(file, expected count, why)` scanned over the tree's own source text, with the searched-for call form assembled by `concat!` (`:679-683`) so the scanner cannot match itself. Applied here, the assertion is that `verify`'s handler and everything it reaches contain **zero** prompt-capable call sites — no `rpassword`, no interactive read, no vault-open path — expressed as an enumerated closed list rather than as a search for English words. **Keep the existing output check**; it is cheap and it catches a different failure. This row adds a second, structural one.
- Accept:
  - A source-shape test enumerates the prompt-capable call sites reachable from `commands::verify` and asserts the set is empty, with exact expected counts, and the scanner is self-exempt by construction (`concat!` or equivalent) rather than by an exclusion the next refactor breaks.
  - A planted fault — an interactive read wired into `verify`'s handler — is watched **red by message**, and the message names the site.
  - The test's own name says what it guarantees, so a later reader does not have to infer it from three forbidden strings.
  - It lands **before** any word-prompt or key-entry vocabulary exists anywhere in `antseal-cli`, and that ordering is stated on the test.
  - The existing substring assertion is kept, not replaced, and its known weakness is recorded at the site so nobody re-derives this row.
- Notes: **A row and not a ledger line, and the reason matters**: its subject is a test, which protocol rule 8 sends to `docs/instrument-ledger.md` by default — but D134 §4 item 1 rules it a new row candidate, D134's owning task is **U32** on the M4 ship path, and the whole value of the work evaporates once the thing it guards against exists. That is the promotion test satisfied by a ruling rather than by inference. **D134's citation of `:772` is corrected here** (the assertion is `:785-789`); a lane following the record's line number lands on an attribute.

### U78 — `init`'s human report has no committed golden rendering, and both freezes pass over it

- Milestone: M4
- Size: S
- Deps: after U11/U18 (the `init` report and its standing warnings), U32 (the release freeze) — **ordered before U32**, because a freeze over an unrendered surface freezes nothing
- Spec: Vault — *"Docs and CLI must nag on both failure modes"* (MVP-SPEC.md line 143); CLI surface (lines 147–149)
- Discovered by: **D140's lane** (2026-08-17), §5 row 2, while measuring whether `init` owed new bytes for Q24's linkage clause; promoted from `docs/instrument-ledger.md` under protocol rule 8 because U32 is on the M4 ship path and its snapshot freeze passes over the exact copy line 143 mandates.
- Problem: **the one CLI output the spec names by obligation is the one output nothing pins.** Line 143 requires the CLI to nag on both failure modes; `init`'s closing report carries them, from `LOSS_WARNING` and `THEFT_WARNING` (`crates/antseal-cli/src/vault/bookkeeping.rs:68-77`). Neither freeze reaches it. `check-copy-style.py` reaches CLI copy **only through committed snapshots** (`:28-33`), and there is no snapshot of `init`'s report. U32's snapshot freeze inherits the same gap. The nearest guard is worse than nothing: `crates/antseal-cli/tests/init_command.rs:698-702` asserts that `render()` contains every line `funding_lines(...)` produces — but `render()` **calls** `funding_lines()` (`init.rs:264`), so the assertion compares a function's output to itself and cannot fail on any wording change. That is the *assertions that cannot fail* class sitting on the surface the spec names.
- Do: Commit a golden rendering of `init`'s human report — the same idiom `cli-surface.help.txt` uses, byte-compared, regenerable behind an explicit bless variable — over a fixture vault with every value that varies (address, paths, timings) substituted deterministically. Assert the two warning constants appear **by identity**, not by substring, so a reworded constant moves the golden and a reviewer sees it. Replace the self-comparing assertion at `init_command.rs:698-702` with one that reads the expected lines from the golden file, and record at the site why the old one could not fail.
- Accept:
  - A committed golden rendering of `init`'s report exists and is byte-compared by a test; a planted one-character change is watched **red by message**.
  - The golden is deterministic across machines: no absolute path, no timestamp, no address that varies between runs, proven by two runs rather than asserted.
  - `check-copy-style.py` reaches the new snapshot — verified by planting a positioning violation into it and watching the lint redden, not by adding it to a list and assuming.
  - `init_command.rs:698-702`'s vacuous assertion is replaced or removed, with the reason recorded so nobody re-derives it.
  - It lands **before** U32 freezes the surface, and U32's entry cites it.
- Notes: **A promotion, and the promotion test is what licences the id.** Its subject is a missing snapshot — instrument-shaped, which rule 8 sends to the ledger by default — but the surface it leaves unpinned is the one MVP-SPEC line 143 mandates by name, and U32 (on the ship path) cannot honestly freeze what has never been rendered. The ledger entry stays as the finding's own record. **A related mis-citation is deliberately not folded in**: `crates/antseal-cli/src/init.rs:343` attributes to line 143 both the venue (`init`) and the word *"once"*, and line 143 contains neither — U18 is the citation for both. That is instrument-class and stays in the ledger.

### U79 — D49 Ruling 3's dry-run shortfall render is unimplemented, and the covering test cannot see it

- Milestone: M4
- Size: S
- Deps: after U14/U16 (the dry-run report), D49 (the ruling), U32 (the release freeze this should precede)
- Spec: Core flows consent step (MVP-SPEC.md line 34); CLI surface (lines 147–149)
- Discovered by: **Q23's lane** (2026-08-17), while checking its page's account of the two funding failures against shipped behaviour.
- Problem: **a resolved decision's ruled behaviour does not exist, and the test written for that behaviour asserts everything except the part that is missing.** D49 §Ruling 3 (`docs/decisions/D49-dry-run-network-semantics.md:98-108`, the sentence at `:100-102`) says that on shortfall a dry run *"still prints the complete report, then exits with the same distinct insufficient-ANT-token / insufficient-ETH-gas codes a real seal would use"*, and grounds the whole *"`seal --dry-run` is the documented CI gate"* claim on it. Shipped, `crates/antseal-cli/src/seal_run.rs:505-521` builds the report at `:514`, runs `report.preflight()?` at `:519`, and returns the report at `:520` — the `?` preempts the render, which happens only from the `Ok` value (`seal_run.rs:279-303`). **On shortfall the user gets the typed error and no report at all.** The real-seal path does it correctly and in the opposite order: `seal_consent.rs:441` emits, `:444` fails. The covering test — `a_dry_run_with_a_drained_wallet_exits_with_the_real_shortfall_code` (`crates/antseal-cli/tests/seal_command.rs:813-858`) — asserts class, exit code and zero payment calls (`:852-855`) and **never that the report printed**, which is exactly why the divergence survived.
- Do: **Rule it before building it.** Either implement the emit — render the report before `preflight()?` in the `DryRun` arm, mirroring `seal_consent.rs:441` — or amend D49 by dated addendum to say the shortfall path is error-only and repair the *"documented CI gate"* claim that rests on it. Whichever way it goes, extend the test to assert the report's presence or absence explicitly, so the behaviour is pinned in the direction chosen.
- Accept:
  - The decision and the code agree, and the agreement is a recorded act — either an implementation or a dated addendum, never a silent divergence.
  - The test asserts the report's presence (or documented absence) on the shortfall path, with a planted regression watched red.
  - `--json` behaviour on the shortfall path is stated, not left to inference: a script reading the single result document needs to know whether one exists.
  - The exit codes stay 20/21 and stay distinct — that half of Ruling 3 is met today and must not regress.
- Notes: The sibling of U80: both are places where D49's text and the shipped surface parted, and neither was visible to any check. Ruling 3 is the one that matters more, because `docs/user/funding-your-wallet.md` now points users at `seal --dry-run` as the live route for cost.

### U80 — The frozen help ships the exact phrase D49 refused

- Milestone: M4
- Size: S
- Deps: after U1 (the frozen CLI surface), D49 §Ruling 4; **U32** is where a re-bless would be ratified for release
- Spec: CLI surface (MVP-SPEC.md lines 147–149); core flows consent step (line 34)
- Discovered by: **Q23's lane** (2026-08-17), while quoting the CLI's own wording into `docs/user/funding-your-wallet.md`.
- Problem: **the snapshot everyone treats as authoritative carries wording a decision named and rejected.** `crates/antseal-cli/tests/snapshots/cli-surface.help.txt:131` reads *"Run every free step and print the plan + **true cost quote**; nothing is paid, anchored, or uploaded (D49)"*, sourced from `crates/antseal-cli/src/cli.rs:311-325`. D49 §Ruling 4 (`:109-116`) refuses that exact phrase: *"claiming 'true cost' (the register's phrase) would overstate; 'true pricing mechanism, indicative figure' is the honest contract."* The renderer already complies — `seal_consent.rs:139`/`:145` label the figure `(indicative)` and `seal_run.rs:286` states the re-quote — so **only the help contradicts the ruling**, and it is the copy a user reads first. It has already propagated: `docs/user/funding-your-wallet.md:49` quotes it. The file is byte-frozen by `help_surface_matches_committed_snapshot` (`crates/antseal-cli/tests/cli_surface.rs:65-88`), regenerable only under `ANTSEAL_BLESS=1`, so correcting it is a deliberate freeze event rather than a typo fix.
- Do: Change `cli.rs:311-325` to D49's own contract wording, re-bless the snapshot as a named act with the reason in the commit, and re-check every place the old phrase propagated (`docs/user/funding-your-wallet.md:49` at minimum). Record at the site that the phrase was ruled against, so a later reader does not restore it as an improvement.
- Accept:
  - The help no longer claims a *"true cost quote"*, and the replacement is D49's wording rather than a third phrasing.
  - The re-bless is recorded as a surface event with its authority named (the freeze failure message names **U1**, not U32 — the freeze owner and the release-ratification owner are different rows, and the act must name the right one).
  - Every propagated copy is corrected in the same act, found by search rather than by memory.
  - `tasks/U.md:314`'s claim — *"`cli-surface.help.txt` was not re-blessed and did not need to be: `--dry-run`'s help already documents D49"* — is corrected at its own site, because it is the sentence that let this survive.
- Notes: The cheapest row in the tranche and the one with the sharpest lesson: a freeze preserves whatever was true when it was taken, including the things a later decision refused. **Correction to the brief:** the freeze is **U1's** (`cli_surface.rs:84` — *"The surface is frozen (U1)"*), not U32's; U32 is the release freeze that would otherwise ratify the phrase for shipping.

### U81 — `StorageError::StrandedPayment` has no `CliError` variant, so money already spent exits as a transient network failure

- Milestone: M4
- Size: S
- Deps: after U2 (the exit-code contract), S13/D37 (the multi-tx payment and its journaled partial receipt), U32 (the exit-code freeze this must precede)
- Spec: Storage payments (MVP-SPEC.md lines 34, 149); Risks — payment handling
- Discovered by: **Q23's lane** (2026-08-17), while tracing how the two funding failures stay distinct all the way to the exit code.
- Problem: **the one storage error that means money moved is classified as the one that means nothing happened.** `crates/antseal-net/src/error.rs:91` defines `StorageError::StrandedPayment { landed_tx_count, reason }` — *"payment stranded mid-sequence: N sub-batch transaction(s) landed before the failure; the journaled partial receipt is authoritative"*. `crates/antseal-cli/src/pipeline/error.rs:304-327` maps the storage boundary onto exit codes and matches only `InsufficientAnt`, `InsufficientGas` and `ProofsExpired`; everything else falls into the catch-all at `:323-325` and becomes `CliError::NetworkFailure` → class `network-failure` → **exit 23** (`crates/antseal-cli/src/error.rs:384`, `:454`). A mid-sequence payment loss therefore exits with the same code a peer timeout does, and a script that retries on 23 — which is the correct response to a transient transport failure — retries against a wallet that has already spent. The catch-all swallows **six** of the nine variants, and `StrandedPayment` is not the only one whose own doc contradicts its class: `NotFound` (`error.rs:127`) documents itself as *"distinct from transport failure"* and lands there too.
- Do: Give `StrandedPayment` its own `CliError` variant and its own class, in an **unused** code from the storage band — D134 §2 R3 makes the exit-code table append-only, no code renumbered or reused, and 44–49 is reserved by `tests/exit_codes.rs` for D69 §3 R9's verdict futures. The message must carry `landed_tx_count` and point at resume, because the journaled partial receipt is authoritative and resume will not re-pay mapped quotes. Decide `NotFound` in the same act — either give it a class or record why the catch-all is right for it — and add the class to the error-code contract document and the exit-code test table.
- Accept:
  - A stranded payment exits with its own code and its own class name; a test drives the backend to that error and asserts the code, and a planted regression to the catch-all is watched red.
  - The new code is appended, never reused, and is outside the 44–49 reservation; `tests/exit_codes.rs` and `docs/testing/error-code-contract.md` both carry it.
  - The message names the recovery (resume) and states that mapped quotes are not re-paid — the user's first question after "money moved" is whether re-running spends again.
  - `NotFound`'s disposition is recorded either way, so the next reader does not re-derive the same catch-all sweep.
  - It lands **before** U32 freezes the exit-code table.
- Notes: **The one place the ANT/gas discipline collapses.** The whole reason `insufficient-ant-token` (20) and `insufficient-eth-gas` (21) are separate codes is that the user remedies them differently; by the same rule, *"nothing happened, retry"* and *"money moved, resume"* are the two most different outcomes in the product and they currently share a code. **Correction to the brief:** `StrandedPayment` is not the sole casualty — six variants share the catch-all, and `NotFound` is a second, weaker instance of the same misclassification.

### U82 — D24's ruled hard CLI error was never built, and two source comments assert it exists

- Milestone: M4
- Size: S
- Deps: after G6 (splitting never applies to binary files), U15/U16 (`seal`'s plan validation), D24 (the ruling)
- Spec: **[Spec citation corrected 2026-08-18 at the wave-25 registration, per D149 §2: lines 60–66 are the `ant-core` pin and the `StorageBackend` signatures; the content-model bullets are 83–85.]** Content model — `--split` and `--no-fine-tree` (MVP-SPEC.md lines 83–85, 121); CLI surface (line 149)
- Discovered by: **Q21's lane** (2026-08-17), while writing §2.7 (sealer as adversary) against the shipped plan validation.
- Problem: **a decision marked RESOLVED whose ruled mechanism does not exist, and the seam it delegates to reports the obligation as discharged from both sides.** `docs/decisions/D24-no-fine-tree-split-conflict.md:3` reads *"Status: RESOLVED — hard CLI error (no silent single-unit override)"*, and `:20-28` is layer 1: if `--split blank-lines` is active and a **text** file selected for splitting also matches a `--no-fine-tree` glob, `seal` must abort during plan validation, before consent and before any derivation or payment, naming the file and both flags. Shipped, `crates/antseal-cli/src/seal_plan.rs:196-210` only records the exclusion per file, and the sole error raised in the whole function is the zero-match glob at `:215-218`; `base_flags` (`:408-417`) applies `with_split` to every file unconditionally. A file matching both flags therefore carries `with_split().with_no_fine_tree()` and is **silently forced single-unit** — the exact outcome D24's status line says was not chosen. Only D24's layer 2 (the model making the contradiction unrepresentable) ships. **Two core sources assert the missing half exists**: `crates/antseal-core/src/content/unit.rs:34` (*"The matching hard / CLI error (naming the file and both flags) is U's, per D24"*) and `crates/antseal-core/src/content/split.rs:63` — and the test suite encodes the silent override as expected, citing D24 while doing it (`crates/antseal-cli/tests/show_command.rs:110-118`, whose fourth fixture constructs exactly the combination D24 forbids).
- Do: Implement layer 1 in plan validation: for each **text** file (binary matches are explicitly not an error under D24 §1, because splitting never applies to them — G6), if the split mode is active and the file matches a `--no-fine-tree` glob, abort with a distinct usage-class error naming the file and both flags and suggesting the two workarounds D24 names. Then fix `show_command.rs:110-118` — its fixture is now unconstructible through the CLI, and its comment reads D24's §2 while ignoring §1.
- Accept:
  - `seal` aborts on the conflicting combination **before** the consent gate and before any derivation or payment, proven by a test that asserts zero backend calls as well as the exit.
  - The message names the file and both flags and offers a workaround, as D24 §1 requires.
  - Binary files matched by `--no-fine-tree` under `--split` remain **not** an error, with a test for that direction so the fix does not over-reach.
  - The two core comments are re-read after the fix and left true; the `show_command.rs` fixture is corrected rather than deleted, with the reason recorded.
- Notes: **The sharpest instance of this project's recurring class**: a doc comment describing a neighbour's obligation as fact. Two crates each point at the other, the CLI never rejects, and the suite locks in the behaviour the decision rejected — so three independent artifacts agreed on something none of them checked.

### U83 — After vault theft there is no route to the payment wallet, and the branch with no route is the default

- Milestone: M4
- Size: S
- Deps: after U10 (the wallet sub-key), U12/D47 (vault export/import), Q24 (the vault-theft page that documents the dead end)
- Spec: Vault — *"The Arbitrum wallet key lives inside the encrypted vault"* (MVP-SPEC.md line 143); Risks — vault theft (line 184)
- Discovered by: **Q24's page work and Q25's `--include-receipt` sweep** (2026-08-17), which measured the wallet affordances end to end; re-measured centrally at registration.
- Problem: **the wallet key enters the vault and never leaves, and `antseal init` with no flags is the branch that makes it unrecoverable.** The key has four inbound routes — `init --wallet import` with `--wallet-key-fd` (`crates/antseal-cli/src/cli.rs:190-200`, `:253-260`), the interactive paste at `init.rs:530-533`, a fresh generation at `init.rs:515-517` through `WalletKey::generate()` (`crates/antseal-net/src/wallet.rs:102-119`), and `vault import` reinstalling a prior export (`vault/export.rs:1104-1106`) — and **no outbound route at all**: none of the nine subcommands (`cli.rs:113`) exports, prints or rotates it, and the only reads are to sign a payment (`commands.rs:381`, `:387`) or to copy it into an export (`vault/export.rs:761`). The export is not a recovery path in the sense that matters: it is encrypted under the same passphrase, so the thief's copy is equally sufficient and it hands the victim no capability the attacker lacks. **The default is `generate`** (`cli.rs:193`, `default_value_t = WalletSource::Generate`) and a generated key is never displayed even once — `init.rs:539-541` keeps only the checksummed address and drops the key — so a user who types `antseal init` and nothing else owns funds they can never sweep from any other wallet. `docs/user/vault-theft.md:258-274` states all of this accurately and its only mitigation, *keep the balance small*, must be acted on **before** the theft by a reader of a post-theft page.
- Do: **Rule the disposition before building anything.** The arms are: (a) accept permanently and move the mitigation upstream — say it at `init` and in `funding-your-wallet.md`, where a user can still act on it, rather than only in the theft page; (b) show the generated key or its export path once at `init`, which trades a documented dead end for a new disclosure surface and must be weighed against project rule 6; (c) add an explicit maintainer-side wallet-key export behind consent, which makes the vault a single artifact holding both the sealing keys and a spendable key in retrievable form. Whichever arm is taken, the **prospective-only** nature of the mitigation must be stated where a pre-theft reader meets it.
- Accept:
  - The disposition is a recorded decision, not an implementation choice made in passing — this touches key custody and project rule 6.
  - If any outbound route is added, it is consent-gated, prints a fingerprint or a path rather than key bytes wherever that suffices, and no key material reaches a log, an error or a fixture.
  - The mitigation appears where it can still be acted on: `init`'s report and `docs/user/funding-your-wallet.md`, not only `docs/user/vault-theft.md`.
  - `docs/user/vault-theft.md:258-274` is re-read after the ruling and left true.
- Notes: **Not an undocumented gap — a documented dead end, which is why it needs a ruling rather than a fix.** The page is candid and every code claim in it checks out. **Correction to the brief:** the brief frames the key as entering *"at `init --wallet import`"*; measured, `--wallet` defaults to `generate`, so the *"if antseal generated it there is no route at all"* branch is the one a user reaches by typing nothing, and the row should be weighed against the default rather than against the import path.

### U84 — `init` tells keyfile users to back the keyfile up with a command that refuses their vault

- Milestone: M4
- Size: S
- Deps: after U8 (the keyfile factor), U12/D47 (vault export/import), U18/U78 (`init`'s report and its committed golden) — **ordered before U32**, because the false sentence is now inside a frozen surface
- Spec: Vault — *"Docs and CLI must nag on both failure modes"* (MVP-SPEC.md line 143); Risks — vault loss (line 184)
- Discovered by: **D148's lane** (2026-08-18), §4.3, while measuring which outbound wallet-key routes exist; locators re-verified centrally at the wave-25 registration, which also found the defect is **wider** than §4.3 framed it.
- Problem: **the advice is unactionable for exactly the users it is shown to, and a second, spec-mandated constant gives the same advice to everybody.** `placement_guidance` (`crates/antseal-cli/src/vault/keyfile.rs:286-290`) closes `init`'s keyfile branch with *"Back it up separately too — `antseal vault export` does NOT contain the keyfile, only the fact that one is required."* The sentence reads as *the export omits the keyfile*, and therefore as an instruction a keyfile user can follow. Measured, `vault export` does not omit it — it **refuses the whole vault**: `crates/antseal-cli/src/vault/export.rs:745-756` returns `CliError::Usage` for any `header_wrap != WRAP_MODE_NONE`, saying the v1 format *"cannot carry a second factor"* and *"Back up the vault directory and the keyfile separately by hand until the export format carries the wrap (nothing was written)"*. So the one command `init` names to a keyfile user is the one command that will not run for them. **The same claim is made a second time by a constant the spec mandates**: `LOSS_WARNING` (`crates/antseal-cli/src/vault/bookkeeping.rs:68-77`) says *"Run `antseal vault export` and keep the backup somewhere else"* unconditionally, and MVP-SPEC.md line 143 is why it is there — so the loss nag itself is false for every wrapped vault. **The guard cannot see any of this**: `the_placement_guidance_says_the_export_does_not_carry_it` (`keyfile.rs:404-414`) asserts `placement_guidance(...)` *contains the string* `"does NOT contain the keyfile"` — it checks that the wording is present, never that the claim is true, so it is green precisely because the sentence exists. **And U78 has now frozen it**: the sentence is committed verbatim in `crates/antseal-cli/tests/snapshots/init-report.txt`'s keyfile section, so the golden pins a false claim and a future reader meets it as ratified copy.
- Do: Decide which of the two is wrong — the copy or the refusal — and fix that one, in one act covering all three sites. If the refusal stands (it is D47's format limit, and the reasoning at `export.rs:749-753` is sound), then `placement_guidance` must say what a wrapped-vault user can actually do (`export.rs`'s own *"back up the vault directory and the keyfile separately by hand"*), and `LOSS_WARNING` must stop naming a command that refuses for a whole class of vaults — which is a **freeze event** for a spec-mandated constant and moves U78's golden in the same change. Replace the copy-contains-copy assertion with one that ties the claim to the behaviour: drive `vault export` against a wrapped vault and assert the guidance does not promise what the refusal denies.
- Accept:
  - No `init` output tells a wrapped-vault user to run a command that refuses their vault, proven by a test that exercises the refusal rather than the wording.
  - `LOSS_WARNING`'s disposition is recorded either way; if it changes, U78's golden is re-blessed in the same act and the diff is justified as a deliberate freeze event naming its authority.
  - `keyfile.rs:404-414` no longer asserts a string against itself; a planted change to the claim's *truth* (not its wording) is watched red.
  - `docs/user/vault-loss.md` and `docs/user/vault-theft.md` are re-read after the fix and left true.
- Notes: **The dominant defect class, arriving with a freeze on top of it.** Three artifacts agree — the copy, the test and now the golden — and none of them checks the thing that is false. This is the U78 lesson one turn worse: U78 froze the surface the spec mandates, which was the right act, and the surface it froze contains a claim the code refuses. That is not an argument against the freeze; it is what the freeze is for, and this row is the freeze doing its job.

### U85 — the resume hint prints a command `seal` now refuses

- Milestone: M4
- Size: S
- Deps: after U19/U20 (`list`'s resume hint), U82/D149 (the new `--split` × `--no-fine-tree` abort), D45 (the exact-flag resume identity)
- Spec: CLI surface (MVP-SPEC.md line 149); Content model — `--split` and `--no-fine-tree` (lines 83–85)
- Discovered by: **U82's lane** (2026-08-18), as the visible face of D149 §6's unresumable-legacy case; path and line range corrected at the wave-25 registration — the file is `crates/antseal-cli/src/listing.rs`, **not** `src/pipeline/listing.rs` as first reported.
- Problem: **the hint that exists to tell a user how to finish a paid, incomplete seal can now print a command that aborts before doing anything.** `seal_invocation` (`crates/antseal-cli/src/listing.rs:1128-1138`) reproduces the stored `SealShapingFlags` verbatim — `split_blank_lines` becomes `--split blank-lines`, each `no_fine_tree` glob becomes `--no-fine-tree <glob>` — because D45 makes the resume identity the *exact* flag set and there is no `--resume` command: re-running `seal` with the same flags **is** the resume. U82 has now made that flag pair a hard refusal in `build_plan` for any non-empty text file the glob matches (`invalid-seal-argument`, exit 27). A work sealed before this wave whose file list contains such a file is therefore **permanently unfinishable through the documented route**, and the route tells the user to do the thing that fails. D149 §6 accepted the unresumability deliberately (zero extent pre-release, no migration); what it did not name is that the product still *prints the failing command*, so the user meets a refusal rather than an explanation.
- Do: Make `list`'s hint honest for the flag pair. Either suppress the hint for a stored shaping U82's predicate would refuse and print the reason and the workaround instead (D24's own two: seal it as a separate work without `--split`, or drop one flag), or keep printing it and precede it with the refusal it will hit. Whichever way, the hint must not be the only thing the user sees. Then decide whether the hint's own committed golden should carry a case in the refused shape at all.
- Accept:
  - A stored shaping that `build_plan` would refuse does not yield a bare hint; the output names the refusal and a workaround, proven by a test that stores such a work and reads what `list` prints.
  - The honest and the ordinary paths are distinguished by a test each, and a planted regression that reverts to the bare hint is watched red.
  - `seal_invocation`'s unit golden is re-read after the change; if it keeps a refused-shape fixture, the reason is recorded at the site.
- Notes: **The pre-existing fixture is production-unreachable and that is worth saying, because it is why nothing went red.** `the_resume_hint_reproduces_every_identity_flag` (`listing.rs:1758`) pins the exact string `antseal seal 'chapter one.txt' notes.md --title 'my thesis' --split blank-lines --no-fine-tree '*.png' --no-anchor --network devnet` — a shaping in which the glob matches **none** of the listed files, which `build_plan` has refused as a zero-match glob since long before this wave and which therefore cannot describe any work that was ever created. So the committed test asserts a command the product already refused, for a *different* reason, and could not have caught this. The production case is the opposite shape: a glob that *did* match a text file, which is exactly what makes the work exist and what U82 now refuses.
